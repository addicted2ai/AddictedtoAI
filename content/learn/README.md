# content/learn/

Static education pages — the durable ladder (`specs/education-static`). One
file per page, front matter declaring `level`, `prerequisites` and `outcome`,
from which the ladder index is generated. These pages carry no perishable
literals: prices, context windows and version numbers are transcluded from
the wiki data layer, never typed in.
