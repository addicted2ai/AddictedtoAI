# content/directory/tools/

Curated tool listings — one file per tool (`specs/directory`). Front matter
declares `url`, `pricing`, `last_verified` and a link to the wiki entry for
the tool (a stub is minted if none exists). Freshness data drives the
could-not-verify and discontinued markers; a listing whose URL stops
resolving is marked, never silently dropped.
