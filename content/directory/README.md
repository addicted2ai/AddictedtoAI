# content/directory/

Curated directory listings (`specs/directory`). The model catalog and the
three standing tables are generated from Pulse data, not stored here; this
tree holds only what a human-equivalent editor picked. Today that is
`tools/`.
