# content/

Every authored file on the site: Markdown with YAML front matter, one file
per published thing. Front matter is validated at build time and a violation
fails the build naming the file and the field (task 2.1, `specs/wiki`).
Nothing here is generated — generated data lives in `data/derived/`.
