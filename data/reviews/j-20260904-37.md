---
job: j-20260904-37
verdict: approve
reasons: []
would-cite: >-
  Nobody links a drop record for pleasure, but the next sweep author does —
  this is the file they open to find out which lines a previous pass already
  examined and cleared, and a quote in the wrong word order would send them
  looking for a construction that is not on the page. Its value is entirely
  as an auditable record of coverage, and after this change both entries can
  actually be checked against the pages they name.
---

Free-form notes.

## What the fix claims, and the check for each half

The diff rewrites two bullets in `data/drops/unit-doubling-sweep-j-20260903-07.md`
and deletes the carry file that put the item in the queue. Two claims are
checkable: the word order of the quotes, and the values inside them.

**Word order — read the pages directly.**

`content/wiki/model/cohere-command-a.md` lines 70–72:

```
publishes at {{fact:model/cohere-command-a#parameters}}, listed
{{fact:model/cohere-command-a#listed_date}} with a context window of
{{fact:model/cohere-command-a#context_window}} — a
```

`content/wiki/model/meta-muse-glimmer-30b.md` lines 96–98:

```
also carries an OpenRouter price, {{fact:model/meta-muse-glimmer-30b#price_input}}
input against {{fact:model/meta-muse-glimmer-30b#price_output}} output over
a context window of {{fact:model/meta-muse-glimmer-30b#context_window}}. The
```

The transclusion follows "context window of" in both, so the new quotes
("with a context window of 256000 tokens — a", "over a context window of
131072 tokens. The") and the new line references (`:71-72`, `:97-98`) are
what the files say. The replaced "adjective slot" reasoning described the
opposite construction; it is gone, and the substituted reason — em dash after
the unit in the first, full stop in the second — is directly visible above.
`grep -rn "adjective slot" data/ content/` now returns only
`data/derived/queue.json`, which is the generated queue the Pulse recomputes,
not a corpus file the job should have hand-edited.

**Values — read the snapshot the facts bind to.**

Both pages bind `context_window` to `openrouter-models` `path: context_length`
(front matter, cohere lines 23–27 / meta lines 14–16, row ids `cohere/command-a`
and `meta/muse-glimmer-30b`). A short script over
`data/sources/openrouter-models/latest.json` printed:

```
cohere/command-a => 256000
meta/muse-glimmer-30b => 131072
```

**Unit — the rendered suffix.** `lib/facts.test.mjs:227` ("2.3 THE CONTROL: a
non-price feed value is not rescaled and keeps its own unit") asserts a
`context_window` fact renders as
`<span class="fact-value">300000</span><span class="fact-unit">tokens</span>`.
So "256000 tokens" and "131072 tokens" are the rendered forms, and the
verdict the bullets carry (not a doubling — punctuation follows the unit, not
a literal) holds.

I did not re-run `npm test` or `npm run build`; the loop recorded both PASS on
`9f1cdf3ffb8b`, the commit this diff was computed from.

## Scope

The work commit `9f1cdf3` touches exactly two paths — the drop record and the
deleted `data/carried/j-20260904-35-carry-1.md`. The carry-file deletion is
what the brief names as the retirement mechanism, so it is inside the outcome,
not beyond it. `.job/brief.md` and `.job/source.json` are loop scaffolding from
the brief commit and are removed at merge. `git status --short` in the worktree
is clean. Nothing else moved.

Nothing to carry, and no proposal — this was a two-bullet correction and it
surfaced nothing beyond itself.
