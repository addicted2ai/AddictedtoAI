---
job: j-20260906-05
verdict: approve
reasons: []
would-cite: >-
  Anyone arguing that a "most capable in its weight class" headline is a
  boundary drawn after the scores were in would paste this: it puts the
  vendor's own comparison table back against the vendor's own claim and shows
  five of the eight rivals ahead of Laguna S 2.1 on the first benchmark in it,
  under a methodology note that scores every competitor at its best published
  number. Second audience, narrower and more durable: someone tracking the
  drift of "open weights" from Apache-2.0 to model-specific licences, who
  needs the controlled case rather than an argument — XS.2 and XS 2.1, same
  architecture by the vendor's own sentence and the same 33,442,617,088
  parameters by the Hugging Face API, with the licence the only thing that
  moved.
proposal:
  slug: the-revision-pass-mishandles-its-review-inputs-and-outputs
  type: machinery
  date: 2026-09-06
  summary: >-
    Two measured defects in how the loop handles a job's SECOND review pass,
    both in the same code path and both silent. (1) The delta-review brief
    embeds a stale diff: it rendered content/wiki/org/poolside.md as a new file
    at blob 416c5ba — the blob at the AUTHOR commit c568829 — while naming
    779d210 as the commit it was computed from, where that file is 07ec802.
    The embedded diff therefore still contained the exact two strings the
    revision had already removed, and omitted the data/derived changes present
    at head. A delta reviewer judging its diff faithfully re-files two fixed
    findings, and because a second non-approval discards the job, that destroys
    correct work. (2) Carried findings from pass 1 are never transcribed: the
    transcription reads verdictPath(ctx, jobId, result.pass ?? 1) for the
    CURRENT pass only, and it runs at terminal outcome, which a `revise` is
    not — so every carry a pass-1 reviewer files on a job that goes to revision
    is read by nothing, ever. The destination filename is also indexed per job
    rather than per pass (`${jobId}-carry-${i+1}.md`) and collides silently,
    skipping rather than overwriting. The proposed job fixes both — red before,
    green after — and either half is worth doing alone.
  evidence: >-
    Measured 2026-09-06 in the review worktree and the main checkout. For (1):
    `git rev-parse "c568829:content/wiki/org/poolside.md"` prints 416c5ba and
    `git rev-parse "779d210:content/wiki/org/poolside.md"` prints 07ec802; the
    brief's hunk header reads `index 0000000..416c5ba` and its body contains
    "spent three years arguing" and "Six days before that Platform post", the
    two strings revision commit 922a0aa removed and the same two findings the
    brief lists as this pass's scope. `git diff --stat c568829 779d210` shows
    four files changed; the brief's diff shows one. For (2): loop/run.mjs:1301
    passes `result.pass ?? 1` and loop/lib/carry.mjs:102 builds
    `${jobId}-carry-${i+1}.md`, skipping on collision at :104. Reproduced on a
    prior job — j-20260903-14 carries a `carry:` block in BOTH its pass-1 and
    pass-2 records, and data/carried/ holds exactly one file for it, whose
    `title` is the PASS-2 entry ("Make the \"fetched twice\" note match the
    transcript it annotates") while its own Origin paragraph misattributes it
    to `j-20260903-14.md`, the pass-1 filename. Pass 1's carry for that job
    ("Note Meta's own 1.3 roadmap line in the org entry's prose body") appears
    nowhere in data/carried/. This job would have lost three the same way; they
    are re-filed by hand in this record's carry: block, which is not a fix.
carry:
  - title: '"Not one of the eight rivals is in that class" overreaches — three publish no size'
    detail: >-
      The body asserts "Not one of the eight rivals in its own comparison table
      is in that class". The entry's own transcluded `comparison_table_sizes`,
      rendered in the next breath, says five rivals publish a size (Tencent Hy3
      295B-A21B, Nemotron 3 Ultra 550B-A55B, Inkling 975B-A41B, DeepSeek-V4-Pro
      Max 1.6T-A49B, Kimi K3 2.8T-A50B) "and Qwen 3.7 Max, Muse Spark 1.1 and
      Claude Fable 5 carrying an em dash where a size would go". For those three
      the size is undisclosed, not known-to-be-larger, so the sentence claims
      more than the table shows. "Not one of the five that publishes a size is
      in that class, and three publish none" is exact and no weaker. Carried
      rather than blocked because the transclusion immediately below discloses
      the em dashes, so the reader can see the gap. Re-filed from the pass-1
      record, confirmed here against the entry's own front matter.
    subject: content/wiki/org/poolside.md
  - title: Restore the vendor's hedge to the weight-class quote
    detail: >-
      `weight_class_claim` quotes "the most capable agentic coding model in its
      weight class by a wide margin" and labels it "the vendor's own bolded
      comparison", which is honest and not a misquotation. But the served
      sentence is "Laguna S 2.1 is, as far as we can measure, <strong>the most
      capable agentic coding model in its weight class by a wide margin</strong>."
      — confirmed by fetching https://poolside.ai/blog/introducing-laguna-s-2-1
      on 2026-09-06 (HTTP 200, 465,772 bytes) and finding "as far as we can
      measure" twice, once in the rendered HTML and once in the Sanity block
      payload as a span reading "Laguna S 2.1 is, as far as we can measure, ".
      Dropping that clause makes Poolside sound more absolute than it was, in a
      paragraph whose whole argument is about the vendor's hedging. Adding it to
      the fact value costs nothing. Re-filed from the pass-1 record, confirmed
      here by my own fetch.
    subject: content/wiki/org/poolside.md
  - title: The proposal's match count for the real Poolside phrase is 2, not 6
    detail: >-
      The proposal's `evidence` says the three absent fragments run "against six
      matches for the phrase Poolside did write, 'the most capable agentic coding
      model in its weight class by a wide margin'". Fetched and counted on
      2026-09-06 (HTTP 200, 465,772 bytes), that phrase occurs exactly twice —
      once in the rendered HTML, once in the Sanity payload — not six times. The
      three absences the proposal actually turns on ("West", "most capable open",
      "open-weight model") are not affected and the finding stands; only the
      contrast figure is wrong. Change "six" to "two". Re-filed from the pass-1
      record, confirmed here by my own count.
    subject: data/proposals/a-feed-row-quotes-a-vendor-sentence-the-vendor-never-wrote.md
  - title: Say "repository" on the two bare parenthetical dates in the licence paragraph
    detail: >-
      The licence paragraph establishes a repository-creation frame ("Laguna
      XS.2's repository went up on 23 April 2026 ... Laguna M.1's on 15 June")
      and then drops the noun for the next two: "Laguna XS 2.1 (20 June) is
      openmdw-1.1 and Laguna S 2.1 (13 July) is ...". Both parentheticals are
      Hugging Face createdAt dates and both are correct, but a reader landing
      mid-paragraph reads them as release dates, and the announcements were
      2 July and 21 July — dates the entry's own timeline carries three rows
      apart from these. Not a false claim; a dropped qualifier on a number the
      entry states twice under two different meanings. "XS 2.1's repository
      (20 June)" would close it.
    subject: content/wiki/org/poolside.md
subject: "content/wiki/org/poolside.md"
reviewed:
  "content/wiki/org/poolside.md": "a01b4f28d02ee6f289ac636182f59c856d5c140f0684fa5ac73fd2ab849b974e"
---

Delta review, pass 2. The previous verdict was `revise` on
`false-or-unsupported-claim` with two named defects, both in the opening
paragraph, both one line. This pass reviews only what changed since that
verdict.

## The diff I was given was stale, and I judged the branch instead

Reported first because it changes what this record means. The brief states the
diff was computed from `779d210a4fa5`. It was not:

```
$ git rev-parse --short=7 "c568829:content/wiki/org/poolside.md"
416c5ba
$ git rev-parse --short=7 "779d210:content/wiki/org/poolside.md"
07ec802
```

The brief's hunk header is `index 0000000..416c5ba` — the blob at the **author**
commit `c568829`, before the revision. Its body still contains "spent three
years arguing" and "Six days before that Platform post", which are precisely
the two strings the previous pass asked to be removed. `git diff --stat
c568829 779d210` reports four files changed, and the brief's diff shows one.

So the diff under review, read literally, would have justified re-filing both
findings verbatim and discarding the job with every one of its 44 minutes lost.
I read the branch instead. Everything below is measured against the working
tree and the committed blobs at `779d210`, which is the commit the loop names
and the commit it ran the gates on.

## Finding 1 — "three years": repaired by removal

The lede now reads:

```
A row on a public router is the thing Poolside's public record argued
against.
```

The unsupported duration is gone from the file, not merely from the sentence.
A grep across the whole corpus returns two unrelated hits and nothing in this
entry:

```
$ grep -n "three years|Six days|Seven days|spent three" content/
content\wiki\concept\chain-of-thought.md:76:three years old.
content\wiki\event\attention-is-all-you-need.md:107:diagram of a variant that was superseded within three years.
content\wiki\org\poolside.md:209:sold on the premise that {{fact:org/poolside#platform_pitch}}. Seven days
```

What survives is the claim's shape, which the previous pass measured and found
well evidenced, and which now rests entirely on the enumeration in the same
sentence — the AWS first-party partnership of December 2024, the Redpanda
tie-up, the Fern Labs acquisition, the Dell configuration, and the Platform
post whose own cited pitch (`platform_pitch`, quoted verbatim in the front
matter) is an argument for not sending data outside a security boundary. Every
element of that list is a `cited` fact or a body link in this diff with a
source. The sentence now asserts no interval, no founding date and no number a
reader cannot check against the rows immediately below it. The author chose
removal over sourcing a founding date; sourcing one would have meant a new fact
and a new fetch, which is wider than the finding.

## Finding 2 — "six days": repaired to seven, and seven is right

```
sold on the premise that {{fact:org/poolside#platform_pitch}}. Seven days
before that Platform post, on 28 April 2026, the same blog said
```

Arithmetic run rather than eyeballed:

```
$ node -e "const a=new Date('2026-04-28T00:00:00Z'),b=new Date('2026-05-05T00:00:00Z');console.log('days',(b-a)/86400000)"
days 7
```

`2026-05-05` is the date this entry's own timeline carries for the Platform
post, so prose and front matter now agree, which is what the finding was about.

The revision commit `922a0aa` is exactly these three lines
(`1 file changed, 3 insertions(+), 3 deletions(-)`) and touches nothing else.
No new claim entered the piece, so there is no new source the revision obliged
me to fetch.

## The rest of the delta: the derived tree

`779d210` adds the mechanical rebuild output that `c568829` did not carry —
`data/derived/aliases.json`, `backlinks.json`, `search-index.json`. I did not
re-run the suite; the loop's record says `npm test` and `npm run build` passed
on this commit. I ran the check that would be red if the derived tree were
hand-written or out of step with the corpus:

```
$ git status --porcelain
(no output)
```

Clean at `779d210` after the loop's build — the recomputed tree is
byte-identical to the committed one, which is the guarantee `data/derived/` is
supposed to carry.

The content of the delta is consistent with the entry. `aliases.json` gains one
record: `"name": "Poolside", "linkable": true, claimed_by: [{ org/poolside,
exclusive }]` — a sole exclusive claimant, which is the condition
`lib/aliases.mjs` sets `linkable` on, so the capitalised name links and nothing
else claims it.

One thing in `search-index.json` looked wrong and was not, so I am recording the
check rather than the suspicion. The new doc carries `"b": false` where the
neighbouring org entry carries `"b": true`, and on a page with 26 facts and 10
timeline events an index-related `false` would be a spec violation against the
wiki tiering requirement. `b` is not indexability:

```
lib/search-index.mjs:54:        b: isEntry ? Boolean(doc.index?.stub) : false,
```

It is the stub flag, carried so the results list can label stubs. `false` is
correct for an entry with a prose body, and the neighbour's `true` means that
neighbour is a stub.

## Why this record carries four findings, three of which are not mine

Pass 1 filed three carried findings. I checked whether they had reached work
and found they had not, and will not:

- `loop/run.mjs:1301` transcribes from `verdictPath(ctx, jobId, result.pass ?? 1)`
  — the current pass's record only — and the block runs at terminal outcome,
  which a `revise` is not.
- `loop/lib/carry.mjs:102` names the destination `${jobId}-carry-${i+1}.md`,
  indexed per job rather than per pass, and on collision at :104 it skips
  rather than overwrites.
- `ls data/carried/ | grep j-20260906-05` returns nothing, so none of pass 1's
  three exist.
- Reproduced on a completed job: `j-20260903-14` has a `carry:` block in both
  its pass-1 and pass-2 records, and `data/carried/` holds exactly one file for
  it. That file's `title` is the **pass-2** entry, while its own Origin
  paragraph names `j-20260903-14.md` — the pass-1 filename. Pass 1's entry for
  that job, "Note Meta's own 1.3 roadmap line in the org entry's prose body",
  appears nowhere in `data/carried/`.

So pass 1's three findings were about to die with a record that had done its
job. I re-filed them above rather than relay them, confirming each on my own
measurement first — the em-dash overreach against the entry's own front matter,
and the other two against a fresh fetch:

```
$ node <tmp>/poolside-check.mjs
HTTP 200 bytes 465772
claim phrase occurrences: 2
"as far as we can measure": 2
--- context ---
"...<p>...Laguna S 2.1 is, as far as we can measure, ...<strong>...the most capable agentic coding mode"
--- context ---
"...text:\"Laguna S 2.1 is, as far as we can measure, \"},{...marks:[\"strong\"],text:\"the most capable agentic coding model in its weight class by a wide margin\"},{...text:\".\"}..."
```

Two occurrences of the claim phrase, not the six the proposal's evidence
asserts; and the hedge is real, appearing in both the rendered HTML and the
Sanity block payload. My byte count (465,772) differs slightly from pass 1's
465,592 — the page is dynamically assembled and drifts — which changes neither
count. Re-filing by hand is a patch for this job, not a fix; the fix is the
proposal.

## Verdict

`approve`. Both required changes are made, both are correct, and the revision
introduced nothing else — no new claim, no new source, no widening. The
previous pass measured roughly two dozen distinct claims in this entry and
found every one held; nothing in this delta disturbs any of them, and the
piece's argument no longer opens with the two numbers that undercut it. The
four carried items are qualifiers and a count in a proposal's evidence, none of
them a defect I am approving over.
