---
job: j-20260905-18
verdict: approve
reasons: []
would-cite: >-
  The next person told to "add a member to the closed list" in this repository,
  as the argument for checking that the list exists before adding to it. This
  draft was sent to add `lead-change` to `changes.jsonl`'s closed kind list,
  went looking for the list, found instead a constant carrying the right
  comment, the wrong values and no importer, and wrote the measurement into the
  change — 182 lines, five kinds, and three of the decoy's five values
  appearing as a `kind` on zero of them. That is the citable move: a
  directive's own premise measured against the tree before it was implemented,
  and the correction filed where an implementer will hit it rather than in a
  report nobody re-reads.
carry:
  - title: Say why RD-005's first branch had to be replaced, not just that it was
    detail: >-
      The wiki delta replaces RD-005's own first attribution branch — "one the
      org entry records citing itself from" (`loops/ui-loop/RULES.md:638-639`)
      — with a declared `publishes_from`, and states the field "SHALL NOT be
      inferred from the entry's own cited source URLs", but never says what
      goes wrong if it is. This tree answers that, and the answer is the
      change's own motivating defect: all 13 `founded` facts on
      `content/wiki/org/` cite `en.wikipedia.org` (measured 2026-09-05), so
      every one of those org entries "records citing itself from"
      `wikipedia.org` — and RD-005's derived branch, run against this corpus,
      would attribute a Wikipedia-sourced claim to Anthropic as Anthropic's own
      words. That is precisely the defect ledger rows 2 and 4 recorded, arriving
      through the repair. Adding those two sentences to uncertainty item 1
      would put the strongest available argument in front of the ui-loop
      session that is being asked to re-decide `publishes_from` versus
      `aliases`, where at present the item argues only from `aliases[].name`
      being classified as a name.
    subject: openspec/changes/separate-a-claim-from-a-fact/specs/wiki/spec.md
  - title: Qualify "material field names" — two of the three are categories, not field values
    detail: >-
      The proposal says of `MATERIAL_KINDS`: "`price`, `context` and `status`
      are material **field** names — they travel on a line's `field`". Parsing
      all 182 lines, the `field` values actually carried are `price_input` (8),
      `price_output` (8), `status` (6) and `context_window` (1). Only `status`
      travels verbatim; `price` and `context` are the spec's and registry's
      names for the material-field *categories* (`data/sources/registry.json`
      declares `price_input`, `price_output`, `context_window`). The
      load-bearing half of the sentence is exactly right and I confirmed it —
      all three appear as a `kind` on zero of the 182 lines — so this is a
      dropped qualifier, not a wrong claim. "are the material field categories,
      carried on a line's `field` as `price_input`, `price_output` and
      `context_window`" would fix it.
    subject: openspec/changes/separate-a-claim-from-a-fact/proposal.md
---

An OpenSpec change draft: `proposal.md`, `tasks.md`, and three deltas
(`wiki` +3 requirements, `pulse` +3, `review` +1). No content, no code, no
machinery. The whole diff is claims about this tree and about five ui-loop
knowledge documents, which is what I checked.

## What I ran, and what it printed

I did **not** re-run `npm test` or `npm run build` — the loop's record above
says they passed on `1367e5b3045b`. I ran the checks that would be red if a
specific claim in the diff were false.

**The gate the diff claims it passed (tasks.md task 27, the only ticked box).**

```
$ openspec validate separate-a-claim-from-a-fact --type change --strict --no-interactive
Change 'separate-a-claim-from-a-fact' is valid

$ node scripts/check-spec-deltas.mjs --strict
prebuild: spec-deltas — 3 unarchived change(s), 7 delta file(s), 11 live spec(s),
STRICT: 0 error(s), 13 warning(s)
```

Zero errors. The warnings are `stale-id` notices that the delta names
identifiers not yet in the tree (`source_host`, `publishes_from`,
`frontier.json`) — expected for an unimplemented change, and the sibling
`tag-the-corpus-by-domain` produces the same shape.

**The corpus measurements in the proposal's "Why".** Parsed
`content/wiki/org/*.md` front matter directly:

```
org files: 16
org entries with >=1 cited fact: 16
total cited facts on org entries: 92
org entries whose FIRST cited fact is `founded`: 13
  exceptions: alibaba-cloud → model_family, inception-labs → product,
              meta-superintelligence-labs → flagship_weights
hosts of `founded` cited facts: { 'en.wikipedia.org': 13 }
mistral-ai has founders fact: true
```

Every figure and every named exception matches the draft exactly, including
the sharpest claim in the change — that all thirteen are Wikipedia-sourced,
so the vendor test blanks all thirteen on the source alone.

**The closed-kind-list measurement**, parsing all 182 lines:

```
changes.jsonl lines: 182
kinds: { release: 60, retirement: 14, arrival: 77, field_change: 23, annotation: 8 }
field values: { price_input: 8, price_output: 8, status: 6, context_window: 1 }
```

Kind counts match line for line. `MATERIAL_KINDS` is at `lib/changes.mjs:35`
with the quoted comment and the five quoted values; a repository-wide Grep
returns the declaration plus only documentation, proposals, review records and
`data/carried/j-20260905-04-carry-1.md` — **no importer anywhere**. The
`review-frontier.md:137` and `frontier-plan.md:610` citations both say what the
draft says they say. So the change's central finding — the "closed list" the
directive told it to add to does not exist, and the thing that looks like it is
a decoy — is confirmed. (One dropped qualifier in how that sentence describes
the decoy's values: carried above, not blocking.)

**The `pulse` delta's snapshot claim**, replayed from the committed blobs via
`execFileSync` (not `git show` through Git Bash, per the CLAUDE.md MSYS note):

```
### 2026-09-03 (…T06:00:03.947Z, 424) -> 2026-09-04 (…T06:00:03.738Z, 427)
  qwen/qwen3.8-max intelligence_index 58.1 -> 53.4 | coding_index 71.8 -> 68.9
                   | agentic_index 58.4 -> 49.9
  rows whose indices moved: 1 (values up 0, down 3)
```

Exactly one row, every value down, all six numbers as written. The "a leader
can lose the lead without anything shipping" requirement rests on a real
measurement. (The 2026-09-04 → 2026-09-05 pair, run for context, reproduces
the AA rebase the sibling change recorded: agentic index count 166 → 99.)

**The registry claims.** Both sources carry `robots` (with `checked_on`) and
`verification` (with a fetch date and result); neither carries any field about
republication — the one `republi` substring in the file is "republish their
price on a timetable" in a `price_input` note, unrelated. The three
Artificial Analysis index paths **are** in `openrouter-models.yields` today,
and are in no `material_fields` entry; `benchmarks.design_arena[]` is in
neither. So the draft's correction of `frontier-plan.md` §0.1 ("recorded as
absent from `yields` on 2026-09-04; that is no longer true … the half that
still holds") is right in both halves — §0.1 does say "Neither key appears in
the registry's `yields` list".

**The counts in uncertainty item 7.** DESK-ORDER-001.md:104-105 says verbatim
"21 cited facts exist across 446 model entries today". Measured: 446 model
entries; **21 model entries carry at least one cited fact**; 86 cited facts on
model entries; 448 cited facts across 89 entries corpus-wide; 544 wiki entry
files. All four of the draft's numbers are exact, and the files-read-as-fields
diagnosis is right.

**Every line pointer.** I printed all 21 cited lines. `lib/review-hash.mjs:71`
= `MECHANICAL_FRONT_MATTER_KEYS = Object.freeze(['timeline'])` and `:99-102` is
the by-name filter; `lib/schema.mjs` 66/209/420/492/502/663 are
`VOLATILITY_DAYS`, `entrySchema`, `SCHEMAS`, the `aliases[].name`
classification, the `facts[].value` classification and
`assertFieldsClassified`; `lib/corpus.mjs:30-49` is `urlFor` and it does
`throw new Error('no URL rule for content type …')` on default, so task 5/6's
carve-out warning is a measured hazard, not a guess; `lib/reviews.mjs:280` is
`reviewablePieces` and the comment above it does say the order is part of the
join; `lib/changes.mjs` 52-54, 171-182, 196, 202 land on the malformed-line
comment, `describeChange` and the two `annotation` tests;
`pulse/lib/diff.mjs` 209/232/250/301 are the four kind literals, 334 is
`appendChanges`, 397 is `if (!l || l.kind !== 'field_change' || l.seeded)`
— so task 21's "true today by accident of that filter's shape" is accurate;
`pulse/lib/queue.mjs:323` and `pulse/lib/indexnow.mjs:101-102` likewise.
`lib/tool-categories.mjs`, `lib/currency.mjs`,
`data/carried/j-20260905-04-carry-1.md` and the schema helpers `entryId`,
`httpUrl`, `isoDate`, `FIELD_NAME_RE` all exist as named.

**The ui-loop sources.** `implementer-ledger.md` rows 2 and 4 are the two
independent `semantic-mislabel` renders of founding dates/founders under
"claimed · unverified"; row 10 is the field-name-test-without-a-source-test
finding citing RT FM-N3, and names `observed_throughput_p50` and
`output_tokens_per_task`/`cost_per_task` exactly as the delta does.
`SPEC-REVIEW-GUIDE.md` §4 row 47's "The single most repeated defect of the run"
is quoted verbatim. `RULES.md` round-4 addendum (598-599) carries the RD-004
sentence the proposal quotes, word for word. The round-5 addendum (627-646)
carries the registrable-domain rule and all four worked cases the delta and
task 14 reuse — `www.tencent.com`, `deepmind.google`, `blog.google` as a
different registrable domain from both, `google.attacker.example` — plus both
rejected tests (label-identity and `endsWith`) and the FM-N6 brand-domain gap.
`frontier-plan.md` §6.3 is titled "Seeding the eight days that exist" and
states the honest limits the tasks transcribe. Beads `addictedtoai-eb4l`,
`-ego8` (whose own text carries the "29 live model pages" figure) and `-c563`
all exist and are what they are called.

## On scope, and on the guide

The directive said "its own change, not folded into 9c9t", "draft it in full,
validate it, and STOP THERE", and "do NOT archive it". The diff is `.job/` plus
one unarchived change directory. Nothing is implemented, nothing is archived,
no reserved path is touched, and the change explicitly declines the migration,
the surface and the rights clearances. That is the stated outcome and not
more. I considered whether the index-registry requirement widens it, since the
directive names two artefacts; `addictedtoai-eb4l`'s own description ends
"Index columns render only when a registry index exists AND rights are
cleared", so it is inside the issue the directive told the job to reference,
and the draft flags the question itself as uncertainty item 5.

The one substantive divergence from the rubric is real and is declared, which
is why it is not a finding. `SPEC-REVIEW-GUIDE.md` row 51 and `RULES.md`'s
FM-N6 note both say brand domains go in `aliases`; the draft adds
`publishes_from` instead. It does not do this silently — it is item 1 of the
proposal's uncertainty list, ranked most likely to be wrong, quoting row 51
and naming three mechanical consequences, and saying "this is the single item
I would most want re-decided". The brief's instruction was to surface such
disagreements rather than pick silently, and the change is explicitly not
final until the ui-loop session reads it. What the item is missing is the
strongest argument available for its own position, which this tree supplies;
that is the first carried finding.

Two things I did not check, stated rather than implied: the "(2026-09-02)"
date attached to the single seeded lead change in uncertainty item 6 —
`frontier-plan.md` §6.3 says "one lead change" without a date, and computing
per-day leaders across nine snapshot blobs was more than the claim's weight in
a doubt paragraph justified; and the sentence-level quotations from
`DESK-ORDER-001.md` §4's K44 amendment beyond the two lines I grepped and read.

## Why approve

Forty-odd distinct checkable assertions, and every one I measured came back
exact — the corpus counts, the named exceptions, the hosts, the kind
histogram, six benchmark numbers, twenty-one line pointers, five external
documents, three beads issues, two gate invocations. The draft's own habit is
the reason: it does not restate the directive's premises, it measures them,
and the two places where the tree disagreed with an artifact — the closed kind
list that is not closed, and §0.1's `yields` claim that has since become false
— are written into the change as sections rather than quietly absorbed. The
uncertainty list is in `proposal.md` where the directive redirected it, is
ranked, and each item says what was chosen and what would change if the round
chose otherwise. Prose-wise it argues from mechanism throughout and reads like
someone who had to find these things out.
