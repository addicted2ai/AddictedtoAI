---
job: j-20260901-16
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that an open-weight retirement is a hosting decision rather
  than a model's death would link this entry: it is one of the few records that
  pins a vendor's own 404-on-31-August notice to a downloadable Modified MIT
  checkpoint, and now the sunset fact says exactly what the vendor page says and
  nothing more, so the argument can quote it without inheriting a clause the
  source never carried.
proposal:
  slug: review-worktrees-carry-no-node-modules
  type: machinery
  date: 2026-09-01
  summary: >-
    A review worktree is checked out without node_modules, so a reviewer cannot
    run any repository script — not measure-payload.mjs, not verify-design.mjs,
    not the tests — and the only checks available are git plumbing, file reads
    and network fetches. That silently narrows every review to what can be read
    rather than what can be measured, which is the opposite of this repo's
    stated rule. The machinery job would make the loop provision the review
    worktree so at least the read-only measurement scripts run there (a linked
    or copied node_modules, or a documented one-command bootstrap named in the
    review brief), so a reviewer can run the check that would be red if a claim
    in the diff were false.
  evidence: >-
    This diff re-records nine numbers under js_payload in data/launch.json.
    Attempting the direct check — npm --prefix <review worktree> run build,
    2026-09-01 — failed at the prebuild with
    "Error [ERR_MODULE_NOT_FOUND]: Cannot find package 'fast-glob' imported from
    ...\\lib\\corpus.mjs", and the worktree has no out/ directory, so
    scripts/measure-payload.mjs could not be run against the branch at all
    (ENOENT on out/index.html). The measurement claim in this diff is therefore
    the one thing in it I could not verify.
carry:
  - title: Reword the K3 entry's "46 days ahead of that" — the listing is 46 days earlier, not later
    detail: >-
      content/wiki/model/moonshotai-kimi-k3.md lines 85-88 read "K2.5's platform
      record reads {{fact:model/moonshotai-kimi-k2-5#api_sunset}}. K3's
      OpenRouter listing date is 46 days ahead of that". With the transcluded
      value now the bare date 2026-08-31, the sentence renders as "reads
      2026-08-31. K3's OpenRouter listing date is 46 days ahead of that" — and
      K3's listed_date is 2026-07-16, which is 46 days BEFORE 2026-08-31
      (15 days left in July + 31 in August). "Ahead of" most naturally reads as
      later, so the sentence now points the interval the wrong way; the interval
      itself is right. "46 days before that" or "precedes it by 46 days" fixes
      it. Not introduced by this diff — the arithmetic anchor is the same date
      it always was — but the shortened value removed the framing that used to
      carry the sentence.
    subject: content/wiki/model/moonshotai-kimi-k3.md
  - title: Confirm the js_payload numbers this repair re-recorded, or move them to their own launch commit
    detail: >-
      Commit 212b982 changes nine numbers under js_payload in data/launch.json
      alongside the one-line front-matter repair. They are internally honest —
      measured_on is already 2026-09-01 and the method line is unchanged — but
      the decreases include /wiki/concept/ai-winter (inline 5.7 → 5.5,
      html 7.8 → 7.6), a page this diff cannot touch, so they are a re-measure
      of the whole current tree riding inside a content repair rather than a
      consequence of it. Re-running scripts/measure-payload.mjs on a fresh
      export and confirming the three rows would settle it; the repo's own habit
      elsewhere is a separate "launch: ..." commit for exactly this.
    subject: data/launch.json
subject: "content/wiki/model/moonshotai-kimi-k2-5.md"
reviewed:
  "content/wiki/model/moonshotai-kimi-k2-5.md": "06b7e6c96c5261e5b06ef7394ec2f03a7224bd41e4a504d39e9e18c96cc71f09"
---

Delta review, pass 2. The finding to answer was `false-or-unsupported-claim`:
pass 1 blocked because the diff bumped `api_sunset`'s `accessed:` to 2026-09-01
while the `value:` still ended "full platform sunset; closed to newly registered
users beforehand", a clause the cited page does not carry — so the fresher date
vouched for words the source never said.

**The diff in the brief is stale.** It shows the `value:` line as unchanged
context, but the branch has moved since:

```
$ git -C <worktree> log --oneline -3
212b982 job j-20260901-16 (repair): bring the api_sunset value to what the cited page carries
0dd225a job j-20260901-16 (repair): refresh the api_sunset accessed date to match the quoted page
09a656e job j-20260901-16: brief
```

I judged the tree, not the printed diff. `git diff --stat main...HEAD`:
`.job/brief.md`, `.job/source.json`,
`content/wiki/model/moonshotai-kimi-k2-5.md` (4 ±), the deleted
`data/carried/j-20260901-15-carry-1.md` (16 −), and `data/launch.json` (18 ±).
`git status --short` is empty.

## What the fact says now

`content/wiki/model/moonshotai-kimi-k2-5.md` lines 59-64:

```yaml
  - field: api_sunset
    source: cited
    value: "2026-08-31"
    source_url: "https://platform.kimi.ai/docs/models"
    accessed: "2026-09-01"
    volatility: dated
```

## What the cited page carries, fetched today

`fetch` with `user-agent: Mozilla/5.0`, no extractor — I searched the served
HTML. `status 200 bytes 258381`:

```
[officially retired] 88607,250400
   ...The <code>kimi-k2.5</code> and <code>moonshot-v1</code> series were
   officially retired on August 31, 2026. Calls to these models now return a
   404 (model not found) error. Please migrate to kimi-k3...
[officially discontinued] 94130,94578,94935,97230
   <span class="cursor-pointer">Deprecated Models</span></h2> <blockquote>
   <code>kimi-k2.5</code> was officially discontinued on <strong>August 31,
   2026</strong> and is no longer maintained or supported...
[August 31, 2026] 88629,94165,94613,250422
[Deprecated Models] 80719,94042,225776,252297
[newly registered] ABSENT
[full platform sunset] ABSENT
[sunset] ABSENT
```

The page, read on 2026-09-01, states 31 August 2026 as the date `kimi-k2.5`'s
API stopped answering, twice, in two independent notices. `2026-08-31` is
exactly that and nothing more. The two clauses pass 1 objected to are gone from
the value and remain absent from the page — the same result the pass-1 record
measured, reproduced independently here. The `accessed: 2026-09-01` bump is now
true of every character it vouches for. The finding is answered.

## The prose the value feeds

`api_sunset` is transcluded in two places, so a shortened value could have
broken a sentence. Both hold:

- `moonshotai-kimi-k2-5.md` line 86: "Moonshot's platform record reads
  {{fact:...#api_sunset}}, and the vendor's model list has now moved to the past
  tense — 'were officially retired on August 31, 2026...'". This was the exact
  collapse pass 1 warned about — if the value had become the retirement wording,
  the sentence would state the same thing twice. It did not; the value carries
  the date alone and the quote carries the past tense, which is the second of
  the two coherent shapes pass 1 named.
- `moonshotai-kimi-k3.md` line 86-87 still reads, but the interval now points
  the wrong way in plain English. Carried above, not blocked on: the number is
  right, the preposition is not, and it is not this diff's sentence.

## The queue item

`data/carried/j-20260901-15-carry-1.md` is deleted in the branch, which is what
the brief requires to retire the item. `data/derived/queue.json` still names the
finding — expected, since the queue is recomputed by the Pulse from tree state
and this branch has not run one.

## What I did not verify, and why I am saying so rather than blocking

`data/launch.json`'s nine payload numbers. I tried the direct check and it is
not available in a review worktree:

```
$ npm --prefix <worktree> run build
Error [ERR_MODULE_NOT_FOUND]: Cannot find package 'fast-glob' imported from
  ...\lib\corpus.mjs
$ node scripts/measure-payload.mjs <worktree>/out
ENOENT: no such file or directory, open '...\out\index.html'
```

No `node_modules`, no `out/`. That is the proposal above. The numbers are not
false on their face — `measured_on` is already 2026-09-01 and the `method`
string is untouched, so they are a same-day re-measurement recorded against the
correct date — and `scripts/measure-payload.mjs` only prints, so someone
transcribed them by hand from a run. I weighed `scope-violation` on them: they
are outside what a one-line front-matter repair needs, and one of the three
measured pages (`/wiki/concept/ai-winter`) cannot be affected by this change.
But they are an honest, correctly dated measurement of the tree being merged, in
the file whose whole purpose is to hold current measurements, and `git log` shows
a prior job commit (`6b0b29a`, a `post`) doing the same. Discarding a job whose
substantive fix is verified correct, on the last pass, over an honest
re-measurement riding along, would be the wrong trade. Carried instead.

I did not re-run `npm test` or `npm run build` for the branch's own sake — the
loop recorded both PASS, and in any case they cannot run here.
