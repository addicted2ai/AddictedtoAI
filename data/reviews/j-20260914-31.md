---
job: j-20260914-31
verdict: approve
reasons: []
would-cite: >-
  Someone deciding whether to build on GLM 4.5V through OpenRouter, reading the
  changed feed and asking whether the "active" badge means the model was
  reprieved. This line gives them the answer: no. The same expiration marker
  has now appeared and vanished three times in eleven days, so a return to
  active is not a sign the model is staying. It links back to the two earlier
  annotations so the pattern can be seen in one place.
---

Checked against the diff, not the author's account.

Scope: the only content change is one line appended at the end of
`data/changes.jsonl`. The diff has just that `+` line, no existing line is
edited, and its `annotates` key matches the change record on line 202
(`openrouter-models|c445d17afde865af|5881134797f7ecd5|z-ai/glm-4.5v|status`,
deprecated -> active, dated 2026-09-09). `.job/brief.md` and `.job/source.json`
are files the loop wrote. The annotation's `date` (2026-09-09) is the date of
the change it annotates, as the earlier annotations on lines 90 and 154 also do.

Claims measured. I ran a Node script calling `git show` through execFileSync
on the cited snapshots:

- `84a6649:data/sources/openrouter-models/previous.json` has
  fetched_at 2026-09-08T06:00:04.287Z, and the GLM row has
  `expiration_date: "2026-12-31"`, context_length 65536, prompt 0.0000006,
  completion 0.0000018.
- `84a6649:.../latest.json` has fetched_at 2026-09-09T06:00:04.950Z, and the GLM
  row has `expiration_date: null` with the same context length and pricing,
  including input_cache_read 0.00000011. So "both committed in 84a6649" is true
  (`git show --stat` lists previous.json and latest.json), and so is "pricing
  and context length identical".
- `HEAD:.../latest.json` has fetched_at 2026-09-14T12:31:37.751Z, and the GLM
  row has `expiration_date: null`. The "still null" claim holds.
- The registry rule: in `pulse/lib/diff.mjs:186-188`, `deriveStatus` returns
  `'active'` when there is no expiration. That matches "absent -> active".
- "Third flip since 2026-08-29": grepping `changes.jsonl` for this row's
  status key finds exactly three field_change records: 2026-08-29
  (deprecated->active), 2026-09-02 (active->deprecated) and 2026-09-09
  (deprecated->active). The two cited annotations exist on lines 90 and 154.

Interpretation: calling the marker unstable rather than a reprieve follows from
those three records, and does not overclaim. The number of flips is data, and
the meaning of the marker was sourced in the 2026-09-02 annotation. Neither
number in the text (dates and timestamps) lacks a source row.

Polarity: the brief asked for an annotation appended as a new line. The diff
does exactly that. There is no inversion.

Minor, not blocking: "it changes only the status badge" skips a small side
effect. Commit 84a6649 also added 3 lines to `content/wiki/model/z-ai-glm-4-5v.md`. `git show 84a6649 --`
on that file shows a generated timeline row: `- date: "2026-09-09" / event:
active / source_url: https://openrouter.ai/api/v1/models`. The 2026-08-29 annotation makes
the same claim, which the machinery makes true in practice, so I am not
carrying it.
