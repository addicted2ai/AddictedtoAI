---
job: j-20260904-17
verdict: revise
reasons:
  - false-or-unsupported-claim
  - broken-reference
would-cite: >-
  Someone arguing that Gemini 3.8 Flash's "same price, bigger bill" release is
  the clearest 2026 case of a vendor shipping a cost increase without touching
  the rate card would link this entry for the arithmetic — ~48k output tokens
  per task lifting cost per task to ~$0.58 while $0.75/$3.75 never moved. But
  they would link the paragraph above the benchmark list, not the benchmark
  list itself, which in this revision hands them a sentence that says the
  benchmark outperforms the models.
proposal:
  slug: flag-fact-values-that-duplicate-their-sentence
  type: machinery
  date: 2026-09-04
  summary: >-
    Add a prebuild check that compares each resolved `{{fact:…}}` value against
    the prose immediately surrounding its transclusion and warns when the two
    share a repeated word-shingle. A cited fact value in this corpus routinely
    carries its own framing ("~48k at high reasoning", "~$0.58 at high
    reasoning", "10.0%, against 8.8% for Gemini 3.7 Flash"), and the sentence
    that transcludes it just as routinely supplies the same framing, so the
    rendered page stutters. Nothing catches this today: the value and the prose
    are correct in isolation and only the join is wrong, so the build passes,
    the tests pass, and the defect reaches the reader. A warning (not an error —
    a legitimate repetition exists) naming file, field and the repeated phrase
    would put it in front of the author at authoring time instead of a reviewer
    two jobs later.
  evidence: >-
    One sentence in content/wiki/model/google-gemini-3-8-flash.md contained the
    defect twice. The reviewer of j-20260903-11 caught one half and carried it
    (data/carried/j-20260903-11-carry-1.md, "'at high reasoning' twice"); their
    quoted render truncated one word before the second half, so the carried
    finding under-counted its own defect. This job then fixed the half that was
    named, deleted the carry file, and left the other half live on two surfaces
    (the model entry and content/wiki/org/google-deepmind.md), where it still
    renders "cost per task to ~$0.58 at high reasoning at high reasoning". A
    mechanical shingle comparison would have flagged both instances at the same
    moment and would not have depended on a reviewer reading to the end of the
    sentence.
---

## Verdict

`revise`. Both carried findings are still standing after this diff, and both
carry files were deleted, so neither would return on the next Pulse run.

## What I checked, and how

### Scope — clean

`git show --stat 7899171` gives four files:

```
 content/wiki/model/google-gemini-3-8-flash.md |  6 +++---
 data/carried/j-20260903-11-carry-1.md         | 16 ----------------
 data/carried/j-20260903-11-carry-2.md         | 16 ----------------
 data/launch.json                              | 18 +++++++++---------
```

`data/launch.json` is the one that needed checking, since the brief names one
content file. `git log -p -6 -- data/launch.json` shows the payload figures
oscillating on every build (`inline_kb_gzipped` for `/catalog` walking
18.2 → 18.3 → 18.2 → 18 → 18.4 → 18.3 → 18.1 across six commits) and shows a
prior repair job carrying them in its own commit the same way — `0c74ab3
job j-20260903-22 (repair): drop the redundant prose 'per million tokens'…`
moved home from 6.5/10 to 6.3/9.6. Re-recorded measurement inside a repair
commit is established practice here, not scope creep. **No `scope-violation`.**

### How a cited fact actually renders

`lib/facts.mjs:249` (`case 'cited'`) emits `quantity(r.value)` followed by a
`fact-source` span — so the value text lands in the sentence verbatim, with
nothing inserted between it and the words on either side. That is the basis for
every rendered reading below; I did not re-run the build (the loop already did,
PASS on this commit).

### Finding 2 — the DeepSWE sentence still does not parse, and now inverts the claim

The diff took the first of the two options the finding offered — dropped
"and on", started a new sentence. The fact value was left whole, so
`content/wiki/model/google-gemini-3-8-flash.md:137-138` now renders:

> …Harvey's Legal Agent Benchmark at 10.0%, against 8.8% for Gemini 3.7 Flash.
> **DeepSWE v1.1 — outperforms most larger frontier models at a fraction of the
> cost.** The row keeps the family-standard…

As a standalone sentence the only available subject for "outperforms" is
"DeepSWE v1.1", so it reads as the benchmark outperforming the models. I
fetched the cited source to check what it actually says:

WebFetch of `https://deepmind.google/models/gemini/flash/`, the entry's own
`source_url` for this fact, returned:

> "On DeepSWE v1.1 (Long-Horizon Software Engineering) 3.8 Flash outperforms
> most larger frontier models in autonomously solving complex engineering
> problems end to end, only at a fraction of the cost."

The subject is 3.8 Flash. The old text ("and on DeepSWE v1.1 — outperforms…")
at least kept the benchmark in a prepositional phrase; the new text promotes it
to subject. This is worse than what was carried, not better — the finding's own
test was "does not parse", and the replacement does not parse either.

The finding's second option is the one that works: reduce the value to the
claim alone, the way `hle_verified` (`"54.9%"`) and `vals_finance_agent_v2`
(`"61.4%, against 59.0% for Gemini 3.7 Flash"`) are written —

```yaml
  - field: deepswe_v1_1
    value: "outperforms most larger frontier models at a fraction of the cost"
```

— and restore the prose to `…and on DeepSWE v1.1, 3.8 Flash {{fact:…}}.` Or, if
the new-sentence route is kept, name the model as the subject rather than
leaving the em dash to do it.

### Finding 1 — half-cleared; "at high reasoning" is still doubled on both surfaces

Trimming `output_tokens_per_task` to `"~48k"` is right, and I confirmed the
attribution argument the finding made. WebFetch of
`https://llm-releases.com/models/gemini-3-8-flash` returned:

> "a ~30% rise in average output tokens per task (to ~48k) lifts cost per task
> to ~$0.58 at high reasoning despite unchanged per-token pricing."

That is a second, independent reading agreeing with the one the carrying
reviewer wrote, so ~48k is the number and "at high reasoning" does attach to
the cost clause. (The llm-releases snapshot at
`data/sources/llm-releases/latest.json` carries only the summary description
for `guid` `bd089f31-…` — the 59/57/52 index figures and the $0.75/$3.75
pricing, no per-task figures — so the feed cannot adjudicate this and the page
is the only source. Noted rather than treated as a defect.)

But the finding named **two** duplications, and the second one survives. It
comes from `cost_per_task`, whose value is `"~$0.58 at high reasoning"`, and
from the prose that transcludes it. `google-gemini-3-8-flash.md:129-131`:

```
{{fact:…#output_tokens_per_task}}, lifting cost
per task to {{fact:…#cost_per_task}} at high
reasoning despite the unchanged per-token price.
```

renders as:

> …average output tokens per task rose to ~48k, lifting cost per task to
> **~$0.58 at high reasoning at high reasoning** despite the unchanged
> per-token price.

`content/wiki/org/google-deepmind.md:99-101` is the same construction and the
same stutter:

> …output tokens per task rose to ~48k, lifting cost per task to **~$0.58 at
> high reasoning at high reasoning** even though the per-token price never
> moved.

The carrying reviewer's quoted render stopped one word short of this — they
truncated at "…to ~$0.58 at high reasoning" — but their stated defect was "'at
high reasoning' twice", and after this diff it is still there twice, on both
pages they named. The fix removed the duplication of "average output tokens per
task" only.

Either move clears it: trim `cost_per_task` to `"~$0.58"` (the exact parallel of
the edit already made, and equally faithful to the source sentence, which puts
the effort level in the clause the prose already supplies), or delete the
prose's trailing "at high reasoning" in both files. Both surfaces are named in
the carried finding itself, so neither is a widening of this job.

### Required changes

1. Make the DeepSWE sentence parse with 3.8 Flash as the subject of
   "outperforms", per the source quoted above — preferably by reducing
   `deepswe_v1_1` to the claim alone and restoring a prepositional "on DeepSWE
   v1.1" in the prose.
2. Remove the surviving "at high reasoning" doubling in
   `content/wiki/model/google-gemini-3-8-flash.md` and
   `content/wiki/org/google-deepmind.md`.
3. Keep `data/carried/j-20260903-11-carry-1.md` deleted only if 2 is done; the
   brief's rule is that a finding left standing keeps its file.

### Not blocking

Re-running `npm test` / `npm run build` — the loop ran both on `789917107d01`,
PASS, and neither would have gone red for any of this: the value and the prose
are each well-formed, and only their join is wrong. That is the gap the
proposal above is about.
