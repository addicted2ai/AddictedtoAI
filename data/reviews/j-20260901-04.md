---
job: j-20260901-04
verdict: approve
reasons: []
would-cite: >-
  A reader working out whether OpenRouter's 2026-09-01 catalog pruning was a
  retreat from batch inference or a Mistral-specific cull would link this
  entry: it fixes "Mistral: Mistral Large 3 2512 (batch)" — the flagship's
  batch twin, at $0.50/$1.50 per million — to a dated, sourced delisting on a
  day the live catalog still served 65 other `:batch` rows and the non-batch
  `mistralai/mistral-large-2512` unchanged. It is the row that makes the
  Mistral-cohort reading checkable rather than plausible, and the one an
  argument about batch pricing withdrawal has to explain away.
carry:
  - title: Restore the trailing newline on the retired entry file
    detail: >-
      The edited file ends `---` with no final newline (the diff prints
      "\ No newline at end of file"), unlike the three entries merged in the
      same repair shape — content/wiki/model/mistralai-codestral-2508-batch.md
      and mistralai-ministral-8b-2512-batch.md both end with one. Nothing fails
      on it; it is a one-byte inconsistency that will show up as noise in the
      next diff that touches this file. Fix: append a newline after the closing
      `---`.
    subject: "content/wiki/model/mistralai-mistral-large-2512-batch.md"
subject: "content/wiki/model/mistralai-mistral-large-2512-batch.md"
reviewed:
  "content/wiki/model/mistralai-mistral-large-2512-batch.md": "d5c38b017f6096ddd4c05f4dfd7ed52d99ff045aef872b8ce7876220c283d968"
---

Verdict: approve.

## The claim, and the checks that would be red if it were false

The diff asserts one thing about the world: `mistralai/mistral-large-2512:batch`
was listed in the 2026-08-31 OpenRouter snapshot and is gone from the catalog,
which justifies `status: retired`, `maintenance: dormant`, removal of the feed
binding, and the timeline row "removed from the OpenRouter model catalog; last
listed in the 2026-08-31 snapshot".

**Committed snapshots, read directly** (script over
`data/sources/openrouter-models/{latest,previous}.json`):

```
latest.json fetched_at: 2026-09-01T06:00:03.724Z
latest.json rowcount: 420 has target: false
latest.json mistral-large rows: mistralai/mistral-large-2512
previous.json fetched_at: 2026-08-31T06:00:03.897Z
previous.json rowcount: 396 has target: true
previous.json mistral-large rows: mistralai/mistral-large-2512 | mistralai/mistral-large-2512:batch
```

Present on the 31st, absent on the 1st. Note the row count *rose* 396 → 420, so
this is not a truncated or partial fetch that lost rows wholesale.

**Live source, fetched during this review** — the cited URL itself,
`https://openrouter.ai/api/v1/models`:

```
status 200
rows 420
target present: false
mistral-large-2512 rows: mistralai/mistral-large-2512
any :batch rows still listed: 65
```

That is the instrument check an absence claim needs. The endpoint answers, is
complete, still carries 65 `:batch` rows and still carries the non-batch sibling
`mistralai/mistral-large-2512` (a different row with its own entry) — so the id
scheme has not changed underneath the corpus and this specific row is genuinely
delisted. The timeline row's wording is supported by the document it names.

**The diff layer already agreed, untouched by this job.** One matching line in
`data/changes.jsonl`:

```
{"date":"2026-09-01","display_name":"Mistral: Mistral Large 3 2512 (batch)",
 "excerpt":{"$status":"active","canonical_slug":"mistralai/mistral-large-2512",
 "context_length":262144,"id":"mistralai/mistral-large-2512:batch",
 "pricing.completion":"0.0000015","pricing.prompt":"0.0000005"}, ...}
```

## The repair shape

`status: active → retired`, `maintenance: living → dormant`, the `feeds:` map
and its four feed-bound facts removed, `timeline: []` replaced by one dated,
sourced row. That is the same shape merged for
`mistralai-ministral-8b-2512-batch` (0176854) and
`mistralai-codestral-2508-batch` (0f48167) — established convention, not an
invention here. I checked whether the removal violates
`openspec/specs/wiki/spec.md:170` ("A vanished row cannot pose as current"):
that scenario governs the *rendering while the row is vanished and unrepaired*,
not what the repair does, and `specs/pulse`'s freshness requirement likewise
describes the derivation. Neither is contradicted. The shape does discard the
last-known price and context window, which is a real loss — but it is already
filed as `data/proposals/freeze-last-known-facts-on-retirement.md`, so it is
neither this job's to fix nor mine to re-propose. I note no proposal for that
reason; nothing else this review surfaced was worth one.

## References and scope

No transclusion targets the removed facts. `grep` over `content/` for the entry
id and the row id returns three hits, all safe:
`content/wiki/org/mistral-ai.md:69` (a `mentions:` entry, which survives the
retirement) and `:99`, where the row id appears inside a prose passage
explicitly dated "as observed on 31 August 2026" — a dated observation the
delisting does not falsify. No `{{fact:model/mistralai-mistral-large-2512-batch#…}}`
exists anywhere in the corpus.

The diff touches the target entry, `.job/` scaffolding (removed before merge),
and four `data/derived/` files (dropped and recomputed at merge). Each derived
change is the mechanical consequence of the front-matter edit and nothing more:
the `$vanished` row leaves `feed-rows.json`, its record leaves
`freshness.json.vanished_feed_rows`, the matching `vanished-feed-row` item
leaves `queue.json` (6 → 5, the other five items byte-identical), and
`search-index.json` flips this entry's `"s"` from `active` to `retired`. Within
scope.

I did not re-run `npm test` or `npm run build` — the loop recorded both PASS on
`d052eb605fd8`, the commit this diff was computed from. No pre-existing review
record names this entry, so nothing is put into the mismatched state by the
edit.
