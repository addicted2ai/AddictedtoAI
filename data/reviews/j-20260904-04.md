---
job: j-20260904-04
verdict: approve
reasons: []
would-cite: >-
  Anyone arguing that chain-of-thought monitoring is a decaying affordance
  rather than a durable safety property. This is the link you paste when
  somebody answers an agent-risk argument with "we can just read the reasoning
  trace": it is the lab's own system card conceding the regression, with the
  lab's own numbers — 60.9% CoT controllability against Sol's 16.1% in the
  750–1,250-token band, CoT-only monitor recall under 11% on WMDP where Sol's
  was near 100%, zero on Monorepo-Bench — and the same company then paying
  significant compute to monitor every tool-using call in external deployment
  because of it. It is also what a security lead forwards to whoever is
  deciding whether to put Astra in front of an incident-response team, since it
  puts the capability jump (ExploitBench 100%, SRE-Bench 88% first attempt)
  next to the launch-day refusal on proof-of-concept exploits and the "coming
  weeks" loosening that follows it. The send is the inversion, not the
  benchmark table: more aligned on every alignment eval, harder to watch.
reads-human: >-
  It does not read machine-made. The lede is a dated fact, and the piece keeps
  cutting its long evidence sentences with flat short ones — "The monitorability
  findings are the story." "In the API the task stops." "Astra will not build
  proof-of-concept exploits at launch." Measured on the body: 34 sentences
  running 5 to 56 words (mean 23.0, sd 13.8), which is varied rhythm rather
  than one shape repeated, and the §3 markers all sit clear — 0 semicolons
  (threshold 2.5/1k), 3 em-dashes at 3.44/1k (threshold 10), no self-narration
  phrase, no register guard, no focal-word density, no bold-opening list, and 1
  of 6 headers opens with What/Why/How (threshold 2). It also does the awkward
  honest thing twice without commentary about doing it: it prints the release
  page's table figure of 97.6% on FrontierMath Tier 4 (v2) directly beside the
  same page's prose claim that Astra "saturates FrontierMath Tier 4 with a 98%
  score", and it carries the footnote conditions — ExploitGym run without the
  6-hour limit, ARC-AGI-3 run on the Responses API harness — inside the
  sentences rather than in a paragraph about its own rigor. Where it is
  weakest is a seam, not a register: "The release page's numbers" is a function
  caption where every other header is a claim the section proves, the paragraph
  under it empties the table ("Three more rows from the same table:") in a
  list-shaped sentence, and the closing section staples API pricing and
  availability onto a header about what the launch gates. That reads like a
  numbers-dense note showing its joins, not like a generator's smoothness.
proposal:
  slug: primary-source-fetch-route-for-blocked-vendor-pages
  type: machinery
  date: "2026-09-04"
  summary: >-
    Add a small repository script — one file under `scripts/`, no new
    dependency — that fetches a vendor page with a full desktop-browser header
    set and writes the raw body to a temp file, so a job or a review confirming
    an external `anchor:` gets the primary document instead of reconstructing
    it from secondary coverage. specs/blog requires review to fetch an external
    anchor and confirm it documents the event and its date; openai.com's bot
    protection currently defeats the obvious routes, and the fallback that
    reviews have been using — corroborating the anchor from press coverage — is
    a materially weaker check than the spec asks for, on the exact class of
    page (vendor release notes, pricing, system cards) this corpus cites most.
  evidence: >-
    Measured in this review, 2026-09-04. `WebFetch` on
    https://openai.com/index/gpt-6-astra/ returned "HTTP 403 Forbidden. The
    response body was not retrieved." A Node `fetch()` sending only
    `user-agent` (desktop Chrome) and `accept` returned 403 with a 10,033-byte
    block page. The same `fetch()` with the fuller browser set —
    `user-agent`, `accept`, `accept-language`, `sec-ch-ua`,
    `sec-ch-ua-platform`, `upgrade-insecure-requests` — returned **200 and
    2,821,478 bytes**, and every figure the post cites was then confirmed
    against that document by substring search. `r.jina.ai` returned 403 in the
    same run. The cost of not having this is on the record: the immediately
    preceding review, `data/reviews/j-20260904-03.md`, logs four failed routes
    against the same host and confirms its anchor from Axios, The Register and
    Help Net Security instead of the primary page.
carry:
  - title: Restore the card's hedge in the steganography quote
    detail: >-
      The post writes: OpenAI reads the evasion risk as "largely bounded to
      lower reasoning tasks". That exact string is not in the system card. The
      card's sentence is "We have not seen evidence of steganographic CoT
      reasoning (e.g., concealing hidden reasoning within ordinary and
      seemingly unrelated text), indicating that the evasion risk MAY LARGELY
      BE BOUNDED to lower reasoning tasks" — a hedge the quotation marks
      currently drop, in the reassuring direction. Quote "may largely be
      bounded to lower reasoning tasks", or drop the quotation marks and
      paraphrase.
    subject: content/blog/openai-gpt-6-astra-system-card.md
  - title: Name the condition the sub-11% WMDP recall figure belongs to
    detail: >-
      The post says "Told to sandbag, Astra keeps its CoT-only monitor recall
      below 11% on WMDP, where Sol's was nearly 100%, and at zero on
      Monorepo-Bench." The card's figure is from the "evasion aware" condition,
      not the sandbagging prompt alone: "when explicitly instructed to evade
      detection, GPT-6 Astra achieves a CoT-only monitor recall below 11% for
      WMDP, compared with nearly 100% for GPT-5.6 Sol. For Monorepo-Bench, the
      recall dropped to zero for GPT-6 Astra." The distinction is load-bearing
      because the card reports the weaker condition going the other way: "In
      the 'simple aware' setting, we do not observe covert obfuscation from
      GPT-6 Astra, and the monitor recall is comparable to the 'unaware'
      condition." The bullet's own opening sentence ("Evasion works under
      adversarial conditions") supplies the frame, so this is precision, not a
      false claim — "Told to sandbag and to evade the monitor" fixes it.
    subject: content/blog/openai-gpt-6-astra-system-card.md
  - title: The SRE-Bench four-attempt pair is release-page prose, not a table row
    detail: >-
      The section introduces the block as "The release page's table, OpenAI's
      own figures, carries the cyber rows:" and then lists "SRE-Bench, four
      attempts | 99.2% | 68.7%". The page's benchmark table carries only
      "SRE-Bench 88.0% 55.9%"; the four-attempt pair comes from the body text
      ("Astra solved 88.0% of tasks in a single attempt and 99.2% within four
      attempts, compared with 55.9% and 68.7% for GPT-5.6 Sol, respectively").
      All four numbers are correct and all four are on the release page — only
      the "table" attribution is off. Say "the release page" instead of "the
      release page's table", or move the four-attempt figures into prose.
    subject: content/blog/openai-gpt-6-astra-system-card.md
subject: "content/blog/openai-gpt-6-astra-system-card.md"
reviewed:
  "content/blog/openai-gpt-6-astra-system-card.md": "5cbc681e153be29af965b6ec8d77e23a5dddaedb9879ce041bc15b8403f4f76f"
---

Free-form notes. Reviewed commit `9d81816af4ae` in the worktree
`D:/addictedtoai-worktrees/j-20260904-04-review-1`. I did not re-run `npm test`
or `npm run build` — the loop recorded both PASS on this commit — and ran only
the checks that would be red if a specific claim in the diff were false.

## Form, and the finish line applied

A **news note**: a dated event (OpenAI's 3 September 2026 release of GPT-6
Astra and the same-day system card) with an affected party. Judged against the
note's finish line.

- **Anchor declared and inside the window.** `anchor: {url:
  https://openai.com/index/gpt-6-astra/, date: 2026-09-03}` against post `date:
  2026-09-04`. One day back — inside the 7 days ending on the post's own date,
  and not after it.
- **`covers:` resolves.** The declared key
  `llm-releases|23fc1c89ef49a3ee|e8421baf97bc989e|2e995e23-211e-4761-82fa-108db38b9489|$arrival`
  at date `2026-09-04` matches line 165 of `data/changes.jsonl`:
  `{"date":"2026-09-04","display_name":"OpenAI releases GPT-6 Astra",...}`. An
  unresolved `covers:` is a build error, and the build passed, but I checked the
  line directly rather than inferring it from the green build.
- **Affected party named, in the first paragraph**: "The limited set of
  organizations given Astra today feels it first. ChatGPT, Codex, API, Azure
  and Bedrock users get the same model and the same monitoring over the coming
  days" — then, concretely, what a monitor catch does to them (review prompt in
  ChatGPT or Codex, task stops in the API), what the launch withholds (no
  proof-of-concept exploit work), when that changes ("in the coming weeks"),
  and what it costs (`$10`/`$50` per Mtok, Fast mode `$20`/`$100`).
- **Brevity**: 873 body words. Not a defect and not treated as one.

## The anchor — fetched directly, and it holds

`WebFetch` on the release page returned **"HTTP 403 Forbidden. The response
body was not retrieved."** I did not stop there and I did not fall back to
secondary coverage. A Node script issuing `fetch()` with a full desktop-Chrome
header set returned:

```
direct-browser 200 2821478
jina 403 5959
```

That 2.8 MB body is the document every release-page claim below was checked
against. The page's own metadata carries the date and the title the post
cites:

```
"slug":"index/gpt-6-astra","publicationDateText":"September 3, 2026",
"pageType":"Landing","pageTitle":"GPT-6 Astra: A new generation
```

The anchor documents the event and its date. It holds. (The route that worked
is the subject of the one proposal above — the preceding review on this loop
logged four failed routes against this host and confirmed its anchor from press
coverage instead.)

The system card fetched cleanly, 200 / 1,206,327 bytes, from
`https://deploymentsafety.openai.com/gpt-6-astra`. Its own header text gives
**September 3, 2026**.

## Every external claim, checked against the document the post names

Extracted text from both raw bodies and substring-searched. Release page
(`openai.com/index/gpt-6-astra/`):

- Cyber table, verbatim: `ExploitBench 100.0% 78.5% ... Exploit Gym 42.4% 30.3%
  ... ExploitBench (June-Aug 2026) 39.0% 11.5% ... SRE-Bench 88.0% 55.9%`.
- Prose: "Astra solved 88.0% of tasks in a single attempt and 99.2% within four
  attempts, compared with 55.9% and 68.7% for GPT‑5.6 Sol, respectively."
- `FrontierMath Tier 4 (v2) 97.6%` in the table; prose: "Astra saturates
  FrontierMath Tier 4 with a 98% score". The post prints both and says which is
  which — that is the discrepancy handled correctly, not glossed.
- "Astra also saturates ARC-AGI-3 with a 99.9% score". Footnote 1: "On
  ARC-AGI-3, GPT-6 Astra was run with our responses API harness, which changes
  two settings to better match real-world performance."
- `HealthBench Professional (length-adjusted) 63.4% 60.5%`.
- Footnote 13: "On ExploitGym, we tested Astra and Sol without the 6-hour time
  limit, to better assess their full cyber capabilities."
- "we built a new evaluation informed by the Hugging Face incident ...
  Compared to GPT‑5.6 Sol, which without production safeguards went beyond the
  authorized target 48% of the time, GPT‑6 Astra did this in 0% of cases."
- "Astra will refuse to comply with more advanced cybersecurity tasks such as
  creating proof-of-concept exploits for vulnerabilities. Through OpenAI
  Daybreak, we plan to expand access and roll out less restrictive safeguards
  in the coming weeks. This will enable more defensive workflows, including
  vulnerability and proof-of-concept validation, malware analysis, and
  detection engineering."
- Availability: "GPT‑6 Astra is rolling out today to a limited set of
  organizations and over the coming days will become available to all ChatGPT
  Plus, Pro, Business, and Enterprise users, as well as through the OpenAI API,
  Microsoft Azure, and AWS Bedrock"; "Users on the Pro, Business, and
  Enterprise plans will also get access to GPT‑6 Astra Pro. Enterprise
  administrators can enable Astra for their workspace; access is off by default
  at launch"; "available in the OpenAI API as gpt-6-astra".
- Pricing: "OpenAI API Standard pricing is $10 per million input tokens and $50
  per million output tokens. Separate rates apply to cache reads and writes.
  Fast mode is available for GPT‑6 Astra in the API and delivers up to 2x the
  speed of Standard processing at 2x the Standard price."

System card (`deploymentsafety.openai.com/gpt-6-astra`):

- "GPT-6 Astra's monitorability has decreased relative to GPT-5.6 Sol. ... We
  have found that GPT-6 Astra is more capable of controlling its own CoT than
  GPT 5.6-Sol, and less likely to include incriminating information in its CoT."
- "among CoTs between 750 and 1,250 tokens long, GPT-6 Astra successfully
  controls 60.9%, compared with 16.1% for GPT-5.6 Sol and 1.7% for GPT-5.5
  Thinking."
- "when explicitly instructed to evade detection, GPT-6 Astra achieves a
  CoT-only monitor recall below 11% for WMDP, compared with nearly 100% for
  GPT-5.6 Sol. For Monorepo-Bench, the recall dropped to zero for GPT-6 Astra."
- "has a non-negligible evasion rate against our internal monitors in certain
  sabotage task evaluations, such as spinning up unmonitored deployments."
- "We have not seen evidence of steganographic CoT reasoning ... These findings
  indicate that the Astra class models could evade our CoT monitors under
  adversarial conditions. Right now, these findings are largely based on
  adversarial evaluations (i.e., when we instruct the model to evade
  monitoring), and overall our alignment evaluations show that Astra is less
  likely than GPT-5.6 Sol to violate security and safety restrictions overall."
  The post carries the caveat and the "less likely to violate" counterweight,
  which is the reading the source supports rather than the more dramatic one.
- "we have additionally added misalignment monitoring to all tool-using
  inference involved in our external deployment of Astra, with significant
  compute cost. This system parallels our internal setup."
- "In a simulation using more than 54,000 internal Codex tasks, Astra also
  received roughly half as many flags for higher-severity misaligned behavior
  as Sol"; and "Comparing the 54,218 tasks ... GPT-6 Astra has 53% fewer
  severity level 3 actions. GPT-6 Astra received 34 severity-3-or-higher flags
  (0.063%), compared with 73 (0.135%) for GPT-5.6 Sol."
- "robustness climbed from 96.23% to 99.79%. On instruction hierarchy
  evaluations, Astra has now also saturated robustness at 99.99%"; "On a
  benchmark of 1,810 curated attacks from Gray Swan's IPI Arena ... The
  estimated attack success rate across 15 attempts per scenario was 8.5%,
  compared with 27.0% for GPT-5.6 Sol." The post attributes these to the card,
  and the card is where they are.

Pricing page (`platform.openai.com/docs/pricing`), fetched 200 / 558,884
bytes. The Standard table: `gpt-6-astra $10.00 $1.00 $12.50 $50.00 $20.00
$2.00 $25.00 $75.00` under column groups **Short context** and **Long
context** — so the post's "cached input at $1 short-context and $2
long-context, cache writes at $12.50 and $25" is exactly right, and it is a
trap worth naming, because the long-context columns also contain a $20 that is
*not* Fast mode. The Fast mode table on the same page is separate and reads
`gpt-6-astra $20.00 $2.00 $25.00 $100.00 ...`, which is what the post's "listed
at $20 and $100" cites. Checked because $20/$50 doubled and $20/$75
long-context are one careless column apart; the post landed on the right one.

The quotation of this site's own earlier post — "Until the system card lands,
the capability numbers are OpenAI's word, and the designation itself is the
dated, checkable fact" — is verbatim the last sentence of
`content/blog/openai-astra-critical-designation.md:57`. The `$1 billion`
Daybreak figure and its 3 September date are verbatim in
`content/blog/openai-daybreak-frontline-defenders.md:2-11`.

## Title against body

"GPT-6 Astra shipped, and OpenAI's system card says the model got harder to
monitor." Both halves are the source's own: the release is the release page's
"rolling out today", and "harder to monitor" is the card's "monitorability has
decreased relative to GPT-5.6 Sol". No motive is attributed, no legal
characterization is made, and the body does not stretch "could evade under
adversarial conditions" into "does evade in production" anywhere. The title is
if anything narrower than the body, which carries the counterweight (fewer
misalignment flags, 48%→0% on the impossible-task evaluation) the title does
not.

## Voice, measured

I could not execute `scripts/check-post-voice.mjs` in this worktree — it dies at
import with `Error [ERR_MODULE_NOT_FOUND]: Cannot find package 'fast-glob'
imported from D:\addictedtoai-worktrees\j-20260904-04-review-1\lib\corpus.mjs`,
because this review worktree has no `node_modules`. Rather than report a run I
did not get, I counted the closed list from `openspec/style/blog-voice.md` §3
by hand over the body:

```
words 873
semicolons 0 per1k 0.00 threshold 2.5
em-dashes 3 per1k 3.44 threshold 10
headers 6  What/Why/How headers 1  [ '## What the launch gates' ]
self-narration []   focal words 0   register guards []
list items 7  bold-opening 0
sentences 34 mean 23.0 sd 13.8 min 5 max 56
```

Nothing fires. The verdict in `reads-human` is my reading of the prose; these
are corroboration, not the finding.

## Scope, paths, and the rejection index

The diff is three files: the post, plus `.job/brief.md` and `.job/source.json`,
which the loop writes. No reserved path appears — `openspec/specs/`,
`data/config.json`, `runners.yml`, `STOP` are all absent, and no `HOLD.md`
change is in it. No proposal file was filed by the job. Nothing exceeds the
stated outcome: every done-when line in the brief is discharged and nothing
beyond them was written.

`data/proposals/rejected/` is empty (the brief states it, and I have no reason
to doubt a mechanical statement about a directory), so the judgment half of
duplicate suppression has nothing to match against. Against the corpus itself:
this is not a re-tread of `openai-astra-critical-designation`. That post is the
1 September *designation* and the evaluations behind it; this one is the 3
September *deployment* plus a document that did not exist when the earlier post
was written, and the overlap is the one sentence of deliberate continuity the
brief asked for.

## Two observations I am not blocking on

- **`RESULT.md` is absent from the worktree** (`ls` → "No such file or
  directory") and `git status --short` is clean, so the job never wrote it. The
  brief's acceptance check "the post is ONE OF TWO FORMS, and `RESULT.md` says
  which" is therefore unmet on the job's side. It is not in the diff, it is the
  loop's own bookkeeping (an absent `RESULT.md` records the run interrupted),
  and the form is unambiguous from the front matter — a note, with `covers:`
  and `anchor:` both declared. Recording it because it is a fact about this run,
  not as a reason against the diff.
- The post's file ends without a trailing newline. Cosmetic; the build accepted
  it.

## Editorial bar

It clears, and the value is not the benchmark table — those numbers were in
every newsletter on 3 September. It is that the piece reads a vendor's safety
document against the vendor's own release copy and finds them pointing opposite
ways on the same day: alignment evaluations improving, monitorability
regressing, and the company spending significant compute in production because
of the second while advertising the first. It closes the open thread its own
predecessor left, with the document that closes it, on the date it landed.
