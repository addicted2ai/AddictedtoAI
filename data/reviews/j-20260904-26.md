---
job: j-20260904-26
verdict: approve
reasons: []
would-cite: >-
  Someone writing about Google's Flash line as a strategy — the org page's
  argument that the cheap tier overtook the expensive one, and that the July
  release "bought its buyers cost, not capability" before "the jump came
  twenty-three days later" — would link this paragraph, because it is now the
  place where the size of that jump is priced on both sides: tokens per task up
  ~30% to ~48k, cost per task ~$0.58 at high reasoning, rate card unmoved. The
  org entry's whole section is about magnitude; before this edit its closing
  sentence was the only one in it that gave a level with nothing to scale it
  against.
subject: "content/wiki/org/google-deepmind.md"
reviewed:
  "content/wiki/org/google-deepmind.md": "e35c3c9bb6391da6026e00b82335fa3fd9c28a919ff7e82c72ccd4a0d7848f5c"
---

The diff is one prose edit to the named target plus the required deletion of
the carry file that put the item in the queue. I checked the figure at its
source myself rather than inheriting the earlier pass's fetch, checked the
clause mapping, and checked the scope.

## The ~30%, fetched at the source, every occurrence

The sentence attributes "~30%" to
`https://llm-releases.com/models/gemini-3-8-flash`, which is the `source_url`
that `content/wiki/model/google-gemini-3-8-flash.md` records for both
`output_tokens_per_task` and `cost_per_task`. I fetched the page and printed
**every** occurrence of `30%`, `48k` and `0.58` in the tag-stripped served
bytes — not a head, not an extractor summary (2026-09-04):

```
$ node <tmp>/j26-fetch.mjs
status 200 final https://www.llm-releases.com/models/gemini-3-8-flash bytes 46694
--- 30% @643: ...52 at low (matching Gemini 3.6 Flash at ~30% lower cost per task)...
--- 30% @1135: ...with cached input keeping a 90% discount; a ~30% rise in average
output tokens per task (to ~48k) lifts cost per task to ~$0.58 at high
reasoning despite unchanged per-token pricing...
### 30%: 2 occurrence(s) in stripped text
### 48k: 1 occurrence(s) in stripped text
### 0.58: 1 occurrence(s) in stripped text
```

**The page carries two different ~30% figures and only one of them is this
claim.** The first (offset 643) is a *cost* comparison — 3.8 Flash at low
reasoning matching 3.6 Flash "at ~30% lower cost per task". The second (offset
1135) is the *token* rise. A search that stopped at the first hit would attach
the wrong one. The edited sentence reads "output tokens per task rose ~30%, to
{{fact:…#output_tokens_per_task}}, lifting cost per task to
{{fact:…#cost_per_task}} at high reasoning even though the per-token price
never moved" — rise on the tokens, "at high reasoning" on the cost, which is
the second sentence's structure clause for clause. Correct one of the two.

The source states the rise without naming what it rose from, and the prose
reproduces that silence rather than inventing a baseline from the surrounding
3.7 comparison. That is the right call.

## What the edit does, and what it leaves alone

`content/wiki/org/google-deepmind.md:98` is the only content line touched:

```
-sentence: output tokens per task rose to
+sentence: output tokens per task rose ~30%, to
```

Prose only. No front matter, no fact values, no transclusion targets — the two
`{{fact:…}}` references are byte-identical to before, so the resolved sentence
becomes "output tokens per task rose ~30%, to ~48k, lifting cost per task to
~$0.58 at high reasoning even though the per-token price never moved." It now
matches the model entry's version of the same sentence
(`content/wiki/model/google-gemini-3-8-flash.md:128-131`) in substance while
keeping its own wording, which is what the carried finding asked for.

The org sentence says "output tokens per task" where the source says "average
output tokens per task" (the model entry keeps "average"). That qualifier was
already absent from this sentence before the diff, "per task" already reads as
a rate, and the figure it qualifies is unchanged — not a defect this edit
introduces and not worth a job of its own, so I am not carrying it.

## Scope

Four paths: `.job/brief.md` and `.job/source.json` (the loop's own artefacts),
`content/wiki/org/google-deepmind.md` (the stated target, one line), and the
deletion of `data/carried/j-20260904-19-carry-1.md` — which the brief requires
of a fixed finding and which is what stops the item reappearing on the next
Pulse run. Nothing else moved; `git status --porcelain` on the branch is empty.
`RESULT.md` is absent from this worktree because `.gitignore:78` excludes it,
not because it was skipped.

## The review binding, which is the merge's job and not a defect here

`content/wiki/org/google-deepmind.md` is currently bound by
`j-20260904-17.pass2.md`, and this edit moves its reviewed-surface hash, so it
would read `mismatched` against that record. `loop/lib/review.mjs:807-828`
writes `subject:`/`reviewed:` into *this* record from the merged tree, and
`lib/reviews.mjs:197-235` orders competing records by their own date, else
their job id — so this record supersedes the 17 binding after merge. Same
situation j-20260904-19 was in with the model entry, and its record shows the
merge rebound it (`j-20260904-19.md:28-29`).

## What I did not re-run

`npm test` and `npm run build` — the loop ran both on `ced13a4dae0a`, PASS, and
neither is the check that would be red if this diff were wrong. A prose
sentence carrying a true percentage and a false one build identically; the
build only proves the two transclusions still resolve, which it did. The check
that would have failed is the source fetch, and that is the one I ran.
