---
job: j-20260914-23
verdict: reject
reasons:
  - spec-violation
would-cite: >-
  A catalog-watcher explaining why a model page can stay live after its router
  row disappears would cite this entry for keeping the OpenRouter binding while
  documenting the delisting separately, so a future re-listing reattaches
  instead of minting a duplicate page.
---

Free-form notes: what I checked, fetched, ran, and observed.

Judgment: this branch's diff does not contain the job's stated outcome. The
outcome was already merged by j-20260914-15 (commit 5bbbc42, approved); this
job is a stale-queue duplicate and its diff is derived-only churn that the
merge strips, leaving an empty source diff. Discard; the substantive answer
stands on j-15's record. This is the delta pass: nothing changed since the
prior verdict to address its findings (same diff 25488ab...e9494bd, no new
content commit), so the second non-approval discards the job.

Scope (diff judged: 25488ab...e9494bd, the same four files the loop's diff
shows). Observed via `git -C <worktree> diff 25488ab...HEAD --name-only`,
which printed exactly:
`.job/brief.md`, `.job/source.json`,
`data/derived/backlinks.json`, `data/derived/search-index.json`.
`--stat` on the same range: 4 files, 547 insertions(+), 1 deletion(-)
(532 + 8 scaffolding, 6 + 1-line derived). No edit to
`content/wiki/model/nousresearch-hermes-4-70b.md` in this diff. No move of
`data/vanished/openrouter-models--nousresearch-hermes-4-70b.md` in this diff
(nothing remains to move; see below).

What the brief required (quoted):
- "Establish which one happened from the vendor's and the feed publisher's
  own sources before writing anything."
- "Do NOT remove the entry or its `feeds:` binding."
- "MOVE this file into `data/vanished/answered/`, unchanged, in the same diff
  as the fix. Do NOT delete it."

What the tree already holds (via ancestry, not this diff):
- `content/wiki/model/nousresearch-hermes-4-70b.md` (read in this worktree,
  80 lines) already carries j-15's approved prose: row left the catalog
  without a date of its own, last seen 2026-09-09, absent from 2026-09-12 and
  2026-09-14 snapshots, departure recorded 2026-09-10; weights remain
  published under `NousResearch/Hermes-4-70B` with model card/FP8/GGUF,
  releases page still lists Hermes-4-Llama-3.1-70B 08/26/25 with no retirement
  note; openrouter.ai page answers 404 while the endpoints record returns the
  old record with `"endpoints":[]`; sibling 405B still in snapshots with one
  live Nebius endpoint; `feeds: openrouter-models: nousresearch/hermes-4-70b`
  kept (lines 10-11); `mentions: [model/nousresearch-hermes-4-405b]`
  (lines 34-35). `git diff 5bbbc42..HEAD -- <page>` printed nothing
  (EXIT:0): byte-identical since j-15's approved commit.
- `data/vanished/answered/openrouter-models--nousresearch-hermes-4-70b.md`
  exists (read: front matter `source: openrouter-models`,
  `row_id: nousresearch/hermes-4-70b`, `last_seen: "2026-09-09"`);
  `git diff 5bbbc42..HEAD -- <answered record>` printed nothing (EXIT:0).
  Top-level `data/vanished/` holds only
  `openrouter-models--openai-gpt-latest.md` and `README.md` plus `answered/`
  (observed via directory read). Nothing remains to MOVE.
- `data/derived/queue.json` in this branch still lists the hermes
  vanished-feed-row item (Select-String for `hermes-4-70b` matched the
  `detail`, `subject: openrouter-models:nousresearch/hermes-4-70b`, and
  `target: content/wiki/model/nousresearch-hermes-4-70b.md` lines). The queue
  is stale: `vanishedRowItems` (pulse/lib/queue.mjs:543-581, read) reads only
  top-level `data/vanished/*.md` files (`readdirSync` + `isFile()` +
  `.md` + not-README), so `answered/` entries never re-enter; and
  `recordVanishedRows` (pulse/lib/vanished.mjs:142-149, read) never
  re-records a row named in `ANSWERED_DIR` ("already been ANSWERED is never
  re-recorded"). The next pulse regenerates the queue without this item.

Polarity: implements / ignores / inverts — this diff IGNORES the two
operative requirements (no page edit, no record move) because both are
already satisfied in its base; it does not invert them (binding kept, entry
kept, no deletion). An omission is still a failure to meet the brief in the
diff under judgment, hence `spec-violation` rather than approval. It is not
a `scope-violation`: the diff touches nothing outside the repair (only .job
scaffolding plus derived rebuild outputs, the same shape j-15's approving
review explicitly held to be not-widening). No reserved path is touched:
`git diff 25488ab...HEAD --name-only` filtered for
`openspec/specs|data/config.json|runners.yml|STOP|HOLD|content/blog` printed
nothing.

Corroboration/agreement: the diff's only two substantive hunks (backlinks
+1 entry under `model/nousresearch-hermes-4-405b` for
`/wiki/model/nousresearch-hermes-4-70b`; search-index `b: true -> false` for
`model/nousresearch-hermes-4-70b`) share one instrument — the build's
recomputation from the already-merged j-15 content edit (mentions added,
prose added so no longer stub; `b` is `Boolean(doc.index?.stub)` per
lib/search-index.mjs:109, observed via grep). Their agreement is recorded as
agreement, never as corroboration. Both hunks are correct in isolation
(backlink correctly reflects `mentions:`, non-stub correctly reflects prose
present), but j-15's own history shows the merge strips exactly this shape:
4d70da5 "drop derived state before merge" removed the backlinks entry and
flipped search-index; 96fda6c "remove job scaffolding before merge" deleted
`.job/brief.md` + `.job/source.json`; merge 7906642 landed only the content
page + the answered move (2 files, observed via `git show --stat` on each).
So this diff's 4 files will be stripped the same way, leaving an empty source
diff. Merging a derived-only diff as `done` buys an empty merge; discarding
the duplicate is the honest end.

Spot checks run in this review (quoted output):
- `git diff 25488ab...HEAD --name-only` printed exactly `.job/brief.md`,
  `.job/source.json`, `data/derived/backlinks.json`,
  `data/derived/search-index.json`.
- `git diff 25488ab...HEAD -- <derived files>` printed the backlinks +6 hunk
  and the search-index `b: true -> false` hunk, matching the loop's diff.
- `git show 5bbbc42 --stat` confirmed j-15's merged content: the 47-line
  prose addition to `nousresearch-hermes-4-70b.md` plus the 100%-similarity
  move of the vanished record into `answered/`, plus derived rebuild.
- `git diff 5bbbc42..HEAD` on the page and on the answered record each
  printed nothing: bytes unchanged since approval.
- j-15's approving record `data/reviews/j-20260914-15.md` was read; it
  source-checked vendor weights, vendor releases, router 404,
  router endpoints-empty, sibling endpoints-Nebius, and both snapshots on
  2026-09-14 with verbatim quotes. Same-day evidence; no refetch was needed
  to establish duplication, and this review makes no new sourced claim about
  the model beyond reporting that the page bytes are unchanged since that
  approval.
- Gates: loop reports `npm run build` PASS and `verify-surfaces` PASS on
  e9494bd; per the brief they were not re-run.

No blog post is in this diff, so the voice question does not arise. No
proposal and no carry entries: the stale-queue window mechanism is already
evidenced above from the Pulse's own code, and there are no small corrections
(the page, binding, answered record and derived hunks are each correct where
they stand).
