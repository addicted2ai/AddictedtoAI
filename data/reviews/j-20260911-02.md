---
job: j-20260911-02
verdict: approve
would-cite: >-
  A course author teaching where training data comes from who needs a handoff
  for the inevitable "but is the scraping legal?" question, and a practitioner
  explaining agent harnesses who needs somewhere to send the reader asking
  "what happens when the instruction came from the environment, not the user" —
  each link earns its referral rather than decorating the paragraph.
subject:
  - "content/learn/what-an-agent-is.md"
  - "content/learn/what-models-are-trained-on.md"
reviewed:
  "content/learn/what-an-agent-is.md": "7ca836060e911d47993818febebb5eb4359bb61ea69997d1087da856f3adad58"
  "content/learn/what-models-are-trained-on.md": "f7c29786d8bafcfe22d4f18da2f5041d26a5f03667bb6dc1990c1b2ff1388c79"
---

Free-form notes: what you checked, what you fetched, what you ran, and what
you observed. Quote the observed output for anything you ran.

Scope: the content diff is exactly the two prescribed sentences — one in
`content/learn/what-models-are-trained-on.md`, one in
`content/learn/what-an-agent-is.md`. No other prose files changed
(`git diff main...HEAD --stat` shows only those two content files plus
`.job/` machinery and `evidence/` gate summaries). Not a scope-violation.

Sentence 1 (`what-models-are-trained-on.md:129`): "is settled nowhere; [that
fight is its own subject](/learn/ai-and-the-law)." Lands at the end of the
AP-deal / market-in-permission paragraph, where the reader is already asking
about the legality of the scraped layer underneath. A real handoff, not a
bolted-on see-also. Pure pointer: names the fight and says it has its own
page; imports nothing of the target's copyright/product-liability analysis.
Direction is prerequisite -> dependent (`ai-and-the-law` front matter lists
`what-models-are-trained-on` as a prerequisite), so no lean.

Sentence 2 (`what-an-agent-is.md:31-32`): "When the say-so it ran on came from
someone other than its operator, [that is its own subject]
(/learn/how-ai-systems-get-attacked)." Lands in "The model never executes
anything", directly after "a harness that ran a deletion without asking" —
the exact moment a reader wonders about hostile instructions. A real handoff.
Pure pointer: one clause of setup intelligible without the target, then the
referral; teaches no attack taxonomy, no defence placement. Direction is
prerequisite -> dependent (`how-ai-systems-get-attacked` front matter lists
`what-an-agent-is` as a prerequisite), so no lean. Either sentence stands
alone; neither had to be forced.

Targets confirmed on disk and read: `content/learn/ai-and-the-law.md` and
`content/learn/how-ai-systems-get-attacked.md` both exist with matching
titles. Links are absolute internal paths of the form the build resolves.

Inbound-link check via Grep tool for
`ai-and-the-law\)|how-ai-systems-get-attacked\)` over `content/` returned
exactly 2 matches: line 129 of what-models-are-trained-on.md and line 32 of
what-an-agent-is.md — i.e. one inbound prose link per previously-zero target,
as required. No other matches, so no pre-existing inbound to confuse the
count.

Gates: loop reports `npm run build` PASS and `node scripts/verify-surfaces.mjs
out` PASS on commit 61bb266f3ab4 (per brief); not re-run per instruction. No
blog posts in diff, so no reads-human obligation. No proposal, nothing to
carry. Polarity: implements the requirement, does not ignore or invert it.
