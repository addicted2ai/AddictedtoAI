---
job: j-20260830-01
verdict: approve
reasons: []
would-cite: >-
  Someone making the case that a vendor catalogue's silent deletions are a
  different event from its announced deprecations would link this page: a 72B
  model that OpenRouter listed on 2026-08-28, never marked deprecated, and
  simply stopped returning on 2026-08-29 — the removal dated and pointed at the
  endpoint that no longer contains it. I would also cite it to anyone asking
  whether this site's derived layer is trustworthy without reading the code:
  I recomputed the whole tree from the branch's own state with the machinery
  from `main` and got a byte-identical result, so the three derived files in
  this diff are a measurement rather than a claim.
subject: "content/wiki/model/arcee-ai-virtuoso-large.md"
reviewed:
  "content/wiki/model/arcee-ai-virtuoso-large.md": "f1a6b3db29de9b21d9e573e3111a8680fdc812f62290518f723cba157041ed9a"
---

Reviewed the diff on `efc1c4c5f03d`. `npm test` and `npm run build` were green on
this commit per the loop; I did not re-run them, and ran instead the checks that
would be red if this diff's specific claims were false.

## Is the row actually gone, or did a fetch blip catch it?

The entry's one new claim is the timeline event — *"removed from the OpenRouter
model catalog; last listed in the 2026-08-28 snapshot"*, sourced to
`https://openrouter.ai/api/v1/models`. Fetched that URL live during this review
(read-only, no credentials, no auth header):

```
http 200
total rows 396
rows matching /virtuoso/i: 0
arcee rows: arcee-ai/trinity-large-thinking
olmo-3-32b-think rows: 0
```

Zero rows contain `virtuoso` anywhere in the row JSON, not just in
`id`/`canonical_slug`/`name`. The delisting is real and current. (The `olmo` line
is incidental: the sibling repair merged at `c57de09` still holds too.)

The committed snapshot pair says exactly what the event text says:

```
latest   date= 2026-08-29 fetched_at= 2026-08-29T07:27:34.466Z rows= 396 virtuoso keys= []
previous date= 2026-08-28 fetched_at= 2026-08-28T18:19:35.242Z rows= 388 virtuoso keys= ["arcee-ai/virtuoso-large"]
```

And the diff layer had already reached the same conclusion on its own, without
this job — `data/changes.jsonl` line 62 (`grep -c virtuoso` over that file returns
`1`, so this is the whole of it):

```json
{"date":"2026-08-29","display_name":"Arcee AI: Virtuoso Large","kind":"retirement","row_id":"arcee-ai/virtuoso-large","source":"openrouter-models","source_url":"https://openrouter.ai/api/v1/models", ...}
```

Same date, same URL as the timeline event. The date is local-2026-08-29 — the
Pulse's own run log prints `date 2026-08-29` — which is the convention this
repository requires, and is worth stating plainly because the job id says
`20260830` and the build stamp on the built page says `2026-08-30T02:30:32Z`.
The content date follows the machine's local date, correctly, not the UTC one.

## The derived files are a recomputation, and I measured that rather than trusting it

Three of the four changed derived files are Pulse output, which a reviewer cannot
take on trust. I recomputed the entire derived tree from this branch's state,
using the **unmodified machinery from `main`** (`git diff --stat 645ef30 efc1c4c`
shows the branch touches no code at all, so main's `pulse/` is the same code):

```
PULSE_ROOT=<review worktree> node D:/AddictedtoAI/pulse/run.mjs --offline --no-mint --no-build --dry-run

pulse: run — root ...j-20260830-01-review-1, date 2026-08-29, offline
pulse: diff openrouter-models — 13 change(s) computed, 0 new line(s) appended
pulse: data layer — 396 catalog row(s), 3 deprecation(s), 57 change(s) in 30d
pulse: freshness — 0 overdue fact(s), 0 stale tutorial(s), 0 listing(s) needing attention, 0 suspect source(s)
pulse: queue — 1 item(s) of 1 (cap 50) — recomputed from state, never accumulated
pulse: publish — DRY RUN — nothing was committed and nothing was pushed
[exited with code 0]
```

Then `git status --porcelain` in that worktree printed **nothing**. A full
independent recomputation reproduced `feed-rows.json`, `freshness.json` and
`queue.json` byte-for-byte. That settles all three at once: nothing was
hand-edited, nothing else was quietly dropped from the queue alongside the
repaired item, and `0 new line(s) appended` confirms no change history was
manufactured. Corroborating count: `feed-rows.json` holds exactly **396**
openrouter keys with `virtuoso present= false`, matching the live API's 396 rows
— the previously preserved `$vanished: true` row is gone because nothing declares
it any more, not because a row was deleted by hand.

The fourth file, `search-index.json` (`"s": "active"` → `"retired"`), is build
output rather than Pulse output; it follows mechanically from the front matter and
the loop's build on this commit was green.

## What the page actually renders

Read the built page from the loop's own build of this commit
(`out/wiki/model/arcee-ai-virtuoso-large.txt`, footer stamp
`2026-08-30T02:30:32Z · efc1c4c5f03d`) rather than inferring it from
`lib/indexability.mjs`:

```
<span class="badge" data-tone="ended">retired</span><span class="badge" data-tone="ended">dormant</span>
<div class="notice" data-tone="ended" ... ><p>A record as of 2026-08-29. No longer actively maintained.</p></div>
<time datetime="2026-08-29" class="rail-date">2026-08-29</time> ... removed from the OpenRouter model catalog;
  last listed in the 2026-08-28 snapshot <a href="https://openrouter.ai/api/v1/models" ...>source</a>
{"name":"robots","content":"index,follow"}
```

The URL still resolves (no 404, `specs/site`), the obituary rule really does flip
it from `noindex` to indexed, and the as-of stamp renders. The entry now carries
`0 sourced facts` per its own meta description — thin, but a legitimate obituary
under the site's rule, which is the same trade the merged olmo repair made.

## Removing the `feeds` binding and the four facts

This is the judgment call, and I reached the same place the `j-20260829-03` review
did, by my own route: `specs/pulse` requires a queue item to leave the moment the
state is fixed, and leaving the declaration in place would keep this entry in
`vanished_feed_rows` forever — a queue item that can never clear, which the
"cannot backlog" requirement forbids. The alternative of transcribing the last
price and context window into `cited` facts would point a fact at a source I have
just measured as no longer containing it. The values survive in `previous.json`
and in the `changes.jsonl` retirement excerpt regardless.

## References and scope

`grep -ril virtuoso content/` returns exactly one file — the entry itself — so
deleting its four feed facts cannot break a transclusion or a mention; that is
measured, not inferred from the green build.

`git diff --stat 645ef30 efc1c4c` is exactly seven files: the one content entry,
the four derived files the recomputation regenerates, plus the loop's own
`.job/brief.md` and `RESULT.md`. No machinery, no reserved paths, no
`changes.jsonl` or `linkcheck.json` edits, nothing outside the stated outcome.

RESULT.md's account matches what I measured on every point I checked, including
the ones it would have been easy to overstate (it names the dry-run flags it used
and does not claim `verify-launch` was run).

## One note, not charged against this diff

The re-listing gap is real here as it was for olmo — `minted.json` still records
this row while the entry no longer declares it, so an OpenRouter re-listing would
log a `slug-collision` warning and enter no queue item. It is already filed on its
own id, `addictedtoai-2wa`, and the author correctly declined to widen this job
into it.
