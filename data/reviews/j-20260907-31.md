---
job: j-20260907-31
verdict: revise
reasons:
  - false-or-unsupported-claim
  - broken-reference
would-cite: >-
  A maintainer deciding whether a machinery job should change worktree dependency
  isolation would use this proposal to compare the loop's executor setup with
  its teardown safety contract.
---

The consolidation itself is present: the carried finding is deleted and the
standing proposal contains the idempotence and author/revision insertion-point
details. No `data/proposals/link-node-modules-before-the-author-runs.md` exists
on either `main` or `HEAD`, so there was no duplicate proposal file left for
this diff to remove.

Required changes:

- Reconcile the standing proposal's summary (lines 7-14) with the current
  implementation. The targeted check
  `rg -n 'linkNodeModules\\(' loop/run.mjs loop/lib/gates.mjs` reported call
  sites at `loop/run.mjs:300`, `loop/run.mjs:712`, and
  `loop/lib/gates.mjs:300`. Thus “Today `linkNodeModules` is called from
  exactly one place” is unsupported, and the author/revision calls described
  as proposed work are already present. Mark that part as historical/already
  landed or remove it so the standing proposal accurately describes the
  remaining work.

- Refresh the retained teardown evidence and references. The targeted check
  `rg -n 'unlinkNodeModules\\(' loop/run.mjs` reported
  `327`, `728`, `1272`, and `1336`; the proposal still cites
  `loop/run.mjs:275`, `:562`, and `:980` and says there are three callers.
  Those references no longer identify the callers. The source also directly
  confirms the stated idempotence at `loop/lib/gates.mjs:157` with
  `return { linked: false, why: 'already present' }`, while the unlink code at
  lines 170-178 still tests `st.isSymbolicLink() || st.isDirectory()`, calls
  `unlinkSync(link)`, and swallows the catch; that is the remaining hazard the
  proposal should cite accurately.

I did not rerun the full test/build/design/surface gates, per the brief's
instruction that they had already passed on this commit. `git diff --check
main...HEAD` was clean.
