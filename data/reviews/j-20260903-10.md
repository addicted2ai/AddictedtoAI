---
job: j-20260903-10
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that diffusion language models have left the demo stage would
  link the org page: it is the one place that shows the lab's two commercial
  dLLM rows side by side with the dated paper lineage Inception itself claims
  (Masked, Block and Remasking Diffusion, and d1), and separates what the
  catalog can verify — two rows, one preview, no weights — from what is only
  the vendor talking. A developer deciding whether to wire Mercury 2.5 into
  something would send the model page's new closing paragraph to whoever signs
  off on the bill, because it is the only writeup that says out loud that the
  $0.04/$0.15 they are looking at is an 80% promo expiring 8 September and not
  the list price, and that the preview alias they would hard-code can change.
proposal:
  slug: diffusion-language-models-explainer
  type: education
  date: 2026-09-03
  summary: >-
    An education page explaining what a diffusion language model is and how it
    differs from the autoregressive models every other row in the catalog
    names: masking and iterative denoising over a block of tokens versus
    left-to-right next-token prediction, why that buys parallelism and what it
    costs, and what "tunable reasoning levels" means when generation is not
    sequential. The corpus now has three pages (org/inception-labs,
    model/inception-mercury-2, model/inception-mercury-2-5-preview) that all
    lean on the phrase "produces and refines multiple tokens in parallel"
    without a single page a reader can follow to find out what that means.
  evidence: >-
    Grepped the whole corpus for "diffusion" on 2026-09-03: 13 files match, and
    every explanatory one is about image generation
    (content/learn/how-image-generation-works.md,
    content/wiki/event/stable-diffusion-release.md,
    content/deltas/text-to-image.md). content/learn/how-a-language-model-works.md
    does not contain the word at all, so the site's account of how a language
    model generates text is autoregressive-only. Inception's about page
    (fetched 2026-09-03) links the primary literature the page would rest on:
    d1 Reasoning https://arxiv.org/abs/2504.12216, Block Diffusion
    https://arxiv.org/abs/2503.09573, Masked Diffusion
    https://arxiv.org/abs/2406.07524, Remasking Diffusion https://remdm.github.io/.
carry:
  - title: Restore "API" to the OpenAI-compatibility quote
    detail: >-
      The new paragraph quotes Inception as saying its models are "OpenAI
      compatible and a drop-in replacement for traditional LLMs". Both vendor
      pages I fetched on 2026-09-03 say "OpenAI API compatible" — the homepage
      reads "Our models are OpenAI API compatible and a drop-in replacement for
      traditional LLMs", and the models page reads "OpenAI API compatible and
      are supported through libraries including AISuite, LiteLLM, and
      LangChain". The meaning is unchanged but the words inside the quotation
      marks are not the source's. Insert "API".
    subject: content/wiki/model/inception-mercury-2-5-preview.md
  - title: Fix the stutter the product fact creates when transcluded
    detail: >-
      The org body reads "The about page calls Mercury
      {{fact:org/inception-labs#product}}", and that fact's value is
      "diffusion-based large language models (dLLMs); it calls Mercury 'the
      world's first commercially available family of diffusion large language
      models'". renderFact (lib/facts.mjs:217) splices the value inline, so the
      rendered sentence says "calls Mercury ... it calls Mercury ..." twice in
      one clause. Dropping "; it calls Mercury" from the fact value, or
      rewriting the lead-in to "The about page describes", fixes it.
    subject: content/wiki/org/inception-labs.md
  - title: Signal that the technologies enumeration is partial
    detail: >-
      The org body presents the about page's research list as "the foundation
      line (Diffusion Models, Flash Attention, Direct Preference Optimization)
      and the discrete-diffusion line Mercury descends from (Masked Diffusion,
      Block Diffusion, Remasking Diffusion, and d1 Reasoning)". The page
      actually lists nine items; Discrete Diffusion Guidance
      (arxiv.org/abs/2412.10193) and Decision Transformers
      (arxiv.org/abs/2106.01345) are dropped without a word saying the
      enumeration is a selection. "among them" or a named omission would fix it.
    subject: content/wiki/org/inception-labs.md
subject:
  - "content/wiki/model/inception-mercury-2-5-preview.md"
  - "content/wiki/org/inception-labs.md"
reviewed:
  "content/wiki/model/inception-mercury-2-5-preview.md": "f73ab2d27c825ced16962799563033480ec6dec819d08bc462a01eb628eafb2e"
  "content/wiki/org/inception-labs.md": "f90ae63b2df0d995aee63f563079ac30832c71704350be813c4321fe8b7cce7f"
---

Free-form notes.

## Scope

Four files: `.job/brief.md` and `.job/source.json` (loop-generated), the new
`content/wiki/org/inception-labs.md`, and a two-hunk edit to
`content/wiki/model/inception-mercury-2-5-preview.md`. The model file's bulk
predates this job — `git log` on it shows `e949896` and `75951c6` from
j-20260902-24 — so what this job actually added there is the "fastest reasoning
LLM" opening-claim quote and the preview-caveats paragraph. Both sit inside the
stated outcome. Nothing else in the corpus was touched, no proposal file was
filed, and no reserved path appears in the diff. Not a scope violation.

Note for the loop, not a verdict matter: `RESULT.md` does not exist in this
worktree (`existsSync` → false), so the author's run left no status file.

## Sources fetched, and what they said

Every quote in the diff, checked against the page it names. Fetched 2026-09-03.

**aimadetools explainer** (`/blog/mercury-2-5-preview-explained/`) — all four
things the new paragraph attributes to it are there verbatim:
- "The currently documented developer path is a text model."
- base_url "https://api.inceptionlabs.ai/v1" against an OpenAI-compatible
  chat-completions endpoint.
- "Confirm the current direct model identifier in Inception's account
  documentation before shipping; preview aliases can change."
- "OpenRouter's launch rates are an 80% promotional discount. They are not
  Inception's direct list price and may change independently."

**openrouter.ai/inception/mercury-2.5-preview** — "Mercury 2.5 is the fastest
reasoning LLM, and the latest diffusion LLM (dLLM) from Inception." The diff's
new sentence — the listing's *opening* claim is that Mercury 2.5 "is the
fastest reasoning LLM" — is exactly right about both the words and their
position. Also confirmed on the page: 1,107 tokens/sec, "10+ point jump in
intelligence over Mercury 2", the GPT-5.6 Luna (Low) / Gemini 3.5 Flash-Lite /
Claude Haiku 4.5 comparison, and the banner "Limited-time 80% discount via
Inception through September 8, 2026 at 07:00 UTC".

**inceptionlabs.ai (homepage)** — "several times faster and less than half the
cost of conventional LLMs"; "leading researchers and engineers from Stanford,
UCLA, Cornell, Google DeepMind, Meta AI, Microsoft AI, and OpenAI";
"currently deploying these diffusion LLMs at Fortune 500 companies";
"sub-300ms time to first token, 5-7x higher throughput, and up to 70% lower
cost per task". All three org facts and the homepage line in the model body
hold. The one deviation is the "OpenAI API compatible" wording, carried above.

**inceptionlabs.ai/about** — "Mercury, the world's first commercially available
family of diffusion large language models (dLLMs)." Heading confirmed as
"Some of the technologies we've developed", with nine linked papers; d1
Reasoning links to `https://arxiv.org/abs/2504.12216`.

**arxiv.org/abs/2504.12216v1** — "d1: Scaling Reasoning in Diffusion Large
Language Models via Reinforcement Learning", v1 submitted Wed, 16 Apr 2025;
abstract: "We propose d1, a framework to adapt pre-trained masked dLLMs into
reasoning models via a combination of supervised finetuning (SFT) and RL." The
entry's gloss — "the April 2025 framework that adapts pre-trained masked dLLMs
into reasoning models via SFT and RL" — is the abstract, and the URL is pinned
to v1 because the claim carries a date. That is the repository's own arXiv rule
followed correctly; the about page links the unversioned form.

**inceptionlabs.ai/models** — "Input, $0.20 / 1M Tokens, Cached Input, $0.02 /
1M Tokens, Output, $0.75 / 1M Tokens" for Mercury 2.5, and "Mercury 1 and
Mercury Edit 2 remain supported for existing customers."

**llm-releases.com/models/mercury-2-5-preview** — "API-only (proprietary) via
Inception and OpenRouter", "Weights: Not released", "Released Aug 31, 2026",
"Vendor-reported figures are claims until independently verified." The org
body's "API-only, no weights released, per its llm-releases entry" is the page.

**openrouter.ai/inception/mercury-2** — release date March 4, 2026; "the first
reasoning diffusion LLM (dLLM)"; input $0.25 / output $0.75 per Mtok.

## Checks I ran against the repository

I did not re-run the suite. I ran the checks that would be red if a specific
claim in the diff were false.

The org body's strongest checkable claim is that `inception/mercury-2` and
`inception/mercury-2.5-preview` "are the only rows in the current OpenRouter
snapshot whose listings describe a diffusion model" and that "no other listing
in the snapshot claims one". Scanned `data/sources/openrouter-models/latest.json`
for `/diffusion|dLLM/i` across the **whole row JSON**, not just the
description — a deliberately wider net than the claim:

```
OR rows: 424 | keys: _key,architecture,canonical_slug,context_length,created,...
diffusion-matching rows: inception/mercury-2 | inception/mercury-2.5-preview
```

Two of 424. The claim holds, and holds under a stricter test than it makes.

Same snapshot, the Mercury 2.5 row, against the facts the body binds:

```
"context_length": 260000, "top_provider": {"max_completion_tokens": 65536}
"architecture": {"modality": "text->text", "input_modalities": ["text"], "output_modalities": ["text"]}
"pricing": {"prompt": "0.00000004", "completion": "0.00000015", "input_cache_read": "0.000000004"}
"supported_parameters": [..., "response_format", ..., "tool_choice", "tools"]
"created": 2026-08-31T20:57:44.000Z
"description": "Mercury 2.5 is the fastest reasoning LLM, and the latest diffusion LLM (dLLM) from Inception. ..."
```

That settles four separate things at once: the new paragraph's "the row's
modalities are text-to-text" is the row; the promo rates are $0.04/$0.15 per
Mtok against the vendor's $0.20/$0.75, which is exactly the 80% the banner
claims, so the body's pricing arithmetic is arithmetic and not assertion; the
pre-existing "checkable on the row, which accepts `tools` and `response_format`"
is checkable and true; and the 2026-08-31 timeline date is the row's own
creation timestamp as well as OpenRouter's and llm-releases' stated release
date. Mercury 2's row gives `created` 2026-03-04T14:57:55Z, matching the org
timeline's first line, and prompt $0.25/Mtok — so "at the promo rate the row
undercuts its predecessor's current listing" is true of the snapshot.

The two feed-detection timeline rows are records, so I checked the record.
`data/changes.jsonl`:

```
2026-09-02 | openrouter-models | arrival | inception/mercury-2.5-preview
2026-09-03 | llm-releases      | arrival | 06a0c9a7-7f66-487f-8180-dbaae938de80
```

and that llm-releases row carries `"pubDate": "Mon, 31 Aug 2026 00:00:00 GMT"`,
which is what the org timeline says it says. Both dates are the site's own
detection dates and are labelled as such, not as release dates — the
distinction the brief was careful about survives into the entry.

## Checklist items

- **Volatile values**: no price, context or output-token literal appears as
  prose text in either file. The org entry binds nothing volatile at all — its
  four facts are `slow`/`static` vendor characterizations with source URLs and
  2026-09-03 accessed dates. The new model paragraph introduces no number of
  its own; the only figure in it, "80%", sits inside a quotation attributed to
  the aimadetools page.
- **Aliases**: org gets "Inception Labs" exclusive and "Inception" shared.
  Correct classing — "Inception" is a word with obvious non-Inception uses and
  is also the vendor prefix on model ids, so exclusive would have been wrong.
- **Prose adds something beyond the data**: yes, and specifically. The
  two-rows-in-424 finding is not on any vendor page and cannot be read off the
  catalog by a visitor; the paper lineage is assembled from a page most readers
  of a model row will never open; and the closing paragraph converts a promo
  banner into a dated warning about what happens on 8 September. The vendor
  claims are labelled as vendor claims throughout — "all of it the vendor
  speaking", "The positioning is Inception's, not measured", "nothing here is
  independently verified" — which is the honest posture for a page whose
  subject publishes no benchmark scores.
- **Rejection index**: empty, so nothing to re-tread.

Approving. The three carries are corrections, not blockers: one word dropped
from inside a quotation, one transclusion that reads awkwardly where it splices,
and one enumeration that is a selection without saying so. None of them make a
claim the sources do not support.
