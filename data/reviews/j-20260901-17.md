---
job: j-20260901-17
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a model's "status" is a property of whoever is answering
  the request, not of the model — they need a page where a router and the vendor
  disagree on the same day, and where the timeline says which of them each row is
  reporting. This row is now the citable half of that: a dated line naming
  OpenRouter as the actor rather than a bare adjective a reader would attach to
  Moonshot.
subject: "content/wiki/model/moonshotai-kimi-k2-5.md"
reviewed:
  "content/wiki/model/moonshotai-kimi-k2-5.md": "06b7e6c96c5261e5b06ef7394ec2f03a7224bd41e4a504d39e9e18c96cc71f09"
---

Free-form notes.

## What the diff claims

One reworded timeline row on `content/wiki/model/moonshotai-kimi-k2-5.md`:

```
   - date: "2026-08-31"
-    event: active
-    source_url: https://openrouter.ai/api/v1/models
+    event: "OpenRouter's row drops its expiry; the derived catalog reads the model active"
+    source_url: "https://openrouter.ai/api/v1/models"
```

plus deletion of `data/carried/j-20260901-15-carry-2.md`, which the brief
requires in the same diff, and the two loop-written `.job/` files. Nothing else
is touched. That is exactly the stated outcome — no scope problem.

## Checking the new row against its source

I did not re-run `npm test` / `npm run build`; the loop's results on
`c81c55564496` are recorded in the brief. The claim that needed a check is the
new sentence's two halves, and both are recorded state rather than prose.

`data/changes.jsonl`, the 2026-08-31 line for this row (`grep -n "kimi-k2.5"`,
2 matches total, both read in full):

```
{"date":"2026-08-31","display_name":"MoonshotAI: Kimi K2.5","excerpt":{"$status":"active",
 ... "expiration_date":null, "id":"moonshotai/kimi-k2.5", ...},"field":"status",
 "kind":"field_change","new":"active","old":"deprecated","row_id":"moonshotai/kimi-k2.5",
 "source":"openrouter-models","source_url":"https://openrouter.ai/api/v1/models"}
```

So on that date, from that URL: the expiry went null and the status moved
`deprecated` → `active`. Both halves of the new event text are the diff record
verbatim, and the `source_url` is the source that produced it.

The second half — "the derived catalog reads the model active" — is a claim
about this repository's own derived layer, so I read it rather than inferring
it. `data/derived/catalog.json` in the branch worktree, the one row whose
`row_id` is `moonshotai/kimi-k2.5`:

```
{ "entry_id": "model/moonshotai-kimi-k2-5", "expiration_date": null,
  "row_id": "moonshotai/kimi-k2.5", "source_url": "https://openrouter.ai/api/v1/models",
  "status": "active" }
```

`status: "active"`, `expiration_date: null`. The row says what the timeline now
says it says.

I also read the annotation line filed against that same change key by job
j-20260831-04, which independently describes the same mechanism ("cleared the
2026-08-31 expiration date it carried in the 2026-08-30 snapshot ... the derived
catalog row now reads `active` while the joined entry ... still declares
`status: deprecated`"). Nothing in the new event text goes beyond it.

I did not fetch `https://openrouter.ai/api/v1/models` live. Deliberately: the
row is a *dated* claim about what the feed carried on 2026-08-31, and today's
response cannot confirm or refute that. The snapshot diff is the artefact that
can, and it does.

## Whether the reword actually fixes the finding

The finding was that a reader met a dated row whose whole text was "active",
with no subject. Read now in sequence, the two 2026-08-31 rows are:

- "Moonshot's own API sunsets the model; migration directed to kimi-k3"
- "OpenRouter's row drops its expiry; the derived catalog reads the model active"

The second names its actor in its first two words and the two rows now read as
the disagreement the body prose spends its second paragraph on ("The catalog
reads this row `active`; this page reads it `deprecated`, and the disagreement
is which signal counts"). The defect is gone, and the fix is consistent with
prose that was already reviewed rather than a new assertion beside it.

The cosmetic half of the finding — the unquoted `source_url` — is fixed too;
all six `source_url` values in the file are now quoted.

`status: deprecated` in the front matter is unchanged and correctly so: the
body argues explicitly for following Moonshot's documentation over the router's
derived field, and this job was not asked to relitigate that.

Nothing to carry and no proposal.
