---
job: j-20260907-26
verdict: approve
reasons: []
would-cite: >-
  Someone explaining how a withdrawn Microsoft model can remain rentable through
  a router would cite this entry for the dated split between Microsoft's vendor
  namespace, Azure's two endpoint records, and OpenRouter's separately attributed
  catalog description.
subject: "content/wiki/org/microsoft.md"
reviewed:
  "content/wiki/org/microsoft.md": "45314b78f04d6479d8919f556736985b9f7a49418fd1e8f18ddcc998769c77d3"
---

The repair addresses both carried findings on `content/wiki/org/microsoft.md`.

Source checks:

- A direct `Invoke-RestMethod` request to
  `https://openrouter.ai/api/v1/models/openai/gpt-6-astra/endpoints` returned
  `astra_endpoint_count: 5` and
  `azure_endpoints: "Azure | openai/gpt-6-astra-20260903 [azure] | Azure |
  openai/gpt-6-astra-20260903 [azure/us]"`. This supports the corrected
  two-endpoint fact and its exact names/tags.
- A direct request to `https://openrouter.ai/api/v1/models` returned the three
  matching IDs `cognitivecomputations/dolphin-mistral-24b-venice-edition`,
  `microsoft/phi-4`, and `microsoft/wizardlm-2-8x22b`, with no `mai` or
  `microsoft-ai` namespace IDs. The current response has 428 rows; the
  unchanged 431-row dated claim is supported by the prior review's 2026-09-06
  measurement, which explicitly recorded the later feed drift as dated rather
  than a defect.
- A direct request to
  `https://openrouter.ai/api/v1/models/microsoft/wizardlm-2-8x22b/endpoints`
  returned the description beginning exactly `WizardLM-2 8x22B is Microsoft
  AI's most advanced Wizard model...`, so removing the attribution aside from
  the fact and placing it in the body is accurate.
- The prior review's recorded sweep measured the historical dead-upstream
  claim: `B: 200 = 177 | non-200 = 1` for the then-current 178 declared IDs,
  with `microsoft/wizardlm-2-8x22b -> microsoft/WizardLM-2-8x22B = 401`.
  The repair only supplies the missing subject in that unchanged dated fact.

Rendering and scope checks:

- `lib/render/entry.mjs` maps every `factsHtml` item into its own `<dd>`, and
  `lib/facts.mjs` renders a cited fact's value before its source link. The four
  repaired values therefore stand alone in the Facts section; the new wording
  supplies the subjects that were previously missing. The targeted runtime
  probe was unavailable because this worktree's dependency junction lacks
  `fast-glob`; I did not touch or reinstall `node_modules`.
- `git diff --check main...HEAD` produced no output. The changed paths were
  `.job/brief.md`, `.job/source.json`, the target wiki entry, and deletion of
  exactly `data/carried/j-20260906-09-carry-1.md` and
  `data/carried/j-20260906-09-carry-2.md`. Both carried files are absent from
  the worktree.
- The supplied gate results were accepted without rerunning them: `npm run
  test`, `npm run build`, `verify-surfaces`, and `verify-design` all passed on
  commit `d49da0c1f50f` before this review.
