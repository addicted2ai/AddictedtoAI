---
job: j-20260908-09
verdict: approve
reasons: []
would-cite: >-
  A maintainer auditing an OpenRouter route withdrawal would link this durable
  record to show that the free row was answered without deleting the model
  entry or its feed join.
---

Free-form notes: The substantive diff retires the pending vanished-row record:
`data/vanished/openrouter-models--minimax-minimax-m2-7-free.md` is absent at
`HEAD`, and the answered file's blob is byte-identical to the pending file's
base blob (`moved_content_equal=true`). The target entry is unchanged by this
branch but still contains `openrouter-models: minimax/minimax-m2.7:free`, and
the direct check printed `binding_present` with that exact line and
`target_diff_clean=true`. The pre-existing answered blob was replaced by the
same finding's more complete pending record; no distinct row or evidence was
lost.

`git diff --check main...HEAD` printed `diff-check=clean`. The local source
check of `data/sources/openrouter-models/previous.json` printed
`snapshot=2026-09-07T06:00:03.028Z`, `id=minimax/minimax-m2.7:free`,
`canonical_slug=minimax/minimax-m2.7-20260318`, `context_length=196608`,
`created=1773836697`, `hugging_face_id=MiniMaxAI/MiniMax-M2.7`,
`name=MiniMax: MiniMax M2.7 (free)`, and the pinned benchmark rows including
`3d` `elo=1231, rank=38, win_rate=50.6`. The current local
`data/sources/openrouter-models/latest.json` check printed
`latest_date=2026-09-08` and `latest_row_present=false`.

Live source checks on 2026-09-08: `https://openrouter.ai/api/v1/models` and
`https://openrouter.ai/api/v1/models/minimax/minimax-m2.7:free/endpoints`
printed `{"catalog_matches":0,"endpoint_id":"minimax/minimax-m2.7:free","endpoint_count":0}`.
The vendor listing at
`https://platform.minimax.io/docs/api-reference/models/anthropic/list-models`
printed `{"status":200,"has_model":true,"has_free_variant":false}`. These
support the existing page's router-specific withdrawal wording and its refusal
to claim a vendor retirement or rename. The supplied test, build, surface, and
design gates were not rerun.
