---
job: j-20260908-14
verdict: revise
reasons:
  - false-or-unsupported-claim
  - intent-not-measurement
  - spec-violation
would-cite: >-
  An editor deciding whether a declined story should be reopened would link the
  retry ledger only if it actually preserves the blocked URL and can show that a
  later successful fetch produces a re-checkable queue item without retrying the
  same URL repeatedly in one day.
---

Free-form notes:

The requested GitNexus change-map could not run: its repository lookup returned
`Error: Repository "D:\\addictedtoai-worktrees\\j-20260908-14-review-1" not found`
and listed only unrelated indexed repositories. I used repository-local checks
instead.

The branch's supplied gate results were not rerun, per the brief. I ran the
changed test directly:

`node --test D:\\addictedtoai-worktrees\\j-20260908-14-review-1\\pulse\\tests\\blocked-primaries.test.mjs`

It could not load the test because this review worktree has no `node_modules`:

`Error [ERR_MODULE_NOT_FOUND]: Cannot find package 'fast-glob' imported from ...\\pulse\\lib\\queue.mjs`

I did not install anything, because the brief forbids touching `node_modules`.

The offline guard was checked independently with a temporary fixture and a
counting fetch stub. It printed `{"calls":0,"status":403,"resolved":false}`.
The ownership guard also behaved as intended: `isEngineWrite` returned
`{"blocked":true,"config":false,"reviews":false}` for the new state file,
the reserved config path and a review record respectively.

`node --check` exited 0 with no output for all five changed JavaScript modules
and the added test file; this established syntax only, not behavior.

I then ran the changed parser directly against the real legacy record
`data/proposals/dropped/openai-cursor-model-supply-termination.md`:

`node --input-type=module -e "import {extractBlockedPrimaries} ..."`

The observed result was:

    {"url":"https://openai.com/policies/services-agreement/`","status":403,"date":"2026-09-08","slug":"openai-cursor-model-supply-termination"}

The URL includes the Markdown closing backtick. The implementation's `cleanUrl`
removes periods, commas, semicolons and colons, but not Markdown delimiters; a
direct URL normalization probe showed the captured value becomes
`https://openai.com/policies/services-agreement/%60`, so the legacy record is
retried against a different resource as written. The source record also says the
same 403 applied to OpenAI's statement URL, but the parser's own rule refuses to
associate a status after a prior URL with the next URL; the direct discovery
check returned only the malformed services-agreement URL.

Required changes:

- Parse real legacy drop-record URLs into the correct fetch URLs, preserving the
  original failure date from the record rather than replacing it with the date
  of every later discovery. Cover each blocked URL that the record identifies,
  or require and validate one explicit machine-readable line per URL.
- Add and run a direct regression for the real Markdown-delimited legacy shape
  and for multiple blocked URLs in one record; the current synthetic test does
  not exercise either case.
- Add a cadence/retirement rule. A direct behavioral probe with one same-day
  record and a mock fetch called the implementation twice with the same
  `day: '2026-09-08'`, producing `calls: 2` and two same-day entries in
  `attempts`. The Pulse's existing source-refusal contract retries at most once
  daily, and this new path currently has no equivalent guard. A second direct
  probe after a successful fetch returned `afterFirst: 1` from
  `resolvedBlockedPrimaries` and left `stored: true`; because `computeQueue`
  emits an item for every such record and nothing clears it, the same verify
  item remains eligible forever. Define an explicit retirement/refile
  transition so a resolved record is surfaced once and then leaves the derived
  queue.
