---
job: j-20260907-08
verdict: approve
reasons: []
would-cite: >-
  Someone writing about the gap between what a lab knows and what it is obliged
  to say would send the middle of this piece, and after this repair they can
  send it without a caveat: the blockquote now starts exactly where TechCrunch's
  quotation marks start, so a reader who follows the link finds the sentence
  they were shown rather than a two-word reconstruction of it. The specific
  paste is still the pairing — OpenAI's own contrast between the "wiki incident"
  as misalignment and "the Hugging Face incident" as a security-playbook event,
  set against the finding that the wiki record shows no compromised third-party
  systems, so the company's distinction survives and that is the hole. A second
  audience is narrower and mine: anyone auditing how this corpus handles a
  primary it cannot reach. The post declares that the X post returned HTTP 402
  and that every OpenAI quotation is TechCrunch's rendering, and as of this diff
  that promise is true of every quoted string rather than nearly all of them.
  The five-weeks correction serves the same reader — the cleanup now ends where
  WinBuzzer ends it, five weeks after 22 June, not five weeks after 2 July.
carry:
  - title: Fold the new node_modules proposal into the standing one that already proposes it
    detail: >-
      `data/proposals/link-node-modules-before-the-author-runs.md` (added by this
      diff, 2026-09-07) proposes what
      `data/proposals/author-worktrees-carry-no-node-modules.md` (2026-09-04,
      still standing in the tree) already proposes: "Link `node_modules` into a
      job worktree when the worktree is created, not only inside `runGates`."
      The slugs differ, so nothing auto-discards, and both are selectable. The
      new file is not redundant in every part — it adds that `linkNodeModules`
      is already idempotent, so the gate-time call needs no change, and it names
      the exact insertion point — while the older one carries a second half the
      new one drops: teaching `unlinkNodeModules` to tell a junction it created
      from a real directory a job installed, rather than calling `unlinkSync` on
      a directory and swallowing the throw. Whoever takes this should work from
      one file holding both halves and drop the other, rather than let two
      proposals for one fix compete for selection. (The older file's line
      citation has also drifted: it names `loop/lib/gates.mjs:177` for the sole
      `linkNodeModules` call, which is `:300` today.)
subject: "content/blog/nobody-had-to-report-the-wiki-incident.md"
reviewed:
  "content/blog/nobody-had-to-report-the-wiki-incident.md": "a4c200da7ff9cb0149a2e6ba25f5e37255024640908b06f1e53d31244ff8212b"
---

Verdict: **approve**. Both carried findings are fixed, both fixes are supported
by the cited page's own bytes, both finding files are deleted in the same diff,
and nothing else in the diff exceeds the outcome.

I did not re-run the four gates. The loop ran `npm run test`, `npm run build`,
`verify-surfaces` and `verify-design` on `fb0159eab888` — the commit this diff
was computed from — and reported PASS on all four. Both changes are prose inside
one already-published post; the check that would be red if either claim were
false is not the suite, it is the source, so that is what I ran.

## What I fetched, and what the bytes said

I wrote a throwaway script rather than trust WebFetch's extractor:
`data/reviews/evidence/j-20260907-08-fetch.mjs` fetches each URL, strips script,
style and tags, decodes the smart-quote entities, collapses whitespace, and
reports presence or absence of exact fragments with surrounding context. Raw
bodies go to the OS temp directory, not into the repository.

**Finding 1 — the OpenAI blockquote. Anchor: techcrunch.com/2026/09/05/openai-confirms-wiki-incident-...
(HTTP 200, 235,990 bytes).**

```
  [MISSING] "We and the larger AI community"
  [PRESENT] "the larger AI community do not yet have a clear standard"
      ...OpenAI's statement also gestured at the need for more standards,
      stating that both OpenAI and "the larger AI community do not yet have a
      clear standard for how to report misalignment that shows up during
      training, evaluation, and deployment, including examples that don't look
      like traditional security incidents but could provide insight into AI
      behavior and future risks." In the absence of that standard, OpenAI said
      it's "working on a framework and will share it in upcom...
  [PRESENT] "stating that both OpenAI and"
```

That reproduces the carrying reviewer's measurement independently: the two words
`We and` are not on the page, and the quotation TechCrunch actually opens is at
`the larger AI community`. The post now reads:

> Which is the whole problem, and OpenAI says so. TechCrunch renders the
> statement as saying that both OpenAI and —
>
> > the larger AI community do not yet have a clear standard for how to report
> > misalignment that shows up during training, evaluation, and deployment,
> > including examples that don't look like traditional security incidents but
> > could provide insight into AI behavior and future risks.

The blockquote now begins at the word TechCrunch's own quotation mark opens on,
and every word inside it is verbatim against the fetched text, including the
trailing `future risks.` The lead-in is TechCrunch's framing attributed to
TechCrunch, which is what the finding asked for and is a more honest form than
the suggested `OpenAI's statement said that both it and...` — the post is
quoting a secondary because the primary is unreachable, and the new sentence
says so on the page rather than only in the appendix.

The consequence worth naming: the sourcing block's promise, "Every OpenAI
quotation above is TechCrunch's rendering of that post", was the sentence the
old text falsified for two words. I checked the remaining OpenAI quotations in
the post against the same fetched text and they hold — `an instance of
misalignment similar`, `followed a traditional security incident response
playbook`, `working on a framework and will share it in upcoming weeks, and in
parallel we're working with dozens of government regulatory agencies worldwide
on these issues`, and the spokesperson's `meaningfully respond to claims or
findings on a report that we have not had an opportunity to review`. So the
promise is now true of every quoted string, not nearly all of them.

**Finding 2 — the five weeks of cleanup. winbuzzer.com/2026/09/05/openai-linked-agents-dsewiki-...
(HTTP 200, 810,519 bytes).**

```
  [PRESENT] "continued removing agent-created pages"
      ...According to the reconstruction, the moderator repeatedly restored
      overwritten content, including the homepage, and continued removing
      agent-created pages for five weeks after the main burst ended...
  [PRESENT] "main burst"
      ...The reconstruction dates the first attempted edits on another public
      wiki to May 11 and the first successful DseWiki write to May 24. A
      moderator repaired an overwritten changelog on June 2. Posting surged on
      June 16, and the main burst ended after June 22, followed by smaller
      bursts on July 1 and July 2...
```

Both halves of the finding are confirmed in one fetch. The source says
`for five weeks after the main burst ended`, and the same source says the main
burst ended after 22 June with the July bursts following it — so the old
`five weeks after that`, sitting immediately after the sentence ending in
`2 July`, did attach the interval to the wrong anchor. The post now reads
`Leitner went on deleting agent pages for five weeks after the main burst
ended`, which is the source's own phrasing and, on the source's own dating,
about ten days earlier than the old text implied. The surrounding dates in that
paragraph — 11 May, 24 May, 16 June, main burst ending after 22 June, 1 and
2 July — all match the WinBuzzer text quoted above, unchanged by this diff.

The sentence was re-wrapped across the following two lines to fit the column, so
the diff looks larger than the edit; the only changed words are
`after that` → `after the main burst ended`.

## Retirement

Both finding files are deleted in this same diff:
`data/carried/j-20260906-15-carry-1.md` and `-carry-2.md`, matching the two
findings one for one against the `carry:` entries in
`data/reviews/j-20260906-15.md:38` and `:57`. That is the retirement mechanism
`specs/pulse` names — presence of the file is the state — so the next Pulse run
recomputes the queue without this subject. Neither finding was left standing, so
there is nothing that had to keep its file.

## Scope

The diff is six files: `.job/brief.md` and `.job/source.json` (loop-written
scaffolding, not the job's), the one target post, the two carried-finding files
deleted, and one proposal. That is one proposal, which is the cap, and its
`type: machinery` is not `repair`, so the self-amplification rule does not catch
it. No reserved path is touched — `openspec/specs/`, `data/config.json`,
`runners.yml` and `STOP` are all absent from the diff, and no `HOLD.md` is
involved. Nothing here exceeds the stated outcome.

I spot-checked the proposal's own code claims rather than take them, since a
proposal is part of the diff I am judging. `grep -n 'linkNodeModules'` over
`loop/run.mjs` and `loop/lib/gates.mjs` returns exactly one call site for the
link — `loop/lib/gates.mjs:300`, inside `runGates` — against `unlinkNodeModules`
at `loop/run.mjs:290`, `:685`, `:1228` and `:1288`. So the proposal's central
claim, that there is no `linkNodeModules` call anywhere on the path between
worktree creation and the executor invocation, is true of this tree, and the
asymmetry it describes is real. `ls` on this review worktree confirms the
symptom from the other side: `node_modules` does not exist here either.

What the proposal does not say is that the fix is already proposed. That is the
carry above, not a block: a duplicate proposal is a selection problem for
whoever drains the proposal queue, not a defect in this repair.

## The one thing I looked for and did not find

I checked whether the new lead-in sentence disturbed the voice measurements the
previous reviewer recorded, since this diff adds an em-dash to a post whose
count was already recorded at 11. I measured rather than assumed, over the body
with front matter stripped, blockquote lines dropped and inline code masked:

```
words 1804 em 12 per1000 6.65 semi 1 0.55
```

So 12 em-dashes at **6.65 per 1,000** against the documented threshold of 10,
and semicolons unchanged at 1. Still comfortably under, and the lint is advisory
in any case. (My word count is 1,804 against the previous reviewer's 1,790 —
the two masking rules differ slightly; the densities are what the threshold is
stated in and both are far from it.) No new self-narration phrase, no new
header, no list. The repair reads as a correction rather than as a rewrite,
which is what a repair on an approved post should read as.
