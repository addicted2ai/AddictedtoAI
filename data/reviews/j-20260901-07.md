---
job: j-20260901-07
verdict: approve
reasons: []
would-cite: >-
  Anyone arguing about what "open weights" now means would send the GLM-5.3
  candidate: a licence that grants freely and then reserves a discretionary
  security review over any Model-as-a-Service operator above $10B revenue is a
  new shape, and the clause is primary, quotable and four days old. A model-
  licensing watcher forwards it to a colleague tracking the open-weights wave
  with no more explanation than "look at clause 2." The Thomson Reuters
  candidate is the one a legal-tech or enterprise-AI reader sends on — a
  content incumbent claiming frontier parity for $40M is a counter-argument to
  the whole open-weights week, not a routine launch. The Tencent entry is the
  weakest of the three as a send, which is exactly where the run ranked it, and
  it earns its place as durable, licence-verified data rather than as a story.
carry:
  - title: Pin the GLM-5.3 comparator benchmark figures against the card before writing
    detail: >-
      The proposal's evidence line carries "CyberGym 84.5 vs GLM-5.2 77.2;
      ExploitGym 105/130 vs 29/39". Fetching
      https://huggingface.co/zai-org/GLM-5.3 on 2026-09-01 confirmed the
      GLM-5.3 side — license "glm-5.3", 753B params, the "Emergent Cyber
      Capability" note, CyberGym 84.5, ExploitGym 105/130 — but the extraction
      surfaced a second ExploitGym row (130/247 at a 6-hour timeout) and did not
      surface either comparator (77.2, 29/39). The "29/39" denominator matches
      neither 130 nor 247, so the pair may be reading across two different
      timeout rows. WebFetch's extractor is not evidence of absence, so this is
      not a finding that the numbers are wrong — it is that the post job must
      read the benchmark table itself, state which timeout row each figure comes
      from, and name the card as the cited document, rather than inheriting the
      pair from this proposal.
    subject: data/proposals/glm-5-3-license-revenue-gate.md
---

Free-form notes.

## What I checked

**The charge, first.** Three candidates filed; all three rest on documents
outside this repository and outside the registered sources (`openrouter-models`,
`llm-releases`). Two vendor press releases and two Hugging Face primary
documents. This is not a run that could have been written from `changes.jsonl` —
the feed lines named Thomson, Hy4 and GLM-5.3 as bare arrivals; the licence
clause, the $40M figure and the Apache-2.0 confirmation are all things the site
could not have thought of by looking at itself. The charge holds.

## What I fetched (all on 2026-09-01)

- `https://huggingface.co/zai-org/GLM-5.3` — card exists. License field
  `glm-5.3`, 753B params, "Emergent Cyber Capability" note present, CyberGym
  84.5 described as state of the art, ExploitGym 105/130. Matches the proposal.
- `https://huggingface.co/zai-org/GLM-5.3/raw/main/LICENSE` — the load-bearing
  claim, and it is verbatim. Clause 2: *"If the Licensee or any of its
  affiliates operates a Model as a Service business, and the aggregate revenue
  of the Licensee and its affiliates exceeds 10 billion US dollars (or the
  equivalent in other currencies) in total over any consecutive 12 months, the
  Licensee must pass Z.AI's security review before using the Software or its
  derivative works for any commercial purpose."* The MaaS definition and the
  `glmlicense@z.ai` contact are both present, as the proposal says.
- `https://www.tencent.com/tencent-releases-and-open-sources-tencent-hy4-preview/`
  — exists, dated August 28, 2026. *"770B total parameters and 49B active
  parameters, and a context window exceeding 1M tokens"*; the blind evaluation
  at 2.99/4.00 across 163 experts and 203 engineering tasks against GLM-5.3 2.92
  and Kimi K3 2.94; pricing USD 0.834 in / 2.501 out / 0.042 cache-hit per
  million; two weeks free on WorkBuddy and CodeBuddy with Hy3 extended to
  September 30; the *"31.8% compared with the baseline"* throughput claim. Every
  figure in the proposal's evidence block is on the page.
- `https://huggingface.co/tencent/Hy4-preview/raw/main/LICENSE` — *"licensed
  under the Apache-2.0"*, *"Copyright (C) 2026 Tencent. All rights reserved."*
  The proposal's claim to have verified the licence from the primary document is
  true, and it is the one candidate that did so.
- `https://www.thomsonreuters.com/en/press-releases/2026/august/thomson-reuters-leverages-its-world-class-data-assets-to-launch-its-own-frontier-model`
  — exists, TORONTO, August 24, 2026. The $40M training investment, *"trained on
  less than 10% of Thomson Reuters content so far"*, the technical-report
  reference, the Hugging Face small open-weight academic/non-commercial version,
  and named quotes from Hasker, Hron and Professor Jonathan H. Choi. Matches.
  The proposal is also honest that the HF repo was **not** fetched in this run
  and that the job must locate it or report it not found — that is the right
  disclosure, not a gap.

## Docket discipline

All three carry `date`, kebab-case `slug`, a `type` from the closed list
(`post`, `entry`, `post`), a `summary`, a retrieval-dated `evidence` block, and
`expires: 2026-09-08` — exactly seven days out, the maximum for an event-driven
candidate, correct for all three. Each has a "Why now" section and a
"What the job would produce (done-when)" section with acceptance lines. No
missing fields on any candidate.

Three candidates filed, at the cap, not over it. The ranking is stated inside
the candidates rather than only in `RESULT.md` — the Tencent entry says "This is
ranked third because the two posts above carry angles no summary can produce",
which makes the order recoverable from the merged diff alone.

## Drop records

Four, in `data/proposals/dropped/`: Ant Ling-3.0-flash-Fin, Cohere Parse 5, IBM
Granite 4.2, Qwen3.8-Flash-Next. Each declares `failed_test` in front matter,
argues it in prose, and carries a "What would make it worth refiling" section
with concrete, dated refile conditions (weights landing on HF with a licence; an
independent benchmark; a licence-terms change). Each names a test and a refile
condition, so each is a record rather than a gesture. As the spec insists, I
read these as evidence of the *form* of the bar and not its rate.

## Repository-internal claims spot-checked

The drop records and proposals make several claims about this repository's own
state; I checked the cheap ones rather than taking them:

- `ls content/wiki/model/ | grep -iE "granite|hy4|qwen3-8"` printed
  `ibm-granite-granite-4-2-8b.md`, `qwen-qwen3-8-flash.md`,
  `qwen-qwen3-8-max.md`, `qwen-qwen3-8-2-4t-a95b.md` and
  `tencent-hy4-preview.md` — every stub the records name exists.
- `content/wiki/model/tencent-hy4-preview.md` is 35 lines of front matter with
  `timeline: []`, `mentions: []` and four feed-bound facts, and no body. The
  proposal's "the wiki stub is still empty of prose" is a measurement, not an
  assumption.
- `data/proposals/rejected/` contains only `README.md`, so no filed slug is an
  auto-discard re-tread, and none of the three slugs collides with a live
  proposal in `data/proposals/`.

## On the absent `RESULT.md`

The diff and the worktree both lack `RESULT.md`, which the brief requires. I did
not treat this as a defect: `git log --all --name-only -- RESULT.md` shows
commit `9bd57e6` *"loop: RESULT.md is scaffolding too — it leaked into main on
the first successful merge"*, so the loop strips it before computing the diff.
Its absence here is the mechanism working.

## What I did not do

I did not re-run `npm test` or `npm run build` — the loop ran both on
`ea4f35d59716` and both passed, and this diff adds seven markdown files under
`data/proposals/` and touches no code, no schema and no content the build
validates.
