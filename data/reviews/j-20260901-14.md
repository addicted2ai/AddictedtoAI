---
job: j-20260901-14
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that "deprecated" is not one fact but two — a vendor
  withdrawing an endpoint and a router still listing and pricing the weights —
  would link this Kimi K2.5 entry as the case where the two answers were
  visibly in tension on one page and the site said which signal it counts and
  why. The paragraph now names its own resolution rule (a cleared
  `expiration_date` derives `active`) instead of leaving a reader to guess why
  the badge disagrees with Moonshot's own 404 notice, which is exactly what a
  post about open-weight retirement being an endpoint event, not a model
  event, needs to cite.
carry:
  - title: The Kimi K2.5 annotation now describes a disagreement that no longer exists
    detail: >-
      data/changes.jsonl line 92 (annotation by job j-20260831-04, on the
      2026-08-31 status field change) still reads "the derived catalog row now
      reads `active` while the joined entry model/moonshotai-kimi-k2-5 …" and
      "the two surfaces now disagree about the same model". This diff removed
      that disagreement — the entry reads `active` too. The annotation is the
      published change-feed line a reader meets, so its live half should say
      what the site now holds: the router's cleared expiry is the signal of
      record and the vendor's sunset lives in the `api_sunset` fact and the
      timeline. A one-clause amendment, not a re-interpretation.
  - title: Reword the entry's opening "date has since come due" clause
    detail: >-
      Paragraph one still reads "this one's date has since come due" before
      paragraph two explains that OpenRouter cleared that very date, so a
      reader meets the death-date framing and the reversal in the wrong order.
      Nothing here is false — the vendor's sunset did fall due on 31 August —
      but "come due" describes the expiry field the router no longer carries.
      Naming it as the vendor's date in the first sentence ("Moonshot's date
      has since come due; the router's is gone") would remove the whiplash.
    subject: content/wiki/model/moonshotai-kimi-k2-5.md
subject: "content/wiki/model/moonshotai-kimi-k2-5.md"
reviewed:
  "content/wiki/model/moonshotai-kimi-k2-5.md": "90ae3cbeb912aaca2780f719b42a03c6f29e74fa8d6f0507b77873e4fcdbc6e0"
---

Reviewed the diff at `779eaf15bd5e`. Did not re-run `npm test` / `npm run
build` — the loop recorded both PASS on this commit. Ran only the checks that
would be red if a claim in the diff were false.

## The one-field edit is the right direction, and the derivation says so

The diff flips `status: deprecated` → `status: active` in the entry's front
matter and rewrites the paragraph that argued the vendor's page outweighed the
router's row.

The derived side, read from the worktree's committed
`data/derived/catalog.json` (lines 2412-2425):

```
      "entry_id": "model/moonshotai-kimi-k2-5",
      "expiration_date": null,
      "price_input": "0.00000045",
      "price_output": "0.00000225",
      "row_id": "moonshotai/kimi-k2.5",
      "status": "active"
```

So the two surfaces now agree, which is the stated outcome. It also confirms
the body's new claim that "the router keeps the row listed and priced" — the
row is present with both prices non-null in the latest derived catalog.

## "A cleared expiry reads `active`" is a claim about code, so I read the code

`pulse/lib/diff.mjs:115-124`:

```js
  if (rule.kind === 'expiration_date') {
    const expires = getPath(row, rule.path);
    if (!expires) return 'active';
```

Exactly what the paragraph asserts, verbatim in the branch. The body does not
claim OpenRouter *says* "active" — it says the catalog's status is derived from
the expiry field — which is the accurate framing, since `$status` is a
synthesized field (`"$status":"active"` in the snapshot excerpt on
`data/changes.jsonl` line 91), not something the router serves.

## The divergence date

`data/changes.jsonl` line 91:

```
{"date":"2026-08-31", … "expiration_date":null, … "field":"status",
 "new":"active","old":"deprecated","row_id":"moonshotai/kimi-k2.5"}
```

Supports "The two surfaces diverged on 31 August, when the router's data
dropped the date the vendor's page still names."

## The body's claim about where the vendor's sunset is still recorded

"recorded in the api_sunset fact and the timeline" — both present and
unchanged in the same file: the `api_sunset` cited fact (lines 59-64, value
"2026-08-31 — full platform sunset…", sourced to platform.kimi.ai) and the
timeline row dated 2026-08-31, "Moonshot's own API sunsets the model;
migration directed to kimi-k3" (lines 69-71). So the diff moves the vendor's
position out of the status field without dropping it from the page.

## Cross-references — nothing else now contradicts the page

Grepped `content/` for every mention of the model (6 files). The only
neighbouring status language is `content/wiki/model/moonshotai-kimi-k3.md:67`,
a dated timeline event calling K3 "migration target for the retiring Kimi
K2.5" — a statement about the vendor's migration, still true, and not a
front-matter status. `content/wiki/org/moonshot-ai.md` carries the same sunset
as a timeline event. No transclusion of this entry's `status` exists anywhere,
so nothing else moved.

`content/tutorials/openrouter-catalog-watch.md:189` shows
`2026-08-31  moonshotai/kimi-k2.5` under "rows with an expiration_date" — a
captured sample run output, dated as such, and out of this job's scope.

## Scope

`content/wiki/model/moonshotai-kimi-k2-5.md` (the target), the deletion of
`data/carried/j-20260901-12-carry-1.md` (which the brief explicitly requires
in this same diff), one proposal file, and the loop's own `.job/` artifacts.
Nothing wider. One proposal, at the cap.

## The proposal the author filed is correct, and I checked it

`lib/build-content.mjs` (the comment above `doc.currentStatus`, read on this
branch) does name this entry as its worked example of a reviewed body keeping
an authored status that "disagrees with the feed", saying its body "argues,
sourced, that Moonshot's own retirement notice outweighs a feed row whose
expiry OpenRouter has since cleared". After this diff that illustration is
false on the only page it names. The mechanism itself is untouched — the
`doc.hasBody ? doc.data.status : …` branch still does what the comment says —
so this is a stale example, not a broken rule, and filing it as a separate
`machinery` job rather than editing `lib/` inside a one-field repair is the
right call. I did not require it in scope for that reason.

## What I did not treat as a defect

Multiple prior records bind a `reviewed:` hash for this path
(`j-20260831-10`, `-11`, `-15`), so this edit leaves the piece mismatched
against them until the merge stamps a fresh hash. That is the normal merge
mechanic on this loop — `j-20260831-11` edited the same file after `-10` bound
a hash — not something this branch got wrong.
