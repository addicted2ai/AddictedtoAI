---
job: j-20260914-23
verdict: reject
reasons:
  - spec-violation
would-cite: >-
  An engineer deciding whether to keep calling nousresearch/hermes-4-70b
  through OpenRouter would link this model page to show the 70B row was
  withdrawn from that catalog while Nous still publishes the weights and the
  405B sibling still serves, so code should move to the sibling or another
  route rather than wait for the row to return.
---

Free-form notes: what I checked, fetched, ran, and observed.

Judgment: this branch's diff does not contain the job's stated outcome. The
outcome was already merged by j-20260914-15 (commit 5bbbc42, approved); this
job is a stale-queue duplicate and its diff is derived-only churn that the
merge strips, leaving an empty source diff. Discard; the substantive answer
stands on j-15's record.

Scope (diff judged: 25488ab...e9494bd, the same four files the loop's diff
shows):
- `.job/brief.md`, `.job/source.json` — loop mechanics, expected.
- `data/derived/backlinks.json` (+6: adds `/wiki/model/nousresearch-hermes-4-70b`
  as a backlink under `model/nousresearch-hermes-4-405b`).
- `data/derived/search-index.json` (1 line: `model/nousresearch-hermes-4-70b`
  `b: true -> false`, i.e. no longer stub).
- No edit to `content/wiki/model/nousresearch-hermes-4-70b.md` in this diff.
- No move of `data/vanished/openrouter-models--nousresearch-hermes-4-70b.md`
  in this diff (nothing remains to move; see below).

What the brief required (quoted):
- "Establish which one happened from the vendor's and the feed publisher's
  own sources before writing anything."
- "Do NOT remove the entry or its `feeds:` binding."
- "MOVE this file into `data/vanished/answered/`, unchanged, in the same diff
  as the fix. Do NOT delete it."

What the tree already holds (via ancestry, not this diff):
- `content/wiki/model/nousresearch-hermes-4-70b.md` (read in this worktree)
  already carries j-15's approved prose: row left the catalog without a date
  of its own, last seen 2026-09-09, absent from 2026-09-12 and 2026-09-14
  snapshots, departure recorded 2026-09-10; weights remain published under
  `NousResearch/Hermes-4-70B` with model card/FP8/GGUF, releases page still
  lists Hermes-4-Llama-3.1-70B 08/26/25 with no retirement note;
  openrouter.ai page answers 404 while the endpoints record returns the old
  record with `"endpoints":[]`; sibling 405B still in snapshots with one live
  Nebius endpoint; `feeds: openrouter-models: nousresearch/hermes-4-70b`
  kept (lines 10-11); `mentions: [model/nousresearch-hermes-4-405b]`.
  Byte-identical to the version j-15's reviewer source-checked on 2026-09-14
  (commit 5bbbc42 diff, quoted in full in this review's working notes).
- `data/vanished/answered/openrouter-models--nousresearch-hermes-4-70b.md`
  exists (read: front matter `source: openrouter-models`,
  `row_id: nousresearch/hermes-4-70b`, `last_seen: 2026-09-09`); top-level
  `data/vanished/` holds only `openrouter-models--openai-gpt-latest.md` and
  `README.md` (observed via Get-ChildItem). Nothing remains to MOVE.
- `data/derived/queue.json` in this branch still lists the hermes
  vanished-feed-row item (rank 85, last seen 2026-09-09), but that file was
  last written by pulse 4809999, which ran BEFORE j-15's merge (log order:
  4809999 pulse, then 45ebd4c, then 7906642 merge of j-15, then train jobs
  16-22, then this branch). The queue is stale; `vanishedRowItems` reads only
  top-level `data/vanished/*.md` (pulse/lib/queue.mjs:543-581) and
  `recordVanishedRows` never re-records a row named in `answered/`
  (pulse/lib/vanished.mjs:142-149), so the next pulse regenerates the queue
  without this item. The duplicate dispatch is a stale-queue window of the
  kind the HOLD-clearing note of 2026-09-14 already described, not new work.

Polarity: implements / ignores / inverts — this diff IGNORES the two
operative requirements (no page edit, no record move) because both are
already satisfied in its base; it does not invert them (binding kept, entry
kept, no deletion). An omission is still a failure to meet the brief in the
diff under judgment, hence `spec-violation` rather than approval. It is not
a `scope-violation`: the diff touches nothing outside the repair (only .job
scaffolding plus derived rebuild outputs, the same shape j-15's approving
review explicitly held to be not-widening). No reserved path is touched in
this diff vs train (verified via `git diff 25488ab...HEAD --stat`: 4 files,
quoted above).

Corroboration/agreement: the diff's only two substantive hunks (backlinks
+1 entry, search-index stub flag true->false) share one instrument — the
build's recomputation from the already-merged j-15 content edit (mentions
added, prose added so no longer stub; `b` is `Boolean(doc.index?.stub)` per
lib/search-index.mjs:109, observed). Their agreement is recorded as
agreement, never as corroboration. Both hunks are correct in isolation
(backlink correctly reflects `mentions:`, non-stub correctly reflects
prose present), but j-15's own merge dropped derived state before merging
(commit 4d70da5 "drop derived state before merge — recomputed from the
merged tree", observed via `git show --stat`), and no pulse has recomputed
since — so this diff's derived refresh will be stripped the same way,
leaving an empty source diff. The loop records empty source diffs as
`failed` (cf. j-20260914-12/14 "empty-diff failed" in the 2026-09-14
HOLD-clearing note). Merging a derived-only diff as `done` therefore buys
an empty merge; discarding the duplicate is the honest end.

Spot checks run in this review (quoted output):
- `git diff 25488ab...HEAD --name-only` printed exactly `.job/brief.md`,
  `.job/source.json`, `data/derived/backlinks.json`,
  `data/derived/search-index.json`.
- `git show 5bbbc42 --stat` confirmed j-15's merged content: the 47-line
  prose addition to `nousresearch-hermes-4-70b.md` plus the 100%-similarity
  move of the vanished record into `answered/`, plus derived rebuild.
- j-15's approving record `data/reviews/j-20260914-15.md` (train) was read;
  it source-checked vendor weights, vendor releases, router 404,
  router endpoints-empty, sibling endpoints-Nebius, and both snapshots on
  2026-09-14 with verbatim quotes. Same-day evidence; no refetch was needed
  to establish duplication, and this review makes no new sourced claim about
  the model beyond reporting that the page bytes are unchanged since that
  approval.
- Gates: loop reports `npm run build` PASS and `verify-surfaces` PASS on
  e9494bd; per the brief they were not re-run.

No blog post is in this diff, so the voice question does not arise. No
proposal and no carry entries: the stale-queue window is already diagnosed
in the train's own HOLD-clearing record, and there are no small corrections
(the page, binding, answered record and derived hunks are each correct where
they stand).
