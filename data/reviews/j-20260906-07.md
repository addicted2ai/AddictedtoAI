---
job: j-20260906-07
verdict: approve
reasons: []
would-cite: >-
  Anyone arguing that a refusal recorded in a data file is worthless until
  something joins it to the tree that can contradict it. This diff is the
  worked example: `declined_fields` was enforced against `material_fields`
  and against nothing else, so 48 fact bindings across 29 published model
  entries pointed at a path the registry formally declined and the build
  stayed green for a day. The link that is worth sending is
  `lib/declined-fields.mjs` plus `data/declined-binding-debt.json` together —
  a detector that fails on a 49th binding, and a dated shrink-only list that
  forgives the 48 without deciding them, with the test asserting the list as
  a ceiling (`<=`) so paying it down stays green and growing it does not.
carry:
  - title: Lower DEBT_CEILING_2026_09_06 in the same diff that pays debt down
    detail: >-
      `lib/declined-fields.test.mjs` enforces the shrink-only claim with
      `assert.ok(green.known <= DEBT_CEILING_2026_09_06)` against a literal
      `const DEBT_CEILING_2026_09_06 = 48`. That binds the debt to its
      2026-09-06 high-water mark, not to its current value: once
      `data/declined-binding-debt.json` is paid down — the filed proposal's
      done-when takes it to zero — the constant still permits up to 48 forgiven
      bindings, so the list could regrow to 48 with the suite green. The file's
      note says "This list has one legal direction, down", and today the
      mechanism enforces only "never above 48". Nothing is wrong as shipped
      (`known` is 48 and the ceiling is 48, so it is currently exact); the fix
      is a discipline the artifacts do not yet state — whoever retires debt
      lowers the constant to the new count in the same diff. One sentence in
      `data/declined-binding-debt.json`'s `note`, beside "This list has one
      legal direction, down", would make it a rule rather than a hope.
    subject: lib/declined-fields.test.mjs
---

Free-form notes.

The reviewed worktree (`j-20260906-07-review-1`) has no `node_modules`, so nothing
importable runs there. The author worktree `D:/addictedtoai-worktrees/j-20260906-07`
is at the identical commit `a7c6439a9df2` with `git status --porcelain` printing
nothing — byte-identical tree — and does have `node_modules`, so every measurement
below was taken there, from a script held outside both worktrees
(`%TEMP%/review-j07-check.mjs`). Nothing in either worktree was modified.

I did not re-run `npm test` or `npm run build`; the loop ran both on this commit.
I ran the changed check itself, four ways.

## 1. The shipped state — measured, and it is green only because of the debt

`declinedFieldsStep({ out })` against the committed registry and the committed
corpus, with the committed `data/declined-binding-debt.json`:

```
warning: data/declined-binding-debt.json: sources[openrouter-models].declined_fields[benchmarks.artificial_analysis]: known declined-binding debt — "benchmarks.artificial_analysis" is declined by source "openrouter-models" and is still bound as a fact 48 time(s) across 29 file(s), listed in data/declined-binding-debt.json. This list may only shrink. [declined-field-bound-debt]
prebuild: declined fields — 1 refusal(s) in data/sources/registry.json against 1816 feed-bound fact(s); 0 contradiction(s) (build error), 48 recorded as debt (warning; this number may only fall)
C: errors=0 known=48 scanned=1816 declined=1 stale=0
```

So: the build stays green on merge, the debt is the only reason, and it says so
on every build. `scanned=1816` is the coverage denominator actually being non-zero
— the check is not reporting `0 contradictions` because it never opened the corpus.

## 2. RED BEFORE — the same corpus with the debt emptied

`declinedFieldsStep({ debt: { known: [] } })`:

```
1 declined-field error(s):
  - data/sources/registry.json: sources[openrouter-models].declined_fields[benchmarks.artificial_analysis]: "benchmarks.artificial_analysis" is declined by source "openrouter-models" (decided_on 2026-09-05) and is bound as a fact by 48 content file binding(s): content/wiki/model/anthropic-claude-fable-5.md:facts.intelligence_index (benchmarks.artificial_analysis.intelligence_index), ...
B: distinct content files named = 29; binding mentions = 48
B: names registry path? true
B: names decided_on? true
```

The directive required the failure to name "both the registry entry and every
binding file". Counted, not eyeballed: 29 distinct `content/wiki/model/*.md` paths
and 48 `:facts.<field> (<path>)` mentions in the one message, plus the registry
path and the `decided_on` date. That is both ends.

## 3. THE GUARD RAIL, TESTED BY ATTEMPTING WHAT IT FORBIDS

The checklist item that matters most here is not "does it fail today" — the debt
makes it not fail today. It is "does a NEW binding fail". I appended one synthetic
document to the real corpus:

```js
{ file: 'content/wiki/model/reviewer-synthetic-new.md',
  data: { facts: [{ field: 'intelligence_index', source: 'feed',
                    feed: 'openrouter-models',
                    path: 'benchmarks.artificial_analysis.intelligence_index' }] } }
```

and ran the step with the **real, committed debt file** loaded:

```
D: threw. names the new file? true
D: files named in the error = content/wiki/model/reviewer-synthetic-new.md
```

It threw, and the error names the new file **and only** the new file — the 48
forgiven ones stayed on the warning side. That is the ratchet behaving as the
module claims, measured rather than read.

I also closed the obvious way around it. Silencing that 49th binding requires
appending its key to `data/declined-binding-debt.json`, which raises the forgiven
count from 48 to 49 — and `lib/declined-fields.test.mjs` asserts
`green.known <= DEBT_CEILING_2026_09_06` (48). I confirmed by measurement that
`known` counts forgiven hits that actually fire (48 today, `stale.length === 0`),
so a 49th forgiven binding trips that assertion and the route around the ratchet
is closed by the suite, not by a comment. The `<=` rather than `===` is the right
choice and the file argues it: equality would make paying the debt down a test
failure.

## 4. The join is a join, not a constant

- `pathsOverlap('benchmarks.aa.x', 'benchmarks.aa')` → `true`;
  `pathsOverlap('benchmarks.aa', 'benchmarks.aa.x')` → `true` (both directions);
  `pathsOverlap('benchmarks.demo_index.intelligence', 'benchmarks.demo')` → `false`
  (segment boundary, not string prefix). The ancestor direction is the one that
  carries all 48 real hits — the registry declines the block, every binding is of a
  leaf — so an equality-only test would have shipped a detector that detected
  nothing.
- `pulse/lib/registry.mjs` now **exports** the predicate rather than the new module
  copying it, so the two enforcement points of one refusal cannot drift. The diff to
  that file is the `export` keyword and comments; no behaviour changed.
- Source scoping holds: the fixture pair `bound-model.md` / `other-feed.md` binds
  the identical dotted path from two different feeds, and the tests assert the
  refusal reaches one and not the other in both directions.

## 5. Independent recount of the numbers the diff asserts

Not read off the debt file — recomputed from `corpus.all` with my own filter:

```
F: independent recount -> 48 bindings across 29 files; by field {"intelligence_index":29,"coding_index":11,"agentic_index":8}
```

29 + 11 + 8 = 48. That matches the debt file's 48 entries, the module header's
"48 fact bindings across 29 model entries", the prebuild comment, and the
proposal's evidence paragraph exactly. `data/sources/registry.json` carries exactly
one refusal, `{"source":"openrouter-models","path":"benchmarks.artificial_analysis","decided_on":"2026-09-05"}`.

## 6. The unit test file, run on its own

`node --test D:/addictedtoai-worktrees/j-20260906-07/lib/declined-fields.test.mjs`:

```
✔ a registry declining a path the corpus binds FAILS the build (25.7047ms)
✔ a registry declining a path nothing binds PASSES (4.2229ms)
✔ a registry with no refusals at all PASSES, and says it looked (3.1391ms)
✔ the exact bound path fires, and only the fact that binds it (0.2811ms)
✔ a refusal DEEPER than the binding fires too — the other overlap direction (0.1422ms)
✔ a string prefix that is not a path segment does NOT fire (0.1197ms)
✔ a refusal does not leak to another source's identical path (6.598ms)
✔ a cited fact is not a binding of anything a registry serves (0.6192ms)
✔ a recorded binding is forgiven with a warning; an unrecorded one still fails (2.9367ms)
✔ a debt covering every binding leaves the build green and warns (3.0802ms)
✔ a debt entry that has stopped firing is reported as removable (2.7446ms)
✔ the debt key is the file, the source, the refusal and the fact field (0.1322ms)
✔ the shipped debt may shrink but may not grow (467.2023ms)
ℹ tests 13  ℹ pass 13  ℹ fail 0
```

The mutation pair the directive demanded is the first two, and they are opposite
outcomes over one held-still fixture corpus: a registry declining
`benchmarks.demo_index` (which two fixture facts bind leaves of) fails, and one
declining `architecture.demo` (which nothing binds) passes with `declined === 1`
and `scanned > 0` asserted — so the passing half cannot pass by the refusal never
having been read.

## 6b. The step is actually wired to the build

`scripts/prebuild.mjs:88` opens `STEPS`, the `declined-fields` entry is inside it,
and `scripts/prebuild.mjs:171-173` is `for (const step of STEPS) { … await
step.run(); }` — called with no arguments, so the build takes the default path
(real registry, real corpus, real debt file) that measurement 1 above exercised.
That measurement is therefore the build's behaviour, not a lookalike.

## 6c. Two supporting claims in the diff, checked rather than taken

- The module treats **every** `declined_fields` entry as a refusal, ignoring
  `decision`. That is correct and not a shortcut: `pulse/lib/registry.mjs:105-110`
  throws unless `entry.decision === 'not carried'`, so there is no other legal
  value to distinguish. It also requires `decided_on` and a non-empty `note`,
  which is why the error message can promise a date.
- The header's justification for `checkReferences: false` names `arxivPinStep` and
  `anchorCheckStep` as precedent. Confirmed: `lib/anchors.mjs:193` and
  `lib/arxiv-pin.mjs:404` both pass `checkReferences: false` on their own corpus
  load. The new module follows an established pattern rather than inventing one.
- The debt file's shape is `{note, recorded, issue, known, repaid}` — the exact
  key set of `data/snapshot-census-debt.json`, so "in the manner of" is literal.
- `addictedtoai-226f`, named in the debt file's `issue` and in the proposal, is a
  real open P1 beads issue whose description matches what the debt file says it
  carries. Not a dangling reference.

## 6d. No blind spot in what the check scans

`corpus.all` is 668 documents spanning every content kind —
`{wiki: 550, learn: 38, tutorials: 4, blog: 14, directory: 35, deltas: 27}` — so
the join is not quietly wiki-only. Feed-bound facts happen to live only under
`content/wiki` today (1816 of them, 0 elsewhere), but a directory tool that bound
a declined path tomorrow would be caught.

## 6e. The proposal's sourced claims, confirmed against the document they name

The proposal's `evidence:` block attributes a set of figures to the registry's own
refusal note. I read `data/sources/registry.json`,
`sources[openrouter-models].declined_fields[0].note`, and it supports every one
verbatim: "between the 2026-09-04T06:00:03Z and 2026-09-05T06:00:04Z fetches, 165
of the 179 rows carrying the block changed at once, 147 directional moves went
DOWN and 0 went up … 181 values went number->null with 0 going null->number". The
Artificial Analysis citation and its "dated September 4, 2026" are likewise in
that note, and the proposal's revisit trigger is quoted exactly: "Revisit if
OpenRouter starts serving the index version and evaluation date alongside the
number, or if coverage stops collapsing on a rebase."

The proposal states plainly that it did **not** re-fetch the Artificial Analysis
article and that a job taking the work on should. That is the right disclosure
rather than a citation passed along as if checked, and the registry note itself
already calls that article "corroborating but not load-bearing".

`data/proposals/rejected/` holds one file and no slug collision with
`decide-the-48-declined-benchmark-bindings`. One proposal on the branch, typed
`repair`.

## 6f. RESULT.md makes the disclosure the directive demanded

`RESULT.md` is gitignored, so it is absent from the diff and from the review
worktree; it is present in the author worktree. First line `done`. The directive
required "If it would go red on merge, say so plainly in RESULT.md" — it does,
under a heading saying so and answering "**Yes.**", with the un-exempted failure
quoted. Its numbers (48 bindings, 29 files, 29/11/8 by field, 1816 feed-bound
facts) match my independent measurements exactly, and it reports its own real-tree
guard-rail probe (a 49th binding added to `z-ai-glm-5-2.md`, prebuild run,
`EXIT=1`, probe reverted) — which is the same experiment I reproduced in §3 by
injection rather than by editing a content file. It is also honest about what it
did not do: `verify-launch`/`verify-design`/`verify-surfaces`/`verify-analytics`
were not run, and it says why.

## 7. Scope

Files touched: `lib/declined-fields.mjs`, `lib/declined-fields.test.mjs`, two
fixture entries under `lib/fixtures/declined-fields/`, `pulse/lib/registry.mjs`
(one `export` + comments), `scripts/prebuild.mjs` (one `STEPS` entry + comment),
`data/declined-binding-debt.json`, one proposal, and `.job/` scaffolding. No
`content/` file, no `openspec/specs/`, no `data/config.json`, no `runners.yml`,
no `package.json`. The directive's two prohibitions are both respected: the
registry declaration is untouched and none of the 48 bindings was removed. One
proposal file, typed `repair` — not `machinery`, so it does not trip the
self-amplification rule.

The build going green rather than red on merge is the debt list doing exactly what
the directive authorised ("add the existing 48 to an explicit, dated, shrink-only
exemption list in the manner of `data/snapshot-census-debt.json`"), and it is not a
silent pass: it prints a warning naming the count, the source and the refusal on
every single build.
