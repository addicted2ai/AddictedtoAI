---
job: j-20260831-13
verdict: approve
reasons: []
would-cite: >-
  Nobody links a four-word pronoun repair on its own; the piece it lives in is
  what gets linked — by someone arguing that an open-weight release can be
  downloadable worldwide and licensed for only part of it, who needs the clause
  numbers to hand (V.4 bars extraterritorial use, VIII.2 terminates on breach,
  II holds the application offer) rather than a paraphrase of "restrictive
  licence".
reads-human: >-
  The one changed sentence is the bluntest in the section — a bare subject and
  a metaphor, no hedge, no signposting — and replacing the pronoun with the
  clause number makes it blunter rather than more formal. It reads like an
  editor with the licence open, not like a model smoothing a transition.
subject: "content/blog/minimax-h3-licence-excluded-territories.md"
reviewed:
  "content/blog/minimax-h3-licence-excluded-territories.md": "e953f071cfef0d7a894cd4352448ea276d29ec39a82f146b642cdb29a561e4ba"
---

Free-form notes.

**What the diff is.** One prose line in
`content/blog/minimax-h3-licence-excluded-territories.md`
(`That sentence is also the door.` → `Section V.4 is also the door.`) plus the
deletion of `data/carried/j-20260831-12-carry-1.md`, which is what the brief
says retires the queue item. `.job/brief.md` and `.job/source.json` are the
loop's own scaffolding, not the author's work.

**Scope.** `git show --stat HEAD` on the reviewed commit `55db703b3955`:

```
 content/blog/minimax-h3-licence-excluded-territories.md |  2 +-
 data/carried/j-20260831-12-carry-1.md                   | 16 ----------------
 2 files changed, 1 insertion(+), 17 deletions(-)
```

That is the stated outcome and nothing else. `git status --short` printed
nothing — the tree is clean, so there is no uncommitted work the diff omits.

**Does the fix actually name the right clause?** Read the whole file rather
than the hunk. The block quotation the old pronoun pointed at is at line 24 and
is introduced at line 22 as `Section V.4 states the other side:`. Between it
and the repaired sentence sit two paragraphs (lines 26 and 28), the second
about 180 words, so the antecedent was genuinely out of reach; line 26 also
opens `That sentence is the one that lands on an existing download`, so the
same demonstrative was doing two different jobs in one section. Naming V.4
resolves both. The substituted noun phrase preserves the original referent
exactly — it disambiguates, it does not re-point the sentence at something
new, which is the failure mode this kind of edit usually has.

The sentence's argument still holds with the clause named: V.4's second
sentence is `Any such use outside the Applicable Territory is not authorized by
this Agreement`, and *this Agreement* is precisely the hinge the following
Section II quotation swings on (another licence can authorize what this one
does not). So "Section V.4 is also the door" is the same claim it was, with a
referent a reader can now find.

**Source check on the clause number.** The only claim the edit newly asserts in
its own right is that the quoted extraterritorial-use language is Section V.4.
Fetched the raw licence — `huggingface.co/MiniMaxAI/MiniMax-H3/raw/main/LICENSE`
— on 2026-08-31. What it returned for clause V.4, verbatim:

> **V.4** "You may not use, reproduce, modify, distribute, or display the
> MiniMax H3 Works or any of their Outputs or results outside the Applicable
> Territory. Any such use outside the Applicable Territory is not authorized by
> this Agreement."

That is character-for-character the block quotation at line 24 of the post,
under the number the repaired sentence now names. The fetch also returned V.1,
V.2, V.3 and V.5, none of which carry that language, so V.4 is the clause
uniquely, not merely plausibly. (The retrieval was through WebFetch, whose
extracted prose is not evidence on its own; I am relying on it only for an
exact-string match against text the post already quotes, which is the one thing
it cannot invent in the post's favour.)

**What I did not re-run.** `npm test` and `npm run build` were run by the loop
on this exact commit and both passed; the brief records that. A one-line prose
substitution inside an existing paragraph changes no front matter, no id, no
link and no transclusion, so there is no build-level claim in this diff for a
targeted check to falsify. The check that would be red if this fix were wrong
is a human reading the section for the antecedent, which is what I did.

**Noted, not blocking, and not carried:** the preceding paragraph already ends
`What the document does say about the excluded territories is the offer in
Section II, which follows`, so the section opens by naming V.4 as the door and
then quotes Section II. That small tension predates this diff — it is the
shape the previous revision left — and the minimal repair correctly did not
widen into it.
