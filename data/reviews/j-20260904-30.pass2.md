---
job: j-20260904-30
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a vendor's own about page uses two different noun phrases
  for the same thing — "diffusion-based language models" in its one-line
  self-description and "diffusion-based large language models" two paragraphs
  later — would link Inception Labs' entry to show a corpus that quotes the
  longer phrase verbatim rather than harmonising the two into a phrase the page
  never wrote. It is the entry a reader lands on when they want to know whether
  Mercury's "dLLM" label is the vendor's word or the site's.
proposal:
  slug: review-diff-must-match-the-commit-it-names
  type: machinery
  date: 2026-09-04
  summary: >-
    The diff embedded in a review brief must be computed from, and be verifiable
    against, the exact commit the brief names as the head it was computed from.
    This pass-2 brief stated the diff was computed from 949075cdee91 and that the
    gates were run on that commit, but the diff it printed showed the tree at
    51a738c93b69 — the pre-revision state. The single content hunk it displayed
    was the change the previous review had ordered reverted, and the revert
    commit was invisible. Add a check in the loop's review-brief assembly that
    the printed diff's head equals the named commit (and that the gate result
    quoted alongside it names the same commit), failing brief generation rather
    than emitting a mismatch.
  evidence: >-
    This brief's header: "computed from commit 949075cdee91". Its printed hunk
    for content/wiki/org/inception-labs.md: "index fb4f338..e1c0d38", removing
    "large". `git show 949075c -- content/wiki/org/inception-labs.md` in this
    worktree prints the opposite hunk, "index e1c0d38..fb4f338", restoring it;
    `git diff 1ed6414..949075c --stat` shows that file absent from the branch
    diff entirely. The brief's own carried pass-1 notes say the gates ran on
    "51a738c93b69", contradicting its header. A reviewer judging only the printed
    diff would have returned a second non-approval on a branch that had already
    made the required change, discarding the job.
---

Delta review, pass 2. The previous verdict was `revise` on
`intent-not-measurement` + `false-or-unsupported-claim`, with three named
required changes. All three are met, and the branch's net effect on the corpus
is now nil — which is the correct outcome, because the finding this job was
created to fix was not real.

**First, the diff I was handed does not match the branch.** The brief says the
diff was computed from `949075cdee91`. It is not. Its one content hunk is
`content/wiki/org/inception-labs.md`, `index fb4f338..e1c0d38`, removing
"large" — the pre-revision state at `51a738c93b69`. What the branch actually
holds at `949075c`:

```
$ git -C .../j-20260904-30-review-2 show 949075c -- content/wiki/org/inception-labs.md
    job j-20260904-30 (repair): restore the verbatim page phrase the review requires
--- a/content/wiki/org/inception-labs.md
+++ b/content/wiki/org/inception-labs.md
-    value: "diffusion-based language models (dLLMs); it calls Mercury '...'"
+    value: "diffusion-based large language models (dLLMs); it calls Mercury '...'"
```

Blob `e1c0d38` → `fb4f338`: the file returns to exactly the blob the diff's own
hunk header names as the *pre-job* state. Confirmed against the working tree —
line 15 of the file reads `diffusion-based large language models (dLLMs); …`.
The full branch diff against `main`:

```
$ git diff 1ed6414..949075c --stat
 .job/brief.md                         | 623 +++++++++++++++++++++++++++++++
 .job/source.json                      |   8 +
 data/carried/j-20260904-28-carry-1.md |  16 -
 3 files changed, 631 insertions(+), 16 deletions(-)
```

`content/wiki/org/inception-labs.md` is not in it. Had I judged the printed
diff, I would have returned the second non-approval that discards the job, on
work that had already complied. Filed as the proposal above.

**Required change 1 — restore the verbatim phrase. Met, and I verified the
source myself rather than taking the pass-1 record's word for it.**

```
$ curl -sL -o incept-about.html -w "%{http_code} %{size_download}\n" https://www.inceptionlabs.ai/about
200 287510
```

Same byte count the pass-1 record and the j-20260904-28 record report, so this
is the same served document. Substring search of the raw markup — not an
extractor — for both candidate phrasings, printing every occurrence (run through
the harness's ripgrep-backed Grep tool, flags as shown):

```
rg -io "diffusion-based (large )?language models" incept-about.html
144:diffusion-based language models
144:diffusion-based large language models
```

Both, one each. And each is the tail of a complete body sentence:

```
rg -o "next generation of diffusion-based large language models\.|building diffusion-based language models for production applications\." incept-about.html
144:building diffusion-based language models for production applications.
144:next generation of diffusion-based large language models.
```

So `diffusion-based large language models` is a contiguous, unmodified phrase of
the cited page, and the restored fact value quotes it. The carried finding's
claim that the page "uses 'diffusion large language models (dLLMs)' only in the
separate Mercury sentence" is false; nothing was welded. The pass-1 review
reached this by measurement and the branch has complied with it.

**Required change 2 — keep the carried file deleted. Met.**
`data/carried/` no longer contains `j-20260904-28-carry-1.md` (eleven carry
files remain, none of them this one), and the deletion is committed on the
branch. The item is retired, which is right: a finding that turns out to be
mistaken is finished, and leaving the file would re-queue it next Pulse run.

**Required change 3 — record the contradicted premise in `RESULT.md`. Met.**
`RESULT.md` is not in the diff and is not in this review worktree, so I read it
from the author worktree (`D:/addictedtoai-worktrees/j-20260904-30/RESULT.md`)
to check a required change I could not otherwise verify. I am disclosing that
because it is the one thing I looked at outside the diff. It records the
finding as a finding — quoting *"We're a team of scientists, engineers, and
builders focused on developing the next generation of diffusion-based large
language models."* as the sentence that contradicts the premise, naming the
287510-byte fetch, and concluding "the repair this job was created to perform
did not exist". That is what the ground rules ask for and what the pass-1
review required.

**Scope.** Three files: the loop's own `.job/` scaffolding from `a8e65da`, and
the carried file the brief explicitly names for deletion. No reserved path. No
content change at all in the merged state.

**What I deliberately did not do.** I did not re-run `npm test` or
`npm run build`; the loop reports both PASS on this commit, and the merged tree
restores `content/wiki/org/inception-labs.md` to a blob that was already in a
passing build. The one substantive claim in this branch is about what the cited
page says, and I ran the check that would be red if it were false. It was
green — twice, for both phrasings.

**Noticed, deliberately not carried.** The fact value attaches `(dLLMs)` to
`diffusion-based large language models`, while the page introduces `(dLLMs)`
against `diffusion large language models` (no hyphen, no "-based") in the
Mercury sentence. That is a real hairline imprecision. I am not carrying it:
the pass-1 review examined this exact string, named it as the acceptable
resolution in its required change, and the corpus has now spent two jobs and
four invocations on one word of one fact. A carry would start a third. If it is
ever worth fixing, it is worth fixing as part of a rewrite of the clause, not as
another single-word repair — and the honest reading is that the page uses both
forms loosely for the same thing, which is what the fact conveys.
