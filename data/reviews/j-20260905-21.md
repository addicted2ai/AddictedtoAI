---
job: j-20260905-21
verdict: approve
reasons: []
would-cite: >-
  Whoever next argues about whether a machine-maintained field's disappearance
  belongs in the changed feed would link the "recommendation that came back with
  the ratification" section of this proposal: it is the only place in the tree
  where an offered feature is declined with three checkable measurements
  attached — the registry's own dated refusal of the same block, a counted 71
  lines against a 182-line history, and the demonstration that the lines would
  cost no model budget, followed by the author declining anyway because cheap is
  not the same as worth reading. A later implementer of `domains_seeded` who is
  tempted to add the emitter would land here first, and the same passage is the
  worked example for the `declined_fields` rule in `data/sources/registry.json`
  being consulted by a code path that would otherwise never read it.
carry:
  - title: Say that 3 of the 71 counted transitions are vanished rows, not vanished fields
    detail: >-
      proposal.md counts "71 lines" as 69 `agentic_index` plus 2 `coding_index`
      number→absent transitions, and I reproduce those two figures exactly from
      `previous.json` (2026-09-04T06:00:03.738Z, 427 rows) and `latest.json`
      (2026-09-05T06:00:04.599Z, 431 rows). But 3 of the 71 are rows that left
      the snapshot entirely rather than rows that dropped the field:
      `qwen/qwen3.8-max` (carried both indices) and `ibm-granite/granite-4.1-8b`
      (carried `coding_index`). Excluding those, 68 surviving rows lost
      `agentic_index` and **zero** surviving rows lost `coding_index` — a
      vanished row is handled by the separate last-known-value/repair rule in
      `specs/wiki`, not by a seeding-signal disappearance. Same instrument shows
      the "2 gained it" on `coding_index` are the two new arrivals
      `openai/gpt-6-astra` and its `:batch` twin, not existing rows regaining a
      value, so the parenthetical "2 rows lost `coding_index` and 2 gained it —
      so even the stable row is not stationary" is row churn described as field
      churn. The conclusion is untouched (68/182 is still two fifths), and the
      draft already flags that instruments differ; the fix is one clause — "71
      number→absent transitions, 3 of them rows that left the snapshot
      altogether" — in proposal.md's finding 2 and in the matching sentence of
      `specs/wiki/spec.md`.
    subject: openspec/changes/tag-the-corpus-by-domain/proposal.md
---

Free-form notes.

**Scope.** `git diff --stat 56bd3a4 0e86ee0` touches four files, all inside
`openspec/changes/tag-the-corpus-by-domain/`: `proposal.md`,
`specs/directory/spec.md`, `specs/wiki/spec.md`, `tasks.md`. No `lib/`, no
`pulse/`, no `openspec/specs/`, no archive. Task boxes: `grep -c "^- \[x\]"`
prints `1` and `^- \[ \]` prints `14` — task 14 was already the drafting gate,
so nothing was ticked, as the brief required. `openspec validate
tag-the-corpus-by-domain --strict` → `Change 'tag-the-corpus-by-domain' is
valid`.

**The ratifications, checked against the order rather than against the brief.**
`loops/ui-loop/graph/knowledge/DESK-ORDER-001.md:165-181`, "Amendments from the
1hjf draft review (2026-09-05)". Every quotation the diff attributes to it is
verbatim:

- `:176-178` — "the facet is optional on EVERY wiki kind (widened from §3's
  list; a `benchmark` entry for FrontierMath is `science-math` by any reading;
  one `entrySchema` for all kinds means restricting is more machinery, not
  less). Both tool sets carry it: directory listings and wiki tool entries."
- `:173-175` — the three field names, their roles, "rendered set = (seeded ∪
  domains) − excluded; a gate fails an excluded value that is in neither seeded
  nor domains."
- `:167` — append-only seeding "ratified under K40 as K47", and `:171-172` the
  recommendation left to the author, worded as the brief says it is:
  "Recommended, author's call".
- `:181` — "Counts: 544 wiki entries (545 files incl. the README); 14 blog
  posts (15 files incl. the README)."
- `:81` — the coverage recount, quoted in the proposal's item 4 word for word.

The proposal's own item 4 also states the numbers behind that recount, and they
are independently reproducible: over `latest.json` I count 431 rows, **265**
carrying at least one of `image`/`video`/`audio` across
`architecture.input_modalities` + `output_modalities` and **166** carrying none,
and **181** rows carrying a `benchmarks.artificial_analysis` block. All three
match the order and the draft.

**The superset check, re-run rather than taken on trust.** I split the live
`### Requirement: No placement is ever sold` in
`openspec/specs/directory/spec.md:76` on blank lines and matched each unit
against the MODIFIED block in the change's `specs/directory/spec.md:92`. Result:
`live units 10 | modified units 19` (unit 0 is the heading), **9 of the 10
byte-identical**, i.e. 8 of the 9 non-heading units — exactly the claim. The one
that differs is the opening paragraph, and whitespace-normalised the difference
is one inserted word:

```
... only (name, date, price, status, category). This ...
... only (name, date, price, status, category, domain). This ...
```

Nothing in the live requirement is dropped. The three added lines in the
directory delta assert this and point at the proposal for the method; both hold.

**The declined recommendation, which is the substantive new judgment.** The
three findings the proposal rests it on:

1. `data/sources/registry.json:63-69` carries `benchmarks.artificial_analysis`
   under `declined_fields` with `"decision": "not carried"` and
   `"decided_on": "2026-09-05"`, and the note contains, verbatim, "Not a column,
   not a fact, not an event", "181 values went number->null with 0 going
   null->number", and "56 of the carrying row ids are `:batch`/`:free` twins of a
   canonical_slug already counted, so a per-row line would emit that one act more
   than once per model." The principle attributed to `pulse/lib/diff.mjs:377-378`
   is there and reads "...this asks the registry the same question `diffSnapshots`
   asks, / so a field is an event in one place or in neither."
2. The volume. `wc -l data/changes.jsonl` → **182**, the figure both this change
   and the `separate-a-claim-from-a-fact` delta state. Over the committed
   snapshot pair (`previous.json` `fetched_at` 2026-09-04T06:00:03.738Z, 427
   rows; `latest.json` 2026-09-05T06:00:04.599Z, 431 rows) I measured:

   ```
   agentic_index      prev# 166  cur# 99   num->absent 69
   coding_index       prev# 181  cur# 181  num->absent  2
   intelligence_index prev# 164  cur# 52   num->absent 114
   ```

   So 166→99 reproduces, the flat 181→181 reproduces, 69 + 2 = the claimed 71,
   and 69 + 2 + 114 = **185**, the exact figure the proposal gives for its
   "number→absent over every row in the earlier snapshot" instrument next to the
   registry's 181. The one imprecision is the `carry:` above: 3 of the 71 are
   rows that left the snapshot, and the 4 "absent→number" rows are new arrivals
   (`openai/gpt-6-astra`, `openai/gpt-6-astra:batch`), not existing rows regaining
   a value.
3. `pulse/lib/diff.mjs:110` declares `INTERPRET_FIELDS` as exactly
   `['price_input', 'price_output', 'price', 'license', 'licence', 'status']` and
   `:399` is `if (!INTERPRET_FIELDS.has(l.field)) return false;` inside
   `uninterpretedChanges`. No domain-seeding field is a member, so the claim that
   these lines would queue no `interpret` work and consume no budget is correct —
   and the draft does not use that as a reason to accept them, which is the part
   the brief asked to be weighed rather than defaulted.

The decision is written where the brief required it (proposal.md, "The
recommendation that came back with the ratification, and the decision on it"),
the prohibition is a rule in the `wiki` delta rather than an omission, the
"what is genuinely lost" paragraph names the cost instead of waving it away and
points at `data/derived/` without building it, and task 12 turns the decision
into an assertion in a test rather than leaving it a sentence.

**The new requirement.** The `domains_excluded` gate replaces the previous
"legal and inert" rule, and the delta answers the objection the old rule was
built on: the gate reads `domains_seeded` and `domains` from the front matter of
the file being validated, never the snapshot, and that is only safe because
seeding is append-only — which K47 now ratifies. The consequent ordering
constraint (an exclusion cannot precede the seed it suppresses) is stated in the
delta rather than left to be discovered, the scenario is flipped to match, and
tasks 2, 8 and 9 move together, including the new control in task 9 for the
legal exclusion so task 8's refusal tests staleness rather than the key's
existence. `lib/domains.mjs` does not exist in this tree, consistent with task 1
of `flag-what-moved-the-frontier` being named a hard prerequisite.

**Prose.** The four "not settled" items became "what the reviewing session
settled" with the original doubt kept beside each outcome, which is the right
shape — a decision recorded without the doubt it resolved invites the
re-litigation the section exists to prevent. The declined-recommendation section
argues rather than announces, and its third finding deliberately refuses the
cheap argument in its own favour. This is worth a reader's time.

Not re-run: `npm test` and `npm run build`, which the loop reports PASS on this
same commit. I ran only the checks that would be red if a specific claim in the
diff were false.
