---
job: j-20260907-16
verdict: revise
reasons:
  - false-or-unsupported-claim
  - intent-not-measurement
would-cite: >-
  Not applicable until the existing 2026-09-05 Qwen retirement line is corrected
  or the changed feed is otherwise made to state the measured substitution.
---

Initial judgment: revise. The committed `data/changes.jsonl` already contains
the Qwen departure under the old `$retirement` key. The new code's append-only
dedupe preserves that line, so the reader-facing changed feed still says
`retired`; the new classification only affects a future departure that is still
present in `previous.json`. The diff's fresh-fixture test does not measure this
existing-history case. Required change: correct the already-recorded event (or
add an append-only correction that the changed feed actually consumes) and add a
targeted regression against the committed snapshots/history proving Qwen no
longer renders as retired while the Granite withdrawal remains a retirement.

Checks and observations:

- `node --test lib/change-kinds.test.mjs lib/changes.test.mjs pulse/tests/diff.test.mjs`
  produced 14 passing assertions, then failed before loading the Pulse test file:
  `Error [ERR_MODULE_NOT_FOUND]: Cannot find package 'cheerio' imported from ...\pulse\lib\sources.mjs`.
  `node_modules` is absent; I did not install or alter it. `node --check` passed
  for all three changed implementation modules.
- The direct reader check over the committed history printed:
  `{"kind":"retirement","title":"Qwen: Qwen3.8 Max","detail":"retired","key":"openrouter-models|554be452279cdebe|380c23a4c00dcada|qwen/qwen3.8-max|$retirement"}`.
  A snapshot check printed:
  `{"previous_has_old":false,"latest_has_old":false,"latest_has_successor":true,"existing_kind":"retirement","existing_key":"openrouter-models|554be452279cdebe|380c23a4c00dcada|qwen/qwen3.8-max|$retirement"}`.
  Thus a subsequent Pulse run cannot recompute that departure from the current
  snapshots, and the existing key prevents the new candidate from being
  appended even if it were supplied.
- The cited source check is directionally supported: opening
  `https://openrouter.ai/qwen/qwen3.8-max` redirected to
  `https://openrouter.ai/qwen/qwen3.8-max-0902`, whose page calls it “an updated
  snapshot of Qwen3.8 Max”; a direct fetch of the cited Alibaba notice returned
  HTTP 200 and its text matched both “automatically transition” and
  “billing items ... pricing”; Alibaba's official model-release search result
  also describes `qwen3.8-max-0902` as an upgraded snapshot of `qwen3.8-max`.
- A direct count over `data/sources/openrouter-models/latest.json` printed
  `{"rows":430,"dated":222,"valid":222,"invalid":[],"tail4":28,"tail2":42,"tail1":6,"colon":87,"sharedExactly2":74}`.
  The diff/registry note says 78 canonical slugs are shared by exactly two rows
  and that every duplicate is a base plus `:batch`; the actual distribution is
  270 singletons, 74 two-row slugs, and 4 three-row slugs (the latter include
  `:free`). Correct the measurement and its conclusion, or rerun it against the
  intended snapshot.
- Executing the added suffix pattern showed
  `[["-20260231",true],["-20261301",false],["-20260031",false],["-20260902",true]]`.
  The implementation claims a real calendar date but accepts the invalid
  February 31 suffix; validate month/day combinations or narrow the claim and
  tests. The changed renderer also throws for malformed non-array successors:
  `{}` and `"bad"` both produced `TypeError: (line.successors ?? []).map is not a function`.
  Either make that reader-side fallback safe or remove the claim that malformed
  substitution lines remain readable.
