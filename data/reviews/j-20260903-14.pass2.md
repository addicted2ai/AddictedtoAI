---
job: j-20260903-14
verdict: approve
reasons: []
would-cite: >-
  Anyone arguing that "open weights" has become a marketing posture rather than
  a shipped artefact would link this entry, because it is now the rare place
  where the claim is pinned to the vendor's own words on the vendor's own
  channel and dated: Meta's 1.3 announcement of 2026-09-02 puts "the Muse Spark
  open weights release" on a roadmap, five months after the April post said
  Meta "hope[s] to open-source future versions," with nothing downloadable in
  between. A reader writing about Meta's retreat from frontier open weights, or
  about how model catalogues badge a licence that no licence text carries, can
  send this and have both the primary source and the catalogue's own
  self-contradiction in one place.
proposal:
  slug: reanchor-muse-spark-entries-on-meta-research-blog
  type: repair
  date: 2026-09-03
  summary: >-
    A repair job that re-sources the Muse Spark model entries on Meta's own
    announcement channel. This job discovered that research.meta.ai carries
    per-release Meta announcement pages for the Spark line
    (/blog/introducing-muse-code-and-muse-spark-1-2 and
    /blog/introducing-muse-spark-1-3, both HTTP 200 on 2026-09-03) which the
    corpus did not know existed. The job cited the 1.3 page for one fact on
    org/meta-superintelligence-labs; that is now the single occurrence of
    research.meta.ai in the entire content tree. The repair would add the
    matching Meta-side source to content/wiki/model/meta-muse-spark-1-3.md and
    its siblings, and to the org entry's 2026-08-05 and 2026-09-02 timeline
    rows, so the Spark family's release facts do not rest solely on a
    third-party catalogue that this same review has shown contradicts itself.
  evidence: >-
    Grep over content/ for "research.meta.ai" returns exactly one occurrence —
    the fact this job just added. content/wiki/model/meta-muse-spark-1-3.md
    carries source URLs on only two hosts, llm-releases.com (5) and
    openrouter.ai (3); no Meta-side source at all. Meanwhile
    https://research.meta.ai/blog/introducing-muse-spark-1-3 fetched HTTP 200
    for me on 2026-09-03 with the dateline "September 2, 2026", and
    https://llm-releases.com/changelog is the source the org entry itself
    already cites as contradicting its own Spark licence badge.
carry:
  - title: Make the "fetched twice" note match the transcript it annotates
    detail: >-
      The evidence file's header says the raw transcript is a "node
      fetch+strip+count script, 2026-09-03; the huggingface.co/meta-models org
      page was fetched twice because its activity feed renders intermittently".
      Grepping "^=== https" over
      data/reviews/evidence/verify-meta-spark-weights-pending-status.raw.txt
      returns 11 fetch blocks and huggingface.co/meta-models appears exactly
      once (line 22). The underlying observation is real — that page's stripped
      length was 2556 in the transcript and 2548 when I fetched it myself
      minutes later — but as written the sentence describes a second fetch the
      transcript does not contain. Either capture both fetches, or say it
      plainly ("the page was fetched in both this run and the earlier one and
      rendered differently; only this run's is captured"). This is the same
      narrative-outruns-transcript shape the previous pass flagged, at much
      smaller scale, which is why it is carried rather than blocked on.
    subject: data/reviews/evidence/verify-meta-spark-weights-pending-status.md
subject: "content/wiki/org/meta-superintelligence-labs.md"
reviewed:
  "content/wiki/org/meta-superintelligence-labs.md": "8ec124cca30eda5d1a70a130e8441120699c2aeea18183966b07dc7f9539346d"
---

Delta review. Previous verdict: `revise` on `false-or-unsupported-claim`, with
three required changes. All three landed. I checked each against the tree and
against the sources, and I re-fetched every instrument the revision added.

## A note on the diff I was handed

The diff in my brief is stale on the one line the previous verdict was about.
It shows `flagship_weights_listing` gaining `— a badge name that no Meta
license text carries`, and its `raw.txt` block is the 46-line pre-revision
capture. The brief states the diff was computed from `8f4ff1e19190`; it was
not. I reviewed the tree instead. `git log --oneline -5` in the review
worktree:

```
8f4ff1e job j-20260903-14 (verify): revision — restore listing value, capture all license-check instruments
d16d4a4 job j-20260903-14 (verify): anchor Muse Spark weights state on Meta's own channels
d6a3be4 job j-20260903-14: brief
```

`git status --short` is empty, so the checkout is exactly `8f4ff1e`. The real
delta (`git diff d16d4a4 8f4ff1e --stat`):

```
 content/wiki/org/meta-superintelligence-labs.md    |  2 +-
 .../verify-meta-spark-weights-pending-status.md    | 26 ++++++----
 ...erify-meta-spark-weights-pending-status.raw.txt | 60 ++++++++++++++--------
 3 files changed, 56 insertions(+), 32 deletions(-)
```

Everything below is judged against that.

## Required change 1 — the unsupported clause: removed

`git diff d16d4a4 8f4ff1e -- content/`:

```
   - field: flagship_weights_listing
     source: cited
-    value: "listed 'Open weights · Meta license (weights pending)' with weights 'Not released' — a badge name that no Meta license text carries"
+    value: "listed 'Open weights · Meta license (weights pending)' with weights 'Not released'"
     source_url: "https://llm-releases.com/models/muse-spark-1-3"
```

That is the entire content change, and it restores the value to the badge
quotation alone. I fetched the cited page myself (raw `fetch` + tag strip +
literal substring count, no WebFetch extractor), 2026-09-03:

```
=== https://llm-releases.com/models/muse-spark-1-3 HTTP 200 RAW 43051 STRIPPED 2125 ===
TERM "Open weights · Meta license (weights pending)" hits=1
  ...Official page ↗ Specifications License Open weights · Meta license
  (weights pending) Weights Not released Architecture unknown...
TERM "Weights Not released" hits=1
```

Both halves of the value are now exact quotations from the page the fact
links, and nothing else is asserted. The transclusion at
`content/wiki/org/meta-superintelligence-labs.md:88` reads back as "was the
first Spark flagship listed 'Open weights · Meta license (weights pending)'
with weights 'Not released'; the card added that 'Meta has signaled open
weights are coming.'" — the em-dash-into-semicolon collision the previous pass
described is gone with the clause.

## Required change 2 — the universal negative: bounded

The finding was kept, but moved out of the fact value and into the evidence
file, and rewritten from a universal to a bounded claim. It now reads "No
license text of that name was found on the checked instruments (all fetched
2026-09-03):" followed by the five instruments and the date. The prior wording
was "No Meta license text carries that name." This is the correction that was
asked for, and it is stated where it can carry its own transcript rather than
inside a value whose only link is the catalogue card.

## Required change 3 — the transcript now matches the narrative

The pre-revision `raw.txt` held 7 fetches; four instruments named in the
narrative were absent from it. The new `raw.txt` holds 11. Grepping
`^=== https` over the file:

```
1:  research.meta.ai/blog/introducing-muse-code-and-muse-spark-1-2   STRIPPED 5535
8:  research.meta.ai/blog/introducing-muse-spark-1-3                 STRIPPED 10663
17: about.fb.com/news/2026/04/introducing-muse-spark-...             STRIPPED 14299
22: huggingface.co/meta-models                                       STRIPPED 2556
27: llm-releases.com/models/muse-spark-1-3                           STRIPPED 2130
34: llm-releases.com/models/muse-spark-1-2                           STRIPPED 2121
41: huggingface.co/models?search=muse+spark                          STRIPPED 1742
45: huggingface.co/meta-models/Muse-Glimmer-30B                      STRIPPED 89683
51: huggingface.co/models?other=meta-license                         STRIPPED 1904
57: llm-releases.com/methodology                                     STRIPPED 2674
61: huggingface.co/meta-llama                                        STRIPPED 102073
```

Lines 45, 51, 57 and 61 are the four that were missing. I fetched all four
myself rather than taking the capture on trust:

```
=== https://huggingface.co/meta-models/Muse-Glimmer-30B HTTP 200 STRIPPED 90424 ===
TERM "license:apache-2.0" hits=8
  ..."tags":["transformers","safetensors","muse_glimmer",...,"license:apache-2.0",...
TERM "meta-license" hits=0
=== https://huggingface.co/models?other=meta-license HTTP 200 STRIPPED 1904 ===
TERM "meta-license" hits=2   (the filter chip only — no model rows)
TERM "Muse" hits=0    TERM "Spark" hits=0
=== https://llm-releases.com/methodology HTTP 200 STRIPPED 2674 ===
TERM "known family" hits=1
  ...Does the license resolve to a known family? Each check carries a severity
  and feeds a confidence score...
TERM "Meta license" hits=0
=== https://huggingface.co/meta-llama HTTP 200 STRIPPED 102274 ===
TERM "Muse" hits=0    TERM "Spark" hits=0
```

Every one holds, and the capture's numbers are the same ones I got. My
Glimmer count differs from the transcript's (`license:apache-2.0` = 8 vs
`apache-2.0` = 13) because those are different search terms on the same page,
not a disagreement. The transcript is a fresh re-run of the whole script —
every stripped length moved a few bytes — which is the honest way to add four
fetches rather than splicing them into an old file.

The narrative's summary table gained a row per new instrument, and the
`flagship_weights_listing` row of the fact-by-fact table now says "**confirmed**
— value restored to the badge quotation alone; the clause asserting what Meta
license text carries was removed because the cited card does not support it".
The evidence file records the correction rather than quietly erasing it.

## What I did not re-check, and why

`npm test` and `npm run build` are recorded PASS by the loop on this exact
commit; I did not re-run them. The `flagship_weights` fact is byte-identical
across the delta and was confirmed exact by the previous pass against
`research.meta.ai/blog/introducing-muse-spark-1-3` — I did not re-litigate it.
Scope is unchanged and clean: one content file, two evidence files, no
reserved paths, no `RESULT.md` in the diff because `.gitignore` carries
`/RESULT.md`.

## Rejection index

`data/proposals/rejected/` is empty; there is nothing this could re-tread. On
the judgment half, the previous pass's reasoning still holds and I agree with
it: the answer this job produced — a September roadmap sentence on Meta's own
blog — existed nowhere in the repository and could not have been written
without leaving it.

## One thing carried, not blocked on

See `carry:` above: the evidence file's header now claims the meta-models org
page "was fetched twice", and the transcript contains one such fetch. The
observation behind it is true (I measured the page rendering at a different
length than the capture did), but the sentence describes the transcript
inaccurately. Too small to hold an otherwise-correct verification for, and
this is the last pass.
