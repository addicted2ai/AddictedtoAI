---
job: j-20260902-02
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a lifecycle "status" on an aggregator is a derived
  quantity, not a vendor announcement, would link this line: it is a worked
  case where the same row read deprecated, then active, then deprecated again
  inside four days without the model, its price or its context changing, and
  the annotation says out loud which rule did the deriving and which field
  moved. A reader running a GLM-4.5V integration would link it for the
  narrower reason — it tells them the model is still callable today, that
  2026-12-31 is the earliest date OpenRouter reserves the right to remove it
  rather than a switch-off date, and that the signal has been unstable enough
  that acting on it alone would have been wrong once already this week.
proposal:
  slug: flag-flapping-feed-fields
  type: machinery
  date: 2026-09-02
  summary: >-
    Teach the Pulse's diff step to recognise when a material field returns to
    a value it already held inside a short trailing window, and mark that
    change record as a flap (a boolean, or the date of the prior opposite
    change) rather than emitting it as an ordinary field_change. The derived
    queue would then rank a flapping signal below a first-time one, and the
    changed feed could render "reverted to its 2026-08-29 value" instead of
    presenting the round trip as two independent events. Nothing about the
    derivation rule changes; only the record gains the fact that it has seen
    this transition before.
  evidence: >-
    Verified while reviewing this diff: data/changes.jsonl:86 records
    z-ai/glm-4.5v moving deprecated -> active on 2026-08-29, line 90 annotates
    it as "a metadata correction on an otherwise unchanged row", and line 152
    records the same row moving active -> deprecated on 2026-09-02 from the
    same expiration_date field returning to 2026-12-31 (confirmed against
    data/sources/openrouter-models/{previous,latest}.json: null on 2026-09-01,
    "2026-12-31" on 2026-09-02). Two interpret jobs have now been spent on one
    oscillating field on one row, and neither change record carries any hint
    that the other exists — the connection survives only because a human
    author happened to go looking for it.
carry:
  - title: Tighten "come and gone twice" to the two transitions actually observed
    detail: >-
      The annotation ends "the marker has now come and gone twice in four
      days". The corpus records two transitions, not two round trips:
      expiration_date was present before 2026-08-29, absent in the 2026-09-01
      snapshot, and present again on 2026-09-02 (data/changes.jsonl:86 and
      :152; verified in the two snapshots). "has now moved twice in four days"
      is the same point and is exactly what the records show.
    subject: data/changes.jsonl
  - title: Give the OpenRouter schema quote the URL its sibling claim already has
    detail: >-
      The annotation cites https://openrouter.ai/api/v1/models/z-ai/glm-4.5v/endpoints
      inline for the callability claim, but attributes the quoted definition
      only to "OpenRouter's own models API schema". The quote is exact and the
      attribution is accurate, but the document is not the rendered docs page
      (https://openrouter.ai/docs/api-reference/list-endpoints-for-a-model
      returns 404) — it is https://openrouter.ai/openapi.json, at
      components.schemas.Model.properties.expiration_date. Naming that URL the
      way the endpoints URL is named would let a later reader check the quote.
    subject: data/changes.jsonl
---

Delta review. I read only what changed between `6773c9b` (the reviewed draft)
and `3d98bf2` (the revision), against pass 1's three findings. I did not re-run
`npm test` or `npm run build`; the loop's PASS on `3d98bf2` is recorded above.

## What actually changed

```
$ git -C <worktree> diff --stat 6773c9b 3d98bf2
 data/changes.jsonl | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
```

One line — the annotation this job itself appended — rewritten in place. No
other file touched, no pre-existing line touched, so the append-only property
of `data/changes.jsonl` still holds (the edited line is the job's own addition,
not a prior record). Scope is unchanged from pass 1's reading and remains
correct: no entry file is edited, which is right for a STUB under
`addictedtoai-ij4h`.

## Finding 1 — the successor claim. Fixed, by removal.

Pass 1 required either a sourced lineage claim or the removal of the sentence.
The revision takes the second option and states the negative honestly:

> "no successor row is identifiable from the feed, so any migration target has
> to be chosen from the current catalog rather than from this change"

I checked that this negative is true of the feed rather than merely unexamined.
Over all 421 rows in `data/sources/openrouter-models/latest.json`, the union of
row keys is:

```
alias_target,architecture,benchmarks,canonical_slug,context_length,created,
default_parameters,description,expiration_date,hugging_face_id,id,
knowledge_cutoff,links,name,per_request_limits,pricing,reasoning,
supported_parameters,supported_voices,top_provider
```

Filtering that set for `/succ|replac|superse|migrat|recommend|instead/i` returns
`[]`. The `z-ai/glm-4.5v` row itself does not carry `alias_target` (its own keys
are the list above minus `alias_target` and `benchmarks`). There is no lineage
field in this feed, so "not identifiable from the feed" is a measured statement
and not a shrug. The three numbers pass 1 objected to (`z-ai/glm-4.6v`, 131K,
$0.30/$0.90) are gone with the sentence.

## Finding 2 — the removal date. Fixed.

"plan around the 2026-12-31 removal date" is now:

> "OpenRouter's own models API schema defines `expiration_date` as \"the date
> after which the model may be removed\", so 2026-12-31 is the
> scheduled-earliest removal date rather than a firm one"

I re-fetched the source rather than trusting pass 1's transcription, because
the attribution changed ("API reference" → "models API schema") and the
attribution is the part that was previously loose:

```
$ node rev2-schema.mjs
HTTP 200
bytes 1888090
components.schemas.Model.properties.expiration_date => {"description":"The date
after which the model may be removed. ISO 8601 date string (YYYY-MM-DD) or null
if no expiration.","example":"2025-06-01","type":["string","null"]}
occurrences of "may be removed": 1
title: OpenRouter API version: 1.0.0
has /api/v1/models path: /embeddings/models,/images/models,/models,/videos/models
```

The quote is verbatim, occurs exactly once in the whole 1.89 MB document, and
sits on the `Model` schema — the schema of the rows `/models` returns, which is
what "models API schema" names. The new attribution is more accurate than the
one it replaces.

## Finding 3 — the flap. Fixed, and it is the best sentence in the annotation.

The revision names the earlier record and quotes its own reading of it:

> "returns the row to the `deprecated` status it last held before the
> 2026-08-29 flip to `active` — which the annotation on that change
> (j-20260830-02) read as a metadata correction on an otherwise unchanged row,
> and which today's reappearance reverts"

Checked line by line against the file:

```
$ sed -n '86p;90p' data/changes.jsonl
{"date":"2026-08-29",...,"row_id":"z-ai/glm-4.5v","kind":"field_change",
 "field":"status","new":"active","old":"deprecated",...}
{"kind":"annotation","annotates":"...|z-ai/glm-4.5v|status","date":"2026-08-29",
 "job":"j-20260830-02","text":"... a metadata correction on an otherwise
 unchanged row (pricing and context identical in both snapshots) ..."}
```

The direction (`deprecated` → `active`), the date, the job id `j-20260830-02`
and the phrase "a metadata correction on an otherwise unchanged row" are all
exactly as the annotation reports them. The characterisation is attributed to
that annotation rather than asserted, which is the right move — it is a prior
reading, not a fact.

## The claims I checked independently

**The rule paraphrase, "(absent -> `active`, within 365 days -> `deprecated`)".**
`pulse/lib/diff.mjs:117-126` — `if (!expires) return 'active'`;
`if (daysUntil < 0) return 'retired'`;
`if (daysUntil <= (rule.deprecated_within_days ?? 365)) return 'deprecated'`.
`data/sources/registry.json:66-70` sets `deprecated_within_days: 365` for
`openrouter-models`. The paraphrase is a correct, shortened statement of the
two arms it names, and it correctly attributes the derivation to this site
rather than to OpenRouter.

**"the 2026-09-01 snapshot carried null".** This is a sharpening of pass 1's
"absent", and it is the more accurate word:

```
latest fetched_at= 2026-09-02T06:00:03.835Z n= 421   expiration_date= "2026-12-31"
previous fetched_at= 2026-09-01T06:00:03.724Z        expiration_date= null
```

The key is present in both snapshots; only the value moved. `deriveStatus`'s
`if (!expires)` treats `null` and absent identically, so the derivation reads
the same either way — but the annotation now says what the file says.

**"it is still listed and both its endpoints (Z.AI, Novita) serve it as of
2026-09-02".** Fetched the URL the annotation itself cites:

```
$ GET https://openrouter.ai/api/v1/models/z-ai/glm-4.5v/endpoints
HTTP 200   name= Z.ai: GLM 4.5V  id= z-ai/glm-4.5v
 endpoint: Novita  status= 0  ctx= 65536  uptime_last_1d= 96.93
 endpoint: Z.AI    status= 0  ctx= 65536  uptime_last_30m= 95.40  uptime_last_1d= 98.77
```

Both endpoints listed, both `status: 0`, both serving. Note that pass 1 observed
`status: -2` on the Z.AI endpoint and flagged that it could not establish what
the negative enum values mean; on this fetch it reads `0`, the same as Novita,
so whatever it was has cleared and the caveat no longer has anything to attach
to. The annotation now cites this URL inline, which is what pass 1 asked for —
the site's own `latest.json` is the `/models` response and carries no endpoint
data, so without the URL there was nothing in-repo to check the sentence
against.

**The record is well-formed and lands on the right change.**

```
$ node rev2-key.mjs
line count 154
last kind annotation job j-20260902-02 date 2026-09-02
annotates resolves: true  2026-09-02 z-ai/glm-4.5v active->deprecated
annotations on that key: 1
```

The `annotates` key resolves to the exact change line it interprets, and there
is exactly one annotation on that key — no duplicate.

## What I did not check

Whether an inference request to `z-ai/glm-4.5v` actually succeeds. That needs a
credential and supplying one is forbidden here, so the listing plus its uptime
figures remains the closest available measurement — which is what the
annotation cites, and it does not claim more than that.

Whether Z.ai has published anything of its own about GLM-4.5V's lifecycle. The
revision no longer makes any claim that would need it: it says only that no
successor is identifiable *from the feed*, which is a statement about the feed
and is measured above. A claim that Z.ai has published no successor guidance
would need Z.ai's sources; the annotation does not make that claim.

## Verdict

`approve`. All three of pass 1's findings are addressed — the unsupported
recommendation is gone rather than patched, the certainty is softened to what
the quoted definition supports, and the flap is named with its record and its
job id. Every remaining claim in the line resolves to either a file in this
repository or a URL the line itself cites, and I confirmed each one above. The
two `carry:` entries are wording, not substance, and neither is worth a second
revision pass on a line that now says less than it did and says all of it
truthfully.
