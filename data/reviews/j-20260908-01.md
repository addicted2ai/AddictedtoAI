---
job: j-20260908-01
verdict: revise
reasons:
  - false-or-unsupported-claim
  - spec-violation
would-cite: >-
  A reader documenting the disappearance of a free OpenRouter route would link
  this entry to show that the route has no live endpoint while MiniMax still
  lists M2.7, supporting a router-specific withdrawal without claiming a vendor
  retirement.
---

The page-level repair is narrowly scoped and the source evidence supports its
central claim. A live fetch of `https://openrouter.ai/api/v1/models` returned
HTTP 200 and `MODEL_MATCHES 0` for `minimax/minimax-m2.7:free`. A live fetch of
`https://openrouter.ai/api/v1/models/minimax/minimax-m2.7:free/endpoints`
returned HTTP 200; the parsed result was `MODEL_ID minimax/minimax-m2.7:free`
and `ENDPOINTS_COUNT 0`, and the response contained `"endpoints":[]`. The
MiniMax documentation URL in the page returned HTTP 200 and its model-listing
snippet contained `"id":"MiniMax-M2.7"` and `"type":"model"`; it did not
contain `minimax/minimax-m2.7:free` or a free-tier model id. These checks support
the page's distinction between the still-listed underlying model and the empty
OpenRouter free route. The checked-in diff also retains the `feeds:` binding.

The durable answered record is not correct as written. The branch's
`data/sources/openrouter-models/latest.json` is dated `2026-09-07` and its
`minimax/minimax-m2.7:free` row has the pinned benchmark value
`{"arena":"models","category":"3d","elo":1231,"rank":38,"win_rate":50.6}`.
The new `data/vanished/answered/openrouter-models--minimax-minimax-m2-7-free.md`
instead records `rank:39, win_rate:50.5` for that row and the other values from
`previous.json`, dated `2026-09-06`. This is the older snapshot, not the
last-known row supplied for the 2026-09-07 disappearance. Regenerate or correct
the answered record so its pinned table exactly matches the task's 2026-09-07
values, while keeping it under `data/vanished/answered/` and preserving the
entry and its feed binding. Until then, the permanent evidence is a false or
unsupported historical record and does not satisfy the required unchanged move.

The loop reported `npm run test`, `npm run build`,
`node scripts/verify-surfaces.mjs out`, and `node scripts/verify-design.mjs out
3211` as PASS before this review; I did not rerun those instructed gates.
