---
job: j-20260914-28
verdict: approve
reasons: []
would-cite: >-
  Someone comparing model aggregators who wants to say how big OpenRouter's
  catalog is right now would link the OpenRouter entry for its models_listed
  count, because it is now a dated figure (447 on 2026-09-14) taken straight
  from the unauthenticated /api/v1/models endpoint, with a transcript anyone can
  recount, instead of a 17-day-old number for a catalog that changes within a day.
subject: "content/wiki/tool/openrouter.md"
reviewed:
  "content/wiki/tool/openrouter.md": "fb1f6b96b1c31b2db117171ee0fc996ddc8e92cf81a93556f665ef272cba544e"
---

## Scope

The branch base is `4de8fe3`. That commit is on `origin/main` and `train`. The
local `main` ref in this worktree is stale, so `git diff main...HEAD` also
picked up earlier loop and pulse commits that aren't this job's work.
`git diff --stat 4de8fe3 HEAD` shows only this job's files:

```
 .job/brief.md, .job/fetch-openrouter.mjs, .job/openrouter-body.json,
 .job/source.json, .job/total-count.mjs            (scaffolding)
 content/wiki/tool/openrouter.md                    |   4 +-
 .../evidence/verify-openrouter-models-listed.md    |  51 +++
 .../verify-openrouter-models-listed.raw.txt        | 462 +++
```

The entry change is two lines, both on the target fact
`tool/openrouter#models_listed`: `value: 398 -> 447` and
`accessed: "2026-08-28" -> "2026-09-14"`. The `models_endpoint` and
`row_id_field` stamps were not touched, and the evidence file says so. No
reserved path was touched. Nothing the job asked for was done in reverse: it
asked for a re-verification with a real date, and the diff re-fetched the
source and stamped the date of that fetch.

## What I ran

I wrote a script (`rv-j28.mjs`, in the OS temp directory) and ran it in the
foreground at 12:38 local:

```
saved body: data.length 447 distinct 447 total_count 447 links {"next":null}
raw transcript id lines 447 match saved order true
latest.json keys [...'fetched_at','row_count'...] row_count 445 fetched 2026-09-14T12:31:37.751Z
live now Mon Sep 14 2026 12:38:46 GMT-0600 (Mountain Daylight Time) HTTP 200 data.length 447 total_count 447 links {"next":null}
```

- **The fetch really ran.** The committed transcript lists 447 ids. They match
  the saved body's `data[].id` exactly, in the same order. The body's
  `total_count` is 447 and `links.next` is null, so there is only one page.
- **An independent re-fetch agrees.** My own GET of the cited URL, three
  minutes after the author's run, returned HTTP 200 with 447 rows and
  `total_count` 447. This is a separate instrument from the author's saved
  body, so it corroborates the count rather than just agreeing with it.
- **The date is honest.** The transcript header says 12:35:23 MDT and the HTTP
  `date` header says 18:35:24 GMT, which is the same moment. The local date is
  2026-09-14, and I read it from `date`.
- **The side claims hold.** `data/sources/openrouter-models/latest.json`
  really has `row_count: 445` and `fetched_at 2026-09-14T12:31:37.751Z`, about
  six hours before 18:35Z, as stated. The 388-vs-398 citation matches
  `data/reviews/j-20260831-10.md` lines 56-73.

## Not blocking

The evidence narrative says "The 738 KB body itself was not committed." In
fact the body is committed at `.job/openrouter-body.json` in commit `e592f3a`.
`.job/` is scaffolding the loop removes before merge, so the sentence will be
true on main. It is still slightly inaccurate about the branch as it stands. It
isn't worth a revision.
