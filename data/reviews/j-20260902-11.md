---
job: j-20260902-11
verdict: approve
reasons: []
would-cite: >-
  Nobody links a dropped-proposal record for its own sake, and that is the
  point of judging it: the reader who reaches this file is a future job asking
  "has this GLM 4.5V flip already been interpreted, and by whom?" — and the
  answer it gives them is now the one the change ledger actually supports.
  Someone arguing that the corpus's declines are auditable rather than
  decorative would cite the pair (the record and the annotation key it names)
  as the worked example of a decline whose cited evidence resolves.
---

Free-form notes.

**What the diff claims.** That `data/proposals/dropped/glm-45v-status-flapping.md`
attributed the wrong flip to job `j-20260902-02`'s annotation — that the
annotation covers the 2026-09-02 `active -> deprecated` flip, not the 2026-08-29
reactivation — and that fixing the attribution leaves the decline's substance
untouched.

**The check that would be red if that were false.** The `annotates` key on the
annotation and the `key` on the change row it points at. I read the raw JSONL
rather than any summary of it.

`data/changes.jsonl` line 154, the annotation:

```
{"kind":"annotation","annotates":"openrouter-models|a9e815e403b4d7a5|31886658dc9b9e56|z-ai/glm-4.5v|status","date":"2026-09-02","job":"j-20260902-02","text":"On 2026-09-02 the OpenRouter row for `z-ai/glm-4.5v` again carries `expiration_date: 2026-12-31`, where the 2026-09-01 snapshot carried null, ... returns the row to the `deprecated` status it last held before the 2026-08-29 flip to `active` — which the annotation on that change (j-20260830-02) read as a metadata correction ...
```

Line 152, the change row it annotates — I split the record and grepped for the
identity fields rather than eyeballing a long line:

```
15:"field":"status"
16:"key":"openrouter-models|a9e815e403b4d7a5|31886658dc9b9e56|z-ai/glm-4.5v|status"
17:"kind":"field_change"
```

Byte-identical to the `annotates` value, and the row carries
`"date":"2026-09-02"` with `"$status":"deprecated"` and
`"expiration_date":"2026-12-31"` in its excerpt. So the annotation is bound to
the 2026-09-02 flip. The pre-fix text was wrong and the post-fix text is right.

**The other flip, to rule out the opposite error** — i.e. that j-20260902-02
might have annotated both, which would have made the old sentence merely
incomplete rather than wrong. Enumerating every status row and annotation on
this row id (`grep -n 'z-ai/glm-4.5v|status'`, four matches, all four read):

```
86:  {"date":"2026-08-29", ... "$status":"active" ... "expiration_date":null ...
90:  {"kind":"annotation","annotates":"openrouter-models|a56a5acd5698a11a|bde830d7800a4672|z-ai/glm-4.5v|status","date":"2026-08-29","job":"j-20260830-02", ...
152: {"date":"2026-09-02", ... "$status":"deprecated" ... "expiration_date":"2026-12-31" ...
154: {"kind":"annotation", ... "date":"2026-09-02","job":"j-20260902-02", ...
```

Two flips, two annotations, two different jobs — 2026-08-29 belongs to
`j-20260830-02`, not `j-20260902-02`. The old sentence had crossed the wires
exactly as the brief described. j-20260902-02's own annotation text names
`j-20260830-02` as the annotator of the earlier change, which is a second,
independent confirmation from inside the record itself.

**Substance of the decline, unaffected.** The paragraph's load-bearing sentence
is "The flip already has a recorded interpretation on this exact row." That was
true before the fix and is true after it — more so, since both flips carry one.
The correction narrows to which flip and which job, which is what the outcome
asked for and nothing more.

**Scope.** `git diff --stat d6ae668^ d6ae668` on this branch:

```
 data/carried/j-20260902-07-carry-2.md             | 16 ----------------
 data/proposals/dropped/glm-45v-status-flapping.md |  4 ++--
 2 files changed, 2 insertions(+), 18 deletions(-)
```

Two files: the two-line correction, and the deletion of the carried-finding file
that the brief explicitly requires as the retirement step. Nothing else. The
`.job/brief.md` and `.job/source.json` additions shown in the review diff are
loop-written job scaffolding, not the author's work.

`data/derived/queue.json` still names `j-20260902-07-carry-2` — I checked, and it
is correct that the author left it alone: `data/derived/` is a pure function the
Pulse recomputes from scratch every run, so the item leaves the queue when the
Pulse next runs, not by being edited out. Hand-editing it would have been the
defect.

**Not re-run:** `npm test` and `npm run build`, which the loop recorded PASS on
this exact commit. Nothing in a two-line edit to a `data/proposals/dropped/`
record is reachable by either; the check that could have been red here was the
JSONL identity check above, and it is green.

**Noticed, not carried.** The rewrap on the edited line: the replacement runs
`...j-20260902-04 applied` / `carried findings about it. The feed flapping is a
gateway-catalog artifact with no vendor` — about 88 columns, where the rest of
the file wraps near 72. Purely cosmetic, in a non-rendered record, and filing a
job to rewrap one line would cost more than the defect. Recording it here rather
than under `carry:` is deliberate; the carry channel is for corrections worth
someone's minutes.
