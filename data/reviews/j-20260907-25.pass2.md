---
job: j-20260907-25
verdict: approve
reasons: []
would-cite: >-
  A maintainer documenting ByteDance Seed's service provenance could link this
  entry to show that BytePlus is the declared service domain while a bare
  "seed" domain is not treated as ByteDance Seed ownership.
subject: "content/wiki/org/bytedance-seed.md"
reviewed:
  "content/wiki/org/bytedance-seed.md": "addc8cff28ffa03f5c56c218cc8ddefa8345c1bcf4bb87b3394b9e5eeb85d6e2"
---

Free-form notes: The repair clears both carried findings in scope. The target
entry now declares `publishes_from: [byteplus.com]`; the two carried-finding
files are deleted. The derived alias record for bare `Seed` is absent, and the
target search-index row retains only `ByteDance Seed Team`.

Source check: the official ByteDance Seed page at
https://seed.bytedance.com/en/seed2_1 has title `ByteDance Seed`, identifies
the Seed2.1 family, and at its model links lists both `Try it` and `API` as
`ai.byteplus.com`. The direct domain validation returned exactly
`{"ok":true}` for `checkPublishesFromValue('byteplus.com')`.

Ownership check: the direct `nameTokens`/ownership execution returned tokens
`["bytedanceseed","bytedance","bytedanceseedteam","team"]`,
`bare_seed_owned: false`, `byteplus_owned: true`, and
`seed_subdomain_owned: true`. Thus the new declaration admits BytePlus, the
bare `seed` registrable label is no longer inferred as owned, and the intended
ByteDance subdomain remains owned. `node --test lib/vendor-domain.test.mjs`
reported 25 tests, 25 pass, 0 fail.

Provider-join check: evaluating the exact `matchProviders` normalization and
containment expression against the real catalog returned
`current: ["bytedance-seed","bytedance"]` and, after adding a synthetic
`seed-future` row, `current_with_future: ["bytedance-seed","bytedance"]`.
The alias deletion therefore closes the board widening while preserving both
current intended provider matches.

Derived-state check: direct JSON inspection returned
`bare_seed_alias: null` and `target_aliases: ["ByteDance Seed Team"]`.
Both carried paths tested false with `Test-Path`. `git diff --check
origin/main...HEAD` completed with no output and exit 0. An import-level board
check could not start because this review worktree has no installed `zod`
dependency; no install or other mutation was attempted, and the focused checks
above used the dependency-free implementation/tests. The four pre-review gates
listed in the brief were not rerun.
