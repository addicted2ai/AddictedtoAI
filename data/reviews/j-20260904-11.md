---
job: j-20260904-11
verdict: approve
reasons: []
would-cite: >-
  A safety researcher arguing that "we found no steganography" is not the
  reassurance it reads as would link this post's bullet list, because it keeps
  OpenAI's own hedge ("may largely be bounded") and names the adversarial
  condition — sandbagging plus an explicit instruction to evade — that the
  sub-11% WMDP recall belongs to, which is exactly the distinction a monitoring
  argument stands or falls on.
reads-human: >-
  It reads human. The bullets carry the numbers without narrating their own
  method, and the repair's two inserted phrases ("Told to sandbag and to evade
  the monitor", "may largely be bounded") land as a writer restoring a source's
  caution rather than as generated hedging — the surrounding sentence rhythm is
  unchanged and nothing was padded to absorb the edit.
carry:
  - title: Fix the now-dangling "the same table" after the release-page reattribution
    detail: >-
      The intro to the benchmark block was changed from "The release page's
      table, OpenAI's own figures, carries the cyber rows:" to "The release
      page, ...", which is the fix carry-3 asked for. But the paragraph below
      the table still reads "Three more rows from the same table: ARC-AGI-3
      99.9% ...", and "the same table" now has no antecedent except the post's
      own table — the one that includes the "SRE-Bench, four attempts | 99.2% |
      68.7%" row that carry-3 established is release-page prose, not a table
      row. The true reading (the release page's benchmark table) is still
      available, so this is not a false claim, but it half-restores the
      attribution the repair removed. "Three more rows from the release page's
      benchmark table:" fixes it.
    subject: content/blog/openai-gpt-6-astra-system-card.md
subject: "content/blog/openai-gpt-6-astra-system-card.md"
reviewed:
  "content/blog/openai-gpt-6-astra-system-card.md": "d3ff4456a24040153546fb07e36f67823a8d93c855e4791a09282a964d0051c9"
---

Three-line repair; I checked each changed line against the source it cites, plus
scope and the retirement of the three carry files.

## What I ran

`node` script fetching both cited pages raw and substring-searching the stripped
text (no WebFetch extractor in the loop for the card):

```
=== https://deploymentsafety.openai.com/gpt-6-astra ===
status 200 text/html
raw bytes 1206327 / text chars 193616
HIT   "may largely be bounded to lower reasoning tasks"  ->  ...We have not seen
evidence of steganographic CoT reasoning (e.g., concealing hidden reasoning
within ordinary and seemingly unrelated text), indicating that the evasion risk
may largely be bounded to lower reasoning tasks. These findings indicate that
the Astra class models could evade our CoT monitors under adversarial
conditions. Right now, these findings are largely based on adversarial
evaluations...
MISS  "largely bounded to lower reasoning tasks"
```

So the string the post used to quote is genuinely absent from the card and the
string it now quotes is present verbatim. Finding 1 is cleared, not paraphrased
around.

Finding 2, from the same fetch:

```
HIT   "evade detection"  ->  ...Unaware, which asks the model to sandbag but
does not mention the CoT monitor; Simple aware, which adds a sentence stating
that the reasoning channel is monitored; Evasion aware, which explicitly
instructs the model to evade detection; Control baseline, the original task with
no additional instructions...

HIT   "below 11%"  ->  ...do not observe covert obfuscation from GPT-6 Astra,
and the monitor recall is comparable to the "unaware" condition. However, when
explicitly instructed to evade detection, GPT-6 Astra achieves a CoT-only
monitor recall below 11% for WMDP, compared with nearly 100% for GPT-5.6 Sol.
For Monorepo-Bench, the recall dropped to zero for GPT-6 Astra.
```

This is the check that matters for the new wording: all four conditions sit on
top of the sandbagging prefix prompt, and "evasion aware" is that prompt *plus*
the instruction to evade. "Told to sandbag and to evade the monitor" is
therefore exactly the condition the sub-11% figure belongs to — the post no
longer reads as though the sandbagging prompt alone produced it, which the card
contradicts one sentence earlier ("simple aware ... comparable to the 'unaware'
condition"). The recall is a CoT-only monitor recall in both the card and the
post, and "at zero on Monorepo-Bench" matches "the recall dropped to zero".

Finding 3 I could not check at first hand. `https://openai.com/index/gpt-6-astra/`
returns **403** to both my raw `fetch` and WebFetch — the same 403 the reviewer
of j-20260904-02 recorded against the same URL on 2026-09-04, so this is the
page's standing posture toward automated clients, not a transient failure. What
I could get:

- Web search returned, from third-party coverage of the release page: "GPT-6
  Astra solved 99.2% of SRE-Bench tasks within four attempts, compared with
  68.7% for GPT-5.6 Sol ... In a single attempt, Astra solved 88.0% of tasks
  compared with 55.9% for GPT-5.6 Sol." All four numbers stand.
- The j-20260904-02 record notes Vellum's reproduction of the release table as
  "SRE-Bench 88.0% vs 55.9%" — the single-attempt pair only, consistent with
  carry-3's claim that the four-attempt pair is body prose rather than a table
  row.

So the numbers are corroborated and the attribution fix is in the direction the
finding asked for. I am recording plainly that the primary page itself is
unreachable from here; the fix does not depend on a number I could not
corroborate, only on dropping a claim about where on the page the numbers live,
which is a weakening — the safe direction.

## Scope

`git show --stat HEAD` on `4e2133b83fcc`:

```
 content/blog/openai-gpt-6-astra-system-card.md |  6 +++---
 data/carried/j-20260904-04-carry-1.md          | 16 ----------------
 data/carried/j-20260904-04-carry-2.md          | 16 ----------------
 data/carried/j-20260904-04-carry-3.md          | 16 ----------------
 4 files changed, 3 insertions(+), 51 deletions(-)
```

Three prose lines and the three retirements the brief names, nothing else. All
three carry files are gone from `data/carried/` (the directory still holds the
24 unrelated items), so none of these findings returns on the next Pulse run.
No reserved path is touched.

The post's approved-surface hash moves with this edit, which would leave it
`mismatched` — but `loop/lib/review.mjs` writes `reviewed:` from the subjects
the merge measures, after the merge, so the rebinding is the normal path and not
something this diff had to do.

I did not re-run `npm test` or `npm run build`; the loop recorded both PASS on
this commit and nothing in a three-line prose diff would move them.

One thing carried forward, above: the "same table" reference the reattribution
left behind. Not worth a revision pass on a repair that did what it was asked.
