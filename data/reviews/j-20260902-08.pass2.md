---
job: j-20260902-08
verdict: approve
reasons: []
would-cite: >-
  Someone writing a migration note for a deprecated router slug, or pushing
  back in a thread where an empty `endpoints` array is being read as proof a
  model is dead, would link this page for the one sentence that says the
  listing is not the routing authority — and for the exact GET they can run
  to watch the same 200 come back empty on their own machine.
carry:
  - title: Add today's recurrence to the delta-review stale-diff proposal's evidence
    detail: >-
      `data/proposals/delta-review-brief-embeds-pre-revision-diff.md`
      (2026-09-01) predicts that a delta-review brief embedding the
      pre-revision diff would make a reviewer "re-issue the identical
      revision finding, and discard the job on its last pass." That happened
      again today, on this job's pass 2: the brief stated the diff was
      computed on commit `d29ae884daf0`, but the embedded diff was
      `55869ad..9b7b984` — the pass-1 text ("Anyone re-running this check
      should note how the record reads"), which `d29ae88` had already
      replaced. `git diff 00ecc2e d29ae88` shows the revision the brief
      omitted. Adding this second dated occurrence to that proposal's
      Evidence section would show the bug is unfixed and has now nearly cost
      two jobs, which is the argument for prioritising it.
    subject: data/proposals/delta-review-brief-embeds-pre-revision-diff.md
subject: "content/wiki/model/anthropic-claude-opus-5-fast.md"
reviewed:
  "content/wiki/model/anthropic-claude-opus-5-fast.md": "98fdcaa7c43886d072737cade002e3326c063a9cd05fac7c23d3e00ae300e4b5"
---

## The brief's embedded diff was stale — read this first

The diff in my brief is **not** the branch state. It carries the pass-1 text
that the previous review rejected, so reviewing it as given would have
reproduced the previous verdict verbatim and discarded the job on its last
pass. I checked the tree instead:

```
$ git -C <worktree> rev-parse HEAD
d29ae884daf038513ff549e2c9d5b304a0146c46
$ git log --oneline -3
d29ae88 job j-20260902-08: reframe the endpoints caveat, name the check, drop the orphaned wrap (done)
00ecc2e job j-20260902-08: reword the endpoints caveat on the Opus 5 fast page (done)
94dc6c3 job j-20260902-08: brief
```

The embedded diff is `55869ad..9b7b984`, i.e. `94dc6c3..00ecc2e` — it stops
one commit short. The real delta since the reviewed commit is
`git diff 00ecc2e d29ae88`, and that is what I judged. `git status
--porcelain` was empty, so HEAD is the tree. This is a known machinery bug
already filed as `data/proposals/delta-review-brief-embeds-pre-revision-diff
.md`; I carried the recurrence rather than filing a duplicate proposal.

## The three required changes, against the actual delta

**1. Name the check — done.** The paragraph now reads:

> An empty `endpoints` list on the old slug proves nothing about routing.
> `GET /api/v1/models/anthropic/claude-opus-5-fast/endpoints` answered HTTP
> 200 with `"endpoints":[]` on 2026-09-02, where the same call on the
> standard row returns a populated list. No published source explains the
> difference, and whether requests still route is answered by the
> deprecation notice above, not by the listing.

"this check" and "the record" are gone; the literal request line is there, so
the dated measurement is reproducible from this page alone. That was the
substantive loss the last pass identified, and it is repaired.

**2. Reframe, not paraphrase — done, and by the option the last review
named.** The previous pass offered "lead from what the empty array does not
prove." The paragraph now opens on exactly that judgment and runs
claim → evidence → limit, where the 4.8 page runs caveat → ambiguity →
reassurance. The specific verbatim survivals that were called out are gone:
`while the standard row still lists endpoints` → "where the same call on the
standard row returns a populated list"; `or genuinely unroutable` and the
aliased-internally speculation are dropped entirely rather than reworded;
`HTTP 200, measured 2026-09-02` → "answered HTTP 200 with `"endpoints":[]` on
2026-09-02". Read side by side with `anthropic-claude-opus-4-8-fast.md` lines
57–65, the two no longer look copy-pasted: one names an API in the abstract
and hedges, the other names a request and refuses an inference.

**3. The orphaned wrap — gone.** `So the slug still / answers, and /
existing requests still serve;` no longer exists; the clause was cut, not
rewrapped, and nothing downstream lost information because the paragraph
above already quotes OpenRouter's "Requests to this model keep working."

## The factual claims, measured today

Not taken on trust from the last pass — re-run at review time, 2026-09-02:

```
$ curl -s -o .ep5f.json -w "fast:%{http_code}\n" \
    "https://openrouter.ai/api/v1/models/anthropic/claude-opus-5-fast/endpoints"
fast:200
$ curl -s -o .ep5s.json -w "std:%{http_code}\n" \
    "https://openrouter.ai/api/v1/models/anthropic/claude-opus-5/endpoints"
std:200
$ node .check-ep.mjs
fast | id: anthropic/claude-opus-5-fast | endpoints: 0
   raw contains "endpoints":[] -> true
std | id: anthropic/claude-opus-5 | endpoints: 10
   raw contains "endpoints":[] -> false
    Claude Platform on AWS | Azure | Anthropic | Google
```

Every element of the sentence holds, including the literal `"endpoints":[]`
the prose puts in a code span — that exact byte sequence is present in the
fast slug's response and absent from the standard row's. The standard row
returns 10 endpoints, which is "a populated list."

## The proposal file in the diff

`data/proposals/prose-inclusion-marker.md` is the job's one permitted
proposal (`type: machinery` against a `repair` job, so no self-amplification),
and its evidence is checkable, so I checked it:

- "lib/transclude.mjs (lines 24-25) defines only FACT_RE and WANT_RE" — true;
  lines 24–25 are exactly those two exports.
- "lines 123-131 make any other `{{...}}` marker a build error named
  bad-marker" — true; line 130 is `rule: 'bad-marker'`, inside the
  `unrecognized marker` diagnostic at 126–131.
- The quoted j-20260902-05 finding — verified against
  `D:/AddictedtoAI/data/reviews/j-20260902-05.md`, lines 26 and 28 carry
  "uniform-block shape the voice bar exists to catch" and "bind the shared
  half as a `{{fact:…}}`-style" verbatim.

So the second cure the finding offered genuinely was not implementable, and
the proposal says so accurately rather than as an excuse. No slug collision
in `data/proposals/rejected/` (which holds only `README.md`).

## Scope

`content/wiki/model/anthropic-claude-opus-5-fast.md` (the named target),
`data/carried/j-20260902-05-carry-1.md` deleted as the brief requires to
retire the queue item, one proposal, and the loop's own `.job/` files.
Nothing wider. The sibling 4.8 page is untouched, which is correct — the
outcome named one target, and editing both would have been the scope
violation.

## Not re-run

`npm test` and `npm run build`, recorded PASS by the loop on `d29ae884`, the
commit I reviewed. I checked the specific claims instead.

## Housekeeping

I wrote three throwaway scripts into the review worktree
(`.check-ep.mjs`, `.check-nl.mjs`, and two `.ep5*.json` captures) and deleted
them; the worktree is as I found it, and a reviewer's edits are discarded
regardless.

## Why approve

The last pass's objection was that a thesaurus run over a fixed skeleton is
not less machine-made than the duplicate it replaced. This revision did not
do that again: it cut two clauses, inverted the paragraph's argument, and
replaced a vague gesture at "the router's endpoints API" with the request
string a reader can paste. The result is shorter than what it replaced and
tells the reader something the 4.8 page does not — that the empty array is
not evidence — which is the difference between two pages worth having open
and one page duplicated.
