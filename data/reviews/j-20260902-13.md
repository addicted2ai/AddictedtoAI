---
job: j-20260902-13
verdict: approve
reasons: []
would-cite: >-
  Nobody links a drop record from outside, so the honest answer is internal: the
  next author who reaches for the Qwen licence angle and follows this pointer to
  see whether the question was already weighed. Before this diff that path
  resolved to nothing and the pointer argued from a file the reader could not
  find; after it, the cross-reference actually lands on the record it claims,
  which is the only thing a drop record is for.
---

Free-form notes.

## What the diff claims, and the direct check for each

**Claim 1 — `data/proposals/qwen38-max-revenue-share-licence.md` does not exist.**
Directory listing of `data/proposals/` in the branch worktree
(`D:/addictedtoai-worktrees/j-20260902-13-review-1`) printed 26 names. No
`qwen38-max-revenue-share-licence.md` among them; the only Qwen-named files at
that level are absent entirely. Confirmed.

**Claim 2 — it is at `data/proposals/dropped/qwen38-max-revenue-share-licence.md`.**
Listing of `data/proposals/dropped/` filtered to `qwen` printed exactly three:

```
qwen38-flash-next-qwen4-preview.md
qwen38-flash-next-recheck.md
qwen38-max-revenue-share-licence.md
```

Confirmed. So the pre-diff text pointed at a path that resolved to nothing and
the post-diff text points at a file that is there.

**The edit itself.** Read the whole of
`data/proposals/dropped/qwen38-flash-next-recheck.md` at the branch commit.
Line 33 now reads ``covered by `data/proposals/dropped/qwen38-max-revenue-share-licence.md`'s``.
The reflow onto the next line is the only other change to that paragraph; the
sentence is otherwise byte-identical, so nothing about the record's argument
moved. Worth noting the file's two *other* internal paths were already correct
before this diff — line 26's `dropped/qwen38-flash-next-qwen4-preview.md` and
line 45's bare `data/proposals/dropped/` — which is what makes the one wrong
segment look like the transcription slip it was, and means there is no second
instance of the same error left in the file.

## The retirement half

The brief required the carried-finding file to be deleted in the same diff, and
`git show --stat HEAD` shows it was:

```
 data/carried/j-20260902-07-carry-1.md               | 16 ----------------
 data/proposals/dropped/qwen38-flash-next-recheck.md |  4 ++--
```

I checked that deleting the file is genuinely what retires the queue item rather
than taking the brief's word for it. `pulse/lib/queue.mjs:394` defines
`carriedFindingItems(root)`, which reads `join(paths(root).root, 'data',
'carried')` and emits one item per file in it (`queue.mjs:427`), and
`queue.mjs:764` pushes those into the queue on every run. The item is derived
from the file's presence, so removing the file removes the item on the next
Pulse run. I also confirmed the live item is the one in question: reading
`data/derived/queue.json` and filtering for `qwen38-max-revenue` returned
exactly one of the three items, `"reason": "carried-finding"`, `"rank": 25`,
subject and target both
`data/proposals/dropped/qwen38-flash-next-recheck.md`. `queue.json` itself is
untouched by the diff, which is correct — it is a pure function of state and the
Pulse recomputes it.

## Dangling references

Grepped the whole worktree for the old string `data/proposals/qwen38-max-revenue-share-licence`.
Three hits remain, and none of them is a defect:

- `.job/brief.md:30` — the brief quoting the finding, a job artifact, not corpus.
- `data/reviews/j-20260902-07.md:21` — the originating verdict record, which
  quotes the wrong text as the evidence for its finding. Rewriting a historical
  review record to hide what it observed would be the wrong repair.
- `data/derived/queue.json:6` — the derived queue item above, regenerated by the
  Pulse.

So the corpus has no remaining reference to the non-existent path.

## Scope

`git diff --stat main...HEAD` is four paths: `.job/brief.md` and
`.job/source.json` (both loop-written job scaffolding, not the author's work),
the two-line correction, and the required deletion. Nothing else — no build
output, no adjacent tidying, no proposal filed. Within the stated outcome.

## Not re-run

`npm test` and `npm run build` were run by the loop on this same commit
(`209ccd3cacfe`) and both passed; per the brief I did not repeat them. Neither
would be sensitive to this change in any case — the edited file is under
`data/proposals/`, which the corpus schema does not validate and no page
renders from.

Nothing to carry, and no proposal. The finding was one wrong path segment, the
fix is that segment, and the mechanism that retires it fired correctly.
