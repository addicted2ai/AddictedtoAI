---
job: j-20260904-16
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a units layer is a prose change, not just a display
  change, would link the Command A entry: the sentence now reads "with a
  context window of 256000 tokens" instead of the noun-pile the transclusion
  produced once the feed unit started rendering, and the figure itself was
  never touched. It is the small worked example of binding a volatile value
  and then having to re-read the sentence around it.
carry:
  - title: Rephrase "a {{fact}} context window" on Muse Spark 1.3, the third instance
    detail: >-
      The same defect survives on a page the original finding did not name.
      content/wiki/model/meta-muse-spark-1-3.md:71 still reads "It keeps the
      family's million-token window — a
      {{fact:model/meta-muse-spark-1-3#context_window}} context window", and
      the built page at out/wiki/model/meta-muse-spark-1-3.html renders it "a
      1048576 tokens ... context window". The fact binds
      openrouter-models|context_length, which lib/units.mjs declares as
      "tokens", so it is the identical cause. The same rewrite this job
      applied — "a context window of {{fact:...}}" — fixes it without touching
      a figure. Out of scope here: the queue item named only cohere-command-a
      and meta-muse-glimmer-30b.
    subject: content/wiki/model/meta-muse-spark-1-3.md
subject:
  - "content/wiki/model/cohere-command-a.md"
  - "content/wiki/model/meta-muse-glimmer-30b.md"
reviewed:
  "content/wiki/model/cohere-command-a.md": "67abb3a6fab049141bdf3c1406e4a477c2d929f071ff92468553b7124809bb40"
  "content/wiki/model/meta-muse-glimmer-30b.md": "e782b5640df481c61d7f9ec84b352005f13b38de33c1b2d2c5079aaaa6bb235a"
---

## What the diff claims, and what I measured

Three files changed in the author's commit `1b6b878`: the two named content
files and the retired carry item. `.job/brief.md` and `.job/source.json` are in
the diff but come from the loop's own scaffolding commit `6fa278c`, not the
author's — `git show --name-status` on both commits confirms the split. No
scope violation.

**The premise is real.** `lib/units.mjs:64` declares
`'openrouter-models|context_length': 'tokens'`, and both entries' `context_window`
facts bind `feed: openrouter-models`, `path: context_length`. So the
transclusion emits value-plus-unit, which is what made "a {{fact}} context
window" render as a noun pile.

**The fix renders correctly.** The review worktree has no `node_modules`, so I
read the artefact from the author's worktree, which sits on the same commit
(`git worktree list` shows `D:/addictedtoai-worktrees/j-20260904-16` at
`1b6b878`), is clean (`git status --porcelain` empty), and carries an `out/`
built at 06:45 today. Tag-stripped text from the exported pages:

```
cohere-command-a.html:
  ... listed 2025-03-13 source , accessed 2026-08-29 with a context window of
  256000 tokens openrouter-models , last checked 2026-09-04 — a full-sized
  release, not a research check...

meta-muse-glimmer-30b.html:
  ... $1.10 per million tokens openrouter-models , last checked 2026-09-04
  output over a context window of 131072 tokens openrouter-models , last
  checked 2026-09-04 . The identical weights are usable two ways: ...
```

Both read as ordinary English now, and the unit still appears exactly once in
each sentence.

**No figure moved.** `data/derived/feed-rows.json` gives
`cohere/command-a -> {"context_length":256000,"tp":256000}` and
`meta/muse-glimmer-30b -> {"context_length":131072,"tp":131072}`, which is what
the pages render. The diff touches no number and no fact declaration, so there
was nothing to re-source: the values are feed-bound and the change is to the
prose around them.

**The carry item is retired in the same diff**, as its own instructions
require: `data/carried/j-20260903-07-carry-2.md` is deleted in `1b6b878`.

## What I did not re-run

`npm test` and `npm run build` had already been run by the loop on this commit
and both passed; I did not repeat them. The one thing worth checking that they
would not have caught is whether the edit leaves these two entries "mismatched"
against their review records — `specs/review` makes that a `verify-launch`
failure. It does not: both records
(`data/reviews/seed-wiki-model-cohere-command-a.md` and
`seed-wiki-model-meta-muse-glimmer-30b.md`) carry `verdict:` and no `reviewed:`
key, so they are **unbound**, which the same requirement says is counted and
fails nothing.

## The one thing I found beyond the diff

Grepping the corpus for the remaining `{{fact:...#context_window}} context
window` shape turned up exactly one more page, recorded under `carry:` above.
It is the same defect from the same cause, on a page the queue item did not
name, so fixing it here would have been the scope violation — but it should not
be lost.
