---
job: j-20260907-04
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that "the model was discontinued" and "one router stopped
  serving it for free" are different events, and that a row leaving a catalog is
  evidence of the second far more often than the first. This page is the worked
  case: same canonical slug and creation timestamp as the surviving row, 34 paid
  endpoints across 24 providers still live, weights still public and MIT on the
  Hub, no alias target and no deprecation date — and a delisting signature
  (metadata still resolves, endpoints array empty) that anyone tracking model
  availability can go and reproduce against their own rows.
carry:
  - title: Say what the vanished facts actually render, not "nothing"
    detail: >-
      The body says "The bound facts above render nothing rather than a dated
      last-known value". The second half is right and I verified it —
      data/derived/feed-rows.json holds "z-ai/glm-5.2:free":
      {"$as_of":null,"$status":null,"$vanished":true} — but the first half is not
      what a reader sees. resolveFact returns state 'vanished' with a null value,
      and lib/facts.mjs:271-277 renders that branch as the ABSENT span plus the
      as-of note, so the page reads "not published — last known value, as of an
      unrecorded date — the source no longer lists this row". "Render nothing"
      would fix to something like "render as not published, with no as-of date to
      show". Worth correcting on its own terms, and doubly so because the standing
      proposal render-pinned-last-known-for-vanished-rows would make this
      paragraph flatly wrong if it lands.
    subject: content/wiki/model/z-ai-glm-5-2-free.md
subject: "content/wiki/model/z-ai-glm-5-2-free.md"
reviewed:
  "content/wiki/model/z-ai-glm-5-2-free.md": "5d444b28286bd6eacf36056248d19bd3f80d832c1c80405489578ad2abdd49fd"
---

Free-form notes.

I did not re-run the four gates the loop already ran on `42c4bf2f1732`. I ran the
specific checks that would be red if a claim in the diff were false, and quote
each below.

## Scope and the mechanical requirements

The diff is the entry body plus a `mentions:` line, the vanished record moved,
and the two `.job/` files the loop writes. Nothing else. The `feeds:` binding is
untouched (`openrouter-models: z-ai/glm-5.2:free` still at line 11) and the entry
was not removed, as the brief required. `git diff --stat` reports the record as
`rename ... similarity index 100%` — moved into `data/vanished/answered/`
unchanged, not deleted, and I read the moved file to confirm the pinned table is
intact. `content/wiki/model/z-ai-glm-5-2.md` exists, so the added mention
resolves.

`status: active` / `maintenance: living` are unchanged, which I checked against
the six entries already in `data/vanished/answered/`
(opus-4-7-fast, opus-4-8-fast, opus-5-fast, granite-4.1-8b, nemotron-3-ultra
batch, qwen3.8-max): every one is `status: active, maintenance: living`. This
diff follows the established pattern rather than departing from it.

## Live sources — every one fetched today, 2026-09-07

`https://openrouter.ai/api/v1/models` → 200, **430 rows**. `z-ai/glm-5.2:free`
absent; `z-ai/glm-5.2` present. `:free` rows: **18**, authors
`cohere dots-studio google inclusionai liquid minimax nvidia poolside
thinkingmachines` — **nine**, none of them z-ai. The page's "430 rows",
"eighteen `:free` rows survive across nine other authors" and "`z-ai/glm-5.2`
itself is still listed" are exact.

`.../models/z-ai/glm-5.2/endpoints` → 200, **34 endpoints**, **24 distinct
provider_name** values: `DeepInfra | Ambient | StreamLake | Novita | Decart |
DigitalOcean | CoreWeave | AtlasCloud | Alibaba | SiliconFlow | Inceptron |
Phala | Mistral | BaseTen | Crusoe | Together | Baidu | Fireworks | Venice |
GMICloud | Parasail | Friendli | Cloudflare | Z.AI`. Every provider the page
names by hand — Z.AI, Alibaba, DeepInfra, Fireworks, Mistral, Together,
Cloudflare — is in that list. Endpoints with prompt and completion both 0:
**0**. "34 endpoints ... from twenty-four providers ... not one of them is
priced at zero" is exact.

`.../models/z-ai/glm-5.2:free/endpoints` → **200**, keys
`id,name,created,description,architecture,endpoints`, `endpoints` length **0**.
"Still resolves ... returns its metadata with an empty `endpoints` array" is
exact, and it is the load-bearing claim of the page.

`https://openrouter.ai/z-ai/glm-5.2:free` → 200. In the served HTML:
`<link rel="canonical" href="https://openrouter.ai/z-ai/glm-5.2"/>`;
`"modelSlug":"z-ai/glm-5.2:free" ... "aliasTarget":null`; and
`{"modelSlug":"z-ai/glm-5.2:free","hfSlug":"zai-org/GLM-5.2"}],"deprecationDate":null`.
All three sub-claims in the "It was not renamed either" paragraph hold, read off
the raw HTML rather than an extractor.

`https://openrouter.ai/docs/guides/routing/model-variants/free` → 200. The two
quoted fragments are present verbatim, in these sentences: "The `:free` variant
allows you to access free versions of models on OpenRouter." and "Free variants
provide access to models without cost, but may have different rate limits or
availability compared to paid versions." The page's paraphrase around them
("documents the suffix as access to ...") does not stretch either one.

`https://huggingface.co/api/models/zai-org/GLM-5.2` → 200,
`{"private":false,"gated":false,"disabled":false,"lastModified":"2026-09-01T11:36:01.000Z","license":"mit"}`.
"Public, ungated, not disabled and MIT-licensed ... last modified 2026-09-01" is
exact on all five.

## Local state

`data/changes.jsonl`, lines dated `2026-09-06`: four, of which exactly one
carries `"source":"openrouter-models"` — the `$retirement` of `z-ai/glm-5.2:free`,
whose excerpt records `"$status":"active"`, `"pricing.prompt":"0"`,
`"pricing.completion":"0"`, `"expiration_date":null`. The other three are
`llm-releases` arrivals (Ling-3.0-flash-Sante, Qwen3.8-Max-0902, GPT-6 Astra
Pro). So "the withdrawal is the only OpenRouter line it recorded that day, and
it records arrivals as readily as retirements" is true, and the "active, priced
at zero, with no expiry date" clause is read off the recorded excerpt. (A naive
`grep -c openrouter` returns 2 here — one llm-releases row carries an
openrouter.ai `source_url`. Counting on the `source` field is the check that
matters.)

Snapshots: `latest.json` is dated 2026-09-07 (430 rows), `previous.json`
2026-09-06 (430 rows). `z-ai/glm-5.2:free` is **absent from both** — which is
what the page says and why no as-of date exists. `z-ai/glm-5.2` in latest:
`canonical_slug: z-ai/glm-5.2-20260616`, `created: 1781631930`,
`context_length: 1048576`. The pinned record gives the withdrawn row
`canonical_slug: z-ai/glm-5.2-20260616`, `created: 1781631930`,
`context_length: 256000`. Same slug, same timestamp, and 256,000 against
1,048,576 — all four numbers in "two variants of one model" and "the free route
was the smaller one" are right. Worth noting the author resisted a trap here:
the pinned `description` for the free row itself says "1M-token context window",
and the page correctly used the row's own `context_length` instead.

`data/sources/llm-releases/latest.json`: the only retirement/deprecation
language in it is "retires its legacy deepseek-chat and deepseek-reasoner
aliases". No GLM 5.2 deprecation of any kind — the claim holds. GLM strings
present are all 5.2 Turbo and the 5.3 family.

## The one thing I checked and found imprecise

The claim "the bound facts above render nothing" is a claim about what the site
does, so I traced it rather than accepting it. `data/derived/feed-rows.json`
holds `"z-ai/glm-5.2:free": {"$as_of":null,"$status":null,"$vanished":true}` —
`pulse/lib/derive.mjs:132`'s literal for a row in neither snapshot. That row is
non-null, so `resolveFact` (`lib/facts.mjs:146`) takes the `$vanished` branch
with a null value, and `lib/facts.mjs:271-277` renders ABSENT plus "last known
value, as of an unrecorded date — the source no longer lists this row". So the
facts render *not published with no date*, not *nothing*. The substance the
sentence is carrying — that there is no dated last-known value, and why — is
correct and verified; the wording is not. Carried rather than blocked.

## No proposal

The obvious one is already filed. The render path above is exactly
`data/proposals/render-pinned-last-known-for-vanished-rows.md` (dated 2026-09-07,
type `machinery`, from the review of `j-20260906-19`), which names
`derive.mjs:119-132` and `facts.mjs:271-277` and proposes falling back to the
pinned `data/vanished/` record. Filing a second one would be noise.
