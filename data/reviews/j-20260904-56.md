---
job: j-20260904-56
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a served-byte count is a timestamp and not a document
  fingerprint would link this, because it is the rare case where the writer
  went back and measured the counter-example to their own tidy pattern: the
  rising-floor ordering held for four hours-apart rows, so the record fetched
  again at forty minutes' remove and reported that the floor came back level
  rather than higher. A reviewer asking "how far apart do two fetches have to
  be before a byte difference means anything?" gets an actual measured band
  here instead of an assurance.
---

Free-form notes.

## What the job was asked to do, and whether each finding is actually cleared

Three carried findings against
`data/reviews/evidence/verify-daybreak-defense-network-product-wording.md`, plus
deletion of the three files that keep them in the Pulse queue.

**Finding 1 — the "inside a single second" descriptor.** Cleared, and cleared in
the sharper of the two available ways. The old sentence lumped both close-spaced
runs under one wrong timescale. The new text splits them and gives each its own
measured span:

> In each of the two closely spaced runs the spread is under ten bytes — 21:36's
> four fetches inside one second, and 22:53's four across 5.3 seconds
> (04:52:58.726Z → 04:53:03.981Z in the transcript)

I checked both halves rather than just the one the finding named.

- 22:53 span, from the transcript block appended by j-20260904-54
  (`...raw.txt:75` and `:78`): `2026-09-05T04:52:58.726Z` →
  `2026-09-05T04:53:03.981Z`. That is 5.255 s, so "5.3 seconds" is right and the
  parenthesised timestamps are the ones actually in the file.
- 21:36 "inside one second" is the half the finding did *not* ask about, and it
  is the half that could have quietly inherited the original error. It holds
  independently — `data/reviews/j-20260904-51.md:59-64` records that review
  re-running the fetch itself:

  ```
  fetch 1: HTTP 200, 376020 bytes, t+448ms
  fetch 2: HTTP 200, 376067 bytes, t+570ms
  fetch 3: HTTP 200, 376063 bytes, t+727ms
  fetch 4: HTTP 200, 376057 bytes, t+847ms
  elapsed 848ms
  ```

  848 ms for four fetches. "Inside one second" is a measurement there, not a
  flourish, so attaching it to 21:36 alone is correct.
- "spread is under ten bytes" for both: 21:36 is 376,065 − 376,057 = 8; 22:53 is
  376,113 − 376,108 = 5. Both under ten.

**Finding 2 — the 20:32 row's two counts came from two header sets.** Cleared.
The row now reads `376,021 / 376,069 — two different header sets, see the
blockquote (UA only, then UA + accept-language + cache-control)`. I checked the
order against the blockquote it points at (lines 26-29 of the evidence file):
"**the full Chrome UA alone** → HTTP 200, 376,021 bytes; **UA +
`accept-language` + `cache-control`** → HTTP 200, 376,069 bytes." The parenthetical
maps the two numbers to the two header sets in the same order the blockquote
does, so the confound the finding wanted kept visible is visible, and the hedge
sentence it supports ("20:32's 376,069 sits above the whole 21:36 run") is
unchanged, as the finding said it should be.

**Finding 3 — the rising floor at sub-hour spacing.** Cleared, and this is where
the job did more than the finding asked. The finding suggested "a half-clause";
the diff instead adds the qualifier *and* a fresh measurement. Both claims in
the new passage check out against sources I read:

- The 23:10 numbers are the reviewer's, not this job's, and the text says so,
  citing `data/reviews/j-20260904-54.md`. That record's own transcript
  (lines 93-96) reads `bytes=376100`, `bytes=376116`, `bytes=376108` with
  machine clock `Fri Sep  4 23:10:08 MDT 2026`. Floor 376,100, which is indeed
  below the 22:53 row's floor of 376,108 (min of 376,112/376,108/376,112/376,113).
  The narrative reproduces the reviewer's figures exactly.
- The 23:33 run is this job's own, appended to the transcript with the same
  header set as the j-54 block. Transcript lines 90-92: 376,108 / 376,116 /
  376,108. Floor 376,108 — "level with that floor, not above it" is right; it is
  equal, and the text says equal rather than rounding it into "below".
- Interval arithmetic: 22:53 → 23:10 is 17 minutes ("seventeen minutes"),
  22:53 → 23:33 is 40 minutes ("forty minutes"). Both correct.
- UTC/local: `2026-09-05T05:33:4x` UTC at MDT (UTC−6) is 23:33 on 2026-09-04,
  matching the recorded machine clock `Fri Sep  4 23:33:46 MDT 2026` and the
  repository's local-date convention.

I want to note approvingly what the new fetch is *for*. The finding could have
been discharged with a hedge — a clause saying the ordering might not hold at
closer spacing costs nothing and is unfalsifiable. The job instead went and
measured at forty minutes and reported a result that makes the original pattern
weaker, not stronger. That is the direction of effort this review exists to
reward, and it is the opposite of a claim written from intent.

## The three carried files

All three are deleted in this diff — `data/carried/` now holds only `README.md`
and `j-20260903-14-carry-1.md`, so nothing from j-20260904-54 survives to
reappear on the next Pulse run. That is the retirement half of the outcome, done
as the brief specified.

## Scope

I checked the two things in the diff that are not the evidence file.

- `.job/brief.md` and `.job/source.json` appear as additions. **Not author
  scope-creep** — the loop writes them into the worktree itself
  (`loop/run.mjs:924-934`) and strips them mechanically before merge
  (`loop/run.mjs:1062`: `git rm -r -q --ignore-unmatch .job RESULT_FILENAME`).
  They show up here only because the diff is computed from branch state. I
  verified this rather than assuming it, because the branch's recent history has
  a commit named "remove job scaffolding before merge", which suggests a prior
  job did by hand what the loop does anyway.
- One proposal file, `author-worktrees-carry-no-node-modules.md`. Within the
  brief's allowance (at most one), and `machinery` ≠ `repair`, so it is not the
  self-amplifying case. I spot-checked its factual claims because they are
  claims about code and therefore cheap to check:
  - "called from exactly one place — `loop/lib/gates.mjs:177`" and three unlink
    callers: confirmed. `linkNodeModules` is defined at `gates.mjs:113` with its
    single call site at `gates.mjs:177`; `unlinkNodeModules` is defined at
    `gates.mjs:127` and called at `run.mjs:275`, `:562`, `:980`. The asymmetry
    is exactly as described.
  - "reaches `unlinkSync` on a directory, throws, and swallows the throw in a
    `catch` whose comment assumes the leftover is 'a junction that will not
    unlink'": confirmed verbatim at `gates.mjs:129-138` — the guard is
    `if (st.isSymbolicLink() || st.isDirectory())` and the catch comment reads
    `/* a junction that will not unlink is left; the worktree removal reports it */`.

  The proposal quotes its own code accurately, which is the part I can check; I
  did not re-run its `npm ci` measurement, and it is a proposal rather than a
  claim in the corpus.

## What I did not re-run

`npm test` and `npm run build` were run by the loop on `c8ce82d49bc3`, the commit
this diff was computed from, and both passed. I did not repeat them. The changed
file is an evidence record under `data/reviews/evidence/`, not corpus content
under a schema, so the checks that would be red if this diff were wrong are the
source checks above — the transcript timestamps, the j-51 and j-54 records, and
the `gates.mjs` line numbers — and those are what I ran.

## Prose

The added text does not read as filler. The one construction I looked at twice
is "The table row is the day's earliest fetch and sits below the rest for that
reason", which now sits after an inserted paragraph arguing the rising floor is
*not* a trend — I wanted to be sure "that reason" had not been orphaned by the
insertion. It has not: "that reason" refers within its own clause to being the
day's earliest fetch, so the sentence still stands on its own.

Nothing to carry and no proposal. The corrections are exactly the three that
were asked for, each is a measurement rather than a hedge, and the file is more
honest after the diff than before it.
