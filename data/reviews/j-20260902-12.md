---
job: j-20260902-12
verdict: approve
reasons: []
would-cite: >-
  Whoever picks up the `delta-review-brief-embeds-pre-revision-diff` machinery
  job would open this paragraph first, because it is the one place that turns a
  single 2026-09-01 anecdote into a dated, twice-observed pattern with the two
  git ranges and the two source lines (`loop/run.mjs:361`,
  `loop/lib/review.mjs:249`) that locate the bug — an argument for prioritising
  it that the original evidence paragraph could not make on its own.
carry:
  - title: Note that the delta review's gates report is stale in the same way the diff is
    detail: >-
      The added paragraph pins the stale-diff half but stops short of the
      stronger form of the same defect. `loop/run.mjs` runs the gates once, at
      line 342, before the review loop, and never re-runs them after the
      revision commit at line 505 — while `runReview` reads `sha` fresh from
      the branch head (`loop/lib/review.mjs:715`, `const before = rev-parse
      branch`) and hands it to `gatesSection`. So a pass-2 brief's sentence
      "The loop ran these itself, in this branch's own worktree, immediately
      before this review on commit `d29ae884daf0`" states a PASS measured on
      the PRE-revision tree under the POST-revision SHA. That is the same
      mismatch as the diff, in the section the brief tells the reviewer not to
      re-run — one added sentence to the evidence paragraph would record it,
      and the machinery job's "check that fails loudly if the two disagree"
      should cover the gate result as well as the diff text.
    subject: data/proposals/delta-review-brief-embeds-pre-revision-diff.md
---

The diff adds one paragraph to `data/proposals/delta-review-brief-embeds-pre-revision-diff.md`
and deletes `data/carried/j-20260902-08-carry-1.md`. Nothing else but the loop's
own `.job/` scaffolding. That is exactly the stated outcome, including the
retire-the-carried-file instruction, and it matches the shape of the previous
merged repair on this queue reason (`c1c9717`, which deleted
`data/carried/j-20260902-07-carry-2.md` and touched one target file, with no
derived-tree churn).

Placement is right too: the new text sits inside `## Evidence`, after the
existing measured paragraph and before `## Origin`, which is what the brief
asked for.

## Every claim in the new paragraph, checked against the thing it names

I did not re-run `npm test` / `npm run build` — the loop recorded both PASS on
`a6ecdfe8a8da`. I ran the checks that would be red if these specific sentences
were false.

**1. "the embedded diff was the pre-revision range: its fast-page hunk carries
`index 55869ad..9b7b984`, which `git diff 94dc6c3 00ecc2e` reproduces exactly"**
— true, byte for byte:

```
$ git -C D:/AddictedtoAI diff 94dc6c3 00ecc2e -- content/wiki/model/anthropic-claude-opus-5-fast.md | grep -E '^(index|---|\+\+\+|@@)'
index 55869ad..9b7b984 100644
--- a/content/wiki/model/anthropic-claude-opus-5-fast.md
+++ b/content/wiki/model/anthropic-claude-opus-5-fast.md
@@ -58,12 +58,12 @@ working and are served by the same fast tier capacity, so no action is
```

**2. "the pass-1 text review 1 rejected ('Anyone re-running this check should
note how the record reads')"** — present in that range, and removed by the
commit the paragraph says removed it:

```
$ git diff 94dc6c3 00ecc2e | grep -c 'Anyone re-running this check should note how the record reads'
1
$ git diff 00ecc2e d29ae88 | grep -n 'Anyone re-running'
9:-Anyone re-running this check should note how the record reads: HTTP 200,
```

**3. "`git diff 00ecc2e d29ae88` ... is absent: the range stops one commit short
of the SHA the brief named"** — `git diff --stat 00ecc2e d29ae88` prints
`content/wiki/model/anthropic-claude-opus-5-fast.md | 17 ++++++------` plus the
job's one proposal, so the revision is real and non-empty, and the embedded
range ends at `00ecc2e`. `d29ae884daf0` is `d29ae88 job j-20260902-08: reframe
the endpoints caveat, name the check, drop the orphaned wrap (done)` — the
commit after the range's end, exactly as claimed.

**4. "that sentence is assembled by `loop/lib/review.mjs`"** — true, and the
quoted string is the template's literal output:

```
$ grep -n "the same commit the diff below was computed from" D:/AddictedtoAI/loop/lib/review.mjs
265:this review${on} — the same commit the diff below was computed from:
```

with `const on = sha ? \` on commit \\\`${sha.slice(0, 12)}\\\`\` : ''` at line
249. `sha.slice(0, 12)` of `d29ae884daf038…` is `d29ae884daf0`, which is the
twelve characters the paragraph quotes.

**5. The delta reviewer's quote** — `data/reviews/j-20260902-08.pass2.md` line
34 reads "The diff in my brief is **not** the branch state.", bold markup
included. Verbatim, right file. Its lines 48–50 independently corroborate the
`55869ad..9b7b984` = `94dc6c3..00ecc2e` identification and the "stops one commit
short" reading, so the paragraph is not the reviewer's claim restated on trust —
I reproduced both from git.

**6. "`loop/run.mjs` computes the review diff once, before the revision pass,
and the delta review reuses that text"** — true and structural, not incidental:
`const diffText = diffAgainst(...)` is line 361, *outside* the `for (;;)` review
loop that begins at 364; it is a `const`, never reassigned; the revision commit
lands at line 505 and the loop sets `pass = 2` at 523 and re-enters `runReview`
with the same `diffText` binding (passed at line 384).

**7. "The machinery is unchanged"** — `git log --oneline --since=2026-08-31 --
loop/run.mjs loop/lib/review.mjs` returns only `887ea8a` and `b83df44`, both
dated 2026-08-31, i.e. nothing since the proposal was filed on 2026-09-01. The
"unfixed" characterisation is measured, not assumed.

**8. Durability of the citations.** The three SHAs the paragraph asks a future
reader to run are not stranded on a deleted branch:
`git merge-base --is-ancestor d29ae88 main` exits 0, so `94dc6c3`, `00ecc2e` and
`d29ae88` are all in main's history and the quoted commands stay reproducible
after the job branches are pruned.

## On "nearly cost two jobs"

Both occurrences are near-misses, and the paragraph says so plainly rather than
inflating them into losses ("the job survived", "is now a near-miss observed
twice in two days, not a hypothetical"). That is the honest reading of both
records, and it is still the right argument for prioritising: the reason nothing
was lost is that two reviewers happened to check `rev-parse HEAD` against their
brief, which is not a mechanism.

## Worth reading

Yes, for its one audience. This is not prose for the site; it is the evidence a
machinery job will act on, and it earns its length by being entirely composed of
things a reader can re-run — two git ranges, two file:line pointers, one quoted
record. Nothing in it is asserted from intent.

## Not blocking

One carry, in the front matter: the gates *result* embedded in a pass-2 brief is
stale in the same way the diff is, which this paragraph does not yet say. See
the `detail` field for the two line numbers.

## Housekeeping

I changed nothing in the reviewed worktree; all checks were reads and `git diff`
against `D:/AddictedtoAI`.
