---
job: j-20260913-01
verdict: approve
reasons: []
would-cite: >-
  A maintainer tracking a vanished model would cite this entry to show, from
  OpenRouter's catalog removal and redirect plus Inception's production release,
  that the preview name graduated at the catalog level while the evidence still
  leaves continuity of weights unresolved.
subject: "content/wiki/model/inception-mercury-2-5-preview.md"
reviewed:
  "content/wiki/model/inception-mercury-2-5-preview.md": "f0f8dca28a7aafe29db94145a777a0581985a6406995b38644006c4e193b0b33"
---

Free-form notes: Reviewed the actual branch HEAD `8e0913b10d06182dc7cea94f0df5760eab70c705`; the pass-2 delta from its prior reviewed target blob `1c5ecb7941a617d23c6f3abd7389a7f38d653124` is the requested repair wording. The target keeps `feeds: openrouter-models: inception/mercury-2.5-preview`, and the vanished record is an R100 rename into `data/vanished/answered/`. No blog post is in the diff, so no voice carry-forward is required.

The focused pass-2 diff is one target page (`git diff HEAD^ HEAD` reported 50 changed lines). Its opening now says the preview row was gone, that OpenRouter dropped it and the replacement is a catalog-level rename/graduation, gives the successor creation timestamp as `2026-09-08T18:28:57Z`, says the old route redirects, and explicitly leaves weight continuity unanswered. The new timeline entries separately cite the Inception blog and OpenRouter catalog.

Direct checks:

- OpenRouter redirect: `curl.exe` printed `HTTP/1.1 307 Temporary Redirect` and `Location: /inception/mercury-2.5`.
- Endpoint check printed `unversioned_id: inception/mercury-2.5`, `unversioned_endpoints: 1`, `dated_id: inception/mercury-2.5-preview`, `dated_endpoints: 0`.
- Catalog check printed `total: 445`, `matches: inception/mercury-2.5`, `preview_present: False`, and `successor_present: True`.
- Inception's announcement returned HTTP 200 and the fetched markup contained the quoted sentence `Today, we’re releasing Mercury 2.5, our most capable production model yet`.
- A direct comparison of the committed 2026-09-09 predecessor/successor rows printed exactly one substantive difference: `hugging_face_id ""` versus `null`; the named context, output cap, parameter list, prices, and description matched.
- The vendor models page contained the preview slug exactly twice, both in links to OpenRouter; after stripping scripts, styles, and tags, the models page and docs had zero visible `preview` word matches. This supports the narrowed claim that neither offers the preview slug as a current model id.

The supplied `npm run build` and `node scripts/verify-surfaces.mjs out` PASS results were not rerun. GitNexus could not attach the temporary review worktree to the available index: `Repository "D:\\Temp\\addictedtoai-worktrees\\jobs\\j-20260913-01-review-2" not found`; I make no complete graph-backed claim. No source code symbol or process changed in the pass-2 delta.
