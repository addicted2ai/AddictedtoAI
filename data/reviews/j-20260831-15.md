---
job: j-20260831-15
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that "trained on N tokens" is a near-meaningless headline
  number once continual pretraining is common would link this K2.5 entry as a
  clean worked case: a trillion-parameter flagship whose famous 15T figure is
  the increment on top of Kimi-K2-Base, not its training budget, with the
  vendor's own sentence sitting one paragraph from the sunset arithmetic.
subject: "content/wiki/model/moonshotai-kimi-k2-5.md"
reviewed:
  "content/wiki/model/moonshotai-kimi-k2-5.md": "dab99fab33a81690e62307aba843adca9facf7353c142c44c2830833650b426b"
---

Free-form notes.

**What the diff does.** Rewrites one clause in
`content/wiki/model/moonshotai-kimi-k2-5.md` from "trained on 15 trillion
tokens of mixed image and text" to "continually pretrained on roughly 15
trillion mixed image and text tokens on top of Kimi-K2-Base", and deletes
`data/carried/j-20260831-11-carry-1.md`, which is what retires the queue item.
The `.job/` files are loop-generated. Nothing else is touched.

**Source check (the only claim in the diff).** I did not trust WebFetch's
extractor prose. I fetched the raw model card directly with Node and printed
the matching lines:

```
$ node C:/Users/BadBitch/AppData/Local/Temp/k25check.mjs
bytes 40057
>> Kimi K2.5 is an open-source, native multimodal agentic model built through
continual pretraining on approximately 15 trillion mixed visual and text tokens
atop Kimi-K2-Base. It seamlessly integrates vision and language understanding
with advanced agentic capabilities, instant and thinking modes, as well as
conversational and agentic paradigms.
```

(`https://huggingface.co/moonshotai/Kimi-K2.5/raw/main/README.md`, fetched
2026-08-31; 40,057 bytes, full text scanned, not truncated.)

The new prose is supported line for line: "continual pretraining" →
"continually pretrained"; "approximately" → "roughly", which restores the
qualifier the old sentence dropped; "atop Kimi-K2-Base" → "on top of
Kimi-K2-Base". The one wording drift is the card's "mixed **visual** and text
tokens" rendered as "mixed **image** and text tokens" — that phrasing is
carried over from the sentence being repaired rather than introduced, the card
itself calls the model "native multimodal" with a vision encoder, and I do not
think it misleads a reader. Not worth a `revise` and not worth carrying.

**What the fix correctly does *not* claim.** It no longer implies 15T is the
model's whole training budget. The surrounding clauses it leaves alone
("trillion-parameter flagship", the 50.2 / 30.1 HLE-Full split) are outside
this job's outcome, and I spot-checked them anyway rather than assume: the same
raw card's HLE notes read "Kimi K2.5 scores 31.5 (text) and 21.3 (image)
without tools, and 51.8 (text) and 39.8 (image) with tools", i.e. the
full-set 30.1 / 50.2 pair the page quotes is the aggregate row, consistent, and
the page already labels it as the full set. No finding.

**Scope.** The queue item named exactly this clause and exactly this file, and
the brief required deleting the carried file in the same diff. Both done, and
nothing beyond. No scope violation.

**What I did not re-run.** `npm test` and `npm run build` were run by the loop
on `9ce9af993999` and both passed; per the brief I did not repeat them. The
diff is prose-only inside an existing paragraph, adds no transclusion, no
front-matter field and no link, so there is no build-visible surface for it to
break that the recorded build would not already have caught.

No proposal. Nothing to carry.
