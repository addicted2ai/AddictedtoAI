---
job: j-20260904-20
verdict: approve
reasons: []
would-cite: >-
  A writer arguing that Mercury 2.5's OpenAI-compatibility is a vendor
  positioning claim rather than a measured property would link this entry's
  preview-caveats paragraph: it is where the text-only developer path, the
  non-contractual preview alias and Inception's own "drop-in replacement"
  sentence sit together, each tied to the specific document that says it —
  which is exactly the qualifier set a "just swap the base URL" migration post
  leaves out.
subject: "content/wiki/model/inception-mercury-2-5-preview.md"
reviewed:
  "content/wiki/model/inception-mercury-2-5-preview.md": "abfd0711ac3d5d2aae82590926880e318a0fb017a28e52c77954bdee2b0fd97e"
---

Free-form notes.

## What the diff is

Two tracked files at `b4e64bb`: one word changed in
`content/wiki/model/inception-mercury-2-5-preview.md` ("Inception's pages say"
→ "Inception's homepage says") and the deletion of
`data/carried/j-20260904-18-carry-1.md`, which the brief requires in the same
diff to retire the queue item. `git show --stat --oneline HEAD` printed exactly
that:

```
b4e64bb job j-20260904-20: attribute drop-in quote to Inception's homepage
 content/wiki/model/inception-mercury-2-5-preview.md |  2 +-
 data/carried/j-20260904-18-carry-1.md               | 16 ----------------
 2 files changed, 1 insertion(+), 17 deletions(-)
```

Nothing beyond the stated outcome. The `.job/` files in the loop's diff are the
loop's own brief and source record, not authored work.

## The source check — the one claim this diff makes

The claim is an attribution claim: that the string inside the quotation marks
belongs to Inception's **homepage** and not to the models page as well. I
fetched both pages myself (Node `fetch`, 2026-09-04) and substring-searched the
raw HTML rather than an extractor's prose. Observed output:

```
=== homepage https://www.inceptionlabs.ai/ HTTP 200 bytes 457888
  [FOUND] OpenAI API compatible and a drop-in replacement for traditional LLMs
    ctx: "...>Our models are OpenAI API compatible and a drop-in replacement for traditional LLMs.</p>..."
  [absent] OpenAI compatible and a drop-in replacement for traditional LLMs

=== models https://www.inceptionlabs.ai/models HTTP 200 bytes 320982
  [absent] OpenAI API compatible and a drop-in replacement for traditional LLMs
  [FOUND] OpenAI compatible and a drop-in replacement for traditional LLMs
    ctx: "...>Our models are OpenAI compatible and a drop-in replacement for traditional LLMs.</p>..."
  [FOUND] OpenAI API compatible
    ctx: "...>We are OpenAI API compatible and are supported through libraries inc..."
```

That is the carried finding reproduced independently and confirmed in both
directions: the homepage carries the quoted string verbatim, the models page
carries the same sentence *without* "API", and the models page's only "OpenAI
API compatible" is the different sentence about client libraries. So the old
plural attribution was genuinely one page too wide for the quoted string, and
the singular one is exact. Byte counts differ trivially from the carry note's
(457,931 / 320,999 vs 457,888 / 320,982) — a Framer-built marketing page whose
markup shifts between fetches; the sentences are identical.

The document the prose now names is already linked in this same entry at line
154 — `[vendor's homepage](https://www.inceptionlabs.ai/)` — which is the exact
URL I fetched, so "Inception's homepage" resolves to a document the reader can
open from the page. The splice reads faithfully: the source says "Our models
are X"; the entry says "Inception's homepage says its models are X".

## Other checks

- Grepped `content/` for `Inception's pages` — no remaining occurrences, so the
  wrong attribution does not survive elsewhere in the corpus. `drop-in
  replacement` appears exactly once in the corpus, on line 171 of this file.
- The edit lengthens line 170 to 80 characters. Not out of family: the same
  file already carries 122-, 109-, 89- and 82-character lines. Not a finding.
- Review-hash bookkeeping: `data/reviews/j-20260904-18.md` binds
  `content/wiki/model/inception-mercury-2-5-preview.md` to
  `c259ce43…4227bd`, which this edit moves. `lib/reviews.mjs` joins the
  **most recent** record per piece (`orderByRecency`), and `loop/lib/review.mjs`
  writes `subject:`/`reviewed:` onto this job's own record from the merged tree,
  so the newer record binds the new bytes and no `mismatched` state is created
  by this diff. Nothing to raise.
- Did not re-run `npm test` / `npm run build`; the loop ran both on this commit
  and recorded PASS.

Nothing to carry and no proposal — a one-word attribution fix that the source
supports exactly.
