---
job: j-20260901-02
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a batch-inference SKU can disappear from a broker's
  catalog while its non-batch sibling stays listed would link this entry: it is
  the dated, sourced record that "Mistral: Codestral 2508 (batch)" was in the
  OpenRouter catalog on 2026-08-31 and out of it on 2026-09-01, with
  mistralai/codestral-2508 itself unaffected — the kind of fact nobody keeps
  once a row is deleted from a live API.
subject: "content/wiki/model/mistralai-codestral-2508-batch.md"
reviewed:
  "content/wiki/model/mistralai-codestral-2508-batch.md": "fedd40ee639f46ad58a053b9a9645e49fb8d87c47ee2b0f2563d74312505c5b7"
---

Free-form notes.

What the diff claims, and what I checked for each.

1. **"removed from the OpenRouter model catalog"** — checked live, not
   inferred. `curl -s https://openrouter.ai/api/v1/models` returned
   `200 695645`; `grep -c "codestral-2508:batch"` on the response printed `0`,
   while `grep -o "mistralai/codestral-[a-z0-9.:-]*" | sort -u` printed exactly
   `mistralai/codestral-2508`. So the `:batch` variant is genuinely delisted
   right now and the plain row survives — the delisting is real and is not a
   fetch artefact.

2. **"last listed in the 2026-08-31 snapshot"** — checked against the committed
   snapshots. `grep -c "codestral-2508:batch"` printed
   `data/sources/openrouter-models/latest.json:0` and
   `data/sources/openrouter-models/previous.json:2`. `previous.json` carries
   `"date": "2026-08-31"` / `"fetched_at": "2026-08-31T06:00:03.897Z"`;
   `latest.json` carries `"date": "2026-09-01"` /
   `"fetched_at": "2026-09-01T06:00:03.724Z"`. The timeline row's date and its
   "last listed" clause both hold.

3. **The diff layer agrees.** `data/changes.jsonl` line 108 records
   `"kind":"retirement"`, `"row_id":"mistralai/codestral-2508:batch"`,
   `"date":"2026-09-01"`, `"source_url":"https://openrouter.ai/api/v1/models"` —
   the same URL the timeline entry cites, and the same date it carries.

4. **The derived files are consistent with the removal, not with a partial
   hand-edit.** In the branch tree, `grep -c "codestral-2508:batch"` on
   `data/derived/feed-rows.json` prints `0`; `freshness.json`'s
   `vanished_feed_rows` still lists exactly the four untouched siblings
   (`mistralai/ministral-8b-2512:batch`, `mistral-large-2512:batch`,
   `mistral-medium-3.1:batch`, `mistral-small-2603:batch`), which is why the
   queue moves 8 → 7 and not to some other number. `search-index.json`'s `s`
   field for this id moves `active` → `retired`, matching the front matter.

5. **Shape and scope.** The content edit is line-for-line the same repair the
   already-merged `j-20260901-01` made to
   `content/wiki/model/kwaipilot-kat-coder-air-v2-5.md` (commit `7420767`):
   `status active → retired`, `maintenance living → dormant`, the `feeds`
   binding and its four feed-bound facts dropped because they cannot resolve
   against a row that no longer exists, and one dated, sourced timeline entry
   replacing `timeline: []`. Nothing outside the named target and its derived
   consequences is touched; `.job/brief.md` and `.job/source.json` are loop
   scaffolding the merge strips, as it did on the precedent branch. No
   `scope-violation`.

Not run: `npm test` and `npm run build` — the loop reports both PASS on this
same commit `0f48167b2038` and re-running them is the one way this review runs
out of time.

One thing I considered and am not blocking on: this repair removes the entry's
last-known price and context-window bindings, so the vanished-row
"last-known value with an as-of date" rendering no longer applies to this page —
the page keeps the death record but loses the numbers it died with. That is the
established, twice-approved shape for this repair (a fact bound to a deleted row
cannot be re-verified), so it is a design question for the corpus rather than a
defect in this diff, and it is not this job's to settle.
