---
job: j-20260904-06
verdict: approve
reasons: []
would-cite: >-
  A defender or a policy reader trying to answer "what does a $1B pledge to
  frontline defenders actually buy, and who is allowed to touch the model
  behind it" would link this page, because the answer is split across four
  OpenAI documents that never appear together: the June expansion post, the
  1 September Preparedness footnote that quietly says Astra's published cyber
  numbers are Daybreak Blue numbers, the 3 September commitment, and a help
  center article that is the only place stating Daybreak and "Trusted Access
  for Cyber" are one program — and the only place that flatly contradicts the
  release page's "less restrictive safeguards in the coming weeks" with
  "Reduced refusals aren't available on Astra for most Daybreak customers."
  Nobody arguing about gated cyber capability wants to reassemble that; this
  page is the reassembly, dated and sourced line by line.
carry:
  - title: Drop OpenAI's "our" from the blue_definition fact value
    detail: >-
      The revision did not touch this, and pass 1 raised it as a carry rather
      than a blocker, so it survives into the merged entry. `blue_definition`
      is OpenAI's verbatim sentence "Daybreak Blue supports common defensive
      work with our mainline models". I read `renderFact` in `lib/facts.mjs`
      (the `cited` branch, lines 249-257): the value is emitted bare inside
      `<span class="fact-value">`, escaped, with no quotation marks, followed by
      "source, accessed 2026-09-04". Inlined mid-sentence in the body's second
      paragraph, the built page therefore says "with our mainline models" in
      this site's voice, about models this site does not own. `red_definition`
      has no such problem because OpenAI wrote that one in the third person.
      Changing the value to "with OpenAI's mainline models" keeps it faithful
      and stops the site claiming the models.
    subject: content/wiki/concept/openai-daybreak.md
  - title: Hand-link the two Daybreak posts that only use the bare name
    detail: >-
      Measured, not inferred. Running this branch's `lib/linker.mjs` over the
      whole body of each post with the linkable alias set the branch's own
      build wrote to `data/derived/aliases.json`, the new entry receives exactly
      one inbound automatic link, from
      `content/blog/openai-gpt-6-astra-system-card.md` (the one place in the
      corpus that writes the full string "OpenAI Daybreak").
      `content/blog/openai-daybreak-frontline-defenders.md` and
      `content/blog/openai-astra-critical-designation.md` get zero, because they
      only ever write "Daybreak" and "Daybreak Blue", both correctly `manual`
      now and therefore never auto-linked. That is the spec working as intended,
      not a defect — but the entry exists so those posts have somewhere to point,
      and the fix is two hand-written markdown links (one per post) to
      `/wiki/concept/openai-daybreak` at the first mention of the program.
    subject: content/blog/openai-daybreak-frontline-defenders.md
proposal:
  slug: detect-self-overlapping-exclusive-aliases
  type: machinery
  date: 2026-09-04
  summary: >-
    Add a build check for the alias shape this job's two reviews each had to
    find by hand: one entry declaring two `exclusive` aliases where the shorter
    is a whole-token substring of the longer. `lib/linker.mjs` rule 3 refuses
    overlapping candidates with no tie-break, "not even when they point at the
    same entry", so that pair makes the entry unlinkable on exactly the pages
    that use its long form — a silent zero-link outcome no gate reports. The
    same check is the natural home for the spec's currently unenforced MUST
    that single common words and bare brand tokens be `manual`.
  evidence: >-
    `lib/aliases.mjs` fails the build only when two DIFFERENT entries claim one
    name as exclusive (line 59, `exclusives.length > 1`); a single entry
    claiming both "OpenAI Daybreak" and "Daybreak" as exclusive passes cleanly,
    and `npm run build` did pass on that state at commit 014120e. Pass 1 of this
    job measured the consequence (0 links on the sentence at
    content/blog/openai-gpt-6-astra-system-card.md:56) and required the
    demotion; on 2026-09-04 I re-ran the fixed state through the same linker
    over the whole post and got 1 link wrapping "OpenAI Daybreak", confirming
    both halves of the mechanism. Pass 1 noted this proposal in a `revise`
    record and it is absent from data/proposals/, so re-noting it here is the
    only way it survives the job.
subject: "content/wiki/concept/openai-daybreak.md"
reviewed:
  "content/wiki/concept/openai-daybreak.md": "0e89790d7307f6e0833a644d1ac973d6170291aa2c347c6b0d70264163c1286b"
---

Delta review, pass 2. The previous verdict was `revise` on one named defect: the
`"Daybreak"` alias classed `exclusive` where `specs/wiki` requires `manual` for
single common words and bare brand tokens. I reviewed only what changed since
that verdict, plus a measurement of whether the change did what it was required
to do.

## The brief's embedded diff is stale — read the branch, not the brief

The brief states the diff below it was computed from commit `b60537374601`. It
was not: the entry it shows carries `class: exclusive` for `"Daybreak"`, which
is the pre-revision state at `014120e`. The branch tip actually carries the fix.
This is the exact failure already filed as
`data/proposals/review-delta-diff-computed-one-commit-stale.md` (dated
2026-09-03, still unselected), so I am not re-filing it — but a reviewer who
trusted the brief's provenance statement here would have re-raised a defect that
is fixed and discarded the job on its last pass, which is the outcome that
proposal exists to prevent. Recording the second occurrence.

The real delta, `git diff 014120e HEAD`:

```
 content/wiki/concept/openai-daybreak.md |  2 +-
 data/derived/aliases.json               | 40 +++++++++++++++++++++++++++++++++
 data/derived/backlinks.json             | 12 ++++++++++
 data/derived/search-index.json          | 16 ++++++++++++-
 4 files changed, 68 insertions(+), 2 deletions(-)
```

One content line:

```diff
   - name: "Daybreak"
-    class: exclusive
+    class: manual
```

The three `data/derived/` files are build output, not authored text, and they
are the branch's own build's output: the author's worktree
(`D:/addictedtoai-worktrees/j-20260904-06`, at the same commit `b605373` the
loop built and tested) reports `git status --porcelain` empty, so the committed
derived tree is byte-identical to what the passing build produced. `derived` is
specified as a pure function of state, and its content here is consistent with
the one-line change:

```
{ "name": "Daybreak",        "linkable": false, "claimed_by": [{ "class": "manual" }] }
{ "name": "OpenAI Daybreak", "linkable": true,  "claimed_by": [{ "class": "exclusive" }] }
```

Nothing else in the delta. No content file outside the outcome, no reserved
path, no second proposal. Not a scope violation.

## Did the fix do what it was required to do? Measured, twice

The required change was justified by a measurement (0 links before, 1 after), so
I re-ran the measurement rather than reading the diff and assuming.

**Instrument note, honestly stated.** This review worktree has no
`node_modules` — `lib/corpus.mjs` fails immediately with
`Cannot find package 'fast-glob'`, so I could not drive the real
`renderMarkdown` pipeline (already filed as
`data/proposals/review-worktrees-carry-no-node-modules.md`; not re-filing).
`lib/linker.mjs` has zero imports, so I ran it directly, feeding it the linkable
alias set from the branch's own `data/derived/aliases.json` (76 linkable names)
and a hast tree I built from each post's real body — headings as `h2`,
`[label](url)` as `<a>`, backticked spans as `<code>`, matching the tags rule 6
blocks. `concatText` before and after linking was byte-identical in every run,
which is the linker's own rule-1 assertion.

First attempt, single sentence only, `openai-gpt-6-astra-system-card.md:56`:

```
of which point at concept/openai-daybreak: [{"name":"OpenAI Daybreak", ...}]
links to concept/openai-daybreak: 0 []
all links made: []
```

Zero — because in an isolated sentence the alias `"OpenAI"` (linkable, →
`org/openai`) has its first occurrence *inside* `"OpenAI Daybreak"`, and rule 3
refuses both. That is a fixture artifact, not the site's behaviour: rule 4 gives
each alias exactly one candidate, its first eligible occurrence **on the page**.
So I re-ran over each whole document, which is what the build does:

```
=== content/blog/openai-gpt-6-astra-system-card.md
  text byte-identical after linking: true
  links to concept/openai-daybreak: 1 [{"name":"OpenAI Daybreak","id":"concept/openai-daybreak"}]
  all links made: [GPT-5.6 Sol -> model/openai-gpt-5-6-sol, OpenAI -> org/openai,
                   OpenAI Daybreak -> concept/openai-daybreak]

=== content/blog/openai-astra-critical-designation.md
  links to concept/openai-daybreak: 0 []

=== content/blog/openai-daybreak-frontline-defenders.md
  links to concept/openai-daybreak: 0 []
```

The link the previous review said was missing now exists: `"OpenAI"` is spent on
an earlier occurrence in the full post, `"OpenAI Daybreak"` no longer collides
with a self-overlapping `"Daybreak"`, and the entry gets its inbound link. Note
the first probe as the reason I do not trust a one-sentence fixture: the two
runs disagree, and only the whole-document one is the site.

The other two posts get no automatic link, because neither ever writes the full
name — they write "Daybreak" and "Daybreak Blue", now `manual` and therefore
never auto-linked, which is precisely what the spec's MUST asks for. That is the
spec's accepted trade ("the cost of refusing is a missing link, which is a
non-event"), not a new defect, and I have carried the two-line hand-link fix
above rather than blocking on it.

## Spec conformance of the new classing

`specs/wiki`, "Aliases are registered, classed, and collision-safe": `manual` —
"Single common words and bare brand tokens (`"Claude"`, `"Gemini"`, `"Llama"`)
MUST be `manual`." `"Daybreak"` is both, and is now `manual`. `"Daybreak Blue"`
and `"Daybreak Red"` were already `manual`; `"OpenAI Daybreak"` remains the
single `exclusive`, linkable name, matching the spec's own worked example shape
(the distinctive full name linkable, the short common token not). The alias
still reaches readers through the client-side name search — `search-index.json`
carries `"a": ["Daybreak","Daybreak Blue","Daybreak Red"]` for this page — so
demoting it cost discoverability nothing.

## What I did not re-check

Pass 1 fetched all five cited sources through the browser-header route and
confirmed every quoted fragment by substring search, one by one, including the
two pages `WebFetch` 403s on; it found every fact, every prose quote and every
timeline dateline supported, and its only objection was the alias classing. None
of those files changed in this delta — the entry's facts, timeline, prose and
mentions are byte-identical to the state pass 1 verified, which `git diff
014120e HEAD` above shows exhaustively. Re-fetching five pages to re-confirm
text that did not change is the way this pass runs out of time; the delta brief
asks for the delta, and the delta is one line plus its build output.

I did verify the one prose-adjacent claim I am carrying forward, in the code
rather than by eye: `lib/facts.mjs:249-257` emits a `cited` fact's value bare,
with no quotation marks, which is what puts OpenAI's "our" in this site's voice
when `blue_definition` is inlined.

`npm test` and `npm run build` both PASS on `b605373` per the loop, run in the
author's worktree; I did not re-run them. The defect pass 1 caught was invisible
to both, which is the proposal above.
