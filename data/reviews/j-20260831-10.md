---
job: j-20260831-10
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that "open weights change what a retirement means" now has a
  single dated worked example to paste: on 31 August 2026 Moonshot's own docs
  flipped kimi-k2.5 to past tense and a 404, OpenRouter silently blanked the
  expiry field it had carried since before the 28 August snapshot, and the
  Modified MIT weights stayed downloadable through both. I would also link it
  in the narrower argument that vendor lifecycle signals and router metadata
  are not interchangeable evidence — this page is the case where the two
  surfaces disagreed on the day it mattered, and it shows both rather than
  picking the convenient one.
subject: "content/wiki/model/moonshotai-kimi-k2-5.md"
reviewed:
  "content/wiki/model/moonshotai-kimi-k2-5.md": "c547ec7c2a17992216c08186a8fa191a3a2a2407fa170febee909f2842999867"
---

Reviewed the diff at `5d0c463e0306`. Did not re-run `npm test` / `npm run build`
— the loop recorded both PASS on this commit. Ran the specific checks that would
be red if a claim in the diff were false.

## The two new quotations — fetched and confirmed verbatim

`curl -L https://platform.kimi.ai/docs/models` → `http=200 size=258244`. Stripped
tags and searched the text. Both quotes are present word for word.

Paragraph 1's quote, as the page serves it today:

> The kimi-k2.5 and moonshot-v1 series were officially retired on August 31,
> 2026. Calls to these models now return a 404 (model not found) error. Please
> migrate to kimi-k3 or other latest models.

The entry quotes that minus the leading "The" and with the two model names in
code ticks. The "pointing migrations at `kimi-k3`" clause is supported by the
sentence that follows.

Paragraph 2's quote, under the page's own `Deprecated Models` heading:

> Deprecated Models — kimi-k2.5 was officially discontinued on August 31, 2026
> and is no longer maintained or supported. Please use the latest Kimi model
> kimi-k3 for continued support and enhanced reasoning capabilities.

So "files `kimi-k2.5` under 'Deprecated Models'" is literally true: it is a
section heading, and further down the same section a table lists
`kimi-k2.5  Deprecated`. That is a named source for keeping `status: deprecated`,
which is what the brief's status clause required.

The claim that the vendor page "has now moved to the past tense" is corroborated
rather than asserted: the wording the entry quoted before this revision — "no
longer available to newly registered users" — now returns **0** occurrences on
that page. The page really did change, so the author fetched it rather than
inventing the new wording.

## The retained count — 388, and it is right

This looked wrong at first. `content/tutorials/openrouter-catalog-watch.md` records
`rows 398` for a 2026-08-28 snapshot, and `content/wiki/tool/openrouter.md` carries
`models_listed: 398, accessed 2026-08-28`. Walked the git history of
`data/sources/openrouter-models/latest.json` via `execFileSync('git', ...)`:

```
snapshot.date=2026-08-28  rows=388  non-null expiration_date=8  kimi-k2.5.expiration_date="2026-08-31"
snapshot.date=2026-08-29  rows=396  non-null expiration_date=7  kimi-k2.5.expiration_date="2026-08-31"
snapshot.date=2026-08-30  rows=396  non-null expiration_date=7  kimi-k2.5.expiration_date="2026-08-31"
snapshot.date=2026-08-31  rows=396  non-null expiration_date=6  kimi-k2.5.expiration_date=null
```

The Pulse's 28 August snapshot was fetched at `18:19:35Z` and holds 388 rows; the
tutorial's own script fetched later the same evening (`2026-08-28T20-26-39` and
`20-40-24`) and got 398. Both records are true of different fetches hours apart.
388 is the correct count for the snapshot this entry's feed binding actually reads,
and the eight rows are exactly the eight the entry means:

```
2026-08-31 moonshotai/kimi-k2.5      2026-12-31 z-ai/glm-4.5      2098-12-31 z-ai/glm-5.3
2026-09-30 dots-studio/dots-3-note-preview:free                   2098-12-31 z-ai/glm-5.3-flash
2026-12-31 z-ai/glm-4.5v             2098-12-31 z-ai/glm-5-turbo  2098-12-31 z-ai/glm-5v-turbo
```

The same table confirms the two other new assertions: the row *did* carry
`2026-08-31` ("This row had a death date on it"), and OpenRouter cleared it
between the 30 and 31 August snapshots ("OpenRouter cleared the expiry from its
own row").

## The machinery claim in paragraph 2

"the catalog's status is derived from that field — a cleared expiry reads
`active`" is a claim about code, so I read the code rather than the prose.
`pulse/lib/diff.mjs:116` `deriveStatus()`:

```js
if (rule.kind === 'expiration_date') {
  const expires = getPath(row, rule.path);
  if (!expires) return 'active';
```

and `data/derived/catalog.json` for this row:

```json
{ "row_id": "moonshotai/kimi-k2.5", "entry_id": "model/moonshotai-kimi-k2-5",
  "expiration_date": null, "status": "active" }
```

Both halves of the sentence hold. The two surfaces do disagree, the body now says
so and says which signal it is following and why, so the brief's one failing
outcome (silently leaving them to disagree) is avoided.

## Volatile values, transclusions, timeline

No bare literal replaced the vanished transclusion. The opening re-anchors on
`{{fact:...#api_sunset}}`, a `cited` fact carrying `source_url` and
`accessed: "2026-08-28"`, and `{{fact:...#expiration_date}}` is *kept* in
paragraph 2, where its absent rendering is now the evidence for the sentence
rather than a contradiction of it. Confirmed the rendering path at
`lib/facts.mjs:115` (`ABSENT = '<span class="fact-absent">not published</span>'`)
and `:240` (absent state emits `ABSENT + sourceLinkForFeed(...)`, so the
"cleared" claim renders with `openrouter-models, fetched <date>` beside it).
`api_sunset` is `volatility: dated`, and `VOLATILITY_DAYS.dated = null`
(`lib/schema.mjs:97`) via `isOverdue()` — so it renders with its accessed date and
no overdue flag, which is the intended behaviour for a dated fact.

Both `2026-08-31` timeline rows survive and stay distinguishable — the Moonshot
sunset row sourced to `platform.kimi.ai/docs/models`, the Pulse's `active` row
sourced to `openrouter.ai/api/v1/models`. Aliases untouched and sanely classed
(two `exclusive` on the full name and the router id, `manual` on the
provider-prefixed display name). Arithmetic in the untouched paragraphs still
checks: 27 Jan → 31 Aug 2026 = 216 days; 27 Jan → 16 Jul = 170.

## Scope and prose

The commit touches one content file (17 insertions, 6 deletions) plus the loop's
own `.job/` scaffolding. Nothing widened. No proposal file was added.

On the editorial bar: paragraph 2 does discuss the site's own catalog, which
brushes the cut list's self-reference clause, but the brief explicitly required
the body to explain the catalog/entry split, and the paragraph's payload — a
router's metadata and a vendor's documentation disagreeing about one model on the
day of its sunset — is a real finding rather than process talk. It closes on the
right sentence ("The router's data stopped carrying a date; the vendor's page says
the model is discontinued"). The revision also strengthens the body's existing
argument instead of orphaning it: the row is still listed and priced on OpenRouter
on the day Moonshot's own API starts returning 404, which is the open-weight point
the entry was already making.

Two things I noticed and am not treating as defects. The vendor page says
"officially retired" while the front matter reads `deprecated` and the schema
offers `retired` — the body defends `deprecated` from the page's own "Deprecated
Models" filing, which is sourced and defensible, but it does not say why it
preferred that over the banner's wording. And the transcluded `api_sunset` value
still ends "closed to newly registered users beforehand", a clause the cited page
no longer contains; it is pinned by `accessed: 2026-08-28` and the prose
immediately notes the page has since changed, so the entry is honest about it.

One process note for the loop, not a verdict reason: there is no `RESULT.md` at
the root of this worktree and `git status` is clean.
