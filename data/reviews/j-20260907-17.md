---
job: j-20260907-17
verdict: approve
reasons: []
would-cite: >-
  A maintainer documenting how the site's changed-feed links and frontier
  coverage are derived would cite this README as a map from each claim to the
  current implementation lines.
subject: "content/wiki/README.md"
reviewed:
  "content/wiki/README.md": "33ce08d5548d615ae05b3835a18cc7b7f442aed7f295262f04d45fcf24bb66ed"
---

Free-form notes: I spot-checked both changed README citations against the
current source. README line 25 now names `lib/render/frontier.mjs:93-104`, and
that range is the `matchProviders` function: it builds normalized names from
the organisation display name and aliases, then matches them to normalized
catalog providers. README line 176 now names `lib/changes.mjs:111-119`; the
current source shows `feedRowIndex` at line 114 and the claimed Map key at line
118 (`index.set(`${source}|${rowId}`, doc)`). The mirrored frontier block
comment now carries the same current citation. Both carried-finding files are
absent, so the two findings are retired in the same diff. `git diff --check
main...HEAD` exited 0 with no output. The supplied gate results were not
rerun, as instructed. A separate runtime probe was blocked before execution by
`ERR_MODULE_NOT_FOUND: Cannot find package 'zod' imported from ...\lib\schema.mjs`;
I did not install dependencies or otherwise route around that block. The diff
contains no `content/blog/*.md` path, so no voice carry-forward is required.
