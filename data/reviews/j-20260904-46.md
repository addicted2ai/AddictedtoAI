---
job: j-20260904-46
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a work queue built on file-presence state needs to know
  what happened downstream of the file would link the proposal this job filed:
  it is a measured case of a finding surviving its own fix, with the queue's own
  source comment naming the blind spot and the merged post that closed it. The
  diff itself is queue hygiene rather than a citable artefact — its value is
  that it retires two findings a reader of the site's Astra coverage would
  otherwise see re-dispatched against a post that already answers them.
carry:
  - title: Fix the review line citations in the new proposal's evidence block
    detail: >-
      data/proposals/retire-carried-findings-satisfied-by-downstream-work.md
      says "That job's own reviewer recorded both as handled
      (`data/reviews/j-20260904-04.md:31-33` and `:186-188`)". Both of those
      ranges are about the FrontierMath finding only — :31-33 reads "it prints
      the release page's table figure of 97.6% on FrontierMath Tier 4 (v2)
      directly beside the same page's prose claim", and :186-188 reads "The post
      prints both and says which is which". The reviewer's confirmation of the
      first finding (the closing sentence quoted in full) is at :263-266: "The
      quotation of this site's own earlier post ... is verbatim the last
      sentence of `content/blog/openai-astra-critical-designation.md:57`."
      Replacing ":31-33" with ":263-266" would make the citation say what the
      record says. Nothing downstream depends on it — both findings are
      verifiable in the post itself — but the proposal's argument is built on
      cited line ranges and one of them points at the wrong finding.
    subject: data/proposals/retire-carried-findings-satisfied-by-downstream-work.md
---

The diff makes one claim, implicitly and by deletion: both carried findings were
already satisfied before this job started, so the files that carry them may go
without any content change. That claim is checkable, and I checked it directly
rather than accepting the accompanying proposal's account of it.

## The two findings, against the corpus

**Finding 1 — quote the closing sentence in full.** Read
`content/blog/openai-astra-critical-designation.md` line 58 (not :57 as both the
carry file and the j-20260904-04 review record say — the sentence is on 58 in the
current file):

> Until the system card lands, the capability numbers are OpenAI's word, and the
> designation itself is the dated, checkable fact.

And `content/blog/openai-gpt-6-astra-system-card.md:17`:

> The site's post on the 1 September announcement ended on the open question:
> "Until the system card lands, the capability numbers are OpenAI's word, and the
> designation itself is the dated, checkable fact."

Verbatim, both halves, including the clause the carry file said the truncation
dropped. The finding asked that "any post written from this docket should quote
the whole sentence or ellipse the cut"; the post written from this docket quotes
the whole sentence.

**Finding 2 — disambiguate 97.6 vs 98.** `content/blog/openai-gpt-6-astra-system-card.md:44`:

> FrontierMath Tier 4 (v2) is 97.6%, while the page's prose says Astra
> "saturates FrontierMath Tier 4 with a 98% score".

That is exactly the shape the finding asked for — 97.6 as the measured row, 98%
attributed to the announcement's prose, each named as what it is, neither printed
as a second score. I did not stop at the post: `data/reviews/j-20260904-13.md:66-67`
carries a later reviewer's parse of the release page's `<table>` elements, and
TABLE 3 row 2 reads `FrontierMath Tier 4 (v2) | 97.6% | 83.0% | 87.8% | ...`, so
97.6 is a genuine table row rather than a figure taken from a secondary write-up.
The carry file's own supporting source was Vellum's benchmark post; the primary
page corroborates it.

## The docket the findings were filed against

The brief names `data/proposals/gpt-6-astra-release-system-card.md` as the target.
That path does not exist on this branch. It is at
`data/proposals/consumed/gpt-6-astra-release-system-card.consumed-20260904T080518.md`,
whose footer records:

> - job: j-20260904-04 (post)
> - merged as: `e2ef5abce28d061dbfb6d5f3c5700fdd9adab715`
> - produced: `content/blog/openai-gpt-6-astra-system-card.md`

So the docket is finished, it produced exactly the post I checked above, and there
is no future post from it that either finding could still constrain. Both findings
are spent. Deleting their files is the correct disposition, and it is the only
disposition that removes them from the queue.

I checked the two cited commits exist and are what the proposal says they are:
`3dda1b9 job j-20260904-02: records (done)` (wrote the carry files) and
`9d81816 job j-20260904-04: post — GPT-6 Astra release and system card
(monitorability decline)` (the post commit; `e2ef5ab` is its merge).

## The shape of the diff, since a no-content-change repair is the risky shape

A repair that deletes two guardrail files and changes no content is, structurally,
indistinguishable at a glance from clearing a halt without fixing anything — the
proposal filed with it says so itself, which is to its credit. What separates them
here is that the fix is locatable and quotable in the tree, which I did above, and
that the retirement instruction inside each deleted carry file is the mechanism the
spec prescribes: "This file's presence is what puts the finding in the Pulse's
derived queue." Nothing in `pulse/` was touched, no detector was weakened, and
`data/carried/` still holds three unrelated findings
(`j-20260903-14-carry-1.md`, `j-20260904-07-carry-1.md`, `j-20260904-45-carry-1.md`)
— the diff removed exactly the two it was dispatched for.

## Scope

Within it. The outcome named two files to delete and it deleted those two. `.job/brief.md`
and `.job/source.json` are loop-written job artefacts, not the runner's work. The
one proposal is the one the brief explicitly permits; its `type: machinery` is on
the closed list and differs from this job's stamped `repair`, so it does not trip
the self-amplification rule, and no matching slug exists in `data/proposals/rejected/`.

## The proposal filed alongside

I verified its two load-bearing external claims rather than its argument. The
`pulse/lib/queue.mjs` quotation is accurate — lines 172-175 read "Ranked
deliberately LOW — below every timer and below `want-eligible-mint` — because
there is no automatic way to tell a fixed carried finding from an unfixed one
(the fixing job's own diff has to delete the file that names it; see
`carriedFindingItems` below)" — and the ellipsis in the proposal cuts only the
"below every timer" clause, which changes nothing. Its claim to be distinct from
`resolve-moved-subjects-when-a-carried-finding-becomes-a-job` (2026-09-02) also
holds: that one resolves a subject path that moved to `consumed/`, this one asks
the queue to name the artefact the consumed proposal produced. Adjacent, not the
same remedy, and the proposal says so itself. The one imprecision I found is in
its line citations, carried above rather than blocked on.

I did not re-run `npm test` or `npm run build` — the loop ran both on `ed9971cc`
and recorded PASS. No claim in this diff would be red under either; the claims
here are about corpus text, and I checked those by reading the corpus.
