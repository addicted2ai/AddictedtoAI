---
job: j-20260907-15
verdict: approve
reasons: []
would-cite: >-
  An author about to file an `org` entry for an unmatched catalog provider, and
  the reviewer who reads that entry afterwards: this file is where the two of
  them find out that the Players Board joins on aliases rather than on a `feeds`
  map, and that the honest answer for a publishing handle is no entry at all.
  The section the diff repairs is the one that stops a directive's wrong
  sentence — "its `feeds` map, which is the join the board relies on" — from
  being copied into eight more entries, and its line citations are the only
  thing letting the next implementer check that ruling against the code instead
  of taking it on trust.
proposal:
  slug: check-code-citations-in-prose-resolve
  type: machinery
  date: 2026-09-07
  summary: >-
    Add a prebuild step that checks every `path.mjs:N` / `path.mjs:N-M` citation
    written in `content/**` prose. Two assertions, both cheap and both
    mechanical: the cited file exists and has at least M lines, and — where the
    same sentence carries a backticked identifier that appears in that file at
    all — that identifier appears inside the cited range. The second assertion is
    the one with teeth and it is deliberately narrow: it fires only when the
    prose names a symbol beside the citation, so a citation pointing at a comment
    block or an anonymous region is left alone rather than guessed at. It cannot
    catch a citation that names nothing, which is a stated limit, not an
    oversight.
  evidence: >-
    Three reviewers have now hand-re-measured line citations in this one file.
    `data/carried/j-20260906-11-carry-1.md` re-derived six of them by hand after
    a 29-line block comment shifted them; this review found two more that had
    drifted since that measurement was taken on 2026-09-06 — `lib/changes.mjs:60-67`
    for `feedRowIndex`, which now lives at 112 (measured in this worktree at
    d4f14730083b), and `lib/render/frontier.mjs:50` at README line 25, which now
    lands inside `norm()`. The `feedRowIndex` case is exactly the shape the
    proposed check catches: the sentence backticks `feedRowIndex` and the cited
    range does not contain that string. Every one of these was found by a human
    reading, none by any automated check, and each cost a review pass that could
    have been spent on judgment.
carry:
  - title: Re-cite lib/changes.mjs:60-67 against feedRowIndex's current location
    detail: >-
      README line 176 reads "`lib/changes.mjs:60-67` — `feedRowIndex` keys one Map
      on `${source}|${rowId}`". Measured in this worktree at d4f14730083b:
      `feedRowIndex` is declared at `lib/changes.mjs:112` and its body runs to
      119; lines 60-67 are the tail of an unrelated function plus a comment about
      malformed change lines ("A malformed line is the Pulse's problem to report,
      not a reason for the site to stop rendering the other 59"). The claim the
      sentence makes is correct — line 116 is literally
      `index.set(\`${source}|${rowId}\`, doc);` — only the citation has drifted.
      `j-20260906-11-carry-1.md` explicitly cleared this citation as resolving
      correctly when it was measured on 2026-09-06, so the drift is newer than
      that finding and was outside this job's brief. Fix: cite `:111-119`. The
      same stale range is repeated in the block comment at
      `lib/render/frontier.mjs:85-86`, which is code rather than content but
      would be worth correcting in the same pass.
    subject: content/wiki/README.md
  - title: Re-cite lib/render/frontier.mjs:50 at README line 25 to the alias join
    detail: >-
      README line 25 reads "`/frontier`'s Players Board joins `content/wiki/org/*.md`
      to catalog providers by alias (`lib/render/frontier.mjs:50`)". Line 50 is
      `return String(s ?? '')`, inside the `norm()` helper; the alias join is
      `matchProviders` at 93-104. This citation is the same 2026-09-05-era number
      the job just corrected at README line 155, and it was left standing because
      the carried finding scoped itself to the "What an `org` entry declares"
      section — so the file now cites `:50` and `:93-104` for the same mechanism
      in two places. Fix: cite `:93` or `:93-104`, matching line 155.
    subject: content/wiki/README.md
subject: "content/wiki/README.md"
reviewed:
  "content/wiki/README.md": "d755ed0045364cdc892d1e8eb258d882b08f3b831057839bdda52f06b8ff9eb9"
---

Approve. Both carried findings are cleared, the fix for the second is better
than the fix the finding prescribed, and the diff touches nothing else.

## Finding 1 — the `technique/` enumeration

The diff changes README line 124 from "`content/wiki/technique/` carries" to
"carries entries such as". That is the repair the finding asked for, and the
sentence's conclusion still holds. Checked both halves in this worktree:

```
$ ls content/wiki/technique/ | wc -l
15
$ grep -ril "merg" content/wiki/technique/ content/wiki/concept/
content/wiki/technique/chain-of-thought-prompting.md
content/wiki/technique/low-rank-adaptation.md
content/wiki/technique/prompt-tuning.md
content/wiki/technique/pruning.md
content/wiki/technique/quantization.md
content/wiki/technique/reinforcement-learning-with-verifiable-rewards.md
content/wiki/concept/chain-of-thought.md
content/wiki/concept/emergence.md
content/wiki/concept/grokking.md
content/wiki/concept/in-context-learning.md
content/wiki/concept/tokenization.md
```

15 files, so "entries such as" before a list of three is now accurate where
"carries" followed by three was not. The `merg` hits are the substring noise the
carrying reviewer already predicted — `emergent`/`emergence` and, in the six
`technique/` files, `merged`/`merging` used in passing rather than as a subject.
No entry under either directory covers model merging, so the trailing "and
nothing on merging" survives the edit unchanged.

## Finding 2 — the frontier.mjs line citations

The carried finding prescribed a specific arithmetic: "add 29 to each of the six
frontier.mjs numbers". **The author did not do that, and was right not to.**
Adding 29 would have produced `:79-90` and `:564-578`; both are wrong against
the file as it stands today. The author appears to have re-measured instead, and
the two numbers it wrote are exact. Verified by reading the file at
d4f14730083b:

- README line 155 cites `lib/render/frontier.mjs:93-104` for `matchProviders`.
  Line 93 is `function matchProviders(org, catalogRawRows) {` and line 104 is its
  closing `}`. The prose claim resolves too: line 94 builds `names` from
  `org.data.display_name` and `(org.data.aliases ?? []).map((a) => a.name)`, and
  line 101 is `names.some((n) => n === p || n.includes(p) || p.includes(n))` —
  "contains one of those names or is contained by it", verbatim.
- README line 161 cites `:484-498` for the K21 no-filter comment. Line 484 is
  `* K21 (keeper, delegated): board membership is EDITORIAL, never feed-gated.`
  and 498 is the comment's closing `*/`, immediately above
  `export function renderPlayersBoard` at 499. The comment says "Every `orgs`
  entry becomes a row below regardless of whether any catalog row matches it
  (`boardRowHtml`'s `!row` branch)", which is what the README sentence claims it
  says.

The finding named six citations; the file holds three, and the third
(`frontier.mjs:50` at line 25) is in a different section. The other four —
`orgOwnDomains`, `isVendorSourced`, `claimRank` and the registrable-domain
comment — are **absent from the README entirely**, and were already absent on
`main` before this diff: `grep -no "[a-z/-]*\.mjs:[0-9-]*" content/wiki/README.md`
returns eight citations in total and none of them. The `publishes_from` section
that used to carry them now cites `lib/vendor-domain.mjs` with no line numbers at
all, which is where `separate-a-claim-from-a-fact` moved that test. So the
finding was written against a version of the section that no longer exists, and
the two citations that survived it are the two the diff repairs. Nothing was
skipped.

## The retirement files

Both are deleted in the same diff: `data/carried/j-20260905-25-carry-1.md` and
`data/carried/j-20260906-11-carry-1.md`. That is what `specs/pulse`'s
"presence-is-the-state" rule requires, and it is the whole retirement — no
bookkeeping step, correctly.

## Scope

`content/wiki/README.md` (two sentences), the two carried files, and the
loop-generated `.job/brief.md` and `.job/source.json`. No reserved path is
touched. Nothing here exceeds the stated outcome.

## What I did not re-run

The loop recorded `npm run test`, `npm run build`, `verify-surfaces` and
`verify-design` all passing on d4f14730083b, the commit this diff was computed
from, so I ran none of them. The claims in this diff are line citations and a
directory enumeration; the checks that would be red if they were false are the
two greps and the four file reads quoted above, and I ran those instead.

## Carried, not blocked on

Two further stale citations in the same file, both quoted in the front matter.
Neither was in this job's brief — one is in a section the carried finding
explicitly scoped itself out of, and the other was explicitly cleared as
"unaffected and resolving correctly" by the 2026-09-06 measurement and has
drifted since. A repair that did what it was asked, correctly, should not be
sent back for adjacent drift it was not pointed at.
