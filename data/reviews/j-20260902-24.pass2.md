---
job: j-20260902-24
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that diffusion LLMs have stopped being a demo and started
  being a line item would link this: it is the only page I know of that puts
  Inception's own 1,107 tok/s capability claim next to OpenRouter's live P50
  for the same row, dates both, and says which is which. The concrete argument
  it settles is "is the speed claim measured?" — the answer here is no, with
  the traffic-derived median (359 tok/s, 2026-09-02) sitting beside it so the
  reader can size the gap themselves. Secondarily, a cost watcher pricing a
  latency-bound pipeline before September 8 links it for the promo-versus-list
  arithmetic against Mercury 2's live row.
proposal:
  slug: delta-review-diff-recomputed-from-branch-head
  type: machinery
  date: 2026-09-02
  summary: >-
    A machinery job to make the delta-review brief's "diff under review" a diff
    of the branch's current HEAD, not the diff that the previous review pass
    was given. On this job the pass-2 brief embedded the pre-revision diff: it
    still showed the two P50 figures, the moved quotation mark and the doubled
    `introductory_pricing_ends` value that the revision had already fixed, so a
    reviewer who judged the embedded diff rather than opening the worktree
    would have re-raised three findings that no longer exist and discarded the
    job on its last pass. The fix is to recompute the diff at review time from
    the branch head (and, if a delta review wants a narrower view, to diff the
    previous review's commit against the head rather than reusing the earlier
    payload), plus a check that the brief's diff blob ids match the branch.
  evidence: >-
    Observed 2026-09-02 in the pass-2 brief for j-20260902-24. The embedded
    diff ends at blob d231d33 for
    content/wiki/model/inception-mercury-2-5-preview.md
    ("index bf0ee09..d231d33"), which is the blob at commit e949896 (the
    original entry). The branch head the brief itself names as reviewed,
    d56d8db04529, holds blob 2793b10 for that path — verified with
    `git rev-parse e949896:<path> d56d8db:<path> HEAD`, which printed
    d231d33db491..., 2793b1095dd2... and d56d8db04529... respectively. The
    revision commits 75951c6 and d56d8db are absent from the embedded diff
    entirely, including the derived-file updates they carry.
carry:
  - title: Add the missing trailing newline to the entry and the proposal file
    detail: >-
      Both files added by this job end without a final newline —
      `git diff` reports "\ No newline at end of file" for
      content/wiki/model/inception-mercury-2-5-preview.md (after
      "which accepts `tools` and `response_format`.") and for
      data/proposals/context-window-transclusion-unit-doubling.md. Nothing
      breaks and the build passes; it just makes every future diff of these
      files noisier than it needs to be. One newline each fixes it.
    subject: content/wiki/model/inception-mercury-2-5-preview.md
subject: "content/wiki/model/inception-mercury-2-5-preview.md"
reviewed:
  "content/wiki/model/inception-mercury-2-5-preview.md": "b17115076c7bd2a569150f2fe709c3c66197516fc1b0f65e6bf4b0faa5f1376f"
---

Delta review, pass 2. The previous verdict asked for three changes under
`false-or-unsupported-claim` and `spec-violation`. All three are made, and I
confirmed each against the live source rather than against the author's
account.

**First: the diff embedded in this brief is stale, and I did not review it.**
It ends at blob `d231d33` for the entry, which is the blob at `e949896` — the
*pre-revision* commit. The branch head the brief names as reviewed and built,
`d56d8db04529`, holds blob `2793b10`:

```
$ git -C <worktree> rev-parse e949896:content/wiki/model/inception-mercury-2-5-preview.md \
      d56d8db:content/wiki/model/inception-mercury-2-5-preview.md HEAD
d231d33db491ef7c7047ec8987c0daa66fd3843f
2793b1095ddd2a84797752905ddd2ab3d5321bdc
d56d8db04529264b311e2161a86e15b017896024
```

Judged on the embedded text, all three previous findings would still stand and
this job would be discarded on its last pass for defects that no longer exist.
I reviewed the branch instead — the file on disk and `git diff 5cf12e1 d56d8db`
— and filed the machinery proposal above. This is the one thing in this review
a reader should not have to take on trust, so the command and its output are
quoted in full.

**What the revision actually changed** (`git diff 5cf12e1 d56d8db`): two new
`cited`/`dated` facts `observed_throughput_p50` ("359 tok/s") and
`observed_latency_p50` ("1.37 s"); paragraph 2 rewritten to transclude them
and to state the rolling window; `introductory_pricing_ends` trimmed from
`"2026-09-08 07:00 UTC; 80% off input and output on the OpenRouter listing"`
to `"2026-09-08 07:00 UTC"`; the paragraph-5 quotation corrected from
`quality "comparable to ...` to `"comparable quality to ...`; and the three
derived artefacts (`aliases.json`, `backlinks.json`, `search-index.json`)
regenerated to match. Nothing else moved.

**Required change 1 — the P50 figures.** Fetched
`https://openrouter.ai/inception/mercury-2.5-preview` twice on 2026-09-02
(HTTP 200, 401,986 bytes both times) with a plain `fetch` and searched the raw
HTML for exact substrings — counts printed:

```
  [11] "359"
  [2]  "1.37"
  [0]  "1.24"
  [2]  "p50_throughput"
  [2]  "p50_latency"
  [2]  "window_minutes"
```

The rendered stat cards and the embedded payload both agree with the entry:

```
<p class="text-2xs ...">Throughput</p><p class="... tabular-nums">359<span ...>tok/s</span></p>
  <p class="text-2xs ...">P50<!-- -->, best across providers</p>
... 1.37<span ...>s</span> ... <p class="text-2xs ...">P50<!-- -->, best provider</p>

\"p50_throughput\":359,\"p75_throughput\":499,\"p90_throughput\":583,
\"p95_throughput\":641,\"p99_throughput\":749.09,\"latency_request_count\":9137,
\"throughput_request_count\":9137,\"request_count\":9137,\"window_minutes\":30
```

So the two figures the previous pass could not reproduce are gone, the
replacements are the ones the page actually publishes, and the prose's new
clause — "OpenRouter computes them over a rolling 30-minute window of live
traffic" — is exactly `"window_minutes":30` in the same stats object that
carries `p50_throughput`. `1.24` still does not occur anywhere in the page,
which is the right outcome now that nothing claims it.

The spec half is satisfied too. `specs/wiki`, "Volatile facts travel by
transclusion, never by restatement": the values now live in the entry's
`facts:` block with `source: cited`, `source_url`, `accessed: 2026-09-02` and
`volatility: dated`, and the body transcludes them. I checked the rendering
rather than assuming it — the built page in the job worktree
(`out/wiki/model/inception-mercury-2-5-preview.txt`) emits:

```
<span class="fact fact-inline" data-field="observed_throughput_p50" data-source="cited"
 data-state="cited" ...><span class="fact-value">359 tok/s</span><span class="fact-source">
 <a href="https://openrouter.ai/inception/mercury-2.5-preview" ...>source</a>,
 accessed 2026-09-02</span></span>
```

— value, linked source and the as-of date, which is the whole point of the
`dated` class. No unit is appended and none should be: `lib/units.mjs:69`
returns null for anything whose `source !== 'feed'`, so the "359 tok/s"
literal cannot double the way the entry's own filed proposal describes for
feed facts. I confirmed that by reading the function, and the built page
above shows it: `<span class="fact-value">359 tok/s</span>` with no
`fact-unit` sibling, against `260000` + `<span class="fact-unit">tokens</span>`
for the feed-bound `context_window` two paragraphs down.

**Required change 2 — the quotation.** The entry now reads `"comparable
quality to cost-optimized frontier models like GPT-5.6 Luna (Low), Gemini 3.5
Flash-Lite, and Claude Haiku 4.5"`. That exact string occurs 18 times in the
fetched HTML; the previous, mark-shifted form `"comparable to cost-optimized
frontier models"` occurs 0 times. Quoted as written.

**Required change 3 — `introductory_pricing_ends`.** Value is now
`"2026-09-08 07:00 UTC"`. The built sentence reads "...an 80% cut from the
vendor's own list that the banner dates through **2026-09-08 07:00 UTC**." —
the doubled discount clause is gone and the field named `_ends` returns an
end.

**Re-confirmed while I was in the sources** (not delta, but cheap and it is
what the checklist asks): `https://www.inceptionlabs.ai/models` returned 200,
320,982 bytes, containing "Comparable quality to cost-optimized frontier
models" (1), "$0.20" (1), "$0.75" (2), "260K" (1) — so "the models page
repeats the comparison" and the two `list_price_*` facts hold. On the
OpenRouter page: "produces and refines multiple tokens in parallel" (22),
"achieving 1,107 tokens/sec on standard GPUs" (22), "10+ point jump in
intelligence over Mercury 2" (18), and the banner "Limited-time 80% discount
via Inception through September 8, 2026 at 07:00 UTC" (16) — every quotation
in the body is exact.

**Derived artefacts.** The revision regenerated `data/derived/aliases.json`
(the three new aliases, `linkable: true` for the two `exclusive` ones and
`false` for the `shared` "Mercury 2.5"), `backlinks.json` (four inbound rows,
one per mention) and `search-index.json` (aliases populated, `"b": false` now
that the entry is no longer bare). These are the mechanical consequence of the
entry, they match the front matter, and their absence from the original commit
is what the revision corrected. Not scope creep.

**Scope.** Unchanged from pass 1 and still clean: the one entry the outcome
names, `.job/`, one proposal file, and the derived files that entry produces.
Nothing widened, nothing re-treads the (empty) rejection index.

**What I did not run.** `npm test` and `npm run build` — the loop reports both
PASS on this commit and re-running them is the documented way for this review
to waste its budget. The build stamp inside the job worktree's output reads
`2026-09-03T05:20:48Z · 5cf12e1c5543+dirty`, i.e. it was produced from the
revision's working tree before the final commit; the rendered body it contains
is byte-identical to the committed prose I read, which is why I was willing to
use it as evidence of rendering.

**Quality.** Paragraph 2 now does more than it did before the revision, not
less: it names the quantity mismatch, gives the reader both numbers, and warns
that one of them is a half-hour median that will read differently tomorrow.
That last clause is the honest thing the previous draft was missing, and it is
the sentence that makes the page worth citing in an argument about whether
1,107 tok/s means anything. `editorial` clause 1 was already cleared by the
two-diffusion-rows-of-421 observation and the promo-versus-list arithmetic;
nothing in the delta weakens it.
