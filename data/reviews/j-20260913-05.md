---
job: j-20260913-05
verdict: approve
reasons: []
would-cite: >-
  Someone deciding whether to use Mistral's batch endpoints — e.g. weighing the
  batch discount on Large 3 2512 or Medium 3.1 against the standard rows — would
  land on one of these entries for the live feed-backed price and context
  figures plus the removal-and-relist history that explains why the entry
  briefly read retired.
subject:
  - "content/wiki/model/mistralai-codestral-2508-batch.md"
  - "content/wiki/model/mistralai-mistral-large-2512-batch.md"
  - "content/wiki/model/mistralai-mistral-medium-3-1-batch.md"
reviewed:
  "content/wiki/model/mistralai-codestral-2508-batch.md": "30ab82d40f84975317fd2ddcb6fc4d2ab5afea69c91f6d0d4de158ce1dfba597"
  "content/wiki/model/mistralai-mistral-large-2512-batch.md": "15869a7c716867400e246997d8293633dc02c30f79784fb381a240d7b9f24900"
  "content/wiki/model/mistralai-mistral-medium-3-1-batch.md": "8a7cc4f544534d70a151ae2bc6a73503a0da949809b34195a8f940985962ab9b"
---

Free-form notes: what I checked, what I ran, and what I observed.

Files judged: the job commit 5465d1b8d5de touches exactly two content files —
`content/wiki/model/mistralai-mistral-large-2512-batch.md` and
`content/wiki/model/mistralai-mistral-medium-3-1-batch.md` (confirmed via
`git show 5465d1b --stat`: "2 files changed, 56 insertions(+), 4 deletions(-)").
Both are declared subjects in `.job/source.json` (`declared_subjects`
includes both plus the read-only precedent
`content/wiki/model/mistralai-codestral-2508-batch.md`); the precedent file is
untouched, as the brief requires. No blog posts in the diff, so the voice
question does not arise. No proposal file on the branch. Rejection index is
empty — nothing to fuzzy-match. The loop's pre-review gates (`npm run build`,
`node scripts/verify-surfaces.mjs out`) are recorded PASS on this same commit;
per instruction I did not re-run them.

Spot-checks against sources (three different instruments, so agreement here is
corroboration, not shared-instrument agreement):

1. `data/sources/openrouter-models/latest.json` (snapshot date 2026-09-12,
   445 rows, rows keyed by id): both `mistralai/mistral-large-2512:batch` and
   `mistralai/mistral-medium-3.1:batch` are present as live rows. The entries'
   `feeds:` bindings match the row ids exactly, and `display_name` matches each
   row's `name` exactly ("Mistral: Mistral Large 3 2512 (batch)";
   "Mistral: Mistral Medium 3.1 (batch)"). Row detail confirms feed-backed
   fact paths exist: `pricing.prompt`, `pricing.completion`, `context_length`
   (262144 large / 131072 medium), `$status` active. (Observed via node
   reads of the JSON; full row bodies printed during the run.)
2. `data/changes.jsonl` (228 lines): line 110 is the 2026-09-01
   `$retirement` for mistral-large-2512:batch and line 208 is its 2026-09-10
   `$arrival`; line 111 / line 209 are the same dated pair
   (retirement/arrival) for mistral-medium-3.1:batch. Both entries record BOTH
   dated timeline rows rather than either alone, as prescribed, with event
   wording identical to the precedent.
3. Precedent `content/wiki/model/mistralai-codestral-2508-batch.md` (read-only,
   unmodified): both restored entries are structurally identical to it —
   same `status: active` / `maintenance: living`, same `feeds:` key shape,
   same four facts (price_input, price_output, context_window, status, all
   `source: feed` / `volatility: fast`), same two-row timeline form.

Polarity: the diff implements every prescribed element (binding restored,
facts restored, retired/dormant flipped to active/living, both timeline rows
recorded) and inverts none. Scope: the diff touches only the two declared
repair subjects — no `scope-violation`.

One non-blocking observation (not a finding): the brief's prescribed-repair
parenthetical lists "tokenizer" among the canonical feed-backed facts, but the
landed precedent — which the same brief names as the form to match — carries
exactly the four facts restored here, with no tokenizer fact. Matching the
record-bound precedent is the correct call; noting it here so a future brief
either adds the fact to the precedent family or drops the word.
