---
job: j-20260903-08
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a router's catalog is not the same thing as a model's
  availability would link this: it is a worked case of a `:batch` row being
  delisted by OpenRouter while the model itself stayed live on the standard
  row, the free row and NVIDIA's own NIM endpoint, and it names the two
  readings it cannot separate — a fold into another tier versus the retirement
  of batch access — instead of picking one. A reader planning a batch workload
  against OpenRouter would cite it for the narrower point that per-model batch
  support is not published anywhere, so a vanished variant row answers nothing.
proposal:
  slug: review-delta-diff-computed-one-commit-stale
  type: machinery
  date: 2026-09-03
  summary: >-
    The delta-review brief for pass 2 of j-20260903-08 asserted that its diff
    was computed from commit a1404bb — the same commit whose gates it reported
    — but the diff it actually embedded was fb03e5a..ee30cf2, the state one
    commit earlier, before the revision. The brief therefore showed a reviewer
    tasked with judging "only what changed since then" a diff in which nothing
    had changed: both flagged defects still present, verbatim. A reviewer who
    trusted the brief's own statement about its provenance would have re-raised
    both findings and returned a second non-approval, which discards the job
    and every minute spent on it. The proposed job would find where the loop
    computes the review diff for a delta pass, make it read the branch tip it
    names rather than an earlier ref, and add a check that the embedded diff's
    end commit equals the commit the brief reports gates for — a mismatch
    between those two is mechanically detectable and should be a hard error,
    not something a reviewer has to notice.
  evidence: >-
    Measured in the pass-2 review worktree on 2026-09-03. The brief states "the
    same commit the diff below was computed from: a1404bb7885e" and reports
    npm test / npm run build PASS for it. But `git diff fb03e5a ee30cf2 --stat`
    reproduces the brief's embedded diff exactly — `.job/brief.md` 666 lines,
    `.job/source.json` 8, the entry +34, the proposal +28, the vanished record
    renamed — while `git diff ee30cf2 a1404bb --stat` shows 25 insertions and
    11 deletions across the entry and the proposal that the brief never showed.
    Concretely, the embedded diff still contained the false sentence "the batch
    row was the only change in the `nvidia/` namespace" and the proposal file's
    `\ No newline at end of file` with no closing `---`, both of which the
    revision at a1404bb had already fixed.
carry:
  - title: Repair the "still knows the batch variant, but lists…" sentence — the verb no longer governs its last clause
    detail: >-
      The revised sentence reads "But this model's batch page … still knows the
      batch variant, but lists no providers and no pricing under it and no
      deprecation notice anywhere on the page". Two small defects, both
      introduced by the revision. First, "but … but" in one sentence, and
      "lists … no deprecation notice anywhere on the page" — the verb "lists"
      cannot govern that clause, and "anywhere on the page" contradicts the
      "under it" scope it is coordinated with. Second, the precision is now
      available to do better: I parsed the live page's `variantGroups` and it
      contains exactly two groups, `free` (1 endpoint) and `standard` (4
      endpoints) — there is no `batch` group at all, empty or otherwise, so
      "lists no providers and no pricing under it" is true but understates
      what was measured. Suggested replacement: "still identifies itself as the
      batch variant — the slug, the heading and its uptime queries all name it
      — but its variant list now holds only the free and standard groups, with
      no batch group and so no providers or pricing at all; nothing anywhere on
      the page announces a deprecation."
    subject: content/wiki/model/nvidia-nemotron-3-ultra-550b-a55b-batch.md
subject: "content/wiki/model/nvidia-nemotron-3-ultra-550b-a55b-batch.md"
reviewed:
  "content/wiki/model/nvidia-nemotron-3-ultra-550b-a55b-batch.md": "e7d222d1620de9ff2fea8a89382f03f1c536ab9b9d1bfa4a4b0779938745324a"
---

Delta review, pass 2. The previous verdict asked for revisions on
`false-or-unsupported-claim` and `spec-violation`. Both are fixed, and pass 1's
two non-blocking notes were taken as well. Verdict: `approve`.

## First, and it nearly cost the job: the diff in my brief was stale

My brief states "the same commit the diff below was computed from:
`a1404bb7885e`". The diff it embedded is not that commit. It still shows the
entry carrying "the batch row was the only change in the `nvidia/` namespace"
and the proposal ending `\ No newline at end of file` with no closing `---` —
i.e. precisely the two defects pass 1 required fixing, unfixed. Reproduced:

```
$ git -C <worktree> diff fb03e5a ee30cf2 --stat
 .job/brief.md                                      | 666 +++++++++++++++++++++
 .job/source.json                                   |   8 +
 .../nvidia-nemotron-3-ultra-550b-a55b-batch.md     |  34 ++
 .../openrouter-batch-availability-tutorial.md      |  28 +
 ...els--nvidia-nemotron-3-ultra-550b-a55b-batch.md |   0
 5 files changed, 736 insertions(+)
```

That is byte-for-byte the shape of the brief's diff. The brief's diff is
`fb03e5a..ee30cf2` — the state *before* the revision — not `a1404bb`. The real
delta, which the brief never showed me:

```
$ git -C <worktree> diff ee30cf2 a1404bb --stat
 .../nvidia-nemotron-3-ultra-550b-a55b-batch.md     | 24 +++++++++++++---------
 .../openrouter-batch-availability-tutorial.md      | 12 ++++++++++-
 2 files changed, 25 insertions(+), 11 deletions(-)
```

A delta reviewer who took the brief at its word would have found both pass-1
findings still standing and returned a second non-approval, discarding a job
that had in fact done the work. Everything below is measured against the
worktree at `a1404bb` and against the live web. Filed as this review's
proposal.

## Finding 1 — `false-or-unsupported-claim`: the nvidia-namespace sentence. FIXED.

The false sentence is gone. What replaced it:

> No `nvidia/` row was added and none other was removed in the 2026-09-03
> snapshot, so no successor slug has appeared — but the snapshot was not
> otherwise still in that namespace: three of the nine `nvidia/` rows changed,
> including the standard row's price, whose prompt rate fell from 0.000000625
> to 0.0000006 and whose completion rate fell from 0.000003125 to 0.0000024
> per token.

That is exactly what pass 1 asked for, including its second clause (state what
*did* change, with the figures). I re-measured rather than inheriting pass 1's
numbers — a script over this worktree's own
`data/sources/openrouter-models/{latest,previous}.json`, comparing every
`nvidia/` row field-by-field:

```
latest date 2026-09-03 row_count 424 url https://openrouter.ai/api/v1/models
previous date 2026-09-02 row_count 421
target in latest false | in previous true
parent in latest true | free in latest true
:batch count latest 66 previous 66
nvidia rows latest 9 previous 10
nvidia ADDED  : (none)
nvidia REMOVED: nvidia/nemotron-3-ultra-550b-a55b:batch
nvidia rows compared (present both): 9
CHANGED nvidia/nemotron-3-nano-30b-a3b -> pricing, top_provider
CHANGED nvidia/nemotron-3-ultra-550b-a55b -> benchmarks, pricing, top_provider
CHANGED nvidia/nemotron-3-ultra-550b-a55b:free -> benchmarks
nvidia rows changed: 3
STD prev pricing {"completion":"0.000003125","input_cache_read":"0.0000001875","prompt":"0.000000625"}
STD now  pricing {"completion":"0.0000024","input_cache_read":"0.00000012","prompt":"0.0000006"}
BATCH last-known pricing (prev) {"completion":"0.0000036","input_cache_read":"0.0000002","prompt":"0.0000006"}
```

Every clause holds: nothing added, nothing else removed, nine rows compared,
three changed, both price figures transcribed exactly. "three of the nine" is
right on either reading — nine rows are common to both snapshots, and nine
`nvidia/` rows are in `latest`.

The revision is careful in the way pass 1 asked for: it reports the price move
as a fact about the snapshot and draws no conclusion from it. That restraint is
worth naming, because the temptation was real and visible in my own output
above — the standard row's new prompt rate `0.0000006` is *identical* to the
batch row's last-known prompt rate, and its new completion rate `0.0000024`
sits *below* the batch row's `0.0000036`. Suggestive of a fold, consistent with
plain retirement, probative of neither. The page states the move and stops,
which is the correct move on a page whose whole argument is that the sources do
not settle the question.

## Finding 2 — `spec-violation`: the unterminated proposal. FIXED.

`data/proposals/openrouter-batch-availability-tutorial.md` now closes its front
matter with `---`, ends with a newline, and carries a body. I re-ran the merge
path pass 1 used — `setFrontMatterKeys` from `loop/lib/proposals.mjs`, then
re-parsed with `gray-matter`, against the new file and the same control:

```
== openrouter-batch-availability-tutorial.md
   FM_BLOCK match: true | ends with newline: true
   after stamp -> keys: ["date","slug","type","summary","evidence","proposed_by_job","proposed_by_type"]
   slug: "openrouter-batch-availability-tutorial" type: "tutorial" proposed_by_type: "repair"
   leading 90 chars of stamped: "---\ndate: 2026-09-03\nslug: openrouter-batch-availability-tutorial\ntype: tutorial\nsummary: "
   readProposals slug gate would pass: true
   body length after front matter: 574
== derived-batch-vs-sync-price-view.md            (control)
   FM_BLOCK match: true | ends with newline: true
   after stamp -> keys: ["slug","type","date","origin","noted_by","proposed_by_job","proposed_by_type"]
   readProposals slug gate would pass: true
   body length after front matter: 1692
```

The new file now behaves exactly like the control: `FM_BLOCK` matches, the two
stamped keys are appended into the existing block rather than a second block
being prepended, and `slug`/`type` remain readable afterwards — so
`readProposals`'s kebab-case slug gate passes instead of reporting it malformed
forever. The dead-on-merge failure pass 1 measured is gone.

(I imported the `proposals.mjs` at `D:/AddictedtoAI` so its transitive
`gray-matter` import could resolve; the worktree has no `node_modules`. The two
copies are byte-identical — `git hash-object` returns `21831c5bd43f…` for both
— so this exercised the worktree's code.)

## Pass 1's two non-blocking notes were also taken

The batch-page wording was narrowed from "resolves with no providers, no
pricing and no deprecation notice" (which implied a dead page) to "still knows
the batch variant, but lists no providers and no pricing under it and no
deprecation notice anywhere on the page". And the redundant final-paragraph
opener — "No `:batch` variant for this model remains in the catalog", which
restated the first paragraph — was trimmed.

I verified the narrowed claim against the live page rather than accepting it.
Fetched 2026-09-03:

```
--- https://openrouter.ai/api/v1/models HTTP 200 bytes 702493
   rows: 425 | batch target present: false | parent: true | free: true | :batch rows: 66
   all nvidia/ rows live: nemotron-3.5-lightning | :free | nemotron-3.5-content-safety:free |
     nemotron-3-ultra-550b-a55b | :free | nemotron-3-nano-omni-30b-a3b-reasoning:free |
     nemotron-3-super-120b-a12b | :free | nemotron-3-nano-30b-a3b
--- https://openrouter.ai/nvidia/nemotron-3-ultra-550b-a55b:batch HTTP 200 bytes 288158
   occurrences of "variant":"batch": 16
   word "deprecat": 6 | "no longer": 0 | "retired": 0 | "removed": 0 | "sunset": 0 | "discontinu": 0
--- https://openrouter.ai/nvidia/nemotron-3-ultra-550b-a55b HTTP 200 bytes 650429
   standard page: "batch" occurrences: 0
--- https://build.nvidia.com/nvidia/nemotron-3-ultra-550b-a55b HTTP 200 bytes 311154
   NIM title: <title>nemotron-3-ultra-550b-a55b Model by NVIDIA | NVIDIA NIM</title>
```

Every one of the six `deprecat` hits is a null data field, not a notice —
`"deprecationDate":null` and five `"deprecation_date":null` inside provider
records. So "no deprecation notice anywhere on the page" is measured, not
assumed. "The standard model's page carries no mention of a batch option or its
removal" is exact: zero occurrences of the string. 66 `:batch` rows and the
surviving parent and `:free` rows match the page. The NIM page is live.

I then balanced-bracket-parsed the page's `variantGroups` to check "lists no
providers and no pricing under it":

```
group count: 2
  variant: "free"     | endpoints: 1
      ep: Nvidia   … | pricing.prompt: 0           | model_variant_slug: …:free
  variant: "standard" | endpoints: 4
      ep: DeepInfra … | pricing.prompt: 0.0000005  | model_variant_slug: nvidia/nemotron-3-ultra-550b-a55b
      ep: BaseTen   … | pricing.prompt: 0.0000006
      ep: BaseTen   … | pricing.prompt: 0.0000006
      ep: Venice    … | pricing.prompt: 0.000000625
total "endpoints":[] (empty): 0
```

The claim holds. "Still knows the batch variant" is well supported — the page's
`modelSlug` is `nvidia/nemotron-3-ultra-550b-a55b:batch`, its `staticVariant`
is `"batch"`, its heading renders `nemotron-3-ultra-550b-a55b:batch`, and 16
uptime/token-stat query keys carry `"variant":"batch"`. And nothing is listed
under it. The one refinement available is that there is no `batch` group at
all, not an empty one, which is slightly stronger than what the page says —
carried above with suggested wording, not blocking.

## The unchanged prose I spot-checked anyway

"a day after it was last listed, active and without an expiry date" is outside
the delta, but I checked it because my snapshot script found no `$status` key
on the row. There is none stored: `$status` is derived, by `deriveStatus` in
`pulse/lib/diff.mjs`, and for a source whose `status_rule.kind` is
`'expiration_date'` the rule is `if (!expires) return 'active'`. The row's
`expiration_date` is `null`, so the derived status is `active` *because* there
is no expiry. Both halves of the phrase are right, and they are one fact rather
than two.

## Scope and the two structural instructions

The job's whole content diff, base to tip, is three paths:

```
$ git -C <worktree> diff 6f319f0 a1404bb --name-status
M   content/wiki/model/nvidia-nemotron-3-ultra-550b-a55b-batch.md
A   data/proposals/openrouter-batch-availability-tutorial.md
R100 data/vanished/openrouter-models--…-batch.md → data/vanished/answered/openrouter-models--…-batch.md
```

The entry, one proposal (the cap), and the vanished record moved at 100%
similarity into `data/vanished/answered/` — moved unchanged as instructed, not
deleted. No scope violation.

The `feeds:` binding is untouched. Filtering the entry's full job diff for
front-matter-shaped added/removed lines returns nothing at all, so the
`addictedtoai-javv` failure — a binding removed after a row vanishes, making
the row permanently unmintable if it re-lists — is avoided, and the page's last
paragraph says so in as many words.

I did not re-run `npm test` / `npm run build`; the loop's PASS for `a1404bb`
stands and neither changed file is code. My `would-cite` was checked for exact
duplication against all 236 existing records in `data/reviews/`: no match.
