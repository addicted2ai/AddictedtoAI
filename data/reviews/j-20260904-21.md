---
job: j-20260904-21
verdict: approve
reasons: []
would-cite: >-
  Someone writing about Meta's stalled open-weights line would link the Muse
  Spark 1.3 entry for the licence paragraph, not for this sentence — the
  entry's argument is that the llm-releases card has read "Open weights · Meta
  license (weights pending)" since the 1.2 flagship on 5 August 2026 and has
  not moved one release later, which is a checkable claim about a promise
  rather than a restatement of a spec sheet. The line this job repaired is the
  spec-sheet run-up to that argument, and its value is that a reader arriving
  from /catalog can now read the context-window figure as a sentence instead
  of tripping over a unit wedged between an article and its noun. A reader
  citing the licence claim reaches it through this paragraph, so the paragraph
  being readable is part of why the entry is citable at all.
subject: "content/wiki/model/meta-muse-spark-1-3.md"
reviewed:
  "content/wiki/model/meta-muse-spark-1-3.md": "c26ac8d881699d3380d6ce3fcc0f567989036a94ee8b4e1fee84010c277c3f6e"
---

## What the diff claims, and what I measured

The author's own commit (`9c5a927`) touches exactly two paths — two lines in
`content/wiki/model/meta-muse-spark-1-3.md` and the deletion of
`data/carried/j-20260904-16-carry-1.md`. The `.job/brief.md` and
`.job/source.json` in the diff above came from the loop's own brief commit
`18d7148`, not from the job:

```
9c5a927 job j-20260904-21 (repair): Rephrase 'a {{fact}} context window' on Muse Spark 1.3
 content/wiki/model/meta-muse-spark-1-3.md |  4 ++--
 data/carried/j-20260904-16-carry-1.md     | 16 ----------------
 2 files changed, 2 insertions(+), 18 deletions(-)
```

That is the stated outcome and nothing else.

### The rendered sentence, before and after — measured, not inferred

The brief's claim is about what the built page *renders*, so I rendered it.
`out/` does not exist in this worktree and the worktree has no `node_modules`,
so I ran `lib/build-content.mjs`'s `buildSite({ write: false, redirects: false })`
from `D:/AddictedtoAI` (clean at `3f28319`, the exact parent of this branch, so
`lib/` and `data/` are byte-identical to this branch's) with `contentRoot`
pointed first at this branch's `content/` and then at the parent commit's, and
printed the entry's rendered body around `million-token`.

**Before** (`contentRoot: D:/AddictedtoAI/content`):

```
confirmation when needed. It keeps the family's million-token window — a
<span class="fact fact-inline" ...><span class="fact-quantity"><span class="fact-value">1048576</span><span class="fact-unit">tokens</span></span>...</span> context window — and the
```

i.e. "a 1048576 tokens context window" — the defect the carry named, confirmed
present rather than taken on trust.

**After** (`contentRoot: <this branch>/content`):

```
confirmation when needed. It keeps the family's million-token window — a context window of
<span class="fact fact-inline" ...><span class="fact-quantity"><span class="fact-value">1048576</span><span class="fact-unit">tokens</span></span>...</span> — and the
```

i.e. "a context window of 1048576 tokens". The figure is untouched — the same
`fact-value` `1048576` and the same `fact-unit` `tokens`, from the same
`openrouter-models` fact, "last checked 2026-09-04" — so this is a prose
change and not a data change, which is what the brief asked for.

The fact's binding matches the carry's account of the cause:
`content/wiki/model/meta-muse-spark-1-3.md:32-36` binds `context_window` to
`openrouter-models` path `context_length`, and `lib/units.mjs:64` declares
`'openrouter-models|context_length': 'tokens'`. `displayQuantity` rescales only
`PER_TOKEN_PRICE_PATHS`, so the context window renders verbatim with its unit
appended — identical cause to the two pages `j-20260904-16` fixed.

### The rest of the page

The three other transclusions in the same paragraph are price facts and read
correctly with their units in place ("the row lists $1.25 per million tokens
input against ... output, with ... on a cache read"), so no second instance of
this shape survives on the page. I did not go looking beyond it; that is out of
scope here and the brief says so.

### The carried file

`data/carried/j-20260904-16-carry-1.md` is deleted, which is what the brief
requires to retire the queue item. Nothing in the tree references it —
`grep -rn "j-20260904-16-carry-1"` over `.mjs`/`.json`/`.md` returns nothing —
and the other seventeen carried files and the directory README are untouched.

### The review-hash question, checked and clear

`data/reviews/j-20260903-12.pass2.md:62` binds a reviewed-surface hash for this
exact path, so the edit makes that record's hash stale, and specs/review has
`verify-launch` fail on **mismatched**. This is not a defect in the diff: the
merge writes a fresh `reviewed:` block into the new record (visible at
`data/reviews/j-20260904-16.md:30-32`, which did the same for the two pages that
job repaired), and `lib/reviews.mjs` supersedes an older record with a newer one
naming the same path — its own header calls that out at lines 293-302, "without
it a mismatch has no way to clear". So the stale binding clears at merge, and I
am not blocking on it.

### What I did not run

`npm test` and `npm run build` — the loop ran both on `9c5a9279142c`, this
branch, and recorded PASS. I ran the one check that would have been red if the
diff's claim were false: the render itself.
