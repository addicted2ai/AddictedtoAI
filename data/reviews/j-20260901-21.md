---
job: j-20260901-21
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that an open-weight retirement is a hosting decision rather
  than a model's death would link this paragraph: it holds a dated overlap
  window (K3 listed 2026-07-16, K2.5's vendor API sunset 2026-08-31, 46 days
  apart) next to the licence that keeps the older checkpoint downloadable
  afterwards, so the sentence "what lapsed was a hosted endpoint rather than
  the model" is checkable against two bound facts on the page rather than
  taken on the vendor's word.
carry:
  - title: Note that K2.5's catalog row is still live, so the co-listing has not ended
    detail: >-
      The repaired sentence now reads "for the stretch between them both rows
      were live in this catalog at once." That is true of the stretch, but the
      past tense reads as if the co-listing were over, and it is not:
      data/derived/catalog.json still carries row_id "moonshotai/kimi-k2.5"
      with "expiration_date": null and "status": "active" today, and the K2.5
      entry says so itself — "The catalog reads this row `active`; this page
      reads it `deprecated`, and the disagreement is which signal counts"
      (content/wiki/model/moonshotai-kimi-k2-5.md:92-93). What ended on
      2026-08-31 was Moonshot's own endpoint, not the pair of catalog rows. A
      half-clause would settle it — e.g. "both rows were live in this catalog
      at once, as they still are, the older one now on OpenRouter's data
      rather than Moonshot's hosting" — but it is a nuance on an otherwise
      accurate sentence, not a defect worth a revision pass.
    subject: content/wiki/model/moonshotai-kimi-k3.md
subject: "content/wiki/model/moonshotai-kimi-k3.md"
reviewed:
  "content/wiki/model/moonshotai-kimi-k3.md": "693f870a395e0f9b1d699b3335f81b63fea6285072267c854cb65f0073b729d4"
---

Free-form notes.

**Scope.** `git diff --stat main...HEAD` on this worktree:

```
 .job/brief.md                            | 698 +++++++++++++++++++++++++++++++
 .job/source.json                         |   8 +
 content/wiki/model/moonshotai-kimi-k3.md |   4 +-
 data/carried/j-20260901-19-carry-1.md    |  16 -
 4 files changed, 708 insertions(+), 18 deletions(-)
```

Two content-bearing changes: the two-line tense edit on the target file, and
the deletion of the carried finding that generated the item — which the brief
explicitly requires in the same diff ("leaving the file in place causes the
same item to reappear on the next Pulse run"). `.job/` is loop scaffolding, not
authored work. Nothing else is touched, so no scope violation. The job did not
file a proposal.

**The edit itself.** Read at
`content/wiki/model/moonshotai-kimi-k3.md:85-91`:

> K3's OpenRouter listing date is 46 days before that, and for the stretch
> between them both rows **were** live in this catalog at once. What **lapsed**
> at the end of it **was** a hosted endpoint rather than the model

Three verbs moved, nothing else. The direction matches the finding as
transcribed, and it matches the K2.5 page, which is already past tense on the
same event: "`kimi-k2.5` and the `moonshot-v1` series were officially retired
on August 31, 2026. Calls to these models now return a 404 (model not found)
error" (`content/wiki/model/moonshotai-kimi-k2-5.md:87-89`).

**Checks I ran rather than re-running the suite.** The claim the tense change
depends on is that the sunset is in the past, and the arithmetic beside it:

- The sunset date is a cited fact, not prose: `api_sunset`, value
  `"2026-08-31"`, `source_url: https://platform.kimi.ai/docs/models`,
  `accessed: "2026-09-01"` — `content/wiki/model/moonshotai-kimi-k2-5.md:59-64`.
  Today's local date is 2026-09-01, so the window closed yesterday and past
  tense is the correct tense. The paragraph reaches the date by transclusion
  (`{{fact:model/moonshotai-kimi-k2-5#api_sunset}}`), so nothing here is a
  typed literal that can rot.
- "46 days before that": K3's `listed_date` is `"2026-07-16"`
  (`moonshotai-kimi-k3.md:59-64`). 15 days remaining in July + 31 in August =
  46. Holds.
- Both transclusion targets still exist in the K2.5 entry's `facts` list —
  `api_sunset` (line 59) and `license` (line 47) — so the two `{{fact:…}}`
  references in the edited paragraph are unbroken. The loop's `npm run build`
  on this commit passing is the mechanical confirmation of that; an unresolved
  transclusion is a build error by design.

**The one thing I checked and did not block on** is in `carry:` above. I read
`data/derived/catalog.json` directly rather than inferring from the prose:

```
{ "row_id": "moonshotai/kimi-k2.5", "entry_id": "model/moonshotai-kimi-k2-5",
  "expiration_date": null, "status": "active", ... }
```

Both rows are still in the catalog today. The sentence does not actually assert
otherwise — "for the stretch … both rows were live" is a true statement about
that stretch — but the past tense invites the inference, and the K2.5 page
spends a whole paragraph on the fact that the catalog still reads the row
active. It is a shade of implication on a sentence that is otherwise correct,
which is a carry, not a revision.

I did not re-run `npm test` or `npm run build`; the loop recorded both PASS on
`87b9d257c485`, the commit this diff was computed from.
