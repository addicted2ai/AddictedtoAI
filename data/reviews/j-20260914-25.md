---
job: j-20260914-25
verdict: approve
reasons: []
would-cite: >-
  Nobody links a batch-tier model stub for its prose; its value is that the
  Mistral Small 4 batch price and context window render live on the page and
  in the batch-pricing table again. A reader comparing batch discounts across
  OpenRouter would land here, and with the binding restored the numbers they
  see come from the feed instead of a frozen "retired" state that stopped
  being true on 2026-09-10.
subject: "content/wiki/model/mistralai-mistral-small-2603-batch.md"
reviewed:
  "content/wiki/model/mistralai-mistral-small-2603-batch.md": "ca4356a87c72e35791dff124acfb1c949c5ccdc2436a5c466f2aca5f4a24e206"
---

## What the job asked

The row `mistralai/mistral-small-2603:batch` is back in the openrouter-models
snapshot, but the entry at its slug was retired by an earlier repair (commit
89234bf) and no longer declared it. If the entry covers the same subject, put
the `feeds:` binding back.

## What I checked

1. **Same subject, not a collision.** In `data/sources/openrouter-models/latest.json`
   (line 17841) the row id is `mistralai/mistral-small-2603:batch` and its name is
   `"Mistral: Mistral Small 4 (batch)"`. That matches the entry's `display_name`.
   `minted.json` line 942 maps that same row id to
   `entry_id: model/mistralai-mistral-small-2603-batch`, minted 2026-08-28. So
   restoring the binding is the right branch, not renaming.
2. **The restored block matches the minted original.** `git show 193ba10:` on the
   entry gives `status: active`, `maintenance: living`, the `feeds:` line, and four
   facts (price_input/pricing.prompt, price_output/pricing.completion,
   context_window/context_length, status/$status, all `volatility: fast`). The
   diff puts back exactly those lines, field for field. The paths resolve against
   the live row: `pricing.prompt` "0.000000075", `pricing.completion` "0.0000003",
   and `context_length` is present.
3. **The timeline dates are backed by the change log.** `data/changes.jsonl` has
   two matches for the row id. Line 112 is `kind: retirement`, date 2026-09-01,
   which is the existing row. Line 210 is `kind: arrival`, date 2026-09-10, which
   is the new "relisted" row. The source URL is the same catalog endpoint the
   retirement row already cites.
4. **The finding is current.** `data/derived/queue.json` still lists the
   `slug-collision` item for this subject. It will clear once the entry
   declares the row again.
5. **Scope.** Besides the loop's own `.job/` files, the diff touches only the
   target entry. It keeps the earlier retirement row and adds nothing I can't
   trace to a source. No blog post is in the diff, so no voice answer is needed.
   There are no reserved paths.

Polarity: the brief says "restore its feeds: binding" and the diff restores it.
Nothing is inverted.

Gates: the loop reports `npm run build` and `verify-surfaces` as PASS on 0e6ff3e.
Per the brief, I did not re-run them.

On corroboration: checks 1 and 3 both come from the Pulse's own snapshot
pipeline, so they agree rather than corroborate. Check 2 uses a different
instrument (git history of the minted entry) and independently confirms the
shape of the binding.
