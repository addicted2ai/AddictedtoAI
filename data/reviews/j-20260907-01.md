---
job: j-20260907-01
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a model's marketing surface and its serving surface are
  different documents — a router listing, a vendor's own docs table, and the
  weights' model card for one 8B roleplay fine-tune, all disagreeing about the
  context window and about a benchmark ranking that cannot be checked where it
  points. I would expect this linked in a thread about whether OpenRouter
  descriptions are vendor copy or the router's own paraphrase, and by anyone
  making the narrower point that a Hugging Face model card promising "the full
  131K context" is not evidence of what the endpoint will serve you: here the
  card says 131K while both the router row and the vendor's docs table say 32K.
  It is also the only place I know of that puts a Polish operating company, an
  Israeli headquarters field on the router, and an unrelated pharma company of
  the same name in one paragraph.
subject: "content/wiki/org/aion-labs.md"
reviewed:
  "content/wiki/org/aion-labs.md": "f73faedf23e9eca1b7c7ab2ab833cf72b9257ac83c6a91e1ea1dafcac3899aab"
---

Free-form notes.

## What the diff does

Three changes to one entry plus one deletion:

1. Adds a `cited` fact `vendor_rp_context` — the vendor docs table's Context
   column for the `aion-labs/aion-rp-llama-3.1-8b` row.
2. Rewrites the model-card sentence in the body: the ratio prose ("a window
   four times smaller") becomes a transclusion of the router-bound
   `{{fact:model/aion-labs-aion-rp-llama-3-1-8b#context_window}}` plus the new
   cited vendor fact, closing "Here the card is the outlier and the router
   matches the vendor."
3. Drops the "four rows in the catalog snapshot, as observed on 5 September
   2026" clause from the closing sentence.
4. Deletes `data/carried/j-20260906-08-carry-1.md`.

## Scope

The carried finding this diff retires is exactly the queue item the brief names:
`data/derived/queue.json` carries one aion-labs item and it is the text of
`j-20260906-08-carry-1.md`, which asked for precisely this ("Transclude the RP
context window instead of stating a ratio in prose ... Transcluding
`{{fact:model/aion-labs-aion-rp-llama-3-1-8b#context_window}}` and letting the
reader do the division, or rephrasing to name the window rather than the
multiple, removes the rot"). That file's own "Retiring this item" section
requires deleting it in the same diff, so the deletion is the mechanism, not
scope creep. The re-review and the repair item coincide in one job, which the
brief says is the right outcome. Nothing outside `content/wiki/org/aion-labs.md`
and that carried file is touched. Not a scope violation.

## What I fetched and ran

**The new cited fact.** Fetched `https://www.aionlabs.ai/docs/models/` directly
(node `fetch`, tags stripped, not WebFetch's extractor), HTTP 200, 19134 bytes.
The table header is `Context | Input / 1M | Cached / 1M | Output / 1M` and the
RP row reads, verbatim from the stripped text:

```
AionLabs: Aion-RP 1.0 (8B) | aion-labs/aion-rp-llama-3.1-8b | 32K | $0.80 | &mdash; | $1.60
```

So the Context cell for that row is `32K`. The fact's value — "the vendor's own
docs table lists the aion-labs/aion-rp-llama-3.1-8b row at Context 32K" — is
what the document says. `accessed: 2026-09-07` is today's local date, and the
fetch was made today. Source reachable, source says what the fact says.

**The transcluded router value.** `data/sources/openrouter-models/latest.json`
(captured 2026-09-07T06:00:03Z) has `aion-labs/aion-rp-llama-3.1-8b`
`context_length = 32768`; `previous.json` (2026-09-07T00:00:05Z) the same. The
model entry `content/wiki/model/aion-labs-aion-rp-llama-3-1-8b.md` declares
`feeds: openrouter-models: aion-labs/aion-rp-llama-3.1-8b` and a
`context_window` fact bound to `context_length`, so the transclusion resolves to
the live feed value rather than a literal. That is the fix the carry asked for:
the number now moves when the router moves, and the sentence no longer asserts a
ratio that would go quietly wrong.

**The model card, because the new sentence's conclusion rests on it.** Fetched
`https://huggingface.co/aion-labs/Aion-RP-Llama-3.1-8B/raw/main/README.md`,
HTTP 200, 742 bytes; the `rp_model_card` quote is present byte-for-byte
(`exact quote present: true`), including "It can utilize the full 131K context."
So 131K (card) vs 32768 (router) vs 32K (vendor docs): "Here the card is the
outlier and the router matches the vendor" is a correct reading of the three
documents as they stand today, and each of the three is a link on the rendered
page.

**The rest of the page, because a re-review judges the entry as it now
stands.** I sampled the claims most likely to have rotted or to have been
written from plausibility, and every one held:

- `https://api.aionlabs.ai/v1/models` — HTTP 200, shape `{"models":[…]}`,
  exactly four rows (`aion-2.0`, `aion-3.0`, `aion-3.0-mini`,
  `aion-rp-llama-3.1-8b`), every one `"is_moderated":false`, so
  `vendor_model_list` holds. `vendor_rp_description` ("A variant of LLama 3.1 8B
  optimized for immersive roleplaying and storytelling.") and
  `vendor_description_aion_3` (the GLM-family paragraph) are both present
  verbatim, and the RP row there also reports `"context_length":32768` — a third
  document agreeing with the router against the model card.
- `https://raw.githubusercontent.com/boson-ai/RPBench-Auto/main/results/leaderboard_for_display.csv`
  — HTTP 200, 18 lines including the header, first row
  `1,Higgs-Llama-3 70B V2,68.25%,73.68%,70.97%`, last row
  `17,Character.AI,12.47%,16.04%,14.25%`, and `/aion/i` does not match anywhere
  in the file. `rpbench_published_leaderboard` is exact.
- `https://www.aionlabs.ai/docs/` — the `public_model_endpoint` quote is present
  verbatim: "The GET /v1/models endpoint is publicly accessible without
  authentication."
- `https://openrouter.ai/api/v1/providers` — the aion-labs record reads
  `"headquarters":"IL","datacenters":[]`, and its `privacy_policy_url` is
  `https://www.aionlabs.ai/privacy-policy/`, the URL the entry records as 404.
  `router_recorded_headquarters` holds, and so does the setup for the
  Poland-vs-IL sentence.
- The paragraph about the missing closing clause, which is the page's sharpest
  claim and was not checkable from the repo snapshot (OpenRouter's API truncates
  `description`). I fetched both model pages: `openrouter.ai/aion-labs/aion-2.0`
  contains "mature and darker themes" and "nuance and depth";
  `openrouter.ai/aion-labs/aion-3.0` contains neither, and its description ends
  at "…producing stronger narrative structure and more compelling tension and
  conflict." That is exactly what the prose says, from the opposite direction to
  how it would fail.

**The dropped snapshot-census clause.** `data/snapshot-census-debt.json`
contains no `aion` entries (`grep -c aion` → 0), so removing the counted claim
leaves no census obligation behind. The "exactly four models" count survives in
the `vendor_model_list` fact, where it carries its own source and accessed date,
so the count is not lost from the page — only the undated prose restatement of
it is.

I did not re-run `npm test`, `npm run build`, `verify-surfaces` or
`verify-design`; the loop ran all four on `d1eb7649c42d` and reported PASS.

## Checklist

- **Cited facts sourced and supported** — the one new fact fetched and confirmed
  above; the two pre-existing facts the changed sentence now leans on
  (`rp_model_card`, the model entry's feed binding) both re-checked.
- **Volatile values are transclusions or feed-bound** — the change moves in this
  direction, which is its entire point. The one literal remaining ("Context
  32K") lives inside a `cited` fact in front matter with a source URL and an
  accessed date, which is the data layer, not prose. `slow` volatility for a
  vendor docs table's context column is defensible and matches the sibling
  `vendor_rp_description`.
- **Aliases sanely classed** — untouched by this diff, and still sane:
  `AionLabs` exclusive, `Aion Labs` shared, `Aion` and `Deep Forge` manual. The
  body explains why the two-word form is not linkable (an unrelated pharma
  company at `aionlabs.com` owns it), which is the right call.
- **Prose adds something beyond the data** — yes, and more than before. The
  three-way disagreement is now stated with the three numbers rather than as a
  multiple, and the sentence names which document is the outlier, which is a
  judgment the facts alone do not make.

The rewritten closing sentence reads slightly better than what it replaced: the
old clause carried a bare observation date that existed to satisfy a census
check, which is machinery leaking into prose. Dropping it and letting
`vendor_model_list` carry the count is the cleaner shape.
