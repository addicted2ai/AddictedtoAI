---
job: j-20260908-15
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that the November 2022 moment was a deployment decision rather
  than a research breakthrough would link this entry: it now carries OpenAI's own
  launch-day account of the pipeline — supervised fine-tuning on trainer-written
  dialogues, comparison rankings, a reward model, RL fine-tuning, on top of a
  GPT-3.5-series model that had finished training months earlier — which is the
  evidence that the capability predated the text box. The learn page's history
  paragraph is what a reader would follow to get there.
carry:
  - title: Name PPO as the RL algorithm in the ChatGPT launch entry
    detail: >-
      The new body paragraph says OpenAI "used that model for
      reinforcement-learning fine-tuning". The cited archived post names the
      algorithm outright — "Using these reward models, we can fine-tune the
      model using Proximal Policy Optimization" — so the specific mechanism
      specs/editorial asks for is available at zero sourcing cost and was
      generalised away. Replacing the phrase with "fine-tuned it with Proximal
      Policy Optimization" would fix it.
    subject: content/wiki/event/chatgpt-launch.md
subject:
  - "content/learn/where-ai-came-from.md"
  - "content/wiki/event/chatgpt-launch.md"
reviewed:
  "content/learn/where-ai-came-from.md": "db178c015132ae2cbcde70220acb59c5281424f3e834c49d2f2f580413b42b61"
  "content/wiki/event/chatgpt-launch.md": "123f43f2b33557775ff583a09a5ea35ee579e22f1795df95265d7fc3acf71c66"
---

Reviewed the diff for job `j-20260908-15` (repair, `addictedtoai-yjb5`). No
`content/blog/` file is in the diff, so no voice question arises. The diff
touches exactly three things: `.job/` artifacts, one sentence of
`content/learn/where-ai-came-from.md`, and `content/wiki/event/chatgpt-launch.md`
(three fact `source_url`s, one timeline `source_url`, two fact values, one new
body paragraph). That is inside the stated outcome, including the two
"while you are already there" items the directive named. No reserved path is
touched.

**The link claim, measured rather than reasoned about.** The gates' own build
output for this branch is present at
`D:/addictedtoai-worktrees/j-20260908-15/out/`, and its `status.json` reads
`"commit": "d9bb632560aa"`, `"dirty": false` — the same commit this diff was
computed from, so it is the rendered form of exactly these bytes. Grepping the
built learn page:

```
$ grep -o 'href="/wiki/event/chatgpt-launch[^"]*"[^>]*>[^<]*<' out/learn/where-ai-came-from.html
href="/wiki/event/chatgpt-launch" class="wiki-link" data-entry="event/chatgpt-launch">ChatGPT<
```

The inbound link exists and wraps the word the edit added. I also checked that
the edit did not create an ambiguity about *which* occurrence the wrap-only
linker would take: `grep -n "ChatGPT" content/learn/where-ai-came-from.md`
returns exactly one line (132), and the built page carries exactly two
`wiki-link` occurrences, the other being the pre-existing bitter-lesson link.
The tempting shortcut the directive warned about — a broader alias on the entry
— was not taken; `aliases:` is unchanged at `ChatGPT` (exclusive) and
`ChatGPT launch` (shared).

**The sources, fetched.** WebFetch refuses `web.archive.org`, so I fetched the
cited snapshot directly (`curl -sL`, HTTP 200, 58,333 bytes) and read the
stripped text. The document is *"ChatGPT: Optimizing Language Models for
Dialogue"*, dated on the page **November 30, 2022**, snapshot
`20221130211011` — first-party and same-day, which is strictly better than the
encyclopaedia URL it replaces. Every changed value is supported verbatim by
that document:

- `released: "November 30, 2022"` — the post's own dateline.
- `product_type: "a model developed by OpenAI that interacts in a conversational
  way, released as a free research preview"` — *"We've trained a model called
  ChatGPT which interacts in a conversational way"* and *"During the research
  preview, usage of ChatGPT is free."*
- `response_basis: "supervised fine-tuning followed by reinforcement learning
  from human feedback, starting from a GPT-3.5-series model"` — *"We trained an
  initial model using supervised fine-tuning… ChatGPT is fine-tuned from a model
  in the GPT-3.5 series, which finished training in early 2022."*

The new body paragraph checks out sentence by sentence against the Methods
section: *"human AI trainers provided conversations in which they played both
sides—the user and an AI assistant"*; *"comparison data, which consisted of two
or more model responses ranked by quality"*; *"To create a reward model for
reinforcement learning"*; *"Using these reward models, we can fine-tune the
model using Proximal Policy Optimization"*; *"fine-tuned from a model in the
GPT-3.5 series, which finished training in early 2022"*. Nothing in the
paragraph asserts a figure the source does not carry, and no figure was
invented to satisfy the editorial note — the paragraph adds a mechanism and a
date, which is what was asked for. The one thing it softens is the algorithm's
name; that is the `carry:` entry above, not a blocking defect.

I did not re-run the four gates; the loop's own record of them on `d9bb632` is
above the diff, and I ran only the checks that would have been red had the
diff's specific claims been false (the built-tree grep, the occurrence count,
and the source fetch).
