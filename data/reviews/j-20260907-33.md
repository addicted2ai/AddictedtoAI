---
job: j-20260907-33
verdict: revise
reasons: [broken-reference]
would-cite: >-
  A maintainer auditing proposal provenance would cite this repair to show the
  blocked-vendor-fetch route remains selectable rather than a dropped item.
---

The live proposal exists at
`data/proposals/primary-source-fetch-route-for-blocked-vendor-pages.md`; the
`dropped/` path does not. The direct filesystem check printed `True` then
`False` for those two paths.

`git diff --name-status main...HEAD` showed the expected generated job metadata,
deletion of `data/carried/j-20260907-05-carry-1.md`, and modifications to the
two referencing proposal records. `git diff --check main...HEAD` produced no
output. A search of the two changed proposal records found no remaining
reference to the old
`data/proposals/dropped/primary-source-fetch-route-for-blocked-vendor-pages.md`
path; the historical review record and current derived queue still contain
the carried finding text and were not changed by this job.

The first reference now reads “the live proposal already recorded at” the live
path. The second now has the live path but still reads “the argument for the
deferred work at” that path in
`data/proposals/dropped/openai-research-acceleration-view-inside.md`. Change
that phrase to explicitly identify it as the live proposal (for example,
“the argument for the live proposal at”). The carried finding deletion is
otherwise the requested retirement, and no blog post is in this diff, so no
voice carry-forward entry is required.

The branch gates were not rerun because the loop had already reported
`npm run test`, `npm run build`, `node scripts/verify-surfaces.mjs out`, and
`node scripts/verify-design.mjs out 3211` as passing immediately before this
review.
