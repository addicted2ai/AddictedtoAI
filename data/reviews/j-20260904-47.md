---
job: j-20260904-47
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a cheap-tier model's cost rose without its price moving
  would link this Google DeepMind entry, because the paragraph now hands the
  reader the vendor's own sentence — "3.8 Flash works harder" — with a live
  link to the launch post that says it, next to a third party pricing the
  consequence in tokens per task. The link is what makes that paragraph
  quotable by someone who has to check it before repeating it.
subject: "content/wiki/org/google-deepmind.md"
reviewed:
  "content/wiki/org/google-deepmind.md": "2b58f389449d6b46d5a73ca3e3b288d427da7b718feb16a10a82912dda393cce"
---

Free-form notes.

**Scope.** The diff is two files of authored change: nine lines in
`content/wiki/org/google-deepmind.md` (a markdown link wrapped around the words
"launch post", with the surrounding sentence reflowed) and the deletion of
`data/carried/j-20260904-45-carry-1.md`. `.job/brief.md` and `.job/source.json`
are the loop's own artifacts from commit `46e184a`, not author work.
`git show --stat HEAD` on `841f406c4462`:

```
 content/wiki/org/google-deepmind.md   |  9 +++++----
 data/carried/j-20260904-45-carry-1.md | 16 ----------------
 2 files changed, 5 insertions(+), 20 deletions(-)
```

That is exactly the stated outcome — link the URL at line 93, retire the
carried file in the same diff — and nothing else. No prose was rewritten; the
reflow moves words between lines without changing a single one of them.

**The source supports the two quotes, checked against raw bytes.** I fetched
the linked URL from Node (`fetch`, redirect: follow) rather than trusting an
extractor, stripped tags, and searched for the quoted fragments:

```
status 200 final https://blog.google/innovation-and-ai/models-and-research/gemini-models/3-8-flash-and-3-8-flash-cyber/ bytes 406118
"3.8 Flash works harder" -> 196536
   ...These performance gains stem from a core design choice: 3.8 Flash works
   harder. On complex tasks, it exhibits greater diligence — executing extra
   reasoning steps, and calling tools iteratively. At times, the model might
   use more tokens to...
```

The second quote matched case-insensitively, because the entry quotes it
mid-sentence and lowercases the leading "At":

```
idx 196674
At times, the model might use more tokens to maximize performance, especially
at higher effort levels. For applications where compute efficiency is the
primary constraint, developers can utilize lower effort levels to mi
```

So both fragments the paragraph attributes to the launch post are verbatim in
the document the new link points at, and the URL resolves 200 with no redirect
(final URL identical to the requested one). The document is also the 3.8 post,
not the 3.7 one: it announces "improvements from 3.7 Flash", and its footnote
reads "Introductory price expires on December 31, 2026. Starting January 1,
2027, $1.50/1M input tokens and $7.50/1M output tokens will apply" — which is
what the entry's line 104 paragraph says Google's launch post states, so the
paragraph the job did *not* touch is also supported by the document now linked
two paragraphs above it.

**The link matches the sibling entry, as the finding asked.** `grep` for
`3-8-flash` across `content/` shows
`content/wiki/model/google-gemini-3-8-flash.md:150` already carrying
`[launch post](https://blog.google/.../3-8-flash-and-3-8-flash-cyber/)` in
prose, plus the same URL as `source_url` at lines 56 and 110. The org entry now
uses the identical URL — one document, one spelling, and `blog.google` was
already an allowlisted origin in this file (lines 36, 48, the 3.7 post), so the
origin allowlist is not being widened.

**The retirement mechanism is the deletion, and the deletion is correct.**
`pulse/tests/carry-queue.test.mjs` documents the contract under test — "a
carried finding is a queue item exactly while its file exists under
`data/carried/`" — and `carriedFindingItems` reads the directory each run. With
the file gone, the item cannot reappear on the next Pulse run. Leaving it would
have re-queued the same finding.

**What I did not re-run.** `npm test` and `npm run build` are recorded PASS on
this same commit by the loop; I did not repeat them. The claim in this diff that
needed a check was the one about the source, and that is the check I ran and
quoted above.

Nothing to carry and nothing to propose. The line-104 mention of "Google's
launch post" is still unlinked, which is deliberate: the finding scoped the fix
to the first mention, one link per reference is ordinary practice, and widening
it would have been the scope violation.
