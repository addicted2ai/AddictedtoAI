---
job: j-20260901-19
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that Moonshot's K2.5→K3 handover was an overlap rather than a
  clean cutover would link this paragraph: it is the one place that states the
  46-day window between K3's OpenRouter listing and K2.5's API sunset with both
  endpoints bound to transcluded facts, so the interval can be re-derived from
  the catalog instead of taken on the page's word.
carry:
  - title: Update the K3 overlap paragraph's tense — the sunset has now passed
    detail: >-
      With the sunset date 2026-08-31 and today's local date 2026-09-01, the
      paragraph's "for the stretch between them both rows are live in this
      catalog at once. What lapses at the end of it is a hosted endpoint" reads
      as if the window were still open, but it closed yesterday. The K2.5 entry
      already says so in the past tense — "`kimi-k2.5` and the `moonshot-v1`
      series were officially retired on August 31, 2026. Calls to these models
      now return a 404" (content/wiki/model/moonshotai-kimi-k2-5.md:87-89). Past
      tense on the K3 side ("for that stretch both rows were live … what lapsed
      at the end of it was a hosted endpoint") would put the two pages in
      agreement. Outside this job's outcome, which was the direction word only.
    subject: content/wiki/model/moonshotai-kimi-k3.md
subject: "content/wiki/model/moonshotai-kimi-k3.md"
reviewed:
  "content/wiki/model/moonshotai-kimi-k3.md": "f631c671be79877ee6cddf68f566d0db524803d3055bd0c967954f915af7a977"
---

The diff is one word in prose plus the two mechanical retirement steps the brief
names. I did not re-run `npm test` or `npm run build` — the loop recorded both
PASS on `da493900d47e`, the commit this diff was computed from.

**The arithmetic, checked directly.** The claim under repair is that K3's
listing precedes K2.5's API sunset by 46 days.

- `content/wiki/model/moonshotai-kimi-k3.md:61` — `listed_date` `value: "2026-07-16"`,
  `source_url: "https://openrouter.ai/moonshotai/kimi-k3"`.
- `content/wiki/model/moonshotai-kimi-k2-5.md:59-62` — `field: api_sunset`,
  `value: "2026-08-31"`, `source_url: "https://platform.kimi.ai/docs/models"`.
- Interval:
  `node -e "const a=new Date('2026-07-16T00:00:00Z'),b=new Date('2026-08-31T00:00:00Z');console.log((b-a)/86400000)"`
  printed `46`.

So 2026-07-16 is 46 days *before* 2026-08-31, the magnitude in the prose was
never wrong, and "ahead of" was the only defective token. The new line 88 reads
"is 46 days before that", which is what the two front-matter values support.
The sentence it sits in still parses: the transclusion resolves to the bare date
2026-08-31, giving "K2.5's platform record reads 2026-08-31. K3's OpenRouter
listing date is 46 days before that, and for the stretch between them both rows
are live in this catalog at once." Nothing downstream in the paragraph depended
on the direction word — the following sentence's "what lapses at the end of it"
refers to the stretch, not to whichever endpoint came first, and reads the same
either way. The three other `{{fact:…}}` calls in the paragraph are untouched
and the build passed, so no transclusion broke.

**The retirement, checked against what recomputation would produce.** The brief
says deleting `data/carried/j-20260901-16-carry-1.md` is what retires the item.
That file is deleted. `data/derived/queue.json` drops exactly the one item whose
`subject` and `target` are `content/wiki/model/moonshotai-kimi-k3.md`, and
`count`/`total_before_cap` both fall 4→3. I checked the remaining three are the
faithful set difference rather than a hand-trim: the surviving items' titles are

```
carried-finding | 25 | Qualify "the row's first status flip" as first *recorded* flip
carried-finding | 25 | Confirm the js_payload numbers this repair re-recorded, or move them to their ow…
carried-finding | 25 | Pin the GLM-5.3 comparator benchmark figures against the card before writing
```

and `data/carried/` now holds exactly three finding files (plus `README.md`)
whose `title:` fields match those three one-for-one — `j-20260901-13-carry-1.md`,
`j-20260901-16-carry-2.md`, `j-20260901-07-carry-1.md`. No orphan item, no
orphan file.

**Scope.** Three paths beyond the `.job/` scaffolding: the target entry, the
carried file the brief tells the job to delete, and the derived queue that is a
pure function of it. Nothing else moved (`git status --short` on the worktree is
clean).

**One thing I noticed and am not blocking on**, recorded under `carry:` above:
the paragraph's present tense has aged out. The window closed on 2026-08-31 and
today is 2026-09-01, so "both rows are live" and "what lapses" now describe a
window that is over, while the K2.5 entry already narrates the same event in the
past tense. That is a different defect from the one this job was given and
correctly outside its diff.

Also worth a line for the loop rather than the author: no `RESULT.md` exists at
the worktree root and the tree is clean, so this run has no end-status file. The
branch state is nonetheless complete and coherent, which is what I judged.
