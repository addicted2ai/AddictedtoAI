---
job: j-20260831-07
verdict: approve
reasons: []
would-cite: >-
  The durable citation is as a dated, vendor-confirmed instance of the thing
  security arguments usually have to assert abstractly: that a replayed session
  cookie is authentication that already finished, so it walks past MFA rather
  than through it. Anyone who has been told "enable 2FA and you're covered" in a
  thread about account takeover can paste this and point at a named vendor
  emailing its own paying customers to say it signed them out, because signing
  out was the only thing that actually worked. The second argument it supports
  is a billing one rather than a security one: the case that a metered AI
  subscription has become a theft target valued for the inference it buys, and
  the evidence is that Anthropic's response was shaped like fraud response —
  strip the saved card, refund what was unauthorized — not like a breach
  response. The send case is the sharper one. A household or team that shares a
  Claude login sends this to whoever else holds it, and the operative line to
  send it with is that the sign-out already happened, so the remaining exposure
  is a laptop nobody has cleaned yet.
reads-human: >-
  Judging the revision, since that is what this pass covers. The delta made the
  piece read more human, not less: the passage the first pass named as
  machine-made was prose narrating its own rigor, and the boast inside it is
  gone, leaving a plain dated source list that shows the work instead of
  advertising it. The one new sentence is the stiffest thing in the piece — "A
  third action shows up only in BleepingComputer's reporting, nowhere in the
  email text Search Engine Journal reproduces" opens with ledger language,
  counting actions like a checklist, and then hangs the quote off a colon rather
  than letting it land in a clause. A reporter would more likely have written
  that BleepingComputer adds something the email leaves out. But it is stiffness
  in the service of telling a reader where a fact came from and where it did
  not, which is the opposite of the padding this reason exists to catch, and one
  bureaucratic sentence does not make 573 words machine-made. What carries the
  rest is a willingness to say things flatly and then stop: "Login sessions. Not
  passwords." "Switching on 2FA afterwards guards a door the intruder is not
  using." "Clean the machine first." The sixth paragraph makes a real argument —
  that the two remedies arrive in the wrong order, and that logging back in on a
  dirty machine just mints a fresh cookie into the same collection — and it is
  an argument the sources do not hand over ready-made. Measured directly on the
  revised file rather than inferred: zero em-dashes and zero semicolons.
subject: "content/blog/claude-session-theft-infostealers.md"
reviewed:
  "content/blog/claude-session-theft-infostealers.md": "94e21a4e9fd0c48ca0653d936fbeb70c15aa1771ad27315e6d93d778b747d283"
---

Delta review, pass 2. The first pass returned `revise` with one reason,
`false-or-unsupported-claim`, against one sentence. I reviewed the delta since
that verdict and re-fetched every source the delta touches.

## The diff in the brief was stale — I reviewed the branch state instead

The brief's diff shows the pre-revision text of the post, including the exact
sentence the first pass rejected. The branch does not. `git diff a493ff7
28f6678 --stat` returns:

```
 content/blog/claude-session-theft-infostealers.md | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)
```

One file, three hunks. I reviewed the file as it exists on disk at
`28f667865d06`, which is the commit the loop ran the gates on.
`git status --porcelain` is empty, so the working tree matches that commit.
Flagging this because a reviewer who trusted the embedded diff would have
returned `revise` against a sentence that is no longer in the post, and that
would have discarded the job on its last pass.

## The required change was made

The rejected sentence read:

> All four accounts below were retrieved on 31 August 2026, and the malware list
> and the three response actions appear in every one.

It now reads:

> All four accounts below were retrieved on 31 August 2026.

The corroboration claim is gone rather than rescoped. The related soft spot the
first pass named — the refund sentence sitting inside a paragraph attributed to
Search Engine Journal — was also repaired. It was:

> "Signing you out cancels that session everywhere, so the stolen copy stops
> working." Anthropic also refunded charges it identified as unauthorized.

and is now:

> "Signing you out cancels that session everywhere, so the stolen copy stops
> working." A third action shows up only in BleepingComputer's reporting,
> nowhere in the email text Search Engine Journal reproduces: the company is
> "refunding charges it identifies as unauthorized."

That is the repair the first pass asked for and it is the correct one: the
refund is now attributed to the outlet that actually carries it, and the post
tells the reader outright that it is not in the email.

## What I fetched, all on 2026-08-31

**BleepingComputer** — the quote is exact and the attribution is right. Asked
for verbatim transcription of any refund sentence, it returned:

> "The company is signing affected users out of Claude, removing saved payment
> methods, and refunding charges it identifies as unauthorized."

The post quotes "refunding charges it identifies as unauthorized" — character
for character, including the present-tense "identifies", which the previous
version of the sentence had drifted into "identified". Correct now.

**Search Engine Journal** — the absence claim holds. Asked whether refund,
refunds, refunded, refunding, reimburse or reimbursement appear anywhere on the
page, it returned `ABSENT`, and transcribed the email's actions section, which
has exactly two items:

> "Signed out the sessions involved. ... Signing you out cancels that session
> everywhere, so the stolen copy stops working. ... Removed your saved payment
> method, so it can't be charged through Claude. Your current plan continues for
> the billing period you've already paid for. To renew after that, or to make
> any purchase, you'll need to add a payment method again in Settings."

So "nowhere in the email text Search Engine Journal reproduces" is true, and it
is the half of the new sentence that does the work.

**The second hunk matches its source exactly.** The first pass made a
non-blocking observation that "ordinary downloads and malicious apps" was a
shade broader than the source. The author took it. The post now says "through
downloads or malicious apps", and BleepingComputer reads:

> "According to Anthropic, the malware typically arrives through downloads or
> malicious apps and steals information stored locally, including browser
> passwords, login cookies, and credentials belonging to other apps."

Verbatim, including the disjunction. The post attributes this phrasing to
BleepingComputer rather than to Anthropic, which is right — BC is paraphrasing
the email here, and the email's own wording is different.

## The imprecision I am approving over, stated plainly

The word `only` in "shows up only in BleepingComputer's reporting" is not exact,
and I want it on the record rather than glossed. Two of the four outlets the
post itself links at the bottom also carry the refund. Fetched today:

- **CyberPress**: "Anthropic has signed out affected sessions, removed stored
  payment methods, and refunded confirmed fraudulent transactions, but stressed
  these account-side fixes don't remove malware from an infected device".
- **Cybersecurity News**: "The company has begun signing out compromised
  accounts, stripping saved payment methods, and issuing refunds as it works to
  contain the damage." and "refunded confirmed fraudulent transactions."

So the refund is in three of four, not one of four.

I am not returning `revise` for it, and the reasoning is not that this is the
last pass. The sentence has two available readings. Under the dominant one —
set up by the appositive that immediately follows it, in a paragraph whose
entire subject is what the email said — `only` contrasts *reporting* with *the
email text*, and BleepingComputer is named because BC is the source being
quoted. That reading is true. Under the uncharitable one, `only` asserts
exclusivity among outlets, and that is false against two linked pages.

Three things keep this below the bar for a second non-approval. The error, on
the false reading, runs in the conservative direction: it claims a *weaker*
evidentiary base than actually exists, so no reader is misled about the world
or about Anthropic's conduct. The post makes no per-outlet content claim about
CyberPress or Cybersecurity News anywhere, so nothing it says about those two
sources is contradicted. And the reader who follows the link the sentence
actually attributes lands on BleepingComputer and finds the quoted words there —
which is exactly the failure the first pass identified and the exact thing that
has been fixed. `false-or-unsupported-claim` is defined as a claim the cited
source does not support; the cited source supports this one.

A one-word trim — dropping `only`, or writing "in the reporting rather than in
the email" — would make the sentence exact. It is correction-block sized, not
rejection sized, and I am recording it here so anyone who wants to make it has
the fetched evidence to hand.

## What I did not re-check, and why

The first pass verified the anchor, the Anthropic blockquote, the
"signing you out ... doesn't remove the malware" quote, the CyberPress quote,
the two Search Engine Journal quotes, the malware list and platform split, the
`status.claude.com` measurement, the absence of an Anthropic-published page, and
the absence of every forbidden item (no user count, no dollar total, no claim
about who the bad actor is). None of those lines are in the delta — the three
hunks touch the refund sentence, the arrival phrasing and the closing sentence,
and nothing else. Re-verifying unchanged text is not what a delta review is for,
and this pass had one named finding to judge.

Scope: the delta is one content file. It adds no files, touches no reserved
path, and does not disturb the derived artifacts — the title is unchanged, so
`backlinks.json` and `search-index.json` still match. The proposal
`claude-artifacts-fakeagent-malvertising` is unchanged by the revision and was
already within scope at pass 1.

Voice: I measured the punctuation markers directly on the revised file rather
than reconstructing the lint harness, since the delta adds about ten words to a
draft the first pass already measured at zero tripped markers. Grep for `[—;]`
over the post returns "Found 0 total occurrences". Positive control on the same
file confirms the pattern engine is matching that file rather than failing
silently.

Gates: `npm test` and `npm run build` were run by the loop on `28f667865d06`,
the revision commit, and both PASS. I did not re-run them.

## Proposal

None. This review surfaced nothing that is not this job's work; the one thing I
noticed is the `only` wording above, which belongs in this record rather than in
a proposal.
