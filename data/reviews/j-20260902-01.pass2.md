---
job: j-20260902-01
verdict: approve
reasons: []
would-cite: >-
  Anyone arguing that "the row disappeared from the catalog" and "the endpoint
  stopped working" are different events, and that a catalog diff alone cannot
  tell them apart. These three pages are the worked example: one snapshot, one
  router, three withdrawn slugs, and two opposite outcomes — 4.8 and 5
  deprecated-but-still-serving behind a new service-tier flag, 4.7 dropped with
  no successor because Anthropic's own API errors on fast mode for that model.
  A developer who wakes up to a 404-shaped monitoring alert on any of the three
  would link the page for their model to find out which of the two happened to
  them, and a writer covering router-side consolidation would cite the 4.7 page
  specifically for the negative evidence — the absent deprecation notice — that
  splits the cohort.
carry:
  - title: Note that the deprecated fast slugs list zero endpoints, and attribute "still serves" to OpenRouter
    detail: >-
      Both pages state in their own voice that the old slug "still answers and
      still serves" (4.8) / "the slug still answers, and existing requests still
      serve" (5). The sourcing is sound — OpenRouter's notice says exactly that,
      and it is quoted and cited — but the router's own endpoints API disagrees
      in shape: GET https://openrouter.ai/api/v1/models/anthropic/claude-opus-4.8-fast/endpoints
      returns HTTP 200 with the model record and an empty "endpoints" array (same
      for claude-opus-5-fast and claude-opus-4.7-fast; the surviving standard rows
      return 8, 11 and 10 endpoints respectively). Measured 2026-09-02. Nothing
      published resolves whether an empty endpoints list means the slug is aliased
      internally or genuinely unroutable, and it cannot be settled without an API
      key. A clause of the form "the model record still answers, though the
      router's endpoints listing for it is now empty; OpenRouter's notice is the
      authority that requests still route" would keep the reader's trust if they
      run the same check.
    subject: content/wiki/model/anthropic-claude-opus-4-8-fast.md
subject:
  - "content/wiki/model/anthropic-claude-opus-4-7-fast.md"
  - "content/wiki/model/anthropic-claude-opus-4-8-fast.md"
  - "content/wiki/model/anthropic-claude-opus-5-fast.md"
reviewed:
  "content/wiki/model/anthropic-claude-opus-4-7-fast.md": "a8b6c011061ce5f93e59a65971ab94c7d82395e9eb6bbbd17020ba1acaea9330"
  "content/wiki/model/anthropic-claude-opus-4-8-fast.md": "2ca823396f03bc9fe510cf04eafb2c48055d22ea34d96171a99ec608a1bcc88a"
  "content/wiki/model/anthropic-claude-opus-5-fast.md": "fc6a51c99c08bf839188e641d45372fad248bba27123005cf8ec5c663f93b73a"
---

Delta review, pass 2. The prior verdict raised six points under
`false-or-unsupported-claim` and `intent-not-measurement`. All six are fixed,
and every substantive claim the revision added is measured below. Approving.

**First, a discrepancy in my own brief, recorded because it nearly cost this
review.** The diff pasted into this brief is stale — it shows only the authoring
commit `8e4ee56`, not the revision `0e08fcf` the brief names as the commit the
diff was computed from. The pasted diff still contains "The row is not coming
back", "this one was the short-lived member", "the slug no longer resolves" and
"A request pointed at the old slug fails now" — i.e. exactly the text the
previous review asked to have removed. Those strings are **not on the branch**.
I reviewed the branch, not the paste:

```
$ git log --oneline -3
0e08fcf job j-20260902-01 (repair) revision: cite OpenRouter's deprecation notice, fix the migration instruction and the cohort split
8e4ee56 job j-20260902-01 (repair): document the vanished Anthropic fast rows as a router consolidation, not a tier retirement
5673290 job j-20260902-01: brief
$ git rev-parse HEAD
0e08fcf3900d30d46ffdc2dc4b725a5560a9266c   # matches the commit the brief names
$ git status --short                       # clean
```

The pasted diff also omits `data/derived/search-index.json`, which the real
diff touches. A reviewer who judged the paste would have re-issued all six
findings verbatim against text that no longer exists and discarded a corrected
job. Worth fixing in whatever assembles the delta-review brief.

I did not re-run `npm test` or `npm run build`; the loop's PASS on this commit
stands.

## What I fetched

All live, 2026-09-02, from a Node 24 script using plain `fetch` — no auth
header, raw bytes written to disk and searched there, not WebFetch's extractor:

```
or-47f.html   HTTP 200  229323 bytes  https://openrouter.ai/anthropic/claude-opus-4.7-fast
or-48f.html   HTTP 200  279583 bytes  https://openrouter.ai/anthropic/claude-opus-4.8-fast
or-5f.html    HTTP 200  279202 bytes  https://openrouter.ai/anthropic/claude-opus-5-fast
api-47f.json  HTTP 200     541 bytes  .../api/v1/models/anthropic/claude-opus-4.7-fast/endpoints
api-48f.json  HTTP 200     584 bytes  .../api/v1/models/anthropic/claude-opus-4.8-fast/endpoints
api-5f.json   HTTP 200     563 bytes  .../api/v1/models/anthropic/claude-opus-5-fast/endpoints
api-47.json   HTTP 200    7950 bytes  .../api/v1/models/anthropic/claude-opus-4.7/endpoints
api-48.json   HTTP 200   10845 bytes  .../api/v1/models/anthropic/claude-opus-4.8/endpoints
api-5.json    HTTP 200    9760 bytes  .../api/v1/models/anthropic/claude-opus-5/endpoints
or-5-std.html HTTP 200  935319 bytes  https://openrouter.ai/anthropic/claude-opus-5
fastmode.md   HTTP 200   27680 bytes  https://platform.claude.com/docs/en/build-with-claude/fast-mode.md
```

Plus `data/sources/openrouter-models/{previous,latest}.json` read out of this
worktree.

## The six prior findings, one at a time

**1 — "the slug no longer resolves" / "fails now". Fixed, and now sourced to the
document that settles it.** Both pages carry the router's own words. I stripped
the tags out of the notice on each page and diffed it against the quoted text:

```
or-48f.html: "As of September 1, 2026, this dedicated fast model is deprecated. Fast mode is
now served by the fast service tier endpoint on Claude Opus 4.8 — request it with
service_tier: "fast" or speed: "fast". Requests to this model keep working and are served by
the same fast tier capacity, so no action is required, but new integrations should target the
regular model. Learn more in our service tier docs:
https://openrouter.ai/docs/guides/features/service-tiers"

or-5f.html:  identical, naming Claude Opus 5.
```

The 4.8 page quotes `"Requests to this model keep working and are served by the
same fast tier capacity, so no action is required, but new integrations should
target the regular model"` and the 5 page quotes the same sentence. Both are
exact, character for character. Both attribute it to the model page **and** to
`https://openrouter.ai/docs/guides/features/service-tiers` — the same URL the
notice itself links. See the `carry:` entry above for the one wrinkle I could
not resolve.

**2 — the migration instruction. Fixed.** Both pages now say
`service_tier: "fast"` or `speed: "fast"` on the parent row. That is the
router's stated method, verbatim from the notice. The "pin the Anthropic Fast
provider" phrasing is gone from the instruction (the 5 page still uses
"Anthropic Fast" descriptively for what the provider row is *called*, which is
correct — see 7 below).

**3 — the notice is now cited. Fixed**, on 4.8 and 5, with both links.

**4 — the cohort split. Fixed, and it is now the strongest paragraph in the
set.** The 4.7 page's second paragraph states the negative directly. Measured by
counting substrings in the saved HTML:

```
                 "As of September 1, 2026"  "deprecated"  "keep working"  "service_tier"  lucide-triangle-alert
or-47f.html                              0             0               0               0                      0
or-48f.html                             12            12               8               8                      1
or-5f.html                              12            12               8               8                      1
```

`indexOf('As of September')` returns `-1` on the 4.7 page and a hit on the other
two. The API description confirms the same split in plain text: 4.8-fast and
5-fast both carry a trailing "Note: As of September…", while 4.7-fast's
description ends at the docs link with no note at all.

**5 — "this one was the short-lived member". Fixed and inverted, correctly.**
The page now says it "had been there longest" and prints all three dates. From
the live model records' `created` epochs:

```
anthropic/claude-opus-4.7-fast  created 1778613011 -> 2026-05-12T19:10:11Z
anthropic/claude-opus-4.8-fast  created 1779913703 -> 2026-05-27T20:28:23Z
anthropic/claude-opus-5-fast    created 1784912546 -> 2026-07-24T17:02:26Z
```

2026-05-12, 2026-05-27, 2026-07-24 — exactly the three dates on the page, in
the order it gives them.

**6 — "the row is not coming back". Gone.** Replaced with the present-tense
claim the doc supports ("fast mode on Opus 4.7 is not available anywhere
today") plus an explicit refusal to assert what the router has not said ("no
statement about calls at the old slug… do not build on it"). The 6× pricing
tension the prior review asked to be acknowledged is now acknowledged, with the
source quoted — see below.

## The revision's new claims, each checked

- **4.7-fast premium pricing.** Page: "0.00003 in / 0.00015 out per token
  against the standard row's 0.000005 / 0.000025 — six times the base". From
  `previous.json` (date 2026-09-01, `row_count` 420): `anthropic/claude-opus-4.7-fast`
  in `0.00003` out `0.00015`; `anthropic/claude-opus-4.7` in `0.000005` out
  `0.000025`. 0.00003/0.000005 = 6, 0.00015/0.000025 = 6. Exact.
- **The 6× quote is live and unmodified.** `api-47f.json` description:
  `"Fast-mode variant of [Opus 4.7](/anthropic/claude-opus-4.7) - identical
  capabilities with higher output speed at premium 6x pricing."` The page's
  quotation matches. Note the 4.8/5 descriptions say "2x pricing" instead —
  the page correctly attributes 6× to the 4.7 row only.
- **The vanishing itself.** `previous.json` (2026-09-01) `-fast` ids:
  `anthropic/claude-opus-4.7-fast, anthropic/claude-opus-4.8-fast,
  anthropic/claude-opus-5-fast, morph/morph-v3-fast`, all with
  `expiration_date: null`. `latest.json` (2026-09-02, `row_count` 421) `-fast`
  ids: `morph/morph-v3-fast`. All three standard rows survive at unchanged
  prices. Every page's framing of the snapshot boundary holds.
- **$10 / $50, twice the standard $5 / $25.** The `anthropic/fast`-tagged
  endpoint on both standard rows: `"pricing":{"prompt":"0.00001",
  "completion":"0.00005"}` — $10 and $50 per million. The other endpoints on
  those rows are `0.000005` / `0.000025` — $5 and $25. Exactly twice. And
  `0.00001 / 0.00005` is identical to the vanished 4.8-fast and 5-fast rows'
  last-known values in `previous.json`, which is what both pages claim.
- **"Anthropic Fast" is the router's display name.** `provider_name` in the
  endpoints API is the bare `"Anthropic"` with `"tag":"anthropic/fast"`, so the
  API alone does not support the label. The live model page does:
  `https://openrouter.ai/anthropic/claude-opus-5` contains
  `"provider_display_name":"Anthropic Fast"` six times, and the rendered
  provider table has `aria-label="Open Anthropic Fast details"`. The 5 page's
  phrasing is right.
- **4.7 has no fast endpoint.** `api-47.json`: 8 endpoints, tags
  `amazon-bedrock/global, google-vertex/global, claude-on-aws, anthropic,
  azure/global, amazon-bedrock/eu-west-1, google-vertex/europe,
  google-vertex/us`. No `anthropic/fast`. The page's "no fast endpoint among
  them" is measured. Conversely `api-48.json` (11 endpoints) and `api-5.json`
  (10) each contain exactly one `anthropic/fast` — which is what the 4.7 page
  means by "both of which are live on this router as standard rows with a fast
  service tier".
- **The Anthropic doc quote.** `fast-mode.md` line 29: "Fast mode is not
  available on Claude Opus 4.7. Requests to `claude-opus-4-7` with
  `speed: "fast"` return an error; unlike Claude Opus 4.6 (see the following
  note), requests do not fall back to standard speed." The page quotes the
  middle sentence and paraphrases the 4.6 contrast. Line 33 confirms the 4.6
  behaviour it contrasts against. Lines 21–22 list Opus 5 and Opus 4.8 as the
  supported models.
- **"research preview", "2.5 times", "twice the base price", and the beta
  header.** Line 2: `title: Fast mode (research preview)`; line 10: "Fast mode
  is in research preview." Line 7: "up to 2.5x higher output tokens per second
  from Claude Opus 5 and Claude Opus 4.8 at premium pricing. Set `speed: "fast"`
  with the `fast-mode-2026-02-01` beta header on your request to opt in." Line
  224 pricing table: `| Claude Opus 5 / Claude Opus 4.8 | $10 USD / MTok | $50
  USD / MTok |`, twice the $5/$25 base. Every element of both pages'
  Anthropic-side instruction — the header string, the parameter, the multiplier,
  the preview status — is on that page.

## Scope

```
$ git diff --stat f8e3c52 0e08fcf
 .job/brief.md                                      | 612 +++++
 .job/source.json                                   |   8 +
 content/wiki/model/anthropic-claude-opus-4-7-fast.md |  39 ++
 content/wiki/model/anthropic-claude-opus-4-8-fast.md |  31 ++
 content/wiki/model/anthropic-claude-opus-5-fast.md   |  31 ++
 data/derived/search-index.json                     |   6 +-
```

Three named entries plus the loop's own files. No entry deleted, no `feeds:`
binding removed, no front matter touched on any of the three — correct, since
the brief said the last-known-value rendering is designed behaviour and needs no
repair. No reserved path. The `search-index.json` change is the derived
regeneration of exactly these three rows (`"b": true` → `"b": false` on the
three fast entries and nothing else), which is the build's own output of the
same edit, not extra work. No review record binds any of the three
(`grep 'anthropic-claude-opus-(4-7|4-8|5)-fast' data/reviews/` → no matches), so
no mismatch arises.

## Why approve rather than carry the residual

The one thing I could not settle is in `carry:` — the deprecated slugs' empty
endpoints array. It is a real observation and I would rather it were on the
page, but it does not make the pages wrong: the claim they make is OpenRouter's
own published claim, quoted exactly and cited to the page and the docs it
links, and OpenRouter is the authority on whether its own routing works. That is
the opposite of the defect the previous pass caught, which was an inference from
a snapshot's silence dressed as a fact about the network. This draft consistently
says who says what, and on 4.7 it says plainly that the sources do not settle
the slug's fate rather than filling the gap. That is the behaviour the brief
asked for.
