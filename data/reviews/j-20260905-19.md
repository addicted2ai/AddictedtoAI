---
job: j-20260905-19
verdict: approve
reasons: []
would-cite: >-
  The implementer who writes the `superRefine` in `lib/schema.mjs` for task 3, and
  the reviewer of that implementation, both need the paragraph this revision adds
  to `specs/blog/spec.md` — it is the only place in the tree that explains why a
  frontier flag with no `domains` passes, and it names the four posts that would
  be silently unflaggable if someone "fixed" the gate back to requiring one. It is
  also what a future keeper session would link if K46 is ever challenged: it
  carries the argument (K38 made absence a stated value; the ≥1 bar predates the
  removal of `text`) rather than only the ruling id, which is what makes it
  survive the loss of the ui-loop artifacts it cites.
carry:
  - title: Call K46 a delegated ruling, not a keeper ruling
    detail: >-
      `proposal.md` says "Resolved as keeper ruling K46" and `specs/blog/spec.md`
      says "The bar was withdrawn as keeper ruling K46". The record does not
      support "keeper". `loops/ui-loop/state.md:59` records it as "**K46**
      (BLIND-002)" with no keeper attribution, in a list that marks keeper
      decisions explicitly ("K23 keeper, K24–K29 delegated"; "**K43** (keeper,
      2026-09-05)"; "**K45** (keeper via orchestrator)"). BLIND-002's own "Scope
      of the ruling" says the opposite in as many words: "This is not escalated:
      under K40 it is far short of 'insane catastrophic project threatening'."
      K46 was taken under K40 blanket delegation while the keeper was away.
      Replacing "keeper ruling K46" with "ruling K46, taken under the K40
      delegation" in both places fixes it, and it matters precisely because the
      passage exists to tell a later reader who decided this and on what
      authority.
    subject: openspec/changes/flag-what-moved-the-frontier/specs/blog/spec.md
---

Reviewed the four named edits, the two deletions, and the diff's boundary.

## The requirement change (the one that is not a wording fix)

Re-read the amended source first, as the brief directs.
`loops/ui-loop/graph/knowledge/DESK-ORDER-001.md` §1 now reads, at line 31:

```
domains: [coding, agents]      # OPTIONAL (K46, BLIND-002): values from the closed vocabulary (§3); absent = general
```

and at lines 52–56:

> **Gate (build):** `frontier: true` without `frontier_reason` in F1–F5, or with any
> `domains` value outside the vocabulary, fails the build. `domains` may be absent:
> by K38 absence IS the value "general" (K46, BLIND-002 — the ≥1 bar was written
> while `text` still existed to carry general stories; a court filing, a
> regulator's action, a licence term or a system card are frontier events with no
> domain).

The diff's three requirement-bearing edits are exactly that gate:

- `specs/blog/spec.md:34` — `domains` is now "OPTIONAL, flagged or not; zero or
  more values"; `specs/blog/spec.md:60-64` — the build fails a missing/invalid
  `frontier_reason` or an out-of-vocabulary `domains` value, and "**A post
  carrying no `domains` SHALL NOT fail**".
- `specs/loop/spec.md:60-69` — the candidate-flag bar moved to the same shape:
  refused for "no valid criterion, or ... a `domains` value outside the
  vocabulary".
- `proposal.md`'s "What changes" bullets restated to match.

Both directions match the amended §1. Nothing in the diff writes a display rule;
`proposal.md` names the general lane's placement as BRIEF-UI-002's, which is what
BLIND-002:41-47 assigns it ("the lane's position is a presentation detail for
BRIEF-UI-002"). The display contract is untouched, as instructed.

## The two amendment commits, verified

```
$ git show --stat --oneline 9936a76
9936a76 K46 (BLIND-002: domains optional, absent = general) and K47 (append-only seeding); Desk order §1 gate amended
 loops/ui-loop/graph/artifacts/BLIND-002.md      | 50 +++++++++++++++++++++++++
 loops/ui-loop/graph/knowledge/DESK-ORDER-001.md |  9 +++--

$ git show --stat --oneline c76db73
c76db73 Desk order: 1hjf review amendments (append-only seeding K47, three fields, all kinds, both tool sets); stale counts corrected
 loops/ui-loop/graph/knowledge/DESK-ORDER-001.md  | 20 +++++++++++++++++++-
```

Both real, both 2026-09-05, both touching §1. The diff's account of the
amendment is not taken on trust from the brief.

## Quotations checked against their documents, character by character

- `proposal.md`'s restored §1 quotation — "the three most recent per domain, with
  leaders-by-index secondary where a licensed index exists. Not a feed, not a
  ranking." — is verbatim DESK-ORDER-001.md:22-24. This is carry-2 discharged:
  the elided clause is back, and it is the clause that permits an index-derived
  secondary ordering.
- The front-matter line quoted in `proposal.md` is byte-for-byte DESK-ORDER-001.md:31,
  including the comment.
- The gate line quoted — "`domains` may be absent: by K38 absence IS the value
  'general'" — is DESK-ORDER-001.md:53-54.
- The K46 verdict quoted — "RESOLVABLE from the record: option A. `domains` is
  optional on a flagged record; absent means general by K38's own rule" — is
  BLIND-002.md:14-15, and the file exists at the path the diff names.
- K38's substance is confirmed independently at `loops/ui-loop/state.md:46`:
  "**K38** domain vocabulary: 'general' is the unmarked default, `text` is not a
  [tag]". So the diff's central argument — a gate that fails a record for
  carrying the vocabulary's own default contradicts the vocabulary — rests on a
  ruling that says what the diff says it says.

## The four posts, checked rather than assumed

`ls` on the four paths the blog delta names returns all four:
`content/blog/doj-statement-of-interest-llm-training-fair-use.md`,
`content/blog/eu-ai-office-first-enforcement-rfis.md`,
`content/blog/glm-5-3-license-revenue-gate.md`,
`content/blog/openai-gpt-6-astra-system-card.md`. The claim they carry ("four such
posts existed on 2026-09-05") is a count of named files, all of which exist, and
the F4/F5 characterisation is defensible against F4 ("a verbatim vendor claim ...
labelled unverified") and F5 ("a material change in access") at
DESK-ORDER-001.md:41-42. BLIND-002:32-33 calls them "F5/F4/F2 shaped"; the diff's
narrower "F4- and F5-shaped" is if anything the more accurate of the two, since
F2 is a lead change on an index and none of the four is one. Not a finding.

## Carry-1 discharged

`specs/loop/spec.md:3-5` now reads "The scout's charge, its two-test bar, its
docket discipline, its drop records, its synthesis branch, its blocked-streak
witness and its review gate are all unchanged." "its cap of three," is gone, which
is the whole of the carried finding, and the list that remains is still true of
this delta — the modified bullet touches the candidate flag and the cap exemption
and nothing else on that list.

## The retirement, which is the part most easily forgotten

Both carry files are deleted in this diff (`data/carried/j-20260905-15-carry-1.md`,
`-carry-2.md`), which is what each file's own "Retiring this item" section
requires: "leaving the file in place causes the same item to reappear on the next
Pulse run." Fifteen other carry files remain under `data/carried/`; only the two
this job discharged were removed. Correct scope on the deletion.

## Scope

Eight files. Two are `.job/` scaffolding (removed at merge, per the pattern in
`ba2a048`). Two are the mandated deletions. Four are the named edit sites plus
`tasks.md`, which had to move with the requirement — tasks 3, 4, 5 and 12 all
encoded "no `domains`" as a refusal and would otherwise have contradicted the
spec they implement. No task box changed state: the diff adds and removes zero
lines matching `\[x\]`, and the file still carries 12 unchecked boxes. Nothing
was archived, nothing implemented. No reserved path touched.

## The one honest uncertainty, and where it was put

`proposal.md:227-233` flags that the amended §1 names absence and says nothing
about `domains: []`, and that treating the two spellings alike is "a reading, not
a transcription", inviting an implementer to object before task 3 is written. That
is correct — I checked §1 and it genuinely does not mention the empty list — and
it is in `proposal.md`, which survives the merge, rather than in `RESULT.md`,
which does not. This is the right handling of an open question and I am not
blocking on the reading itself: K38's "general is a stated value, not an
omission" leaves no ground on which `[]` and absence could be made to differ.

## What I ran

- `openspec validate flag-what-moved-the-frontier --strict` →
  `Change 'flag-what-moved-the-frontier' is valid`. (The `--change` flag the
  project docs give no longer exists on this CLI: `error: unknown option
  '--change'` / `(Did you mean --changes?)`. The positional form works.)
- `grep -rn "at least one\|no .domains.\|REQUIRED when\|empty .domains\|domains: \[\]"`
  across the change directory, to find any surviving text still requiring a
  domain. Every hit is either the new permissive language, the historical
  narration of the withdrawn bar, or `frontier_reason`'s own conditional
  requirement. No stale "≥1 domain" rule survives anywhere in the change.
- I did not re-run `npm test` or `npm run build`; the loop reports both PASS on
  `7ddcf5ae9b5b`, the commit this diff was computed from, and this diff touches
  no code path either could exercise.
