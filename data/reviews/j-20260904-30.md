---
job: j-20260904-30
verdict: revise
reasons:
  - intent-not-measurement
  - false-or-unsupported-claim
would-cite: >-
  A reader trying to establish what Inception Labs claims for itself, in the
  lab's own words rather than a summariser's, would link this entry's product
  fact — it is the one line in the corpus that fixes the vendor's self-description
  to a dated fetch of the page it came from. That is exactly why the phrase has
  to be one the page actually contains: the entry's value is that it can be
  quoted back at the vendor, and a paraphrase welded to an abbreviation the page
  glosses differently cannot be.
carry:
  - title: >-
      Bump the product fact's accessed date if its value is re-derived from a
      2026-09-04 fetch
    detail: >-
      The diff rewrites the `product` fact's value but leaves
      `accessed: "2026-09-03"` in place, while the justification for the rewrite
      is a fetch dated 2026-09-04. Whichever value survives the revision, the
      `accessed:` date should be the date of the fetch the value was actually
      read off, since `volatility: slow` measures its interval from that field.
    subject: content/wiki/org/inception-labs.md
---

Free-form notes.

**The short version.** The premise this job was given is false, and the cheap
direct check that shows it is a substring search of the very page the fact
cites. The about page contains **both** phrasings. The original value was not a
weld of two phrases; it was a verbatim quotation of a sentence on the page. The
diff replaces that verbatim phrase with a different one and attaches to it an
abbreviation the page never attaches to it.

**What I fetched, and what it says.** `curl -sL https://www.inceptionlabs.ai/about`
on 2026-09-04, 287510 bytes — the same byte count the j-20260904-28 record
reports for its own fetch, so this is the same served document that record
worked from. I searched the raw HTML rather than an extractor. Every occurrence
of "diffusion" in the served markup:

```
      6 Diffusion
      2 Diffusion Guidance
      2 Diffusion Models
      1 Diffusion is the modeling shift needed for a future where AI is always-on and operates at
      1 Diffusion models generate through a coarse-to-fine process: rather than committing token-
      1 diffusion large language models (dLLMs). Mercury models seamlessly integrate into existin
      3 diffusion technology to develop a new generation of LLMs. Our dLLMs are much faster and m
      1 diffusion-based language models for production applications.
      1 diffusion-based large language models. Our founders have pioneered breakthroughs in model
```

The last two are the point. Both are inside rendered body paragraphs of the
about page — not a nav blob, not an embedded payload for another route. With
200 characters of leading context each:

```
="transform:none"><p class="framer-text framer-styles-preset-1g51cv9" data-styles-preset="Z8VNlpH4w">We’re a team of scientists, engineers, and builders focused on developing the next generation of diffusion-based large language models. Our founders have pioneered breakthroughs in model architectures, training met
```

```
e"><p class="framer-text framer-styles-preset-1g51cv9" data-styles-preset="Z8VNlpH4w" dir="auto" style="--framer-text-alignment:left">Inception is an AI research and product company. We’re building diffusion-based language models for production applications.</p><p class="framer-text framer-styles-preset-1g51
```

Same `<p>`, same preset class, both served, both visible text. I corroborated
with `WebFetch` on the same URL as a second instrument with a different failure
mode: its extraction reports "We're a team of scientists, engineers, and
builders focused on developing the next generation of diffusion-based large
language models." as sentence five of the page. Two instruments, one of which
renders and one of which does not, agree that the sentence is there. (Its
extraction *omitted* the "for production applications" sentence, which is the
usual reminder that its prose is not evidence of absence in either direction —
the raw markup is.)

**So the finding this job was created to fix is wrong on its face.** The carried
text asserts the about page "says 'We're building diffusion-based language
models for production applications' — no 'large' — and uses 'diffusion large
language models (dLLMs)' only in the separate Mercury sentence the fact already
quotes. The two phrases have been welded into one." The first clause is true;
the second is false, and the "welded" diagnosis that rests on it is false with
it. `diffusion-based large language models` is a contiguous, unmodified,
in-body phrase of the cited page. Nothing was welded. The prior value was a
quotation.

**Why the new value is not simply an equal alternative.** After the edit the
fact reads:

```
diffusion-based language models (dLLMs); it calls Mercury 'the world's first commercially available family of diffusion large language models'
```

The page introduces `(dLLMs)` exactly once, glossing `diffusion large language
models`. The edit strips the "large" out of the phrase the abbreviation stands
for and then leaves the abbreviation attached to it, immediately before quoting
the page's own un-stripped `diffusion large language models` in the next clause.
So the merged line would tell a reader that the about page abbreviates
"diffusion-based language models" as "dLLMs", which the cited source does not
support, and would do it two words before quoting the phrase the source does
abbreviate that way. The original had one page sentence behind it; this has a
half of one page sentence plus a gloss borrowed from another.

**Verdict reasoning.** `intent-not-measurement` is the primary reason and it is
the whole of the defect: the repair was written from the brief's description of
the source rather than from a measurement of the source, and the measurement
that refutes it costs one `grep` against markup the job had to download anyway.
`false-or-unsupported-claim` is the narrower, published consequence — the cited
page does not support the `(dLLMs)` gloss as the edit now attaches it.

I want to be fair about what is *not* wrong here: the diff is clean in scope
(the target, the carried file the brief names for deletion, and the loop's own
`.job/` scaffolding from commit `a8e65da`), touches no reserved path, and the
rendered sentence it produces is not a lie about the world — Inception does
build diffusion-based language models. This is a `revise`, not a `reject`.

**Required changes.**

1. Restore the `product` fact's value to the phrase the cited page contains:
   `diffusion-based large language models (dLLMs); it calls Mercury '…'`. If
   there is a positive reason to prefer the page's opening sentence instead,
   that is defensible — but then the `(dLLMs)` gloss must move to sit with the
   phrase the page actually abbreviates, or come out. What cannot stand is
   `diffusion-based language models (dLLMs)`.
2. Keep the deletion of `data/carried/j-20260904-28-carry-1.md`. The item is
   retired either way — a finding that turns out to be mistaken is still
   finished — and leaving the file would re-queue it on the next Pulse run.
3. Record in `RESULT.md` that the carried finding's premise was contradicted by
   the cited source, quoting the page sentence that contradicts it. The ground
   rules make this a successful outcome, not a failed one: "If the underlying
   resource is genuinely gone, record that as the finding rather than inventing
   a replacement" — the same instruction applies to a defect that turns out not
   to be there. Without that written down, the next reader of
   `j-20260904-28.md` sees only a carry that was silently actioned and has to
   re-derive all of this.

**What I deliberately did not do.** The loop reports `npm test` and `npm run
build` PASS on `51a738c93b69`; I did not re-run them. This diff makes exactly
one substantive claim — that the cited page's wording lacks a word — and I ran
the one check that would be red if that claim were false. It was red.
