---
job: j-20260902-04
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that an OpenRouter `expiration_date` is a soft scheduling
  marker rather than a shutdown notice would link this annotation: it is the
  only place that pairs the vendor's own definition of the field — now with the
  document and JSON path to check it against — with a four-day record of the
  same row's marker vanishing and returning, which is the concrete evidence
  that the date is not a commitment.
---

Free-form notes.

Scope. The diff is two carry-file deletions, one line of `data/changes.jsonl`,
and the loop's own `.job/brief.md` and `.job/source.json`. Nothing else. I
word-diffed the annotation text between `HEAD~1` and `HEAD` rather than trusting
the unified diff's single rewritten line; the only two changes are the two the
brief asked for, and the rest of the string is byte-identical:

```
--- REMOVED ---
defines `expiration_date` as "the date after which the model may be removed", so 2026-12-31 is the scheduled-earliest removal date rather than a firm one; the marker has now come and gone
--- ADDED ---
(`https://openrouter.ai/openapi.json`, `components.schemas.Model.properties.expiration_date`) defines `expiration_date` as "the date after which the model may be removed", so 2026-12-31 is the scheduled-earliest removal date rather than a firm one; the marker has now moved
tails identical: true
```

Finding 1 — "moved twice", not "come and gone twice". I did not take the carry
file's word for the snapshot history. I read `z-ai/glm-4.5v`'s `expiration_date`
out of every committed version of `data/sources/openrouter-models/latest.json`
via `git show <sha>:<path>` in a Node script (per the Windows note about MSYS
mangling `rev:path`):

```
f8e3c527 2026-09-02 expiration_date = "2026-12-31"
f3f8e62f 2026-09-01 expiration_date = null
771f5e2b 2026-08-31 expiration_date = null
0fa34f6d 2026-08-30 expiration_date = null
4edb269c 2026-08-29 expiration_date = null
04c6c8f7 2026-08-28 expiration_date = "2026-12-31"
```

Present, then absent across four consecutive snapshots, then present again:
two transitions, one gone and one come, four days apart — not two round trips.
`grep -n 'z-ai/glm-4.5v' data/changes.jsonl` returns exactly two `field_change`
rows for this row (`:86`, 2026-08-29, deprecated→active; `:152`, 2026-09-02,
active→deprecated) and no others, so no flip is hiding between the snapshots.
The old wording overstated the record; the new wording is what the records show.
The neighbouring clause "which the annotation on that change (j-20260830-02)
read as a metadata correction" also checks out against `:90`, which is that job's
annotation and says exactly that.

Finding 2 — the schema URL. Fetched `https://openrouter.ai/openapi.json` (200,
1,888,090 bytes) and read the named path directly rather than searching prose:

```
components.schemas.Model.properties.expiration_date = {
  "description": "The date after which the model may be removed. ISO 8601 date string (YYYY-MM-DD) or null if no expiration.",
  "example": "2025-06-01",
  "type": ["string", "null"]
}
```

The quoted fragment is exact (lowercased initial capital, standard for a
mid-sentence quote; the omitted second sentence is format detail, not meaning),
and the string "The date after which the model may be removed" occurs exactly
once in the whole document, so the path names it unambiguously. The URL and the
JSON path in the new text are both correct.

Sibling claim, re-checked because the whole line is in the diff.
`https://openrouter.ai/api/v1/models/z-ai/glm-4.5v/endpoints` returns 200 and
lists both providers named — `Novita` (`status: -2`, `uptime_last_30m` 94.4)
and `Z.AI` (`status: 0`, `uptime_last_30m` 97.5) — so "both its endpoints
(Z.AI, Novita) serve it as of 2026-09-02" is supported; Novita's negative status
gave me pause, but a 94% 30-minute uptime is a provider serving traffic, not a
dead endpoint. `https://openrouter.ai/api/v1/models` still carries
`{"id":"z-ai/glm-4.5v","expiration_date":"2026-12-31"}`, so the annotation's
lead is also still true live.

The 404 claim. `https://openrouter.ai/docs/api-reference/list-endpoints-for-a-model`
returned **404** when I fetched it, confirming the carry file's note. It matters
only that the author did not import that claim into the corpus — the annotation
asserts nothing about the docs page, and the carry file that did is deleted, so
nothing in the tree now carries a dead-URL claim that could rot.

Retirement. `data/carried/` now holds `README.md`,
`j-20260902-01-carry-1.md` and `j-20260902-03-carry-1.md` — both
`j-20260902-02-*` files are gone, so neither finding re-queues.

I did not re-run `npm test` or `npm run build`; the loop recorded both PASS on
this commit. Working tree was clean apart from the four scratch scripts I wrote
under `.job/` to do the checks above, which I deleted.
