---
job: j-20260907-23
verdict: approve
reasons: []
would-cite: >-
  A maintainer explaining the withdrawal of OpenRouter's free GLM 5.2 route could link this page to distinguish an absent published fact from a dated last-known value, while retaining the pinned evidence in the vanished-row record.
subject: "content/wiki/model/z-ai-glm-5-2-free.md"
reviewed:
  "content/wiki/model/z-ai-glm-5-2-free.md": "e219a5a5998b33a369321ce8d168b499527587d3c09d14dc58b55b2f9b7b9d30"
---

Free-form notes: The diff is limited to the requested repair, its generated job
metadata, and deletion of the carried finding file. The target paragraph now
says that the bound facts render as not published with no as-of date to show;
the finding file is deleted as required to retire the queue item.

I checked `lib/facts.mjs` directly. `resolveFact` returns
`{ state: 'vanished', value: missing ? null : value, asOf: row.$as_of ?? null }`.
The renderer's vanished branch uses `const value = r.value == null ? ABSENT :
quantity(r.value)` and emits `last known value, as of ${... ?? 'an unrecorded
date'} — the source no longer lists this row`. That supports the repair's
distinction: the visible value is `not published`, while the as-of date is
unrecorded rather than a dated last-known value.

The committed data check printed:

`{"$as_of":null,"$status":null,"$vanished":true}`

The snapshot check printed:

`latest.json: date=2026-09-07; row_count=430; hit=0`

`previous.json: date=2026-09-06; row_count=430; hit=0`

The corresponding freshness record printed `"has_last_known": false`,
`"last_seen_date": null`, and the target path. The pinned record exists at
`data/vanished/answered/openrouter-models--z-ai-glm-5-2-free.md`.

The read-only fetch of `https://openrouter.ai/api/v1/models` returned the
surviving `z-ai/glm-5.2` record and no `z-ai/glm-5.2:free` match in the lookup.

The targeted test command
`node --test --test-name-pattern="vanished declared row" lib/facts.test.mjs`
could not load because the shared install lacks `fast-glob`; it printed
`Error [ERR_MODULE_NOT_FOUND]: Cannot find package 'fast-glob'`. I did not
touch the shared `node_modules` or rerun the already-reported full gates.
