---
job: j-20260903-07
verdict: approve
reasons: []
would-cite: >-
  The public artefact here is five sentences that stopped saying "tokens
  tokens", which nobody links. The durable one is the demonstration that a
  same-line grep cannot audit a wrapped-prose corpus: the defect this job was
  dispatched against had six instances, the brief's own greppable pattern
  found three, and the sixth only surfaced when the scan read across the line
  break. Anyone arguing that lib/units.mjs needs a build-time check rather
  than another prose sweep would cite this branch as the worked example —
  a hand sweep that was careful, was reviewed twice, and still needed the
  reviewer's own instrument to reach complete.
proposal:
  slug: unit-literal-after-transclusion-lint
  type: machinery
  date: 2026-09-03
  summary: >-
    Add a prebuild step that fails the build when a unit-rendering
    transclusion is immediately followed by a prose literal duplicating the
    rendered unit — "tokens" after a fact whose FEED_UNITS entry is "tokens",
    "per token"/"USD"/"Mtok" after a "USD per token" fact — scanning the text
    that follows the closing braces across line wraps rather than per line.
    The corpus already fails the build on unknown front-matter keys, bogus
    mentions and unresolved transclusions; this is the same class and the same
    remedy. It also addresses the real cause rather than the instance: adding
    a path to lib/units.mjs silently changes how every piece of prose that
    already transcludes that path reads, and nothing today tells the author
    which pages just changed meaning.
  evidence: >-
    Measured on branch job/j-20260903-07 at 2196fc1cc6a9, 2026-09-03. Two
    successive hand sweeps of this exact defect class, each reviewed, still
    needed a machine to close: the author's same-line pattern found four of
    the six instances; the pass-1 reviewer's cross-newline scan found the
    fifth (openai-gpt-5-4.md:55-56, where the transclusion ended one line and
    the literal "tokens." opened the next). My own scan of all 368
    transclusions in content/ — 152 of them targeting a fact that unitFor()
    gives a unit, 0 targets unresolvable — now returns 0 suspects, but only
    because it reads 60 characters past the closing braces including
    newlines. Separately, seven of the seven figures the sweep's own drop
    record quotes for the lines it judged clean do not match what
    lib/facts.mjs renders for those transclusions, which is what a hand audit
    of rendered output costs and a mechanical check does not.
carry:
  - title: Correct every rendered figure quoted in the unit-doubling drop record's "judged not a doubling" and "checked and clean" sections
    detail: >-
      The five rows of the record's "Fixed" table are now correct and I
      verified each against lib/facts.mjs. Every figure in the two sections
      below it is not. Rendering each quoted line's transclusions through
      renderFact(..., { inline: true }) against data/derived gives:
      cohere-command-a.md:72 renders "256000 tokens context window", the
      record says 200000; meta-muse-glimmer-30b.md:98 renders "a 131072
      tokens context window", the record says 1000000;
      inception-mercury-2-5-preview.md:128 renders "260000 tokens of context
      and a max output of 65536 tokens", the record says 1107 for both;
      qwen-qwen3-8-27b.md:77 renders "262144 tokens of context; this row
      lists 1000000 tokens", the record repeats 262144 for the second;
      x-ai-grok-4-6.md:110 renders "1000000 tokens of context at 0.00000125
      USD per token input", the record says 0.0000008;
      z-ai-glm-5-1.md:80 renders 1048576, the record says 1000000;
      spacexai.md:71 renders 2000000, the record says 200000. The READINGS
      those figures annotate are all correct — my own corpus-wide sweep
      returns 0 suspects — so the record's auditable claim holds and nothing
      on the public site is affected; the quotes illustrating it were not
      read off a rendered page. The 1107 that appears twice is traceable:
      inception-mercury-2-5-preview.md:110 quotes the vendor's "achieving
      1,107 tokens/sec on standard GPUs", a throughput claim inside a
      quotation, which the same record separately and wrongly calls "a
      currency-literal lint case" — it is neither a currency literal nor a
      context window. Fix: re-render each line and replace the figure, and
      drop the currency-literal sentence.
    subject: data/drops/unit-doubling-sweep-j-20260903-07.md
  - title: Consider rephrasing "a <N> tokens context window" now that the unit renders
    detail: >-
      Not a doubling, and this sweep was right to leave both alone, but the
      units feature changed how they read. cohere-command-a.md:72 now renders
      "256000 tokens context window" and meta-muse-glimmer-30b.md:98 "a
      131072 tokens context window" (both measured through renderFact, not
      taken from the drop record, which gives different figures for both).
      The unit appears once, so nothing is factually wrong; the noun phrase
      is just ungrammatical where it used to read "a 256000 context window".
      Rephrasing to "a context window of {{fact:...}}" fixes both without
      touching a figure. Carried forward from the pass-1 record, which the
      loop reads only at the final pass.
    subject: content/wiki/model/cohere-command-a.md
subject:
  - "content/wiki/model/anthropic-claude-haiku-4-5.md"
  - "content/wiki/model/anthropic-claude-opus-4-8.md"
  - "content/wiki/model/openai-gpt-5-4.md"
  - "content/wiki/model/openai-gpt-5-6-luna.md"
  - "content/wiki/org/anthropic.md"
reviewed:
  "content/wiki/model/anthropic-claude-haiku-4-5.md": "b23900499872924df3bd96d2bb492d9f858d6a47f45bb4ae4fd47c72ca50ccbc"
  "content/wiki/model/anthropic-claude-opus-4-8.md": "635e9a25c73525264f4b7542fc029503cfdaf752597e9a2a7b9da2661769c009"
  "content/wiki/model/openai-gpt-5-4.md": "248873e8ed7b60291f8b908efa0e89c47f4cce49c035ac35f59bd2e452ea5ac8"
  "content/wiki/model/openai-gpt-5-6-luna.md": "2635ef237d85417577981ffbc180aeb1710eef3a405460f4321aa615e008b0b5"
  "content/wiki/org/anthropic.md": "81c12ea63581498295b1286b3e478c3090c85c4ac834d8891c4f983c68953291"
---

## Verdict: approve

Both pass-1 findings are resolved, and I measured both rather than reading the
commit message. The content deliverable — the thing that reaches the public
site — is complete: my own corpus-wide sweep finds nothing left.

### The delta

`git show --stat 2196fc1` — two files, `content/wiki/model/openai-gpt-5-4.md`
(4 lines) and `data/drops/unit-doubling-sweep-j-20260903-07.md` (13). Nothing
else moved. `git diff --stat HEAD main -- lib scripts data/derived` prints
**nothing**, so the branch touches no machinery and no derived data; the
renderer I measured against is byte-identical to main's, which is why running
it from the main checkout is a valid measurement of this branch's content.

Note on the brief I was given: its embedded diff is **not** the branch state.
It reproduces `c299b62` — no `openai-gpt-5-4.md` hunk, and the drop record
still carrying the uncorrected Luna figure — while its gates section names
`2196fc1cc6a9`. I judged the tree, via `git show 2196fc1`. This is the third
recorded instance of the same defect and it is already filed twice
(`data/proposals/delta-review-brief-embeds-pre-revision-diff.md`, which logs
2026-09-01 and 2026-09-02 recurrences, and
`delta-review-diff-recomputed-from-branch-head.md`), so I have not proposed it
again. The same proposal's "gates half" applies here too: the `npm test` /
`npm run build` PASS reported against `2196fc1` was, on that machinery
reading, measured on the pre-revision commit. I could not re-run either — this
review worktree has no `node_modules` (also already filed, as
`review-worktrees-carry-no-node-modules.md`) — but the revision is a deleted
word and a moved period in prose, and my sweep parsed all 368 transclusions in
`content/` with 0 unresolvable targets, so the transclusion syntax is intact.

### Finding 1 — the line-wrapped doubling on gpt-5-4: fixed, measured

`content/wiki/model/openai-gpt-5-4.md:54-57` now reads:

```
Four releases in a row shipped with the same context ceiling: GPT-5, 5.1,
5.2 and 5.3-Codex all cap at {{fact:model/openai-gpt-5#context_window}}.
This row broke the plateau —
{{fact:model/openai-gpt-5-4#context_window}}, more than double — and every
```

The period moved up to line 55 and the literal `tokens` that opened line 56 is
gone. Rendering the target through the real renderer
(`renderFact(fact, { dataLayer, feeds, today, entryId }, { inline: true })`
against `data/derived`, tags stripped and the source element removed):

```
model/openai-gpt-5#context_window
  unit="tokens"  renders as: "400000 tokens"
model/openai-gpt-5-4#context_window
  unit="tokens"  renders as: "1050000 tokens"
```

So the built sentence is "all cap at 400000 tokens. This row broke the plateau
— 1050000 tokens, more than double" — the unit reads once, the sentence break
is clean, and "more than double" still holds against 400000.

### Finding 2 — the drop record's Luna figure: corrected, measured

The table row now reads `"runs 1050000 tokens **tokens** against Nano's" →
"runs 1050000 tokens against Nano's"`. Measured:

```
model/openai-gpt-5-6-luna#context_window
  unit="tokens"  renders as: "1050000 tokens"
```

I re-measured the other four rows of the same table rather than trusting
pass 1:

```
model/anthropic-claude-haiku-4-5#context_window   -> "200000 tokens"
model/anthropic-claude-opus-4-5#context_window    -> "200000 tokens"
model/openai-gpt-5#context_window                 -> "400000 tokens"
model/anthropic-claude-opus-5#price_output        -> "0.000025 USD per token"
```

All five "Fixed" rows are now correct. The header's count moved 4 → 5 and its
new sentence describes the wrap accurately.

### The check that would be red if the sweep were still incomplete

I re-ran my own instrument against the post-revision tree rather than
re-running the loop's suite. Parse every entry's `facts:` block into
`<id>#<field>` → unit via `lib/units.mjs`'s `unitFor`, then scan every
`{{fact:…}}` in `content/`, reading the **60 characters that follow the
closing braces, across newlines**, for `tokens|per|USD|Mtok|dollars|/token`:

```
FEED_UNITS entries: 8
unit-carrying facts declared in corpus: 1292
transclusions total: 368
transclusions of unit-carrying facts: 152
targets with no '#': 0
--- SUSPECT (literal immediately after a unit-rendering transclusion) ---
suspect count: 0
```

Instrument ruled out: 368 is every transclusion in the corpus (the same count
pass 1 measured with an independent `grep -ro`), 0 targets failed to resolve
to an entry, and the scan does catch this shape: the pre-revision text at that
site was `…#context_window}}` + newline + `tokens.`, and my literal pattern
(`^[\s,.;:—–-]*\b(tokens?|per|USD|Mtok|dollars?|/token)\b` applied to the
following 60 characters) matches a leading newline before `tokens` by
construction — which is the one instance pass 1 blocked on. It now returns
zero. The sweep is complete.

### Why this is an approve and not a discard

The record's "judged not a doubling" and "checked and clean" sections quote
figures the renderer does not produce — seven of seven, enumerated in the
first `carry:` entry above, with what each actually renders. That is the same
family as the Luna figure pass 1 blocked on, and I do not want it read as
overlooked.

I am not blocking on it, for three reasons I can defend. The brief's done-when
requires the rendered sentence before/after **for each page fixed** — that is
the "Fixed" table, and all five of its rows are now correct and measured. What
it requires of the other lines is a drop record "naming the reading", and
every one of the seven readings is right: my independent sweep, which does not
share the author's pattern or its blind spot, returns zero suspects across the
whole corpus, so the record's auditable claim — these lines were considered
and are clean — holds. Second, pass 1 read those seven and explicitly cleared
them ("I read each of the seven and agree with all seven"); the author fixed
precisely what pass 1 named and nothing it was told was fine, which is
compliance, not evasion. Third, nothing here reaches a reader: `data/drops/`
is an internal audit file, and the five corrected pages are the public
surface. Discarding correct, verified content work over decoration on an
internal record would cost the site more than the defect does — so it goes to
`data/carried/`, where a repair job can be dispatched at it with the measured
figures already written down.

### Scope, and the rest of the checklist

- **Scope.** The delta touches one content file and one drop record. No
  reserved path, no machinery, no `data/config.json`, no second proposal. The
  branch as a whole is five one-word prose edits, the drop record and `.job/`.
  Within the stated outcome.
- **Rejection index** is empty. `data/proposals/rejected/` holds only
  `README.md`, so there is nothing to match against, and this is a mechanical
  defect class rather than a re-tread of an idea. The noted proposal's slug
  does not collide with anything in `data/proposals/` either.
- **Review hashes.** The revision edits a fifth reviewed surface, so
  `openai-gpt-5-4.md`'s recorded hash moves too — the same rebinding the other
  four already need, and the merge's job rather than the author's.
- **The proposal and the first carry entry are re-noted deliberately.**
  `loop/run.mjs:1206` and `:1231` pass `verdictPath(ctx, jobId, result.pass ?? 1)`
  — the FINAL pass's record only — and `data/carried/` contains no
  `j-20260903-07-*` file while `data/proposals/` contains no
  `unit-literal-after-transclusion-lint.md`, so pass 1's noted proposal and
  carried finding were never transcribed and would die with this record if I
  did not repeat them. I independently support both, and I corrected the
  second one's meta-muse figure, which pass 1 had taken from the drop record.
