---
job: j-20260914-24
verdict: approve
reasons: []
would-cite: >-
  A developer whose code still sends `~openai/gpt-latest` to OpenRouter and is
  wondering why it now lands on GPT-5.6 Sol rather than GPT-6 Astra would be
  linked here: the page shows the family-wide pointer was a router construct
  that split into four tier pointers on 2026-09-11, that the old slug now
  resolves to the Sol pointer, and that no OpenAI model was retired.
subject: "content/wiki/model/openai-gpt-latest.md"
reviewed:
  "content/wiki/model/openai-gpt-latest.md": "a85c12d0b7510222d9786a07f7c8465e211b71ec0820df0c119f8ba12df6b1b4"
---

Scope: the diff adds body prose and three `mentions` to the target entry,
regenerates the derived backlinks/search index those mentions produce, and
moves the vanished record into `data/vanished/answered/` at 100% similarity
(unchanged, as the brief requires). The `feeds:` binding is kept, as required.
`.job/` scratch scripts are the author's measurement instruments. No blog post
is in the diff, so no voice question applies. No inversion of any brief
requirement found.

Checks run by this reviewer, 2026-09-14 ~12:06 MDT (own scripts, not the
author's — fresh fetches, so they corroborate rather than merely agree):

- Live `https://openrouter.ai/api/v1/models` (447 rows): `~openai/gpt-latest`
  absent; present `~openai/gpt-sol-latest` (created 2026-09-11T12:48:48Z,
  alias_target `openai/gpt-5.6-sol`, prompt 0.000002 / completion 0.00001 —
  same as the pinned last-known rates), `~openai/gpt-terra-latest`,
  `~openai/gpt-luna-latest`, `~openai/gpt-astra-latest`; `openai/gpt-5.6-sol`
  still listed. Supports "one pointer became four", "direct successor", "same
  prompt and completion rates", and "still a listed row".
- `created` 1777318334 → 2026-04-27T19:32:14Z. Supports the 2026-04-27 date.
- `GET /api/v1/models/~openai/gpt-latest/endpoints` → `200`, payload
  `{"data":{"id":"~openai/gpt-sol-latest",...`; `GET
  openrouter.ai/~openai/gpt-latest` → `307 /~openai/gpt-sol-latest`. Both
  endpoints arrays printed `[]`, as did the Sol pointer's. Supports that
  paragraph exactly.
- Latest-resolution guide: alias slugs in the page are
  `~author/family-latest ~anthropic/claude-opus-latest
  ~anthropic/claude-sonnet-latest`. Supports "documents the form" and "names
  only two Anthropic aliases".
- `data/changes.jsonl` lines 220–224: all dated 2026-09-11 — gpt-latest
  withdrawal plus the four tier pointers. Line 180: GPT-6 Astra dated
  2026-09-05 (Pulse detection date; the API `created` is 2026-09-04T20:13Z,
  which is 2026-09-04 local — "entered the same catalog on 2026-09-05" reads
  as the Pulse's recording date and is correct against the corpus record).
- "Neither publisher explains the change" is stated as open rather than
  guessed, which is what the brief asks for.

Build and verify-surfaces were run by the loop (PASS); mentions resolve, so
not re-run.
