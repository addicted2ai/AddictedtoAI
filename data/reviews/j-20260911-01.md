---
job: j-20260911-01
verdict: approve
would-cite: >-
  Someone untangling the RLHF-versus-PPO confusion online — e.g. arguing that
  DPO replaced PPO inside the same preference-to-reward-to-policy pipeline
  rather than replacing RLHF itself — could paste this entry as the reference
  that holds the pipeline/optimiser distinction with the three dated papers
  attached.
subject:
  - "content/wiki/technique/proximal-policy-optimization.md"
  - "content/wiki/technique/rlhf.md"
reviewed:
  "content/wiki/technique/proximal-policy-optimization.md": "2dd442a95de33b135900d7d9ff7b75dd46bfeb441a68d8fa07cb19629a6b9945"
  "content/wiki/technique/rlhf.md": "f3f17b8b17ba62a0aac33780765cd04fcb8aa91da720d8d26e7e333686aa09eb"
---

Checked the two-file outcome against the brief, the alias mechanism, and the
three arXiv sources. All citations fetched from /abs/ (latest version, no
date-pinned version claim, so no v-pin needed) and confirmed against abstracts:

- pipeline_stages (cites 2305.18290): abstract says RLHF is "first fitting a
  reward model that reflects the human preferences, and then fine-tuning the
  large unsupervised LM using reinforcement learning to maximize this
  estimated reward without drifting too far from the original model." Supports
  "preference comparisons, then a reward model fitted to them, then policy
  optimisation against that model constrained to stay near the starting
  policy."
- dpo_substitution (cites 2305.18290): abstract says DPO solves "the standard
  RLHF problem with only a simple classification loss," "eliminating the need
  for sampling from the LM during fine-tuning," and "exceeds PPO-based RLHF
  in ability to control sentiment of generations, and matches or improves
  response quality in summarization and single-turn dialogue." Supports the
  fact value nearly verbatim.
- origin_2017 (cites 1706.03741): abstract says "goals defined in terms of
  (non-expert) human preferences between pairs of trajectory segments,"
  "effectively solve complex RL tasks without access to the reward function,
  including Atari games and simulated robot locomotion," and "successfully
  train complex novel behaviors with about an hour of human time." Supports
  the fact value.
- instructgpt_result (cites 2203.02155): abstract says "outputs from the 1.3B
  parameter InstructGPT model are preferred to outputs from the 175B GPT-3,
  despite having 100x fewer parameters" plus "improvements in truthfulness
  and reductions in toxic output generation while having minimal performance
  regressions on public NLP datasets." Supports the fact value.
- Timeline dates match submission histories observed on the same /abs/ pages:
  1706.03741 v1 Mon 12 Jun 2017; 2203.02155 v1 Fri 4 Mar 2022; 2305.18290 v1
  Mon 29 May 2023.

Alias mechanism checked directly against lib/aliases.mjs:69 (linkable only
when exactly one declaration and it is exclusive) and 9-16 (second declarant
is ambiguity, not build failure). Ran node over
data/derived/aliases.json: "RLHF", "Reinforcement learning from human
feedback", and the title-case variant each now resolve to exactly one
exclusive claimant (technique/rlhf), all linkable:true. PPO diff is exactly
the two requested alias deletions, nothing else (diff --stat: 4 deletions in
proximal-policy-optimization.md). New entry is kind:technique as instructed,
sitting alongside PPO/DPO with mentions to both. The extra title-case
exclusive alias beyond the two named in the brief is a case-variant of the
same name, not a scope widening.

Polarity: implements, does not invert. Scope: two content files plus derived
rebuild outputs (aliases.json, backlinks.json, search-index.json) and
.job/* bookkeeping only; how-models-are-trained.md and DPO page correctly
unedited — confirmed RLHF text exists there to auto-link
(how-models-are-trained.md line 123 "RLHF wherever you meet it";
direct-preference-optimization.md line 60), so the wrap-only linker claim is
plausible and hand-wiring was rightly avoided. Optional mentions: addition
declined by author; defensible either way per brief, no scope issue.

Volatile values: none — all facts static/dated with source_url + accessed.
Prose adds the pipeline-rationale and the PPO-slot distinction beyond the
data. Editorial call (RLHF = pipeline, PPO = interchangeable part) is
defensible on the page and supported by the DPO abstract's framing. No
corroboration pair shares a single instrument beyond the abstracts
themselves; each fact was checked against its own paper's abstract, not one
shared helper. Did not re-run build/surfaces per instruction (loop reports
both PASS on 0df6263a29e0).
