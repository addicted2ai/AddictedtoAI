---
job: j-20260914-15
verdict: approve
reasons: []
would-cite: >-
  A researcher distinguishing a router delisting from a model retirement would
  link this entry: it shows the OpenRouter row gone (page 404, endpoints array
  empty) while the vendor's own weights and releases page still publish the
  same 70B model with no retirement note, so the absence is a listing event,
  not a model death.
subject: "content/wiki/model/nousresearch-hermes-4-70b.md"
reviewed:
  "content/wiki/model/nousresearch-hermes-4-70b.md": "1c292ec709b5fcca18414c355a68e9156bf912a648fc8be75889bb9e387babc4"
---

Free-form notes: what I checked, fetched, ran, and observed.

Scope: the substantive diff is exactly the stated outcome — prose added to
`content/wiki/model/nousresearch-hermes-4-70b.md` plus the required move of
`data/vanished/openrouter-models--nousresearch-hermes-4-70b.md` into
`data/vanished/answered/` at similarity 100% (unchanged). `feeds:
openrouter-models: nousresearch/hermes-4-70b` is kept (lines 10-11 of the
entry). The other touched paths are `.job/brief.md`, `.job/source.json`
(loop mechanics) and derived `backlinks.json` / `search-index.json` rebuild
outputs. No widening, no `scope-violation`. No blog post in the diff, so no
voice question to answer.

Polarity: implements, does not invert. The brief required: establish which of
retire/rename/retier/delist happened from vendor and feed-publisher sources,
keep the entry and binding, move the vanished record unchanged. The diff does
all three: it concludes router-delisting with the model still served, keeps
entry + binding, moves the record byte-identical.

Source checks (all fetched by me in this review, 2026-09-14):

- Vendor weights
  `https://huggingface.co/NousResearch/Hermes-4-70B` — resolves, model card
  present: "Hermes 4 70B is a frontier, hybrid-mode reasoning model based on
  Llama-3.1-70B", hybrid reasoning mode section, FP8 link
  (`NousResearch/Hermes-4-70B-FP8`) and GGUF link
  (`lmstudio-community/Hermes-4-70B-GGUF`) both present. Supports "weights
  remain published ... hybrid reasoning mode on Llama-3.1-70B ... links to FP8
  and GGUF variants".
- Vendor releases `https://nousresearch.com/releases` — resolves; rows
  "Hermes-4-Llama-3.1-70B ... 08/26/25" and "Hermes-4-Llama-3.1-405B ...
  08/26/25" both present, no retirement note; "Hermes-4.3-Seed-36B ...
  Roughly equivalent performance to Hermes-4-70B at half the model size ...
  12/3/25" present verbatim. Supports the sibling-not-rename / still-listed
  claims and the quoted sentence.
- Router page `https://openrouter.ai/nousresearch/hermes-4-70b` — fetch
  returns "StatusCode: non 2xx status code (404 GET
  https://openrouter.ai/nousresearch/hermes-4-70b)". Supports "answers 404".
- Router endpoints
  `https://openrouter.ai/api/v1/models/nousresearch/hermes-4-70b/endpoints` —
  returns `{"data":{"id":"nousresearch/hermes-4-70b", ... "endpoints":[]}}`.
  Supports "returns the old record with endpoints:[]".
- Sibling endpoints `.../nousresearch/hermes-4-405b/endpoints` — returns one
  endpoint with `"provider_name":"Nebius"`, `"tag":"nebius/fp8"`. Supports
  "holds one live Nebius endpoint".
- Local snapshots (this branch): `latest.json` date 2026-09-14 row_count 445,
  `previous.json` date 2026-09-12 row_count 445; `nousresearch` keys in both
  are exactly `hermes-3-llama-3.1-405b, hermes-3-llama-3.1-70b,
  hermes-4-405b`, and the `hermes`-filtered key set is identical. Supports
  "absent from the catalog snapshots of 2026-09-12 and 2026-09-14", "no new
  nousresearch/ row arrived", "remaining Hermes entries are Hermes 3 and the
  405B". Last-seen 2026-09-09 and no `expiration_date` match the pinned table
  and the vanished record (`last_seen: 2026-09-09`).

Corroboration: the delisting conclusion rests on different instruments, not
one repeated — vendor weights page + vendor releases page (vendor voice) vs
router 404 + router endpoints-empty (publisher voice) vs local catalog
snapshots (derived state). Agreement between the 404 and the empty-endpoints
record is same-publisher agreement, recorded as such; the vendor-side evidence
is what corroborates that this is a listing withdrawal rather than a model
retirement. No pair of findings shares a single helper/fixture/derivation
path and pretends to be independent.

Gates: loop reports `npm run build` PASS and `verify-surfaces` PASS on commit
5bbbc4238cc2; per the brief I did not re-run them. No re-run was needed for
any claim; the snapshot checks above were run with `node -e` against the
branch's own JSON files and quoted verbatim.

Minor observations, not blocking: the front-matter `timeline` event
`retired` reads at first glance as contradicting the body's "What did not
happen is a retirement of the model", but in context it is the row's
departure record dated 2026-09-10 with the catalog as source, and the body
explicitly scopes the non-retirement to the model itself — acceptable, no
`revise`.
