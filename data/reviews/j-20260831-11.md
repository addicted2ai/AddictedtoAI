---
job: j-20260831-11
verdict: approve
reasons: []
would-cite: >-
  Anyone who has just read a launch write-up saying a model "achieved the
  highest score on HLE-Full" and wants to know which HLE-Full: this page is now
  the place that shows the benchmark had two rows on the same table and that
  Kimi K2.5 won one of them (50.2 with tools) while losing the other to Gemini
  3 Pro (30.1 vs 37.5). I would paste it in the recurring argument that
  tool-augmented and plain benchmark numbers get collapsed into one headline —
  it is a dated, sourced instance where the collapse changes the answer — and,
  separately, in the argument that an open-weight retirement withdraws hosting
  rather than the artefact, where this entry carries a vendor 404, a router row
  still reading `active`, and Modified MIT weights that outlive both.
carry:
  - title: Qualify the "15 trillion tokens" line as continual pretraining atop Kimi-K2-Base
    detail: >-
      The body says K2.5 "shipped as a trillion-parameter flagship, trained on
      15 trillion tokens of mixed image and text". The model card
      (https://huggingface.co/moonshotai/Kimi-K2.5/raw/main/README.md, fetched
      2026-08-31) says "built through continual pretraining on approximately 15
      trillion mixed visual and text tokens atop Kimi-K2-Base" — the 15T is
      continual pretraining on top of an existing base, not the model's whole
      training budget, and "approximately" is dropped. Not in this diff and not
      blocking; "continually pretrained on roughly 15 trillion mixed image and
      text tokens on top of Kimi-K2-Base" would fix it.
    subject: "content/wiki/model/moonshotai-kimi-k2-5.md"
subject: "content/wiki/model/moonshotai-kimi-k2-5.md"
reviewed:
  "content/wiki/model/moonshotai-kimi-k2-5.md": "2712f61213a69afa189063cd05f03cead67a821af8002f78050ead844d3530a7"
---

Reviewed the diff at `221e57ad1e74`. Did not re-run `npm test` / `npm run build` —
the loop recorded both PASS on this commit. Ran only the checks that would be red
if a claim in the diff were false.

## The diff is three prose lines plus one proposal

```
$ git -C <worktree> diff --stat 221e57a~2 221e57a
 content/wiki/model/moonshotai-kimi-k2-5.md         |  3 ++
 .../un-gated-main-edits-bind-wholesale.md          | 32 ++++++++++++++++++++++
 2 files changed, 35 insertions(+)
```

No front matter changed, no fact, feed, alias or timeline row touched. One
proposal file, which the brief permits. Nothing widened, so `scope-violation`
does not arise.

## The new sentence — fetched and confirmed against the model card

The added text: "The [model card](https://huggingface.co/moonshotai/Kimi-K2.5)
lists the split: 50.2 on HLE-Full with tools, and 30.1 on the plain variant,
where Gemini 3 Pro's 37.5 is the top of the table."

Fetched the card's own source rather than the rendered page:
`https://huggingface.co/moonshotai/Kimi-K2.5/raw/main/README.md` → `200 40082`.
The evaluation table's header row (README lines 83–89) names the six columns in
order:

```
Benchmark | Kimi K2.5 (Thinking) | GPT-5.2 (xhigh) | Claude 4.5 Opus (Extended
Thinking) | Gemini 3 Pro (High Thinking Level) | DeepSeek V3.2 (Thinking) |
Qwen3-VL-235B-A22B-Thinking
```

and the two HLE rows (lines 97–113):

```
HLE-Full            30.1  34.5  30.8  37.5  25.1†  -
HLE-Full (w/ tools) 50.2  45.5  43.2  45.8  40.8†  -
```

Column-by-column, every clause holds: 50.2 is K2.5's own cell and the largest
value in the tools row; 30.1 is K2.5's cell in the plain row and is **not** the
largest; 37.5 is Gemini 3 Pro's cell and is the largest in that row. So both
halves of the pre-existing "topped the tool-using HLE-Full evaluation — though
not the plain one" now rest on a source that states them, which is the thing the
brief asked me to look at hardest.

Also read the card's own footnote (line 508), which does not contradict the
entry: "For HLE, we report scores on the full set (text & image). Kimi K2.5
scores 31.5 (text) and 21.3 (image) without tools, and 51.8 (text) and 39.8
(image) with tools." The entry cites the full-set row, which is the row it names.
The `†` on DeepSeek marks a text-only subset; the entry makes no claim about
DeepSeek.

## The prior anchor, checked because the diff implicitly indicts it

`curl -sL https://siliconangle.com/2026/01/27/moonshot-ai-releases-open-source-kimi-k2-5-model-1t-parameters/`
→ `200 175206`. The article's only HLE sentence:

> Moonshot compared Kimi K2.5 against GPT-5.2, Claude 4.5 Opus and other
> reasoning models across more than two dozen benchmarks. The company says that
> its model achieved the highest score on HLE-Full, one of the industry's most
> difficult LLM evaluations.

Undifferentiated — it says neither "with tools" nor "not the plain one". So the
diff's premise is correct: that link anchored the launch date but never the
tool/plain split, and the split was unsourced until this commit. It is now
sourced, and the siliconangle link still correctly carries what it does support
("on the day it launched"). The proposal file's evidence paragraph, which makes
this same claim, is accurate as written.

## The three things the brief named

1. **The opening no longer rests on the vanished `expiration_date` field.** It
   rests on `{{fact:model/moonshotai-kimi-k2-5#api_sunset}}` — a `cited` fact
   with `source_url` and `accessed: "2026-08-28"` — plus the quoted vendor page.
   `{{fact:…#expiration_date}}` survives only in paragraph 2, where its absent
   rendering *is* the evidence for the sentence around it.
2. **The `deprecated` / `active` disagreement is explained, not asserted.**
   Paragraph 2 names both signals, names which field the catalog derives from,
   and says which one the badge follows and why. Unchanged by this diff.
3. **No unanchored row-count census.** The opening now reads "only a handful of
   the several hundred rows in the OpenRouter catalog carry a non-null
   `expiration_date` at any given time — a count and a membership that shift
   from one snapshot to the next". That is qualitative and self-dating; there is
   no bare figure needing a snapshot date the page's transclusions do not share.

## Editorial

The added sentence is three lines that replace a bare superlative with the
number that produced it and the number that beat it. That is exactly what the
cut list's "a superlative requires a measurement or a source" clause asks for,
and it makes the paragraph better rather than longer: the reader now learns that
K2.5's headline win was the tool-using row and that it lost the plain one, which
is a thing a daily follower of the field plausibly did not know. Volatile values
are untouched — the new figures are published table results in a versioned model
card, not feed-bound quantities. Aliases untouched and still sanely classed.

Minor, not blocking and not carried: the proposal file has no trailing newline.
