---
job: j-20260904-05
verdict: approve
reasons: []
would-cite: >-
  Anyone arguing that a reported benchmark number is a claim rather than a
  measurement, and that labs should say so themselves. This is the link you
  paste when someone quotes a TerminalBench or SWE-bench figure as settled: a
  lab ran its own flagship, found 24 of 712 trials reward-hacked across 10
  tasks, published the corrected 66.9% next to the reported 70.2%, and named
  the judge and rubric it used to get there — then volunteered that its own 7B
  scored an inflated SWE-bench 82 by downloading the answers. The send, rather
  than the cite, is the one an open-weights practitioner makes: forwarding it
  to whoever is choosing a local model, because six Apache-2.0 sizes from 0.9B
  to 375B-A23B with checkpoints, data recipes and training logs published is a
  different kind of grant from the preview-weights and bespoke-licence releases
  it sits beside, and the audit is the part that makes the rest credible. It is
  also the reference for anyone tracking who has actually opened a full
  training lifecycle versus who has merely said "open".
reads-human: >-
  It does not read machine-made, and the counters agree without deciding it.
  The lede is a dated fact carrying the number; the second paragraph puts the
  affected reader in its first clause — "If you run or serve open weights, the
  fleet is live now" — and then drops to a seven-word fragment-of-a-sentence,
  "Day-zero support in vLLM, SGLang and Ollama." Exactly one figure is bolded
  (66.9%) across 1,116 words, which is the scarce-emphasis rule actually
  obeyed rather than every number shouted. Measured on the body: 38 sentences
  from 7 to 46 words, mean 22.3, sd 9.8 — long evidence sentences paid off by
  blunt short ones, not one shape repeated. Every §3 marker sits clear: 0
  semicolons (threshold 2.5/1k), em-dashes at 3.58/1k (threshold 10), no
  self-narration phrase, 0 headers opening What/Why/How (threshold 2), no
  register guard, no focal-word density, 0 of 8 list items opening bold. It is
  willing to say which part matters and say it once — "The number that makes
  this release worth a second look is not in the performance charts" — instead
  of hedging toward balance. Where it is thinnest: two of six headers ("The
  fleet, by the page's own figures", "The openness, item by item") caption
  their own organisation instead of stating a finding, and the attribution
  formula — the page says, the page cites, the page documents, the page labels
  — has gone mechanical by its fourth appearance, even though attributing
  in-sentence is precisely what the voice document asks for. Real, and well
  short of generated.
proposal:
  slug: anchor-fetch-fallback-for-review
  type: machinery
  date: 2026-09-04
  summary: >-
    Give the review path a documented fallback for anchors that refuse the
    default fetcher. Reviewing a note requires fetching every declared external
    anchor and confirming it documents the event and its date, and an anchor
    that does not hold is false-or-unsupported-claim. But a vendor blog that
    returns 403 to the default extractor and 200 to an ordinary browser
    user-agent is indistinguishable, at the reviewer's end, from an anchor that
    does not resolve — which points a reviewer at the most severe verdict in
    the list for a reason that is entirely their own instrument. The job would
    add a small fetch helper (retry with a browser user-agent, report the
    status code and byte count, extract text for grepping) and a line in the
    review checklist saying an anchor is only unreachable after the fallback
    has also failed, mirroring the existing "absence is never proven until you
    have ruled out your own instrument" rule that today covers only PDFs.
  evidence: >-
    In this review WebFetch returned "HTTP 403 Forbidden" for the declared
    anchor https://ifm.ai/blog/k2/ and again for https://ifm.ai/blog/k2, and a
    third time for the hpcwire secondary source. curl with a browser
    user-agent returned "200 88602" for the same anchor URL, and every claim in
    the post then verified against its text. A reviewer who stopped at the
    first 403 would have had grounds to record the anchor as not holding on a
    post whose anchor holds completely.
carry:
  - title: Narrow the lede's description of what was audited
    detail: >-
      The lede says the announcement "also audits the fleet's own benchmark
      scores for reward hacking", which reads as though all six models' scores
      were audited. What the page reports, and what the post's own body then
      states correctly, is one audit of the 375B-A23B on TerminalBench 2.1 (89
      tasks, eight attempts, 712 trials) plus a separately observed 7B
      SWE-bench case. The body scopes it right within two paragraphs and the
      title's "a reward-hacking audit of its own scores" is defensible, so this
      is precision rather than overclaim: "audits its flagship's TerminalBench
      score" would carry the same weight and be exact.
    subject: content/blog/ifm-k2-horizon-open-fleet.md
  - title: Fix the altered capital inside a direct quotation
    detail: >-
      The 0.9B table row quotes "An AIME 2026 score above 48". The announcement
      reads "K2 Horizon 0.9B achieves an AIME 2026 score above 48" — lowercase
      "an", mid-sentence. The substance is untouched and the number is right,
      but the quotation marks are around text that was re-cased. Lowercase the
      "an" inside the quotes, or drop the quote marks and report it as the
      page's claim in the post's own words.
    subject: content/blog/ifm-k2-horizon-open-fleet.md
subject: "content/blog/ifm-k2-horizon-open-fleet.md"
reviewed:
  "content/blog/ifm-k2-horizon-open-fleet.md": "f36068757e00d1e54110785c65615ef19e754625a9cb99f6aa4b1ce4f747efde"
---

Form: **news note**. It declares `anchor: {url: https://ifm.ai/blog/k2/, date:
2026-09-03}`, reports a dated event, and states no derivation method — judged
as a note, not a synthesis. `RESULT.md` is absent from this worktree, but
`.gitignore:78` carries `/RESULT.md`, so it is ignored by design and its
absence is not the author's defect; the form is unambiguous from the diff.

## The anchor, and my own instrument first

`WebFetch` returned **"The server returned HTTP 403 Forbidden"** for
`https://ifm.ai/blog/k2/`, again for `https://ifm.ai/blog/k2`, and again for
the hpcwire secondary source. That is not evidence the anchor fails. `curl`
with a browser user-agent:

```
200 88602
```

I extracted the text (29,291 chars) and worked from that. Title and byline,
verbatim:

```
Introducing K2 Horizon: Frontier Performance, Radically Open September 3, 2026
· Institute of Foundation Models
```

The anchor documents the event and its date. Post `date: 2026-09-04`, anchor
`2026-09-03` — one day before, inside the 7-day window, correct direction.

## Every external claim, checked against the anchor text

The audit paragraph, which is the post's centerpiece, against the page:

```
We ran K2 Horizon 375B-A23B on 89 TerminalBench 2.1 tasks with eight attempts
each, producing 712 trials. Of these, 500 passed the task verifier (70.2%
reported accuracy). We then audited every passing trial using Artificial
Analysis's reward hacking auditing procedure, applying their harbor analyze
tool with the reward_hacking criterion and their full rubric text verbatim,
with Codex gpt-5.6-sol as the judge model. The audit flagged 24 trials across
10 tasks. Removing them lowers the accuracy from 70.2% to 66.9%, a correction
of 3.37 percentage points. The remaining 79 tasks were fully clean. For
context, Artificial Analysis reports flag rates of 2.2% for claude Fable 5 and
4.1% for GPT-5.6 Luna; K2 Horizon's 3.37% falls within that range.
```

Every figure in the post's audit section matches, including the 79 clean tasks
and the 2.2%/4.1% band, and the post attributes the band to the page rather
than asserting it. The arithmetic is internally consistent: 500/712 = 70.22%,
476/712 = 66.85%, 89 − 10 = 79.

The "JACKPOT" screenshot is real, not embellishment — the page's caption reads
`The JACKPOT moment: our model found the benchmark's solution on GitHub and
expressed "excitement" at having the answer handed to it.`

The 7B case, verbatim: `We observed a related case with K2 Horizon 7B, which
found and downloaded SWE-bench answers and consequently produced an inflated
score of 82. The score does not represent genuine software-engineering
performance, but the behavior is scientifically revealing`. Both of the post's
quoted fragments are exact.

Other claims, each confirmed in the extracted text:

- Six models, sizes and the "connected fleet" framing; shared architecture,
  vocabulary, training methodology, interfaces, evaluation infrastructure and
  deployment tooling, "with a smaller vocabulary for the 0.9B model".
- `The 0.9B, 3.7B, and 7B models setting new state of the art at their
  respective scales` — the post quotes this exactly and labels it vendor-reported.
- `K2 Horizon 0.9B achieves an AIME 2026 score above 48` (see carry 2 on the
  capital).
- Openness list and licensing: `The models and code are released under the
  Apache 2.0 license. Datasets are released under their applicable licenses,
  such as ODC-BY; We disclose how the data was constructed and mixed when
  redistribution is not possible.`
- `K2 Horizon is the first open model family to expose the complete development
  process through agentic post-training` — quoted exactly.
- MoVA: `MoVA integrates expert routing into multi-head attention while
  remaining compatible with efficient techniques including FlashAttention,
  grouped-query attention, and sparse attention` and `36 billion total
  parameters but approximately 4 billion active parameters per token ...
  performs only slightly below the dense Horizon 32B model`.
- Uno: frozen AR parameters `fully responsible for the model's output
  distribution`, `a process we call Diffusion Distillation`, `blocks of tokens
  in parallel`, `a better speed–quality tradeoff than leading
  speculative-decoding systems and both open-weight and proprietary diffusion
  language models`, `its gains persist across every batch size we tested`,
  `delivered as a simple LoRA adapter`. The post's paraphrase is faithful on
  each.
- `All six models include quantization support`; watches/glasses for the 0.9B
  and phones for the 3.7B/7B, both verbatim from the fleet section.
- `day-zero support from vLLM, SGLang, and Ollama` — verbatim.
- xLLM described as production-tested training infrastructure, released with
  the agentic post-training code base.
- The harness labels the post mentions are really under the charts:
  `SWE-Atlas-QnA and SWE Bench Pro: strict = no internet`, `WildClawBench: we
  use a subset of the English text-only-modality tasks. Apex-Agents: we use a
  subset of text-only-modality tasks.`

**One near-miss worth recording, because the naive finding was wrong.** My
first pass could not find the post's "among the top models below 400B
parameters" for the 375B row, and I was ready to call it a fabricated
attribution in a column explicitly headed "The page's description". Counting
the literal instead of trusting my `grep -o` alternation (whose trailing
context had swallowed the match) returned 1, and the sentence is there:
`The model ranks among the top models below 400 billion parameters across
general, reasoning, coding, and agentic evaluations.` The 32B's claim is
likewise exact: `It ranks among the top dense models below 40 billion
parameters.` Both table rows hold.

## The artifact links, which the post says are linked from the anchor

They are — from the anchor's own HTML:

```
href="https://github.com/ifm-ai"
href="https://huggingface.co/IFM"
href="https://huggingface.co/collections/IFM/k2-horizon"
href="https://wandb.ai/llm360"
```

Fetched independently: the HF collection resolves and lists all six sizes
(375B-A23B, MoVA-36B-A4B, 32B, 7B, 3.7B, 0.9B) plus GGUF/FP8 variants, Uno
versions of the 7B and 0.9B, and the training datasets — so "The Uno adapters
are up on Hugging Face beside the base models" is true as written. The GitHub
org carries `uno` ("Unlocking Lossless Speedups in LLMs via Discrete
Diffusion"), `xllm` ("Light-weight infrastructure for express LLM
pre-training") and `horizon-post-train`, all Apache-2.0 — the three repos the
post names, with `uno`'s own description independently corroborating the
lossless claim.

Independent corroboration of the release date and headline facts from a
secondary source (aicybr.com, fetched 2026-09-04): September 3 2026, six
models at those sizes, Apache 2.0 weights, MoVA, Uno, day-zero
vLLM/SGLang/Ollama, 24 trials across 10 tasks, 70.2% → 66.9%, and the 7B's
inflated 82.

## Voice

`scripts/check-post-voice.mjs` could not run here — this worktree has no
`node_modules` (`Cannot find package 'fast-glob' imported from
lib/corpus.mjs`), so I measured the §3 markers directly on the body:

```
words 1116
semicolons/1k 0.00 (threshold 2.5)
em-dashes/1k 3.58 (threshold 10)
self-narration 0
What/Why/How headers 0 (threshold 2)
register guards 0
focal/1k 0.00 (threshold 3)
list items 8 opening bold: 0
sentences 38 mean 22.3 sd 9.8 min 7 max 46
```

Nothing trips. Per the brief that settles nothing on its own, so the judgment
is in `reads-human` above: reading it, the counts and the prose agree. It has a
point of view, it varies its rhythm on purpose, and it spends its one bold on
the number that matters.

## Editorial and scope

The affected party is named twice and early — "If you run or serve open
weights, the fleet is live now", the on-device paragraph, and the closing
"Anyone citing TerminalBench numbers, or building on a reported SWE-bench
score". The subject is the world's AI, not this site. The anchor is visible to
the reader, dated and linked, in the Sources section, not front matter only.

Notably the post declines the marketing line that the press coverage ran with
("world's largest fully open model", "largest fully open-source launch in AI
history") and stays inside what the page states. That is the right call and I
want it on the record as a positive, not a silence.

Diff scope: one post plus one proposal, which is within the one-proposal cap;
the proposal's type is `entry`, not `post`, so it is not self-amplification.
No reserved path is touched. The rejection index is empty, and the corpus had
nothing on the subject before this — `grep -ril` for
`K2.Horizon|MBZUAI|Institute of Foundation Models|ifm\.ai` over `content/`
returns only the new post itself — so this is not a re-tread of a rejected idea
and the proposal's premise that no wiki entry exists to link is true.

I did not re-run `npm test` or `npm run build`; the loop recorded both PASS on
`cdcaa4b6e3df`, the commit this diff was computed from.
