---
job: j-20260904-17
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that per-token price is the wrong unit for comparing model
  generations would link this row: it is the rare page that holds the rate
  fixed, names the token count that moved underneath it, and prices the
  difference per task from a source independent of the vendor's own launch
  post. A buyer defending a budget overrun on an unchanged price sheet has
  the whole argument in two sentences here.
proposal:
  slug: delta-review-diff-must-include-the-revision-commit
  type: machinery
  date: 2026-09-04
  summary: >-
    The delta-review brief for this pass declared its diff computed from
    `bb4aa3e39b68` and then embedded the diff of the previous commit
    (`7899171`), so the revision this pass exists to judge was absent from the
    only artefact the reviewer is told to judge. A reviewer working from the
    supplied diff alone would have re-observed both pass-1 findings verbatim,
    returned a second non-approval, and discarded a job whose revision had
    already landed. The proposed job fixes whichever step in the loop resolves
    the head commit for a delta review so the embedded diff and the declared
    commit are the same object, and adds the cheap regression test — a branch
    with two job commits, where the delta brief must contain the second one's
    hunks.
  evidence: >-
    The pass-2 brief states "computed from commit `bb4aa3e39b68`". Its diff
    body shows `output_tokens_per_task` at `"~48k"` and the DeepSWE
    transclusion standing as its own sentence — the state at `7899171`. It
    shows none of `bb4aa3e`'s three hunks. `git diff --stat f3c12b2 7899171`
    returns exactly the supplied file list and counts (6 files, 654
    insertions, 44 deletions, `content/wiki/model/google-gemini-3-8-flash.md |
    6 +-`), while `git diff --stat f3c12b2 bb4aa3e` returns `8 ++++----` for
    that file. Both run 2026-09-04 in the review worktree.
carry:
  - title: Put the ~30% rise back into the model entry's prose
    detail: >-
      The carried finding that prompted trimming `output_tokens_per_task` to
      "~48k" argued the trim "loses nothing, since the ~30% and the effort
      level are already in the sentences". The effort level is; the ~30% is
      not. `git grep -n "30%\|~30"` over
      content/wiki/model/google-gemini-3-8-flash.md and
      content/wiki/org/google-deepmind.md returns nothing, so after this diff
      the relative-rise figure appears nowhere in the corpus for this row. The
      surviving prose reads "average output tokens per task rose to ~48k",
      which gives a reader no way to tell whether ~48k is a large move — and
      the paragraph's whole argument is that it is ("What changed is the
      invoice per task"). llm-releases carries it plainly: "a ~30% rise in
      average output tokens per task (to ~48k) lifts cost per task to ~$0.58 at
      high reasoning". Fixed by a prose edit, not a fact edit — "average output
      tokens per task rose ~30%, to {{fact:...#output_tokens_per_task}}" —
      leaving the trimmed value alone.
    subject: content/wiki/model/google-gemini-3-8-flash.md
subject: "content/wiki/model/google-gemini-3-8-flash.md"
reviewed:
  "content/wiki/model/google-gemini-3-8-flash.md": "2b5c8abf1f2df319cffa986698ce8f74520ba6ed6f474bdff6ac189f9779a05a"
---

Both pass-1 findings are cleared. The revision is `bb4aa3e`, and it is the
change I had to go and find, because the brief did not contain it.

## First: the supplied diff is one commit stale

The brief says its diff was "computed from commit `bb4aa3e39b68` — the same
commit the diff below was computed from". It was not. The embedded diff shows
`output_tokens_per_task` trimmed to `"~48k"` and the DeepSWE transclusion left
standing as a bare sentence — precisely the state pass 1 rejected. `bb4aa3e`'s
hunks are absent.

```
$ git diff --stat f3c12b2 7899171
 .job/brief.md                                 | 634 +++++++++++++++++++++
 .job/source.json                              |   8 +
 content/wiki/model/google-gemini-3-8-flash.md |   6 +-
 data/carried/j-20260903-11-carry-1.md         |  16 -
 data/carried/j-20260903-11-carry-2.md         |  16 -
 data/launch.json                              |  18 +-
 6 files changed, 654 insertions(+), 44 deletions(-)
```

That is byte-for-byte the file list, the counts and the per-file line deltas of
the diff I was handed — including `content/...flash.md | 6 +-`, where the true
head gives `8 ++++----`. So the artefact I was told to judge is
`f3c12b2..7899171`, not `f3c12b2..bb4aa3e`.

This matters more than a bookkeeping note: this is the last pass, and a
reviewer who took the supplied diff at its word would have found both pass-1
defects sitting there unchanged, written `revise` a second time, and discarded
a job that had already fixed them. Filed as the proposal above. Everything
below is judged against the real head.

## Finding 2 — the DeepSWE sentence, cleared

`git show bb4aa3e` on the front matter:

```
-    value: "DeepSWE v1.1 — outperforms most larger frontier models at a fraction of the cost"
+    value: "outperforms most larger frontier models at a fraction of the cost"
```

and on the prose (`content/wiki/model/google-gemini-3-8-flash.md:137-138`):

```
-Harvey's Legal Agent Benchmark at {{fact:...#harveys_legal_agent_benchmark}}.
-{{fact:...#deepswe_v1_1}}. The row keeps the
+Harvey's Legal Agent Benchmark at {{fact:...#harveys_legal_agent_benchmark}},
+and on DeepSWE v1.1, 3.8 Flash {{fact:...#deepswe_v1_1}}. The row keeps the
```

This is the option pass 1 named as preferable, taken in full: the value reduced
to the claim alone, the benchmark returned to a prepositional phrase, and the
model made the explicit subject of "outperforms". Rendered — `lib/facts.mjs:249`
emits `quantity(r.value)` inline, so the value lands verbatim between the words
either side of it — the sentence closes:

> …Harvey's Legal Agent Benchmark at 10.0%, against 8.8% for Gemini 3.7 Flash,
> and on DeepSWE v1.1, 3.8 Flash outperforms most larger frontier models at a
> fraction of the cost.

It parses, and the subject is the model, which is what the source says. I
fetched the entry's own `source_url` for that fact —
`https://deepmind.google/models/gemini/flash/`, 2026-09-04 — and it returned:

> "On DeepSWE v1.1 (Long-Horizon Software Engineering) 3.8 Flash outperforms
> most larger frontier models"

Same subject, same verb, same object. The list is a mild mixed construction
(three `X at Y` items then a full clause), but it reads and it is what the
carrying reviewer asked for; that is not a `revise`.

The same fetch confirmed the two neighbouring values the sentence joins, which
this diff did not change but which the sentence now depends on for its rhythm:
HLE-Verified — "3.8 Flash also achieves a 54.9% on HLE-Verified" — and Vals
Finance Agent V2 at 61.4%, both matching their recorded values.

## Finding 1 — the doubling, cleared on both surfaces

Pass 1 was right that trimming `output_tokens_per_task` alone left "at high
reasoning" doubled, and right about where the second copy came from. `bb4aa3e`
trims the other value:

```
-    value: "~$0.58 at high reasoning"
+    value: "~$0.58"
```

That one edit fixes both named surfaces without touching the second file, which
is also why the diff stays inside the target. Reading the two paragraphs at
head:

`content/wiki/model/google-gemini-3-8-flash.md:128-131` renders

> average output tokens per task rose to ~48k, lifting cost per task to ~$0.58
> at high reasoning despite the unchanged per-token price.

`content/wiki/org/google-deepmind.md:98-101` renders

> output tokens per task rose to ~48k, lifting cost per task to ~$0.58 at high
> reasoning even though the per-token price never moved.

One "at high reasoning" each, one "output tokens per task" each. Both stutters
gone.

I checked the attribution independently rather than inheriting pass 1's. WebFetch
of `https://llm-releases.com/models/gemini-3-8-flash`, 2026-09-04:

> "a ~30% rise in average output tokens per task (to ~48k) lifts cost per task
> to ~$0.58 at high reasoning despite unchanged per-token pricing."

~48k is the token figure, ~$0.58 the cost figure, and "at high reasoning"
attaches to the cost clause — which is exactly where both prose sentences now
put it, in their own words rather than inside a value. What the trim did lose is
the ~30%, which is the carry above, not a blocker: nothing rendered is false, a
reader is only shown less than the source offers.

## Scope — clean

```
$ git diff --stat f3c12b2 bb4aa3e -- content/ data/
 content/wiki/model/google-gemini-3-8-flash.md |  8 ++++----
 data/carried/j-20260903-11-carry-1.md         | 16 ----------------
 data/carried/j-20260903-11-carry-2.md         | 16 ----------------
 data/launch.json                              | 18 +++++++++---------
```

One content file — the named target. Both carry files deleted, which the brief
requires for a finding that is fixed, and both are. `data/launch.json` carries
only re-measured payload figures (`/catalog` inline 18.3 → 18.1, home html
8.4 → 8.7) and is unchanged since `7899171`; pass 1 established that a repair
commit carrying its own build's measurements is standing practice here, and
nothing about it moved since. `git status --porcelain` is empty.

`.job/brief.md` and `.job/source.json` are the loop's own artefacts, not the
job's work.

## What I did not re-run

`npm test` and `npm run build` — the loop ran both on this commit, PASS, and
neither is the check that would have caught either finding: a well-formed value
and well-formed prose fail nothing at the join. The checks that matter here are
the two source fetches above and reading the rendered join, and those are what I
ran.
