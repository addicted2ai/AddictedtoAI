---
job: j-20260904-48
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a proposal's evidence block has to cite the line that
  proves the claim rather than the line that happens to be nearby would link
  this: the old citation pointed at a paragraph about FrontierMath while the
  sentence being evidenced was about a self-quotation forty lines further down,
  and the fix is the smallest possible demonstration that a line range is a
  claim which can be checked and be wrong.
---

Free-form notes.

## What the diff is

Two authored changes plus loop scaffolding:

- `data/proposals/retire-carried-findings-satisfied-by-downstream-work.md` — one
  line, `:31-33` → `:263-266` in the `evidence:` block.
- `data/carried/j-20260904-46-carry-1.md` — deleted, which is what retires the
  queue item, exactly as the brief's "Retiring this item" section requires.
- `.job/brief.md` and `.job/source.json` are the loop's own commit
  (`6c18df4 job j-20260904-48: brief`), not the author's. `loop/run.mjs:1047`
  strips `.job/` and `RESULT.md` before merge and `run.mjs:283`/`:563` filter
  `.job/` out of the diff the breakers see, so their presence here is not a
  scope question. `git show --stat 2cff042` confirms the author's own commit
  touched exactly the two files above.

Nothing else. `git diff --stat HEAD~2 HEAD` printed only those four paths.

## The claim the fix makes, checked directly

The claim is that `data/reviews/j-20260904-04.md:263-266` is the reviewer's
confirmation of the *first* carried finding (the closing sentence of the
earlier post, quoted in full), and that `:31-33` was not.

Read `data/reviews/j-20260904-04.md` at those offsets:

- **263–266** —

      263	The quotation of this site's own earlier post — "Until the system card lands,
      264	the capability numbers are OpenAI's word, and the designation itself is the
      265	dated, checkable fact" — is verbatim the last sentence of
      266	`content/blog/openai-astra-critical-designation.md:57`. The `$1 billion`

  That is the confirmation of the first finding, and the range ends on the line
  the sentence ends on. Correct.

- **31–33** (the range removed) —

      31	page's table figure of 97.6% on FrontierMath Tier 4 (v2) directly beside the
      32	same page's prose claim that Astra "saturates FrontierMath Tier 4 with a 98%
      33	score", and it carries the footnote conditions — ExploitGym run without the

  FrontierMath only. It confirmed nothing about the self-quotation, so the old
  citation was pointing at the wrong finding. The removal is right.

- **186–188** (the range kept, for the second finding) —

      186	- `FrontierMath Tier 4 (v2) 97.6%` in the table; prose: "Astra saturates
      187	  FrontierMath Tier 4 with a 98% score". The post prints both and says which is
      188	  which — that is the discrepancy handled correctly, not glossed.

  That is the FrontierMath confirmation and it is still cited for the
  FrontierMath finding. Unchanged and still correct.

The evidence block introduces the two findings in the order "quotes the closing
sentence ... and `:44` prints ...", then cites `:263-266` and `:186-188` in that
same order. The pairing now reads left-to-right correctly, which it did not
before.

## Adjacent citations spot-checked (unchanged rows)

- `data/reviews/j-20260904-13.md:67` — the proposal says a later reviewer
  confirmed 97.6% is a genuine `<table>` row. Line 67 of that record reads
  `    FrontierMath Tier 4 (v2) | 97.6% | 83.0% | 87.8% | 87.8% | 73.2% | -`,
  under a `--- TABLE 3 (5 rows) ---` header at :65. Supports the claim.
- `content/blog/openai-astra-critical-designation.md` — the proposal's `:58` for
  the closing sentence is the correct line; the paragraph ending "the
  designation itself is the dated, checkable fact" is file line 58. (The
  j-20260904-04 record itself says `:57` at its line 266 — off by one, but that
  is a fixed historical record and not this diff's text. Not carried, because
  a past verdict record is not a document a later job edits.)

## Retirement

`data/carried/` now holds `README.md`, `j-20260903-14-carry-1.md` and
`j-20260904-07-carry-1.md`. `j-20260904-46-carry-1.md` is gone and no other
carry file was touched, so exactly the dispatched item leaves the queue.

## Not re-run

`npm test` and `npm run build` were run by the loop on this same commit
(`2cff042d9d0f`) and both passed; per the brief I did not repeat them. The
change is one substring inside a YAML folded scalar in a `data/proposals/`
file, which nothing in the build parses for line numbers, so the check that
would be red if the claim were false is the record read above, and it is green.
