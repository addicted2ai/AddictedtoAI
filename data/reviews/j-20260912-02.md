---
job: j-20260912-02
verdict: approve
reasons: []
would-cite: >-
  A future maintainer debugging a held train would cite this change as the
  worked example of the hyphen-vs-colon gate resolution gap: the frozen
  TRAIN_GATES names could not resolve through either runGates branch, and the
  fix plus the resolution test that pins all six names.
---

Free-form notes: what I checked, what I ran, and what I observed.

Scope: the job's own commit a88efde touches exactly loop/lib/gates.mjs and
loop/tests/train-gate-resolution.test.mjs (git show --stat HEAD: 2 files).
TRAIN_GATES names and order are unchanged; package.json is untouched in the
job commit (still carries verify:launch / verify:analytics colon forms, no
hyphen scripts — read package.json lines 19-20 to confirm the defect
premise); no reserved path, hold, or train branch file is in the job commit.
The wider main...HEAD diff includes train ancestry (68ca9ee/dd6bd52/c6d082b),
which is base, not this job's work. No content/blog files in the job commit,
so no voice question to answer.

Defect premise confirmed by measurement, not reading: base 366c8cd's
gates.mjs has NODE_GATES entries only for verify-surfaces and verify-design
(git show 366c8cd:loop/lib/gates.mjs), while TRAIN_GATES already froze all
six hyphenated names — so verify-launch/verify-analytics resolved neither via
NODE_GATES nor via package.json and failed closed on every run. Both script
files exist in the worktree (scripts/verify-launch.mjs, scripts/verify-analytics.mjs:
Test-Path True/True).

Ran the changed check (the new test file), foreground, in this worktree:
node --test loop/tests/train-gate-resolution.test.mjs → 6 pass, 0 fail:
- THE FROZEN SIX ALL RESOLVE THROUGH runGates, under the production floors
- the two new gates run as REAL child processes with the production argv shape
- EACH FROZEN NAME WITHOUT RESOLUTION FAILS CLOSED
- verify-launch reuses the train build
- verify-analytics is a check, not a record
- a gate NAME alone still resolves to the right command for all six

Ran the guard-rail suite the acceptance criteria names: node --test
loop/tests/gates.test.mjs loop/tests/job-gate-set.test.mjs → 15 pass, 0
fail, including "A MISSING gate script is a FAILURE, not a silent skip" —
the fail-closed path still fails closed. Did not re-run npm run build /
verify-surfaces: the loop already ran both PASS on this branch's commit and
the brief instructs not to repeat them.

Direct state check via gates.mjs exports (script file, foreground):
{"launchFloorMs":275,"analyticsFloorMs":4875,"nodeGates":["verify-surfaces","verify-design","verify-launch","verify-analytics"],"train":["test","build","verify-surfaces","verify-design","verify-launch","verify-analytics"],"launchArgs":[],"analyticsArgs":["out","3212"],"analyticsNoRecord":["data/launch.json"],"designEnv":{"ATAI_VERIFY_DESIGN_NO_RECORD":"1"}}.
So: launch floor 275ms admits the ~1s reuse run the comment claims; launch
args carry no --no-build (grep of scripts/verify-launch.mjs confirms no-arg
invocation reuses via hasCurrentBuild, while --no-build records the SKIP
wording "THE BUILD WAS NOT VERIFIED BY THIS RUN"); analytics args are
out + 3212 matching the script's argv contract (argv[2] export dir or base
URL, argv[3] port default 3212; it writes data/launch.json via LAUNCH_FILE),
and the no-record restore for that path lives in nodeRun's try/finally with
the design env discipline untouched.

Corroboration: the stubbed-spawn resolution arm and the fail-closed arm share
one instrument (runGates with stubbed child), so their agreement is recorded
as agreement only. Corroboration comes from different instruments: the REAL
child-process arm (real node runs the argv), the pre-existing
gates.test.mjs real-child arms (missing-script FAILURE, design no-record),
and the source reads (package.json colon names, base NODE_GATES with two
entries, verify scripts' argv/launch.json handling).

Polarity: implements, does not invert. No scope-negation tripped; the
brief-requested recalibration and noRecordPaths addition are the fix, not
widening.
