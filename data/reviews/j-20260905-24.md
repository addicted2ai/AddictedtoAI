---
job: j-20260905-24
verdict: approve
reasons: []
would-cite: >-
  The orchestrator (or the maintainer) the next time somebody proposes putting a
  Design Arena elo or win-rate on the board: this record is the thing you cite to
  say the rights question is answered "yes, with a condition" and that the
  condition — a visible Design Arena credit and link beside every displayed value
  — is what is missing, not the publisher's permission. It would also be cited in
  the narrower argument it settles cleanly: a permissive developer-docs grant and
  a restrictive ToS on the same site are not a contradiction to be resolved by
  picking the friendlier one, and the operative question is who the grant is
  addressed to. The 165/243 reconciliation is the other citable half — DESK-ORDER-001
  §4's "165 rows" is the non-empty count, and the next reader no longer has to
  re-derive which figure a stated number meant.
carry:
  - title: Make the six new tools/ scripts runnable after the job worktree is deleted
    detail: >-
      Five of the seven scripts hardcode the job's own worktree as their root —
      `const root = 'D:/addictedtoai-worktrees/j-20260905-24'` in
      tools/measure-design-arena.mjs and tools/measure-design-arena-history.mjs,
      `const OUT = 'D:/addictedtoai-worktrees/j-20260905-24/data/reviews/evidence/...'`
      in tools/fetch-design-arena-terms.mjs, the same prefix throughout
      tools/append-evidence-transcripts.mjs, and it is the default of
      `process.argv[2]` in tools/scan-out-for-design-arena.mjs. That worktree does
      not survive the merge, so `node tools/measure-design-arena.mjs` from the
      repository root will fail on a path that no longer exists — while the
      evidence file states the scripts were "kept so the run reproduces rather
      than being described". The two pre-existing scripts in the same directory
      (tools/ui-evidence.mjs, tools/ui-invariants.mjs) hardcode no absolute path.
      Deriving the root from `process.cwd()` (or an argv override, as
      scan-out-for-design-arena.mjs already half does) would fix all five.
  - title: tools/strip-body.mjs is listed as evidence machinery but nothing ran it
    detail: >-
      The evidence file names seven scripts "kept so the run reproduces", among
      them "`strip-body.mjs` (HTML → text)". tools/fetch-design-arena-terms.mjs
      carries its own inline `strip()` and never imports it, and the `.raw.txt`
      transcript records four appended runs — measure, measure-history,
      check-quotes, scan-out — with no strip-body invocation. Either drop it from
      the file's list of what reproduces the run, or say it was an ad-hoc helper.
---

Fresh context, no edit rights. Everything below was run or fetched by me in this
worktree (`D:/addictedtoai-worktrees/j-20260905-24-review-1`, commit `fa0b7fb`);
I did not re-run `npm test` or `npm run build`, which the loop had already run on
this commit.

## 1. The quotes, fetched independently (the checklist's "confirm the source")

I did not read the author's `.raw.txt` as evidence of what the documents say — I
re-fetched all five documents the registry row quotes with `node:fetch` and
substring-matched the quotations against the fetched bytes (raw, entity-decoded,
and tag-stripped). Every fragment the registry row and the evidence file rest on
is present:

```
== https://docs.designarena.ai/introduction.md -> 200 bytes=2495
  PRESENT "Data from the Design Arena API is free to use for personal and commerc…"
  PRESENT "**Attribution is required.** If you display this data publicly (dashbo…"
  PRESENT "Credit **Design Arena** as the source"
  PRESENT "Provide a visible link to [designarena.ai](https://designarena.ai)"
  PRESENT "By applying for API access, you agree to these attribution requirement…"
  PRESENT "API access requires an API key"
== https://docs.designarena.ai/api-reference/overview.md -> 200 bytes=7516
  PRESENT "All API requests require authentication using a Bearer token."
  PRESENT "Describe your use case and agree to the attribution requirements"
== https://www.designarena.ai/terms-and-conditions -> 200 bytes=262816
  PRESENT "Last Updated: June 7, 2026"
  PRESENT "Arcada Labs and its licensors exclusively own all right, title, and in…"
  PRESENT "Use, display, mirror or frame the Services or any individual element w…"
  PRESENT "for any commercial purpose or for the benefit of any third party"
  PRESENT "to generate data, derived or aggregated in deidentified form, from suc…"
== https://www.designarena.ai/robots.txt -> 200 bytes=102
  PRESENT "Disallow: /api/"   PRESENT "Allow: /"
== https://www.designarena.ai/developers/apply -> 200 bytes=216198
  PRESENT "as the source and include a visible link to"   PRESENT "I will credit"
```

robots.txt in full, so the registry's block quote can be compared character for
character — it matches (the record drops only the trailing newline):

```
"User-Agent: *\nAllow: /\nDisallow: /api/\nDisallow: /admin/\n\nSitemap: https://designarena.ai/sitemap.xml\n"
```

The apply-form sentence is the one the author flagged as split by `<strong>` and
an `<a>`; my check reproduces that — it matches on the tag-stripped text and on
the two fragments that straddle no markup, exactly as the record says, and the
record's disclosure that the instrument (not the document) produced the first
ABSENT reading is accurate.

**Judgment on the reading, not just the strings.** The ToS and the docs page do
say opposite-tending things, and the record does not hide it: §17(a)'s ownership
claim, §7's "Service Information" definition and §8's display/commercial-use
prohibitions are quoted in the registry row itself, alongside the grant. The
record's stated resolution — that §8 governs use of the Site and App and is
silent about data received from a third party, so it declines to extend the
docs' grant rather than contradicting it — is a defensible reading, and it is
written down *as* a reading with the counter-reading named. That is the honest
shape. Crucially the record does not use it to clear anything: the conclusion is
still "not cleared", on two stated grounds (the grant addresses an API applicant
this repository is not; the attribution condition is about display and is unmet).

## 2. The feed counts, re-measured from the committed snapshots

I wrote my own counter rather than running the author's (theirs points at a
worktree path outside this one). Independent result:

```
latest   fetched_at=2026-09-05T06:00:04.599Z rows=431 carrying=243 nonEmpty=165 empty=78
         entries=1577 slugs=120 pairs=25 arenas=agents,models keys=arena,category,elo,rank,win_rate
previous fetched_at=2026-09-04T06:00:03.738Z rows=427 carrying=243 nonEmpty=166 empty=77
         entries=1573 slugs=121 pairs=25
```

Every figure in the registry row and the evidence table reproduces: 431 rows,
243 carriers, **165 non-empty**, 78 empty arrays, 1,577 entries, 120 distinct
`canonical_slug`s, 25 arena/category pairs, entry shape `{arena, category, elo,
rank, win_rate}`. And the nine-snapshot history, re-derived by me from
`git log`/`git show` on `data/sources/openrouter-models/latest.json`, is
byte-for-byte the same table the evidence file prints — 2026-08-28 nonEmpty=148
through 2026-09-05 nonEmpty=165, `pairs=25` on every one of the nine.

The 165-vs-243 reconciliation is the substantive finding here and it checks out
against the source of the number: `loops/ui-loop/graph/knowledge/DESK-ORDER-001.md`
line 97 says "`benchmarks.design_arena[]` (arena, category, elo, rank, win_rate)
on 165 rows across 25 arena/category pairs" — the non-empty count and the pair
count, both confirmed. So the directive's "165 feed rows" is not the carrier
count, and recording both is a real correction to a figure that would otherwise
have been re-derived wrong.

## 3. "No value renders" — checked at the mechanism, not only at the output

I could not scan `out/` (this review worktree has no build, and re-running the
build was not the check I was asked to spend the run on). I ran the check that
would be **red** if the claim were false — what the render path actually reads:

```
keys(openrouter-models) = […, material_fields, declined_fields, field_rights_note, field_rights, …]
yields          — 28 paths, none of them benchmarks.design_arena
material_fields — price_input, price_output, context_window, status  (no design_arena)
declined_fields — benchmarks.artificial_analysis only
field_rights    — benchmarks.design_arena | 2026-09-05 | "republication permitted with attribution; …"
```

So the path is in neither list that makes anything render, exactly as claimed,
and the new `field_rights` list is inert by construction. A corpus-wide grep
confirms no content file references `design_arena` at all (the only hits outside
`data/`, `tools/` and this job's own artefacts are the directive, DESK-ORDER-001,
the ui-loop knowledge notes and prior review records). The values do sit in
`data/derived/feed-rows.json` (245 occurrences) — the record says so rather than
claiming they are absent from the repository, and `lib/data-layer.mjs` exposes
that join surface only through declared fact bindings, of which there are none
for this path.

I also confirmed the scan's *reported* non-zero, which is the part a
conclusion-shaped scan would have filtered: `win_rate` appears in exactly two
content files corpus-wide —
`content/wiki/technique/proximal-policy-optimization.md` (`- field: win_rate`,
transcluded at line 109) and `content/wiki/event/alphago-lee-sedol.md`
(`- field: cyclic_adversary_win_rate`) — which is precisely the account the
evidence file gives of the 19 hits in 6 built files. Reporting that hit instead
of suppressing it is what makes the zeros credible.

## 4. Checklist items

- **Evidence of execution, not plausibility**: `data/reviews/evidence/…​.raw.txt`
  carries 17 real request records (12 documents + the 5 legal-path probes that
  404'd — I counted the `==== GET` blocks) with statuses, byte counts and keyword
  tallies, plus the four appended local runs' stdout. The four documents the
  finding quotes are transcribed whole; the rest are marked elided. I reproduced
  the fetches and the counts independently and got the same answers.
- **Dates honest**: `checked_on: 2026-09-05` and every `fetched_on: 2026-09-05`
  match the run (transcript stamped `2026-09-06T05:03:25Z`, local date
  `2026-09-05` — the repository's stated local-date convention).
- **Unexecuted steps disclosed**: yes, and this is the strongest part of the
  record. It states that no API key was applied for, that applying is an
  outward-facing act reserved to the maintainer, that no fetched document
  answers whether the grant reaches a party who never applied, and it names in
  order what would clear the field. Nothing is asserted as settled that was not.
- **Perishables declared**: the ToS is pinned to its "Last Updated: June 7, 2026",
  the docs quotes carry `fetched_on`, the feed measurement names the snapshot and
  its `fetched_at`, and the note says what would make the answer revisitable.
- **Scope**: the diff is the registry row plus its evidence, the scripts that
  produce the evidence, and one proposal (`machinery` type, so not a
  self-amplifying `verify`). Nothing renders, nothing was made material, no
  reserved path was touched. `data/config.json`, `runners.yml`, `openspec/specs/`
  are untouched. That is the stated outcome and not more.

Two small things I am not blocking on are in `carry:` above: the new `tools/`
scripts hardcode the job worktree as their root and will not run after the merge
that keeps them, and `strip-body.mjs` is listed as part of what reproduces the
run although nothing in the transcript invokes it.
