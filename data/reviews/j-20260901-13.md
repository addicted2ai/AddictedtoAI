---
job: j-20260901-13
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a derived-status flip on a feed with no status field of
  its own is still a genuine deprecation and not a registry artefact would link
  this annotation: it is the row-level record that separates "OpenRouter added
  an expiration_date" from "the site's own rule changed its mind", and a reader
  auditing why model/nex-agi-nex-n2-pro started rendering `deprecated` on
  2026-09-01 needs exactly this line to close the question.
carry:
  - title: Qualify "the row's first status flip" as first *recorded* flip
    detail: >-
      The new clause reads "a real lifecycle event: the row's first status flip,
      so it cannot be a metadata correction of a dropped date." Measured, the
      openrouter-models change history in data/changes.jsonl spans three days
      only — 2026-08-29 (29 lines), 2026-08-31 (1), 2026-09-01 (38) — while the
      row's own `created` timestamp (1780937140, ~June 2026) predates that
      window by months. So "the row's first status flip" is true of the recorded
      diff history, not of the row's life, and the absolute "cannot" is one word
      stronger than the evidence. Inserting "recorded" — "the row's first
      recorded status flip" — fixes it without touching the reasoning.
    subject: data/changes.jsonl
---

Free-form notes.

**Scope.** The diff is three things: the one-line rewrite of the `nex-n2-pro`
annotation in `data/changes.jsonl` (l.145), the deletion of
`data/carried/j-20260901-09-carry-1.md` (which the brief requires in the same
diff — leaving it standing re-derives the item on the next Pulse run), and the
loop's own `.job/brief.md` / `.job/source.json`. Nothing else is touched. The
annotation line is byte-identical outside the replaced clause. No
`scope-violation`.

**The substituted clause, checked directly.** Old: "a real lifecycle event,
since OpenRouter serves no status field of its own." New: "a real lifecycle
event: the row's first status flip, so it cannot be a metadata correction of a
dropped date." The new clause is the one that actually discharges the burden —
the old subordinate explained why the status is *derived*, not why the flip is
*real*. This is the fix the carried finding asked for, and it does not borrow
the sibling annotation's wording verbatim.

**Counted the status flips rather than trusting the brief.**

```
$ grep -c 'nex-n2-pro' data/changes.jsonl
2
```
(l.114 the `field_change`, l.145 the annotation — no third line.)

```
$ grep -o '"key":"openrouter-models[^"]*status"' data/changes.jsonl
"key":"openrouter-models|a56a5acd5698a11a|bde830d7800a4672|z-ai/glm-4.5v|status"
"key":"openrouter-models|7ddcd368c2b6d52e|13514954c61b0211|moonshotai/kimi-k2.5|status"
"key":"openrouter-models|13514954c61b0211|a9e815e403b4d7a5|nex-agi/nex-n2-mini|status"
"key":"openrouter-models|13514954c61b0211|a9e815e403b4d7a5|nex-agi/nex-n2-pro|status"
```
Four status `field_change` rows on this source, exactly one of them
`nex-n2-pro`. Within the recorded history the flip is the first, as claimed.
(The window that history covers is the carry above.)

**Every other claim on the line, spot-checked against the snapshots.**

- *"gained an `expiration_date` of `2026-09-04` that it did not carry in the
  2026-08-31 snapshot"* — read both snapshot files directly:
  `previous 2026-08-31 exp= null` / `latest 2026-09-01 exp= 2026-09-04`,
  same `created=1780937140` in both, so it is the same row. Confirmed.
- *"now derives `deprecated` where it previously derived `active`"* — l.114's
  `field_change` carries `"old":"active","new":"deprecated"` on
  `field:"status"`. Confirmed.
- *"renders `deprecated` on its built page through the feed-bound `$status`
  fact"* — `data/derived/feed-rows.json` carries
  `"nex-agi/nex-n2-pro":{"$as_of":"2026-09-01","$status":"deprecated","$vanished":false,…}`,
  and `content/wiki/model/nex-agi-nex-n2-pro.md` binds
  `field: status / source: feed / path: $status`. Confirmed. (The stub's own
  front-matter `status: active` is the entry-level maintenance field, not the
  rendered fact — it is not what the sentence claims.)
- *"its timeline carries the mechanically appended `deprecated` event dated
  2026-09-01"* — the stub's l.33:
  `timeline: [ { date: "2026-09-01", event: deprecated, source_url: https://openrouter.ai/api/v1/models } ]`.
  Confirmed.
- *"the same rule derives `retired` once 2026-09-04 passes"* — consistent with
  the registry's expiration-date rule as the sibling annotation states it; not
  independently re-derived, and it is unchanged text from the approved
  j-20260901-09 line.

**Not re-run:** `npm test` and `npm run build`, which the loop recorded PASS on
`a818eef662e0`, the commit this diff was computed from. The claims in this diff
are about recorded data, and the checks above are the ones that would have gone
red had they been false.

No proposal.
