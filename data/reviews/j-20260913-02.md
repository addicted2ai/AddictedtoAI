---
job: j-20260913-02
verdict: approve
would-cite: >-
  Someone reconciling why a retired batch-model page turned active again would link this entry:
  it re-binds the live OpenRouter row and keeps the 2026-09-01 removal beside the 2026-09-10
  return, so the status flip reads as a catalog relisting rather than a new model.
subject: "content/wiki/model/mistralai-codestral-2508-batch.md"
reviewed:
  "content/wiki/model/mistralai-codestral-2508-batch.md": "30ab82d40f84975317fd2ddcb6fc4d2ab5afea69c91f6d0d4de158ce1dfba597"
---

## Verdict

`approve`. The diff implements the queue item's prescribed repair for the same-subject case:
the entry keeps the `openrouter-models: mistralai/codestral-2508:batch` binding, restores the
canonical feed-backed facts, flips `retired/dormant` back to `active/living`, and records the
return as a dated timeline event. No blog post is touched, so no voice question arises.

## What I checked

- `data/sources/openrouter-models/latest.json` line 16129 holds the
  `"mistralai/codestral-2508:batch"` row with `"name": "Mistral: Codestral 2508 (batch)"`
  (line 16202), `"pricing": {"completion": "0.00000045", "prompt": "0.00000015"}` (lines
  16204-16207) and `"context_length": 256000` via `top_provider` (line 16225) — the row is
  live, and the entry's display name matches it exactly, so the same-subject branch applies.
- `data/changes.jsonl` line 206 records the `2026-09-10` `$arrival` for
  `row_id "mistralai/codestral-2508:batch"` with excerpt `$status "active"`, and line 108
  records the `2026-09-01` `$retirement` — supporting both timeline rows and the
  `retired -> active` flip. The two findings share one instrument (the OpenRouter snapshot
  derivation behind `latest.json` and `changes.jsonl`), so I record them as agreement, not
  independent corroboration.
- Facts/paths (`pricing.prompt`, `pricing.completion`, `context_length`, `$status`, all
  `volatility: fast`) are byte-identical in shape to the sibling live batch entry
  `content/wiki/model/mistralai-mistral-medium-3-5-batch.md` and the non-batch counterpart
  `content/wiki/model/mistralai-codestral-2508.md` — the canonical binding, not an invention.
- Only one content file declares this row id, so there is no genuine two-subject collision to
  disambiguate. `status: active` / `maintenance: living` matches the sibling active batch
  entry convention.
- Derived hunks (`backlinks.json`, `search-index.json`) are mechanical rebuild output: the
  `s: retired -> active` flip for this entry is the status change itself, and the
  weathernext/inception additions reference content (`content/blog/weathernext-3-operational-weatherbench.md`,
  `content/wiki/model/inception-mercury-2-5-preview.md`) that already exists on this branch
  from earlier jobs — stale derived state refreshed by the build, not a scope widening.
  (Prior job `j-20260913-01` drops derived state before merge in any case.)
- Loop-reported gates on commit `063eb46` (`npm run build` PASS,
  `node scripts/verify-surfaces.mjs` PASS) were taken as stated and not re-run, per the brief.
- Note on provenance (not blocking): the `feeds:` line itself predates the author commit — it
  was added in `bf0ebf1` ("proving arrangement (O8, council-adjudicated)" orchestrator pre-edit),
  so the author diff under review keeps rather than adds it. The merged end state still satisfies
  the queue item's "restore its feeds: binding" branch, and the author's own additions (facts,
  status flip, return timeline row) are the remainder of that same repair, so this is recorded
  as observed, not as a scope or authorship objection.
