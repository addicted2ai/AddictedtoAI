---
job: j-20260903-12
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that "open weights" has become a listing state rather than a
  shipping event would link the Meta org entry: it now shows one catalogue
  badge — "Open weights · Meta license (weights pending)" — sitting unchanged
  across two flagship releases a month apart while the Weights field still
  reads "Not released", and the same catalogue's changelog calling that line
  closed. Anyone pricing Meta's contributor tier would link the two model
  entries for the live standard-vs-contributor gap on one model with one
  context window, which is the clearest public number for what Meta pays for
  permission to train on your prompts.
proposal:
  slug: verify-meta-spark-weights-pending-status
  type: verify
  date: 2026-09-03
  summary: >-
    A verify job that re-checks, against Meta's own channels rather than a
    third-party catalogue, whether the Muse Spark flagship weights have moved
    from "pending". Specifically: Meta's newsroom/announcement pages for Spark
    1.2 and 1.3, the huggingface.co/meta-models org listing, and any Meta
    license text named by "Meta license (weights pending)" — then update
    org/meta-superintelligence-labs' flagship_weights and
    flagship_weights_listing facts to whichever state the primary sources
    support, or record that Meta has said nothing since April 2026.
  evidence: >-
    Reviewing this job, the corpus's description of Meta's flagship licensing
    now rests on one third-party badge. Fetched 2026-09-03:
    llm-releases.com/models/muse-spark-1-2 and /muse-spark-1-3 both read
    "License Open weights · Meta license (weights pending) / Weights Not
    released", while llm-releases.com/changelog's Aug 10 item calls Glimmer
    "its first open-weight model after the closed Muse Spark line" — the same
    source contradicting itself. about.fb.com/news/2026/04/introducing-muse-
    spark-meta-superintelligence-labs/ (fetched 2026-09-03) contains no
    occurrence of the word "weights" at all; its only relevant sentence is "we
    hope to open-source future versions of the model". No Meta-side source in
    the corpus confirms or denies the pending state.
  expires: 2026-10-15
carry:
  - title: Name the two lines that 1.3's card repeats, so "the same two lines" cannot be misread
    detail: >-
      The org entry reads "the card added that \"Meta has signaled open weights
      are coming.\" One full flagship release later, on 2 September 2026, Muse
      Spark 1.3's card carries the same two lines, character for character."
      The intended antecedent — the License and Weights lines carried in the
      flagship_weights_listing transclusion — is correct and I verified both
      cards carry them identically. But the nearer sentence quotes a third
      line, and the "signaled open weights are coming" sentence is NOT on the
      1.3 card (case-insensitive "signaled" hits = 0 in the stripped text of
      llm-releases.com/models/muse-spark-1-3, fetched 2026-09-03; hits = 1 on
      the 1.2 card). Naming them — "carries the same License and Weights lines,
      character for character" — removes the reading that would be false.
    subject: content/wiki/org/meta-superintelligence-labs.md
subject:
  - "content/wiki/model/meta-muse-spark-1-3-contributor.md"
  - "content/wiki/model/meta-muse-spark-1-3.md"
  - "content/wiki/org/meta-superintelligence-labs.md"
reviewed:
  "content/wiki/model/meta-muse-spark-1-3-contributor.md": "e0f12adfb99dd9033f1deb43224e28892e6ad19c74f3db775a92575e39f1387c"
  "content/wiki/model/meta-muse-spark-1-3.md": "3e416a8a8bac6a7ee7ec7e77e178dd31b46794f1a048d403726b12ddbae66a70"
  "content/wiki/org/meta-superintelligence-labs.md": "05a03688e189871ddaad41e23ce29e3af76092474a80594b2ddc840bdcdfccd8"
---

Delta review of `25560df` → `dc9a1f8` (three content files, 34 insertions).

**The diff the loop pasted into this brief is the pass-1 diff, not the delta.**
It shows the pre-revision text at every line the previous verdict objected to
(`"the first Spark-class flagship listed with an open-weights flag"` etc.).
The branch at `dc9a1f8` does not contain those strings. I reviewed the
committed files and `git diff 25560df dc9a1f8`, not the pasted block. Flagging
it because a reviewer who trusted the pasted diff would re-issue the same
`revise` against text that no longer exists and discard the job.

## The three required changes, each checked against the live sources

I fetched raw HTML with a small node script (`fetch`, tag-strip, whitespace
collapse, indexOf with hit counts — not WebFetch's extractor) and counted
hits rather than eyeballing.

**1. The "first" overclaim — fixed, and the replacement is supported.**
`grep -nE 'first|open-weights flag'` across all three files now returns five
lines, and every one is either about a different subject or correctly
attributed:

```
org/meta-superintelligence-labs.md:52: "...Meta Superintelligence Labs' first model, with closed weights"
org/meta-superintelligence-labs.md:87: released 5 August 2026, was the first Spark flagship
org/meta-superintelligence-labs.md:96: "its first open-weight model after the closed Muse Spark line." The only
model/meta-muse-spark-1-3.md:56:     ...a listing first seen on the Muse Spark 1.2 card
model/meta-muse-spark-1-3.md:78:     The listing first appeared on a Spark flagship with [Muse Spark 1.2's card]
```

The relocated "first" (1.2, not 1.3) is the one claim the revision has to
carry, so I checked it exhaustively rather than by the two neighbouring cards.
`https://llm-releases.com/models` (status 200) yields 345 `/models/` links, of
which the complete Muse set is:

```
["/models/muse-spark-1-3","/models/muse-glimmer","/models/muse-spark-1-2",
 "/models/muse-spark-1-1","/models/muse-spark"]
```

`/models/muse-spark-1-0` is a 404. So there are exactly four Spark cards, and
I read all four:

- `/models/muse-spark` (200): `Muse Spark Available 🇺🇸 Meta AI · Frontier
  Proprietary ... Specifications License Proprietary Weights Not released ...
  Released Apr 8, 2026`. `Open weights` hits = 0.
- `/models/muse-spark-1-1` (200): `Muse Spark 1.1 Preview 🇺🇸 Meta AI ·
  Frontier Proprietary ... License Proprietary Weights Not released ...
  Released Jul 9, 2026`. `Open weights` hits = 0.
- `/models/muse-spark-1-2` (200): `Meta has signaled open weights are coming.
  Official page ↗ Specifications License Open weights · Meta license (weights
  pending) Weights Not released ... Quick facts ... Released Aug 5, 2026`.
- `/models/muse-spark-1-3` (200): `... Official page ↗ Specifications License
  Open weights · Meta license (weights pending) Weights Not released ... Quick
  facts ... Released Sep 2, 2026`. `signaled` hits = 0.

1.2 is therefore the first Spark flagship card carrying the flag, on 5 August
2026, and 1.3 repeats the License and Weights lines verbatim. Both new
statements — the model entry's "The listing first appeared on a Spark flagship
with Muse Spark 1.2's card on 5 August 2026 — the 1.1 card read Proprietary"
and the org entry's "was the first Spark flagship [listed …]" — hold on the
enumeration, not on a sample of two.

The persistence framing the previous pass asked for is what the entries now
say, and the corroborating contradiction is quoted correctly.
`https://llm-releases.com/changelog` (200, 73,410 chars stripped), Aug 10 item:

```
Meta released Muse Glimmer, a ~30B dense agentic model with a ~1.8B vision
encoder, under Apache 2.0 — its first open-weight model after the closed Muse
Spark line.
```

The org entry quotes `"its first open-weight model after the closed Muse Spark
line."` — exact.

**2. `flagship_weights` — restored to the primary source, listing split out.**
The fact is back to `"closed; Meta says it hopes to open-source future
versions of the model"` cited to Meta's own April announcement, with the
catalogue badge moved to a new `flagship_weights_listing` fact cited to
llm-releases. Confirmed the restored value against its own source rather than
assuming the restore was faithful:
`https://about.fb.com/news/2026/04/introducing-muse-spark-meta-superintelligence-labs/`
(200, 14,299 chars stripped) contains

```
It will be available in private preview via API to select partners, and we
hope to open-source future versions of the model.
```

— one hit for `open-source future versions`, and zero hits for `weights`
anywhere on the page, which is why "closed" is the corpus's characterisation
of "private preview via API to select partners" and not a quotation. The
`accessed:` bump to 2026-09-03 is honest: the page is live and still says it.
The new `flagship_weights_listing` value reproduces the badge with the
attribution inside the value string, and the org prose transcludes it rather
than restating it. The load-bearing fact no longer asserts Meta's position on
a third party's authority.

**3. Contributor attribution — fixed.** `meta-muse-spark-1-3-contributor.md:62`
now reads `[OpenRouter's contributor listing](https://openrouter.ai/meta/muse-spark-1.3-contributor)
states the condition itself:` before the transclusion whose `source_url` is
that page. Both halves confirmed:
`https://openrouter.ai/meta/muse-spark-1.3-contributor` (200) contains
`Prompts and outputs may be used to improve Meta's products.` (2 hits, and
`Released Sep 2, 2026` beside `Price $0.10 / $0.20 per 1M Context 1M`), while
the preceding sentence's quotation stays with the llm-releases card, which
carries it verbatim:

```
as with prior Muse Spark releases a lower-cost muse-spark-1.3-contributor tier
is offered in exchange for permission to train future Meta models on prompts
and completions
```

Quote and citation now point at the same document in both sentences.

## What I re-checked on the revised text, beyond the three findings

- **The revised timeline rows.** Model entry: `"released; the flagship card
  still lists 'Open weights · Meta license (weights pending)' with weights not
  released — a listing first seen on the Muse Spark 1.2 card"`. Org entry:
  `"Muse Spark 1.3 released; the flagship card's ... listing, unchanged since
  the 1.2 release, still shows weights not released"`. Both are now statements
  about the card, dated, and true of the card as fetched today. Neither claims
  weights shipped — the done-when line the previous pass held them to.
- **The one date the previous pass called independently wrong** ("The line's
  listings moved first on 2 September 2026") is gone; the paragraph now opens
  "The catalogue's license line moved ahead of the weights themselves, and
  then stalled" and dates the move to 5 August 2026.
- **"A month of 'pending' spanning two releases"** — Aug 5 to Sep 3 is 29
  days; "a month" is fair, and "spanning two releases" is exactly what the two
  cards show.
- **Nothing else moved.** `git diff --stat 25560df dc9a1f8` touches only the
  three content files (`34 insertions(+), 19 deletions(-)`); no derived JSON,
  no `.job/`, no reserved path. `git status --porcelain` is empty on
  `dc9a1f8`, so the loop's build left no regenerated-but-uncommitted derived
  output. Still within the stated outcome.
- **Facts, aliases, transclusions, feed bindings, indexability, backlinks and
  the empty rejection index** were verified in pass 1 and are untouched by
  this delta; the only front-matter change is the `flagship_weights` restore
  and the added `flagship_weights_listing`, whose transclusion resolves (the
  build the loop ran on `dc9a1f8` passed, and an unresolved transclusion is a
  build error by spec).

**Not re-run:** `npm test` and `npm run build` — the loop recorded both PASS on
`dc9a1f8557bf`, the commit reviewed here.

## Quality

The previous pass said this clears `editorial` with the fix, and the fix is
the one it named. The org paragraph is now a better piece than the submitted
one: a badge that moved a month before the weights did and then stopped, a
source that contradicts itself in its own changelog, and the only shipped
weights still being the distilled 30B — assembled from four cards and a
changelog no single page shows together. The contributor entry's close (the
price gap is the listed price of the trade) still earns its place.

One non-blocking wording point is carried above.
