---
job: j-20260906-11
verdict: approve
reasons: []
would-cite: >-
  Whoever writes the next batch of `entry` directives for the 26 still-uncovered
  catalog providers, and whoever eventually implements a real org-to-catalog
  join. This is the record they will cite to refuse a `feeds` map on an
  `org` entry — not as an opinion about tidiness, but because it names the three
  readers (`pulse/lib/corpus.mjs`, `pulse/lib/mint.mjs`, `lib/changes.mjs`) that
  would act on such a binding without ever asking what kind of entry wrote it,
  and shows what each would do wrong. It is also the thing to link when someone
  reaches for `publishes_from` and gets a red build: it says the key does not
  exist yet, names the two unchecked tasks that would add it, and says what the
  honest interim rendering is.
carry:
  - title: Re-cite the frontier.mjs line numbers in the new README section against the post-merge file
    detail: >-
      Every `lib/render/frontier.mjs` line citation in the new
      "What an `org` entry declares" section was computed against the file as it
      stood BEFORE this same diff inserted its 29-line block comment at line 50,
      so each is short by exactly 29 in the merged tree. Measured 2026-09-06 in
      this worktree at `4ebc33db7002`: `matchProviders` is at 79 (cited
      `:50-61`), `orgOwnDomains` at 346 (cited `:317-326`, with "the test at
      `:323`" now at 352), `isVendorSourced` at 358 (cited `:329-341`),
      `claimRank` at 373 (cited `:344-352`, and "`:340`" is now 369), the
      registrable-domain comment carrying the `blog.google` / `deepmind.google`
      example at 337-344 (cited `:309-315`), and the `renderPlayersBoard`
      no-filter comment at 567 (cited `:535-549`). Five of the six now land on a
      different function than the one named — `:329-341` for `isVendorSourced`
      resolves to `registrable`, `:344-352` for `claimRank` resolves to
      `orgOwnDomains` — which is worse than resolving to nothing in a record
      whose whole purpose is that the next implementer can check it against the
      code. The function names are given alongside every citation, so nothing is
      unfindable; the fix is to add 29 to each of the six frontier.mjs numbers.
      The citations to `pulse/lib/corpus.mjs`, `pulse/lib/mint.mjs`,
      `lib/changes.mjs` and `lib/schema.mjs` are unaffected and all resolve
      correctly, and the block comment inside `frontier.mjs` itself cites no
      line numbers in its own file.
    subject: content/wiki/README.md
proposal:
  slug: declare-which-entry-kinds-a-source-row-may-bind-to
  type: machinery
  date: 2026-09-06
  summary: >-
    This ruling is enforced by a README and by nothing else. `entrySchema`
    (`lib/schema.mjs:210-223`) accepts `feeds` on every kind alike —
    `feeds: z.record(z.string().min(1), z.string().min(1)).optional()`, with no
    kind gate — so an `org` entry declaring `openrouter-models:
    "minimax/minimax-m2"` builds green today, and the three consumers this diff
    enumerates then act on it: no `model/` stub is minted for that row, the
    model's mechanical status events land on the organisation's timeline, and
    the changed feed's entry link goes to whichever of the two claimants the
    loader reached last. The convention this repository states everywhere else
    is "fail the build, don't warn". A `machinery` job would move the constraint
    into the source registry, where the join is already described: let a source
    entry declare which entry `kind`(s) its rows may be bound to (both current
    sources are model-keyed — `openrouter-models` `row_id_field: "id"`,
    `llm-releases` `row_id_field: "guid"`), and fail the build naming the entry,
    the source and the row id when a binding violates it. That keeps the rule
    where a future organisation-keyed source could legitimately relax it,
    instead of hard-coding "only `model` may declare `feeds`".
  evidence: >-
    Read in this worktree at commit 4ebc33db7002 on 2026-09-06:
    `lib/schema.mjs` — `entrySchema` is `.strict()` and its `feeds` key is an
    unconstrained optional string record with no reference to `kind`;
    `data/sources/registry.json:10` and `:158` — the two registered sources'
    `row_id_field` values are `id` and `guid`; `pulse/lib/corpus.mjs:219-235`
    (`feedBindings` iterates `corpus.entries` whole and reads `e.feeds` with no
    kind test), `pulse/lib/mint.mjs:139-142` (`if (declared.has(rowId))
    continue;`) and `:258-263` (`byRow.set(`${source} ${rowId}`, e)` over every
    entry), and `lib/changes.mjs:60-67` (`index.set(`${source}|${rowId}`, doc)`,
    last write wins). The diff under review establishes that all three would
    misbehave on an org-declared `feeds` binding; what it cannot do, being an
    `interpret` job, is make the build refuse one. Measured the same day: 0 of
    the 24 files in `content/wiki/org/` contains the string `feeds`, so this is
    a gate to add before the defect occurs, not a repair of one that has.
subject: "content/wiki/README.md"
reviewed:
  "content/wiki/README.md": "ebb2135a645f1aa36812c4a4ed2176fc82e7a07d4622ef5f544a87405c452470"
---

Free-form notes.

The diff is comment-and-record only: `lib/render/frontier.mjs` gains a block
comment and no executable line, `content/wiki/README.md` gains one section, and
`DIRECTIVES.md` gains a bracketed correction on eight lines. `npm test` and
`npm run build` were run by the loop on this commit and passed; I did not
re-run them. Everything below is a targeted check of a specific claim.

**The central claim — the board's org join is the alias join, not `feeds`.**
Confirmed twice over.

`matchProviders` (now `lib/render/frontier.mjs:79-89`) reads:

```
  const names = [org.data.display_name, ...(org.data.aliases ?? []).map((a) => a.name)]
    .map(norm)
    .filter((n) => n.length >= 3);
  const providers = new Set();
  for (const row of catalogRawRows) {
    const p = norm(row.provider);
    if (!p) continue;
    if (names.some((n) => n === p || n.includes(p) || p.includes(n))) providers.add(row.provider);
  }
```

Display name plus aliases, normalised, containment either way, minimum three
characters — exactly as the record describes, and `feeds` appears nowhere in it.

The stronger half of the claim is the negative one, so I ran the enumerating
check rather than reading around: `grep -n feeds lib/render/frontier.mjs`
returns nine lines, of which seven are inside the comment this diff adds, one is
line 644 (`el('caption', {}, 'Organisations tracked by this site and their
newest listed model, as the tracked feeds state it today')`) and one is line 697
(prose inside another block comment, "whatever the tracked feeds"). Zero data
reads. The record's "The string occurs in this file only in the board's table
caption and in comments" is exact.

**"No org entry could carry one honestly."** `data/sources/registry.json` line
10 gives `openrouter-models` `"row_id_field": "id"` and line 158 gives
`llm-releases` `"row_id_field": "guid"` — a model slug and one release, neither
an organisation. Confirmed.

The three-reader table is the part of the record a reader is most likely to take
on trust, so I read all three:

- `pulse/lib/corpus.mjs:219-235` — `feedBindings` loops `for (const e of
  corpus.entries)` and `Object.entries(e.feeds)`, with no `kind` test anywhere;
  `declaredRowIds` is built from it. Claim holds.
- `pulse/lib/mint.mjs:141-142` — `const declared = declaredRowIds(corpus,
  source.id);` then `if (declared.has(rowId)) continue;`. An org-declared row id
  would indeed suppress the `model/` stub. Claim holds.
- `pulse/lib/mint.mjs:258-263` — `appendTimelineEvents` builds `byRow` from
  every entry's `feeds`, `byRow.set(`${source} ${rowId}`, e)`. Claim holds. (Its
  key separator is a space, not the `|` the record quotes — but the record
  quotes `|` for `lib/changes.mjs`, which is where `|` actually is, so nothing
  is misattributed.)
- `lib/changes.mjs:60-67` — `index.set(`${source}|${rowId}`, doc)` inside a loop
  over entries: one Map, last write wins, silently. Claim holds, separator and
  all.

**"none of the 24 files in `content/wiki/org/` contains the string `feeds`".**
Counted rather than sampled: `ls content/wiki/org/ | wc -l` → `24`, and
`grep -rlc feeds content/wiki/org/ | wc -l` → `0`. Exact on both halves.

**The brand-domain half.** `orgOwnDomains` (now `:346-355`) ends with
`if (reg && reg.label.length >= 3 && tokens.has(reg.label)) domains.add(reg.domain);`
and `isVendorSourced` (now `:358-370`) ends with
`return reg.label.length >= 3 && orgNameTokens(org).has(reg.label);`. So the
record's sharpest claim — that *both* halves are gated on the org's own name
tokens, and therefore that no entry-side declaration can admit a host whose
registrable label is not a name token — is true of the code as written, not
inferred from intent. `claimRank` (now `:373-381`) does drop the fact:
`if (!isVendorSourced(fact, org)) return 0;`. The `blog.google` → `blog` and
`deepmind.google` → `deepmind` example is the file's own, quoted from the
comments above `orgOwnDomains` and inside `isVendorSourced`.

The MiniMax instance: `content/wiki/org/minimax.md` declares `display_name:
MiniMax` and aliases `MiniMax`, `MiniMax Group Inc.`, `稀宇科技`, so the name
tokens include `minimax`. `minimax.io` reduces to label `minimax` and passes;
`hailuoai.video` → `hailuoai` and `talkie-ai.com` → `talkie-ai` are in no token
set and cannot pass by either branch. The record states this as a consequence of
the code and attributes the "these are MiniMax's product brands" half to the
proposal rather than asserting it itself, which is the right division — the
entry file mentions neither domain (`grep hailuoai\|talkie content/wiki/org/minimax.md`
returns nothing).

**"`publishes_from` … does not exist".** `entrySchema` (`lib/schema.mjs:209-223`)
lists `id, kind, display_name, aliases, status, maintenance, feeds, facts,
timeline, mentions, themes` and closes `.strict()`. `grep -rn publishes_from lib/`
returns nothing. So an entry declaring it fails on an unknown key, as stated.
`openspec/changes/separate-a-claim-from-a-fact/tasks.md` lines 75, 78, 83, 92
and 95 are tasks 12–16 and all five render `- [ ]`; task 12's own text is
"`lib/schema.mjs`, `entrySchema` (line 209): `publishes_from`, optional", which
independently corroborates both the line number and that the key is unlanded.

**"the section above repeats the first of them in passing".** `content/wiki/README.md`
line 35 does carry the phrase "an org entry with a feeds map", quoted from
`addictedtoai-2ok0` — so the record's self-correction of the file it is being
added to is accurate rather than rhetorical.

**Completeness and scope.** `grep -c "WHAT THE ENTRY MUST CARRY OR THE ROW IS
BLANK" DIRECTIVES.md` → `8`; `grep -c "corrected 2026-09-06 j-20260906-11"` →
`8`; the count of lines carrying the sentence without a correction is `0`. Every
instance is annotated and none was missed. Worth recording for whoever reads
this next: all eight of those lines now carry `[done …]` markers, so the
proposal's premise that "seven of them still unrun" would be corrected in flight
had aged out before this job ran. The diff does not claim otherwise — the
appended text is scoped to "the directives still to be written for the uncovered
providers", which is the honest and still-live target (26 of them). The three
edited paths are the two homes the outcome names (directive text, frontier.mjs
block comment) plus the record they both point at; nothing else is touched, and
no executable line changes.

**Duplicate check.** The rejection index is empty. Reading the section
immediately above in `content/wiki/README.md`, the ruling from `j-20260905-25`
answers a different question — which of the 34 uncovered providers are
organisations at all — and explicitly leaves the mechanism question open. This
is not a re-tread of it.

One finding carried, not blocked on: the six `lib/render/frontier.mjs` line
citations inside the new README section were computed against the pre-insertion
file and are each short by the 29 lines this diff itself adds. Detail and the
measured line numbers are in the front matter above.
