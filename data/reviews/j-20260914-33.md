---
job: j-20260914-33
verdict: approve
reasons: []
would-cite: >-
  Whoever next declares a `companion` on openrouter-models, or reopens
  addictedtoai-8ho/ak9, would link this comment in the argument that the
  changed feed currently carries no price event of any kind, and that a
  vendor-bound price line would still be dropped from interpretation by
  `isNonEventField` unless the headline flag and the vendor field are
  separated first.
carry:
  - title: Recount the "14 later snapshots" figure in the diff.mjs finding
    detail: >-
      pulse/lib/diff.mjs says kimi-k2.5 "held that value in all 14 later
      snapshots". Reading every committed openrouter-models/latest.json by
      `date` gives 2026-09-01..09-12 and 2026-09-14 after 2026-08-31, which
      is 13 distinct dates (no 09-13), or 14 counting 08-31 itself. The
      value did hold in every one of them. Say "13 later snapshots", or
      "every snapshot from 2026-08-31 to 2026-09-14", whichever the author's
      instrument actually counted.
    subject: pulse/lib/diff.mjs
---

## Scope

The outcome was to investigate the effect of `event: false` on both OpenRouter
price fields and record the finding either way. The diff is comment-only in
`pulse/lib/diff.mjs`. It adds a finding block under the `event: false`
rationale and corrects a paragraph in `uninterpretedChanges`. That paragraph
predicted ak9 would clear the flag, which is stale. Both hunks record the
finding, and neither touches content or reserved paths. `git -C D:/AddictedtoAI
diff f05ffbb -- pulse/lib/diff.mjs` shows that main's copy differs from the
branch only in these comment lines, so behaviour is unchanged. There is no
polarity problem: the job asked for a record and the diff is one.

## What I ran and observed

The review worktree has no `node_modules`, and I did not create one. I imported
`diff.mjs` from the main checkout, where the code is identical per the diff
above. I read `registry.json` and the git history from this branch. The script
was `%TEMP%/rev33/check.mjs`.

- **"No source in the registry declares [a companion]"**: confirmed. Grep for
  `companion` in `data/sources/registry.json` found 0 matches. The script
  printed `sources key true companions 0`.
- **"The 16 price lines ... are all dated 2026-08-29"**: confirmed. Grep for
  `"field":"price_` in `data/changes.jsonl` matched 16 lines (64-67, 69-70,
  73-74, 78-79, 81-84, 88-89), and every one carries `"date":"2026-08-29"`.
- **The trap claim**: confirmed. I wrote one vendor-bound `price_input` line on
  `openrouter-models`, dated today, into a temp `changes.jsonl`. The script
  printed `isNonEventField true` and `with registry 0 without 1`. The fixture
  at `pulse/tests/diff.test.mjs:543` and `:557` does declare the headline
  `event: false` and names the companion rate `price_input`, as the comment
  says. `vendorPriceChanges` (`diff.mjs:505-520`) emits `field` taken from
  those rate names, under `source: source.id`. One nuance: "the SAME field"
  holds only if the first real companion names its rates the way the fixture
  does. That is the natural declaration, so it is fair to call a trap.
- **The kimi-k2.5 history**: confirmed. The script printed, per snapshot date:
  2026-08-28/29/30 `0.0000006 0.000003`, then 2026-08-31 through 2026-09-14
  `0.00000045 0.00000225` in every snapshot. That is exactly a 25% cut on both
  fields, and the price did not oscillate. The count of later snapshots looks
  off by one; see `carry`.
- **Not replayed by me**: the "285 price lines would have emitted" figure. It
  is not load-bearing for the finding. The load-bearing fact is that 0 price
  lines were emitted after 2026-08-29, and the grep above measured that
  directly.

## Judgement

The finding is honest about what the snapshots can show: "cannot prove a vendor
cut; they can only show it did not oscillate". Its main claims were measured,
and I reproduced them. It also corrects a stale forward-looking paragraph that
would otherwise mislead the person who declares the first companion. It stays
in scope.
