---
job: j-20260914-26
verdict: approve
reasons: []
would-cite: >-
  Someone choosing a browser inference runtime who needs to pin a current
  onnxruntime-web version would rely on this entry's latest_release fact. The
  re-verification lets them trust that it is current as of 2026-09-14 and not
  a two-week-old reading.
subject: "content/wiki/tool/onnx-runtime-web.md"
reviewed:
  "content/wiki/tool/onnx-runtime-web.md": "36eb10c3540c76c079a21e7bf8f8fa3a4aefd705f8aa98ad7ed22aa70daa67c6"
---

## What was checked

**Scope.** The loop's diff touches exactly one content line,
`content/wiki/tool/onnx-runtime-web.md` `latest_release.accessed`
`"2026-08-28"` → `"2026-09-14"`. The value `"1.29.0"`, `source_url` and
`volatility` are unchanged. The other files are the evidence pair under
`data/reviews/evidence/` plus job scaffolding under `.job/`. That includes the
author's fetch script `.job/fetch-ort.mjs`, which is the instrument behind the
raw transcript. `license` and `repository` were not touched. There is no scope
violation and nothing is inverted: the brief asked for a re-verification with
the stamp moved to the real date, and that is what the diff does.

**Independent re-fetch (a different run, same source).** At
Mon Sep 14 2026 12:22:03 GMT-0600 I ran a separate node fetch:

```
200 {"extensions":"1.9.0-extensions","latest":"1.29.0","dev":"1.31.0-dev.20260914-8d85527a0"}
onnxruntime-web@1.29.0 1.29.0
```

This matches the author's raw transcript byte for byte on the dist-tags body,
and on `_id` and `version` from the cited `source_url`
(`https://registry.npmjs.org/onnxruntime-web/latest`). The author's run was
stamped 12:19:30 MDT with server `date: Mon, 14 Sep 2026 18:19:30 GMT`. That is
the same instant, and 2026-09-14 is the correct local date. My fetch hit the
same registry endpoint as the author's, so it counts as agreement from a
repeated measurement rather than independent-instrument corroboration. For a
registry dist-tag, the registry is the authority, and no second instrument
would add anything.

**Evidence honesty.** The narrative correctly says the `dev` nightly is not the
`latest` release, and it discloses that the static facts were left alone. The
raw transcript is present and unabridged.

**Gates.** Not re-run, per the brief. The loop recorded a PASS for build and
verify-surfaces on 09dc643.

A note on method: a plain `git diff main --stat` from this worktree shows about
60 files. That is because `main` has advanced past the branch point; those
files are not this branch's work. The loop's merge-base diff is the one judged
here.
