---
job: j-20260904-45
verdict: approve
reasons: []
would-cite: >-
  Nobody links this file — it is a docket annotation, not a published page. It
  earns its place by settling a specific dispute a later entry job would
  otherwise have had to re-litigate: whether "+7.5-9.7% recall" and the "more
  tokens at higher effort levels" sentence are really on the 9to5Google launch
  write-up. Someone drafting the Gemini 3.8 Flash Cyber entry, or auditing why
  the wiki quotes Google's blog rather than 9to5Google for those two lines,
  would read it and stop worrying. Its wider value is as a worked instance of
  the house rule it invokes: an extractor's silence is not absence, and the
  disproof is raw bytes.
carry:
  - title: Link the Gemini 3.8 launch post where the org entry quotes it
    detail: >-
      `content/wiki/org/google-deepmind.md` quotes Google directly — 'the launch
      post explains that "3.8 Flash works harder" ... and that "at times, the
      model might use more tokens to maximize performance, especially at higher
      effort levels"' (lines 93-96) — and again at line 104, "Google's launch
      post calls it introductory pricing". Neither mention is hyperlinked, and
      the only 3.8-era link anywhere near them is
      [9to5Google](https://9to5google.com/2026/09/02/gemini-3-8-flash-launch/)
      at line 68. The two `blog.google` URLs in the file (lines 36, 48) are the
      *3.7* Flash post, a different document. Both quotes are verbatim in
      https://blog.google/innovation-and-ai/models-and-research/gemini-models/3-8-flash-and-3-8-flash-cyber/
      (verified from raw bytes for this review), so nothing here is wrong — a
      reader just cannot get from the quote to the document. Fix: link that URL
      on "the launch post" at line 93, as
      `content/wiki/model/google-gemini-3-8-flash.md` already does for the same
      launch.
    subject: content/wiki/org/google-deepmind.md
---

Free-form notes.

## What the diff claims

Two things the carried finding (`data/carried/j-20260903-09-carry-1.md`) could
not reproduce through WebFetch are, per this diff, present verbatim in the raw
HTML of both 9to5Google's write-up and Google's own launch post — so no prose
in the corpus needs correcting, and the carried file is retired in the same
diff. Nothing in the content corpus is touched.

## What I ran

A throwaway node script (`.rev-fetch.mjs`, deleted after the run — this
worktree is unchanged) that fetches each URL, keeps the response as a Buffer,
and does plain `indexOf` on the decoded bytes. No extractor anywhere in the
path, which is the whole point of the check.

Both pages returned HTTP 200 with no redirect (`finalUrl` equals the requested
URL in both cases).

`https://9to5google.com/2026/09/02/gemini-3-8-flash-launch/` — 201,843 bytes:

```
HIT  @151366  "+7.5-9.7%"
HIT  @151376  "higher recall on their internal penetration testing benchmark"
HIT  @151444  "2.3-5.2x lower cost"
HIT  @146339  "At times, the model might use more tokens to maximize performance"
HIT  @146406  "especially at higher effort levels"
HIT  @146246  "exhibits greater diligence"
HIT  @146312  "calling tools iteratively"
```

Context printed around each hit, whitespace-collapsed:

> `<li>Wiz found that Gemini 3.8 Flash Cyber achieves +7.5-9.7% higher recall
> on their internal penetration testing benchmark for a 2.3-5.2x lower cost
> compared to other leading frontier models.</li>`

> `<p ...>Google says these "performance gains stem from a core design choice:
> 3.8 Flash works harder."</p> <blockquote ...><p ...>On complex tasks, it
> exhibits greater diligence — executing extra reasoning steps, and calling
> tools iteratively. At times, the model might use more tokens to maximize
> performance, especially at higher effort levels.</p></blockquote>`

`https://blog.google/innovation-and-ai/models-and-research/gemini-models/3-8-flash-and-3-8-flash-cyber/`
— 406,618 bytes: the same seven fragments all HIT, the Wiz sentence identical
inside `<li data-block-key="1kg3h">`, and the diligence/more-tokens sentences
in one `<p data-block-key="0vkqt">` that opens "These performance gains stem
from a core design choice: 3.8 Flash works harder."

So the diff's two load-bearing claims are confirmed independently of the
author, from bytes, on both documents. The diff's further claim that the two
readings "are not rivals: they are consecutive sentences of one blockquote" is
also exactly what the markup shows — the earlier reviewer's extractor returned
the first sentence and dropped the second, and dropped the percentage range
from the Wiz line. The finding was a flag, not an error, and it resolves the
way the finding itself said it might.

The byte counts the diff records (201,798 and 406,118) differ from mine by 45
and 500 bytes. Both pages carry ad and analytics markup that varies per
response; the fragments under test are in the article body and identical. Not
a defect and not worth a note in the corpus.

## The rest of the diff

- `data/carried/j-20260903-09-carry-1.md` deleted. That is the documented way
  to retire the item — the brief and the file's own "Retiring this item"
  section both say the file's presence is what puts it in the derived queue.
  Grepped the tree for `j-20260903-09-carry-1`: no remaining reference outside
  the annotation text itself, so the deletion leaves no dangling pointer.
- The annotation was appended to
  `data/proposals/consumed/gemini-3-8-flash.consumed-20260903T193127.md`. The
  brief names `data/proposals/gemini-3-8-flash.md` as the target; that path no
  longer exists, having been retired into `consumed/` on 2026-09-03. Same
  docket, correct file, and appending to a consumed record cannot re-arm it for
  selection.
- The diff's claim about where the corpus cites these lines holds, with one
  wrinkle worth recording. `content/wiki/model/google-gemini-3-8-flash.md`
  attributes the Wiz recall figure explicitly and by link: "Google's claims for
  it, all vendor-reported, are in the [launch post](https://blog.google/...
  /3-8-flash-and-3-8-flash-cyber/) ... Wiz measured +7.5-9.7% higher recall on
  its internal penetration-testing benchmark at 2.3-5.2x lower cost". That is
  exactly right against the bytes.
  `content/wiki/org/google-deepmind.md` does carry the "works harder" and "more
  tokens" quotes, and attributes them in prose to "the launch post" — but with
  no link on either mention, and the only 3.8-era link in that file is the
  9to5Google one. Since both documents carry the sentence verbatim, no
  attribution in the corpus is false and "no prose changed" remains the right
  outcome; the missing link is a separate small fix, carried above rather than
  blocked on. Fixing it would have been outside this job's stated outcome
  anyway.
- `.job/brief.md` and `.job/source.json` are the loop's own scaffolding, not
  authored content; the loop strips them at merge (see `71075c5`). Not counted
  against scope.

Nothing here exceeds the stated outcome: one file retired, one record
annotated, zero content edits. `npm test` and `npm run build` were run by the
loop on this commit and passed; I did not re-run them, and nothing in this diff
could plausibly move either.

## Not blocking

The worktree root has no `RESULT.md`. The loop evidently accepted the run
anyway and computed this diff, so I assume it consumes and removes that file
rather than committing it; recording the observation in case that assumption is
wrong for someone reading later.
