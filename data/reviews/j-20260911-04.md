---
job: j-20260911-04
verdict: approve
would-cite: >-
  A curriculum maintainer deciding whether a pointer sentence needs a declared
  prerequisite edge would cite this as the clean case: the one-line link that
  names where a subject lives without importing what it teaches, plus the
  explicitly recorded decision to leave a page standing on its own.
subject: "content/learn/who-builds-ai.md"
reviewed:
  "content/learn/who-builds-ai.md": "868934460a7a864b469d6a56c941d6298023e2927e26b79b999e479d060e3bc5"
---

Notes: what I checked, ran, and observed.

Diff under review (commit 7fab0b7): one prose hunk — `content/learn/who-builds-ai.md:48` changes "costs is a subject of its own." to "costs is [a subject of its own](/learn/what-it-costs-to-build-and-run-ai)." — plus standard `.job/` scaffolding. No blog post in the diff, so no voice question arises.

1. Target existence (brief: "confirm that on disk before acting"). Ran:
   `Test-Path content/learn/what-it-costs-to-build-and-run-ai.md` → True.
   `Test-Path content/learn/what-a-neural-network-is.md` → True.
   So the link target exists; the added link is exactly the edit class that can fail the build, and the loop's own gates on this same commit are `npm run build` PASS and `node scripts/verify-surfaces.mjs` PASS. I did not re-run the suite per the brief; the gates plus the on-disk existence check are two different instruments agreeing the link resolves, which I record as corroboration, not mere agreement.

2. Instance (1), why-bigger-got-better: no diff touches it, and that is correct rather than a gap. The brief describes its aside as carrying no link, but on disk line 134 already reads `[a subject of its own](/learn/what-it-costs-to-build-and-run-ai)`, and blame attributes line 134 to commit 79466a8e of 2026-08-30 — i.e. resolved before this job existed. There was nothing to add, and adding a second link would have been the scope error, not the fix.

3. Instance (2), looking-inside-a-model prerequisite judgment: no diff touches it either, and I independently reach the keep-standing answer. The page's prerequisites are `how-a-language-model-works` and `what-safety-training-changes`; its opening establishes the unreadable-numbers premise itself from the weights-file observation, and its neuron/superposition passages teach what they need inline rather than leaning on `what-a-neural-network-is`. Declaring the edge would be a real curriculum-graph change with no paying need, and the brief itself names standing alone as defensible. The author's commit message records the decision ("record looking-inside prerequisite decision"); the diff correctly carries no file change for a no-change decision.

4. Surface sweep for siblings: grepped `content/learn` for `subject of its own|its own page|beyond this page|own subject` — 8 matches, and after this diff every one of them already carries a link (how-a-model-uses-your-documents:166, how-models-are-trained:198, machines-that-act-in-the-world:157, running-a-model-yourself:93, what-an-agent-is:32, what-models-are-trained-on:129, who-builds-ai:48, why-bigger-got-better:134). who-builds-ai was the last unlinked instance of the shape, so the author's single addition is both defensible and complete; nothing was left on the table that I can defend adding.

5. Prerequisite rule (pointer vs lean): the added sentence tells the reader the bet's cost has its own page; it imports none of that page's content, so no new edge was needed on who-builds-ai (prerequisites still just `what-ai-actually-is`). No spec-violation.

6. Polarity and scope: the diff implements the brief's "add any you can defend" clause and inverts nothing — it adds what should be added, touches only the repair, and rewrites no approved prose beyond the link markup. Not a scope-violation.

No proposal surfaced; nothing to carry.
