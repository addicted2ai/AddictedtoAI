---
job: j-20260901-22
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a model's "retirement" is not one event but several
  that disagree — vendor endpoint, router listing, downloadable weights —
  would link this K3 paragraph as the compact case: the successor's listing
  predates the predecessor's sunset by 46 days, both catalog rows are live
  today, and the thing that lapsed was Moonshot's hosted endpoint while the
  open weights and the OpenRouter row both survive it. It is the sentence a
  deprecation-policy or model-lifecycle argument needs to show that "sunset"
  names a hosting decision, not the disappearance of a model.
subject: "content/wiki/model/moonshotai-kimi-k3.md"
reviewed:
  "content/wiki/model/moonshotai-kimi-k3.md": "411454e0b47f7102667d9e080a38281856188a2fe739c2b97f55a7414f1ebce7"
---

Free-form notes.

**Scope.** The diff changes one paragraph in
`content/wiki/model/moonshotai-kimi-k3.md`, deletes
`data/carried/j-20260901-21-carry-1.md` (which the brief explicitly requires as
part of the same diff), and adds the loop's own `.job/brief.md` and
`.job/source.json`. Nothing else. That is exactly the stated outcome, and no
reserved path is touched.

**The carried finding is retired correctly.** `data/carried/` in this worktree
now holds only `README.md` and `j-20260901-07-carry-1.md`; the j-20260901-21
carry file is gone, so the item will not reappear on the next Pulse run.

**The factual claim the repair adds — "as they still are".** Read
`data/derived/catalog.json` directly rather than taking the brief's word for it:

```
{ "row_id": "moonshotai/kimi-k2.5", "entry_id": "model/moonshotai-kimi-k2-5",
  "expiration_date": null, "status": "active", "source": "openrouter-models" }
{ "row_id": "moonshotai/kimi-k3",   "entry_id": "model/moonshotai-kimi-k3",
  "expiration_date": null, "status": "active", "source": "openrouter-models" }
```

Both rows are present, both `active`, both `expiration_date: null`. The
present-tense "as they still are" is a measurement of the committed catalog, not
an inference.

**The second half — "the older one now on OpenRouter's data rather than
Moonshot's hosting".** Checked against the K2.5 entry, which states the same
split in its own words (`content/wiki/model/moonshotai-kimi-k2-5.md`): "The
catalog reads this row `active`; this page reads it `deprecated`, and the
disagreement is which signal counts. OpenRouter cleared the expiry from its own
row ... and the catalog's status is derived from that field — a cleared expiry
reads `active`. ... The router's data stopped carrying a date; the vendor's page
says the model is discontinued." The K3 paragraph's new clause is a compressed
restatement of that, and the two entries now agree rather than contradicting.
The catalog row's `source: "openrouter-models"` confirms the mechanical half:
the row persists because OpenRouter's `/api/v1/models` snapshot still carries
it.

**The surrounding sentence's unchanged arithmetic, spot-checked since the repair
leans on it.** K3's `listed_date` fact in the front matter is `2026-07-16`
(source_url `https://openrouter.ai/moonshotai/kimi-k3`, accessed 2026-08-28);
the K2.5 sunset the paragraph transcludes is 2026-08-31. 2026-07-16 →
2026-08-31 is 46 days, which is what the prose says.

**Tense consistency after the edit.** "What lapsed at the end of it was a hosted
endpoint rather than the model" now reads correctly against the added clause:
the past tense attaches to the endpoint, which did lapse, and no longer to the
co-listing, which has not.

**Not re-run:** `npm test` and `npm run build`, per the brief — the loop ran both
on `fe797bd7cfb7` and both passed. The transclusions in the edited paragraph
(`#api_sunset`, `#license`) are unchanged by this diff and the build would fail
on an unresolved one, so their resolution is already established by that pass.

Nothing carried and no proposal: the finding this job existed to fix is fixed in
the wording the carrying reviewer proposed, and reading the paragraph fresh
turned up nothing else.
