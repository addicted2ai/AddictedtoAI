---
job: j-20260904-31
verdict: approve
reasons: []
would-cite: >-
  Nobody links a one-line source_url swap, so the citation this earns is the
  page it repairs: someone arguing that a catalogue badge and a shipped
  artefact are different claims will quote the Meta Superintelligence Labs
  entry's "first Spark flagship listed 'Open weights · Meta license (weights
  pending)' with weights 'Not released'" sentence — and that quotation is only
  usable if the "source" link beside it opens the 1.2 card the sentence names
  rather than the 1.3 card it is contrasted against. A reader who clicks
  through and lands on a different model than the one in the clause stops
  trusting the passage, which is the whole cost this fixes.
subject: "content/wiki/org/meta-superintelligence-labs.md"
reviewed:
  "content/wiki/org/meta-superintelligence-labs.md": "cc88334d0837f7520b76cbe4bf6fb89407f9d4785b006e03f93828b19129d6d1"
---

Free-form notes.

## What the diff is

Four files, one substantive line:

- `content/wiki/org/meta-superintelligence-labs.md:26` — `flagship_weights_listing`'s
  `source_url` moves from `.../muse-spark-1-3` to `.../muse-spark-1-2`.
- `data/carried/j-20260904-29-carry-1.md` — deleted, which the brief names as
  the retirement mechanism for the queue item.
- `.job/brief.md`, `.job/source.json` — loop-generated job scaffolding.

`git diff --stat 64a10dc..af1f8d5` printed:

```
 .job/brief.md                                   | 623 ++++++++++++++++++++++++
 .job/source.json                                |   8 +
 content/wiki/org/meta-superintelligence-labs.md |   2 +-
 data/carried/j-20260904-29-carry-1.md           |  16 -
 4 files changed, 632 insertions(+), 17 deletions(-)
```

No proposal file, no other content touched, no reserved path touched. That is
the stated outcome and nothing wider — no `scope-violation`.

## Does the new source support the fact value?

The value is `"listed 'Open weights · Meta license (weights pending)' with
weights 'Not released'"`, `accessed: "2026-09-03"`. The evidence transcript
`data/reviews/evidence/verify-meta-spark-weights-pending-status.raw.txt`
(fetched 2026-09-03, the same date the fact stamps) records the 1.2 card at
HTTP 200 and quotes it directly:

```
=== https://llm-releases.com/models/muse-spark-1-2 HTTP 200 STRIPPED 2121 ===
TERM "Meta license" hits=2
  ...ding agent powered by the model. Meta has signaled open weights are coming.
  Official page ↗ Specifications License Open weights · Meta license (weights
  pending) Weights Not released Architecture unknown...
TERM "Weights Not released" hits=1
```

Both quoted fragments — `Open weights · Meta license (weights pending)` and
`Weights Not released` — are present verbatim on the 1.2 card. The new
`source_url` supports the value it is now attached to, and the unchanged
`accessed: 2026-09-03` is truthful for it, because that card was fetched in the
same run as the 1.3 card. The narrative beside the transcript
(`verify-meta-spark-weights-pending-status.md:23`) records the same thing:
"llm-releases.com/models/muse-spark-1-2 and /muse-spark-1-3 (the badge's own
source) | both still: ...". So the swap loses no support; the two cards carry
the badge identically.

## Does the fix actually change what renders?

I read the code path end to end rather than asserting it. `lib/facts.mjs:249-258`,
the `cited` branch:

```js
inner =
  quantity(r.value) +
  `<span class="fact-source"><a href="${safeUrl(fact.source_url)}" rel="nofollow noopener">source</a>` +
  `, accessed ${escapeHtml(fact.accessed)}</span>` +
  overdue;
```

and `safeUrl` (`lib/facts.mjs:51-53`):

```js
export function safeUrl(url) {
  return /^https?:\/\//i.test(String(url ?? '')) ? escapeHtml(url) : '#';
}
```

`https://llm-releases.com/models/muse-spark-1-2` matches the scheme test and
contains none of the five characters `escapeHtml` rewrites, so it reaches the
`href` byte-identical. The transclusion at line 88 of the entry therefore now
emits a `source` link to the 1.2 card, inside the sentence whose subject
(line 86) is the 1.2 card. That is exactly the defect the carried finding
described, removed.

**One check I attempted and could not run, reported rather than routed
around.** I wanted to render the fact directly and read the emitted `href`
instead of reading the code that emits it. This worktree has no
`node_modules`, so `import`ing `lib/facts.mjs` fails at `zod` (via
`lib/schema.mjs`):

```
Error [ERR_MODULE_NOT_FOUND]: Cannot find package 'zod' imported from
D:\addictedtoai-worktrees\j-20260904-31-review-1\lib\schema.mjs
```

My attempt to junction the main checkout's `node_modules` into the worktree was
blocked by the sandbox ("new-item targeting 'D:\AddictedtoAI\node_modules' was
blocked"). I did not look for another way round it. What I have instead is a
complete read of the two functions on the path, both short and both pure, plus
the loop's `npm run build` PASS on `af1f8d5d09da` — which is the check that
would have gone red had the transclusion stopped resolving.

## The rest of the page still holds after the swap

The concern with moving a citation is orphaning the claim it used to carry. It
does not happen here:

- The 1.3 card's identity claim in the prose (lines 90-92, "Muse Spark 1.3's
  [card](https://llm-releases.com/models/muse-spark-1-3) carries the same
  License and Weights lines, character for character") is cited by its own
  inline link, not by this fact. The transcript's 1.3 block quotes the same two
  strings, so that sentence keeps its support.
- The `2026-09-02` timeline row still cites the 1.3 card, correctly — it is a
  row about the 1.3 release.
- `flagship_weights` still cites the 1.3 *announcement* on research.meta.ai,
  which the transcript confirms carries the one `open weights release` hit.
- `grep` for `flagship_weights_listing` across the tree finds exactly one
  definition and one transclusion, both in this file, so no other page's
  citation moved underneath it.

## Reviewed-surface hash

The edit is to front matter, which `lib/review-hash.mjs` deliberately includes
in the reviewed surface — its header names "a swapped `source_url` would read as
unchanged" as the reason. So this file's hash no longer equals the binding in
`data/reviews/j-20260904-29.md:39`. That is the ordinary consequence of any
approved page being edited, not a defect in the diff: `resolveReviews`
supersedes an older record with a newer one per file
(`lib/reviews.mjs:332-399`), and the merge writes this record's own rebound
hash. I note it only because `verify-launch` fails on `mismatched` and was not
among the checks the loop ran on this commit.

## What I did not re-run

`npm test` and `npm run build` were run by the loop on this exact commit and
both passed; per the brief I did not repeat them.
