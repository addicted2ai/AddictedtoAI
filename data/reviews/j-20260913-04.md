---
job: j-20260913-04
verdict: approve
reasons: []
would-cite: >-
  A developer weighing the batch tier of Ministral 3 8B for a cheap
  vision-capable run would link this entry: it binds the live OpenRouter batch
  row for price and context window and keeps the September removal next to the
  September return, so the active status reads as a relisting, not a new model.
subject: "content/wiki/model/mistralai-ministral-8b-2512-batch.md"
reviewed:
  "content/wiki/model/mistralai-ministral-8b-2512-batch.md": "ac6f067994efffca6af1f2585266e7e47975ad2e37bce6507511bf4f2b495b05"
---

## Verdict

`approve`. I received only the reviewed surface for
`content/wiki/model/mistralai-ministral-8b-2512-batch.md` (this outcome carries
no diff; the executor left the O8 pre-edit bytes unchanged). The page implements
the queue item's same-subject branch: it restores the
`openrouter-models: mistralai/ministral-8b-2512:batch` binding, carries the
canonical feed-backed facts, flips `retired/dormant` to `active/living`, and
records the return as a dated timeline event. No blog post is touched, so no
voice question arises.

## What I checked

- `data/sources/openrouter-models/latest.json` (date `2026-09-12`,
  `row_count` 445, url `https://openrouter.ai/api/v1/models`) holds the
  `"mistralai/ministral-8b-2512:batch"` row with
  `"name": "Mistral: Ministral 3 8B 2512 (batch)"`,
  `"pricing": {"completion": "0.000000075", "prompt": "0.000000075"}` and
  `"context_length": 262144` — the row is live and the entry's display name
  matches it exactly, so the same-subject branch applies, not the genuine
  collision branch. No rename or disambiguation was needed.
- `data/changes.jsonl` line 109 records the `2026-09-01` `$retirement` and line
  207 records the `2026-09-10` `$arrival` for
  `row_id "mistralai/ministral-8b-2512:batch"` (excerpt `$status "active"`) —
  supporting both timeline rows and the `retired -> active` flip. The two
  findings share one instrument (the OpenRouter snapshot derivation behind
  `latest.json` and `changes.jsonl`), so I record them as agreement, not
  independent corroboration.
- Facts/paths (`pricing.prompt`, `pricing.completion`, `context_length`,
  `$status`, all `volatility: fast`) are byte-identical in shape to the
  approved sibling repair `content/wiki/model/mistralai-codestral-2508-batch.md`
  (job `j-20260913-02`) — the canonical binding, not an invention. Volatile
  values are feed-bound, not literals; the single alias is `class: manual` on
  the exact row name, sanely classed; there is no prose body, so the
  prose-adds-something check is vacuously satisfied.
- Only one content file declares this row id
  (`content/wiki/model/mistralai-ministral-8b-2512-batch.md` line 11); the
  other hit (`content/wiki/org/mistral-ai.md` line 100) is a prose mention, not
  a `feeds:` declaration, so there is no two-subject collision to resolve.
- Polarity: the page implements the work source; it does not ignore or invert
  it. Scope: this invocation reviews only the named page and the page carries
  no scope widening.
- Loop-reported gates on commit `b8746fbd307e`
  (`npm run build` PASS, `node scripts/verify-surfaces.mjs` PASS) were taken as
  stated and not re-run as a substitute for reading, per the brief. A direct
  `node scripts/verify-surfaces.mjs out` in this worktree fails on
  `Cannot find package 'fast-glob'` (missing shared install), which is an
  environment finding about this worktree, not evidence about the page, so it
  does not move the verdict.
