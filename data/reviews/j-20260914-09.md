---
job: j-20260914-09
verdict: approve
reasons: []
would-cite: >-
  A reader asking whether the paid Nex-N2-Pro router listing lapsed on its own
  expiry date or the model itself was pulled would link this entry: it dates
  the row's exit to its expiration_date of 2026-09-08 alongside the same-fetch
  arrival of the free N2.5 pair, places the 404 page with its emptied endpoints
  record next to the still-published apache-2.0 weights, and retains the feed
  binding while declining to say whether either listing ever served the same
  weights.
subject: "content/wiki/model/nex-agi-nex-n2-pro.md"
reviewed:
  "content/wiki/model/nex-agi-nex-n2-pro.md": "1ac38de111028b0f656ccf1ffcc2617ebb8fa2309c00dbd13b3aff9d7ac30305"
---

## Verdict

`approve`. The diff answers the vanished-row question from the vendor's and the
feed publisher's own sources, keeps the entry and its `feeds:` binding, moves
the vanished record unchanged, and stays inside the stated outcome.

No `content/blog/` file is in this diff, so the voice question is not
triggered and neither approve branch for a post applies.

## What I checked

Scope: `git show HEAD --stat` in the review worktree reports exactly two
paths for the author's commit `ceca8ea168ff`:

- `content/wiki/model/nex-agi-nex-n2-pro.md` (60 insertions, 1 deletion)
- `data/vanished/{ => answered}/openrouter-models--nex-agi-nex-n2-pro.md`
  (pure rename, `similarity index 100%` in the loop diff)

The loop's `npm run build` PASS and `verify-surfaces` PASS on `ceca8ea168ff`
were taken as run, not re-run, per the brief.

## Source checks (all measured, all quoted)

1. Catalog snapshots (local instrument). `latest.json` reports `date`
`2026-09-14` with `row_count` 445; `previous.json` reports `2026-09-12` with
`row_count` 445. In both, `'nex-agi/nex-n2-pro' in rows` is false and the only
`nex-agi` keys are `nex-agi/nex-n2.5-mini:free` and
`nex-agi/nex-n2.5-pro:free`. Their `created` values are `1788890050`
(`2026-09-08T17:54:10.000Z`) for the pro-free row and `1788890061`
(`2026-09-08T17:54:21.000Z`) for the mini-free row — eleven seconds apart on
2026-09-08T17:54Z, as the page says. The successor rows' entries both carry
`{ date: "2026-09-09", event: arrived }`, and the answered record carries
`last_seen: "2026-09-08"`, `date: "2026-09-09"`, so "arrival in the 2026-09-09
fetch — the same fetch that stopped carrying this row" is the shared
2026-09-09 date on both sides. The two snapshots share one fetcher, so their
agreement is agreement, not corroboration.

2. Pinned last-known values. The answered record's `expiration_date 2026-09-08`,
`hugging_face_id nex-agi/Nex-N2-Pro`, and description opening "Nex-N2-Pro is an
agentic mixture-of-experts model from Nex AGI, with 17B active parameters out
of 397B total. Built on the Qwen3.5 architecture, it accepts text and image
input and produces..." match the page's restatement ("carried
`expiration_date` 2026-09-08, was last seen 2026-09-08"; "with 17B active
parameters out of 397B total" built on Qwen3.5). The live successor row in the
snapshot has `hugging_face_id "nex-agi/Nex-N2.5-Pro"` and its description opens
"Nex-N2.5 is an agentic model built to turn goals into working, verified
outcomes." — the fragment the page quotes — so "the listings point at
different weights" is measured, not inferred.

3. Vendor (independent instrument). Fetched
`https://github.com/nex-agi/Nex-N2.5`: "Today, Nex-AGI officially introduces
**Nex-N2.5**, its next-generation family of agentic models." and
"Nex-N2.5-mini and Nex-N2.5-Pro continue to build on the multimodal
foundations of Nex-N2, with focused improvements in computer use, web
browsing, and visually grounded agentic capabilities." Both sentences appear
verbatim as the page quotes them. Fetched
`https://huggingface.co/nex-agi/Nex-N2.5-Pro`: carries the same two sentences
and links OpenRouter for the Pro and the mini. The page's "calls the arrivals
a new family, not a rename" is supported.

4. Publisher state (independent instrument). Fetched
`https://openrouter.ai/nex-agi/nex-n2-pro`: "StatusCode: non 2xx status code
(404 GET https://openrouter.ai/nex-agi/nex-n2-pro)" — the page's "answers
404". Fetched `.../api/v1/models/nex-agi/nex-n2-pro/endpoints`: returns the
old record (`"id":"nex-agi/nex-n2-pro"`, created 1780937140, Qwen3 tokenizer
block) with `"endpoints":[]` — the record survives with empty supply, as
claimed. Fetched `.../nex-n2.5-pro:free/endpoints`: one endpoint,
`"provider_name":"Nex AGI"`,
`"name":"Nex AGI | nex-agi/nex-n2.5-pro-20260907:free"` — one live Nex AGI
endpoint, as claimed. The vendor-vs-publisher agreement (new family announced;
old page 404 with empty supply, new free row served) is corroboration across
different instruments.

5. Not retired. Fetched `https://huggingface.co/nex-agi/Nex-N2-Pro`: header
"License: apache-2.0" with the full Nex-N2 model card present ("we are
officially releasing and open-sourcing ... **Nex-N2**", Nex-N2-Pro weight
downloads and deployment sections live). Neither it, the N2.5 announcement,
nor the fetched OpenRouter records announce an end to Nex-N2. The page's
negative claims are scoped to "fetched for this job" / "nothing on OpenRouter
says", which is what was checked.

6. Mechanics. `feeds: openrouter-models: nex-agi/nex-n2-pro` is retained; the
`mentions:` target `model/nex-agi-nex-n2-5-pro-free` exists with the matching
feed row `nex-agi/nex-n2.5-pro:free`; all six `{{fact:...}}` references name
fields (`price_input`, `price_output`, `context_window`) present in both
entries' `facts:` maps. The vanished move is a pure rename. The pre-existing
`timeline` line (deprecated 2026-09-01, retired 2026-09-09) is untouched; the
prose distinguishes the row's retirement from the model's, so there is no
contradiction.

## Corroboration

The two snapshot dates agree through one instrument (one fetcher) — agreement
only. The classification (paid listing ended on its stated expiry date as the
N2.5 family arrived; model itself not retired) rests on three different
instruments: the catalog snapshots, the vendor's GitHub/HF publications, and
OpenRouter's page/endpoints API — which corroborate each other.

## Polarity

The work source required: establish which of retire/rename/tier-fold/delist
happened from vendor+publisher sources (done — expiry-date delisting of a paid
row as the N2.5 family arrived, model not retired); do NOT remove the entry or
its binding (kept); MOVE the record to `answered/` unchanged in the same diff
(done). Nothing is inverted; nothing widens scope.
