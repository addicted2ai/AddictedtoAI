---
job: j-20260903-17
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that "we trained our own frontier model" claims should be read
  against the base checkpoint, not against whichever competitors the vendor chose
  to tabulate. This post is now the compact worked example: $40M and 35,207 B200
  GPU-hours bought 2.9 points of overall average over a free Apache-2.0 base, and
  on the legal benchmark the vendor's own press release leads with, the result is
  below that base. A procurement analyst, a legal-tech skeptic, or anyone writing
  about the "sovereign AI" pitch would link this paragraph specifically, because
  the base-model columns are the part every vendor summary omits.
carry:
  - title: Say that Thomson is last of all five columns on Stanford LegalBench
    detail: >-
      The revised sentence reads "Stanford LegalBench 79.9 sits below both bases
      (Qwen 80.3, Snowdon 80.9) as well as behind Gemma's 83.1". It names three
      of the four columns that beat Thomson on that row and silently drops the
      fourth: Haiku 4.5 scores 80.7 on Stanford LegalBench (verified in the raw
      model card at
      huggingface.co/thomsonreuters/Thomson-1.0-Small/raw/main/README.md,
      fetched 2026-09-03). Haiku is the model the immediately preceding sentence
      cites by name as trailing at 68.2 overall, so its absence here is the one
      that costs most. The true and sharper statement is that on the headline
      legal benchmark, Thomson-1.0-Small places LAST of the five columns in its
      own table — behind both of its bases and behind both competitors it beats
      on the overall average. That is the same class of omission the finding this
      job just cleared was about, one row further down.
    subject: content/blog/thomson-reuters-thomson-model.md
  - title: The download counts moved ~35% inside the day they are stamped with
    detail: >-
      The post now says bartowski's GGUF "had 70,172 downloads in the 30 days to
      3 September against 1,130 for the source repo". Re-fetching the same two
      API endpoints during this review, also on 2026-09-03, returned downloads
      94564 for bartowski/thomsonreuters_Thomson-1.0-Small-GGUF and 1231 for
      thomsonreuters/Thomson-1.0-Small. Nothing in the post is wrong — the
      figures are a dated measurement and the 30-day qualifier the job added is
      correct — but a rolling counter that moves 35% inside its own stamped date
      is a volatile value typed into prose, which is the shape CLAUDE.md's
      "volatile values are bound, never typed" convention exists for. A later
      pass could bind these to a feed fact, or soften the sentence to the point
      it is actually making (the conversion out-downloads the source repo by
      roughly two orders of magnitude) so it does not decay.
    subject: content/blog/thomson-reuters-thomson-model.md
subject: "content/blog/thomson-reuters-thomson-model.md"
reviewed:
  "content/blog/thomson-reuters-thomson-model.md": "3ffe7daae9d698eec07cbdec829339f0f350d3ca4101ef1853968f8ef7ce2726"
---

Three carried findings, three fixes, three carried files deleted in the same
diff. All three land, nothing beyond them is touched, and every number the diff
adds checks out against its source.

## What I checked

**Scope.** `git diff --stat HEAD~1 HEAD` in the review worktree:

```
 content/blog/thomson-reuters-thomson-model.md |  6 +++---
 data/carried/j-20260903-02-carry-1.md         | 16 ----------------
 data/carried/j-20260903-02-carry-2.md         | 16 ----------------
 data/carried/j-20260903-06-carry-1.md         | 16 ----------------
 data/changes.jsonl                            |  2 +-
 5 files changed, 4 insertions(+), 52 deletions(-)
```

Exactly the target file, exactly the three named carry files, and the one
`changes.jsonl` line the third finding is about. `.job/` scaffolding aside,
there is nothing else. The other fifteen files in `data/carried/` are untouched,
including `j-20260903-02-carry-3.md`, which shares an origin job but names a
different subject (`data/proposals/feed-descriptions-unsynced-with-the-artefacts-they-name.md`)
and was correctly left standing.

**Finding 1 — the base-model columns.** Fetched the raw model card, not the
rendered page, at
`https://huggingface.co/thomsonreuters/Thomson-1.0-Small/raw/main/README.md`.
The three rows the diff touches:

```
| Benchmark           | Thomson-1.0-Small | Snowdon-1.1-Small | Qwen3.6-35B-A3B | Gemma 4-31B | Haiku 4.5 |
| Overall Avg.        | 74.6              | 71.7              | 71.7            | 71.2        | 68.2      |
| Stanford LegalBench | 79.9              | 80.9              | 80.3            | 83.1        | 80.7      |
| MBE Bar Exam        | 83.4              | 83.1              | 80.1            | 88.8        | 77.3      |
```

Every figure the new prose asserts is in that table: both bases at 71.7, the
arithmetic `74.6 − 71.7 = 2.9` behind "the $40M bought 2.9 points over the free
base it repurposed", LegalBench 79.9 against Qwen 80.3 and Snowdon 80.9, MBE
83.4 against Snowdon 83.1 and Gemma 88.8. The "both Apache-2.0" clause is the
one claim not on that page, so I checked it at the source: the HF API records
give `license: "apache-2.0"` for both `tri-fair-lab/Snowdon1.1-Small` and
`Qwen/Qwen3.6-35B-A3B`. The one thing left on the table is Haiku's LegalBench
80.7 — carried above, not blocked on.

**Finding 2 — the 30-day qualifier.** The substantive claim is about what the
Hugging Face `downloads` field means, and the diff states it correctly: it is a
rolling 30-day count, not a cumulative total. The supporting parenthetical is
checkable and checks out — `createdAt` for `thomsonreuters/Thomson-1.0-Small` is
`"2026-08-18T15:33:05.000Z"`, so a 30-day window ending 3 September opens on
4 August, before the repo existed, and does cover its whole life. (bartowski's
conversion, `createdAt "2026-08-26T04:45:39.000Z"`, is newer still, so the same
holds a fortiori for the figure the sentence leads with.) The Sources bullet now
carries four distinct API URLs where it carried one; I fetched all four and all
four return valid model records — `thomsonreuters/Thomson-1.0-Small`,
`tri-fair-lab/Snowdon1.1-Small`, `Qwen/Qwen3.6-35B-A3B` and
`bartowski/thomsonreuters_Thomson-1.0-Small-GGUF`. No 404, no gate, no login
wall. The counts themselves have moved since the post was written; that is the
second carry above and not a defect in this diff.

**Finding 3 — the annotation.** The job replaced the bare job id with the post's
title and its URL: `…the correction in prose is the post on this site, 'Thomson
Reuters says its $40M in-house model is on par with the latest frontier models',
at /blog/thomson-reuters-thomson-model.` Two things had to be true for this to
be a fix rather than a reshuffle, and I checked both rather than assuming them.

*The URL has to be the post's real URL.* `lib/corpus.mjs:39` builds a post's href
as `` `/blog/${doc.slug}` ``, and `lib/corpus.mjs:87` derives `slug` from the
filename (`base.replace(/\.md$/, '')`) — so `content/blog/thomson-reuters-thomson-model.md`
lives at `/blog/thomson-reuters-thomson-model`. The direct confirmation is that
the already-built derived layer agrees:
`data/derived/search-index.json:66` carries `"u": "/blog/thomson-reuters-thomson-model"`.
The route exists (`app/blog/[slug]/page.tsx`). The string a visitor now reads is
one they can actually type.

*The longer text has to survive rendering.* The original finding is that this
string reaches visitors as escaped plain text in the home rail and in the RSS
item body, so a truncation would have swallowed the URL at the end and left the
fix invisible. `lib/render/home.mjs:48-52` emits `line.annotations` into a
`change-annotation` element with no per-annotation slicing; the only `slice`
calls in that file (lines 65 and 84) bound how many feed lines and rows are
shown, not the length of any one annotation. The URL sits at the very end of the
string, which is the position a truncation would eat first, so this mattered.

Everything else on that JSONL line — `kind`, `annotates`, `date`, `job`, and the
whole body of the annotation up to the final clause — is byte-identical.

## What I did not re-run

`npm test` and `npm run build` both passed on this commit before the review and
I took that as given rather than spending the run on it. The checks I ran are the
ones that would be red if a specific claim in *this diff* were false: the raw
model card for the benchmark figures, four HF API endpoints for the source links
and the creation dates, and the corpus/render source plus the derived search
index for the URL and the truncation question.

One thing I considered and satisfied myself about: editing an approved post moves
its reviewed-surface hash, which per `specs/review` makes it **mismatched**, and
`verify-launch` fails on mismatched. That is not a defect here — `lib/reviews.mjs`
(around line 296) documents that a later record SUPERSEDES an earlier one
precisely "without it a mismatch has no way to clear", and the merge writes
`reviewed:` afresh. A repair on approved prose is the designed path, not an
accident, so it needs no action from this job.

## Voice

The two new sentences in the benchmark paragraph do the thing the post was
already good at — put the vendor's own number next to the number the vendor left
out of its summary, and let the gap speak. "so the $40M bought 2.9 points over
the free base it repurposed" is the sentence the whole post was building toward
and it had been missing. The downloads sentence is the weaker edit: a nested
parenthetical inside an em-dash aside inside an already long sentence, and it now
carries three separate qualifications where the point is simple. It is accurate,
which is what the finding asked for, and it is not bad enough to send back.
