---
job: j-20260914-29
verdict: approve
reasons: []
would-cite: >-
  Someone telling a reader which Transformers.js release to install for in-browser
  inference would link the entry's version fact. It now carries an access date
  inside its 14-day window, and that date is backed by a saved registry response
  showing 4.2.0 is still npm's `latest` tag.
subject: "content/wiki/tool/transformers-js.md"
reviewed:
  "content/wiki/tool/transformers-js.md": "fb1e4720a7185d9b72b003e952476d5cfaa1c0b190fedf99b3e46a79e99767f3"
---

## What I checked

**Local date.** `date` printed `Mon Sep 14 12:46:50 MDT 2026`, so `accessed: "2026-09-14"` is the real local date of the check.

**Re-fetched the cited source myself.** Using the fact's own `source_url`, plus the dist-tags endpoint as a second route:

```
{"next":"4.0.0-next.11","latest":"4.2.0"}
@huggingface/transformers@4.2.0 5a5342a1a148c5d297ed8384d21cee1824fab031 Apache-2.0
```

`_id` and `version` are 4.2.0. The `dist.shasum` (`5a5342a1…`) matches the one in the author's raw transcript byte for byte. So the transcript really is a response from this endpoint, not a reconstruction. My fetch and the author's used the same instrument (the npm registry), so this counts as agreement and reproduction, not independent corroboration. For a claim that is literally "what the registry's latest tag says", the registry is the only authority.

**Scope.** The job's own commits are `19c24d3` (the brief) and `33c41f1`. `33c41f1` touches exactly three files. One is `content/wiki/tool/transformers-js.md`, where the only change is `version.accessed` going from 2026-08-28 to 2026-09-14. The other two are the evidence narrative and the raw transcript. The value was not changed. The `license` and `repository` stamps were left alone and the narrative says so. I ran `git diff --stat main...HEAD` and it lists many more files. Those come from the base's pulse/job commits that the local `main` ref does not yet contain, not from this job. The diff the loop computed shows only the five files above.

**Polarity.** The brief asks to re-verify the overdue fact and move the stamp to the real check date "and to nothing else". The diff does exactly that. Nothing is inverted.

**Evidence honesty.** The narrative says two `set-cookie` headers were removed, and the transcript marks where. It also says the dist-tags cross-check is quoted in the narrative rather than saved as its own file. Both redactions are disclosed.

I did not re-run the gates. The loop reports that build and verify-surfaces passed on 33c41f1.
