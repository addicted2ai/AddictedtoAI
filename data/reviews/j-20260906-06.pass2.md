---
job: j-20260906-06
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that "open-weight" is a release sequence rather than a
  property would link this. The entry is the only place that lines up
  inclusionAI's four Ling 3.0 shipments against both clocks — router listing
  and Hugging Face repository creation — and shows the API leading every
  single time, with the gap narrowing from ten days (flash) to seven (Fin) to
  still-open (Sante), and the one weights-first model being precisely the one
  that was never sold. A reader making the narrower point that release
  trackers decay would cite the llm-releases contradiction: Apache 2.0 and
  "Not released" still served on 2026-09-06 for weights that have been MIT and
  downloadable since 2 August. And anyone arguing that router catalogs are a
  poor proxy for what a lab actually ships would cite the closing turn — this
  org's entire catalog presence is one fine-tuned MoE while its traffic sits
  in an LLaDA diffusion line no router carries.
subject: "content/wiki/org/inclusionai.md"
reviewed:
  "content/wiki/org/inclusionai.md": "06cbd4eed5c62cc24b60b6991cf374945d74a91d3940a31a4349e7a4f29400c5"
---

Delta review, pass 2. The previous verdict required exactly two changes. Both
are made, and the revision commit touches nothing else.

**The diff in my brief was stale.** It shows the file at its pre-revision
state — the `sante_weights_absent` value still reading "reach back to
2026-08-26" and the prose still reading "four mission cards". The worktree at
`5f838718332f` does not. I judged the branch state, not the brief's rendering
of it:

```
$ git log --oneline -3
5f83871 job j-20260906-06 (entry): revision — correct two claims the sources do not support
fc57c06 job j-20260906-06 (entry): write the wiki org entry for the catalog provider `inclusionai`
ba16ce3 job j-20260906-06: brief
$ git rev-parse HEAD
5f838718332f2c3bb06b45b4f2fdad03ab4f7dfe
```

The actual delta, `git diff fc57c06 5f83871` — one file, three insertions,
three deletions:

```diff
   - field: sante_weights_absent
     source: cited
-    value: "no repository whose name contains \"Sante\" appears among the 100 most recently created inclusionAI repositories, which reach back to 2026-08-26 and whose newest is dated 2026-09-05T05:16:19Z"
-    source_url: "https://huggingface.co/api/models?author=inclusionAI&limit=100&sort=createdAt&direction=-1"
+    value: "no repository whose name contains \"Sante\" exists under the inclusionAI account — a Hugging Face model search for that string, scoped to the account, returns an empty list"
+    source_url: "https://huggingface.co/api/models?search=Sante&author=inclusionAI"
     accessed: "2026-09-06"
     volatility: dated
@@
-plainly on its own site, as one of four mission cards:
+plainly on its own site, as one of three mission cards:
```

```
$ git diff --stat fc57c06 5f83871
 content/wiki/org/inclusionai.md | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)
```

Nothing else changed. No `scope-violation`, and no new surface to re-check
beyond these two claims.

## Required change 1 — the Sante fact. Fixed, and fixed the better way.

The author took the stronger of the two options the previous verdict offered:
rather than correcting "2026-08-26" to "2025-10-10", the fact now cites the
search endpoint, which supports the claim exhaustively instead of by a window.
The false clause is gone entirely — there is no longer any statement about how
far a listing reaches, so there is nothing left to be wrong about.

I fetched the newly cited URL verbatim on 2026-09-06 and, because an empty
result is exactly the shape a broken query also produces, I ruled out my own
instrument with three controls before accepting it:

```
CITED    200 bytes=2 rows=0
   ids:
CONTROL1 200 bytes=18723 rows=47
   ids: inclusionAI/Ling-3.0-flash-Fin | inclusionAI/Ling-3.0-tiny | inclusionAI/Ling-3.0-flash | inclusionAI/Ling-3.0-tiny-GGUF | inclusionAI/Ling-3.0-tiny-singprobe | inclusionAI/Ling-3.0-flash-singprobe | inclusionAI/Ling-3.0-tiny-int4 | inclusionAI/Ling-3.0-flash-GGUF
CONTROL2 200 bytes=542 rows=1
   ids: inclusionAI/Ling-3.0-flash-Fin
CONTROL3 200 bytes=12106 rows=36
   ids: Santenana/vit_model | Santenana/Pokemons_Clasiffication2-Santiago-Garcia | ...
DIRECT   Ling-3.0-flash-Sante -> 401
```

- `CITED` is the fact's own `source_url`, `?search=Sante&author=inclusionAI` —
  200, two bytes, `[]`. Exactly what the value says it returns.
- `CONTROL1` (`search=Ling&author=inclusionAI`) returns 47 rows, so the
  `search` + `author` combination is a working query and not one that silently
  empties whenever both parameters are present.
- `CONTROL2` is the one that matters most: `search=Fin&author=inclusionAI`
  returns `inclusionAI/Ling-3.0-flash-Fin`. "Fin" and "Sante" occupy the
  identical position — a suffix on a `Ling-3.0-flash-` name under the same
  account — so this demonstrates that if a Sante repository existed, this exact
  query shape would find it.
- `CONTROL3` (`search=Sante`, unscoped) returns 36 rows, so "Sante" is a string
  Hugging Face happily matches. The emptiness comes from the account scoping,
  which is what the fact claims.

The new value is also more careful than the old one in a way worth naming: it
says "no repository whose name contains \"Sante\" exists under the inclusionAI
account", and that is precisely what a `search` scoped by `author` establishes —
where the superseded version asserted a property of a 100-row window and then
described that window wrongly.

The prose sentence the fact feeds is unchanged and still holds: a direct request
for `inclusionAI/Ling-3.0-flash-Sante` returns `401`, an authentication error
rather than a record, as printed above.

## Required change 2 — "four mission cards". Fixed, and I counted them myself.

Fetched `https://www.inclusion-ai.org/about` on 2026-09-06 rather than taking
the previous verdict's count on trust:

```
status 200 final https://www.inclusion-ai.org/about/ bytes 9520
missionCard occurrences: 3
card titles (3): ["Open-Source Commitment","Collaborative Ecosystems","Bold Experimentation"]
quote present: true :: Share code, models, and data (within legal/ethical
quote present: false :: envisions AGI as humanity's shared milestone, not
quote present: true :: founded by Ant Group with researchers from OpenAI,
```

Three `missionCard_` class occurrences, three card titles, and
"Open-Source Commitment" is one of them — so "as one of three mission cards"
introducing the `open_source_commitment` transclusion is now correct, and the
attribution of that quote to that card is correct with it. The quote itself is
present verbatim in the fetched bytes.

The one `false` above is my instrument, not the corpus, and I ruled it out
rather than reporting it. The page HTML-escapes the apostrophe in "humanity's"
as `&#x27;`, so a raw substring search for the plain-text quote misses it —
neither an ASCII `'` nor a typographic U+2019 matches the served bytes:

```
ascii apostrophe present: false
curly apostrophe present: false
context: "envisions AGI as humanity&#x27;s shared milestone, not a privileged asset\">"
```

Decoding the entity resolves it, and the quote is present in full:

```
occurrence 3 "...<h1 class=\"visionTitle_V9I9\">Vision</h1><p class=\"visionText_GpWC\">inclusionAI (IAI) envisions AGI as humanity&#x27;s shared milestone, not a privileged asset. Backed by Ant Research, we&#x27;re forging a global ope..."
full vision quote present after entity decode: true
```

That third occurrence is the rendered body copy — a `visionText_GpWC` paragraph
directly under the `Vision` `<h1>` — so the `vision` fact's attribution "the
Vision section" is right as well; the first two occurrences are `<meta>`
description tags. This fact is outside the delta and was confirmed in pass 1,
but a bare `false` sitting in a review record would read as a finding, so it is
resolved here rather than left standing.

## What I did not re-check, and why

`npm test` and `npm run build` were run by the loop on this exact commit and
both passed; the brief records that and instructs against repeating them. The
revision changes one YAML string value, one URL and one word of prose — it adds
no field, no transclusion target and no link, so there is no build-surface
change for a rebuild to catch that the loop's run did not already cover.

Everything else in the entry was verified in pass 1 and is byte-identical here:
the ten-tweet launch thread with its timestamps and the measured negative on
weights/licence/Hugging Face mentions, all five Hugging Face model records and
their arithmetic (165,472 + 127,486,240,128 = 127,486,405,600), the four
OpenRouter rows with the `max_completion_tokens` contrast, the GGUF download
ratio, the llm-releases licence contradiction, and the alias classing. Pass 1
confirmed the two front-matter absences the brief demanded are correct rather
than omissions — `publishes_from` cannot validate against `entrySchema`'s
`.strict()` key set, and `feeds` is not the join `lib/render/frontier.mjs` uses
for the org/provider board. I re-read neither claim's evidence, having no
reason from this delta to doubt them and no mandate to re-review unchanged
bytes.

Both required changes are made, both are verified against freshly fetched
sources, the fix to the first is materially better than the minimum the verdict
asked for, and the diff went nowhere else. Approve.
