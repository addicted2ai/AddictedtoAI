---
job: j-20260904-28
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that Inception Labs is a category of one rather than a faster
  member of an existing category would link this entry: it is the only place
  that sets the two OpenRouter diffusion rows against the lab's own about-page
  pitch and then marks, explicitly, which parts are the vendor speaking and
  which the catalog can corroborate. The paragraph this job repaired is the
  hinge of that argument, so it now survives being quoted.
carry:
  - title: Fix the "large" the product fact inserts into the about page's wording
    detail: >-
      The `product` fact's value opens "diffusion-based large language models
      (dLLMs)", and with the new lead-in this renders as "The about page
      describes diffusion-based large language models (dLLMs)". The about page
      (https://www.inceptionlabs.ai/about, fetched 2026-09-04) says "We're
      building diffusion-based language models for production applications" —
      no "large" — and uses "diffusion large language models (dLLMs)" only in
      the separate Mercury sentence the fact already quotes. The two phrases
      have been welded into one. "diffusion-based language models" would match
      the sentence the clause paraphrases.
    subject: content/wiki/org/inception-labs.md
subject: "content/wiki/org/inception-labs.md"
reviewed:
  "content/wiki/org/inception-labs.md": "3b3d0ebf1959d2c8125ffd0228c56110adc98120f986c5c2a767fb13cbf13949"
---

Free-form notes.

**Scope.** `git show --stat HEAD` on `31ee2bcf3d5a`:

```
 content/wiki/org/inception-labs.md    |  9 +++++----
 data/carried/j-20260903-10-carry-2.md | 16 ----------------
 data/carried/j-20260903-10-carry-3.md | 16 ----------------
 3 files changed, 5 insertions(+), 36 deletions(-)
```

Three files: the target and the two carried findings the brief names for
deletion. The `.job/brief.md` and `.job/source.json` in the reviewed diff came
in on the loop's own brief commit (`53f244d`), not this one. No reserved path
touched. Nothing widened.

**Finding 1 — the transclusion stutter. Measured, not inferred.** `renderFact`
(`lib/facts.mjs:217`) splices `quantity(r.value)` verbatim into the sentence,
so the rendered text is fully determined by the lead-in plus the front-matter
value. Grepping the whole file for the stuttering phrase:

```
15:    value: "diffusion-based large language models (dLLMs); it calls Mercury 'the world's first commercially available family of diffusion large language models'"
```

One hit, and it is inside the fact value. The body's lead-in is now `The about
page describes {{fact:org/inception-labs#product}}` (line 64), so the rendered
clause reads "The about page describes diffusion-based large language models
(dLLMs); it calls Mercury 'the world's first…'" — "calls Mercury" once, and the
"it" now resolves to the about page, which is what the sentence means. Before
the fix the lead-in supplied a second "calls Mercury" of its own; that
occurrence is gone from the file. The author took the second of the two repairs
the carrier offered, verbatim.

**Finding 2 — the partial enumeration. Checked against the source.** Fetched
`https://www.inceptionlabs.ai/about` on 2026-09-04 with `curl` (HTTP 200,
287510 bytes) and searched the raw HTML rather than trusting an extractor. The
heading is present as served:

```
Some of the technologies we’ve developed
```

and all nine items are in the served markup — `Diffusion Models`, `Flash
Attention`, `Direct Preference Optimization`, `Masked Diffusion`, `Block
Diffusion`, `Remasking Diffusion`, `Discrete Diffusion Guidance`, `Decision
Transformers`, `d1 Reasoning` — each appearing twice (the page ships two SSR
variants). The arXiv ids the carrier named for the two omitted papers are both
in the markup too: `2412.10193` and `2106.01345`, alongside `2504.12216` for
the d1 link the entry already carries. So the carrier's count was right: the
entry names seven of nine. The diff inserts "among them," before the two
groupings, which is the exact hedge the finding asked for and is now true of a
list I have counted rather than assumed.

Note the page itself already says "Some of", so the entry's "among them" is
about the entry's own selection, not the page's. That is a slight doubling but
it is the honest reading: without it a reader takes the seven as the whole
list, which is the defect that was carried.

**What I deliberately did not do.** The loop reports `npm test` and `npm run
build` PASS on this same commit; I did not re-run them. The claims in this diff
are a rendered-text claim and a source-enumeration claim, and both are checked
above by the narrow checks that would be red if they were false.

**On the carry.** The `product` fact's value is untouched by this diff — it is
the same string it was before — so its wording is not this job's defect. But
the lead-in change promotes that string from "what the page calls Mercury" to
"what the page describes", which is the first time the first clause has stood
on its own, and standing on its own it welds "diffusion-based language models"
(the page's opening line) to "large" (from the Mercury sentence). Small, real,
not worth blocking a repair that did exactly what it was asked; carried above.

One cosmetic thing I am not carrying: the diff leaves `descends` and `from` on
separate source lines with `from` alone on line 78. Markdown joins them with a
space, so the rendered prose is unaffected.
