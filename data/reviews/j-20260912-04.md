---
job: j-20260912-04
verdict: approve
reasons: []
would-cite: >-
  A curriculum author deciding where the RLHF pipeline belongs in a training overview would link this page's assembled-pipeline passage, since it now points at the dedicated technique entry instead of leaving the term unlinked.
subject: "content/learn/how-models-are-trained.md"
reviewed:
  "content/learn/how-models-are-trained.md": "c576088e0eb61e9312c6fe345fb7f9c039d4de59f01ab22d492f63f6cd6fcb89"
---

Checked the register-judgement brief against the diff: the author kept the page's dashes (31 em-dashes + 2 semicolons both before and after, measured via node count — current file `emdash: 31 semi: 2 rlhf-link: true`, parent commit `parent emdash: 31 semi: 2`) and instead wired the one unlinked load-bearing term to its wiki entry. That is exactly the second branch the directive allowed (leave earning punctuation alone, defend one other improvement), not a quota edit and not an inversion.

Spot-checked the changed rows: `content/wiki/technique/rlhf.md` exists with `id: technique/rlhf` and `display_name: "Reinforcement Learning from Human Feedback"`, so the new front-matter `mentions: technique/rlhf` entry and the body link `[reinforcement learning from human feedback](/wiki/technique/rlhf)` both resolve; the loop's own gates on commit `f22915237f36` (`npm run build` PASS, `node scripts/verify-surfaces.mjs out` PASS) confirm no broken internal reference, and I did not re-run the suite. The surrounding InstructGPT/2017-robot prose already describes the same three-stage pipeline the wiki entry holds, so the link adds navigation without adding or moving any claim — nothing to source-check beyond the target's existence.

Scope: diff touches only the learn page plus the derived `backlinks.json` entry the build regenerates. No blog post is merged, so no voice answer is owed. No de-dashing band was derived, no stale adjacent-bead list was acted on.
