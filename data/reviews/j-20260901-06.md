---
job: j-20260901-06
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that OpenRouter's batch tier is being pruned model by model
  rather than withdrawn wholesale would link this entry alongside its sibling
  records: it fixes the delisting of mistralai/mistral-small-2603:batch to a
  date and a snapshot while the non-batch mistralai/mistral-small-2603 keeps
  serving, which is the sort of per-variant detail a pricing or availability
  argument needs and that a vendor changelog does not preserve.
subject: "content/wiki/model/mistralai-mistral-small-2603-batch.md"
reviewed:
  "content/wiki/model/mistralai-mistral-small-2603-batch.md": "8dcc91814d4365919da827d5dbb7d5ab46212ffbffb9eeb0e92c3bb3b8582e77"
---

Free-form notes.

**What the diff claims.** `mistralai/mistral-small-2603:batch` vanished from the
OpenRouter models feed; the entry is retired, its `feeds` binding and four
feed-bound facts removed, and one dated sourced timeline row added — plus the
derived files recomputed.

**Checks I ran (I did not re-run `npm test` / `npm run build`; the loop's PASS on
`89234bfd0ff9` stands).**

1. *Is the row actually gone from the live source?* Live fetch of the cited URL
   during this review:

   ```
   curl -s https://openrouter.ai/api/v1/models  -> 200, 695683 bytes
   grep -o '"mistralai/mistral-small-2603:batch"' -> 0 matches
   grep -o '"mistralai/mistral-small-2603"'       -> 2 matches
   grep -o '"id":"mistralai/[^"]*:batch"'         -> "id":"mistralai/mistral-medium-3-5:batch"
   ```

   The delisting is real, the non-batch sibling survives, and only one Mistral
   batch row remains live. This is a check of the whole document, not a
   head-truncated one.

2. *Is "last listed in the 2026-08-31 snapshot" true?* In-repo snapshots:
   `data/sources/openrouter-models/previous.json` — `"date": "2026-08-31"`,
   `row_count: 396`, and `grep -c '"mistralai/mistral-small-2603:batch"'` → **2**;
   `latest.json` — `"date": "2026-09-01"`, `row_count: 420`, same grep → **0**.
   The diff layer agrees: `data/changes.jsonl:112` carries a `retirement` record
   dated `2026-09-01` for `row_id mistralai/mistral-small-2603:batch` with
   `source_url https://openrouter.ai/api/v1/models`. The timeline row's date and
   source both check out, and 2026-09-01 is the local date of the authoring
   commit (`Tue Sep 1 02:16:06 2026 -0600`).

3. *Are the derived files consistent with the content change, and did anything
   stale get left behind?* Repo-wide grep for `mistral-small-2603`:
   `data/derived/catalog.json` now carries only the non-batch row
   (`entry_id model/mistralai-mistral-small-2603`, `row_id
   mistralai/mistral-small-2603`) — the batch row is gone from the catalog, as
   it must be once the `feeds` binding is removed. `data/derived/status-tables.json`'s
   four hits are all the `$retirement` change record itself (the deprecations
   strip), which is correct and unchanged. `freshness.json`'s
   `vanished_feed_rows` empties, which follows mechanically from the entry no
   longer declaring the row. No derived file still describes this row as a live
   active listing.

4. *Is the new queue item in `queue.json` legitimate rather than invented?* The
   added `carried-finding` item for `mistralai-mistral-medium-3-1-batch.md`
   derives from `data/carried/j-20260901-05-carry-1.md`, which is present in the
   tree (`ls data/carried/` → `README.md`, `j-20260901-04-carry-1.md`,
   `j-20260901-05-carry-1.md`). It is the previous review's carry surfacing on
   recomputation, not this job widening its scope. Count stays 5 → 5.

5. *Trailing newline* — the defect carried against two sibling entries.
   `tail -c 12 | od -c` prints `o n s :   [ ] \n - - - \n`: this file ends with a
   newline, and the diff shows no "\ No newline at end of file" marker.

**Scope.** Five files: the one target entry plus four derived artefacts that are
pure functions of it. `.job/brief.md` and `.job/source.json` are loop
scaffolding. Nothing else is touched; no reserved path is touched.

**On the repair shape.** Removing the `feeds` binding and the four bound facts
rather than leaving them to render last-known-with-as-of is the same shape the
five preceding cohort jobs used and that `j-20260901-05.pass2.md` approved; the
last-known rendering is the interim display for a row whose fate is undecided,
and this row's fate is decided and now recorded on the timeline. I am not
re-litigating a settled cohort decision on this job's diff.

Nothing to carry, and no proposal — the cohort-level idea already exists as
`data/proposals/batch-cohort-vanished-row-repairs.md`, so noting it again would
be manufacturing a field.
