---
job: j-20260904-12
verdict: approve
reasons: []
would-cite: >-
  Nobody links a front-matter line, so the citable thing here is the wiki entry
  the fix makes reachable in both directions: a reader arguing that OpenAI's
  Critical designation for Astra and its $1B defender subsidy are one
  Daybreak-gated story now lands on /wiki/concept/openai-daybreak and finds both
  posts in its "Appears in" rail, instead of an entry that pointed nowhere while
  two posts pointed at it.
subject:
  - "content/blog/openai-astra-critical-designation.md"
  - "content/blog/openai-daybreak-frontline-defenders.md"
reviewed:
  "content/blog/openai-astra-critical-designation.md": "0abe500db76c88e70a84a1c5854a8b2ecf3168bb3e67174ebcb8e939d2a1f561"
  "content/blog/openai-daybreak-frontline-defenders.md": "ad6ffdbebb9919ec81add4d1c65a566df4faa918434d520a54637ca190c0c752"
---

Verdict formed from measurement, not from the diff's stated intent. The claim
under test was narrow: does adding `- concept/openai-daybreak` to the two posts'
`mentions:` actually produce the backlink the carried finding said was missing?

**What I ran.** The worktree has no `node_modules`, so I could not import its
`lib/build-content.mjs` directly (`ERR_MODULE_NOT_FOUND: fast-glob`). Instead I
confirmed the branch's `lib/mentions.mjs` is byte-identical to the main
checkout's — `git hash-object lib/mentions.mjs` returns
`c5785d7a91c47cbda55afc14626f23d0ffbf1e2d` in both — and drove the main
checkout's `buildBacklinks`/`renderAppearsIn` over front matter parsed from the
**branch's** files. Output, verbatim:

```
content/blog/openai-daybreak-frontline-defenders.md => mentions: ["org/openai","concept/openai-daybreak"]
   body links daybreak: true
content/blog/openai-astra-critical-designation.md => mentions: ["org/openai","model/openai-gpt-5-6-sol","concept/openai-daybreak"]
   body links daybreak: true
entry id: "concept/openai-daybreak" kind: concept
backlink rows for concept/openai-daybreak: [
 {
  "url": "/blog/openai-astra-critical-designation",
  ...
 },
 {
  "url": "/blog/openai-daybreak-frontline-defenders",
  ...
 }
]
rail HTML: <aside class="rail rail-appears-in" aria-label="Appears in"><h2>Appears in</h2><ul><li><a href="/blog/openai-astra-critical-designation">…</a></li><li><a href="/blog/openai-daybreak-frontline-defenders">…</a></li></ul></aside>
```

So the ids parse (not a silent `mention:`/`mentions:` typo that would validate
into nothing), the id string matches the entry's own `id:` in
`content/wiki/concept/openai-daybreak.md`, the backlink map gains the key that
`data/derived/backlinks.json` lacked, and the rail that
`lib/render/entry.mjs:173` renders is non-empty. That is the finding closed, in
the direction it was reported.

**Completeness.** `Grep` for `wiki/concept/openai-daybreak` across
`content/blog/` returns exactly two files — the two this diff edits. There is no
third post carrying the body link without the mention, so the repair is whole
rather than partial.

**Reviewed-surface hashes — checked because this edit moves them.**
`lib/review-hash.mjs` exempts only `timeline` from the hash, so touching
`mentions:` changes both posts' reviewed surface, and specs/review makes a
mismatched piece a `verify-launch` failure. Measured with `reviewedHash`:

```
content/blog/openai-daybreak-frontline-defenders.md
  branch  : ad6ffdbebb9919ec81add4d1c65a566df4faa918434d520a54637ca190c0c752
  on main : 2b13f776a90b3c79089d231db955960e94f15bc933127422211bb51835bb28d4
content/blog/openai-astra-critical-designation.md
  branch  : 0abe500db76c88e70a84a1c5854a8b2ecf3168bb3e67174ebcb8e939d2a1f561
  on main : 756ec4205827e1c3f9f88412990beb1cab7c7ef29988bdee454c8b7777e3edbc
```

Both `on main` values equal the bindings recorded in
`data/reviews/j-20260904-10.md:67-68` exactly, which is the mechanism working:
the current binding is real and current. The branch moves both, and the merge
binds this record (`j-20260904-12`, higher seq on the same day) over
`j-20260904-10` under `resolveReviews`' recency rule — the same path job
j-20260904-10 itself took when it changed these two posts' bodies. Not a defect
of this diff; recorded so a later reader does not re-derive it.

**Scope.** `git diff --name-status main...HEAD` is five paths: the two posts,
the deleted `data/carried/j-20260904-10-carry-1.md` (which the brief requires
deleting in the same diff, and which is the only copy — no duplicate remains in
`data/carried/`), and the loop's own `.job/brief.md` and `.job/source.json`.
Nothing in `lib/`, `pulse/`, `loop/`, `scripts/` or `data/config.json`
(`git diff --stat main...HEAD -- lib scripts pulse loop data/config.json` prints
nothing). No widening.

I did not re-run `npm test` or `npm run build` — the loop recorded both PASS on
`6dfe537f96f0`, the commit this diff was computed from.

Carrying nothing and proposing nothing: the diff is four added/removed lines
doing exactly what the finding asked, and I found no second thing worth a
later run's minutes.
