---
job: j-20260904-18
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a diffusion-LM vendor's speed pitch has to be read
  against its preview caveats would link this entry: it is the one page that
  puts Inception's own throughput and cost claims next to the text-only
  developer path, the non-contractual preview alias, and the fact that
  OpenRouter's launch rate is an 80% promotional discount rather than
  Inception's list price — the exact set of qualifiers a "Mercury is 7x
  cheaper" post drops.
carry:
  - title: Attribute the drop-in-replacement quote to the homepage, not to "Inception's pages"
    detail: >-
      The corrected sentence reads: Inception's pages say its models are
      "OpenAI API compatible and a drop-in replacement for traditional LLMs".
      Fetching both pages with curl on 2026-09-04 (HTTP 200, 457,931 and
      320,999 bytes), the homepage carries that sentence verbatim — "Our models
      are OpenAI API compatible and a drop-in replacement for traditional
      LLMs." — but the models page carries the SAME sentence without "API":
      "Our models are OpenAI compatible and a drop-in replacement for
      traditional LLMs." (The models page does say "OpenAI API compatible" in a
      different sentence, the one about AISuite/LiteLLM/LangChain.) So the
      plural attribution is one page too wide for the string inside the
      quotation marks. Changing "Inception's pages say" to "Inception's
      homepage says" — or linking the homepage on that clause the way the
      earlier paragraph already does — would make the named document the one
      quoted.
    subject: content/wiki/model/inception-mercury-2-5-preview.md
subject: "content/wiki/model/inception-mercury-2-5-preview.md"
reviewed:
  "content/wiki/model/inception-mercury-2-5-preview.md": "c259ce43db34faf22e3680982799b674feb00647951794e52fc45041cb4227bd"
---

Both carried findings are fixed and both carried files are deleted in the same
commit, which is what the brief asked for.

**Finding 2 — the quote.** Fetched both vendor pages directly rather than
trusting the carried finding's transcription:

```
$ curl -sL --max-time 45 https://www.inceptionlabs.ai/ ... -w "%{http_code} %{size_download}\n"
200 457931
200 320999
```

Grepping the raw HTML (not an extractor's prose) for the phrasing:

```
$ grep -o -i "OpenAI[^\"<]\{0,90\}compatible[^\"<]\{0,90\}" incep-home.html | sort -u
OpenAI API compatible and a drop-in replacement for traditional LLMs.
--- models ---
OpenAI API compatible and are supported through libraries including AISuite, LiteLLM, and LangChain.
OpenAI compatible and a drop-in replacement for traditional LLMs.
```

With surrounding markup, the homepage sentence is
`<p ...>Our models are OpenAI API compatible and a drop-in replacement for
traditional LLMs.</p>` — so the edited line now matches the homepage word for
word. That is the substantive fix and it is correct. The wrinkle is that the
models page's copy of the same sentence has no "API" (raw markup:
`<p ...>Our models are OpenAI compatible and a drop-in replacement for
traditional LLMs.</p>`), which means the pre-fix text was also findable on a
vendor page and the surviving plural "pages" is now slightly wide. Not worth a
`revise` on a one-word repair — carried above.

**Finding 1 — trailing newlines.** The entry now ends `...independently."\n`:

```
$ tail -c 20 content/wiki/model/inception-mercury-2-5-preview.md | xxd | tail -2
00000000: 6e67 6520 696e 6465 7065 6e64 656e 746c  nge independentl
00000010: 792e 220a                                y.".
```

The finding's second file, `data/proposals/context-window-transclusion-unit-doubling.md`,
is not in the diff, and I checked why rather than assuming it was skipped: it no
longer exists at that path — it was consumed on 2026-09-03 and now lives at
`data/proposals/consumed/context-window-transclusion-unit-doubling.consumed-20260903T163209.md`,
whose last bytes are `never be written about again.\n`. The newline is already
there, so there was nothing left to fix and nothing left standing. (`RESULT.md`
is absent from this review worktree because `.gitignore:78` ignores `/RESULT.md`
— that is the checkout, not a missing report.)

**Scope.** Four files: the entry, the two carried files, and `data/launch.json`.
The `launch.json` rows are re-measured payload figures (`/` html 8.7 → 8.6,
`/catalog` inline 18.1 → 18.0) — written by `scripts/verify-design.mjs`, which is
the only writer of those keys, so they are a byproduct of running a gate rather
than authored content. The immediately preceding repair job (`7899171`,
j-20260904-17) carried the same kind of `launch.json` update in its own commit,
so this is established practice here and not a widening of the outcome. No
reserved path is touched; `data/derived/queue.json` still names the two deleted
carried files but is recomputed every Pulse run, which is exactly the mechanism
the brief describes for retiring the items.

I did not re-run `npm test` or `npm run build` — the loop recorded both PASS on
this commit.
