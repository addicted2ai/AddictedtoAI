---
job: j-20260914-10
verdict: reject
reasons:
  - scope-violation
would-cite: >-
  Someone deciding whether to keep a repair job that answered nothing after its revision would reference this rejection: the expiry answer and the answered-record rename already exist from the earlier job, so merging scaffolding alone preserves no work and the stale queue item disappears on the next recomputation.
---

## Verdict

`reject` under `scope-violation`. The delta since pass 1 (`121634b..HEAD`, commit `4d68354`) reverts the two derived hunks, so the diff under review (`e20cd17..HEAD`) is now only `.job/brief.md` plus `.job/source.json` — scaffolding the merge removes — with still no edit to `content/wiki/model/nex-agi-nex-n2-pro.md` and no move of a vanished record. Both halves of the stated outcome remain omitted, and the required work already lives on the base from `j-20260914-09` (`ceca8ea`, approved in `j-20260914-09.md`). This is the second non-approval, so the job is discarded.

No `content/blog/` file is in this diff, so the voice question is not triggered and neither approve branch for a post applies.

## What I checked

Scope: `git -C "D:\addictedtoai-worktrees\j-20260914-10-review-2" diff e20cd17..HEAD --name-only` reports exactly:

```
.job/brief.md
.job/source.json
```

`git show HEAD --stat` reports the revision `4d68354` touches only `data/derived/backlinks.json` (8 deletions) and `data/derived/search-index.json` (1 insertion, 1 deletion) — the exact revert of `121634b`. The loop's `npm run build` PASS and `verify-surfaces` PASS on `4d68354` were taken as run, not re-run, per the brief. Targeted checks only, quoted below.

1. Retirement state (filesystem, measured). Targeted node script printed `PENDING_EXISTS=false` and `ANSWERED_EXISTS=true`. `data/vanished/` lists only `answered/`, `openrouter-models--nousresearch-hermes-4-70b.md`, `openrouter-models--openai-gpt-latest.md`, `README.md`; `data/vanished/answered/` contains the `nex-n2-pro` record. There is still nothing for this job to MOVE.

2. Entry already answers (measured). The worktree entry carries the full `j-20260914-09` body with `FEEDS_KEPT=true` (still `openrouter-models: nex-agi/nex-n2-pro`) and `HAS_BODY=true`. The fix was present at `e20cd17` and this diff does not touch it.

3. Derived revert (measured). The same script printed `BACKLINK_KEY_PRESENT=false` for `model/nex-agi-nex-n2-5-pro-free`, and a follow-up printed `ROW={"u":"/wiki/model/nex-agi-nex-n2-pro",...,"b":true}` — i.e. the revision undid pass-1's correct backlink and stub-flag flip and restored the stale base derivation. That staleness is a build-recomputation matter, not this job's outcome; the point for scope is that the revision removed the only non-scaffolding hunks rather than adding the missing entry/vanished work.

## Corroboration

The duplicate-of-answered classification rests on two different instruments: the filesystem state (pending absent, answered present) and the worktree entry content (fix with binding kept, already present at the base). The derived revert observation shares one instrument (the checked-in build derivation) with pass 1's derived observation: agreement, never corroboration.

## Polarity

The work source required: establish retire/rename/tier-fold/delist from vendor+publisher sources and write it on the page (done by `j-20260914-09`, still untouched by this diff); do NOT remove the entry or its binding (satisfied vacuously — this diff removes nothing); MOVE the record to `answered/` unchanged in the same diff (already done on the base; this diff moves nothing). Nothing is inverted. The failure remains omission plus wrong paths, which is `scope-violation`: the diff touches only `.job/` scaffolding while the outcome names the entry and the vanished file.

Discarding this job loses no work: the answer, the binding, and the answered record all survive on the base, and the next Pulse queue recomputation (which reads `data/vanished/` pending only) will drop the stale `openrouter-models:nex-agi/nex-n2-pro` item without this diff.
