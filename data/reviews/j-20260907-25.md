---
job: j-20260907-25
verdict: revise
reasons:
  - spec-violation
would-cite: >-
  A maintainer could link this entry when explaining why BytePlus's externally
  linked Seed endpoint belongs to the ByteDance Seed team while an unqualified
  Seed name is not used as a catalog join.
---

Free-form notes: The first carried finding is fixed: the target declares
`publishes_from: [byteplus.com]`, and the two carried files are deleted. The
official ByteDance Seed model page (`https://seed.bytedance.com/en/seed2_1`)
was fetched today; its page title is `ByteDance Seed`, it identifies the
Seed2.1 family, and its text says, “The Seed2.1 model family is officially
released, offering two AI productivity models in different sizes: Pro and
Turbo.” Its `Try it` and `API` links both point to `ai.byteplus.com` (lines 8,
12 and 17 in the fetched page). The actual
`checkPublishesFromValue("byteplus.com")` direct check returned `{ "ok": true }`.

The second finding is only partly fixed. The `Seed` alias is gone from the
entry, `data/derived/aliases.json` has no `Seed` record, and the target's
search-index row retains only `ByteDance Seed Team`. However, the actual shared
`nameTokens` implementation still tokenizes the unchanged display name
`ByteDance Seed` into `seed`. A direct execution of `nameTokens`,
`registrableDomainOfUrl` and `isSubjectOwnedUrl` against a host whose
registrable label is exactly `seed` returned:

```text
tokens: ["bytedance", "bytedanceseed", "bytedanceseedteam", "seed", "team"]
bare_seed: {"domain":"seed.example","label":"seed"}
bare_seed_owned: true
byteplus_owned: true
seed_subdomain: {"domain":"bytedance.com","label":"bytedance"}
seed_subdomain_owned: true
```

Therefore a registrable domain whose bought label is `seed` remains treated as
owned by this entry even after the alias deletion. The required revision is to
close that ownership-token path as well (while preserving the intended
ByteDance/ByteDance Seed provider match), then regenerate any affected derived
data and show a direct check that the bare `seed` ownership case is false. The
current diff does not establish that, so it does not clear the carried finding.

For the board half, I evaluated the `matchProviders` implementation's exact
normalisation and containment expression against the real
`data/derived/catalog.json` rows plus a synthetic `seed-future` provider. The
result was:

```text
current: ["bytedance", "bytedance-seed"]
current_with_future: ["bytedance", "bytedance-seed"]
old_with_future: ["bytedance", "bytedance-seed", "seed-future"]
```

That confirms the alias deletion closes the board-join widening while retaining
the two intended current providers; it does not close the ownership widening
because the display-name token remains.

Focused repository checks: `git diff --check` passed. The branch's four
pre-review gates were not rerun, per the brief. The focused Node tests could not
start because this review worktree has no `node_modules` directory; no install
or other mutation was attempted.
