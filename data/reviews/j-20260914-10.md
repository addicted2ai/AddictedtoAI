---
job: j-20260914-10
verdict: reject
reasons:
  - scope-violation
would-cite: >-
  A maintainer triaging a stale derived queue that re-selected an already-answered vanished row would cite this record: it shows the paid listing's expiry answer already lives on the entry with its binding kept, so the duplicate job's derived-only churn adds no prose and should be discarded while the next Pulse recomputation clears the item.
---

## Verdict

`reject` under `scope-violation`. The diff under review (`e20cd17..HEAD`: `data/derived/backlinks.json`, `data/derived/search-index.json`, plus `.job/` scaffolding) omits both halves of the stated outcome and touches only paths outside it: no edit to `content/wiki/model/nex-agi-nex-n2-pro.md`, no move of a vanished record. The required work already lives on the base branch from `j-20260914-09` (`ceca8ea`: 60-line entry prose, pure rename to `data/vanished/answered/`), approved in `j-20260914-09.md`. This job re-selected the same subject from a stale `data/derived/queue.json` and committed only the delayed derived recomputation of that prior fix.

No `content/blog/` file is in this diff, so the voice question is not triggered and neither approve branch for a post applies.

## What I checked

Scope: `git diff e20cd17..HEAD --name-only` in the review worktree reports exactly `.job/brief.md`, `.job/source.json`, `data/derived/backlinks.json`, `data/derived/search-index.json`. The loop's `npm run build` PASS and `verify-surfaces` PASS on `121634b` were taken as run, not re-run, per the brief. Targeted checks only, quoted below.

1. Retirement state (filesystem, measured). `data/vanished/` holds only `README.md`, `openrouter-models--nousresearch-hermes-4-70b.md`, `openrouter-models--openai-gpt-latest.md` — no pending `openrouter-models--nex-agi-nex-n2-pro.md`. `data/vanished/answered/openrouter-models--nex-agi-nex-n2-pro.md` exists. Targeted node script printed `PENDING_EXISTS=false` and `ANSWERED_EXISTS=true`. There was nothing for this job to MOVE; the brief's retiring step is already satisfied on the base.

2. Entry already answers (measured). `e20cd17:content/wiki/model/nex-agi-nex-n2-pro.md` already carries the full `j-20260914-09` body (expiry-date departure, N2.5 family arrival eleven seconds apart, vendor 404/empty-endpoints state, weights still published, binding kept). Script printed `FEEDS_KEPT=true`, `HAS_BODY=true`, `MENTIONS_FREE=true`. `IN_LATEST=false`, `IN_PREV=false` with `LATEST_DATE=2026-09-14`, `PREV_DATE=2026-09-12`.

3. Derived hunks are correct but unrequested (measured). `node -e` over the worktree printed `has-key true` with `[{"url":"/wiki/model/nex-agi-nex-n2-pro",...,"file":"content/wiki/model/nex-agi-nex-n2-pro.md"}]` for `model/nex-agi-nex-n2-5-pro-free` — the correct backlink for the `mentions:` added by `j-20260914-09` — and `{"u":"/wiki/model/nex-agi-nex-n2-pro",...,"b":false}` — the correct stub-flag flip for an entry that now has a body (`lib/search-index.mjs: b from doc.index?.stub`). Both share one instrument (the build's derivation from the already-fixed entry): agreement, never corroboration.

## Corroboration

The two derived changes agree through one instrument (one build derivation of one prior content edit). The classification that matters here (duplicate of an answered repair) rests on two different instruments: the filesystem state (pending absent, answered present) and the base-branch content (fix already present at `e20cd17`).

## Polarity

The work source required: establish retire/rename/tier-fold/delist from vendor+publisher sources and write it on the page (already done by `j-20260914-09`, ignored by this diff); do NOT remove the entry or its binding (satisfied vacuously — this diff removes nothing); MOVE the record to `answered/` unchanged in the same diff (already done on the base; this diff moves nothing). Nothing is inverted — no deletion, no loosening, no rewiring. Template scope-negations do not apply. The failure is omission plus wrong paths, which is `scope-violation`: the diff touches only `data/derived/*` while the outcome names the entry and the vanished file.

Discarding this job loses no work: the answer, the binding, and the answered record all survive on the base, and the next Pulse queue recomputation (which reads `data/vanished/` pending only) will drop the stale `openrouter-models:nex-agi/nex-n2-pro` item without this diff.
