---
job: j-20260912-01
verdict: approve
reasons: []
would-cite: >-
  A reply to anyone citing the Bitter Lesson as settled pro-scaling lore —
  it keeps the six-day Brooks rebuttal and Sutton's own 2025 pushback on the
  same page as the essay, now without the page making claims about who reads
  what.
subject: "content/wiki/concept/the-bitter-lesson.md"
reviewed:
  "content/wiki/concept/the-bitter-lesson.md": "cd989508e4a278767713269957bb27d0a93ea34753ea11195c80a21073fd3470"
---

Notes: reviewed commit 676b4e10f781 (HEAD), the only author commit on this
branch. `git show --stat HEAD` reports exactly 1 file changed:
`content/wiki/concept/the-bitter-lesson.md`, 4 insertions / 5 deletions. The
wider `main...HEAD` stat in this worktree lists dozens of files, but those are
ancestors from a stale base (prior jobs, pulse data, derived outputs) — the
author's own commit is single-file and in-scope, so no `scope-violation`.

Polarity: implements, not inverts. The brief offered DROP / ATTRIBUTE /
explicit-editorial-voice and required one applied consistently. The author
picked ATTRIBUTE throughout:

- line 71: "rarely read" -> "rarely cited alongside the essay in the sources
  surveyed here" (the bead's suggested form verbatim).
- lines 98-100: "the most-quoted argument in favour of the current paradigm"
  -> "an argument often invoked in favour of the current paradigm in the
  sources surveyed here".
- lines 99-100: "most of the people quoting it have never seen" -> "that is
  rarely cited alongside it in those sources".

Checked: `Select-String` for `rarely read|most-quoted|never seen|less read`
against the file returns no matches — all three unmeasurable world-readership
claims are gone, and the "soften to less read" trap the brief forbade was not
used. No citation was invented; the surrounding argument (Brooks six
objections, Sutton dispute, "worth reading all three primary documents") is
intact. The retained "six days later" / "within the week" date claims were
already byte-verified in seed-the-bitter-lesson.md (Brooks 19 March 2019, six
`<li>` objections) and are untouched.

Checklist spot-check: the new scoped claims are claims about this page's
surveyed sources rather than about the world, which is what makes them
checkable rather than guessed — exactly the property the brief required of
the ATTRIBUTE branch. No `content/blog/*.md` file is in the diff, so no
`reads-human` / `reads-human-from` entry is required. Build and surfaces were
verified by the loop on this commit (both PASS) and were not re-run per the
brief; the only command run here was the read-only `git show` / phrase grep
quoted above.

Corroboration: single-instrument agreement only (one file read plus one grep
over the same bytes) — recorded as agreement, not corroboration.
