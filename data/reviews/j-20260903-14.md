---
job: j-20260903-14
verdict: revise
reasons: [false-or-unsupported-claim]
would-cite: >-
  Someone arguing that "open weights are coming" should be dated against the
  vendor's own words rather than a catalogue's badge would link the org entry's
  flagship_weights fact: after this job it quotes Meta's own 2026-09-02
  sentence ("the Muse Spark open weights release") instead of a third-party
  card, which is the difference between "a tracker says pending" and "Meta says
  it is still on the roadmap." That is a genuinely better anchor and it is why
  this is a revise on one clause, not a reject.
proposal:
  slug: repair-muse-spark-1-3-license-fact-third-party-only
  type: repair
  date: 2026-09-03
  summary: >-
    content/wiki/model/meta-muse-spark-1-3.md's `license` fact (line 48-53)
    still reads "Open weights · Meta license (weights pending); weights not
    released", cited solely to https://llm-releases.com/models/muse-spark-1-3,
    and its 2026-09-02 timeline row (line 55-57) restates the same badge from
    the same single source. Job j-20260903-14 moved the ORG entry off that
    badge and onto Meta's own channel but left the MODEL entry untouched —
    correctly, since its brief named only the two org facts. A repair job
    should carry the same anchoring across: re-cite the model entry's license
    fact to research.meta.ai/blog/introducing-muse-spark-1-3, state the badge
    as the catalogue's characterisation rather than as the licence, and check
    meta-muse-spark-1-2 and -1-1 for the same shape.
  evidence: >-
    Read from this branch 2026-09-03: content/wiki/model/meta-muse-spark-1-3.md
    lines 48-57, both facts source_url'd to llm-releases only. Fetched
    2026-09-03 by this review: https://research.meta.ai/blog/introducing-muse-spark-1-3
    (HTTP 200) contains "license" 0 times and carries the roadmap sentence "We
    have an exciting roadmap lined up, including bigger models, the Muse Spark
    open weights release, and more. Stay tuned." — i.e. the same Meta-side
    anchor this job used for the org entry is available and unused on the model
    entry.
carry:
  - title: Note Meta's own 1.3 roadmap line in the org entry's prose body
    detail: >-
      The body still frames the pending state as resting on the catalogue
      alone — "A month of 'pending' spanning two releases, with nothing
      downloadable, is its own fact — and the catalogue contradicts itself
      about what the flag means" — and never mentions the one genuinely new
      thing this job found, that Meta itself named "the Muse Spark open weights
      release" on its roadmap on 2026-09-02. The flagship_weights fact is not
      transcluded in this entry (only in model/meta-muse-glimmer-30b.md:114),
      so a reader of the org page never sees it. One sentence in the second
      paragraph, citing research.meta.ai/blog/introducing-muse-spark-1-3, would
      fix it. Not required here because it is a body-prose widening beyond the
      two facts the brief named.
    subject: content/wiki/org/meta-superintelligence-labs.md
---

## Verdict

**Revise.** The substance of this job is correct and independently reproduced —
the one clause it appended to the second fact is not supported by that fact's
own source, and it renders into published prose.

## What I ran

I did not re-run `npm test` / `npm run build`; the loop recorded both PASS on
`d16d4a4aaf86`. I re-fetched the primary sources myself, with a raw
`fetch` + tag-strip + literal-substring script (not WebFetch's extractor), all
on 2026-09-03.

### The claim that holds — `flagship_weights`

`https://research.meta.ai/blog/introducing-muse-spark-1-3` — HTTP 200, raw
188043 bytes, stripped 10663:

```
TERM "open weights release" hits=1
  ...long-horizon agentic tasks. Looking Forward We have an exciting roadmap
  lined up, including bigger models, the Muse Spark open weights release, and
  more. Stay tuned...
TERM "license" hits=0
TERM "pending" hits=0
TERM "September" hits=1
  ...Introducing Muse Spark 1.3 September 2, 2026 · 3 minute read...
```

So the new value — `"not released; Meta's 1.3 announcement (2026-09-02) names
'the Muse Spark open weights release' on its roadmap"` — is exact on the quote,
exact on the date (the page's own dateline is "September 2, 2026"), and the
source_url change to research.meta.ai is the right move. The supporting
negatives reproduce too:

- `research.meta.ai/blog/introducing-muse-code-and-muse-spark-1-2` (HTTP 200,
  stripped 5535): `weights` hits=0, `open` hits=0, `license` hits=0 — the 1.2
  announcement says nothing about weights, as the evidence file states.
- `about.fb.com/news/2026/04/...` (HTTP 200, stripped 14299): `weights` hits=0,
  `open-source` hits=1 — "we hope to open-source future versions of the model"
  is still live. The corpus's older cited value was not wrong; it was just
  five months stale, and moving off it is an improvement.
- `huggingface.co/meta-models` (HTTP 200): `Spark` hits=0, `spark` hits=0 —
  no Muse Spark repo; the org carries only the Glimmer family.

The honest "it has not moved" finding is the right result and it is recorded as
such rather than dressed up. `accessed:` stayed 2026-09-03, which is the real
date the check ran. Scope is clean: three files, two of them the evidence
capture, and the one content change touches exactly the two facts the brief
named. `RESULT.md`'s absence from the diff is not a defect — `.gitignore:78`
carries `/RESULT.md`.

### The claim that does not hold — `flagship_weights_listing`

The value gained the clause `— a badge name that no Meta license text carries`.
Its `source_url` is unchanged: `https://llm-releases.com/models/muse-spark-1-3`.
I fetched that page (HTTP 200, stripped 2130):

```
TERM "Meta license" hits=2
  ...Specifications License Open weights · Meta license (weights pending)
  Weights Not released Architecture unknown...
TERM "weights pending" hits=2
TERM "Not released" hits=1
```

The page supports the badge quotation and nothing else. It says nothing about
what any Meta license text does or does not carry. A `cited` fact renders with
its source reachable and the reviewer's named check is that *the source says
what the fact says*; here half the value has no reachable support at all.

Two things make this worse than a stylistic nit:

1. **It renders as published prose.** The value is transcluded mid-sentence at
   `content/wiki/org/meta-superintelligence-labs.md:88`, so the page now reads:
   "…was the first Spark flagship listed 'Open weights · Meta license (weights
   pending)' with weights 'Not released' — a badge name that no Meta license
   text carries; the card added that 'Meta has signaled open weights are
   coming.'" An em-dash aside colliding with a semicolon, and the reader's only
   link is to a page that does not make the claim.
2. **The same point is already made two sentences later, properly sourced.**
   Lines 92-97 already say the catalogue "contradicts itself about what the flag
   means", citing `llm-releases.com/changelog`. The appended clause is
   redundant with sourced prose.

And the claim as written is a universal negative — "no Meta license text" —
drawn from a bounded search.

### The evidence file overstates its own transcript

`verify-meta-spark-weights-pending-status.md` says of the license-name check:
"Checked instruments: the HF license tag on Meta's only open-weight repo …; the
HF models filter `other=meta-license` …; the meta-models and meta-llama org
pages …; both Meta announcement pages …; llm-releases' own methodology page".
The raw transcript beside it records **seven** fetches and four of those
instruments are not among them: `meta-models/Muse-Glimmer-30B`,
`models?other=meta-license`, `meta-llama`, and `/methodology` appear nowhere in
`verify-meta-spark-weights-pending-status.raw.txt` (46 lines, read in full).

To be fair to the author, I ran all four and **they all hold**:

```
=== https://huggingface.co/meta-models/Muse-Glimmer-30B HTTP 200 ===
TERM "apache-2.0" hits=13   ..."license:apache-2.0"...  "License: Apache 2.0"
TERM "meta-license" hits=0
=== https://huggingface.co/models?other=meta-license HTTP 200 STRIPPED 1904 ===
TERM "meta-license" hits=2   (both the filter chip and "Active filters:
                              meta-license" — no model rows)
TERM "Muse" hits=0
=== https://llm-releases.com/methodology HTTP 200 ===
TERM "known family" hits=1
  ...Does the license resolve to a known family? Each check carries a
  severity and feeds a confidence score...
=== https://huggingface.co/meta-llama HTTP 200 ===
TERM "Muse" hits=0   TERM "Spark" hits=0
```

So this is not an invented finding. It is an uncaptured one, on the checklist
item that reads "there is evidence the steps were actually executed" — and it is
uncaptured for precisely the sentence that made it into the corpus.

## Required changes

1. **Remove `— a badge name that no Meta license text carries` from
   `flagship_weights_listing`'s value** (`content/wiki/org/meta-superintelligence-labs.md:25`),
   restoring the value to what its cited page supports. If the finding is worth
   keeping in the entry, it belongs in the body where it can carry its own
   citation, not inside a value whose only link is the catalogue card.
2. **If any form of the license-name finding is kept**, state what was actually
   searched rather than a universal negative — e.g. "no licence text of that
   name appears on Meta's announcement pages, the `meta-models` or `meta-llama`
   HF orgs, or HF's `meta-license` tag (checked 2026-09-03)" — and put those
   four fetches in `…raw.txt` so the transcript matches the narrative.
3. **Bring `verify-meta-spark-weights-pending-status.md` into line with its
   transcript**: either add the four missing fetches to the raw capture, or say
   plainly which instruments were run without being captured. Nothing else in
   the evidence file needs to change; every other claim in it reproduced.

Nothing else is required. `flagship_weights` should stay exactly as written.

## Rejection index

`data/proposals/rejected/` is empty, so there is nothing this could be a
re-tread of. On the judgment half: the proposal this job came from asks a
question the corpus could not answer from its own contents — whether Meta's own
channels say anything about the Spark weights — and the answer (a September
roadmap sentence that no file here contained) could not have been produced
without leaving the repository. It is not a re-worded repeat of anything.
