---
job: j-20260908-10
verdict: approve
reasons: []
would-cite: >-
  A reader tracking temporary free access would link this entry when asking
  whether OpenRouter's `:free` label meant MiniMax M3 itself disappeared: it
  pins the catalog disappearance to 2026-09-08, shows the free route has no
  providers while the standard and batch variants remain, and connects the
  timing to the dated MiniMax Week offer without pretending a vendor retirement
  notice exists.
subject: "content/wiki/model/minimax-minimax-m3-free.md"
reviewed:
  "content/wiki/model/minimax-minimax-m3-free.md": "27a9ef5dc1bff8715066f37699d173648449432c50888c0b027b94f81835ccd4"
---

Free-form notes: I reviewed the branch diff against the stated repair only. The
substantive changes are the target wiki entry, its generated search-index row,
and the required move of the vanished record; `.job/brief.md` and
`.job/source.json` are the loop's job metadata. `git diff --stat main...HEAD`
reported 5 changed paths, and `git diff --check main...HEAD` produced no
output.

The target still contains `feeds: openrouter-models:
minimax/minimax-m3:free`, and the vanished record moved with `R100` similarity
from `data/vanished/openrouter-models--minimax-minimax-m3-free.md` to
`data/vanished/answered/openrouter-models--minimax-minimax-m3-free.md`. It was
not deleted. The branch snapshots show the row in `previous.json` dated
`2026-09-07` and absent from `latest.json` dated `2026-09-08`; the target's
front matter and the moved record agree on the id and binding.

Targeted live checks, run during this review, returned these observations:

```
OpenRouter /api/v1/models: minimax/minimax-m3 and
  minimax/minimax-m3:batch present; minimax/minimax-m3:free absent
OpenRouter /api/v1/models/minimax/minimax-m3:free/endpoints:
  {"data":{"id":"minimax/minimax-m3:free","endpoints":[]}}
OpenRouter /api/v1/models/minimax/minimax-m3-20260531/endpoints:
  populated endpoint list, including CoreWeave, GMICloud, and other providers
OpenRouter human page https://openrouter.ai/minimax/minimax-m3:free: HTTP 200,
  page contains the free slug
GMI Cloud MiniMax Week: HTTP 200; campaign date text 2026.08.24–2026.09.06,
  “Free for 14 days”, “Standard pricing applies afterward”, and an OpenRouter
  link that resolves to https://openrouter.ai/minimax/minimax-m3:free
MiniMax M3 page: HTTP 200; contains “MiniMax M3”, “API”, and “MiniMax-M3”
```

The changed prose accurately distinguishes the still-rendered human page from
an actually populated route. GMI Cloud's first-party campaign page supports
the time-limited offer and its end date, while MiniMax's first-party page still
lists M3 and its API. Those sources support a withdrawal of the free route after
the offer, not a claim that MiniMax retired or renamed M3; the prose explicitly
says that no more specific withdrawal notice is available. The derived
`search-index.json` change from `"b": true` to `"b": false` is also correct:
`lib/search-index.mjs` defines `b` from `doc.index?.stub`, and adding a body
makes this entry no longer a stub. The supplied test, build, surface, and
design gates were not re-run, per the review instructions; no blog post is in
the diff, so no voice carry-forward is required.
