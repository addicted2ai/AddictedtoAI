---
job: j-20260907-31
verdict: approve
reasons: []
would-cite: >-
  A maintainer diagnosing false-red job gates would link this proposal when
  explaining that pre-executor node_modules linking has landed while teardown
  still mishandles a real directory.
---

Free-form notes: The changed proposal now accurately marks the author and
revision link calls as already landed and limits the remaining work to
teardown safety. The direct check
`rg -n "linkNodeModules\\(|unlinkNodeModules\\(" loop/run.mjs loop/lib/gates.mjs`
reported link calls at `loop/run.mjs:300`, `loop/run.mjs:712`, and
`loop/lib/gates.mjs:300`, and unlink callers at `loop/run.mjs:327`, `:728`,
`:1272`, and `:1336`. The helper check reported
`loop/lib/gates.mjs:157: if (existsSync(link)) return { linked: false, why: 'already present' };`.
The teardown source still shows `st.isSymbolicLink() || st.isDirectory()` at
line 172, `unlinkSync(link)` at line 173, and the swallowing catch, supporting
the proposal's remaining-hazard claim.

The proposal's evidence cites those current call sites and helper lines. The
carried finding and duplicate proposal are both absent; `Test-Path` returned
`False` for each. `git diff --check main...HEAD` was clean. I did not rerun the
full gates because the brief records them as already passed on this commit.
