---
job: j-20260901-20
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a diff-history annotation must say what it measured
  rather than what it implies would link this line: it is the same claim about
  Nex-N2-Pro's deprecation, narrowed by one word to the evidence the file
  actually holds — a change record that spans three days cannot speak for a row
  created months before it.
---

Free-form notes.

**Scope.** The branch diff is four files: `.job/brief.md` and `.job/source.json`
(loop artifacts), the deletion of `data/carried/j-20260901-13-carry-1.md` (the
retirement the brief explicitly requires in the same diff), and a one-line
change in `data/changes.jsonl`. The content change is exactly the insertion of
`recorded` into one annotation. Nothing wider.

```
 .job/brief.md                         | 698 +++++++++
 .job/source.json                      |   8 +
 data/carried/j-20260901-13-carry-1.md |  16 -
 data/changes.jsonl                    |   2 +-
```

**I did not re-run `npm test` / `npm run build`** — the loop ran both on
`9a6810a4fcfe` and both passed. Instead I ran the checks that would be red if
the specific claims here were false.

**JSONL integrity.** Wrote a throwaway node script against
`data/changes.jsonl` and parsed every line (the edited line is the last one in
the file, so a botched escape would land exactly there):

```
lines 145 parsed 145 bad 0
```

**The finding's own measurement, re-measured.** The carried finding claimed the
openrouter-models change history spans three days and that the row's `created`
timestamp predates that window by months. Both hold:

```
openrouter-models lines by date: { '2026-08-29': 29, '2026-08-31': 1, '2026-09-01': 38 }
nex-n2-pro lines: 2
  2026-09-01 field_change status ...|nex-agi/nex-n2-pro|status created=1780937140 exp="2026-09-04"
  2026-09-01 annotation ...|nex-agi/nex-n2-pro|status
```

`1780937140` is ~2026-06-08, roughly twelve weeks before the earliest recorded
line. So the unqualified "the row's first status flip" was indeed a statement
about the diff history, not about the row — and `recorded` is the precise word
that makes it one.

**The narrowed claim is true.** Every status-field change this file records on
this source:

```
status-field changes on openrouter-models: 4
  2026-08-29 z-ai/glm-4.5v         "deprecated" -> "active"
  2026-08-31 moonshotai/kimi-k2.5  "deprecated" -> "active"
  2026-09-01 nex-agi/nex-n2-mini   "active" -> "deprecated"
  2026-09-01 nex-agi/nex-n2-pro    "active" -> "deprecated"
```

`nex-agi/nex-n2-pro` appears once, on 2026-09-01 — so this is its first
recorded status flip, and the two earlier flips on the source belong to other
rows and run the other direction (`deprecated -> active`), consistent with the
sibling annotation's "both of which were dropped expiration dates".

**Reference held.** The annotation's `annotates` key
(`openrouter-models|13514954c61b0211|a9e815e403b4d7a5|nex-agi/nex-n2-pro|status`)
still joins the `field_change` row printed above — the edit touched the `text`
field only.

**Retirement.** `data/carried/j-20260901-13-carry-1.md` is deleted in the same
diff, which is what the brief says removes the queue item. Confirmed it is the
only file under `data/carried/` touched.

Nothing to propose and nothing to carry.
