---
job: j-20260904-55
verdict: approve
reasons: []
would-cite: >-
  Whoever picks up the machinery job this proposal describes: the summary is now
  the complete list of what `publishStep` can hand back, so step 2 can be built
  by carrying `reason` through rather than by re-deriving which non-publish
  outcomes exist. Anyone arguing that a proposal's enumeration should be counted
  in the source rather than recalled from a log would link the evidence
  paragraph, which names the return sites and says why seven returns produce
  nine values.
---

Free-form notes.

## What the diff claims, and what I measured

Every claim in the changed prose is a claim about `pulse/lib/publish.mjs`, which
this diff does not touch, so each is checkable directly. I read the file in this
worktree at `pulse/lib/publish.mjs` and ran the enumerating greps whole (no
`head` on a counting command).

`grep -n "published:"` over the file returned exactly eight lines:

```
493:    return { published: false, reason: 'no-config' };
565:    return { published: false, reason: 'disabled', commit };
570:    return { published: false, reason: 'hold', commit };
575:    return { published: false, reason: commitBlocked, commit, ...(commit.foreign ? { foreign: commit.foreign } : {}) };
623:    return { published: false, reason: 'dry-run', commands, poll: statusUrl(), commit };
634:    return { published: false, reason: 'nothing-owned', commit };
699:        return { published: true, stamp: id, commit: expected, committed: commit, indexnow };
714:  return { published: false, reason: 'stamp-did-not-advance', hold: file, expected, last_seen: lastSeen ?? null, commit };
```

That is the evidence paragraph's sentence line for line: eight sites, one `true`
at :699, seven `false` at :493, :565, :570, :575, :623, :634 and :714. The
function signature opens at :464 (`export async function publishStep(`), as the
text says.

The nine-values-from-seven-returns claim turns on :575 returning a variable. I
checked every assignment to it — `grep -n "commitBlocked"` returned the
declaration at :516 and exactly three assignments, `'tree-unreadable'` (:525),
`'foreign-content'` (:541) and `'commit-failed'` (:553, guarded by
`commit.reason === 'commit-failed'`). Nothing assigns `'dry-run'` to it: :550
sets `commit.reason = 'dry-run'` on the phase-1 record only, which is the near
miss that would have made the count wrong. So seven `false` returns do carry
nine distinct reason values, and the nine named in the summary — `no-config`,
`disabled`, `hold`, `tree-unreadable`, `foreign-content`, `commit-failed`,
`dry-run`, `nothing-owned`, `stamp-did-not-advance` — is the complete set.

The two body claims added at step 2 also hold. `stamp-did-not-advance` is
returned at :714 immediately after `const file = writeHold(` at :705, so it does
write `HOLD.md` itself at the line named. `nothing-owned` at :634 sits below the
`if (!effective)` return at :561 and below the `if (dryRun)` return at :623, so
it is reachable only on a real publishing run — "reached only after the
`publish: true` gate at :561" is right, and the dry-run return above it is what
keeps `assumePublish && dryRun` from being a counterexample.

## The front matter still parses, and the list is actually in it

The reason list lives inside a folded (`>`) scalar with backticks, colons and em
dashes, which is where a front-matter edit of this shape usually breaks. I
parsed the file with the repository's own `gray-matter` and asserted each of the
nine values appears in the parsed `summary` string:

```
KEYS: [ 'date','slug','type','summary','evidence','proposed_by_job','proposed_by_type' ]
TYPE: machinery | DATE: 2026-09-04T00:00:00.000Z | SLUG: surface-the-publish-steps-discarded-result
missing from summary: []
```

(Script written to the OS temp dir, importing `gray-matter` from the main
checkout because this worktree has no `node_modules`; it only read the file.)

## Scope and the retirement step

`git diff --stat main...HEAD` is four paths: the loop's own `.job/brief.md` and
`.job/source.json`, the deletion of `data/carried/j-20260904-53-carry-1.md`, and
32 changed lines in the target proposal. Nothing else — no source edit, no
`data/derived/` hand-edit, no reserved path.

The carried file is gone from `data/carried/` (the directory now holds only
`README.md`, `j-20260903-14-carry-1.md` and the three `j-20260904-54` carries),
which is the retirement the brief asked for. `data/derived/queue.json` still
contains the item, correctly: it is a pure function of state that the next Pulse
run recomputes, and editing it here would have been the wrong fix.

I did not re-run `npm test` or `npm run build` — the loop ran both on
`04240c17ea04` and recorded PASS, and no code path changed in this diff anyway.

## Nothing carried, nothing proposed

The line references in the proposal will drift the next time `publish.mjs` is
edited, but that is how the whole corpus cites code here and the surrounding
prose already names the file and the symbol, so a future reader can re-find
them. Not a finding.
