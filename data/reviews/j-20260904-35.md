---
job: j-20260904-35
verdict: approve
reasons: []
would-cite: >-
  An auditor checking whether this site's internal sweep records can be trusted
  as evidence rather than as recollection — this record now quotes figures that
  a reader can reproduce by rendering the named transclusions, which is the
  difference between an auditable coverage claim and a plausible one.
carry:
  - title: Correct the word order of the two "context window" quotes in the unit-doubling drop record
    detail: >-
      The record's cohere-command-a.md:72 line quotes "with a 256000 tokens
      context window" and reasons that "the transclusion fills the adjective
      slot of the noun phrase 'context window'"; meta-muse-glimmer-30b.md:98
      quotes "over a 131072 tokens context window" with "same reading as
      above". Neither page is written that way. cohere-command-a.md:71-72 reads
      "listed {{fact:...#listed_date}} with a context window of\n{{fact:...
      #context_window}} — a", and meta-muse-glimmer-30b.md:97-98 reads "output
      over\na context window of {{fact:...#context_window}}. The" — in both the
      transclusion FOLLOWS the noun phrase, so the pages render "with a context
      window of 256000 tokens — a" and "over a context window of 131072
      tokens.". The verdict (not a doubling) is unaffected — nothing literal
      follows the unit in either — but the quoted rendering and the "adjective
      slot" reasoning describe a construction that is not on the page. Fixing
      it means re-quoting both lines in the page's own word order and replacing
      the adjective-slot reason with the one that actually applies (the unit is
      followed by punctuation, not by a literal). Out of this job's scope,
      which was figures only.
    subject: data/drops/unit-doubling-sweep-j-20260903-07.md
---

Free-form notes.

## What the diff claims

Two author-attributable changes: the figures in the drop record's "Judged not a
doubling" and "Checked and clean" sections are replaced with re-rendered ones,
the stray currency-literal bullet is dropped, and `data/carried/
j-20260903-07-carry-1.md` is deleted (which is what retires the queue item, per
the brief). `.job/brief.md` and `.job/source.json` are loop scaffolding — the
loop commits them (`2447856 job j-20260904-35: brief`) and removes them before
merge, as `git log -- .job` shows for every prior job — not scope creep.

## What I ran

The claim under review is "this figure is what that transclusion renders", so I
rendered them. A throwaway script (written, run, then deleted so it could not
be swept into a later commit — it is four lines and is fully described here):
`loadDataLayer('data/derived')` + `renderFact(fact, { dataLayer, feeds,
entryId }, { inline: true })` for each fact named on each quoted line, tags
stripped. The review worktree has no `node_modules`, so I ran it in
`D:/AddictedtoAI`; `git diff --stat ce2ecd7ad3ee -- lib/ data/derived/
content/wiki/` printed nothing, so those are the same bytes as the reviewed
commit. Output, verbatim:

```
content/wiki/model/cohere-command-a.md -> {{fact:model/cohere-command-a#context_window}} => "256000tokensopenrouter-models, last checked 2026-09-04"
content/wiki/model/meta-muse-glimmer-30b.md -> {{fact:model/meta-muse-glimmer-30b#context_window}} => "131072tokensopenrouter-models, last checked 2026-09-04"
content/wiki/model/inception-mercury-2-5-preview.md -> {{fact:model/inception-mercury-2-5-preview#context_window}} => "260000tokensopenrouter-models, last checked 2026-09-04"
content/wiki/model/inception-mercury-2-5-preview.md -> {{fact:model/inception-mercury-2-5-preview#max_output_tokens}} => "65536tokensopenrouter-models, last checked 2026-09-04"
content/wiki/model/qwen-qwen3-8-27b.md:77 first -> {{fact:model/qwen-qwen3-6-27b#context_window}} => "262144tokensopenrouter-models, last checked 2026-09-04"
content/wiki/model/qwen-qwen3-8-27b.md:78 second -> {{fact:model/qwen-qwen3-8-27b#context_window}} => "1000000tokensopenrouter-models, last checked 2026-09-04"
content/wiki/model/x-ai-grok-4-6.md:110 -> {{fact:model/x-ai-grok-4-3#context_window}} => "1000000tokensopenrouter-models, last checked 2026-09-04"
content/wiki/model/x-ai-grok-4-6.md:111 -> {{fact:model/x-ai-grok-4-3#price_input}} => "$1.25per million tokensopenrouter-models, last checked 2026-09-04"
content/wiki/model/z-ai-glm-5-1.md:80 -> {{fact:model/z-ai-glm-5-2#context_window}} => "1048576tokensopenrouter-models, last checked 2026-09-04"
content/wiki/org/spacexai.md:71 -> {{fact:model/x-ai-grok-4-20#context_window}} => "2000000tokensopenrouter-models, last checked 2026-09-04"
```

Every figure in the diff matches: 256000, 131072, 260000, 65536, 262144 then
1000000, 1000000, 1048576, 2000000. Ten for ten.

I resolved which fact each quoted line actually holds with Grep rather than
trusting the record's ids, because three of the seven lines quote a *different*
row than the page they sit on: `qwen-qwen3-8-27b.md:77` renders
`qwen3-6-27b`'s window (the second figure is line 78's `qwen3-8-27b`),
`z-ai-glm-5-1.md:80` renders `glm-5-2`'s, and `spacexai.md:71` renders
`grok-4-20`'s. All three read the way the diff has them.

## The one place the author departed from the brief, and it is the right call

The brief (transcribed from j-20260903-07's verdict, written 2026-09-03) says
`x-ai-grok-4-6.md:110` renders "0.0000008 → 0.00000125 USD per token input".
The diff instead writes "$1.25 per million tokens input". The diff is correct
and the brief is stale: `lib/units.mjs`'s `displayQuantity` was changed on
2026-09-03 (see its own header, "The half of that decision about SCALE was
reversed on 2026-09-03") to route every `PER_TOKEN_PRICE_PATHS` value through
`formatPrice`, which returns `$1.25` with the unit `per million tokens`. The
render above confirms it. An author who had copied the brief's figure instead
of measuring would have written a number the page does not show — this one
measured.

## What I did not re-run

`npm test` and `npm run build` — the loop ran both on `ce2ecd7ad3ee` and
recorded PASS. Nothing in this diff is under `lib/`, `content/` or the build's
input path in any case: `data/drops/` and `data/carried/` are records.

## Other claims in the touched sections, spot-checked

- "inception-mercury-2-5-preview.md:129 ('a max output of 65536 tokens;')" —
  line 129 is `max output of {{fact:...#max_output_tokens}};`, which renders
  65536. Holds.
- "tencent-hy4-preview.md:123 ('{{fact:…}}.' — nothing after)" — line 123 is
  `{{fact:model/tencent-hy4-preview#max_output_tokens}}. The weights were not`.
  Holds.
- "only two learn pages carry transclusions" — Grep for `{{fact:` across
  `content/` returns 55 files, 53 of them under `content/wiki/`; the only two
  outside it are `content/learn/open-weights-and-closed-models.md` and
  `content/learn/what-a-benchmark-measures.md`. Holds, and nothing under
  `tutorials/`, `blog/` or `directory/` carries one.
- The dropped bullet was right to drop: `inception-mercury-2-5-preview.md:110`
  is inside a quotation of the vendor's own copy ("achieving 1,107 tokens/sec
  on standard GPUs"), a throughput claim, and calling it a currency-literal
  lint case was wrong on both counts.
