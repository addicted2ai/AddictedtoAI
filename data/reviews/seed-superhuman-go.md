---
job: seed-superhuman-go
verdict: reject
reasons: [false-or-unsupported-claim]
would-cite: "As it stands, nobody could safely cite it: the linked release note answers the strength question with an internal Leela Zero network number, so the delta's central claim rests on a bridge the reader has to build alone."
reviewer: task-6.5 seed reviewer (fresh context, no edit rights)
date: 2026-08-28
---

Checked both ends by fetching. The author's own self-criticism of this delta
is accurate, and it is disqualifying rather than cosmetic.

- End B (the load-bearing failure): fetched the KataGo v1.0 release page and
  the GitHub API record (published 2019-02-27T00:22:20Z — the date is
  exact). The release body, in full: "Initial release of KataGo as of the
  completion of its initial main run ('g65') up to roughly the strength of
  LZ130 in a week on a few dozen GPUs." "Trained in a week on a few dozen
  GPUs" is supported verbatim. "An engine of comparable strength" — the
  claim that carries the whole delta, comparable meaning the AlphaGo of end
  A, above the strongest humans per the title — is supported by nothing on
  the cited page. "Roughly the strength of LZ130" is an unexplained internal
  reference to a Leela Zero network; a reader who clicks through cannot cash
  it out into "above professional strength" without outside research the
  delta neither provides nor cites.
- End A: fetched the DeepMind AlphaGo page. It supports "March 2016," the
  4-1 score, and — verbatim — "AlphaGo defeated a human Go world champion a
  decade before experts thought possible." It does not state the specific
  date 2016-03-15 used in the front matter. The date is real (game five's
  date), but the cited source does not carry it.

Why reject rather than revise: the fix is not wording. No rewording keeps
the delta's point while citing a source that says "roughly the strength of
LZ130." The routine end needs different evidence entirely, which under
specs/review is a new job, not a revision pass. The idea is genuinely
strong and should return with a source that actually says what the delta
needs said — for example an open engine with a documented match record
against top professionals, or a later KataGo release with a published
strength evaluation against a superhuman baseline.

Cutting this leaves 13 deltas against the floor of 12.
