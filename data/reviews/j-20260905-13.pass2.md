---
job: j-20260905-13
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a benchmark publisher's rebase is a corpus-integrity
  event rather than a scoreboard event would link the Google DeepMind and
  OpenAI pages as the worked example: the numbers a page narrates can all be
  live and the sentence around them still false, and these pages now show what
  it looks like to re-derive the comparison instead of hedging it.
proposal:
  slug: recompute-the-review-diff-for-each-pass
  type: machinery
  date: 2026-09-05
  summary: >-
    `loop/run.mjs` computes the reviewed diff once, at line 412, BEFORE the
    `for(;;)` review loop it is used inside, and never recomputes it. A
    revision pass commits to the branch from within that loop, so the pass-2
    reviewer is handed the pass-1 diff — the author's revision is invisible in
    the one artefact the brief calls authoritative ("The loop computed this
    diff itself from the branch state"). Move the `diffAgainst` call inside
    the loop, or recompute it before each `runReview`. The fix is one line and
    the failure it prevents is total: a delta reviewer who trusts the embedded
    diff re-issues findings against text that no longer exists, the verdict is
    a second non-approval, and `pass >= 2` discards the whole job.
  evidence: >-
    Measured on this review's own brief, 2026-09-05. The brief states the diff
    "was computed from" commit 4319f60a11ee; its `google-deepmind.md` hunk is
    `730ffae`'s, still containing the by-effort sentence that 4319f60 exists
    to remove. `git diff --stat 730ffae 4319f60` reports 2 files changed, 12
    insertions, 11 deletions — none of it present in the embedded diff. The
    cause is visible in the source: `loop/run.mjs:412` `const diffText =
    diffAgainst(ctx.repoRoot, base, branch);` sits above `let pass = 1;` and
    the `for(;;)` at line 415, while `runReview(..., diffText, ...)` is called
    at line 431 inside it. The same loop already knows the branch moves — the
    merge gate at line 455 re-reads it, commented "a revision pass can add a
    file after the author run, and the gate must compare the record against
    what is actually about to merge" (lines 451-454). The diff was not given
    the same treatment. Also inconsistent, and the tell that first exposed it:
    the header's gate-commit and spend figures ARE interpolated fresh, so the
    brief reports a PASS on 4319f60 beside a diff of 730ffae.
carry:
  - title: Attribute the by-effort split on the 3.8 Flash entry as a pre-v4.2 reading
    detail: >-
      The entry renders every declared fact (`renderFacts` in
      `lib/render/entry.mjs` lays out all of `doc.factsHtml`), so its Facts
      section shows the feed's `intelligence_index` of 47.1 beside the cited
      `intelligence_index_by_effort`, "59 at high reasoning, 57 at medium, 52
      at low; up 3 from Gemini 3.7 Flash at high, level with sub-maximum
      efforts of GPT-5.6 Sol and Grok 4.6" (accessed 2026-09-03, the day
      before the rebase). Three things collide on screen, measured against
      latest.json today: every effort level sits above the 47.1 headline for
      the same model; "up 3 from Gemini 3.7 Flash at high" contradicts the
      live gap of 47.1 - 45.2 = 1.9; and "level with sub-maximum efforts of
      GPT-5.6 Sol and Grok 4.6" is false where Sol is 51.3 and Grok 4.6 is
      50.6, both maxima below the quoted 52. This is the defect job
      j-20260905-13 was dispatched to end, on a page its directive put out of
      scope ("and nothing else"), which is why it is carried rather than
      blocked. The fix is a line of attribution marking the split as a
      pre-v4.2 reading, per addictedtoai-ego8 — not deletion of the fact.
    subject: content/wiki/model/google-gemini-3-8-flash.md
subject:
  - "content/wiki/org/anthropic.md"
  - "content/wiki/org/deepseek.md"
  - "content/wiki/org/google-deepmind.md"
  - "content/wiki/org/openai.md"
  - "content/wiki/org/spacexai.md"
reviewed:
  "content/wiki/org/anthropic.md": "ba9dc25de5e2729a36c3cc0dc2bc55cf5844a9fe76e89d9c027c3f207b343288"
  "content/wiki/org/deepseek.md": "c0f8d929665f164db7a6d6504b2a2f3981704ae5ea972c53e5c0509c7bee4594"
  "content/wiki/org/google-deepmind.md": "4746979e8f0bc7a751658ef9a6d1c773eabf2cc3b8cba776da5400054e194a38"
  "content/wiki/org/openai.md": "5bd66b99b7adad860a0611e8273cf951e818a7cdb5514a51a39fca4a62ad9503"
  "content/wiki/org/spacexai.md": "eb4a6f4cddecdd0636b699433076e005ccf74ad932b0797609046f87705506b8"
---

Delta review, pass 2. The previous verdict named three required changes; I
checked each against the sources rather than against the author's account,
and re-derived the new claims the revision introduced.

## The diff embedded in this brief is stale — I reviewed the real delta

The brief states the diff below it "was computed from" commit `4319f60a11ee`.
It was not. The embedded diff contains the by-effort sentence that the
revision commit exists to remove, and its `google-deepmind.md` hunk matches
`730ffae` — the commit the *previous* pass reviewed. Measured:

```
$ git log --oneline -3
4319f60 job j-20260905-13: revision — drop the pre-rebase by-effort split, correct the proposal
730ffae job j-20260905-13: repair five org pages after the index rebase
6cde3a5 job j-20260905-13: brief

$ git diff --stat 730ffae 4319f60
 content/wiki/org/google-deepmind.md                      |  9 +++------
 data/proposals/flag-declared-feed-fields-that-go-null.md | 14 +++++++++-----
 2 files changed, 12 insertions(+), 11 deletions(-)
```

So I judged `git diff 730ffae 4319f60` — two files — which is what a delta
review is for. I did not re-run `npm test` or `npm run build`; the delta is
one removed transclusion and a YAML prose edit, and I ran the targeted checks
those two changes could break instead. (Noting the stale-diff observation
because a reviewer who trusted the embedded diff would have re-issued the
pass-1 finding against text that no longer exists.)

## Finding 1 and 2 — the scale collision and the publication-date claim

Both were in one sentence, and the revision took the second of the two
repairs the previous pass offered: it dropped the
`intelligence_index_by_effort` transclusion entirely rather than attributing
it. The paragraph now reads:

> ...puts `google/gemini-3.8-flash` at **47.1**, against **45.2** for the
> August release and **40.3** for July's — two climbs in the six weeks
> between them. The Flash row lists at **$0.00000075** input; the Pro
> preview, six months older, lists at **$0.000002**.

That removes all three defects at once: the 59/57/52 no longer sits above the
47.1 headline, the "up 3 from Gemini 3.7 Flash" contradiction of the 1.9 gap
is gone with it, and the unsupported "published ... on 3 September" is gone
with the sentence. The `9to5Google` and `llm-releases` citations both survive
elsewhere on the page (lines 66 and 94), so nothing was orphaned by the cut.

**The fact itself was not touched**, which is what `addictedtoai-ego8`
requires. It is still declared on the model entry, unedited:

```
content/wiki/model/google-gemini-3-8-flash.md:59
  - field: intelligence_index_by_effort
    source: cited
    value: "59 at high reasoning, 57 at medium, 52 at low; up 3 from Gemini 3.7 Flash at high, level with sub-maximum efforts of GPT-5.6 Sol and Grok 4.6"
    source_url: "https://llm-releases.com/models/gemini-3-8-flash"
    accessed: "2026-09-03"
```

A grep of `content/` for `intelligence_index_by_effort` returns exactly that
one line and nothing else, so the org page was its only consumer and no other
page is left transcluding it. There is no orphan-fact check to trip: grepping
`lib/` for `unused|orphan` returns only `reviews.mjs`'s orphan *records*.

**The new claim, re-derived.** "Two climbs in the six weeks between them" is
arithmetic I had to check, not inherit. Resolved from
`data/sources/openrouter-models/latest.json` (`fetched_at`
`2026-09-05T06:00:04.599Z`):

```
google/gemini-3.5-flash        idx=null   prompt=0.0000015   created=2026-05-19
google/gemini-3.6-flash        idx=40.3   prompt=0.00000075  created=2026-07-21
google/gemini-3.7-flash        idx=45.2   prompt=0.00000075  created=2026-08-13
google/gemini-3.8-flash        idx=47.1   prompt=0.00000075  created=2026-09-02
google/gemini-3.1-pro-preview  idx=null   prompt=0.000002    created=2026-02-19
```

40.3 → 45.2 → 47.1 is two climbs, both up. 21 July → 2 September is 43 days,
so "the six weeks between them" is right to the week and is a genuine
improvement on the bare "in six weeks" it replaced, which left "them"
unanchored. Every transclusion surviving in the changed paragraphs resolves
to a number — 47.1, 45.2, 40.3, 0.00000075, 0.000002 — none absent. The
untouched paragraph below still checks out on the same values: 0.0000015 →
0.00000075 is exactly halved, 3.7 and 3.8 both hold 0.00000075, and 21 July →
13 August is twenty-three days exactly.

## Finding 3 — the proposal's evidence

Re-measured from the two committed blobs myself rather than trusting either
the proposal or the previous pass:

```
prev fetched_at: 2026-09-04T06:00:03.738Z rows: 427
latest fetched_at: 2026-09-05T06:00:04.599Z rows: 431

rows carrying a number for intelligence_index: prev 164 -> latest 52
{ down: 50, up: 0, same: 0, lostNumberButRowLives: 113, rowVanishedEntirely: 1 }
ratio p25/median/p75: 0.781 0.803 0.830
row ids present previously and absent now: 2 [ 'ibm-granite/granite-4.1-8b', 'qwen/qwen3.8-max' ]
   ibm-granite/granite-4.1-8b  prev index = null
   qwen/qwen3.8-max            prev index = 53.4
```

**114 → 113 is corrected in all three places** it appeared (front matter and
two body passages); parsing the file with the repo's own `gray-matter`
reports no `114` left in either the front matter or the body, and no `No row
vanished` string anywhere. The front matter still parses to the five required
keys, `type: machinery` against a `repair` origin, so no self-amplification.

**The replacement clause is the one thing I pushed on**, because it makes a
new claim: "that detector fired on those two and on nothing else." As an
absolute statement about `vanished_feed_rows` that would be false — the
detector's current output is six, not two:

```
data/derived/freshness.json (generated_at 2026-09-05) vanished_feed_rows: 6
  anthropic/claude-opus-4.7-fast, anthropic/claude-opus-4.8-fast,
  anthropic/claude-opus-5-fast, ibm-granite/granite-4.1-8b,
  nvidia/nemotron-3-ultra-550b-a55b:batch, qwen/qwen3.8-max
```

But both sentences scope themselves explicitly to "the same fetch", and on
that scope the claim measures out exactly. The durable records under
`data/vanished/answered/` are dated by the run that wrote them:

```
anthropic/claude-opus-4.7-fast          last_seen 2026-09-01  date 2026-09-02
anthropic/claude-opus-4.8-fast          last_seen 2026-09-01  date 2026-09-02
anthropic/claude-opus-5-fast            last_seen 2026-09-01  date 2026-09-02
nvidia/nemotron-3-ultra-550b-a55b:batch last_seen 2026-09-02  date 2026-09-03
ibm-granite/granite-4.1-8b              last_seen 2026-09-04  date 2026-09-05
qwen/qwen3.8-max                        last_seen 2026-09-04  date 2026-09-05
```

Exactly two records were written on the 09-04 → 09-05 fetch, and they are the
two the proposal names. Both are declared by an entry's `feeds:` map
(`content/wiki/model/qwen-qwen3-8-max.md:11`,
`content/wiki/model/ibm-granite-granite-4-1-8b.md:11`), and both already have
repaired entries dated 2026-09-05 describing the withdrawal — which is why
`data/derived/queue.json` currently holds zero `vanished-feed-row` items out
of five. The clause is accurate as written and the contrast it draws (113
fields invisible against 2 rows caught) survives at the wider scope too.

## Scope

Clean. The delta touches one content file named in the directive and the one
proposal file the job is permitted. No reserved path, no second proposal, no
fact edited or deleted — `git diff 730ffae 4319f60` shows no change inside any
`facts:` block on any entry.

## Carried, not blocked

Removing the transclusion moves it off the org page but not off the site.
`renderFacts` in `lib/render/entry.mjs` lays out **all** of `doc.factsHtml`,
so `content/wiki/model/google-gemini-3-8-flash.md` renders the preserved
cited split in its own Facts section, directly beside the feed's rebased
47.1. Measured against `latest.json` today:

```
google/gemini-3.8-flash  47.1     <- the headline the entry also shows
google/gemini-3.7-flash  45.2     <- so the live 3.8-vs-3.7 gap is 1.9, not "up 3"
openai/gpt-5.6-sol       51.3     <- below the quoted "52 at low"
x-ai/grok-4.6            50.6     <- below the quoted "52 at low"
```

So all three of the collisions the previous pass named still exist for a
reader, one page over. Keeping the fact is correct — `addictedtoai-ego8`
settles that — and the model entry was explicitly outside this directive's
"and nothing else", so repairing it here would have been the scope violation.
Carried as a line-of-attribution fix rather than raised as a block.

## The proposal noted above

I did not propose the systemic version of the content defect — that is
already the proposal on this branch. The one I noted is the loop bug this
review tripped over. Having found the stale diff, I read the code rather than
guessing at it: `loop/run.mjs:412` computes `diffText` above `let pass = 1`
and the `for(;;)` at 415, and `runReview` consumes it at 431 inside that
loop, so a revision committed between passes is never re-diffed. The merge
gate three lines below re-reads the branch for precisely this reason and says
so in its comment; the diff does not. It is a one-line fix guarding a total
failure — a delta reviewer who trusts the embedded diff re-issues findings
against text that no longer exists, which on pass 2 discards the job.
