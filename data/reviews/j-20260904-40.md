---
job: j-20260904-40
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a cleanup call whose return value is thrown away is not
  a cleanup at all would link this: the deletion ran against a branch this
  job's own worktree still held, git refused it every single time, and because
  the boolean came back into nothing the loop went 0-for-113 without ever
  saying a word. The fix is an ordering move plus reading the answer, and its
  test reproduces git's actual refusal rather than describing it.
carry:
  - title: Prune worktree metadata after the rmSync fallback, or a failed `worktree remove` still blocks the deletion
    detail: >-
      `removeWorktree` (loop/lib/git.mjs:95) runs `worktree remove --force` and
      then `worktree prune`, and run.mjs:1347 does an `rmSync` of the directory
      afterwards — that fallback exists because the remove is not always
      believed. On Windows a `worktree remove --force` can fail on a locked
      file; the prune then finds the directory still present and prunes
      nothing, the `rmSync` deletes the directory, and no prune ever runs
      again. Measured in a throwaway repo: with the directory gone but the
      admin entry unpruned, `deleteBranch` returns `false`; a `git worktree
      prune` at that point and the identical call returns `true` and the
      branch is gone. Nothing breaks today — the new code logs "could not
      delete the merged branch …" and moves on — but one `gitTry(repo,
      ['worktree','prune'])` after the `rmSync` in run.mjs would close the
      residual case.
---

Reviewed `d1e940c4ef8b`. `npm test` and `npm run build` were run by the loop on
this commit and are not re-run here.

## The claim, and the measurement

The diff claims git refuses to delete a branch a linked worktree still has
checked out, that the old call site was in exactly that state, and that moving
the call after `removeWorktree` makes it succeed. I ran both orderings against
the real `deleteBranch`/`removeWorktree` from `loop/lib/git.mjs` in a throwaway
repository built under the OS temp directory (job branch, held by a linked
worktree, merged `--no-ff` into `main`), and deleted the probe afterwards:

```
[OLD ORDERING] deleteBranch returned: false
[OLD ORDERING] branch still exists: true
[OLD ORDERING] jobBranches left: ["job/probe"]
[OLD ORDERING] git said: error: Cannot delete branch 'job/probe' checked out at 'C:/Users/BadBitch/AppData/Local/Temp/probe-old-a78kC0/wt'
[OLD ORDERING] merged file on main: job work
[NEW ORDERING] deleteBranch returned: true
[NEW ORDERING] branch still exists: false
[NEW ORDERING] jobBranches left: []
[NEW ORDERING] merged file on main: job work
```

Red before, green after, and the error string is verbatim the one the code
comment quotes. The merged work is on `main` in both, which is the other half
worth checking: deleting the branch is not deleting the job.

I also attempted what the fix must not do — turn a git refusal into an
exception, which every other `gitTry` caller would feel:

```
[STALE WORKTREE] deleteBranch returned: false (no throw)
[NO SUCH BRANCH] deleteBranch returned: false (no throw)
```

`deleteBranch` itself is unchanged in the diff (`loop/lib/git.mjs:162` still
`return gitTry(...).ok`), so `gitTry` semantics are intact by inspection as
well as by attempt.

## The end-to-end half

I could not execute `loop/tests/branch-cleanup.test.mjs` in this review
worktree: it has no `node_modules`, `loop/lib/runners.mjs` imports `yaml`, and
linking the main checkout's `node_modules` in was blocked by the sandbox
("New-Item targeting 'D:\AddictedtoAI\node_modules' was blocked"). Reporting
that rather than routing around it. It is not a gap in the evidence: `npm test`
PASSed on this commit per the loop, `scripts/run-tests.mjs:55` searches `loop`,
so the new file ran inside that pass, and the ordering measurement above is the
same assertion at the level the change actually operates on.

The test file itself reads sound against the code: it builds its own repo under
`tmpdir` via the existing `makeRepo` helper, touches nothing here, and the
`CONTROL — a DISCARDED job keeps its branch` case is the assertion that matters
most — `run.mjs:1229-1241` leaves an unmerged branch alone, and a fix that
deleted unconditionally would have destroyed the only copy of rejected work
while still passing the happy-path test.

## The counting claims

Both numbers in the comment and the test header are correct as of today,
counted on the shared refs from this worktree:

```
merge commits on main matching job <id> (<type>):  112
job/* branches merged into main:                   113
```

## Placement and scope

`mergedBranch` is set only inside the merged branch of the outcome switch
(`run.mjs:1227`) and read only at `:1367`, after `removeWorktree` and the
`rmSync`. There is no `return` between the two points, so no path reaches a
merge and then skips the deletion. `mergeLocal` has one production call site
(`run.mjs:1109`) and `deleteBranch` now has exactly one (`run.mjs:1368`) —
grep over the repository, which is also the claim the brief made about the
pre-change state.

Scope is clean: `loop/run.mjs` and one new test file, plus the `.job/` brief
harness. No sweep of the existing 113 branches (that is `addictedtoai-jl09`),
no change to `deleteBranch`, no content and no reserved path touched.
