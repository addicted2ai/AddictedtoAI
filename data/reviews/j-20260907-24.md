---
job: j-20260907-24
verdict: approve
reasons: []
would-cite: >-
  An AI catalog researcher could cite this entry when distinguishing model authorship from serving infrastructure, using the OpenRouter endpoint records to support that Amazon Bedrock is the sole provider behind Amazon's Nova rows.
subject: "content/wiki/org/amazon.md"
reviewed:
  "content/wiki/org/amazon.md": "cb68c16aebc012346df2f78523dde111f41448e4a1ae0c3cb4cb166783b1f78c"
---

Free-form notes: I checked the diff against `main`. The only substantive content change is in `content/wiki/org/amazon.md`: the sentence now says "the only endpoint provider", both `portfolio` and `internal_origin` cite `https://aws.amazon.com/nova/`, and both carried-finding files are deleted. The other changed paths are the job's generated `.job/brief.md` and `.job/source.json`.

Targeted OpenRouter check, fetched from the five endpoint URLs named by the subject rows, printed:

    nova-2-lite-v1 endpoints=2 providers=Amazon Bedrock
    nova-lite-v1 endpoints=2 providers=Amazon Bedrock
    nova-micro-v1 endpoints=2 providers=Amazon Bedrock
    nova-pro-v1 endpoints=2 providers=Amazon Bedrock
    nova-premier-v1 endpoints=1 providers=Amazon Bedrock

Targeted AWS check printed:

    old=200 final=https://aws.amazon.com/nova/
    canonical=200 final=https://aws.amazon.com/nova/

The canonical page-content check printed `portfolio=True` and `origin=True`, confirming both quoted fact strings are present at the destination. The retirement check printed `carry1_exists=False` and `carry2_exists=False`.

The loop-provided gate results for this branch were `npm run test` PASS, `npm run build` PASS, `verify-surfaces` PASS, and `verify-design` PASS; I did not rerun the prohibited full suite.
