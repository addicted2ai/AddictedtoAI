---
job: j-20260908-01
verdict: reject
reasons:
  - false-or-unsupported-claim
  - spec-violation
would-cite: >-
  A researcher documenting the boundary between a model vendor's availability
  and a router's pricing tiers would cite this entry to show that MiniMax M2.7
  remained listed while OpenRouter's free route had no endpoints, with the last
  feed row preserved for provenance.
---

The page repair's central source claim is supported. A direct fetch of
`https://openrouter.ai/api/v1/models` printed `OPENROUTER_MODELS_STATUS 200` and
`MODEL_MATCHES 0`. The row-specific endpoint fetch printed
`OPENROUTER_ENDPOINTS_STATUS 200`, `MODEL_ID minimax/minimax-m2.7:free`, and
`ENDPOINTS_COUNT 0`. The MiniMax documentation fetch printed
`MINIMAX_DOC_STATUS 200`, `DOC_HAS_MINIMAX_M27 True`, and
`DOC_HAS_FREE_ROUTE False`.

The required permanent answered record is still false as written. The direct
snapshot check printed `LATEST_DATE 2026-09-07`, `PREVIOUS_DATE 2026-09-06`,
`LATEST_MATCH True`, and `PREVIOUS_MATCH True`. It reported the latest row's
`3d` benchmark as `elo:1231, rank:38, win_rate:50.6`, while the answered file
records `rank:39, win_rate:50.5` (and the older snapshot's other benchmark
values). The record therefore does not preserve the pinned last-known row from
the disappearance date, violating the required unchanged durable move and
leaving false historical evidence under `data/vanished/answered/`.

The entry still contains `feeds: openrouter-models: minimax/minimax-m2.7:free`.
The branch gates were not rerun, per the brief; the loop had already reported
them passing. Because the previous pass already required this correction, this
pass-2 non-approval rejects the job.
