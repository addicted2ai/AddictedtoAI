---
job: j-20260902-17
verdict: approve
reasons: []
would-cite: >-
  Nobody links a proposal file, so the question is what this edit buys the post
  that will be written from it. It buys the one thing that post's argument
  cannot afford to lose: the vendor labelled exactly one of the eight ExploitGym
  columns with a configuration qualifier, and a post whose thesis is "this row
  was not produced alike" would have been quietly undercut by a writer who
  copied "Fable 5" out of the proposal and never saw that the card said
  something narrower. The edit puts the card's own words in front of the writer
  at both places they would reach for them.
carry:
  - title: Say in the evidence block that the card never defines "(w/ fallback)"
    detail: >-
      The proposal now carries the card's qualifier verbatim, but the card
      explains it nowhere — `grep -n -i "fallback"` over the fetched card
      returns exactly one line, the table header at line 22, and no footnote,
      caption or prose says what fallback was enabled, to what, or for which
      runs. That is a trap for the writer: a qualifier reproduced without a
      definition invites a plausible gloss ("with fallback routing", "with a
      fallback model"), which would be an invention attributed to Z.ai. One
      clause in the evidence block — that the qualifier appears only in the
      header and the card states no meaning for it — closes it, and is also the
      strongest version of the like-for-like point: the one column the card
      qualifies is the one column it declines to explain.
    subject: data/proposals/glm-5-3-license-revenue-gate.md
---

Free-form notes.

## What I did not re-run

`npm test` and `npm run build` are recorded PASS on `78ed490a4bc7` in the brief;
I did not repeat them. I ran the two checks that would be red if this diff's
claims were false: the card's own text, and the mechanism that retires the queue
item.

## Check 1 — the card really does label the column "Fable 5 (w/ fallback)"

Fetched the model card as raw markdown, not through the rendered page or an
extractor:

```
$ curl -sL -o /tmp/glm53card.md -w "%{http_code} %{size_download}\n" \
    https://huggingface.co/zai-org/GLM-5.3/raw/main/README.md
200 14209
```

Same byte count the pass-1 reviewer of j-20260902-15 recorded, so this is the
same document that proposal was written from.

```
$ grep -c "Fable 5" /tmp/glm53card.md
1
$ grep -n "Fable 5" /tmp/glm53card.md
22:| Benchmark  | GLM-5.3 | GLM-5.2 | Kimi K3 | DeepSeek-V4 Pro-0813 | Qwen3.8-Max | Opus 4.8 | Fable 5 (w/ fallback) | GPT-5.6 Sol |
$ grep -n "ExploitGym (2h" /tmp/glm53card.md
33:| ExploitGym (2h / 6h) | 105 / 130 | 29 / 39 | 36 / 70 | – | 14 / 26 | 80 / 120 | 181 / 247 | **216 / 293** |
```

(Column padding trimmed here for width; the lines are otherwise verbatim.)

So the carried finding was accurate on its own terms — grep count 1, the sole
occurrence is the header, and the qualifier is the vendor's, not an editorial
addition. Both edited sentences now name the column as the card labels it.

Both edits also remain true after the change:

- **Summary.** "third on the card's own row (behind Fable 5 (w/ fallback) and
  GPT-5.6 Sol)" — 216 > 181 > 105 at 2h and 293 > 247 > 130 at 6h, from line 33.
  Third at both budgets, and the two ahead of it are exactly those two columns.
- **Evidence.** "the GLM-5.2, Opus 4.8, Fable 5 (w/ fallback) and GPT-5.6 Sol
  figures on the same row carry no stated method" — line 70's footnote names
  GLM-5.3, Kimi-K3 and Qwen3.8 Max and gives TPS factors for exactly those
  three, so the four named columns are indeed uncovered. DeepSeek-V4 Pro-0813 is
  a dash and correctly goes unmentioned.

The brief asked for the qualifier "at least on first mention of its 181/247".
The proposal never prints 181/247 — that figure lives in the finished post, not
here — so the honest reading of the instruction against this target is "every
place the proposal names the column", which is what the diff does: both
occurrences, `grep -c "Fable 5" data/proposals/glm-5-3-license-revenue-gate.md`
= 2, both qualified. Nothing was left half-corrected for the writer to trip on.

## Check 2 — deleting the carried file is what actually retires the item

I read the mechanism rather than trusting the file's own "Retiring this item"
paragraph. `carriedFindingItems` in `pulse/lib/queue.mjs:394-431` reads every
`*.md` under `data/carried/` except `README.md`, skips a file only when it has
no front matter or an empty `title:`, and pushes exactly one
`repair` / `carried-finding` item per surviving file. There is no resolved flag
and nothing that consults the subject it points at — presence is the state, so
deletion is the retirement.

```
$ ls data/carried/
README.md
j-20260902-16-carry-1.md
```

`j-20260902-15-carry-1.md` is gone from the tree, and the deleted blob in the
diff matches this job's brief verbatim, so the right file was removed. (I tried
to import `carriedFindingItems` and enumerate directly; this review worktree has
no `node_modules`, so the import fails on `fast-glob` before reaching the
function. Reading the function and listing the directory answers the same
question.)

## Scope

`git log --stat` shows the author's commit `78ed490` touching two files and
nothing else: `data/proposals/glm-5-3-license-revenue-gate.md` (+2/-2) and the
deletion of `data/carried/j-20260902-15-carry-1.md` (-16). The `.job/brief.md`
and `.job/source.json` that appear as additions in the diff came from the loop's
own preceding commit `634e216` ("job j-20260902-17: brief"), not from this
run's work. No reserved path is touched. No proposal surfaced — one finding
carried, above.
