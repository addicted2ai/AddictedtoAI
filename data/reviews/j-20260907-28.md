---
job: j-20260907-28
verdict: approve
reasons: []
would-cite: >-
  A reader comparing an open-weight lab's lineage with its inference portal would
  link this entry when arguing that, as counted on 6 September 2026, the portal
  exposed 333 distinct model IDs after the 13 tilde-prefixed free-tier rows were
  excluded, with no Nous-family name among them.
subject: "content/wiki/org/nous-research.md"
reviewed:
  "content/wiki/org/nous-research.md": "6c4bf6ee5077c8eb94c67abccfa3e22554f9e221e69dc68f9329b90c727fa581"
---

Free-form notes: I judged the substantive diff against the stated repair only.
The only content change is `content/wiki/org/nous-research.md`; the three named
`data/carried/` files are deleted, and the added `.job/` files are the loop's
job metadata. There is no `content/blog/` path, so no blog voice answer is
required.

`git diff --check main...HEAD` produced no output. I did not rerun the suite or
build gates because the brief reports `npm run test`, `npm run build`,
`node scripts/verify-surfaces.mjs out`, and `node scripts/verify-design.mjs out
3211` passing on this commit.

Source checks:

- Fetching the four declared Hugging Face API URLs returned:
  `NousResearch/Hermes-4.3-36B likes=299`,
  `NousResearch/Hermes-4-70B likes=215`,
  `NousResearch/Hermes-4-14B likes=181`, and
  `NousResearch/Hermes-4-405B likes=94`. The narrowed fact cites only the
  first URL and reports only its 299 likes.
- Fetching `https://nousresearch.com/introducing-hermes-4-3` found the full
  performance sentence in the page metadata. In the body it says `Hermes 4.3
  is our first production model post-trained entirely on the <a
  href="https://psyche.network/">Psyche</a> network`; the HTML link explains
  why the second fact's plain-text quote is not one contiguous raw HTML
  substring. Both dated facts are supported by the named launch page.
- Parsing the embedded `displayName`/`modelName`/`tokenPrice` rows from
  `https://portal.nousresearch.com/` produced `parsed=346 distinct=346
  nonFreeRows=333 nonFreeDistinct=333 matchingNames=0` and `freeRows=13`.
  The 13 excluded rows are the page's `~`-prefixed rows; the remaining count
  and the no-match claim reproduce the changed fact's stated convention.

The local wiki spec states that `slow` is rechecked within 120 days while
`dated` is true as of its date, displayed with the date, and never rechecked;
`lib/schema.mjs` measures `dated` with no freshness interval. The two launch
quotes now use that correct class. The narrowed likes fact and the portal
count both retain reachable source URLs and their access dates. All three
carried findings are deleted in the same diff as their fixes.
