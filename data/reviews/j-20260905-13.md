---
job: j-20260905-13
verdict: revise
reasons:
  - false-or-unsupported-claim
would-cite: >-
  Someone arguing that a benchmark rebase is an editorial event, not a data
  event: four of these five pages now show what it looks like to re-derive a
  comparison against the numbers as they stand rather than patching the old
  sentence, and the fifth shows the trap — a vendor-reported score frozen on
  the old scale sitting one line below the catalog's rebased one.
carry:
  - title: Re-check the org pages' mentions lists against the prose they now carry
    detail: >-
      anthropic.md still declares `model/anthropic-claude-opus-4-7` and
      `model/anthropic-claude-opus-4-8` under `mentions:`, and openai.md still
      declares `model/openai-gpt-5-4`, but after this repair none of the three
      is named anywhere in those pages' bodies. Nothing fails — mentions are
      validated for existence, not for prose presence, and the build passed —
      but the backlink each generates now points at a page that no longer
      discusses the model. Worth a sweep deciding whether the binding or the
      omission is the thing to fix.
    subject: content/wiki/org/anthropic.md
---

Reviewed the diff against the 2026-09-05 snapshot the build reads. I did not
re-run `npm test` or `npm run build`; the loop's PASS on `730ffae890f4` is
recorded above and re-running it is not the check that would be red if any
claim here were false.

## What I ran

`data/sources/openrouter-models/latest.json` (`fetched_at`
`2026-09-05T06:00:04.599Z`, `row_count` 431) is the authority for every feed
fact on these pages. I resolved each entry's `feeds:` row id and `path:` by
hand rather than trusting the prose.

**Every transclusion on all five changed pages, resolved — counted, not
sampled:**

```
=== org/anthropic: 4 transclusions
=== org/spacexai: 8 transclusions
=== org/openai: 6 transclusions
=== org/google-deepmind: 10 transclusions
=== org/deepseek: 4 transclusions

TOTAL transclusions on the five pages: 32; absent/null: 0
```

The primary repair holds: not one of the five pages still renders a
transclusion that resolves to absent. Every id the directive named as dead
(`opus-4-7`, `opus-4-8`, `grok-4-3`'s index, `gpt-5-4`'s index,
`gemini-3-1-pro-preview`'s index, `gemini-3-5-flash`'s index) is gone from the
prose, and every fact was left in its entry's front matter unedited, as
`addictedtoai-ego8` requires.

## Comparative claims, re-derived against today's values

Measured values (`benchmarks.artificial_analysis.intelligence_index`,
`pricing.prompt`, `context_length`):

```
anthropic/claude-opus-4.5   ctx 200000    price 0.000005  (created 2025-11-24)
anthropic/claude-opus-4.6   ctx 1000000   price 0.000005  (created 2026-02-04)
anthropic/claude-opus-4.7   ctx 1000000   price 0.000005
anthropic/claude-opus-4.8   ctx 1000000   price 0.000005
anthropic/claude-opus-5     ctx 1000000   price 0.000005
x-ai/grok-4.3   price 0.00000125 (2026-04-30)   x-ai/grok-4.6 price 0.000002 (2026-08-12)
x-ai/grok-4.5   index 45.5 (2026-07-08)         x-ai/grok-4.6 index 50.6
openai/gpt-5.6-sol 51.3 / 0.000002   terra 46.8 / 0.000002   luna 43.4 / 0.0000002
google/gemini-3.5-flash price 0.0000015   3.6 0.00000075   3.7 0.00000075   3.8 0.00000075
google/gemini-3.6-flash 40.3 (07-21)   3.7 45.2 (08-13)   3.8 47.1 (09-02)
deepseek/deepseek-v4-flash-0731 price 0.000000065  index 40.8
openai/gpt-5.4 price 0.0000025   google/gemini-3.5-flash price 0.0000015
```

Checked and **true as rewritten**:

- anthropic — "quintupled": 200000 → 1000000 is exactly 5×, and the 4.5 row is
  dated 2025-11-24, so "the November row" holds. "Every release since carries
  the [1000000] this one does" holds for 4.6, 4.7, 4.8 and 5. "A list price
  that never moved" holds: all five rows are `0.000005`/`0.000025`.
- spacexai — price 0.00000125 → 0.000002 rose; index 45.5 (July row) → 50.6
  (August row) rose. Both surviving points, correctly re-anchored to 4.5→4.6
  after `grok-4.3`'s index went null.
- openai — Sol 51.3 > Terra 46.8 > Luna 43.4 matches the changelog's promised
  ordering. Sol and Terra both list at `0.000002`, Luna at `0.0000002`: "three
  rungs; the input column has two" is exactly right, and it is a genuinely
  better sentence than the "lands within a point" arithmetic it replaced.
- deepseek — 0.0000025 / 0.000000065 = 38.5×, and 0.0000015 / 0.000000065 =
  23.1×. "Both are more than an order of magnitude above the DeepSeek row" is
  true of each. Dropping the index comparison and keeping the price argument
  was the right call; the Willison quote still has DeepSeek's own 40.8 as its
  anchor one paragraph up.
- google-deepmind — "two climbs in six weeks": 40.3 → 45.2 → 47.1, both up,
  21 July to 2 September = 43 days. "Halved": 0.0000015 → 0.00000075 exactly.
  "The two releases since have held it there": 3.7 and 3.8 both `0.00000075`.
  "Twenty-three days after the price cut": 21 July → 13 August = 23 days
  exactly, and 3.7 lists at the same price as 3.6. All correct.

Also checked the spacexai framing "What rose while the window fell" — the
index climb it now cites (4.5→4.6) sits in a stretch where the window was flat
at 500000, while the price claim (4.3→4.6) does span the fall. Read as a
summary across the four-release run rather than an interval claim it holds, so
I am not blocking on it.

## The required change

**`content/wiki/org/google-deepmind.md`, second paragraph — two numbers on two
different scales, presented as one.**

The paragraph now reads (rendered):

> ...puts `google/gemini-3.8-flash` at **47.1**, against **45.2** for the
> August release and **40.3** for July's — two climbs in six weeks.
> llm-releases published its own split of the 3.8 Flash score by reasoning
> effort on 3 September: **59 at high reasoning, 57 at medium, 52 at low; up 3
> from Gemini 3.7 Flash at high, level with sub-maximum efforts of GPT-5.6 Sol
> and Grok 4.6**.

`intelligence_index_by_effort` is a **cited** fact on
`content/wiki/model/google-gemini-3-8-flash.md` carrying
`accessed: "2026-09-03"` — retrieved the day *before* the rebase. Measured
from the two committed blobs:

```
google/gemini-3.8-flash   58.7 -> 47.1   ratio 0.802
google/gemini-3.7-flash   56   -> 45.2   ratio 0.807
```

So llm-releases' "59 at high" tracked the pre-rebase catalog value of 58.7,
and the sentence was coherent when written. It is not now. Three separate
things break, all reader-visible:

1. Every effort level in the cited split (59 / 57 / 52) sits **above** the
   headline 47.1 the previous sentence gives for the same model — the lowest
   effort exceeds the overall score.
2. "Up 3 from Gemini 3.7 Flash at high" contradicts the sentence immediately
   before it, which puts the 3.8-vs-3.7 gap at 47.1 − 45.2 = **1.9**.
3. "Level with sub-maximum efforts of GPT-5.6 Sol and Grok 4.6" is false on
   today's numbers: Sol is 51.3 and Grok 4.6 is 50.6, so "52 at low" is above
   both models' *maxima*, not level with their sub-maximum efforts.

This is the same defect the job was dispatched to end — a rebase leaving prose
asserting a comparison the numbers no longer support — on a sentence the
author rewrote, so it is in scope rather than pre-existing damage left alone.
The directive's "do not add a rebase aside to every page" forbids five copies
of a generic aside; it does not license leaving one page where two scales
collide on screen, and `addictedtoai-ego8` (quoted in the directive) says the
fix for exactly this shape "is a line of attribution". Either repair is fine:

- attribute the split as a pre-v4.2 reading, so the reader knows the 59/57/52
  and the 47.1 are not the same scale; **or**
- drop the `intelligence_index_by_effort` transclusion from this paragraph and
  keep what the paragraph is arguing, which the surviving 40.3/45.2/47.1 run
  already carries on its own. The fact itself stays in the model entry either
  way — do not edit or delete it.

**Same sentence, second point:** "llm-releases **published** its own split ...
**on 3 September**" is not supported by the record. The front matter carries
`accessed: "2026-09-03"`, which is when the corpus retrieved the page, not
when llm-releases published it — 3.8 Flash shipped 2 September, so either date
is possible. The text this replaced ("splits the same index by reasoning
effort") made no publication-date claim. Say "as read on 3 September", or drop
the date.

**Third, in `data/proposals/flag-declared-feed-fields-that-go-null.md` —
the `evidence:` field states two things the blobs it cites contradict.**

The proposal's argument is sound and its five named cross-entry pages all
check out (I resolved each: `opus-4-8`→`opus-4-7#intelligence_index` null,
`kimi-k3`→`kimi-k2-5#intelligence_index` null, `gpt-5-6-luna`→
`gpt-5-4-nano#intelligence_index` and `#agentic_index` both null,
`gpt-5-6-terra`→`gpt-5-5#intelligence_index` null, `glm-5-1`→
`glm-5-2#intelligence_index` null). The 164 → 52 count is exactly right. But:

```
{ down: 50, up: 0, same: 0, lostNumberButRowLives: 113, rowVanishedEntirely: 1 }
ratio p25/median/p75: 0.781 0.803 0.827
row ids present previously and absent now: 2
  [ 'ibm-granite/granite-4.1-8b', 'qwen/qwen3.8-max' ]
```

- "**114** rows lost the number while the row itself stayed live" — measured
  **113**. The 114th, `qwen/qwen3.8-max`, did not keep its row; the whole row
  left the snapshot.
- "**No row vanished**, so `vanished_feed_rows` produced nothing and no queue
  item existed" — two row ids vanished between the two fetches, and **both are
  declared** by an entry's `feeds:` map (`qwen-qwen3-8-max.md` and
  `ibm-granite-granite-4-1-8b.md`); `qwen/qwen3.8-max` carried 53.4 the day
  before. `vanished_feed_rows` therefore had material that day, and the clause
  claiming it produced nothing is false.

The proposal does not need this claim to stand — "a field going null on a
surviving row is invisible to every detector the site has" is still true, and
that is the whole idea. Fix the two numbers and drop the "no row vanished"
clause rather than the proposal.

## Scope

Clean. The diff touches the five named content files and nothing else in
`content/`; `.job/brief.md` and `.job/source.json` are loop-written, and there
is exactly one proposal file, typed `machinery` rather than `repair`. No
reserved path is touched. No facts were edited or deleted on any of the five
pages — I diffed the front matter of all five and every `facts:` block is
byte-identical to its parent.
