---
job: j-20260904-53
verdict: approve
reasons: []
would-cite: >-
  Whoever next diagnoses a Desk run that reported `done` while the live site's
  build stamp stopped advancing. They would link this proposal in the argument
  that the foreign-content refusal is not the defect — the defect is that
  `publishStep` returns a reason with no consumer, so a correct guardrail stops
  the run without reporting to anything that outlives the run. It is also the
  citable answer to "why is a beads issue not enough here": beads issues are not
  one of the loop's three work sources, so an issue alone can never reach the Desk.
proposal:
  slug: beads-child-issue-ids-fail-the-declared-issue-join
  type: machinery
  date: 2026-09-04
  summary: >-
    `loop/lib/issues.mjs`'s `ISSUE_ID_RE` is `/^addictedtoai-[a-z0-9]{2,32}$/`,
    which cannot match a beads CHILD id — the dot in `addictedtoai-vqp7.1` is
    outside the character class. Because `readProposals` treats a declared
    `issue:` that fails the format check as MALFORMED and skips the whole
    proposal (loop/lib/proposals.mjs:229-241), a proposal whose issue happens to
    be a child issue must either drop the join or be silently removed from the
    work sources. That is a guardrail that reads as present and, for one whole
    class of real ids, refuses the correct value. The proposed job widens the
    format to admit beads' `<parent>.<n>` suffix (and whatever `bd` mints for
    deeper nesting), adds the child form to `loop/tests/issues.test.mjs`, and
    checks `scripts/verify-issue-links.mjs` resolves it against the store. The
    header's own reasoning already argues for this direction: "a format check
    that rejected a real id would be worse than one that accepts a string beads
    would never mint".
  evidence: >-
    Measured 2026-09-04 against this worktree's own modules:
    `isIssueId('addictedtoai-vqp7.1')` returns `false` while
    `isIssueId('addictedtoai-vqp7')` returns `true`, and
    `declaredIssueIds('addictedtoai-vqp7.1')` returns
    `{"present":true,"ids":[],"malformed":["addictedtoai-vqp7.1"]}`.
    `addictedtoai-vqp7.1` is a real, `bd show`-able issue — it is the very issue
    this job filed. The proposal under review names it in prose only and declares
    no `issue:` field, which is the only way it could stay selectable.
carry:
  - title: Complete the reason list in the proposal's summary — three more non-publish reasons exist
    detail: >-
      The summary enumerates the reason values as "(`foreign-content`,
      `commit-failed`, `tree-unreadable`, `hold`, `disabled`)". Counted directly
      in pulse/lib/publish.mjs, `publishStep` has eight `return { published: …`
      sites and returns three further `published: false` reasons the list omits:
      `no-config` (:493), `nothing-owned` (:634) and `stamp-did-not-advance`
      (:714), plus `dry-run` (:623). `nothing-owned` matters most — it is on the
      real path (:632, reached only when publish is true) and is exactly as
      silent as `foreign-content`; `stamp-did-not-advance` is the one that
      already writes HOLD.md and so needs no new surfacing. Naming the omitted
      three, or dropping the parenthetical for "whatever `reason` the step
      returns", would keep step 2 of the plan from being built against a partial
      list.
    subject: data/proposals/surface-the-publish-steps-discarded-result.md
---

## What I checked

The diff is two files: the carried finding `data/carried/j-20260904-50-carry-1.md`
is deleted (which is what retires it, per its own "Retiring this item" section),
and one proposal is added. The issue the brief actually asked for lives in beads,
outside the diff, so I checked it there.

**The issue exists and says what the brief asked.** `bd show addictedtoai-vqp7.1`
returns an open P2 bug, "The Desk's publish can now be refused by a file no job in
the run touched, and the run still reports done", whose body reproduces the
measurement, names `4f8d9a3`, `loop/run.mjs:1552`, `pulse/lib/publish.mjs:527` and
`:573`, and explicitly states it is not a defect report against the landed change.
That is the carried item's ask, discharged.

## Every cited line, read

- `loop/run.mjs:1552` — `if (publishAfterRecords) await publishStep(ctx, { cfg, owned: staged });`
  A bare `await`, no assignment. The claim "both callers throw the return value
  away" holds here.
- `pulse/run.mjs:284` — `await publishStep(root, { dryRun: …, assumePublish: …, log, owned });`
  Also bare; `owned` is built at :279-283 from mints, timeline appends and vanished
  records, exactly as the evidence says.
- `pulse/lib/publish.mjs:527` — `} else if (tree.foreignContent.length) {`, setting
  `commitBlocked = 'foreign-content'` at :541.
- `pulse/lib/publish.mjs:573` — `if (commitBlocked)` returns `{ published: false,
  reason: commitBlocked, commit, …foreign }` without reaching the push.
- `loop/run.mjs:1564` — `ctx.log(\`job ${jobId} complete — outcome ${outcome}\`)`,
  the unconditional terminal line the proposal's step 2 says already exists. It
  does, and it prints twelve lines after the discarded publish result.
- `4f8d9a3` — `loop: publish after the job's records exist, and declare what it wrote`. Correctly named.

## I reproduced the load-bearing measurement

The evidence block is the whole basis of the proposal, so I ran it rather than
reading it. Scratch repo under the OS temp dir (`node <tmp>/repro.mjs`), this
worktree's `data/config.json` copied in, one committed base, then a single stray
`content/wiki/model/someone-elses-draft.md` left dirty; `--dry-run` with
`assumePublish`. Nothing touched `D:/AddictedtoAI` and no remote was configured.

Declared caller (`owned: []`):

```
{"published":false,"reason":"foreign-content","commit":{"attempted":true,"committed":false,"paths":[],"reason":"foreign-content","foreign":["content/wiki/model/someone-elses-draft.md"]},"foreign":["content/wiki/model/someone-elses-draft.md"]}
DECLARED commit — refusing: 1 uncommitted file(s) under content/ that this run did not write (content/wiki/model/someone-elses-draft.md). …
DECLARED publish — not pushing: the commit above stopped at `foreign-content` — a run that could not commit its own state has nothing it can honestly publish
```

Undeclared caller (`owned` absent), same tree:

```
pulse: publish   would run: git -C …\pubrepro-38tHVz add data content
pulse: publish   would run: git -C …\pubrepro-38tHVz commit -m "pulse: 2026-09-04 data and content update"
pulse: publish   would run: git -C …\pubrepro-38tHVz push origin main
```

Both halves of the quoted measurement reproduce exactly, including the "exactly
two log lines" claim — the declared call emitted two `log.step` lines and no
others. The proposal's abbreviated JSON omits the `commit` sub-object, but every
field it does assert is present with the asserted value, so I read that as
abbreviation rather than overstatement.

## The other factual claims

- **"sets no ledger field"** — `grep published` finds nothing in `loop/lib/ledger.mjs`,
  and the ledger line is appended at `loop/run.mjs:1029`, more than five hundred
  lines *before* the publish step runs. Supported twice over.
- **"changes no exit code"** — `loop/run.mjs:1571` returns
  `{ started, jobId, branch, outcome, mergedSha, ledgerLine, result }`. The publish
  result is not among them.
- **"beads issues do not feed the derived queue"** — this is the sentence that
  justifies writing a proposal at all, so I checked it rather than granting it.
  `gatherCandidates` (`loop/lib/select.mjs:68-`) reads exactly three sources:
  `readDirectives`, `readQueue`, `readProposals`. `loop/lib/issues.mjs`'s own header
  says it "deliberately does not … invoke `bd`, read the Dolt store, or check that
  an id names an issue that exists". An issue is a format-checked join, never a work
  source. The claim is correct and the proposal is the only route.
- **`data/config.json` has `publish: true`** — confirmed, line 2.
- **`addictedtoai-ps3`** — cited in `publish.mjs:533` and in `publish.test.mjs`'s
  header, which records the 2026-08-29 incident the refusal exists to prevent.
  The quoted guardrail sentence ("reports it and stops; it does not loosen the
  guardrail to get past it") is verbatim from the project instructions.

## Whether this diff is in scope, and whether the proposal is well-formed

The brief asked for one thing — file the finding as its own issue — and the
retirement note asked for the carried file to be deleted in the same diff. Both
are done. The added proposal is the one thing that goes past the literal ask, so I
checked whether it is sanctioned rather than assumed. It is: the loop has explicit
machinery for proposals a job's branch adds (`applyProposalMergeRules` — cap, stamp
`proposed_by_type`, auto-discard on self-type), so a job adding a proposal is a
designed act, not an escape. This job's `.job/source.json` records `"type":
"repair"` and the proposal declares `type: machinery`, so the self-amplification
check at `proposals.mjs:746` does not fire, and one file is within any cap. It also
survives `readProposals`: kebab `slug`, `machinery` is in `JOB_TYPES` (measured —
`["interpret","verify","entry","tutorial","post","education","scout","repair","prune","machinery"]`),
no `issue:` to malform, no `expires:`, and no colliding slug in
`data/proposals/rejected/`. It cools until 2026-09-07. Not a scope violation.

`data/derived/queue.json` still lists the deleted carried file as a subject. That
is correct and not a broken reference: `data/derived/` is a pure function of state,
recomputed from scratch on every Pulse run, and no job regenerates it.

## What I did not do

I did not re-run `npm test` or `npm run build`; the loop reports both PASS on
`a0cc2fb4ff26`, the commit this diff was computed from. I made no edit to the
worktree.
