---
job: j-20260905-16
verdict: approve
reasons: []
would-cite: >-
  Whoever implements the seeding step will link the wiki delta's second
  requirement — it is the only place in this repository that argues, from the
  fact that MECHANICAL_FRONT_MATTER_KEYS is matched by bare key name with no
  per-kind scoping, that a facet with a machine regime and an editorial regime
  cannot be one field, and it shows both failure modes (an unreviewed judgment
  on one side, 544 spuriously mismatched review records on the other) instead
  of asserting a preference. Separately, the ui-loop session will cite the two
  recount parentheticals when it reconciles its own artifacts: they are the
  record that "28 of 35 listings map to no domain", which DESK-ORDER-001 §3
  leans on to make the facet optional on tools, is 35 minus seven category
  *names* rather than a count of listings, and that the decision it supports
  survives the correction anyway.
carry:
  - title: Soften "at least two listings in three" — 23 of 35 is 65.7%, just under two-thirds
    detail: >-
      The revision replaced the old "four listings in five" with "at least two
      listings in three", in the proposal ("An axis that leaves at least two
      listings in three with nothing to say") and in the directory delta ("A
      required field here would force a wrong answer onto at least two listings
      in three"). At the lower of the two readings the same sentences state —
      23 of 35 — the fraction is 0.6571, and two in three of 35 is 23.33, so
      the "at least" floor is breached by a third of a listing. It holds at the
      26 reading (0.7429). Both exact counts are printed alongside, so nothing
      is hidden, but the qualifier is doing work it does not quite earn.
      "roughly two listings in three", or simply "onto 23 of the 35", would be
      exact under either reading.
    subject: openspec/changes/tag-the-corpus-by-domain/specs/directory/spec.md
---

Delta review, pass 2. Scope: only the revision commit.

**The diff pasted into my brief was stale.** It showed the branch as of
`ab01deb` — the pre-revision draft, with `28` still in both places. `git
rev-parse HEAD` is `3bda9ad50a3d2751643bbcace98371ff0ed50793`, the tree is
clean, and the revision is real. I judged the actual committed state, not the
pasted diff. `git diff ab01deb 3bda9ad --stat`:

```
 .../changes/tag-the-corpus-by-domain/proposal.md   | 21 ++++++++++++++++-----
 .../specs/directory/spec.md                        | 22 ++++++++++++++++------
 openspec/changes/tag-the-corpus-by-domain/tasks.md |  5 +++--
 3 files changed, 35 insertions(+), 13 deletions(-)
```

Three files, exactly the three the previous findings named. Nothing else moved.

## Finding 1 — the 28 count. Fixed, and the fix reproduces.

I recounted the tree myself rather than accepting either number. Script over
`content/directory/tools/`, parsing `category:` out of each file's front matter:

```
md files: 36 listings with category: 35
inference 5 | local 5 | coding 3 | data 3 | observability 3 | retrieval 3 |
training 3 | agents 2 | audio 2 | evaluation 2 | frameworks 2 | image 2
sum: 35
seven neutral categories total: 23 | plus retrieval: 26
```

(`README.md` is the one file with no front matter, which is why 36 files give
35 listings.)

Every figure the revision now states is exact. The seven per-category counts it
prints — `local` (5), `inference` (5), `training` (3), `observability` (3),
`data` (3), `frameworks` (2), `evaluation` (2) — match one for one and sum to
23. The twelve it attributes to the other side — coding (3), agents (2), image
(2), audio (2), retrieval (3) — also match. The alternative reading it offers,
26 with `retrieval` left unmapped, is 23+3 and holds.

The attribution of the wrong figure is likewise exact. All three upstream
artifacts do say 28, at the lines the revision names:

- `EN-domain-facet.md:133` — "…observability, data, frameworks, evaluation →
  **none**. **28 of 35 listings map to no domain** — different axes, so
  `domain` must be optional on tools."
- `DESK-ORDER-001.md:84` — "…28 of 35 tool listings map to no domain, so it is
  optional on tools."
- `SPEC-REVIEW-GUIDE.md:36` — "…is a different axis — 28 of 35 listings map to
  no domain."

And §4's own mapping is verbatim what the revision says it is, including the
counts in parentheses: "coding(3)→`coding` · agents(2)→`agents` ·
image(2)→`image` · audio(2)→`audio` · retrieval(3)→`research` · local,
inference, training, observability, data, frameworks, evaluation → **none**."
So the revision's "that is the count under `EN-domain-facet.md` §4's own
mapping, which sends `retrieval` (3) → `research`" is supported by the document
it names, and the diagnosis "28 is 35 minus the seven category **names**" is
arithmetic anyone can check.

Both requested pieces landed: the number is corrected in both places, and the
discrepancy is flagged in the same parenthetical form the draft already used
for 545-versus-544 and the stale 431/446 — so the reviewing session sees that
its own artifact carries the figure rather than inheriting it silently. The
conclusion is unchanged, as asked: `domains` stays optional on tool listings,
and the delta says so explicitly.

The one residue is the derived fraction, which I have carried rather than
blocked on. See `carry:` above — 23/35 is 0.6571 against two-thirds at 0.6667.
The exact counts sit in the same sentences, so a reader is not misled about
anything material, and blocking a last-pass job on a third of a listing when
the arithmetic is printed beside the claim would be manufactured severity.

## Finding 2 — the §6 attribution. Fixed, and §6 says what the revision says.

`tasks.md` previously read "DESK-ORDER-001 §6 sequences §3 after §1 and after
the reviewing session has read this delta." It now reads "…§6 sequences §3
after §1 (§6 says nothing about review); and the maintainer's 2026-09-05
instruction is that the ui-loop session reads this delta before it is treated
as final."

`DESK-ORDER-001.md:112-121`, read in full:

```
## 6. Sequence

1. §1 fields + criteria + gate, and the backfill (unblocks the UI's next brief immediately).
2. §3 vocabulary after the keeper's three answers; gate on it.
3. §5 radar feeds registered.
4. §4 claim record and lead-change kind; rights checks; indices registered only when rights clear.
5. §2 coverage widening, ongoing.
```

Five ordered items, §3 second after §1, and no mention of review, a reviewing
session, or the ui-loop reading anything. The correction is right on both
halves: what §6 does say, and where the other half actually comes from.

## Gate, re-run here

```
$ openspec validate tag-the-corpus-by-domain --type change --strict --no-interactive
Change 'tag-the-corpus-by-domain' is valid
exit=0
```

I did not re-run `npm test` or `npm run build`; the loop ran both on this
commit and reported PASS, and nothing in a three-file markdown revision under
`openspec/changes/` could move them.

## Scope

The revision touched only `openspec/changes/tag-the-corpus-by-domain/`. No file
under `openspec/specs/` was modified, nothing was archived, `data/config.json`,
`runners.yml` and `lib/` are untouched, and no entry or listing was tagged. The
stated outcome — draft in full, validate, stop — is still exactly what the
branch contains.

One observation, not a finding and not part of the diff: there is no `RESULT.md`
at the worktree root and `git status` is clean, so I could not read the
author's account of which requirements it was least sure about. The four
uncertainties are named in the proposal's last section, which is where the
outcome required them, so nothing is missing from the deliverable itself.
