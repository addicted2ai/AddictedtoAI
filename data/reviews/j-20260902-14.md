---
job: j-20260902-14
verdict: approve
reasons: []
would-cite: >-
  Whoever picks up the `delta-review-brief-embeds-pre-revision-diff` machinery
  job would link this paragraph, because it is what tells them the fix is two
  values wide and not one: the embedded diff AND the gate report are both
  computed once before the revision pass and both re-served under a SHA read
  fresh from the post-revision branch head. Anyone arguing that a reviewer's
  "do not re-run the gates" instruction needs the SHA it names to be the SHA
  the gates ran on would cite the j-20260902-08 pair of briefs it points at.
---

Free-form notes.

The diff adds one paragraph to `data/proposals/delta-review-brief-embeds-pre-revision-diff.md`
and deletes `data/carried/j-20260902-12-carry-1.md`. That is exactly the stated
outcome (target file + the brief's "delete this file as part of the same diff"
retirement instruction) and nothing else; `.job/brief.md` and `.job/source.json`
are the loop's own. No code, no derived state, no reserved path.

I did not re-run `npm test` / `npm run build` — the loop reports both PASS on
`a05511c9ffc0`, and this diff is two markdown files. I ran the checks that would
be red if the paragraph's specific claims were false.

**Every line-number and quote claim in the added paragraph, checked against the
files in this worktree:**

- `loop/lib/review.mjs:264` — reads
  `The loop ran these itself, in this branch's own worktree, immediately before`
  (the sentence continues on 265 with the interpolated ` on commit \`${sha.slice(0,12)}\``
  built at line 249). The quoted sentence is where the paragraph says it is.
- `loop/run.mjs:342` — `gateResult = typeof gates === 'function' ? gates(ctx, worktree) : runGates(ctx, worktree);`,
  with `gateReport = { ran: true, ok: ..., results: ... }` on 343. Confirmed:
  this is *outside* the `for (;;)` review loop, which opens at line 364.
- `loop/run.mjs:505` — `commitAll(ctx.repoRoot, worktree, \`job ${jobId}: revision\`, ...)`.
  Confirmed as the revision commit.
- **"never re-runs them after the revision commit"** — the check that would be
  red if false: `grep -rn "runReview(" loop/run.mjs` prints exactly one line,
  `380:    const rev = await runReview(ctx, {`, and its argument at line 389 is
  `gates: gateReport` — the object assigned once at 343. `diffText` is likewise
  computed once, at 361, outside the loop. There is no second `runGates` call
  anywhere in the loop body. Confirmed.
- `loop/lib/review.mjs:715` — `const before = gitTry(ctx.repoRoot, ['rev-parse', branch]).stdout.trim();`,
  handed on at 729 as `sha: before` into `assembleReviewBrief`, which at 354
  calls `${gatesSection(gates, sha)}`. The "reads the SHA fresh from the branch
  head and hands it to `gatesSection`" claim holds end to end.
- **"Do not re-run them."** — `loop/lib/review.mjs:269`, verbatim, inside the
  same section. Confirmed.

**The j-20260902-08 claim is the one that could have been written from intent
rather than measurement, so I measured it.** Both briefs from that job are still
on disk in the worktree root. Grepping the gates section of each:

`j-20260902-08-review-1-brief.md`:

```
54:The loop ran these itself, in this branch's own worktree, immediately before
55-this review on commit `00ecc2eedb6f` — the same commit the diff below was computed from:
57:- `npm run test` — **PASS**
58:- `npm run build` — **PASS**
```

`j-20260902-08-review-2-brief.md`:

```
54:The loop ran these itself, in this branch's own worktree, immediately before
55:this review on commit `d29ae884daf0` — the same commit the diff below was computed from:
57:- `npm run test` — **PASS**
58:- `npm run build` — **PASS**
```

Byte-identical PASS lines, two different SHAs. That is the defect the paragraph
describes, observed directly rather than inferred from the code path — one gate
run, reported twice, the second time under a commit it was not measured on.

The two SHAs are what the paragraph says they are:
`git -C D:/AddictedtoAI log --oneline -1 00ecc2e` prints
`job j-20260902-08: reword the endpoints caveat on the Opus 5 fast page (done)`
(the author commit, carrying the text review 1 rejected), and
`git rev-parse d29ae88^` prints `00ecc2eedb6f0509e6012ede0d7e901e87f596c2` —
so `d29ae884daf0` is the revision commit whose direct parent is the commit the
PASS was actually measured on. "PRE-revision tree under the POST-revision SHA"
is precise, not rhetorical.

**The deletion.** `pulse/lib/queue.mjs:394` `carriedFindingItems` produces one
queue item per file under `data/carried/`, so removing the file is the
documented retirement and nothing else needs editing.
`grep -rn "j-20260902-12-carry-1"` over the worktree returns nothing — no
dangling reference left behind. No derived state is committed, consistent with
`data/derived/` being recomputed from the merged tree.

**One thing I checked before deciding not to raise it:** the proposal's opening
paragraph (line 10), which a future machinery job reads as its outcome, still
names only the diff. But `loop/lib/proposals.mjs:265` builds the job detail as
`${p.fm.summary ?? ''}\n\n${p.body}` — the whole body — so the new paragraph
does reach the job that acts on this proposal. Not a gap.

Nothing to carry, nothing to propose.
