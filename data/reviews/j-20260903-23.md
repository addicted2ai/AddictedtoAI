---
job: j-20260903-23
verdict: approve
reasons: []
would-cite: >-
  A licensing writer arguing that "open-weight" has become a marketing word:
  this is the rare piece that reads the PolyForm Strict grant literally, notices
  that the community GGUF conversion outdrawing the official repo 77-to-1 is
  itself outside the grant it was published under, and shows its download
  arithmetic well enough that someone can check the window before quoting it.
carry:
  - title: Fold the GGUF's two 26 August dates into one clause
    detail: >-
      The repaired sentence now states 26 August twice for the same repo under
      two different labels — "bartowski's GGUF of Thomson-1.0-Small, latest
      update 26 August" and, in the parenthetical, "the conversion was created
      26 August". Both are true (the API gives createdAt 2026-08-26T04:45:39Z
      and lastModified 2026-08-26T07:26:55Z, under three hours apart), but a
      reader meeting the same date twice in one sentence reads it as an editing
      slip rather than as two distinct facts that happen to coincide. The
      sentence also now carries an em-dash pair inside a parenthesis inside an
      em-dash clause. Folding the opening clause to "bartowski's GGUF of
      Thomson-1.0-Small, created and last updated 26 August" states both facts
      once and lets the parenthetical shrink to the source repo's date.
    subject: content/blog/thomson-reuters-thomson-model.md
subject: "content/blog/thomson-reuters-thomson-model.md"
reviewed:
  "content/blog/thomson-reuters-thomson-model.md": "fa9fe8110b002d1343085b2851615768a609239ee0daa0c0561d1e42d7d9e83b"
---

## What the diff is

One sentence in `content/blog/thomson-reuters-thomson-model.md`, plus the
deletion of `data/carried/j-20260903-21-carry-1.md` that the brief requires in
the same diff.

Before:

> (the source repo is recent enough, created 18 August, that its window spans
> its whole life)

After:

> (both repos are recent enough — the conversion was created 26 August, the
> source repo 18 August — that each window spans its whole life)

## The check that would be red if the claim were false

The change rests on two `createdAt` values. I fetched both Hugging Face API
records directly rather than trusting the finding that prompted the job. Run
via a small Node script (`fetch` on the two documented API URLs, which are the
same two the piece already cites in its Sources list):

```
{
 "status": 200,
 "id": "bartowski/thomsonreuters_Thomson-1.0-Small-GGUF",
 "createdAt": "2026-08-26T04:45:39.000Z",
 "lastModified": "2026-08-26T07:26:55.000Z",
 "downloads": 94564
}
{
 "status": 200,
 "id": "thomsonreuters/Thomson-1.0-Small",
 "createdAt": "2026-08-18T15:33:05.000Z",
 "lastModified": "2026-08-28T12:27:31.000Z",
 "downloads": 1231
}
```

Fetched 2026-09-03, both HTTP 200.

- "the conversion was created 26 August" — `createdAt 2026-08-26T04:45:39.000Z`.
  Supported exactly, and it is the same value the carry record quoted, so the
  finding was not itself an error being propagated.
- "the source repo 18 August" — `createdAt 2026-08-18T15:33:05.000Z`.
  Supported; unchanged from the old text.
- "each window spans its whole life" — the post is dated 2026-09-03, so a
  rolling 30-day window opens 2026-08-04. Both creation dates fall after that
  boundary (22 days and 16 days before publication respectively), so neither
  window truncates anything. The deduction holds for both repos, which is
  precisely what the old parenthetical denied by singling out one of them.

The unchanged claim the sentence still makes around the repair also survives
the same fetch: 94564 / 1231 = 76.8×, which is what "roughly two orders of
magnitude" is doing there. Neither record returns a `downloadsAllTime` field,
so "lifetime totals" is a deduction from the creation dates rather than a
second measurement — but the diff does not use that phrase in the prose, it
says "each window spans its whole life", which is the deduction and is sound.

## Scope

`git diff main...HEAD --stat` gives four paths: the two loop-written job
artifacts (`.job/brief.md`, `.job/source.json`), the one-sentence content
change, and the carry-file deletion. The prose change is one line, one
sentence, in the stated target file. The deletion is the retirement step the
brief mandates; `grep` for `j-20260903-21-carry-1` across the worktree returns
no remaining references, so nothing dangles. Nothing exceeds the outcome.

## Not re-run

`npm test` and `npm run build` were run by the loop on this same commit
(`7c6b5f8eadb1`) and both passed. I did not repeat them — the diff is one
sentence of prose inside an already-validated file and touches no schema field,
no transclusion, no link, and no front matter.
