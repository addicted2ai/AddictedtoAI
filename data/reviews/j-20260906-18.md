---
job: j-20260906-18
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a re-run of an already-merged directive should be spent
  on independent re-judgment rather than on rewriting correct bytes — this is
  the worked example of a verify job that found nothing wrong, said so, and
  still returned something: four criteria the first pass walked silently, now
  on the record, plus the measured cause of its own duplication.
carry:
  - title: Attribute the "without a person guiding each step" phrase to the one post that carries it
    detail: >-
      `verify-frontier-backfill-recheck.md` ("Domains: nothing to add") states
      that "both Astra posts describe a model acting 'without a person guiding
      each step'". Measured: the phrase appears only in
      `content/blog/openai-astra-critical-designation.md` (line 17). In
      `content/blog/openai-gpt-6-astra-system-card.md` the phrase is absent and
      the substring "agent" does not occur anywhere in the file at all
      (case-insensitive Grep, no matches), so nothing in that post supports the
      attribution. The conclusion the passage reaches — assign no `agents`
      domain, because `lib/domains.mjs` forbids inference — is unaffected and
      is if anything stronger. The fix is one word: name the single post that
      carries the phrase instead of "both Astra posts".
    subject: data/reviews/evidence/verify-frontier-backfill-recheck.md
---

Free-form notes.

## What I ran, and what it printed

I did not re-run the four gates the loop had already run on `d629bdd7149f`. I
ran the checks that would be red if a specific claim in this diff were false.

**1. The re-execution is reproducible, not narrated.** The diff's transcript
(`verify-frontier-backfill-recheck.raw.txt`) is the stdout of
`tools/check-frontier-backfill.mjs`, the gate committed by the *previous* job
(78625a5) and unmodified here. I ran it myself in this review worktree:

```
$ node tools/check-frontier-backfill.mjs
# 16 post files under content/blog/ (README.md excluded)
...
# split: 6 flagged, 10 declined, 16 total
# content/wiki/org/ — 24 entries
aion-labs, alibaba-cloud, amazon, anthropic, bytedance-seed, cohere, deepseek,
google-deepmind, inception-labs, inclusionai, meta-superintelligence-labs,
microsoft, minimax, mistral-ai, moonshot-ai, nous-research, nvidia, openai,
perplexity, poolside, spacexai, tencent, thinking-machines-lab, z-ai
| IFM / MBZUAI (decline) | NO |
| Thomson Reuters (decline) | NO |
RESULT: every post satisfies postSchema and matches the committed decision table.
EXIT=0
```

Row for row, this is the committed transcript. The org-entry absences that
carry the F3-first-branch declines for IFM and Thomson Reuters are measured
output, not assertion. (The worktree carries no `node_modules`; I junctioned the
repository's in, as `loop/lib/gates.mjs` does — the diff's own note about this
gap is accurate, the first run failed `ERR_MODULE_NOT_FOUND` on `gray-matter`.)

**2. The corpus really is the corpus the table judged.**

```
$ git log --oneline 78625a5..HEAD -- content/blog     (empty)
$ git diff --stat 78625a5..HEAD -- content/blog       (empty)
```

So "every existing blog post" is covered by construction. Ten posts carry no
`frontier` key, which is correct rather than incomplete: DESK-ORDER-001 §1 line
29 declares `frontier: true # optional; absent = false`, and the directive's
other half — "record each decline with its reason in the review" — is what the
evidence files discharge.

**3. The two prebuild numbers, which are the only quantitative claims in the
diff the loop's gate list did not already cover.** I ran `node
scripts/prebuild.mjs` and it printed, verbatim:

```
prebuild: review binding — of 180 reviewable piece(s): recorded 55,
  mismatched 0, unbound 125, missing 0
prebuild: volatile-literal coverage — post: 0 document(s) with an
  author-prose front-matter field scanned, 16 with none
```

Both are quoted exactly right in the evidence file, including the arithmetic
around them: the six `mismatched` pieces that `j-20260906-17` disclosed as its
expected end state are cleared, and `recorded` stands at 55. I did not re-run
`verify-launch.mjs --no-build` because this review worktree has no `out/` tree
to check against; `mismatched 0` is the input that would have made it red, and
I measured that directly.

**4. Thirty-five quoted fragments, checked against the post bytes.** I scripted
a substring check of every quotation the two evidence files attribute to a post
— the IFM page title "Frontier Performance, Radically Open", "among the top
models below 400B parameters", the 2.2% / 4.1% Artificial Analysis flag rates,
"setting new state of the art at their respective scales", "the complete
development process through agentic post-training"; the wiki-incident post's
`.blob.core.windows.net`, `/etc/hosts`, `97.5`, `8.5.2`, `collusion.wiki`,
"fourteen minutes"; Thomson Reuters' "on par with the latest frontier models"
and "surpassing recent flagship releases"; Daybreak's "2,000 approved
organizations" and "in the coming weeks"; the EU AI Office post's "No model has
been pulled, no market access restricted"; the DoJ post's "letter the court is
free to read"; GitSpawn's "The model never enters into it"; MiniMax's
"Applicable Territory" and "is not authorized". **31 of 35 matched on the first
pass, and I ruled out my own instrument on all four misses before concluding
anything:**

- `"zero data retention is not available in workspaces … where Covered Models
  can be accessed"` — present, at lines 38-41 of the EFS post, with the elided
  span exactly as the ellipsis promises. My substring was defeated by the
  ellipsis, not by the corpus.
- `31 August 2026` — present at line 46 ("Fable 5.1 and Mythos 5.1 on 31 August
  2026"); I had searched the American form. `9 June 2026` also present. So the
  distinction the F5 consistency argument turns on — the EFS lockout has *dated
  entries by named models*, the ASL-2 sentence has none — is a fact about the
  two documents, which is what the evidence file claims for it.
- `It does not say "no"; it says nothing` — present at line 79 of the
  usage-policy post; the evidence file normalises the inner double quotes to
  single. Not worth carrying.
- `"without a person guiding each step"` in the *system card* post — genuinely
  absent, and so is the substring "agent". That one is carried above.

**5. The proposal's code claims, all four, read at the cited lines.**

- `loop/run.mjs` ~1099-1117 writes `{job, type, source, slug, path, issues}` into
  `.job/source.json` — no `lineNumber`. Confirmed; the committed
  `.job/source.json` in this very diff is the artifact, and it has no line number.
- `loop/run.mjs:963` — `job = { type: typed.type, source: 'resumed', title:
  \`resume ${jobId}\`, detail: '' }`. Confirmed verbatim.
- `loop/run.mjs:1402` — `if (job.source === 'directive' && job.lineNumber) {`.
  Confirmed verbatim. Both conjuncts are therefore false for every resumed
  directive job, which is the whole finding.
- `loop/lib/directives.mjs:107-119` — `markDirectiveDone` is idempotent and
  indexes `DIRECTIVES.md` by line number, so the proposal's step 4 (a stale
  index would mark the wrong line done) is a real hazard, not a rhetorical one.
- `loop/tests/resume-directives.test.mjs` covers `markDirectiveDone` twice
  (lines 146, 149) and resume *selection* (lines 33, 78, 90), and never merges a
  resumed job. The gap is where the proposal says it is.

And the measurement the proposal rests on, which is also the honest answer to
why this job exists:

```
$ grep -n "j-20260906-17" DIRECTIVES.md      (no matches)
$ sed -n '114p' DIRECTIVES.md
- verify: backfill `frontier` / `frontier_reason` / `domains` on every existing
  blog post ... addictedtoai-9c9t [unparked 2026-09-06 by the orchestrator: ...]
```

No `[done …]` marker. `data/reviews/j-20260906-17.md` reads `verdict: approve`;
`data/ledger.jsonl` records that job `"outcome":"done"` with `"mm":20`; and
`git show --stat 78625a5` is the backfill touching exactly the six flagged
posts. Every figure in the proposal's `evidence:` block is one I reproduced.

**6. The two-invocation claim, which is checkable and I checked it.** The
evidence file says the first invocation's transcript and this one differ only
in the timestamp line. `git diff eaac7a0 d629bdd -- ...recheck.raw.txt` prints
exactly one changed line, `# node v24.13.0, 2026-09-06T23:58:23.983Z` →
`2026-09-07T05:05:24.632Z`. Byte-identical otherwise, as stated — and the UTC/
local reconciliation (05:05Z = 23:05 Mountain on 2026-09-06) is disclosed rather
than quietly reconciled, which is the right handling under this repository's
local-date rule.

## On the judgments themselves

A backfill's content is sixteen judgment calls, and re-judging them is the only
thing a second pass can usefully do. I read F1–F5 in DESK-ORDER-001 §1 (lines
36-42) before reading the diff's reasoning about them, and I agree with the
six/ten split. The two calls I pushed hardest on:

- **IFM K2 Horizon under F5.** F5 is "a material change in access: *a frontier
  model* withdrawn, gated, or opened." The models were unambiguously opened; the
  argument turns entirely on the noun, and the evidence file's reading is right
  that the only "frontier" characterisation available is IFM's own page title,
  which the post explicitly declines to endorse, and that the sole cross-vendor
  numbers are reward-hacking *flag rates* — a cheating measure, not a capability
  measure. I confirmed all of those strings are in the post. The consequence the
  file draws is the sharp one and it is correct: an org entry for IFM would
  unblock F3's first branch and would not touch F5 at all, because coverage
  settles who is a covered organisation, not what a frontier model is.
- **`anthropic-usage-policy-government-exceptions.md` under F5 versus the
  flagged EFS post.** This is the one real consistency risk in the set and the
  diff went looking for it rather than waiting to be asked. The distinction it
  draws is documentary: dated entries by named models on one side, a standing
  scope with no crossing on the other; an affirmative restriction on one side,
  the post's own "it does not say 'no'; it says nothing" on the other. I checked
  both quotes in both posts. The flag and the decline both hold, and they hold
  for reasons a reader can re-derive.

## On scope

The stated outcome is the backfill and its decline records. This diff changes no
post front matter, and I judge that correct rather than evasive — the keys are
already right (I ran the gate), and rewriting them would put six approved review
records back to `mismatched` for nothing, which the diff says and which the
`mismatched 0` line above shows would otherwise be the cost. What it does add
sits inside the outcome or inside an explicit allowance: the widened refile
condition is a decline record for a decline the directive names; the deletion of
`data/carried/j-20260906-17-carry-1.md` is that carry's own stated retirement
mechanism, in the same diff as the fix it asked for, and the fix it asked for —
judging F5 on its own terms rather than through an F3-shaped trigger — is
delivered in full; `tools/recheck-frontier-backfill.mjs` is the harness that
produces the transcript the acceptance checks demand; and there is exactly one
new proposal file, which is the cap. Not a scope violation.

No proposal noted. The one idea this review surfaced — a merge-side resume test
— is already the diff's own proposal, and manufacturing a second would be worse
than noting none.
