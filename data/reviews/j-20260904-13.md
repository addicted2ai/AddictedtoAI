---
job: j-20260904-13
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that a lab's headline prose and its own benchmark grid do not
  always agree would link this post's benchmark paragraph, because it prints
  OpenAI's table figure for FrontierMath Tier 4 (v2), 97.6%, directly against
  the same page's prose claim that Astra "saturates FrontierMath Tier 4 with a
  98% score" — and now says, correctly, which of the two is a row and which is
  a sentence.
reads-human: >-
  It reads human, and the one changed clause is the reason. "Three more rows
  from the release page's benchmark table" is the phrasing a writer reaches for
  when a pronoun has drifted off its antecedent — it names the thing instead of
  pointing at it, costs four words, and changes no rhythm around it. Nothing was
  padded to absorb the edit and the paragraph still ends on the HealthBench pair
  rather than on a summary of itself.
subject: "content/blog/openai-gpt-6-astra-system-card.md"
reviewed:
  "content/blog/openai-gpt-6-astra-system-card.md": "4190a9d402bc2dbcc13c56086d0d50314ed9420138beb9fa8157068aee0524b0"
---

Reviewed commit `d257b543b847` in `D:/addictedtoai-worktrees/j-20260904-13-review-1`.
I did not re-run `npm test` or `npm run build` — the loop recorded both PASS on
this commit and a one-clause prose diff cannot move either. I ran the one check
that would be red if the diff's claim were false: fetching the release page
itself and reading its table markup.

## The claim under review

The diff replaces "Three more rows from **the same table**" with "Three more
rows from **the release page's benchmark table**". That asserts three specific
things are rows in a table on `openai.com/index/gpt-6-astra/`: ARC-AGI-3 99.9%,
FrontierMath Tier 4 (v2) 97.6%, and HealthBench Professional 63.4% / 60.5%.

This mattered more than it looks. The finding it repairs (carry-3 of
`j-20260904-04`) established that one figure the post presents as a table row —
the SRE-Bench four-attempt pair — is release-page **prose**. So a repair that
re-asserted a table attribution for three further figures without checking them
would reintroduce the exact defect, one paragraph lower. The prior reviewer
could not check it: `j-20260904-11.md` records openai.com returning **403** to
both WebFetch and a raw `fetch`, and fell back to third-party reproductions.

## What I fetched, and what it says

WebFetch on the release page returned "HTTP 403 Forbidden" for me too. A Node
script issuing `fetch()` with a desktop-Chrome header set did not:

```
status 200 bytes 2821474
table elements found: 9
```

I parsed the `<table>` elements rather than substring-searching the text, because
the question is precisely *is this a row or is this a sentence* — and a substring
search cannot tell those apart. All nine tables, printed as rows. The three the
diff claims:

```
--- TABLE 8 (4 rows) ---
    Abstract reasoning | GPT-6 Astra | GPT-5.6 Sol | Claude Fable 5.1 | ...
    ARC-AGI-3 | 99.9% [T7] | 7.8% | - | - | 30.2% | -

--- TABLE 3 (5 rows) ---
    Academic | GPT-6 Astra | GPT-5.6 Sol | Claude Fable 5.1 | ...
    FrontierMath Tier 4 (v2) | 97.6% | 83.0% | 87.8% | 87.8% | 73.2% | -

--- TABLE 4 (5 rows) ---
    Science and Health | GPT-6 Astra | GPT-5.6 Sol | ...
    HealthBench Professional (length-adjusted) | 63.4% | 60.5% | 58.1% 10 | ...
```

All three are table rows, all three figures match the post exactly, and the
"length-adjusted" qualifier the post attaches to HealthBench is the row label's
own parenthetical. The new attribution is true, not merely safer than the old
one.

ARC-AGI-3 was the one I expected to fail, because the page's opening prose also
carries the number — "Astra also saturates ARC-AGI-3 with a 99.9% score" — which
is exactly the shape that trapped the SRE-Bench figure. It is in both places.
The table row is the stronger claim and it holds. Footnote 1 is verbatim what
the post attributes to it: "On ARC-AGI-3, GPT-6 Astra was run with our responses
API harness, which changes two settings to better match real-world performance."

I also confirmed, from the same fetch, that the finding's premise still stands
and the intro sentence above the table is right to have dropped "table":

```
TABLE 5 (Cybersecurity):  SRE-Bench | 88.0% | 55.9% |  |  | 12.5% |
prose: "Astra solved 88.0% of tasks in a single attempt and 99.2% within four
attempts, compared with 55.9% and 68.7% for GPT-5.6 Sol, respectively."
```

So the four-attempt pair is prose, the single-attempt pair is the row, and the
post's block is now introduced by "The release page ... carries the cyber rows",
which covers both honestly.

The one imprecision I weighed and am not blocking on: the page has **nine**
benchmark tables, and the three rows come from three different ones (Academic,
Science and Health, Abstract reasoning), so "the release page's benchmark table"
is singular where the page is plural. They render as one tabbed benchmark block
with category headers, which is what a reader sees, and the phrasing is verbatim
what the finding prescribed. It is strictly more accurate than "the same table",
which pointed at the post's own five-row table. Correct direction, and not worth
a revision pass.

## Scope

```
d257b54 job j-20260904-13 (repair): fix dangling 'the same table' ...
 content/blog/openai-gpt-6-astra-system-card.md |  2 +-
 data/carried/j-20260904-11-carry-1.md          | 16 ----------------
 2 files changed, 1 insertion(+), 17 deletions(-)
```

One clause and the retirement the brief requires. The `.job/brief.md` and
`.job/source.json` in the computed diff are the loop's own preceding commit
(`26f339c job j-20260904-13: brief`, 2 files, +631), not the author's work.
`data/carried/` holds 24 items and `j-20260904-11-carry-1.md` is gone from it, so
this finding does not return on the next Pulse run. No reserved path is touched.

The post's approved-surface hash moves with this edit, which would read
`mismatched` — `loop/lib/review.mjs` rebinds `reviewed:` after the merge, so that
is the normal path and not something this diff had to carry.

Nothing carried forward and no proposal. The finding was narrow, the fix is the
finding's own prescribed wording, and the check the prior reviewer could not run
now runs green against the primary document.
