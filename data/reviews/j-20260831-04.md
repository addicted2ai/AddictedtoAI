---
job: j-20260831-04
verdict: approve
reasons: []
would-cite: >-
  Anyone arguing that a derived lifecycle status is a reading of vendor
  metadata rather than a lifecycle event would link this line: it is the clean
  case where a router's `expiration_date` going null on the very day it fell
  due flipped the derived badge from `deprecated` to `active` for a model whose
  vendor platform was sunsetting that same day. It is also the citation for the
  narrower point that derived and authored surfaces drift — the annotation
  names the catalog row and the entry disagreeing about one model on one date,
  which is the kind of concrete instance a piece on freshness machinery needs
  and usually has to invent.
proposal:
  slug: price-moves-invisible-to-the-changed-feed
  type: machinery
  date: 2026-08-31
  summary: >-
    Investigate and report on the consequence of `event: false` on both
    OpenRouter price fields in `data/sources/registry.json`. Between the
    2026-08-30 and 2026-08-31 snapshots the `moonshotai/kimi-k2.5` row cut
    `pricing.prompt` from 0.0000006 to 0.00000045 and `pricing.completion` from
    0.000003 to 0.00000225 — a 25% cut on both — and `data/changes.jsonl`
    records nothing at all for it, because `diffSnapshots` skips any field the
    registry flags `event: false` (pulse/lib/diff.mjs:224). Price is still a
    catalog column and a bound fact, so the number on the page is current; what
    no longer exists is any dated record that it moved. The job would establish
    whether that is the intended trade (the flag cites addictedtoai-8ho) or
    whether the changed feed has quietly lost the one field a reader most
    expects it to carry, and record the finding either way.
  evidence: >-
    Measured in this branch's worktree on 2026-08-31 by reading the two
    committed snapshots. `data/sources/openrouter-models/previous.json`
    (date 2026-08-30) row `moonshotai/kimi-k2.5`: pricing.prompt "0.0000006",
    pricing.completion "0.000003". `latest.json` (date 2026-08-31, fetched_at
    2026-08-31T06:00:03.897Z): "0.00000045" and "0.00000225". A scan of all 92
    lines of `data/changes.jsonl` finds exactly two mentioning kimi-k2.5 — the
    status field_change under review and this job's annotation — and no price
    line. `registry.json` marks both price fields `"event": false`;
    `pulse/lib/diff.mjs:224` skips them; `isScheduled` is not the cause, since
    the row carries no `pricing.overrides`.
---

Reviewed the diff for job j-20260831-04: one appended annotation line in
`data/changes.jsonl`, one proposal file, and the job brief. Nothing else is
touched, and the annotation is a new final line — no existing line is edited.
That matches the stated outcome and the append-only rule.

I did not re-run `npm test` or `npm run build`; the loop's PASS on `45fc94cdbb06`
is recorded above the diff. Every check below is a targeted one aimed at a
specific claim in the diff.

## The annotation's claims, one at a time

**"cleared the 2026-08-31 expiration date it carried in the 2026-08-30
snapshot".** Read both committed snapshots directly. `previous.json` reports
`date 2026-08-30`, `fetched_at 2026-08-30T06:00:03.808Z`, 396 rows; its
`moonshotai/kimi-k2.5` row has `"expiration_date": "2026-08-31"`.
`latest.json` reports `date 2026-08-31`, `fetched_at 2026-08-31T06:00:03.897Z`,
396 rows; the same row has `"expiration_date": null`. Confirmed, including
"on the day that date fell due".

**"the registry's expiration-date rule derives `active` where it derived
`deprecated`" and "had the date stayed, that same rule would have derived
`retired` from 2026-09-01".** This is the one claim in the annotation that
could not be read off a file, so I measured it. `pulse/lib/diff.mjs` and
`pulse/lib/core.mjs` are byte-identical between this worktree and the main
checkout (`git hash-object`: `4dad552e…` and `f4ced9f5…` on both sides), so I
called the real `deriveStatus` against the real registry source object from the
main checkout, where `node_modules` exists — this worktree has none, so the
import of `cheerio` via `sources.mjs` fails there. Output:

```
today()= 2026-08-31
exp=null         daysSince= null -> active
exp=2026-08-31   daysSince= 0 -> deprecated
exp=2026-08-30   daysSince= 1 -> retired
exp=2026-09-01   daysSince= -1 -> deprecated
exp=2098-12-31   daysSince= -26420 -> active
```

The third line is the counterfactual the annotation asserts, in its exact
equivalent form: an expiry one day in the past derives `retired`. Had
`2026-08-31` survived into tomorrow it would be one day past, so `retired` from
2026-09-01 is right, and `deprecated` — not `retired` — was still correct
today. The annotation gets the boundary the right way round, which is the easy
thing to get wrong here.

**"the derived catalog row now reads `active`".** `data/derived/catalog.json`,
row `moonshotai/kimi-k2.5`: `"status": "active"`, `"expiration_date": null`,
`"entry_id": "model/moonshotai-kimi-k2-5"`. Confirmed, including the join the
next clause depends on.

**"the joined entry … still declares `status: deprecated`".**
`content/wiki/model/moonshotai-kimi-k2-5.md:5` is `status: deprecated`.
Confirmed. Note the annotation says *declares*, which is the accurate verb —
the entry also carries a `status` fact bound to `$status`, so the page is not
uniformly "deprecated" either. The wording does not overreach.

**"renders its feed-bound `expiration_date` as `not published`".**
`data/derived/feed-rows.json` gives that row `expiration_date: null` and
`$vanished: false`. In `lib/facts.mjs`, `resolveFact` treats `null` as
`missing` (line 142) and, with `$vanished` false, returns `{state:'absent'}`
(line 151); `ABSENT` at line 115 is
`<span class="fact-absent">not published</span>`, and `lib/facts.test.mjs:68`
pins exactly that string for the absent state. The entry declares the fact
feed-bound to `path: expiration_date` (lines 37–41) and transcludes it at
line 84. Confirmed — the vanished-row branch, which would have rendered a
last-known value instead, is not taken.

**"opens on the claim that this row carries a death date almost none do".**
Line 81: "This row has a death date on it, which almost none do." Confirmed
verbatim, and the transclusion two sentences later is the one that now reads
`not published`, so the contradiction the annotation points at is real and on
the live page.

## The proposal in the diff

Within the allowance: one file, type `entry` (not `interpret`, so not
self-amplification), front matter complete and dated today with an `expires`
seven days out, which is the honest carrier for evidence read off a snapshot
pair that rotates on the next Pulse run. Its factual claims spot-check clean:
the 388-rows/eight-expiry count it defends is the registry's own recorded probe
detail for 2026-08-28; `appendTimelineEvents` (pulse/lib/mint.mjs:188) does
what it says — it appends a dated `{date, event, source_url}` and never touches
an authored `status:`, and the entry carries the `2026-08-31 / active` event it
wrote (lines 72–74) alongside the sunset event; the `api_sunset` fact is
`source: cited` to platform.kimi.ai, not feed-bound, so the proposal is right
that OpenRouter clearing a field says nothing about Moonshot's platform. I
could not re-verify its one build-output claim (`out/…html` rendering
`data-state="absent"`) because this worktree has neither `out/` nor
`node_modules`; the resolution measurement above is the same fact one layer up.

## What I noted and did not hold against it

The same row also cut both prices ~25% in the same snapshot pair
(prompt 0.0000006 → 0.00000045, completion 0.000003 → 0.00000225), which is
arguably the stronger evidence that this is a live model rather than one being
wound down, and the annotation does not mention it. I checked before treating
it as an omission: both price fields are flagged `"event": false` in the
registry, `diffSnapshots` skips them (diff.mjs:224), and no price change line
exists for this row — so pricing is outside what the changed feed calls an
event at all, and an annotation keyed to the status change is not wrong to stay
inside that boundary. It is the proposal I noted instead.

The annotation is one sentence of mechanism and one of consequence, cites the
change key it annotates, states no number without the row that carries it, and
its second half is a real editorial finding rather than a restatement of the
data — it is the reason the proposal exists. It reads as someone who looked at
the page, not at the diff.
