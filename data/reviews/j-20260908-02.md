---
job: j-20260908-02
verdict: reject
reasons:
  - spec-violation
  - broken-reference
  - scope-violation
would-cite: >-
  A maintainer documenting the lifecycle of a vanished OpenRouter route would
  cite the durable answered record only if the published model entry and its
  feed binding remained available for readers and future re-listing.
---

The diff removes `content/wiki/model/minimax-minimax-m2-7-free.md` by moving it
to `data/vanished/answered/`, despite the task explicitly requiring that the
entry and its `feeds: openrouter-models: minimax/minimax-m2.7:free` binding be
kept. The moved file is the wiki entry itself, not a vanished-finding record:
its SHA-1 is identical to the main-branch target (`e32232899a1a8d6ff73b875be8330a85938f76be`), and its front matter has no
top-level `title`, `subject`, `source`, `row_id`, `last_seen`, or `date` finding
fields. It therefore preserves the wrong file in the answered store rather
than the vanished record with its pinned evidence.

The direct filesystem check printed `CONTENT_EXISTS=False` and
`ANSWERED_EXISTS=True`; the answered file still prints the entry's `feeds:`
block, but that does not keep the published entry or its binding in the corpus.
The root `data/vanished/` check printed `PENDING_RECORDS=1` and `README.md`, so
there is no pending vanished-row record for this job to move.

The diff also removes the MiniMax model from the organization’s mentions and
from aliases, backlinks, and search index. That is unrelated removal of the
published model surface, not an answer to the finding. The direct derived-state
check still finds `data/sources/openrouter-models/minted.json` pointing to
`content/wiki/model/minimax-minimax-m2-7-free.md`, and
`data/derived/catalog.json` still naming the entry; the content path is absent.
The built output check printed `OUT_ENTRY=False` and `OUT_DIR=False`, while the
main-branch search index had the route at line 3288 and the redirect check
printed `REDIRECT_MATCHES=NONE`. Thus the diff also removes an existing URL
without a redirect.

`git diff --check main...HEAD` produced no output. The loop-reported full gates
were not rerun, as required by the brief. This cannot be approved without
restoring the published entry and preserving its binding, adding the required
answered vanished-row record as a separate unchanged move, and restoring the
derived references/URL handling that the forbidden removal disturbed.
