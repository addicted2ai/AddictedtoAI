---
job: j-20260904-14
verdict: approve
reasons: []
would-cite: >-
  Someone writing about the gap between a press release's "open-weight" framing
  and the licence actually shipped with the weights would link this for the
  paragraph that reads PolyForm Strict literally and then points at the
  community GGUF — created and last updated the same day, outdrawing the
  official repository by two orders of magnitude on the platform's own 30-day
  counter — as the redistribution the grant does not cover. It is citable
  because the download comparison names its window instead of hiding behind a
  bare ratio.
subject: "content/blog/thomson-reuters-thomson-model.md"
reviewed:
  "content/blog/thomson-reuters-thomson-model.md": "89860dbcd56a32b642b6143850a64d2348099ac0935d2963dfcabdde858f13c1"
---

## What the diff is

Two files in the job's own commit (`2557ab1`): one sentence in
`content/blog/thomson-reuters-thomson-model.md` and the deletion of
`data/carried/j-20260903-23-carry-1.md`, which the brief requires in the same
diff. `git show --stat 2557ab1` prints exactly that:

```
 content/blog/thomson-reuters-thomson-model.md |  2 +-
 data/carried/j-20260903-23-carry-1.md         | 16 ----------------
 2 files changed, 1 insertion(+), 17 deletions(-)
```

The `.job/brief.md` and `.job/source.json` that also appear in the diff under
review are the loop's own scaffolding, written in the preceding commit
(`dc66c5b`, "job j-20260904-14: brief"), not the author's work.

Before → after, the only prose that moved:

> ...bartowski's GGUF of Thomson-1.0-Small, **latest update 26 August**, has
> out-downloaded the source repo ... (both repos are recent enough — **the
> conversion was created 26 August, the source repo 18 August** — that each
> window spans its whole life)

> ...bartowski's GGUF of Thomson-1.0-Small, **created and last updated 26
> August**, has out-downloaded the source repo ... (both repos are recent
> enough — **the source repo created 18 August** — that each window spans its
> whole life)

That is the fold the carried finding asked for, in the finding's own words, and
nothing else changed.

## The checks that would be red if the claims were false

I did not repeat `npm test` / `npm run build` — the loop ran both on this same
commit and both passed, and the diff touches one line of prose, no front
matter, no transclusion, no link. I re-fetched the two Hugging Face API records
the piece already cites in its Sources list, because the new clause makes a
claim about `lastModified` that the previous wording did not:

`https://huggingface.co/api/models/bartowski/thomsonreuters_Thomson-1.0-Small-GGUF`

```
createdAt:     "2026-08-26T04:45:39.000Z"
lastModified:  "2026-08-26T07:26:55.000Z"
downloads:     135520
```

`https://huggingface.co/api/models/thomsonreuters/Thomson-1.0-Small`

```
createdAt:     "2026-08-18T15:33:05.000Z"
lastModified:  "2026-08-28T12:27:31.000Z"
downloads:     1338
```

Fetched 2026-09-04. Claim by claim:

- **"created and last updated 26 August"** — both `createdAt` and
  `lastModified` on the GGUF fall on 2026-08-26, and `lastModified` is still
  that value today, eight days on, so the "last updated" half is not a stale
  reading of a repo that has since moved. This is the one claim the repair
  newly asserts, and it is the one I fetched for. The two timestamps match the
  carried finding's quoted values exactly, so the finding was not propagating
  its own error.
- **"the source repo created 18 August"** — `createdAt 2026-08-18T15:33:05Z`.
  Unchanged from the previous text and supported. Note the source repo's
  `lastModified` is 28 August, not 26; the sentence does not claim otherwise,
  which is why "created" had to survive in that clause and did.
- **"each window spans its whole life"** — the post is dated 2026-09-03, so a
  rolling 30-day window opens 2026-08-04. Both creation dates fall inside it
  (16 and 22 days before publication), so neither window truncates. The
  deduction still covers both repos even though only one date now appears
  inside the parenthesis, because the GGUF's date is stated eight words
  earlier in the same sentence.
- **"roughly two orders of magnitude"** (unchanged) — 135520 / 1338 = 101×
  today, against 77× at the previous review's fetch. Still what that phrase is
  doing, and moving in the direction that makes it more true, not less.

## Scope and retirement

`data/carried/j-20260903-23-carry-1.md` is deleted, which is the retirement
step the brief mandates. Grep for `j-20260903-23-carry-1` across the whole
worktree returns no files, so nothing dangles; the other twenty-odd files in
`data/carried/` are untouched. `26 August` now appears once in the piece, which
is the defect the job existed to remove.

Nothing in the diff exceeds the stated outcome.

## Noted, deliberately not carried

The sentence still nests an em-dash pair inside a parenthesis inside an
em-dash clause — the second half of what the carried finding complained about.
But the finding's own prescribed remedy was "let the parenthetical shrink to
the source repo's date", which is precisely what was done, and it did shrink
from two clauses to one. Carrying the residue forward would put a third repair
job on the same sentence for a purely typographic preference, which is worse
for the piece than the nesting is.
