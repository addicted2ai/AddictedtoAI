---
job: j-20260901-05
verdict: approve
reasons: []
would-cite: >-
  A reader tracking which OpenRouter batch endpoints Mistral actually still
  serves would link this entry to show that mistral-medium-3.1:batch stopped
  being offered on 2026-09-01 while the non-batch sibling kept running — it is
  the citable form of "the batch tier was pruned per-model, not withdrawn as a
  product", and it carries the last-listed date rather than silently vanishing.
proposal:
  slug: delta-review-brief-embeds-pre-revision-diff
  type: machinery
  date: 2026-09-01
  summary: >-
    The pass-2 delta-review brief for this job stated that the diff under review
    was "computed from the branch state" on commit ed7b7d76cacf, but the diff it
    embedded was in fact main..2cb0d029110f — the pre-revision commit the first
    review had already rejected. A machinery job should find where the loop
    assembles a delta-review brief and make the embedded diff be computed from
    the SHA the brief names, with a check that fails loudly if the two disagree.
  evidence: >-
    Measured in this review. `git diff --stat main 2cb0d02` prints exactly the
    four files the brief embedded (.job/brief.md 689, .job/source.json 8,
    content/wiki/model/mistralai-mistral-medium-3-1-batch.md 34,
    data/derived/search-index.json 2). `git diff --stat main ed7b7d7` prints
    seven files, adding data/derived/feed-rows.json, freshness.json and
    queue.json — the three files the revision commit was asked for and did
    commit. A reviewer working from the embedded diff alone would have seen the
    unrevised tree, re-issued the identical revision finding, and discarded the
    job on its last pass. The failure mode is silent and costs a whole job.
carry:
  - title: Restore the trailing newline on the retired entry file
    detail: >-
      content/wiki/model/mistralai-mistral-medium-3-1-batch.md ends `---` with
      no final newline — read directly, its last six bytes are `[]\n---`, and
      the diff prints "\ No newline at end of file". This is the same one-byte
      inconsistency already carried against
      content/wiki/model/mistralai-mistral-large-2512-batch.md
      (data/carried/j-20260901-04-carry-1.md), and the sibling entries
      mistralai-codestral-2508-batch.md and mistralai-ministral-8b-2512-batch.md
      both end with one. Nothing fails on it. Fix: append a newline after the
      closing `---`. Worth doing in the same diff as the mistral-large-2512
      carry rather than as its own job.
    subject: content/wiki/model/mistralai-mistral-medium-3-1-batch.md
subject: "content/wiki/model/mistralai-mistral-medium-3-1-batch.md"
reviewed:
  "content/wiki/model/mistralai-mistral-medium-3-1-batch.md": "c5470570e2535d7f80e502df228eb4f4e3efcf12590c0f4a068637439207060f"
---

# Delta review, pass 2 — j-20260901-05

The first pass asked for one thing: the commit message stated three
measurements about the derived tree that were false of the tree it described.
The author took resolution 1 — re-ran the recomputation for real, committed the
three derived files, and rewrote the paragraph. Every claim in the rewritten
message now measures true. Approving.

## What actually changed since the previous verdict

The previous review judged commit `2cb0d02`. The branch head is now `ed7b7d7`.
The delta between them is exactly the three files the revision was asked for:

```
$ git diff --stat 2cb0d02 ed7b7d7
 data/derived/feed-rows.json | 123 --------------------------------------------
 data/derived/freshness.json |   8 ---
 data/derived/queue.json     |  17 +++---
 3 files changed, 9 insertions(+), 139 deletions(-)
```

The content file's front matter is byte-identical across the revision — it does
not appear in that stat — so the previous reviewer's verification of the entry's
facts still stands unaltered, and I did not re-litigate it.

**A warning for whoever reads this record next: the diff embedded in my brief
was not the diff of the commit it named.** The brief said it was computed from
`ed7b7d76cacf`, but what it contained was `main..2cb0d02` — the pre-revision
tree, showing only the content file and `search-index.json`:

```
$ git diff --stat main 2cb0d02
 .job/brief.md                                      | 689 +++++++++++++++++++++
 .job/source.json                                   |   8 +
 .../model/mistralai-mistral-medium-3-1-batch.md    |  34 +-
 data/derived/search-index.json                     |   2 +-
 4 files changed, 705 insertions(+), 28 deletions(-)

$ git diff --stat main ed7b7d7
 .job/brief.md                                      | 689 +++++++++++++++++++++
 .job/source.json                                   |   8 +
 .../model/mistralai-mistral-medium-3-1-batch.md    |  34 +-
 data/derived/feed-rows.json                        | 123 ----
 data/derived/freshness.json                        |   8 -
 data/derived/queue.json                            |  17 +-
 data/derived/search-index.json                     |   2 +-
 7 files changed, 714 insertions(+), 167 deletions(-)
```

Read from the brief alone, the revision looked as though it had never happened,
and the correct-looking action would have been to re-issue the same finding and
discard the job on its final pass. I judged the branch instead of the brief.
That is what the proposal above is for.

## Verifying each claim in the rewritten commit message

The message's derived-state paragraph now reads:

> Derived files recomputed with the Pulse's own pipeline (node pulse/run.mjs
> --offline --no-mint --no-build --dry-run) and committed from that run's
> data-layer writes: feed-rows.json drops the vanished row, freshness.json's
> vanished_feed_rows loses the entry (2 -> 1, leaving only
> mistral-small-2603-batch), and the queue's vanished-feed-row item for this
> entry is gone. The recomputed queue holds 5 items: the small-2603 batch
> repair, the scout, the two nex-agi interpret items, and — picked up in this
> run — the carried trailing-newline finding for the mistral-large-2512 entry
> (data/carried/j-20260901-04-carry-1.md). search-index.json regenerated by the
> build flips this entry's status to retired.

**"feed-rows.json drops the vanished row."** True, and the change is a pure
deletion — the file diff is 123 removed lines and zero added ones:

```
$ git diff 2cb0d02 ed7b7d7 -- data/derived/feed-rows.json | grep -c '^-'
124          # 123 content lines + the `--- a/…` header
$ git diff 2cb0d02 ed7b7d7 -- data/derived/feed-rows.json | grep -c '^+'
1            # the `+++ b/…` header alone
```

Grepping the committed tree confirms nothing survives:

```
$ git grep -c "mistral-medium-3.1:batch" ed7b7d7 -- \
    data/derived/feed-rows.json data/derived/freshness.json \
    data/derived/queue.json data/derived/search-index.json
ed7b7d7:data/derived/queue.json:1
```

feed-rows.json and freshness.json return zero matches. The single queue.json
match is **not** a stale repair item — I read the file to check, and it is one
line of the scout item's assembled change-feed context
(`2026-09-01 retirement — Mistral: Mistral Medium 3.1 (batch) …`), which is
exactly where a recorded retirement is supposed to appear. That is the opposite
of the stale reference the previous review found.

**"freshness.json's vanished_feed_rows loses the entry (2 -> 1, leaving only
mistral-small-2603-batch)."** True, read from the committed file:

```
vanished_feed_rows: [{"entry_id":"model/mistralai-mistral-small-2603-batch",
"has_last_known":true,"last_seen_date":"2026-08-31",
"path":"content/wiki/model/mistralai-mistral-small-2603-batch.md",
"row_id":"mistralai/mistral-small-2603:batch","source":"openrouter-models"}]
```

One element, and it is the one named. The diff removed precisely the
`model/mistralai-mistral-medium-3-1-batch` object and nothing else.

**"the queue's vanished-feed-row item for this entry is gone… the recomputed
queue holds 5 items: the small-2603 batch repair, the scout, the two nex-agi
interpret items, and… the carried trailing-newline finding."** True, item for
item. `data/derived/queue.json` now carries `"count": 5`,
`"total_before_cap": 5`, and five objects in this order:

1. `vanished-feed-row` repair, rank 85, subject
   `openrouter-models:mistralai/mistral-small-2603:batch`
2. `scout-due`, rank 62, subject `2026-09-01`
3. `uninterpreted-status-change` interpret, rank 50, `nex-agi/nex-n2-mini`
4. `uninterpreted-status-change` interpret, rank 50, `nex-agi/nex-n2-pro`
5. `carried-finding` repair, rank 25, subject
   `content/wiki/model/mistralai-mistral-large-2512-batch.md`, titled
   "Restore the trailing newline on the retired entry file"

The item this job's own outcome names — rank 85, subject
`openrouter-models:mistralai/mistral-medium-3.1:batch` — is absent. That is the
check that would be red if the repair had not landed, and it is green.

I checked that the fifth item is derivable rather than invented, since the
message credits it to this run: `git ls-files data/carried/` prints
`data/carried/README.md` and `data/carried/j-20260901-04-carry-1.md`, so the
file whose presence produces that queue item is in the tree. The item appearing
is a mechanical consequence of recomputing over the merged state, not authored
work by this job, and it is not scope creep: `data/derived/` is a pure function
of state, and the loop drops derived state before merge and recomputes it anyway
(`78af8d4`, `8170be1`).

**"search-index.json… flips this entry's status to retired."** Unchanged from
the previous pass and already verified there — `"s": "active"` → `"s": "retired"`
— and the entry legitimately keeps its two `mistral-medium-3-1-batch` references
in that file because the entry still exists, retired rather than deleted.

## The precedent claim now holds

The previous review's sharpest point was that "Same repair shape as…" was false
of the derived-state handling, because the four sibling jobs committed the
recomputation and this one had not. That is now resolved by the tree rather than
by softening the sentence. Compare:

```
d052eb6 content: retire model/mistralai-mistral-large-2512-batch
 entry.md | 34 ++--   feed-rows.json | 125 --   freshness.json | 8 --
 queue.json | 12 +-   search-index.json | 2 +-

ed7b7d7 content: retire model/mistralai-mistral-medium-3-1-batch
 entry.md | 34 ++--   feed-rows.json | 123 --   freshness.json | 8 --
 queue.json | 17 +-   search-index.json | 2 +-
```

Same five files, same shape. The queue delta is 17 lines rather than 12 because
this run also picked up the carried finding, which is explained in the message.

## Source facts

Not re-fetched. The previous pass fetched
`https://openrouter.ai/api/v1/models` live during its review and recorded the
result in its verdict (420 rows, target absent, non-batch sibling present, 65
`:batch` rows, `mistralai/mistral-medium-3-5:batch` the only surviving Mistral
batch row), and the content file's front matter has not changed by a byte since
that verification. Re-fetching would test today's catalog against a verdict
already recorded against it, which is not the check this pass needs.

## Gates

Not re-run. The loop recorded `npm run test` PASS and `npm run build` PASS on
`ed7b7d76cacf`, which is the branch head I reviewed — the same commit, not the
pre-revision one the embedded diff came from. Nothing I checked touches code or
content that would move either result, and the one thing I am carrying is a
trailing newline that fails nothing.

## Not blocking

The content file still ends `---` with no final newline; its last six bytes read
`[]\n---`. This is the identical defect already carried against the
mistral-large-2512 entry and it is in the `carry:` block above, to be fixed
alongside it rather than as its own job.
