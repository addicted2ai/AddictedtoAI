---
job: j-20260908-13
verdict: revise
reasons:
  - false-or-unsupported-claim
  - intent-not-measurement
would-cite: >-
  A maintainer comparing current OpenRouter catalog prices would link the
  completed derived view to show every synchronous/`:batch` pair and the
  providers whose batch lanes diverge from the usual discount.
---

Required changes: derive the view from the current OpenRouter catalog snapshot
or another complete current-row data source, not only the append-only change
history. The committed latest snapshot has 69 synchronous/`:batch` pairs across
12 providers, while this implementation produces only 6 pairs across 5
providers, so it does not implement “each model ... across every provider.”
Regenerate `data/derived/batch-pricing.json` from that complete input and add a
regression fixture containing a current row that has no change-history line.

Also fix the history tie-break if `data/changes.jsonl` remains an input: the
code compares `previous.key` but never stores `key` in the map. A direct
out-of-order same-date fixture produced `{"sync_prompt":"20","expected_if_key_max":"10"}`;
the test suite only exercises newest-by-date, not this tie case.

Checks performed:

- `node --test pulse/tests/batch-pricing.test.mjs` passed:
  `2 tests`, `pass 2`, `fail 0`.
- Running the changed `deriveBatchPricing` module against a temporary copy of
  the committed `data/changes.jsonl` printed:
  `{"row_count":6,"provider_count":5,"providers":["anthropic","deepseek","google","meta","openai"]}`.
- A read-only check of
  `data/sources/openrouter-models/latest.json` printed:
  `{"snapshot_date":"2026-09-08","source_rows":428,"batch_count":69,"paired_count":69,"providers":["anthropic","deepseek","google","meta","minimax","mistralai","moonshotai","openai","qwen","thinkingmachines","x-ai","z-ai"]}`.
- The supplied branch gate results were not rerun, as instructed. The diff
  contains no `content/blog/*.md` file, so the blog voice requirement does not
  apply. No proposal was surfaced.
