---
job: j-20260902-19
verdict: approve
reasons: []
would-cite: >-
  Nobody links a proposal's evidence block — the reader is the author job that
  will write the GLM-5.3 licence post, and the argument this clause serves is
  that one: it stops that writer glossing Z.ai's "(w/ fallback)" column label
  into "with fallback routing" and attributing the gloss to Z.ai. Someone
  arguing that vendor benchmark tables are not leaderboards would cite the
  underlying observation — the one column the card qualifies is the one column
  whose method footnote does not cover it, and the qualifier itself is never
  defined.
carry:
  - title: Note that the card's only "fall back" is PostTrainBench's, unrelated to the column label
    detail: >-
      The new clause says "no footnote, caption or prose says what fallback was
      enabled". That is true for the one-word string — `grep -n -i "fallback"`
      over the card fetched 2026-09-02 returns exactly line 22, the table header
      — but the card does contain the TWO-word form: the PostTrainBench footnote
      reads "Runs that fail to produce a score fall back to the official
      zero-shot base-model baseline score", scoped to Z.ai's own GLM-5.3
      evaluation and never mentioning Fable 5. A writer who greps the card for
      "fall back" will hit it, and it is exactly the plausible gloss the clause
      exists to forbid. Half a clause closes it: the card's only "fall back" is
      PostTrainBench's, about GLM-5.3's own scoring, and says nothing about the
      Fable 5 column label.
    subject: data/proposals/glm-5-3-license-revenue-gate.md
---

## What the diff is

Two author-made changes (commit `59f9330`): three folded lines added to the
`evidence:` block of `data/proposals/glm-5-3-license-revenue-gate.md`, and the
deletion of `data/carried/j-20260902-17-carry-1.md`, which is the retirement
mechanism the brief names. That is exactly the stated outcome and nothing else.

`.job/brief.md` and `.job/source.json` also appear in the diff, but they are the
loop's own scaffolding commit (`5890091 job j-20260902-19: brief`), not the
author's work, and the previous job's history shows the loop stripping them
itself before merge (`cf92c7b job j-20260902-18: remove job scaffolding before
merge`). Not counted against scope.

## What I checked

**The source, refetched today.** I did not rely on the brief's account of the
card:

```
$ curl -sS -o /tmp/glm53card.md -w "%{http_code} %{size_download}\n" \
    https://huggingface.co/zai-org/GLM-5.3/raw/main/README.md
200 14209

$ grep -n -i "fallback" /tmp/glm53card.md
22:| Benchmark                    | GLM-5.3   | GLM-5.2 | Kimi K3  | DeepSeek-V4 Pro-0813 | Qwen3.8-Max | Opus 4.8 | Fable 5 (w/ fallback) | GPT-5.6 Sol   |
$ grep -c -i "fallback" /tmp/glm53card.md
1
```

One occurrence, in the benchmark table's column header, on the raw Markdown of
the card itself (`/raw/main/README.md` — not WebFetch's extraction of the
rendered page, whose prose is evidence in neither direction). So "appears only
in that header" and "the card states no meaning for it" both hold. The card has
a `## Footnotes` section at line 58 with fifteen per-benchmark entries; I read
them (lines 58–74) and none defines the qualifier — consistent with the count
above, and checked by reading rather than inferred from the grep alone.

**The premise the new clause leans on**, which is pre-existing text but is what
makes the clause's point ("the one column the card qualifies is the one column
it declines to explain"). The ExploitGym footnote reads:

> "We evaluate GLM-5.3, Kimi-K3 and Qwen3.8 Max in Claude Code 2.1.207 … The
> reported results are single-run Pass@1 on 869 tasks under two timeout budgets:
> 2 hours and 6 hours, which are calculated as the API inference time rescaled
> by per-model tokens per second rate (per-model TPS sourced from Artificial
> Analysis; that is, we rescale GLM-5.3's results by 115 TPS, Kimi K3's results
> by 40 TPS and Qwen3.8 Max's results by 47 TPS), plus the non-API overhead."

Three models named, and Fable 5 is not one of them. The evidence block's
"states its method for GLM-5.3, Kimi K3 and Qwen3.8 Max only" is accurate to the
document, and the ExploitGym row on the card confirms the column set the clause
lists (`105 / 130 | 29 / 39 | 36 / 70 | – | 14 / 26 | 80 / 120 | 181 / 247 |
216 / 293`).

**That the edit parses.** The addition sits inside a `>` folded scalar, so a
stray indent or colon would silently reshape the field. Parsed the file with the
same `gray-matter` the loop's `readMarkdownDir` uses:

```
slug: "glm-5-3-license-revenue-gate"
type: "post"
keys: date,slug,type,summary,evidence,expires,proposed_by_job,proposed_by_type
```

and the folded `evidence` string reads as one continuous sentence: `… so the row
is not a like-for-like leaderboard. The "(w/ fallback)" qualifier on Fable 5
appears only in that header, and the card states no meaning for it — no
footnote, caption or prose says what fallback was enabled, to what, or for which
runs. The licence text itself, fetched 2026-09-01 — …`. No line-fold artefact,
no broken key.

I did not re-run `npm test` or `npm run build`; the loop ran both on
`59f933022b8e` and recorded PASS.

## What I did not block on

The clause says "appears only in that header" and the preceding sentence never
mentions a header — the referent is the benchmark table's column header, which
is recoverable from "the qualifier on Fable 5" and from the quoted label itself,
so it is loose rather than wrong. Not worth a job.

The one substantive near-miss is carried above: the card's PostTrainBench
footnote contains the two-word "fall back", which a future writer grepping for
the concept will find, and which is about GLM-5.3's own scoring rather than the
Fable 5 label. The clause as written is true; it is one string-match away from
being read as false.
