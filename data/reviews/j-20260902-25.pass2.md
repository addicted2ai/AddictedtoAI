---
job: j-20260902-25
verdict: approve
reasons: []
would-cite: >-
  Someone arguing that "open weights" is not one thing would link this entry:
  it puts an Apache-2.0 770B MoE and a revenue-gated bespoke licence side by
  side on the same page, with the licence taken from the LICENSE file rather
  than the model card banner, so the comparison survives a hostile reader. The
  second audience is anyone quoting Hy4 preview's 2.99/4.00 lead — the entry is
  the page that says, in one place, that the raters were 163 of the vendor's
  own staff, that the margin is 0.05 and 0.07 on a four-point scale, and that
  the throughput and self-improvement numbers are Tencent's word with no method
  attached.
carry:
  - title: Date the two new timeline rows to a document that states the date
    detail: >-
      The revision correctly moved the Apache-2.0 and four-channel claims onto
      rows sourced to the documents that state them, but each new row carries
      `date: "2026-08-28"` and cites a document that does not state that date.
      The model card's News item ("We open-source **Hy4 preview** and **Hy4
      preview-FP8** model weights on [Hugging Face], [ModelScope], [GitCode],
      and [CNB]", line 127 of the raw README fetched 2026-09-03) is undated, and
      the LICENSE file carries only "Copyright (C) 2026 Tencent." The date is
      true and corroborated — the press release is dated 2026-08-28 and the HF
      repo's `lastModified` is 2026-08-28T14:58:32Z — but each row's own
      citation supports the event, not the date. The same applies to the
      `open_weights` fact, whose value ends "on the release date" while citing
      the undated model card. Adding the press release alongside, or dropping
      the date qualifier from `open_weights`, closes it. Note the HF API also
      reports `createdAt` 2026-08-27T08:52:40Z, so the repo predates the
      announcement — another reason not to hang the publication date on the
      repo's own documents.
    subject: content/wiki/model/tencent-hy4-preview.md
subject: "content/wiki/model/tencent-hy4-preview.md"
reviewed:
  "content/wiki/model/tencent-hy4-preview.md": "ea9da4be97c25c3287782454fef8edbd02395d09fd296b60ae24ac9f813696b7"
---

Delta review. Pass 1 required one change and named three more to fix in the
same pass. All four are in `9d1e2be`, and the delta is 19 lines of the entry
plus regenerated derived data — nothing else.

## The required change

`git diff 7a508bf 9d1e2be -- content/wiki/model/tencent-hy4-preview.md`:

```
 timeline:
   - date: "2026-08-28"
-    event: "released and open-sourced under the Apache License 2.0, with instruct weights and an FP8 variant published on Hugging Face, ModelScope, GitCode and CNB"
+    event: "released and open-sourced"
     source_url: "https://www.tencent.com/tencent-releases-and-open-sources-tencent-hy4-preview/"
+  - date: "2026-08-28"
+    event: "instruct weights and an FP8 variant published on Hugging Face, ModelScope, GitCode and CNB"
+    source_url: "https://huggingface.co/tencent/Hy4-preview"
+  - date: "2026-08-28"
+    event: "licensed under the Apache License 2.0"
+    source_url: "https://huggingface.co/tencent/Hy4-preview/blob/main/LICENSE"
```

That is the prescribed fix exactly: the press release now carries only
"released and open-sourced", which is what it says, and the licence and the
four-channel publication hang on the documents that state them. I refetched
both rather than taking pass 1's word.

**LICENSE** — `https://huggingface.co/tencent/Hy4-preview/raw/main/LICENSE`,
`200`, 11,625 bytes. First lines, verbatim:

```
Tencent is pleased to support the open source community by making Tencent Hy4 preview available.

Copyright (C) 2026 Tencent.  All rights reserved.

Tencent Hy4 preview is licensed under the Apache-2.0.

Terms of the Apache-2.0:
```

The row says "licensed under the Apache License 2.0" and the cited file says
so in its own words.

**Model card** — `https://huggingface.co/tencent/Hy4-preview/raw/main/README.md`,
`200`, 10,744 bytes. Line 127, under `## News`:

> `* 🔥 We open-source **Hy4 preview** and **Hy4 preview-FP8** model weights on [Hugging Face](...), [ModelScope](...), [GitCode](...), and [CNB](...).`

and the Model Links table at lines 131–134 lists both variants across all four
hosts, "Hy4 preview | Instruct model" and "Hy4 preview-FP8 | FP8 quantized
instruct model". The row's four hosts and the FP8 variant are both there.

**Repo metadata**, as a cross-check on the licence and the date
(`https://huggingface.co/api/models/tencent/Hy4-preview`, `200`, 24,731 bytes):

```
createdAt    2026-08-27T08:52:40.000Z
lastModified 2026-08-28T14:58:32.000Z
cardData.license "apache-2.0"
tags(license) [ 'license:apache-2.0' ]
LICENSE sibling true
```

The licence tag agrees with the LICENSE file, so the entry's stated discipline
— taking it from the file, not the banner — costs it nothing here, and the
`lastModified` corroborates 2026-08-28. `createdAt` being 2026-08-27 is why I
carried the date-sourcing note above rather than treating the rows as fully
self-supporting.

## The three non-blocking items

All three landed.

1. **The "no measurement" characterisation.** Was "No measurement accompanies
   either claim on the announcement"; is now "What is absent for both is the
   method, the identity of the baseline, and any independent check, so both are
   recorded here as claims with dates, not as facts." That is the true and
   stronger sentence, and it no longer contradicts the 31.8% figure quoted two
   lines above it.
2. **The `parameters` stutter.** The fact value dropped its "— mixture-of-experts"
   tail (`"770B total, 49B activated per token"`), so the sentence "It is a
   mixture-of-experts model of `{{fact:…#parameters}}` with a context window of
   …" now renders once, not twice.
3. **"fourth-generation".** Now "next-generation", which is the press release's
   own word and consistent with the model card's "new-generation". No
   generation is counted that a source does not count.
4. The file ends with a newline — `od -c` on the last bytes prints
   `...c a c h e   r e a d s . \n`, and the diff's `\ No newline at end of
   file` marker is gone.

## The derived data in the delta

`data/derived/aliases.json`, `backlinks.json` and `search-index.json` changed
too. This is regenerated build output, not authored state, and it is exactly
what the entry implies: the three new aliases registered ("Hy4 preview"
exclusive/linkable, "Hy4" shared/not-linkable, "tencent/hy4-preview"
exclusive/linkable), backlinks for the four mention targets
(`org/tencent`, `model/tencent-hy3`, `model/z-ai-glm-5-3`,
`model/moonshotai-kimi-k3`), and the search-index row flipping `"b": true` →
`"b": false` because the entry now has a prose body. `lib/aliases.mjs` and
`lib/mentions.mjs` own these files, the loop ran `npm run build` on this exact
commit in this worktree, and `git status --short` here shows nothing tracked as
modified — so the committed derived tree is byte-identical to what the build
regenerates from the committed content. Not authored, not stale, and `data/` is
not a reserved path. No scope violation: the branch touches `.job/`
(loop-written), the one entry named in the outcome, one proposal of a different
type, and regenerated derived output.

## What I did not re-run

`npm test` and `npm run build` were run by the loop on `9d1e2be` and both
passed. I did not repeat them. The checks above are the ones that would have
been red if the revision's specific claims were false.

## What still stands from pass 1

I did not re-verify what pass 1 already fetched and quoted and the revision did
not touch — the feed bindings, the OpenRouter sole-provider enumeration, the
blind-evaluation numbers, the free-access window, the llm-releases quote, the
Kimi K3 licence transclusion, the mention targets. The one thing I did
re-confirm from that set is the licence, because it is the entry's headline
claim and the row asserting it was rewritten in this delta.

The defect pass 1 blocked on is gone, and it was the only one: the entry's most
checkable line now cites a document that contains the word "Apache-2.0", which
is what it was always claiming.

## Housekeeping

I wrote `.rev2-fetch.mjs`, `.rev2-api.mjs` and `.rev2/` into the review
worktree to fetch and read the sources, and deleted them before ending. Nothing
tracked was modified.
