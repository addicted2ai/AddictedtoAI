---
job: j-20260914-32
verdict: approve
reasons: []
would-cite: >-
  Someone writing an explainer on how RLHF turned GPT-3.5 into a chatbot would
  link this entry for the pipeline paragraph, which now names Proximal Policy
  Optimization as the optimiser step instead of a generic "reinforcement-learning
  fine-tuning", citing the launch post itself.
subject: "content/wiki/event/chatgpt-launch.md"
reviewed:
  "content/wiki/event/chatgpt-launch.md": "fc1e7b5495b6894bbe6c8a19271c94a170d0631915050641630a7262d5ce4119"
---

## Scope

`git log --stat` on the branch: the job's commits are `a710fef` (the loop's
`.job/brief.md`, `.job/source.json`) and `c6d35e6`, which touches exactly:

```
 content/wiki/event/chatgpt-launch.md  |  4 ++--
 data/carried/j-20260908-15-carry-1.md | 16 ----------------
```

The loop's diff shows the same. The first file is the target. Deleting the
second retires the item, as the work source asks. No reserved path is touched,
and no blog post is in the diff, so the voice question does not apply.

## Source check

I fetched the cited snapshot
`https://web.archive.org/web/20221130211011/https://openai.com/blog/chatgpt/`
with curl and saved it to a temp file. `grep -c "Proximal Policy Optimization"`
returned `1`. The text around that match:

> We randomly selected a model-written message, sampled several alternative
> completions, and had AI trainers rank them.  Using these reward models, we can
> fine-tune the model using <a ...openai-baselines-ppo/>Proximal Policy
> Optimization</a>. We performed several iterations of this process.

The new text says OpenAI "used that reward model to fine-tune the chatbot with
Proximal Policy Optimization". The source supports it: reward models are used
to fine-tune the model with PPO. "The chatbot" stands in for the source's "the
model". That is a fair reading, since the model being fine-tuned is the one that
became ChatGPT, and it adds no claim. The source says "reward models" (plural)
and "several iterations". The entry's singular "a reward model" was in the
existing sentence and is not part of this diff.

## Polarity

The work source asked for the generic RL phrase to become the named algorithm,
PPO. The diff does exactly that and deletes the carry file as instructed. Nothing
is inverted.

## Gates

The loop recorded `npm run build` PASS and `verify-surfaces` PASS on
`c6d35e66033a`. I did not re-run them. The change adds no link, transclusion or
mention.

## Not blocking

Line 54 now runs past the file's usual line wrap. That is cosmetic only: it does
not change the rendered text, and it is not worth a carry.
