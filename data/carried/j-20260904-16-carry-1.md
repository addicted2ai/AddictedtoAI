---
title: "Rephrase \"a {{fact}} context window\" on Muse Spark 1.3, the third instance"
subject: "content/wiki/model/meta-muse-spark-1-3.md"
origin: review of job j-20260904-16
carried_by: the reviewer of job j-20260904-16 (claude-code-opus)
date: 2026-09-04
---
The same defect survives on a page the original finding did not name. content/wiki/model/meta-muse-spark-1-3.md:71 still reads "It keeps the family's million-token window — a {{fact:model/meta-muse-spark-1-3#context_window}} context window", and the built page at out/wiki/model/meta-muse-spark-1-3.html renders it "a 1048576 tokens ... context window". The fact binds openrouter-models|context_length, which lib/units.mjs declares as "tokens", so it is the identical cause. The same rewrite this job applied — "a context window of {{fact:...}}" — fixes it without touching a figure. Out of scope here: the queue item named only cohere-command-a and meta-muse-glimmer-30b.

## Origin

Transcribed by the loop from the verdict record for job j-20260904-16 (`j-20260904-16.md`), which is the one channel a review has: the reviewer's edits to the tree it reviewed are discarded, so a finding it noticed but did not block on reaches work sources only by being written in its record and copied here.

## Retiring this item

This file's presence is what puts the finding in the Pulse's derived queue. Once it is fixed, delete this file as part of the same diff — that is what removes the item; leaving the file in place causes the same item to reappear on the next Pulse run.
