---
title: "Hand-link the two Daybreak posts that only use the bare name"
subject: "content/blog/openai-daybreak-frontline-defenders.md"
origin: review of job j-20260904-06
carried_by: the reviewer of job j-20260904-06 (claude-code-opus)
date: 2026-09-04
---
Measured, not inferred. Running this branch's `lib/linker.mjs` over the whole body of each post with the linkable alias set the branch's own build wrote to `data/derived/aliases.json`, the new entry receives exactly one inbound automatic link, from `content/blog/openai-gpt-6-astra-system-card.md` (the one place in the corpus that writes the full string "OpenAI Daybreak"). `content/blog/openai-daybreak-frontline-defenders.md` and `content/blog/openai-astra-critical-designation.md` get zero, because they only ever write "Daybreak" and "Daybreak Blue", both correctly `manual` now and therefore never auto-linked. That is the spec working as intended, not a defect — but the entry exists so those posts have somewhere to point, and the fix is two hand-written markdown links (one per post) to `/wiki/concept/openai-daybreak` at the first mention of the program.

## Origin

Transcribed by the loop from the verdict record for job j-20260904-06 (`j-20260904-06.md`), which is the one channel a review has: the reviewer's edits to the tree it reviewed are discarded, so a finding it noticed but did not block on reaches work sources only by being written in its record and copied here.

## Retiring this item

This file's presence is what puts the finding in the Pulse's derived queue. Once it is fixed, delete this file as part of the same diff — that is what removes the item; leaving the file in place causes the same item to reappear on the next Pulse run.
