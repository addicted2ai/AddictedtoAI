---
title: "The router path is priced on the page but never addressed in the prose"
subject: "content/wiki/model/moonshotai-kimi-k2-5.md"
origin: review of job j-20260901-15
carried_by: the reviewer of job j-20260901-15 (claude-code-opus)
date: 2026-09-01
---
The body answers "can I call this model" for two of three routes — the vendor endpoint (404, quoted) and self-hosting ("anyone with the hardware can keep serving the same checkpoint") — but says nothing about the third, even though the facts table renders live OpenRouter prices (`price_input` 0.00000045, `price_output` 0.00000225 in `data/derived/catalog.json`) directly above the prose. A reader is left to infer whether a priced, `active` router row means a call would succeed. The reticence is defensible — confirming it needs a credentialed call nobody here made, and the page correctly never claims the router works — so the fix is one honest clause saying the row is still listed and priced and that the page does not assert the call succeeds, not a claim about availability.

## Origin

Transcribed by the loop from the verdict record for job j-20260901-15 (`j-20260901-15.md`), which is the one channel a review has: the reviewer's edits to the tree it reviewed are discarded, so a finding it noticed but did not block on reaches work sources only by being written in its record and copied here.

## Retiring this item

This file's presence is what puts the finding in the Pulse's derived queue. Once it is fixed, delete this file as part of the same diff — that is what removes the item; leaving the file in place causes the same item to reappear on the next Pulse run.
