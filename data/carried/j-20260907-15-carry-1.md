---
title: "Re-cite lib/changes.mjs:60-67 against feedRowIndex's current location"
subject: "content/wiki/README.md"
origin: review of job j-20260907-15
carried_by: the reviewer of job j-20260907-15 (claude-code-opus)
date: 2026-09-07
---
README line 176 reads "`lib/changes.mjs:60-67` — `feedRowIndex` keys one Map on `${source}|${rowId}`". Measured in this worktree at d4f14730083b: `feedRowIndex` is declared at `lib/changes.mjs:112` and its body runs to 119; lines 60-67 are the tail of an unrelated function plus a comment about malformed change lines ("A malformed line is the Pulse's problem to report, not a reason for the site to stop rendering the other 59"). The claim the sentence makes is correct — line 116 is literally `index.set(\`${source}|${rowId}\`, doc);` — only the citation has drifted. `j-20260906-11-carry-1.md` explicitly cleared this citation as resolving correctly when it was measured on 2026-09-06, so the drift is newer than that finding and was outside this job's brief. Fix: cite `:111-119`. The same stale range is repeated in the block comment at `lib/render/frontier.mjs:85-86`, which is code rather than content but would be worth correcting in the same pass.

## Origin

Transcribed by the loop from the verdict record for job j-20260907-15 (`j-20260907-15.md`), which is the one channel a review has: the reviewer's edits to the tree it reviewed are discarded, so a finding it noticed but did not block on reaches work sources only by being written in its record and copied here.

## Retiring this item

This file's presence is what puts the finding in the Pulse's derived queue. Once it is fixed, delete this file as part of the same diff — that is what removes the item; leaving the file in place causes the same item to reappear on the next Pulse run.
