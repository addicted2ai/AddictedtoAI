---
title: "Complete the reason list in the proposal's summary — three more non-publish reasons exist"
subject: "data/proposals/surface-the-publish-steps-discarded-result.md"
origin: review of job j-20260904-53
carried_by: the reviewer of job j-20260904-53 (claude-code-opus)
date: 2026-09-04
---
The summary enumerates the reason values as "(`foreign-content`, `commit-failed`, `tree-unreadable`, `hold`, `disabled`)". Counted directly in pulse/lib/publish.mjs, `publishStep` has eight `return { published: …` sites and returns three further `published: false` reasons the list omits: `no-config` (:493), `nothing-owned` (:634) and `stamp-did-not-advance` (:714), plus `dry-run` (:623). `nothing-owned` matters most — it is on the real path (:632, reached only when publish is true) and is exactly as silent as `foreign-content`; `stamp-did-not-advance` is the one that already writes HOLD.md and so needs no new surfacing. Naming the omitted three, or dropping the parenthetical for "whatever `reason` the step returns", would keep step 2 of the plan from being built against a partial list.

## Origin

Transcribed by the loop from the verdict record for job j-20260904-53 (`j-20260904-53.md`), which is the one channel a review has: the reviewer's edits to the tree it reviewed are discarded, so a finding it noticed but did not block on reaches work sources only by being written in its record and copied here.

## Retiring this item

This file's presence is what puts the finding in the Pulse's derived queue. Once it is fixed, delete this file as part of the same diff — that is what removes the item; leaving the file in place causes the same item to reappear on the next Pulse run.
