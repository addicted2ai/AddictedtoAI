---
title: "Add concept/openai-daybreak to the mentions list of the two posts that now link it"
subject: "content/blog/openai-daybreak-frontline-defenders.md"
origin: review of job j-20260904-10
carried_by: the reviewer of job j-20260904-10 (claude-code-opus)
date: 2026-09-04
---
This diff hand-links both posts to /wiki/concept/openai-daybreak, but neither adds `concept/openai-daybreak` to its `mentions:` front matter, and `lib/mentions.mjs` builds backlinks ONLY from `mentions:` — never from body links (`buildBacklinks` iterates `doc.data.mentions` and nothing else). Measured: `data/derived/backlinks.json` on this branch holds 185 keys and none of them contains "daybreak", and the entry was added in 014120e, which `git merge-base --is-ancestor 014120e 01be0c7` confirms predates the pulse commit that recomputed that file — so the zero is real, not stale. The result is a one-directional relation: the posts point at the entry and the entry's "Appears in" rail renders empty (`renderReferencedHere`/the backlinks rail return '' when the list is empty). Adding `- concept/openai-daybreak` to `mentions:` in content/blog/openai-daybreak-frontline-defenders.md and content/blog/openai-astra-critical-designation.md closes it. Not blocking: the carried finding asked for two markdown links and got exactly two markdown links.

## Origin

Transcribed by the loop from the verdict record for job j-20260904-10 (`j-20260904-10.md`), which is the one channel a review has: the reviewer's edits to the tree it reviewed are discarded, so a finding it noticed but did not block on reaches work sources only by being written in its record and copied here.

## Retiring this item

This file's presence is what puts the finding in the Pulse's derived queue. Once it is fixed, delete this file as part of the same diff — that is what removes the item; leaving the file in place causes the same item to reappear on the next Pulse run.
