# AR-001 — arbitration, round-2 disagreements (both finalists)

```yaml
id: AR-001
version: 1
schema: loops/ui-loop/graph/schemas.md#arbitration
depends_on: [DR-CP-UI-001-1-2, DR-CP-UI-001-2-2, JURY-BRIEF-UI-001-AB, JURY-BRIEF-UI-001-BA, RT-CP-UI-001-1-2, RT-CP-UI-001-2-2, JV-struct-CP-UI-001-1-2, JV-sys-CP-UI-001-2-2]
arbitrator: Fable (K13); every decision cites a clause; the unanchored is routed, not decided
```

## Decisions

**D1 — DL S18 scope (DR-1 C1).** In scope; critical stands. CP-UI-001-1's own `reuses` line for
`/wiki/<entry>` promises "filling S18's dead column"; BRIEF R-C requires the dead column resolved
(60% floor, or keeper retires with cause); RULES R13 is the rule, S18 only its assertion. A packet
binds its builder to what it promised — "pre-existing debt" describes the port, not the promise. RT's
anchor check concurs; JV-hier's "declared debt" reading is overruled on this point. → RD-001 fix 4.

**D2 — DL board: presentation fix or architecture defect (DR-1 C2).** Architecture defect, by
construction. RT FM11: `renderIndexBoard` maps `sources` to one constant string and reads neither
org nor source; the element declares a `data_source` it never queries, so BRIEF R-B ("every element
names a data source present today") is met on paper and broken in code. A CSS column collapse
satisfies F-hier-2-3/F-sys-2-1's text but is RT's own "relocation-not-resolution" and cannot meet
K19 ("leads with a players board"). Ruling: no revision of this element is complete without a
per-(org, column) lookup; the packet's collapse is accepted only as that lookup's output when nothing
is published. → RD-001 fix 2. WHICH columns the board carries (borrow PB's catalog columns, or wait
for the Desk order) is unanchored and a frontier-collapse risk (contract rule 2) → JURY for_keeper #2.

**D3 — PB home door (DR-2 C1).** Two clauses, two axes, both hold. Position: RULES R6 governs the
home first viewport; JV-hier q2 PASS is correct and F-hier-2's decline stands. Character: K20
requires a treatment per template and the door is `/frontier`'s only presence on the landing surface,
so F-sys-2-4 is admissible — typographic half only. Three uniform single-line link rows are R8's
rules-FORBIDDEN shape; per-row rules there would break R8 to buy coherence. Ruling: lead-pair weight
yes, row rules no. Reachability at 390: F-hier-10's own invariant says "the nav counts"; under R6
(nothing displaces the feed) the nav item is the anchored minimum. Whether the DOOR itself must sit
in the 390 first viewport is unanchored → for_keeper #3. → RD-002 fix 3.

**D4 — PB's unilateral S13/S14/S18 retirement (DR-2 keeper item).** Ratification is K14's ("keeper
rules") → for_keeper #5. Decided here: retiring an assertion does not retire its rule. RULES.md
preamble — a rule with no executable form is keeper-verified, not void; CHARTER slot 3 — rule numbers
are the API. R13's 60% floor still binds `.entry-rails` (44.9%); F-struct-3 stands. → RD-002 fix 4.

**D5 — `/tutorials/<entry>` overflow at 390 (PB F-struct-1; DL JV-hier q8, unfiled).** R2 is law
(K14) and speaks of routes, not regressions; "inherited" is no exemption. Both builds fail it. →
RD-002 fix 5; DL's widened reflow sample (RD-001 evidence-fix) surfaces it behind the larger
flagship break.

**D6 — K22 jury row.** No capture or gate renders a domain row in either build; a one-order ranking
from code reading is not shared evidence (contract rule 5). No preference; a gate rendering a fixture
domain row is requested (evidence-fix) before the next jury.

## Routed, not decided

JURY-BRIEF-UI-001 for_keeper 1–8 (identity; index-data columns; door at 390; door ordering,
CHARTER slot 1; RULES rewrite; catalog@390 vs I14; MR-UI-001 to include the blank; K7 check-in).
F-sys-2-8 stays `measure` (K9).
