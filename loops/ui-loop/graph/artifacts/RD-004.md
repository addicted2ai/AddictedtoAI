# RD-004 — revision directive, Players Board, iteration 5 (delegated, K40/K41)

```yaml
id: RD-004
version: 1
schema: loops/ui-loop/graph/schemas.md#revision-directive
depends_on: [RD-003, RT-CP-UI-001-2-4, JV-sys-CP-UI-001-2-4, SCORE-CP-UI-001-2-4]
target: CP-UI-001-2 Players Board, branch ui/concept-2 (D:\AddictedtoAI-c2)
budget: one iteration, one fix, nothing else
fix:
  - {rank: 1, finding_id: "RT-CP-UI-001-2-4 FM-N3 (+ JV-sys F-sys-4-1)", clause: "BRIEF R-B and anti-requirements: vendor language only verbatim, ATTRIBUTED, labelled; K10 fence (sourced, honest)",
     invariant: "A fact renders in the VENDOR CLAIM cell only if (a) its field is in the Tier-1 benchmark allow-list AND (b) its cited source is the vendor itself (source URL host is the organisation's own domain as recorded in the org entry, or the fact is explicitly attributed to the vendor in its source record); third-party measurements (OpenRouter traffic-derived observed_*, llm-releases.com analyses, cost_per_task, output_tokens_per_task) never render there; the cell shows the claim fragment followed by the vendor name as source, so the READ column cannot be mistaken for the claim's source; a gate check falsified both ways asserts no non-vendor-sourced fact renders in the cell."}
do_not_touch: [everything else from RD-002/RD-003 do_not_touch; the lede sentence's wording except its data-derived counts; content and data]
re_evaluate:
  judge-system: [q8 ALLR]
  red-team: [anchor FM-N3]
  stand: [everything else in JV-*-CP-UI-001-2-4]
```

Authorised under the keeper's blanket delegation (K40) because the defect is a mislabelled claim on
the flagship, the brief's central honesty rule, and the merge (K39) presupposes a clean flagship.
Recorded as K41. Not a scope change: one cell's admission rule.
