# THE RECORD WALL, ROUND 2 — REVIEW2

## VERDICT
revise

## 1. The property, in one sentence, written first and never edited
A brief that reaches a worker repeats its frozen authority verbatim, directs edits only inside its listed scope, and names an existing check for every enforcement claim it makes.
Written after reading only: repository root listing, `scripts/` listing, and `scripts/brief-lint.mjs` in full (including its header comment and every check comment). Had not read at that moment: the diff from the base, `RESULT1.md`, `RESULT2.md`, or `scripts/brief-lint-records.test.mjs` in any part.

## 1b. Where section 1 turned out to be wrong, and what changed my mind
Section 1 still states the underlying property the linter exists to enforce, and nothing in the diff, either report, or the suite showed that property to be mis-stated. What changed is what the wall holds. The wall does not hold section 1 directly. It holds anchor presence at expected counts, well-formed records with future expiries, and one behaviour twin for E7sfx, with twenty-three twins outstanding as dated debt to 2026-10-10 (outlier B3 to 2026-12-01). An added conjunct preserving the anchor substring leaves Check A green while the vehicle flips to refused, which this review re-ran for E7sfx and which the packet adopts as ruling rather than repair. That proxy-versus-property divergence is therefore documented debt with a deadline, not a hidden mis-statement in section 1. Per the brief it is handed to a third party rather than resolved here, and it outranks everything else in this report. Local date of this note: 2026-09-10.

## 2. THE SWEEP — all six fields, and what each is tied to
Each record carries `id`, `anchor`, `expect`, `owner`, `expiry`, `reason`. Untouched copies kept at `C:\Users\BadBitch\AppData\Local\Temp\opencode\w3-review2-orig.mjs` (records, hash `84B12D97D6F44C8C0082CB28FC6D1082D7B94DDD867573649E3140660ECCD896`) and `C:\Users\BadBitch\AppData\Local\Temp\opencode\w3-lint-orig.mjs` (linter, hash `6294C11533EE19E12283ED4F5AFCBCC75884ECC293F85D57D14418357EA15792`). Every mutation below applied, suite run, restored from the copy, suite run again. After every restore the suite was green again (17 pass, 0 fail) and the file hash matched the clean hash. Full logs observed without pipes or truncation. Local date throughout: 2026-09-10.

| field | check tying it to a sibling | substitution that survives | what it buys |
|---|---|---|---|
| `id` | none to any sibling within the same record. `countProblems` checks the set against external `EXPECTED_IDS` plus `EXPECTED_COUNT`; `shapeErrors` checks uniqueness across records; the batch control checks the expiry partition (outlier B3 versus the 23). None verify that `B2` goes with `B2` anchor, reason, owner, or expiry. | Swap `id` between B2 and B6trim (both batch, same expiry 2026-10-10; anchors, expects, owners, expiries, reasons stay in place). Before: 17 pass, 0 fail. Mutated: 17 pass, 0 fail. Restored: 17 pass, 0 fail, byte-identical yes. | The wall still reports 24 correct while each of the two refusals would name the wrong record. An actor wanting the wall to stop guarding which arm is which can mislabel without red. Deleting one record and adding a new `id` with the same expiry does refuse (missing plus extra naming Nsplit or the new name), so the set is pinned; the mapping is not. |
| `anchor` | tied to `expect` only, via `anchorBad` (`got !== rec.expect`) plus `checkAnchors`. No tie to `id`, `owner`, `expiry`, or `reason`. | Two surviving forms. (a) Replace B2 anchor with `BRIEF OK` (occurs once in the linter, same `expect: 1`). Before 17 pass. Mutated 17 pass. Restored 17 pass, identical yes. (b) Swap anchors between B2 and B6trim (expects both 1, ids, reasons, expiries stay). Before 17 pass. Mutated 17 pass. Restored 17 pass, identical yes. | The wall watches different text at the same count while the original arm is unguarded. A later edit to the original trailing-strip arm no longer reds the wall. This is the set-versus-mapping failure the count pin repeated: three parties asked whether the number 24 was right; none asked whether each name still points at its own arm. |
| `expect` | tied to `anchor` via the same `anchorBad` strict inequality. No tie to any other sibling. | Alone it does not survive: B2 `expect: 1` to `2` gives 15 pass, 2 fail (`every anchor resolves` plus `later-record` naming B2 1x versus 2x). Restored 17 pass, identical yes. As a pair it does: the anchor swaps above keep `expect: 1` with a different once-occurring anchor and stay green. | Alone the tie holds. Together the pair is untied to the rest of the record: moving the pair moves the guard without moving the judgement, expiry, or owner. |
| `owner` | none. `shapeErrors` requires non-empty only. | B2 `owner: orchestrator` to `reviewer-two` (well-formed, non-empty). Before 17 pass. Mutated 17 pass. Restored 17 pass, identical yes. | Accountability evasion. The wall claims each refusal names the owner responsible, but any non-empty owner passes. A record can be re-owned without red. The reports state owner suitability stays with review; the mechanism enforces presence only, and this substitution proves it. |
| `expiry` | none to any sibling anchor, reason, or owner. Partition tie to `id` only via the batch plus boundary controls (23 share 2026-10-10, B3 holds to 2026-12-01), which pin values to external pinned dates, not to sibling content. | Among the batch any same-value exchange is trivially green. A distinct well-formed value does not survive alone: B6trim `2026-10-10` to `2026-10-12` gives 15 pass, 2 fail (batch wants 23 expired at pinned 2026-10-11, got 22 missing B6trim; boundary day-after refuses fails). Live check stays green (future). Restored 17 pass, identical yes. | Postponement alone is caught by the controls, which is why the batch move is loud. What survives is the mapping gap: expiries can be exchanged among batch members with no red, so which arm carries which deadline inside the batch is unguarded. Moving all 23 plus the pinned control dates together would also stay green; that larger self-consistent edit is visible only in review, which is the stated mitigation. |
| `reason` | none. `shapeErrors` requires non-empty plus a space only. | B2 reason to filler `Some plausible filler words that contain a space and look like reasoning but say nothing about the arm whatsoever.` Before 17 pass. Mutated 17 pass. Restored 17 pass, identical yes. | Judgement hollowing. The wall claims each record carries the section-5 reasoning verbatim, but any spaced filler passes. A reworded reason without re-check passes; a genuine re-check keeping old wording passes with a moved date. Both reports state reason quality stays with review; this substitution is the mechanism proof. |

Count of the class: four fields with no sibling tie at all (`id`, `owner`, `expiry` to content, `reason`), plus the anchor-plus-expect pair untied as a unit to the other four. Only `anchor` to `expect` (and back) is tied. The wall therefore guards that twenty-four well-formed records exist with present anchors and future dates, not that each record still describes its own arm.

## 3. The controls, mutated — every one that stayed green
Baseline before each row: 17 pass, 0 fail on the clean hashes above. Each break applied, suite run, restored from the untouched copy, suite run again. After every restore the suite was green again (17 pass, 0 fail) and the hash matched the clean hash. Full logs observed without pipes or truncation. The report section 6 lists ten mutations claiming each turns something red; I re-ran the ones most likely checked loosely — the twin harness (complex spawn plus temp briefs), the live-versus-effective clock (outer environment interaction), the naming message (content rather than logic), and the counting plus filtering helpers — plus two breaks of my own aimed at furniture. Chosen because a harness with temp areas and spawned runs hides failures most easily, environment interaction is where the pin fix lives, message-content assertions pass while naming nothing most easily, and shared helpers with per-record state hide loosening most easily.

| control | break applied | before | mutated | restored | went red |
|---|---|---|---|---|---|
| `isExpired` boundary | `now > rec.expiry` to `>=` | 17 pass | 16 pass, 1 fail (boundary expiry-day passes now refuses) | 17 pass, identical yes | yes |
| `countOccurrences` blinded | `return n` to `return 0` | 17 pass | 13 pass, 4 fail (every anchor 24 at 0x; bogus real-anchor half; later-record; double synthetic) | 17 pass, identical yes | yes |
| `liveNow` to pin | `return localToday()` to `return effectiveNow()` | 17 pass | 16 pass, 1 fail (live ignores pin: pin 2026-12-02 versus local 2026-09-10) | 17 pass, identical yes | yes |
| `anchorBad` narrowed loop | `for (const rec of records)` to `records.slice(0, 1)` | 17 pass | 16 pass, 1 fail (later-record must name Nsplit, got none; gate Check A itself stays green since it only sees the first record) | 17 pass, identical yes | yes (bind only; gate stays green by construction) |
| strict inequality loosened | `got !== rec.expect` to `got < rec.expect` | 17 pass | 16 pass, 1 fail (doubled anchor must refuse: 0 versus 1) | 17 pass, identical yes | yes |
| `expiryLate` discarded | filter computed then `return []` | 17 pass | 15 pass, 2 fail (shared expiry 0 versus 24; live-pin pinned view 0 versus 24) | 17 pass, identical yes | yes |
| `countProblems` bare message | full missing-plus-extra message to bare `record count N, want 24` | 17 pass | 16 pass, 1 fail (short-wall throws-miss on pattern for Nsplit; thrown message reads bare count with no name, the exact failure named F4) | 17 pass, identical yes | yes |
| twin E7sfx added conjunct (linter arm, anchor left present) | allowance line plus `&& false` in `scripts/brief-lint.mjs` | 17 pass, vehicle passes | 16 pass, 1 fail (vehicle flips to refused naming the suffix path; Check A stays green) | 17 pass, linter hash `6294C1…EA15792` again, identical yes | yes |
| linter reformat preserving behaviour (section 5 control) | E7sfx allowance split after `||` with newline plus indent | 17 pass | 14 pass, 3 fail (E7sfx anchor 0x versus 1x in Check A, bogus real-anchor half, later-record) while the suffix vehicle still passes | 17 pass, identical yes | yes (over-fail with safe sign) |
| `localToday` fixed past | full local getters to `return '2000-01-01'` | 17 pass | 17 pass, 0 fail | 17 pass, identical yes | NO — furniture |
| `shapeBad` seen reset per record | `const seen = new Set()` moved inside the loop | 17 pass | 17 pass, 0 fail | 17 pass, identical yes | NO — furniture |

Every control that stayed green: two. (1) A break inside `localToday` to a fixed past date turns nothing red. The live wall still passes (2000-01-01 precedes every expiry), the live-ignores-pin control still passes (both arms read the same broken value), and the pinned-override control still passes (both arms agree without override). No check binds the helper to the machine calendar; the suite trusts the machine clock, as the reports state, and the midnight local-versus-UTC boundary is pinned by convention plus one control that cannot tell which getter ran on a day they agree. (2) A break inside `shapeBad` resetting the seen set per record turns nothing red. The malformed-twins control tests duplicate via `shapeErrors` directly with an external seen set holding all ids, never via the shared `shapeBad` path, so the shared duplicate logic can be blinded while the control stays green. A wall with two B2 and no B6trim would still refuse via `countProblems` missing-plus-extra, but for the count reason rather than the shape reason, and no existing test requires the shape path to name the duplicate. Both read as coverage in a table and change no colour for their own subject. The report claim that controls are terminal binders whose colour change is itself the proof, with no further layer required or the regress never ends, does not hold for these two: the regress ends by stipulation where the binder is missing.

## 4. The second twin — which record, where in the distribution, what it cost
Record F3i (`/\bonce\b/i`, the once flag), placed at the easy end to bracket the claimed middle. E7sfx needs a Files scope plus a stray sentence plus suffix reasoning in both directions plus the added-conjunct proof; flag arms need only a case change plus a qualifier. The packet names one-character flag arms as the easiest and H4dist counting, B3 history, the star arms needing detail assertions, and the G7hy plus G8sent pair as fiddly or needing a different kind of twin. F3i is the cheapest of that easy group: one sentence, no scope reasoning, no counting, no history.

Vehicle (passes, `BRIEF OK`, status 0): base brief with authority `two-desks-work-orders-and-trains@6c2f515a9e03294f03e7f8be2611a257636860fb`, enforcement line for `scripts/run-tests.mjs`, Files bullet for `scripts/run-tests.mjs`, report `RESULT2.md`, plus `Do this once as an iteration.` Twin (refuses, `BRIEF REFUSED`, status 1, `FAIL` naming the once line): same base plus `Do this ONCE.` Minimal difference is case plus qualifier presence. Both briefs live under `C:\Users\BadBitch\AppData\Local\Temp\opencode\w3-twin2-vehicle.md` and `w3-twin2-twin.md` (7 lines each, 14 lines total, no harness helper needed since direct linter runs suffice). Mutation proof (flag removed, `i` dropped from the once pattern in `scripts/brief-lint.mjs`): twin flips to `BRIEF OK` (loosening, upper-case passes uncaught), while Check A turns red naming F3i 0x versus 1x plus the later-record bind, 15 pass 2 fail. Restored linter hash `6294C1…EA15792`, suite green again 17 pass. Unlike E7sfx where the anchor stays present under the added conjunct, here the anchor pins the flag (removing `i` breaks the substring), so the twin confirms behaviour the anchor already pins rather than exposing an anchor-blind change. That is exactly why this sits at the easy end.

What it cost: about 12 minutes wall-clock and 14 lines. Time covers reading the once logic plus the F3i, F4e, F5b, F6lines reasons, drafting the two one-line briefs, two direct linter runs, and the flag-removal proof with restore. Machine time for the two linter trials is under a second combined; the cost is human design, not compute. Two points bracket one: 14 lines at the easy end versus about 35 minutes and 65 lines in the middle for E7sfx. Flag arms cost less, counting plus history plus star arms will cost more, and at least one arm (B3, no resolving empty blob without manufacturing history) may be unbindable without manufacturing the world. No arm proved unbindable in this review; the B3 limit is carried from the packet and not re-proved here. Local date: 2026-09-10.

## 5. What this suite cannot say, with a constructed example
When a check hands a list, the missing shape is the defect that leaves every listed item present at its count while the claim attached to an item stops being true, plus the defect that reformats the guarded file. Both tested rather than assumed, each with a constructed run.

Reformat. Splitting the E7sfx allowance after `||` with a newline plus indent preserves behaviour (the suffix vehicle still passes `BRIEF OK`) while the anchor `q.endsWith(p) || p.endsWith(q)` is found 0x versus 1x. Mutated suite: 14 pass, 3 fail (Check A naming E7sfx, bogus real-anchor half, later-record). Restored: 17 pass, identical yes, linter hash `6294C1…EA15792` again. The wall therefore over-fails on formatting that touches an anchor substring (false positive with the safe sign, as the reports state) and stays green on formatting that touches nothing (indentation elsewhere preserves every count). Reformatting is not distinguished from a semantic edit when the span is touched; the tripwire cannot tell a wrap from a weakening.

Reason truth with anchors present at counts. Appending `&& false` to the same allowance line leaves the anchor substring present once, so Check A stays green, while the E7sfx vehicle flips from `BRIEF OK` to `BRIEF REFUSED` naming the suffix path (re-ran: 16 pass, 1 fail on the twin vehicle; restored 17 pass, identical yes). Every anchor present at its count, the reason `suffix allowance spared, exact equality would newly refuse short cites of long permitted paths` no longer true, nothing on the anchor list says so. The same added-conjunct shape works for any arm (upstream definition, sibling arm, call site bypassing a definition, dropped conjunct), which is the round-1 impossibility result: twenty-four of twenty-four anchors stay green under an edit changing what the anchored arm does. One twin of twenty-four is built; the other twenty-three remain anchor-only until their dates, which the packet adopts as debt with a deadline rather than repair. A list of present substrings cannot assert present behaviour, and no longer list can.

## 6. The clock — every second stroke tried, and what happened
`BRIEF_RECORDS_NOW` no longer reaches the live wall: `liveNow()` returns `localToday()` always, `effectiveNow()` keeps the pin for controls only, Check E calls `checkExpiry(RECORDS, liveNow())`. Untouched copy as above; past-expiry wall built by moving B6trim to `2026-09-09` (yesterday relative to local 2026-09-10), then outer strokes tried as a gate runner would, then restored to `2026-10-10` with suite green again 17 pass and hash `84B12D…CD896` identical yes.

Strokes tried from outside the suite. (1) Outer pin rescue: past-expiry wall without outer pin gives 12 pass, 5 fail with live naming B6trim expired `2026-09-09 (now 2026-09-10)`. With outer `BRIEF_RECORDS_NOW=2026-09-09` (expiry day, which would rescue if honoured) the live line still reads `(now 2026-09-10)` and still refuses; suite goes 11 pass, 6 fail (the extra fail is the pinned-override control restore arm, which now sees the outer pin instead of local). The pin does not green the live wall at one stroke; it reddens a control instead. (2) Malformed outer pin `not-a-date`: pinned helper returns null, live still reads local 2026-09-10 and still refuses (12 pass, 5 fail). No green. (3) Lower-case outer name plus empty plus whitespace-padded values: helper pattern requires upper-case name plus strict `YYYY-MM-DD`; unmatched names return null and live still refuses. No green. (4) Argument, file, working-tree state: the live path takes no argument and reads no file; changing record expiries is a file edit visible in review rather than a one-stroke environment green. Deleting the test file would silence all 24 at once, as the reports state; the alarm is the gate log holding one fewer test file plus review seeing the deletion, not a mechanism inside the suite. Not run as a whole-suite deletion here (scope forbids the whole suite); stated as the acknowledged out-of-mechanism stroke.

Second stroke found. (5) `TZ=Pacific/Niue` greens the live wall at one stroke. Past-expiry wall without `TZ`: live red (12 pass, 5 fail, now 2026-09-10). With `TZ=Pacific/Niue`: live green (`no record is expired` passes), suite 14 pass, 3 fail (remaining fails are pinned-date controls plus shared-expiry pre-batch arm, which use literal pinned strings and still see the past record). Local 04:14 at minus six hours is 10:14 UTC 2026-09-10; at minus eleven hours it is 23:14 2026-09-09, so `localToday()` moves back one calendar day and the expiry day passes. The pin stroke is closed; the zone stroke is open, and the gate runner passes the whole environment to children while keeping only the last 6000 characters of each script output, per the commit message, so an outer zone value greens every record under no notice at all. A convention that the runner will not set a zone is not a mechanism. This second stroke is worth more than everything else in section 3. Local date of these runs: 2026-09-10.

## 7. What is sound and should not be touched
Strict inequality with the double-occurrence bind (`got !== rec.expect` plus the synthetic `ab` twice case) refuses doubling while holding twice when twice is wanted; loosening to `<` was re-run and turns the double bind red. The expiry boundary (`now > rec.expiry`, day passes, next day refuses, batch sample plus outlier) was re-run both as an operator tightening and as a one-day date move; both turn the boundary plus batch controls red. Naming (missing plus extra identities beside the bare count, batch diffs naming absent versus extra, anchor lines naming `id` with got versus want, expiry lines naming `id` plus owner plus date) was re-run as a bare-count message and turns the short-wall bind red demanding Nsplit. The live-versus-effective split (live always local, controls honour the pin, live-ignores-pin showing all four at once) was re-run as `liveNow` to `effectiveNow` and turns that control red with pin versus local. The E7sfx twin vehicle plus distinct twin plus added-conjunct proof was re-run: anchors stay green while the vehicle flips, which is the packet in miniature and the reason one twin is worth more than longer anchors. These five are load-bearing and survived adversarial runs; narrowing or lengthening any of them risks re-opening what they close.

## 8. What I could not determine
Whether the true total for twenty-four twins can be estimated from one middle plus one easy sample without building a fiddly counting arm (H4dist exact gap), a history arm (B3 with no resolving empty blob), or a star arm needing detail assertions rather than status twins; any total from two points remains a bracket, not a sum. Whether hyphen bullets, padded quotes, upper-case emphasis, or semicolon-split delegation appear in real briefs outside this worktree at the rates the reasons assume; judged plausible as transcribed, with expiries as the re-check. Whether a paper twin (a fixture asserting nothing load-bearing) can be refused by mechanism rather than review; the vehicle plus minimally different twin plus arm mutation is the bar, enforcing it in code is review work. Whether the zone stroke can be closed without abandoning the local-date convention the corpus requires; pinning the zone, reading UTC, or fixing the clock each trades one convention problem for another. Local date: 2026-09-10.

## 9. Blocked or refused calls
None. No command was blocked or refused. No credential was handled or printed. Ran only `node --test scripts/brief-lint-records.test.mjs`, direct `node scripts/brief-lint.mjs` runs on briefs under `C:\Users\BadBitch\AppData\Local\Temp\opencode`, `Copy-Item`, `Get-FileHash`, `Get-Content`, `Get-Date`, and `git -C D:\addictedtoai-worktrees\w3-review` reads (`status`, `log --oneline -12`, `show --stat HEAD`, `diff 8f8b7bc..6c2f515 --stat`, `rev-parse HEAD`). No whole-suite run, no build, no `verify-*`, no Pulse, no Desk. No `git push`, no `gh`, no `bd` writes, no commits. Every mutation was restored from the untouched copies before the next, the suite was green again (17 pass, 0 fail) after every restore, and the final hashes match the clean hashes (`84B12D…CD896` for the records file, `6294C1…EA15792` for the linter). Every date in this report is this machine local date 2026-09-10.
