# Reviewer A2 (sonnet, sealed) — wisdom item 3 (3a required-text tripwire, 3b file-list closure) + two gate-red repairs

Commits read via `git -C D:/AddictedtoAI show <sha>`, all 15 in full. Cross-checked against `.beads/issues.jsonl`, `wisdom/README.md`, `FULL-MEM-LOG.md`, and current `loop/lib/review.mjs`.

## P0

**1. The mutation tests still mutate the shared checkout in place, violating a dated, measured repository mandate — 88d4144 fixes the race, not the hazard.** `loop/tests/lib-mutate.mjs` (added in 88d4144) serializes cross-file access with an atomic `mkdir` lock (genuinely cross-process — `mkdir` under the OS temp dir, one holder wins on `EEXIST`), and its own comment says why: "Node runs test FILES in parallel subprocesses, and two files... read-modify-write-revert the SAME tree file... measured on the 3a merge gate: 2032/2033." That diagnosis is correct and the lock is real. But every locked block still does `writeFileSync(BRIEF_LIB, mutated, 'utf8')` against `D:/AddictedtoAI/loop/lib/brief.mjs` — the tracked file in the shared checkout, not a worktree copy. `FULL-MEM-LOG.md:1782`: *"NEVER MUTATE A FILE IN THE SHARED CHECKOUT AT ALL. A git worktree is the right tool and this repository already uses them for exactly this reason."* That rule exists because of a measured incident (`:1767-1775`, 2026-08-31): a live agent committed during someone else's mutation window and shipped a broken pre-fix line undetected — "every test passed, the build passed, `git status` was clean." `withLibMutation`'s restore runs in a JS `try/finally`, which does not run on SIGKILL or a hard timeout; only the *lock directory* has a stale-reclaim (`STALE_MS=120_000`), not `brief.mjs` itself. A killed `npm test` run can leave the tracked source file corrupted with nothing to catch it beyond a human noticing `git status`. This is the symptom fixed, the cause left standing — and it now runs on every ordinary `npm test`, not just this merge.

**2. The wired-down contradiction check and its promised resolution don't meet.** The plan (`wisdom/README.md:298-300`) specifies: *"Refuses: dispatch on a brief whose required source text is missing, truncated or contradicted."* Brief `b5308d1` originally required refusal on all three (arm 3: "must be refused as contradicted"). Implementation `44ee775` shipped narrower — contradiction "computed, tested and returned... but deliberately NOT wired," reason: wiring it "false-fires on the existing suite (35 red, measured)." Brief was **amended after the code**, in `6302dd5` (11:07) following the implementation (`44ee775`, 11:02), to match what had already shipped — not the reverse. Both `round1-RESULT.md` and `round1-REVIEW.md` (`e9598e1`) push the open question to "round 3b's question... judging polarity needs a review-time reader." But `wisdom/briefs/item3b-round2-BRIEF.md` and its delivered instrument (`scripts/brief-closure.mjs`) are entirely about Files-list closure — no mention of contradiction polarity or a review-time reader anywhere in that round. `grep -n "contradict|polarity|review-time reader|wisdom-3a|item3a|unwired" .beads/issues.jsonl` — **zero matches**. Per this repo's own 2026-09-08 rule, a deferral naming a subject and not fixed in-job must become its own issue or a note on the parent that blocks closure; this one is neither. Net effect: a brief that flatly reverses a required sentence ("must NOT restore the floor") assembles and dispatches today with nothing refusing it.

## P1

**3. `loop/lib/review.mjs` was not touched by any reviewed commit, and the closure check is not wired to any live merge path.** `grep -rn "brief-closure" D:/AddictedtoAI/loop` — no matches; both briefs explicitly mark `loop/lib/review.mjs` read-only. The "automatic merge-gate wiring" is explicitly out of scope and "owned by the orchestrator" (`cbefe2b`, `798ec4c`), and the same beads grep for "brief-closure|automatic merge-gate|false-fire rate" returns nothing — a second unrecorded deferral, same pattern as finding 2. [Arch note: A3 found `review.mjs` WAS touched — by wisdom 5 (`e0d7839`), adding the corroboration section; A2's scope was items 3a/3b, so "not touched by any reviewed commit" is correct for its scope.]

**4. Two named wrong worlds.** Instrument 1 (3a): a negation synonym outside the hardcoded `NEGATIONS` list (`loop/lib/brief.mjs`) — e.g. "is prohibited from restoring the floor" — shares required tokens, triggers neither `missing`/`truncated` (words present) nor `contradicted` (no listed negation), and dispatches clean while reversing meaning. Also stated-but-real: pure token reordering with all words present is excluded from the truncation-order check by design ("order is checked only for the truncation prefix run, never for the complete case"). Instrument 2 (`scripts/brief-closure.mjs`): pin detection is a closed set of five hardcoded identifier patterns; any new exported enum/shape pinned exactly elsewhere under a different name is invisible and prints `CLOSURE OK` regardless — acknowledged in the brief but real.

## P2

**5. "location-independent test paths" (5bb179f) is only partly true.** `scripts/brief-closure.test.mjs:20`: `const MAIN = 'D:/AddictedtoAI';` remains hardcoded after the fix, unlike `INSTRUMENT`/`WORKTREE` which are correctly derived from `import.meta.url`. Mitigating: 9 other `.test.mjs` files in the tree already hardcode this same path.

**6. Counts.** `grep -c "^test("` `loop/tests/brief-required.test.mjs` = 9, matching RESULT1's "9 tests, all GREEN". `scripts/brief-closure.test.mjs` = 8, matching RESULT2's "8/8". [Arch note: the board's "16/16 x3" = 2b's 7 + 3a's 9 run together three times after the lock fix — reconciles.] The "35-red" false-fire count is author-asserted — **not checked**.

## Verified OK
- `03d1f87`'s allow-list entry (`scripts/no-change-dir-refs.test.mjs:44-48`) is narrow (one file, one change-name), reasoned, and genuinely staleness-checked by the pre-existing generic arm `every allow-list entry has a reason and is still live` (`:123-132`), which fails once the pattern stops matching. The honest alternative (copying the fixture) was available but would have reintroduced the "lossy copy / stale authority" defect class item 3 itself targets — this is "fixed the cause," not the detector.
- `lib-mutate.mjs`'s lock is a real cross-process mechanism and correctly diagnoses 2032/2033 as a race, not a defect.
- 3a's contradiction detector is live at assembly level (pinned by arm 5), not decoration.
- Test-arm counts match RESULT claims exactly in both files.
