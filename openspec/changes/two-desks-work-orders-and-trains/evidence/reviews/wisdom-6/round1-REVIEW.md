# REVIEW — Wisdom item 6 (first review revise with 1 load-bearing finding; two delta approves)

Date (local): 2026-09-10. Reviewers: fresh-context sealed review
invocations (separate sessions, no author reasoning supplied,
read-only access, disposable worktrees, kept for the orchestrator to
remove). Scope: the brief (`wisdom/briefs/item6-round1-BRIEF.md`,
second-model DISPATCH with 4 wording fixes applied, lint BRIEF OK
re-run in review) against the implementation (branch
`wisdom/item6-figures`: `b05a903`, then `a777067`, then `3936ae8` —
one new file throughout: `scripts/figure-provenance.test.mjs`) and
the author's `round1-RESULT.md`.

## First verdict: revise (one load-bearing + two notes)

The instrument false-fired on the orchestrator's own prescribed
passing shape: prose patterns matched inside backticked key paths and
captured the next figure's number on wrapped lines, so
value-plus-key-path refused on the value rule before the pointer
check (reviewer probe: main-fixed docs still red with 2 violations).
Prescribed fix: key-path spans never supply captured values.
Notes: document the S5 exemption's per-line boundary; fix the
publish-flag DELETE reason overstatement. All other arms approved
(13/14 with liveness red naming exactly S1–S4+S6-context; S5
exemption sound; mutations byte-identical; sweep honest).

## First fix and delta verdict: revise resolved, new finding

Author revision `a777067` (S5 test-carried live-verified exemption)
approved in part; re-probe surfaced the over-fire above. Second
author revision `3936ae8` (capture masking + two one-liners).

## Second delta verdict: approve

Fix exactly as prescribed; both colours reproduce (branch tree 15/14/1
red-with-5; main-docs probe 15/15/0 green); reviewer's own bypass
attempt escapes only via the documented residual (fully-backticked
value, no prose number); RESULT1 accurate. No silent weakening.

WOULD-CITE: n/a (machinery, no prose)
