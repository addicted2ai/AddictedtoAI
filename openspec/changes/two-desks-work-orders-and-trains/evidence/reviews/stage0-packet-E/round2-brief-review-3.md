I could not write the review file: this session is running under plan mode, which forbids every write including the one the instructions designate. The review itself follows in full.

# BRIEF REVIEW — arch-e-brief-r2.md — VERDICT: REVISE

## Findings

1. **Finding 2's git recipe does not resolve the commit it claims to resolve** (the brief's "Finding 2" fix: "resolve the commit that ADDED it from the merged ancestry — `git -C <ctx.repoRoot> log -1 --format=%H --diff-filter=A <result.mergedSha> -- .job/brief.md`"). Evidence: I ran the exact command on the real repository `D:/AddictedtoAI`, whose history has the identical shape the fixture produces (`.job/brief.md` added on the job branch, removed by the "remove job scaffolding before merge" commit, branch merged with `--no-ff`): merge `94ef7b9` ("job j-20260908-15 (repair)") — `git log -1 --format=%H --diff-filter=A 94ef7b9 -- .job/brief.md` printed `47ab12d` ("job j-20260831-12: brief") — an OLDER, unrelated job's brief-add reached through the first-parent line — while adding `--full-history` printed `6e45340` ("job j-20260908-15: brief"), the job's own add (both are ancestors of `94ef7b9^2`; the default walk follows the TREESAME first parent, not the merged branch). In the brief's own fixture (single job, `main` history never contains the path) the same simplification means the walk never enters the merged branch and the command returns nothing. As written the fix cannot measure the committed brief it names: the worker gets an empty or a wrong-job blob and no instruction covers that case ("If what git returns differs from briefText…" assumes a returned blob). Smallest change: add `--full-history` to the prescribed command, with one clause saying why (default history simplification never enters the second parent when the first parent is TREESAME for the path).

2. **"keeping the zero and the no-record cases" (Finding 3) asks to keep a case that does not exist in the file.** Evidence: `loop/tests/ledger.test.mjs` read whole (168 lines, 4 tests): no arm uses `review-nothing`, an absent record, or a review phase whose `carried` key is absent; `carried` absence is asserted only for the author phase (`:98`). Two readings: (a) add a third fixture arm whose reviewer writes no record and assert the `review1` phase carries no `carried` key; (b) the phrase points at no-record coverage living elsewhere (e.g. `runner-health.test.mjs`, `result-protocol.test.mjs`) that must stay untouched. Either reading builds green; mark it and move on, but a careful worker needs it resolved. Smallest change: say which reading the round means.

3. **Stale line numbers inherited from the freeze text** (non-blocking, all inside verbatim-quoted "Resolved before…" paragraphs that date from `de400f7`; the brief's own operational citations on this tip are all accurate): `run.mjs` abandon-sweep lines are at `:1017`/`:1088`, not `:979`/`:1050`; `removeJobWorktree` begins at `:852` and its junction-refusal block at `:877-900` (brief says `:827-894`, `:852-874`); `review.mjs`'s caller is at `:1215` (cited `:1194`); `conformance.mjs`'s teardown caller at `:389` (cited `:370`). I verified the CLAIMS still hold at the real lines (abandon lines carry none of the new keys; the callers use the guarded `removeWorktree(...)` form), so nothing here turns red — a one-line "numbers date from the freeze" note would keep a worker from hunting for the quoted lines.

## Checked and sound

1. Verbatim quotes — the character comparison was done by the linter; spot-read the quoted blocks against `git -C D:/AddictedtoAI show 9c1d980:openspec/changes/two-desks-work-orders-and-trains/tasks.md` (pattern count for the four "Resolved before E's freeze" markers plus "rides with packet E"/"Serves task 31" = 5; no dropped paragraph found).
2. File list is the closure — greps for `Object.keys(`, `LEDGER_FIELDS`, `varies`, `removeWorktree`/`removeJobWorktree`, `BUILD_INPUT_EXCLUSIONS`, `authority_sha`/`brief_chars`/`gate_seconds`/`carried`, `parseCarry`/`transcribeCarriedFindings` across `loop/tests/`, `scripts/` and `loop/lib/`: the only exact ledger-line/phase/removal/exclusion pins are the ones the brief names — `breakers.test.mjs:172,176-178`, `gate-transport-retry.test.mjs:876-880`, `portability.test.mjs:409` (self-built eight-key line, `Object.keys(line)` vs `[...LEDGER_FIELDS]`, unaffected as the brief says), and `worktree-cleanup.test.mjs` (in the Files list). No pinning file is missing from the list; the round's diff touches only listed files.
3. Quantifiers — the quoted freeze paragraphs resolve "every phase entry"/"every line"/"each of the other callers" concretely, and the code agrees (abandon-sweep lines at `run.mjs:1017/1088` carry none of the new keys; `carried` is 0-when-parsed-empty / absent-when-no-record per `run.mjs:678`).
4. Line numbers — all operational citations verified on tip `db10eac`: `ledger.test.mjs:43,72-103,97,100,102`; `run.mjs:678,1359-1360,1484 (brief_chars: briefText.length),1486 (authority_sha: mergeBaseSha),1503-1504,1957`; `git.mjs:216` (`merge --no-ff`); `mock-executor.mjs:53-85,262`; `mock-proposal-executor.mjs:109-116,269`; `worktree-cleanup.test.mjs:248-252`; `verify-launch.mjs:195-202` (set with `.beads` and the documented rule at 186-194), `hasCurrentBuild` at 285; `portability.test.mjs:409`; `gate-transport-retry.test.mjs:876-880`; `breakers.test.mjs:172-178`. Drift is confined to freeze-era quotes (Finding 3 above).
5. Contradictions — none between prose, quoted standard and code; the gate-transport expected set `['authority_sha','brief_chars','phases']` is consistent with the fixture's stubs carrying no `durationMs` (`PASSING` at `:292`) and `run.mjs:262-270` omitting `gate_seconds` when no result has finite `durationMs` and when the map is empty.
6. Instruments — both properties' enforcers exist and enforce what the brief says: `scripts/no-change-dir-refs.test.mjs` (scans `lib/loop/pulse/scripts/app/tools` for `openspec/changes` not followed by `archive/`) and `loop/tests/portability.test.mjs:109` and `:146` (scan the same machinery trees, excluding test sources, for every registered id/provider/model/harness token and embedded runner commands, with a MIN_SCANNED_FILES floor). Both read and behave as the ground rules claim.
7. Mutations — the five named mutations are all realizable at the cited lines and would go red against the round's fixes: `run.mjs:1486` → `'a'.repeat(40)` (red against a fixture-HEAD equality), `run.mjs:1484` → `1` (red against a committed-brief-length equality), `run.mjs:678` → `carried: 0` (red against the new `review-approve-carry` arm), and reverting the two pins (both deterministically red today: `authority_sha` differs between the two throwaway repos in `breakers`, and the `gate-transport` line already carries `brief_chars`+`authority_sha`). EXCEPT that Finding 2's whole fixture depends on the git command of Finding 1 above.
8. Ambiguity — only Finding 2 above.
9. Report contract — `RESULT2.md` exact numbered name, sections 1-7 in order, blocked calls and unfixed findings both mandated.
10. Ground rules — present: no `cd`, absolute paths, `.job/` for multi-step work, no build/verify-scripts/Pulse/Desk/push, exactly one final `npm --prefix … test` iteration at ≥ 600 s, edits confined to the Files list, blocked calls reported not routed around.

## What I ran

- `git -C D:/addictedtoai-worktrees/fleet6-stage0-E log --oneline -3; rev-parse --short HEAD; branch --show-current` → tip `db10eac`, branch `stage0/e-ledger-teardown`
- Reads (tool): `loop/lib/ledger.mjs`, `loop/tests/ledger.test.mjs`, `loop/tests/mock-executor.mjs`, `run.mjs` ranges (240-299, 640-719, 810-909, 1005-1034, 1340-1539, 1930-1989), `loop/lib/git.mjs:95-224`, `breakers.test.mjs:140-189`, `gate-transport-retry.test.mjs:30-129,830-889`, `mock-proposal-executor.mjs:90-129,245-289`, `scripts/verify-launch.mjs:180-309`, `scripts/no-change-dir-refs.test.mjs` (whole), `worktree-cleanup.test.mjs:240-343`, `portability.test.mjs:95-169`, `scripts/verify-design.mjs:1-80`
- Greps (tool): `LEDGER_FIELDS|makeLedgerLine|readLedger|authority_sha|brief_chars|gate_seconds` over `loop/tests`; `parseCarry|transcribeCarriedFindings|carry:|CARRIED` over the worktree; `Object.keys(|varies = |new Set([|BUILD_INPUT_EXCLUSIONS|removeJobWorktree|removeWorktree` over the worktree; `carried|durationMs|gate_seconds` over `loop`; naming/harness scans over `scripts` and `loop/tests`
- `git -C D:/AddictedtoAI log -1 --format=%H --grep='remove job scaffolding'` → `19c0e3f`
- `git -C D:/AddictedtoAI log --merges --oneline --ancestry-path 19c0e3f..HEAD -n 3`
- `$M = git -C D:/AddictedtoAI rev-list --merges --ancestry-path --reverse 19c0e3f..HEAD | Select-Object -First 1` (M=`94ef7b9`); then `git log -1 --format=%H --diff-filter=A $M -- .job/brief.md` → `47ab12d…`; `git log -1 --format=%H --full-history --diff-filter=A $M -- .job/brief.md` → `6e45340…`
- `git -C D:/AddictedtoAI log -1 --format='merge=%h %s' $M` → `job j-20260908-15 (repair)…`; same for `47ab12d` → `job j-20260831-12: brief`; `6e45340` → `job j-20260908-15: brief`
- `git -C D:/AddictedtoAI merge-base --is-ancestor 6e45340 94ef7b9^2` → 0 (yes); `… 47ab12d 94ef7b9^2` → 0 (yes)
- `git -C D:/AddictedtoAI show 9c1d980:…/tasks.md | Select-String "Resolved before E's freeze|rides with packet E|Serves task 31" | Measure-Object` → 5
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-E grep -n …` over `loop/lib/review.mjs`, `loop/conformance.mjs`, `loop/run.mjs`, `scripts/verify-launch.mjs`, `scripts/verify-design.mjs`, `scripts/tests/verify-launch.test.mjs`, `loop/tests/portability.test.mjs` etc.
- `git -C … show HEAD:package.json` (test script: `node scripts/run-tests.mjs`); `git -C … ls-tree -r --name-only HEAD` test-file census; `git -C … show HEAD:data/config.json` grep (no runner/model keys)
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-E grep -n "hasCurrentBuild|\.beads|test('" -- scripts/verify-launch-build-reuse.test.mjs`

## Out of scope, not pursued

- No test suites were run (review rules permit runs only to confirm an existing test's behaviour; both named reds are deterministic from code shape and needed no run).
- The `--full-history` behaviour was verified on the real repository's same-shaped history; a throwaway-repo reproduction was not possible because this session may not write anything anywhere.
