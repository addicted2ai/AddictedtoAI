# BRIEF REVIEW — arch-e-brief-r2.md — VERDICT: DISPATCH

## Findings
None that would make the worker build the wrong thing or turn a file red the brief forbids it to touch.

1. (Guess, non-blocking — ambiguity, not a defect) Finding 3 says "by giving `writeVerdict` an optional `carry` block parameter". The analogous parameter in `loop/tests/mock-proposal-executor.mjs` (`review-approve-carrying` arm) is named `carryBlock`. A worker could name the new `mock-executor.mjs` parameter `carry` or `carryBlock`; both satisfy the test since the new mode is self-contained. Smallest change if the architect wants determinism: name the parameter explicitly (e.g. "`carryBlock`, matching `mock-proposal-executor.mjs`"). Evidence below (MOCK269 output shows `carryBlock: CARRIED`).
2. (Guess, non-blocking — historical line drift, correctly attributed) Task-quote citations `run.mjs:1369`, `review.mjs:1194`, `conformance.mjs:370` describe the code at `de400f7` (the "Resolved before E's freeze … from `de400f7`" attribution) and are ~20 lines off this tip (`mergeBaseSha` definition now at `run.mjs:1391`; reviewer-worktree removal now at `review.mjs:~1215`; conformance removals now at `:321,:332,:389`). Because the brief attributes them to `de400f7` and every actionable instruction in "What this round fixes" cites tip-accurate lines (verified: `run.mjs:1484/1486/678`, `git.mjs:216`, `mock-executor.mjs:53-85/:262`, `breakers:172-178`, `gate-transport:876-880`), the worker follows the right lines. No change needed.

## Checked and sound
1. VERBATIM QUOTES — passed. The `>` blocks for 3b, 25, 26, 27 match `git -C D:/AddictedtoAI show 9c1d980:openspec/changes/two-desks-work-orders-and-trains/tasks.md` lines 405+ and 997+ (spot-read full paragraphs; linter owns character comparison). Command: `git -C D:/AddictedtoAI show … | Select-Object -Skip 404 -First 60` and `-Skip 996 -First 115`.
2. FILE LIST IS THE CLOSURE — passed. `Object.keys(line` pins exist only at `gate-transport-retry.test.mjs:879` and `portability.test.mjs:409` (plus `breakers.test.mjs:172` on `Object.keys(marked)`); `varies` only at `breakers.test.mjs:176`; `BUILD_INPUT_EXCLUSIONS` only at `scripts/verify-launch.mjs:195,292`; `LEDGER_FIELDS` pins at `ledger.mjs`, `gate-transport-retry`, `portability`, `issues.test.mjs:241-243`. Of these, only `breakers` + `gate-transport-retry` turn red under the packet (new additive keys); both are in Files. `issues.test.mjs:243` (`deepEqual([...LEDGER_FIELDS], eight keys)`) stays green because `LEDGER_FIELDS` is unchanged by design (task 25(vi)); `portability:409` builds its own line via `ledgerSchemaLine` and stays green, as the brief says; `branch-cleanup.test.mjs:86` uses raw `git worktree remove --force` in setup, not `removeWorktree`, so unaffected; `ledger-order/ledger-before-publish/job-budget` reference none of the new keys. Commands: the three `grep -n` runs below.
3. QUANTIFIERS — passed. "Every phase entry / every gate the job's OWN run executed / per REVIEW phase entry / the other callers / every test or script that pins (a)-(d)" are all given concrete domains (phase roles; `runGates` results' `durationMs/1000`, last run wins; `review.mjs:1194` + `conformance.mjs:321,370`; the (a)-(d) enumeration with starting greps). No unresolved "every/each/all/only/the" found in task text without a "Resolved" domain.
4. LINE NUMBERS AND CODE CLAIMS — passed. `ledger.test.mjs:97-102` (carried/brif_chars/authority shape asserts), `run.mjs:1484/1486` (`brief_chars`/`authority_sha`), `:678` (`carried = …carry.length`), `:1358-1360` (write+add `.job/brief.md`), `:1502-1503` (scaffolding-removal commit), `:1957` (`deleteBranch`), `git.mjs:216` (`--no-ff`), `mock-executor.mjs:53-85` (no carry param) / `:262` (`review-approve`) / `mock-proposal:269` (`review-approve-carrying`) + `CARRIED:107-116` (one malformed entry), `breakers:172-178`, `gate:874-880`, `portability:409`, `verify-launch:195/210/279-288`, `run.mjs:271-273` (`who.id`/`who.effort ?? null`) all read as described (within 0/1-based indexing). Commands: the `show HEAD:… | Select-Object -Skip …` runs below.
5. CONTRADICTIONS — passed. Conditional Files entries ("if a finding names it") correctly leave `ledger.mjs/runners.mjs/git.mjs/review.mjs/conformance.mjs/verify-launch.mjs` untouched this round (no finding names a production change); sweep-list rule covers standing pins via RESULT2 rather than edits; `helpers.mjs` read-only respected (not in Files).
6. INSTRUMENTS — passed by the brief's allowed form. Brief says "find what enforces each, and run it" for the naming + change-directory properties (check 6 permits exactly this). Actually enforcing: `loop/tests/portability.test.mjs:109` (names no model/provider/harness — read) and `scripts/shell-token-guard.mjs` + `scripts/shell-token-guard.test.mjs` (forbidden-token guard). Commands: the two `grep -n "enforce|guard"` / portability reads below.
7. MUTATIONS — passed. Finding 1 (`'a'.repeat(40)` at `run.mjs:1486`), Finding 2 (`brief_chars: 1` at `:1484`), Finding 3 (`carried: 0` at `:678`) are each realisable at the named line and go red against the specified value-assertions; the two pin-reverts (round-1 `varies` set; round-1 `['phases']` list) go red on this tip since the lines now carry the additive keys. No stay-green mutation identified.
8. AMBIGUITY — passed, except the non-blocking `carry` vs `carryBlock` guess above. The `log … ^2 -- .job/brief.md` recipe, the "measure line endings and assert the committed text's true length" rule, and the conditional `['authority_sha','brief_chars','phases']` expectation (with the comment explaining `gate_seconds`' absence) each admit one operational reading.
9. REPORT CONTRACT — passed. Brief requires exactly `RESULT2.md` in the worktree root with sections 1-7 in order (commits; findings+class+sweep; tests verbatim; mutation table; registry half; blocked calls; unfixed findings), plus recording blocked/refused calls and unfixed findings with file+line+reason.
10. GROUND RULES — passed. Present: never the token `cd`, absolute paths + `git -C …`, `.mjs` under `.job/` for multi-step, no build/full-verify/Pulse/Desk/push/`bd close`, no edits outside Files (incl. `package.json`, `runners.yml` effort half, `data/config.json`, `CLAUDE.md`, `AGENTS.md`, `data/`), no `git worktree remove` / no `node_modules` deletion, throwaway repos under OS temp, blocked-call rule, local dates.

## What I ran
(All read-only; no tree modified. Per the attached instruction I did not edit the plans file — this message IS the review.)
1. `git -C D:/AddictedtoAI show 9c1d980:openspec/changes/two-desks-work-orders-and-trains/tasks.md | Select-String -Pattern "^\- \[ \] (25|26|27|3b)\." | Select-Object -First 10`
2. `git -C D:/AddictedtoAI show 9c1d980:openspec/changes/two-desks-work-orders-and-trains/tasks.md | Measure-Object -Line`
3. `git -C D:/AddictedtoAI show 9c1d980:openspec/changes/two-desks-work-orders-and-trains/tasks.md | Select-String -Pattern "two-desks|authority:|^Task|tasks 25|Tasks 25" | Select-Object -First 20; echo "---"; git -C D:/AddictedtoAI log --oneline -3 9c1d980`
4. `git -C D:/AddictedtoAI show 9c1d980:openspec/changes/two-desks-work-orders-and-trains/tasks.md | Select-String -Pattern "- \[ \] (25|26|27|3b)\." | Select-Object LineNumber, Line`
5. `git -C D:/AddictedtoAI show 9c1d980:openspec/changes/two-desks-work-orders-and-trains/tasks.md | Select-Object -Skip 404 -First 60 | ForEach-Object { $_.Substring(0, [Math]::Min(220, $_.Length)) }`
6. `git -C D:/AddictedtoAI show 9c1d980:openspec/changes/two-desks-work-orders-and-trains/tasks.md | Select-Object -Skip 996 -First 115 | ForEach-Object { $_.Substring(0, [Math]::Min(220, $_.Length)) }`
7. `git -C D:/addictedtoai-worktrees/fleet6-stage0-E grep -n "Object.keys(line" -- loop/tests scripts loop/lib; echo "===LEDGER_FIELDS==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E grep -n "LEDGER_FIELDS" -- loop/tests scripts loop/lib | Select-Object -First 30; echo "===varies==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E grep -n "varies" -- loop/tests | Select-Object -First 20`
8. `git -C D:/addictedtoai-worktrees/fleet6-stage0-E grep -n "removeWorktree" -- loop/tests loop/lib | Select-Object -First 30; echo "===EXCLUSIONS==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E grep -n "BUILD_INPUT_EXCLUSIONS" -- scripts loop/tests loop/lib | Select-Object -First 30; echo "===deepEqual.*LEDGER\|deepEqual.*line\|snapshot==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E grep -n "deepEqual" -- loop/tests/ledger.test.mjs loop/tests/breakers.test.mjs loop/tests/gate-transport-retry.test.mjs loop/tests/portability.test.mjs loop/tests/issues.test.mjs | Select-Object -First 40`
9. `git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/tests/ledger.test.mjs | Select-Object -Skip 65 -First 45; echo "===RUN1484-1510==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/run.mjs | Select-Object -Skip 1478 -First 32`
10. `git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/run.mjs | Select-Object -Skip 670 -First 18; echo "===1355-1370==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/run.mjs | Select-Object -Skip 1354 -First 18; echo "===1950-1962==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/run.mjs | Select-Object -Skip 1949 -First 14; echo "===GIT216==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/lib/git.mjs | Select-Object -Skip 208 -First 16`
11. `git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/tests/mock-executor.mjs | Select-Object -Skip 48 -First 42; echo "===255-280==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/tests/mock-executor.mjs | Select-Object -Skip 254 -First 28; echo "===MOCK-PROPOSAL100-120==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/tests/mock-proposal-executor.mjs | Select-Object -Skip 99 -First 22`
12. `git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/tests/breakers.test.mjs | Select-Object -Skip 165 -First 20; echo "===GATE874-895==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/tests/gate-transport-retry.test.mjs | Select-Object -Skip 868 -First 28; echo "===PORT400-415==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/tests/portability.test.mjs | Select-Object -Skip 398 -First 18`
13. `git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/lib/runners.mjs | Select-Object -Skip 1 -First 35; echo "===VERIFY190-215==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:scripts/verify-launch.mjs | Select-Object -Skip 185 -First 30; echo "===VERIFY279-295==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:scripts/verify-launch.mjs | Select-Object -Skip 274 -First 22`
14. `git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/lib/git.mjs | Select-Object -Skip 95 -First 30; echo "===RUN827-895==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/run.mjs | Select-Object -Skip 820 -First 60 | ForEach-Object { $_.Substring(0, [Math]::Min(200, $_.Length)) }`
15. `git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/lib/review.mjs | Select-Object -Skip 1205 -First 18; echo "===CONF==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E grep -n "removeWorktree\|teardownRemoval" -- loop/conformance.mjs | Select-Object -First 10; echo "===WC252==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/tests/worktree-cleanup.test.mjs | Select-Object -Skip 240 -First 25`
16. `git -C D:/addictedtoai-worktrees/fleet6-stage0-E grep -n "unarchived\|change directory\|harness" -- loop/tests/portability.test.mjs | Select-Object -First 20; echo "===SCRIPTS==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E grep -ln "model.*provider\|no model\|no harness" -- scripts loop/tests | Select-Object -First 10; echo "===VERIFY-SCRIPTS==="; git -C D:/AddictedtoAI show 9c1d980 --stat | Select-Object -First 10`
17. `git -C D:/addictedtoai-worktrees/fleet6-stage0-E grep -n "enforce\|guard" -- scripts/shell-token-guard.test.mjs | Select-Object -First 10; echo "===SHELLGUARD==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E ls scripts/ | Select-String -Pattern "guard|verify|enforce"; echo "===PORT109==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/tests/portability.test.mjs | Select-Object -Skip 105 -First 35`
18. `git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/run.mjs | Select-Object -Skip 1356 -First 8; echo "===1500-1510==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/run.mjs | Select-Object -Skip 1500 -First 10; echo "===1955-1960==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/run.mjs | Select-Object -Skip 1954 -First 8; echo "===255-275==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/run.mjs | Select-Object -Skip 255 -First 20`
19. `git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:scripts/verify-launch.mjs | Select-Object -Skip 188 -First 10; echo "===210==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:scripts/verify-launch.mjs | Select-Object -Skip 206 -First 10; echo "===245==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:scripts/verify-launch.mjs | Select-Object -Skip 240 -First 12; echo "===MOCK269==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E show HEAD:loop/tests/mock-proposal-executor.mjs | Select-Object -Skip 262 -First 14; echo "===ABANDON==="; git -C D:/addictedtoai-worktrees/fleet6-stage0-E grep -n "makeLedgerLine\|abandon" -- loop/run.mjs | Select-Object -First 15`

## Out of scope, not pursued
- Whether the `--full-history` + `^2` brief-md recipe returns the true brief on all histories (trusted the brief's 2026-09-09 measurement; read-only).
- Whether the gate stub in `gate-transport-retry.test.mjs` records `durationMs` (brief conditions the expected key list on its absence; the worker observes it).
- RESULT1/REVIEW1 contents, suite green-ness, and full-file reads over 200 lines.
