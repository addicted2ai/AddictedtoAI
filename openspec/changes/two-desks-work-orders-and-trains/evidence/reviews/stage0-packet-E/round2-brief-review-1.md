# BRIEF REVIEW — arch-e-brief-r2.md — VERDICT: REVISE

## Findings

**1. Finding 2's prescribed read-back is impossible on this tree — both addresses it names are dead.**
Brief text ("Finding 2", fix paragraph): "the arm can read it back: `git -C <ctx.repoRoot> show <result.branch>:.job/brief.md` (or the same path at `result.mergedSha`) and assert its `.length` EQUALS `line.brief_chars`."
Defect: after `await runLoop(...)` returns on a `done` job, neither object exists. Evidence:
- `run.mjs:1503-1504` — before the merge the run executes `git rm -r -q --ignore-unmatch '.job' RESULT.md` and commits `job <id>: remove job scaffolding before merge`; the comment at `run.mjs:1495` says "so job scaffolding never reaches main". The merged tree (`mergedSha`) therefore does not contain `.job/brief.md`.
- `run.mjs:1956-1957` — `if (mergedBranch) { if (deleteBranch(ctx.repoRoot, mergedBranch)) ...` runs inside the cleanup, before `runLoop` returns (return at `:2120`); a merged job's branch ref is deleted in the fixture. `git show <result.branch>:...` fails with "unknown revision".
- The brief's own citation is wrong too: "(`run.mjs:1326-1327` writes `briefText` and adds it)" — actual write+add are `run.mjs:1359-1360` (`writeFileSync(join(worktree, '.job', 'brief.md'), briefText)`; `gitTry(worktree, ['add', '.job/brief.md'])`); `:1326-1327` is dry-run log text.
Commands: `git grep -n -E "mergedSha|mergeBaseSha|deleteBranch|\.job/brief\.md" -- loop/run.mjs`; `git grep -n -E "mergedBranch|\.job|git rm" -- loop/run.mjs`; reads of `run.mjs:1345-1374`, `:1490-1604`, `:1920-1964`.
Smallest change: tell the arm to resolve the ADD commit of the brief from the merged ancestry instead — the file is reachable in history: e.g. `git log -1 --format=%H <mergedSha> --diff-filter=A -- .job/brief.md` (its last commit touching the path in the merged ancestry is the brief-commit) and compare `line.brief_chars` to the byte length of `git show <that-sha>:.job/brief.md`; note the `.job` removal at `run.mjs:1503-1504` and the merged-branch deletion at `run.mjs:1956` so the worker does not discover both addresses dead by trial.

**2. Finding 3 mandates an edit to `loop/tests/mock-executor.mjs`, which the brief's own Files list and ground rules forbid — and the "reusable pattern" escape does not exist.**
Brief text ("Finding 3"): "read them for a reusable reviewer pattern first; if none fits, add ONE mode to `loop/tests/mock-executor.mjs` (`review-approve-carry`, beside `review-approve` at `:262`) that writes the same approve record plus two `carry:` entries, and use it." Files list: "Work only in these files. Every other path is out of scope" — `mock-executor.mjs` is not listed (`helpers.mjs` is explicitly read-only; `mock-proposal-executor.mjs` is not listed either).
Evidence: no current reviewer mode can write a two-entry carry record through the parser: `mock-executor.mjs`'s `writeVerdict` (`:53-85`) takes no carry parameter and no mode passes one; the only carrying reviewer mode in the tree is `review-approve-carrying` in `mock-proposal-executor.mjs:269` (`CARRIED` at `:109-116` deliberately contains one malformed entry, so `parseCarry` yields **1**, not 2 — `carry.test.mjs:304` proves one transcription), and it lives in a second unlisted file; `corrections.test.mjs` / `discarded-proposal-retry.test.mjs` plant carry records directly for `transcribeCarriedFindings` (`discarded-proposal-retry.test.mjs:237-242`), never through a review phase's parser, so none fits. The "if none fits" branch therefore fires with certainty, into an edit the brief forbids; a worker obeying the ground rules cannot complete Finding 3, and one obeying Finding 3 breaks the rules.
Commands: `git grep -n -E "writeVerdict|function result|process\.argv" -- loop/tests/mock-executor.mjs`; `git grep -n -E "review-approve-carrying|function pmock" -- loop/tests`; reads of `mock-executor.mjs:53-85,230-319`, `mock-proposal-executor.mjs:76-116,249-306`.
Smallest change: add `loop/tests/mock-executor.mjs` (the `writeVerdict` `carry` parameter plus the new mode, with two well-formed entries) to the Files list — or, if the mode is to reuse `writeVerdict`'s existing `carryBlock`, add `loop/tests/mock-proposal-executor.mjs` instead and name the mode there.

**3. (Minor, check 4.)** The `run.mjs:1326-1327` citation in Finding 2's parenthetical does not hold — folded into Finding 1's evidence; the actual lines are `run.mjs:1359-1360`.

## Checked and sound

1. Verbatim quotes — character-level linter already ran; I spot-read the quoted task-25/26/27 and 3b blocks against `git show 9c1d980:...tasks.md` (lines ~432, ~998-1081 confirmed present, resolved "Resolved before E's freeze" paragraphs included); no dropped paragraph seen.
2. Closure over the change — for the actual round-2 diff (three test files) the sweeps are complete and permitted: the only exact pins of the ledger line are `loop/tests/breakers.test.mjs:172-178` and `loop/tests/gate-transport-retry.test.mjs:876-880`, both in the Files list; `loop/tests/portability.test.mjs:409` (hand-built eight-key `makeLedgerLine` line; optional keys emitted only when supplied per `loop/lib/ledger.mjs:133-135`) and `loop/tests/issues.test.mjs:243` (`LEDGER_FIELDS` array pin, unchanged by design) stay green — confirmed by reading; `pulse/tests/scout-queue.test.mjs` writes eight-key lines that still validate. The two closure defects are Findings 1 and 2 above.
3. Quantifiers — "every phase entry", "the callers" (run/review/conformance), "every test that pins…" are each resolved to concrete lists in the brief's own text; the sweep enumerates its four categories and its disposition rule.
4. Line numbers — verified at tip `db10eac`: `ledger.test.mjs:97/100/102` hold what the findings say (arm `:72-103`); `run.mjs:678/1484/1486` hold `carried`/`brief_chars`/`authority_sha` exactly as mutated in the findings; `mock-executor.mjs:262` is `review-approve`; `breakers.test.mjs:172-178` is the shape pin + `varies` set (`:176`); `gate-transport-retry.test.mjs:876-880` is the exact extra-keys pin; `scripts/verify-launch.mjs:195-196` carries `.beads` + the recorded reason. Exception: `run.mjs:1326-1327` (Finding 1).
5. Contradictions — none beyond Findings 1 and 2. The two pins' fixes agree with the code: the gate-transport fixture's gate stubs carry no `durationMs` (`gate-transport-retry.test.mjs:806-809`), and `run.mjs:262-270` returns `undefined` when no gate result has a finite `durationMs`, so `gate_seconds` is genuinely absent and `['authority_sha','brief_chars','phases']` is the right exact set; the breakers fix is right because `makeRepo` does not pin commit dates, so the two fixture repos' initial commits (hence merge bases) differ in sha and `authority_sha` is the field making the round-1 pin red — `brief_chars` is safely kept compared (identical queue items, identical assembled brief).
6. Instruments — both enforcement properties exist and are findable: `scripts/no-change-dir-refs.test.mjs` (walks `lib,loop,pulse,scripts,app,tools` for non-`archive/` change-dir references) and `loop/tests/portability.test.mjs` ("the loop and the loop config name no model, provider or harness at all"); `scripts/shell-token-guard.mjs` exists for the `cd` guard; the targeted-test list files (`ledger-order`, `ledger-before-publish`, `job-budget`, `worktree-cleanup`, `verify-launch-build-reuse.test.mjs`, `ledger.test.mjs`) all exist on the branch.
7. Mutations — each is realisable at the line it names and goes red against the intended fix: `'a'.repeat(40)` at `run.mjs:1486` (red against the HEAD-equality assert), `brief_chars: 1` at `:1484` (fixture brief is far longer), `carried: 0` at `:678` (red once a two-entry carry record runs the parser — see Finding 2 for the missing permission), and each pin reverted to its round-1 form goes red on this tip (authority_sha differs between the two fixture repos; the gate-transport line carries three extra keys today). A mutation that would stay green: `authority_sha: mergeBaseSha.slice(0, 40)` (same value) — trivial same-value mutations are the only green ones.
8. Ambiguity — none a careful worker could act on two ways in a round-blocking sense; "keeping the zero and the no-record cases" is design latitude, and the brief's conditional on Finding 3's executor is resolvable only by editing a file the brief forbids (Finding 2).
9. Report contract — `RESULT2.md`, exact numbered name, sections 1-7 with mutation table, blocked/refused calls section, and unfixed-findings section all present.
10. Ground rules — `cd` ban, absolute-path execution, one final `npm test` iteration (≥600 s) at the committed tip, no push, Files-list-only edits, blocked call → record and stop, mutation-restore discipline, commit-only-named-files: all present.

## What I ran

Shell (all read-only):
1. `git -C "D:/addictedtoai-worktrees/fleet6-stage0-E" log --oneline --max-count=4`
2. `git -C "D:/addictedtoai-worktrees/fleet6-stage0-E" grep -n -E "authority_sha|brief_chars|gate_seconds"`
3. `git -C "D:/addictedtoai-worktrees/fleet6-stage0-E" grep -n -E "Object\.keys\((line|phase)|Object\.keys\(result|carried|parseCarry|carry:" -- loop/tests scripts loop/lib`
4. `git -C "D:/addictedtoai-worktrees/fleet6-stage0-E" grep -n -E "LEDGER_FIELDS|varies|removeWorktree|removeJobWorktree|BUILD_INPUT_EXCLUSIONS"`
5. `git -C "D:/addictedtoai-worktrees/fleet6-stage0-E" grep -n -E "writeVerdict|function result|process\.argv" -- loop/tests/mock-executor.mjs`
6. `git -C "D:/addictedtoai-worktrees/fleet6-stage0-E" grep -n -E "review-approve-carrying|function pmock|export function pmock|const pmock" -- loop/tests`
7. `git -C "D:/addictedtoai-worktrees/fleet6-stage0-E" grep -n -E "mergedSha|mergeBaseSha|deleteBranch|job\.jobId|branch: ?\`|\.job/brief\.md" -- loop/run.mjs loop/lib/git.mjs`
8. `git -C "D:/addictedtoai-worktrees/fleet6-stage0-E" grep -n -E "mergedBranch|\.job|git rm|rm \[" -- loop/run.mjs`
9. `git -C "D:/addictedtoai-worktrees/fleet6-stage0-E" grep -n -l -E "unarchived|must not name|forbid.*id|naming.*runner" -- loop/tests scripts | Select-Object -First 10`
10. `git -C "D:/addictedtoai-worktrees/fleet6-stage0-E" ls-files scripts/verify-launch-build-reuse.test.mjs scripts/verify-launch.mjs loop/tests/ledger.test.mjs` and `git show HEAD:scripts/verify-launch.mjs | Select-Object -Skip 185 -First 30`
11. `git -C "D:/addictedtoai-worktrees/fleet6-stage0-E" grep -n -l -E "no-.*id.*refs|harness.*ids|runners.*yml.*lib|scan.*loop" -- scripts loop/tests` and `git ls-files scripts | Select-String -Pattern "no-|guard|name"`
12. `git ls-files loop/tests | Select-String -Pattern "name|reserved|id-|forbid|registry|clean"` and `git grep -n -l -E "mock-frontier|mock-reviewer" -- scripts loop/lib loop/run.mjs loop/conformance.mjs loop/app.mjs tools app`
13. `git -C "D:/addictedtoai-worktrees/fleet6-stage0-E" grep -n -l -E "runners\.yml|harness" -- scripts/*.mjs | Select-Object -First 12`
14. `git show HEAD:scripts/no-change-dir-refs.test.mjs | Select-Object -First 25` and `git show HEAD:loop/tests/portability.test.mjs | Select-Object -First 30`
15. `git -C "D:/addictedtoai-worktrees/fleet6-stage0-E" grep -n -E "GIT_AUTHOR|GIT_COMMITTER|function makeRepo|const makeRepo|randomUUID|Date\.now|new Date\(" -- loop/tests/helpers.mjs`
16. `git show HEAD:loop/tests/portability.test.mjs | Select-String -Pattern "scan|readdir|SEARCH|DIRS|walk|mock-|provider" | Select-Object -First 15`

Reads (read tool): `loop/tests/ledger.test.mjs` (full, 168 lines); `loop/run.mjs` ranges 262-301, 640-719, 1305-1374, 1490-1604, 1920-1964; `loop/tests/mock-executor.mjs` 1-120, 230-319, 319-407; `loop/tests/mock-proposal-executor.mjs` 1-160, 160-306; `loop/tests/carry.test.mjs` 283-320; `loop/tests/discarded-proposal-retry.test.mjs` 220-290; `loop/tests/breakers.test.mjs` 20-79, 140-264; `loop/tests/gate-transport-retry.test.mjs` 790-889; `loop/tests/helpers.mjs` 100-154. No test suite, build, verifier or mutation was run; nothing was written outside the plans file.

## Out of scope, not pursued

- RESULT1.md / REVIEW1.md were not opened (this review judges the brief, and the quoted standard plus the tip tree were sufficient for every check).
- Task 27's and task 3b's implementations were not re-judged (no round-2 finding names them; their state at tip was confirmed only as far as the sweep needed).
