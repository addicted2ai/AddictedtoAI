# THE PULSE GATE LEASE AT dd777ef — REVIEW3

## VERDICT
revise

## 1. The property, in one sentence, written first and never edited
The Pulse must refuse to rewrite tracked files underneath a live gate holder while letting a dead holder's file pass loudly, with the file mtime alone deciding live from dead. At the moment of writing I had read only pulse/run.mjs, pulse/README.md, and the worktree root listing.

## 1b. Where section 1 turned out to be wrong, and what changed my mind
Section 1 said a dead holder file passes loudly with mtime alone deciding. Reading pulse/run.mjs lines 132-154 and pulse/lib/core.mjs checkLease showed three corrections. First, a dead file left ahead of now beyond 60s does not pass: it refuses under its own future line until a person acts, an unbounded outage visible only in the run own stdout with exit 0. Second, STOP stands before the lease, so a run where both stand reports the maintainer brake with no lease line at all. Third, writes nothing as enforced is narrower than a reader takes it to mean: the refusal trials check only that data/derived/queue.json is absent, not that nothing was written. Section 1 stands as written; this note records where it was incomplete.

## 2. THE SWEEP — every trial that reads or extracts source, and what each still passes on
Enumeration method, 2026-09-10 local: searched pulse/tests/lease.test.mjs for readFileSync on run.mjs, new Function, and import.meta. Found 17 matches for readFileSync overall, but only two sites read the shipped program as text: line 731 const src = readFileSync(new URL('../run.mjs', import.meta.url)) and line 834 the same form. Found one new Function site at line 753. No other trial opens pulse/run.mjs or pulse/lib/core.mjs as text. All other trials reach via spawn of pulse/run.mjs with PULSE_ROOT at a throwaway root or via direct call of checkLease or holderFromLeaseText with live or injected timing. Injected checkLease trials run shipped logic with a fake timepiece; they do not read source and are out of the class for that reason, stated here so omission is not tidy hiding.

| trial | how it reaches its subject | what can change without it noticing | ran |
| display edge pinned exactly at lease.test.mjs:724, 119 renders seconds 120 and 121 render minutes helper reached via source read | reads pulse/run.mjs as text, extracts LEASE_AHEAD_DISPLAY_THRESHOLD_S by regex, locates function formatAheadDuration by indexOf, scans forward for balanced closing brace counting every { and } byte, evaluates extracted bytes via new Function with threshold closed over as argument | bypass of the helper at the call site keeps threshold literal and helper bytes intact while live rendering changes kind: replacing const aheadText = formatAheadDuration(aheadS) with const aheadText = `${aheadS}s` left extraction passing while live hour day decade and 150s trials failed, 35 pass 4 fail collected 39, extraction row stayed green. Second definition cannot be built with the same name in this tree because modules reject duplicate top level function names, and the attempt broke every spawn with SyntaxError Identifier formatAheadDuration has already been declared while extraction still passed, 13 pass 26 fail collected 39, extraction row stayed green. Unbalanced brace in a comment with no behavior change breaks extraction alone: adding // { inside the helper left all live trials green while extraction failed on balanced close found, 38 pass 1 fail collected 39. A renamed helper with a dead correct copy left behind would likewise be tested instead of the live one, by the same indexOf route | yes, three builds above plus baseline 39 pass 0 fail collected 39 |
| stat-failed guard text pins WARN-proceed at lease.test.mjs:807, gap stated in name source pin not a live run | reads pulse/run.mjs as text and asserts three substrings present: lease.state === stat-failed and WARN cannot read mtime of GATE_LEASE and proceeding; refusing on unreadable timing would risk refusing forever | control flow around the strings can change what they do without touching them: changing else if lease.state === stat-failed to else if lease.state === stat-failed && false keeps all three strings present while the arm can never fire and a stat-failed state falls through silently, suite stayed 39 pass 0 fail collected 39. Keeping the strings and adding process.exit(0) after the WARN line keeps all three present while flipping proceed to refuse, the exact forever outcome the design exists to prevent, suite stayed 39 pass 0 fail collected 39. Moving the three strings into a comment or dead branch would also keep the trial green while the live guard is gone, by the same text presence route | yes, two builds above, each 39 pass 0 fail collected 39, restored 39 pass 0 fail collected 39 with hashes matching baseline |
| future refusal remedy reason at lease.test.mjs:698, documents why raising wording is forbidden documentation not coverage | runs the shipped program via runPulse on a 90s ahead throwaway root, then asserts three substrings absent from output: rais and widen and increas in any case | any worse advice without those letter runs passes: replacing the shipped remedy Delete the file only when no gate run is active or correct the writer timepiece with If it keeps firing bump the tolerance band until it stops contains none of the three runs and the denylist row stayed green while six whole line trials failed, 33 pass 6 fail collected 39. Strictly this trial runs the program rather than reading source, so it is outside the class as defined, and it is listed here only because the brief sketched it as the third text trial: its fragility is the same kind, text presence cannot tell good advice from satisfying bad advice | yes, build above 33 pass 6 fail collected 39, denylist row green, restored 39 pass 0 fail collected 39 |
| considered and excluded: fresh refusal STOP absence at lease.test.mjs:143, STOP plus lease lease word absence at lease.test.mjs:304-305, absent and STOP lease word pins | each runs the shipped program and asserts absence from live output | nothing found beyond the remedy row: each is a live output check with a whole output pin beside it that fails on alteration, unlike the two source reads which have no live counterpart for the same arm. Listed so the sweep does not hide a trial to keep the table tidy | yes, baseline 39 pass 0 fail collected 39 covers them |

The class is therefore two trials that read source plus one output denylist with the same text fragility. No other trial in the file opens shipped source as text.

## 3. The three properties — what I found enforcing each, and whether it holds
1. A refusal prints one line and writes nothing. Enforced by pulse/run.mjs fresh arm lines 133-137 and future arm lines 144-150 with process.exit(0) before any pipeline use, plus per refusal trial three output pins gateLines length 1 with whole line regex, nonEmptyLines length 1, leaseWordLines length 1, plus status 0 and queueFile false and lease left standing. Ran: extra line pulse: note: run skipped on the fresh arm failed 11 trials with 28 pass 11 fail collected 39, so the one line half can go red and is not furniture. Writes nothing half holds only something smaller wearing its name: writing data/derived/pwned.txt from inside the fresh arm before the refusal line left the suite 39 pass 0 fail collected 39, because no trial looks beyond queue.json absence. A reader takes writes nothing to mean no filesystem write; the instrument means queue.json absent. 2026-09-10 local.

2. The advice a person reads at four in the morning cannot be silently replaced with worse advice. Enforced by six whole line pins on the future line at 90s and 100s and 150s and hour and day and decade, each ^...$ with path exact holder exact tolerance literal 60s and remedy literal Delete the file only when no gate run is active or correct the writer clock, plus single line pin plus the rais widen increas denylist. Ran: replacing the remedy with If it keeps firing bump the tolerance band until it stops, which contains none of the three runs, left the denylist green while six whole line trials failed, 33 pass 6 fail collected 39, matching the packet own 33/6 claim. So the denylist alone does not hold the property and the whole line pins do on refusal paths. What still gets through is satisfying and outside refusal paths: adding pulse: note: if this keeps firing adjust the band until it stops after the stale WARN line, a line with no GATE_LEASE and no lease word, left the suite 39 pass 0 fail collected 39. Stale leaseWordLines stays 1 and gateLines stays 1 by design, so worse advice on a proceed path is silent to this suite. The instrument holds same line replacement on refusal paths; it does not hold added advice where the suite narrowed lease related output to leave it out. 2026-09-10 local.

3. A rendering that is meant for a person to read cannot change kind unnoticed. Enforced for kind by live trials: 100s asserts seconds shape, 150s asserts minutes shape, hour asserts minutes or hours alternation, day asserts hours or days alternation, decade asserts 10y 0d literally. How each reaches the code is itself part of the answer. Live route: makeRoot throwaway under OS temp area, writeLease with ageMsAgo negative, runPulse spawn of pulse/run.mjs with --no-build --no-mint --offline, regex on stdout. Extraction route at lease.test.mjs:731-757: readFileSync of ../run.mjs, regex for threshold literal, indexOf function formatAheadDuration, brace scan counting every { and } byte, new Function with threshold closed over, asserts on 119 120 121. Ran: bypassing the call site to `${aheadS}s` left extraction green while four live kind trials failed, 35 pass 4 fail collected 39. Adding // { inside the helper with no behavior change left all live green while extraction failed, 38 pass 1 fail collected 39. Duplicate top level function names broke every spawn with SyntaxError while extraction still passed, 13 pass 26 fail collected 39. The live route holds kind change; the extraction route tests bytes that are not the executed path and can pass while the property is false or fail while it is true. 2026-09-10 local.

## 4. THE MUTATIONS — every one, including those that changed nothing
Baseline before every row, 2026-09-10 local: 39 pass 0 fail collected 39 via node --test pulse/tests/lease.test.mjs with no name filter. Untouched copies held at C:/Users/BadBitch/AppData/Local/Temp/opencode/lease-orig/run.mjs and core.mjs and lease.test.mjs. Baseline hashes pulse/run.mjs 99c17b8dd5d4ce41e63afd734851c1ac27f0d7afb76b6ddc74c27a7f69db3404, pulse/lib/core.mjs 0993153ca0c63a3b3bb6478bd2a96e62316ca59cf230fb6c4b7a1f117724f93d, pulse/tests/lease.test.mjs f6a0c0bc3da98386ca02a6dc45e9f18853c73fc05f79b16c38205405dd40b154. After every restore the hashes matched baseline and the suite was green again 39 pass 0 fail collected 39. No other file was edited and nothing was committed.

| arm | mutation | before | mutated | restored | went red |
| stat-failed guard in pulse/run.mjs | condition to lease.state === stat-failed && false, all three pinned strings kept, arm can never fire and falls through silently | 39 pass 0 fail collected 39 | 39 pass 0 fail collected 39, source pin row green | 39 pass 0 fail collected 39, hashes match | no, nothing went red while loud proceed became silent proceed |
| stat-failed guard in pulse/run.mjs, serious direction | keep WARN line bytes and add process.exit(0) after them, all three pinned strings kept, proceed becomes refuse | 39 pass 0 fail collected 39 | 39 pass 0 fail collected 39, source pin row green | 39 pass 0 fail collected 39, hashes match | no, nothing went red while never refuse became refuse forever on unreadable timing |
| ahead rendering call site in pulse/run.mjs | const aheadText = formatAheadDuration(aheadS) to const aheadText = `${aheadS}s`, helper and threshold bytes untouched | 39 pass 0 fail collected 39 | 35 pass 4 fail collected 39, hour day decade and 150s live kind trials failed, extraction row green | 39 pass 0 fail collected 39, hashes match | yes, live kind trials, extraction did not |
| duplicate helper names in pulse/run.mjs | two top level function formatAheadDuration, first decoy correct on 119 120 121, second broken, threshold literal kept | 39 pass 0 fail collected 39 | 13 pass 26 fail collected 39, every spawn failed SyntaxError Identifier formatAheadDuration has already been declared, extraction and stat-failed source rows stayed green | 39 pass 0 fail collected 39, hashes match | yes for live, no for the two source rows which stayed green while the program did not load |
| brace in comment inside helper in pulse/run.mjs | add // { with no behavior change | 39 pass 0 fail collected 39 | 38 pass 1 fail collected 39, only extraction failed on balanced close found, all live green | 39 pass 0 fail collected 39, hashes match | yes, extraction only, behavior unchanged, brittle not blind |
| fresh refusal writes in pulse/run.mjs | write data/derived/pwned.txt inside fresh arm before the refusal line | 39 pass 0 fail collected 39 | 39 pass 0 fail collected 39, fresh writes nothing row green | 39 pass 0 fail collected 39, hashes match | no, nothing went red while refused run wrote a file |
| fresh refusal single line in pulse/run.mjs | add pulse: note: run skipped with no lease word after fresh refusal line | 39 pass 0 fail collected 39 | 28 pass 11 fail collected 39, all fresh kind refusals plus 30s and renewal first leg failed on nonEmptyLines 2 vs 1 | 39 pass 0 fail collected 39, hashes match | yes, 11, not furniture |
| stale proceed extra line in pulse/run.mjs | add pulse: note: if this keeps firing adjust the band until it stops with no GATE_LEASE and no lease word after stale WARN line | 39 pass 0 fail collected 39 | 39 pass 0 fail collected 39, stale rows green | 39 pass 0 fail collected 39, hashes match | no, nothing went red while worse advice printed on a proceed path |
| future remedy wording in pulse/run.mjs | remedy to If it keeps firing bump the tolerance band until it stops, no rais widen increas present | 39 pass 0 fail collected 39 | 33 pass 6 fail collected 39, 90s hour day decade 100s 150s whole line trials failed, remedy denylist row green | 39 pass 0 fail collected 39, hashes match | yes for whole line pins, no for the denylist which stayed green |

## 5. What this suite cannot say, with a constructed example
It cannot say a refused run wrote nothing, only that queue.json is absent. Constructed and run 2026-09-10 local: inside if lease.state === fresh add try writeFileSync(join(root data derived pwned.txt) pwned) before the refusal line. The fresh trial still reports 39 pass 0 fail collected 39 with the refusal line whole, single line held, queue.json absent, lease standing, while pwned.txt exists in the throwaway root. Same holds for any path other than queue.json and for a second advisory line without lease wording on a proceed path, shown by the stale extra line build that stayed 39 pass 0 fail collected 39. A fix aimed at queue.json alone leaves the class open: the claim needs either a directory tree comparison before and after refusal or a narrowed name such as writes no derived state the suite checks.

## 6. What is sound and should not be touched
Live whole line pins on refusal and proceed lines with narrow slots and single line pins on refusal paths: they failed 11 on an added refusal line and 6 on a replaced remedy and 4 on a rendering kind change, each with the failing output quoted in full. Strict millisecond edges by injection for max and tolerance, non-finite mtime and timepiece fixtures, holder truncation at 120 with size and line count far ends, STOP order, renewal pair that names the hole it does not close, and the stat-failed gap stated in the trial own name rather than faked live. Three rounds improved this packet by keeping those live pins; reopening them to loosen shapes or to widen slots would hand back the blindness where added sentences hide. The two source text trials are the place to narrow, not the live pins.

## 7. What I could not determine
Whether any writer timepiece today runs more than 60s fast and how often a dead future file appears in practice; the future line makes it loud when it fires but the population size is unknown until then. Whether a live timing read failure can be induced on another machine where permission bits fail stat; ten approaches were tried and recorded before, and this review did not retry them beyond reading the recorded list. Whether the renewal hole matters in practice depends on holder touch discipline outside this repository, which the suite pins across time but does not watch across time. One cause explains the duplicate name build: modules reject duplicate top level function names, so live spawns failed while extraction passed; no second platform cause is offered. 2026-09-10 local.

## 8. Blocked or refused calls
None. No command was blocked or refused, and nothing was routed around. Environment 2026-09-10 local: node v24.13.0 on win32, NODE_TEST_NAME_PATTERN unset, no name filter, every suite result above states collected 39. No Pulse run against this repository, the Desk, a build, any verify script, or the whole suite; only node --test pulse/tests/lease.test.mjs and throwaway roots under the OS temp area. No git push, no gh, no bd write, no commits. 2026-09-10 local.
