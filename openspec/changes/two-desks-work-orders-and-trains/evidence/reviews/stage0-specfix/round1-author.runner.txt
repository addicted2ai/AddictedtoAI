NOTE — THIS HEADER WAS RECONSTRUCTED, and says so rather than reading as original.
The launch line was overwritten at 19:23 when I re-ran arch-specfix-dispatch.ps1
to verify its new -Cleared guard refuses; the refusal path opens this file with
Out-File (no -Append) and clobbered it. The run itself was NOT affected — it is a
separate process and events.json kept growing throughout. The values below are
the true ones, taken from the board Log line written at 19:21 and from the file
timestamps, not retyped from memory.

launch 19:16:51 sha=53779c3676e4135108aec5b295c4840592f75fa5 worktree=D:\addictedtoai-worktrees\fleet6-specfix branch=stage0-specfix model=openrouter/meta/muse-spark-1.3-contributor variant=xhigh attachments=5

CONTEXT THIS ARTIFACT SHOULD CARRY: this run was launched IN VIOLATION of
A2AI-Orch's hold, because the dispatch script's live-gate guard sampled the
process table during the gap between two sequential gate stages and saw nothing.
Orch reviewed the exposure independently and ruled LET IT RUN: the worktree has
no node_modules (so no build or test can run in it), the main tree is untouched,
and its gate run 3 started at 19:17:40, AFTER this worktree existed — so run 3 is
unaffected by construction rather than by luck. The guard has since been changed
to refuse unless -Cleared is passed explicitly.
exit 0 19:28:40
events.json bytes=469456
RESULT1.md written: True
