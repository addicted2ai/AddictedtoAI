# BRIEF REVIEW — arch-f-brief-r2.md — VERDICT: REVISE

## Findings
1. **Finding 2's remedy is self-contradictory: the prescribed pattern fix cannot make the deleted allow-list entry live again, so restoring it breaks the suite.** Brief says (WHAT MAKES IT RIGHT): "match the terminal form as well as the path form — a change-name segment followed by `/` OR by a word boundary" AND "with a terminal-form match restored, `specs.mjs`'s reference is flagged again and NEEDS ITS ALLOW-LIST ENTRY BACK, reason and all."
   Defect: `loop/lib/specs.mjs:43` is the bare prefix `` `openspec/changes/` `` with no segment. A pattern requiring "a change-name segment followed by `/` OR word boundary" still does not match it, so `violations()` still returns nothing for that file and the restored entry fails the `still live` test (`found.some(v.file===entry.file && entry.match.test(v.reference))`).
   Evidence — command ran and output:
   `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "openspec/changes" loop/lib/specs.mjs` printed only `loop/lib/specs.mjs:23` (archive), `:28` (archive), `:43` (`the directories under 'openspec/changes/',` — bare prefix).
   `git -C D:/addictedtoai-worktrees/fleet6-stage0-F diff ddbfd52..HEAD -- scripts/no-change-dir-refs.test.mjs` printed OLD `BAD_REFERENCE = new RegExp(`${CHANGE_DIR}(?!archive/)`, 'g')` → NEW `...[^\\s/'"\x60]+/` (requires segment+`/`), deleted entry `{file:'loop/lib/specs.mjs', match:/openspec\/changes\/\x60/, reason:'discovers and reads...'}`, and `isAllowed` changed from `entry.match.test(v.text)` to `entry.match.test(v.reference)`.
   Current `scripts/no-change-dir-refs.test.mjs:19` requires segment+`/`, and `:64-66` tests `v.reference` (which excludes the backtick), so even a bare-prefix match `openspec/changes/` would not match restored `/openspec\/changes\/\x60/`. Worker cannot both implement the prescribed regex and restore the entry verbatim without turning `every allow-list entry has a reason and is still live` red. Smallest fix: either prescribe matching the bare prefix too (and state the entry's `match` must be updated to test `v.reference`, not restored verbatim), or drop the "bring the entry back" sentence and tell the worker to prove by scan that no live reference remains.
2. **Check 8 ambiguity — Finding 1 gives two seams for the count surface while Files makes one of them read-only.** Brief says: "carry `entries` in the selector's result (and print it at the caller if that is the cleaner seam), on the ALLOWED and UNRECORDED paths" but Files lists `loop/lib/select.mjs` for finding 1 and says "`loop/run.mjs` … READ-ONLY this round unless finding 1 genuinely cannot be satisfied without touching `loop/run.mjs`". Reading A: add `entries` to `selectJob` return and assert it in `runner-policy.test.mjs`, no `run.mjs` edit. Reading B: log the count at the caller (`run.mjs`), which requires touching a read-only file and a justification paragraph. A careful worker could act on either. Smallest fix: state which seam is required (selector return carries `entries`; caller logging optional/unchanged since `run.mjs:979-983` already logs it).

## Checked and sound
- Check 1 verbatim quotes: spot-checked `> - [ ] 21.` and `Resolved before F's freeze` / `passesToSupersede` / `enabled: false` / `MIN_SCANNED_FILES` present in authority blob; linter already verified substrings — command `git -C D:/AddictedtoAI show ddbfd52...:openspec/changes/two-desks-work-orders-and-trains/tasks.md | Select-String -Pattern "^\s*-\s*\[\s*\]\s*21\."` and `| Select-String -Pattern "Resolved before|passesToSupersede|enabled: false|MIN_SCANNED_FILES|authority_sha"`.
- Check 3 quantifiers: "BOTH programmatic readers", "EVERY return", "SIX directories", "TWO distinct failed check names" all given concrete domains in Resolved (iv)-(vi) and findings — no unresolved set found.
- Check 4 line citations (with minor drift, not REVISE alone): `select.mjs:110-112` return shape without count confirmed; `:170-183` conformance gate confirmed; `no-change-dir-refs.test.mjs:19` trailing-`/` confirmed; `runners.mjs` gate now at `:195-247` vs cited `:203-229`, `run.mjs` count log now at `:979-983` vs cited `:969-977` — function correct, numbers drifted by round-1 inserts.
- Check 6 instruments: `loop/tests/conformance.test.mjs` (`historyEntry` single-name `fabricated-quote-trap`, omission via `checks:[]`), `loop/tests/portability.test.mjs` (`RUNNER_SCAN_ROOTS`, `meetsScanFloor`, per-root loop at `:162`), `scripts/no-change-dir-refs.test.mjs` all exist and enforce the cited property — commands `git -C ... grep -n "loaded.*conformance entries|conformance entr"`, `grep -n "conformance" loop/run.mjs`, `grep -n "authority_sha|AUDITABILITY|LEDGER" loop/lib/ledger.mjs`, `grep -n "deepEqual.*selection|Object.keys.*select"`, `grep -n "RUNNER_SCAN_ROOTS|RUNNER_FIXTURE_ROOTS|meetsScanFloor|MIN_SCANNED_FILES"`.
- Check 7 mutations realisable: F1 generic-`reason` (gate reason at `runners.mjs:238-245` ends with count), F2 terminal-form, F3 `seen.size>0` at `runners.mjs:200-210`, F4 first-available fallback at `:217-225`, F5 removing `loop` from `RUNNER_SCAN_ROOTS` (`portability.test.mjs:32` feeds `:104-105` and expectation at `:162`) — each would go RED against the described fix; no Mutation that would stay green found.
- Check 9 report contract: names `RESULT2.md` (never bare), lists §§1-7, requires first line `done`/`blocked:`, blocked calls and unfixed findings — sound.
- Check 10 ground rules: never-`cd`, absolute paths, no build/full-suite except one final `npm test`, no push, Files-list confinement, blocked-reported-not-routed-around — present and sound.
- Check 2 closure: no pin outside Files found for the selector-shape addition (`deepEqual.*selection` empty), ledger comment already present at `ledger.mjs:88-93` so its exclusion from Files is correct, portability/conformance pins live in listed test files — sound apart from Finding 1 above which turns the live-entry test red.

## What I ran
- `git -C D:/AddictedtoAI show ddbfd52ca16ccf5cff92633d0b571e1a4ce69c91:openspec/changes/two-desks-work-orders-and-trains/tasks.md | head -c 2000; echo "---LEN---"; git -C D:/AddictedtoAI show ddbfd52ca16ccf5cff92633d0b571e1a4ce69c91:openspec/changes/two-desks-work-orders-and-trains/tasks.md | wc -l` (failed: pwsh has no head/wc)
- `git -C D:/AddictedtoAI show ddbfd52ca16ccf5cff92633d0b571e1a4ce69c91:openspec/changes/two-desks-work-orders-and-trains/tasks.md | Select-String -Pattern "^\s*-\s*\[\s*\]\s*21\." | Select-Object -First 5`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "openspec/changes" loop/lib/specs.mjs | Select-String -Pattern "changes" | Select-Object -First 10; echo "---SPEC-GREP-DONE---"; git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "loaded.*conformance entries\|conformance entr" loop/run.mjs loop/lib/select.mjs | Select-Object -First 20`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "conformance" loop/run.mjs | Select-Object -First 30; echo "---RUN-DONE---"; git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "authority_sha\|AUDITABILITY\|LEDGER" loop/lib/ledger.mjs | Select-Object -First 20`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "entries\|topRanked.*null\|selected.*null" loop/tests/runner-policy.test.mjs | Select-Object -First 30; echo "---POLICY-DONE---"; git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "deepEqual.*LEDGER_FIELDS\|Object.keys" loop/tests/issues.test.mjs loop/tests/portability.test.mjs | Select-Object -First 20`
- `git -C D:/AddictedtoAI show ddbfd52ca16ccf5cff92633d0b571e1a4ce69c91:openspec/changes/two-desks-work-orders-and-trains/tasks.md | Select-String -Pattern "Resolved before|passesToSupersede|enabled: false|MIN_SCANNED_FILES|authority_sha" | Select-Object -First 20; echo "---TASKS-PINS-DONE---"`
- `node -p "var CHANGE_DIR=['openspec','changes',''].join('/'); var re=new RegExp(`${CHANGE_DIR}(?!archive/)[^\\s/'\"\x60]+/`, 'g'); JSON.stringify(...)"` (failed: pwsh quoting ParserError; relied on file read of `:19` instead)
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "deepEqual.*selection\|deepEqual.*selectJob\|Object.keys.*select" loop/tests/ | Select-Object -First 10; echo "---SELECTOR-PINS-DONE---"; git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "RUNNER_SCAN_ROOTS\|RUNNER_FIXTURE_ROOTS\|meetsScanFloor\|MIN_SCANNED_FILES" loop/tests/portability.test.mjs | Select-Object -First 20`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F log --oneline -5; echo "---LOG-DONE---"; git -C D:/addictedtoai-worktrees/fleet6-stage0-F diff ddbfd52..HEAD -- scripts/no-change-dir-refs.test.mjs | Select-Object -First 80`
- Reads (no shell): `loop/lib/select.mjs:100-189`, `scripts/no-change-dir-refs.test.mjs` (full), `loop/lib/runners.mjs:195-247`, `loop/tests/portability.test.mjs:25-104,104-193,193-252`, `loop/tests/conformance.test.mjs:1-100,100-249`, `loop/lib/specs.mjs:1-40`, `loop/run.mjs:975-1004`, `loop/lib/ledger.mjs:80-129`

## Out of scope, not pursued
- Reproducing reviewer mutations by execution (forbidden: never against a mutated tree).
- Opening `RESULT1.md`/`REVIEW1.md` beyond what the brief tells its worker to do.
- Full-suite run, build, or verifier run.

