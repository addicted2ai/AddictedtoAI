# BRIEF REVIEW — arch-f-brief-r1.md — VERDICT: REVISE

## Findings

1. **Mutations § contradicts (vi)(a) on the runner-id extension mutation — worker would expect red and observe green.** Brief (vi)(a) correctly states: "BOTH `.ts` and `.tsx` must be dropped for the `app/` per-root assertion to go red, and that is the mutation." Brief Mutations § states: "Task 22(vi)(a): `.tsx` dropped from the runner-id extensions — the `app/` per-root assertion red." Dropping only `.tsx` leaves `app/sitemap.ts` scanned so the per-root `>=1` assertion stays GREEN. EVIDENCE — command `git -C D:/addictedtoai-worktrees/fleet6-stage0-F ls-files -- "app/*" | Select-String -Pattern "\.(tsx|ts|mjs|css|json|md|yml)$" | Group-Object { $_.Line.Substring($_.Line.LastIndexOf('.')) } | Select-Object Name, Count` printed `.css 1 / .mjs 3 / .ts 1 / .tsx 27`, and `loop/tests/portability.test.mjs:94-107` shows the runner-id scan extensions are `.mjs .md .json .yml` with `app/` contributing 0 today because its 3 `.mjs` files are `*.test.mjs` skipped by `{ skipTests: true }` (`:34-44`). Smallest fix: change the Mutations bullet to "`.ts` AND `.tsx` both dropped … go red" to match (vi)(a).

2. **Mutations § mislabels the absent-as-PASS arm as (d); (vi) correctly says (g) red and ONLY (g), (d) green.** Brief (vi) Mutation: "an absent check treated as PASS turns (g) red and ONLY (g) — it leaves (d) green, since (d) carries no absent entry." Brief Mutations §: "An absent check treated as PASS; arm (d) goes red." (d) is FAIL,PASS,PASS,FAIL,PASS with no absent entry, so absent-as-PASS cannot turn it red; (g) is the only fixture containing an omitted-check entry. Worker following the Mutations § would mutate, observe (d) green, and record a failed mutation. EVIDENCE — brief text itself (both sentences quoted verbatim above); no command needed beyond reading the brief, plus `loop/tests/conformance.test.mjs:1-167` contains no absent-check fixture today so (d)/(g) are both new arms defined only by the brief. Smallest fix: change Mutations bullet to "arm (g) goes red (and only (g))".

3. **LEDGER_FIELDS mutation red list is incomplete (minor, not REVISE-driving alone).** Brief (vii)/Mutations claim extending `LEDGER_FIELDS` to require `authority_sha` turns `issues.test.mjs:242-243` and `portability.test.mjs:409` red. `loop/tests/gate-transport-retry.test.mjs:878-882` also goes red: it asserts `Object.keys(line).filter((k) => !LEDGER_FIELDS.includes(k))` deep-equals `['authority_sha','brief_chars','phases']`; moving `authority_sha` into `LEDGER_FIELDS` makes the actual `['brief_chars','phases']`. EVIDENCE — read `loop/tests/gate-transport-retry.test.mjs:867-882` and `loop/lib/ledger.mjs:22-31` (`LEDGER_FIELDS` 8 keys). Smallest fix: add `gate-transport-retry.test.mjs:878-882` to the expected-red list, or state "at least" those two.

## Checked and sound

- Check 1 VERBATIM QUOTES: sound — `>` blocks match tasks 21/22 at `443b540` (linter-verified; read once, no dropped bold Resolved/CARRIED/00:36 paragraph seen). Command: `git -C D:/AddictedtoAI show 443b540:openspec/changes/two-desks-work-orders-and-trains/tasks.md | Select-String -Pattern "^\s*-\s*\[\s*\]\s*2[12]\."`.
- Check 2 CLOSURE (spent most effort): sound for the committed diff — `conformanceGate/recordConformance/loadConformance` production readers are only `select.mjs:154` + `run.mjs:970` (grep), `failed` pinned only in `conformance.test.mjs:96` (in Files), `blocked` only truthy-checked in `runner-policy` (in Files), `runnerJobTypeGate` pure call in `selector-rules.test.mjs:415-448` unaffected by `enabled`, `pulse/tests/publish.test.mjs:731` names `data/conformance.json` only as an `isEngineWrite==false` path, ledger comment pins only `issues.test.mjs:242-243` + `portability.test.mjs:404-411` (both read-only-listed). Commands: conformanceGate/recordConformance grep; `LEDGER_FIELDS|selectJob|escalationTarget|pickRunner` grep; `gate.failed|.blocked` grep; `CHECKS|skipTests|LEDGER_FIELDS` grep; `Object.keys` grep on selector/runner-policy/budget.
- Check 3 QUANTIFIERS: sound — "three consecutive", "both roles before every other gate" (three sites + positions), "never escalates", "BOTH readers print count" all given concrete domains/lines. Command: reads of `runners.mjs:124-187`, `select.mjs:146-269`, `run.mjs:934-984/1105-1137`.
- Check 4 LINES/CLAIMS: sound (modulo trivial off-by-one below) — `conformance.mjs:406-419` overwrite + `:437-470` record-after-verdicts, `runners.mjs:172-187` gate + `:124-142` pickRunner, `select.mjs:154-167` conformance-first + `:265-269` escalationTarget, `run.mjs:943-944/970/1125-1136`, `portability :31-32/:39/:91/:94-107/:133/:162/:174-201/:404-411`, `no-change-dir-refs :19/:33-49/:69-71/:87`, `specs.mjs:20-29` archive form (DONE, correctly excluded from Files), `exit-code-refusal :80-103` legacy shape, `data/conformance.json` 9 keys. Commands: reads listed + `git -C D:/AddictedtoAI show 443b540:data/conformance.json | node -p "Object.keys(...).length"` → `9`.
- Check 5 CONTRADICTIONS (beyond Findings 1-2): none blocking — Files reasons match file contents; `enabled` absent-means-enabled keeps shipped `runners.yml` loading; ground-rules scopes (`loop/` vs `loop+pulse+scripts+data/config.json` vs `+lib/app/tools`) stated correctly with narrowing recorded at `portability :146-155`.
- Check 6 INSTRUMENTS: sound — portability scans, `scripts/no-change-dir-refs.test.mjs`, `selector-rules` real-registry, `budget/expiring-proposal-precedence/runner-health` import `selectJob`, legacy-shape writers are `exit-code-refusal` + `runner-policy:111-131`, disabled-escalation correctly stated as "no instrument, (iv)(d) is first". Commands: greps above + reads of `selector-rules:415-448`, `specs.mjs:1-40`, `helpers.mjs`.
- Check 7 MUTATIONS REALISABLE: sound except Findings 1-2 — overwrite restore at `:415`, threshold default param, ignore-field, `escalationTarget`/`pickRunner` drops, span-vs-line plant, floor `>=0`, `skipTests` non-propagation at `:39`, `LEDGER_FIELDS` extension are all realisable at the named lines.
- Check 8 AMBIGUITY: none blocking — fourth-early-return shape, both-roles `run.mjs` shape `{started:true,selected:null,refused,rule:'runner:disabled'}`, per-root `filesUnder` form, BAD_REFERENCE widening through segment+`/`, floor-helper arm are each single-reading.
- Check 9 REPORT CONTRACT: sound — numbered `RESULT1.md` (never bare), 7 sections in order with blocked-calls and unfixed-findings sections required.
- Check 10 GROUND RULES: sound — no-`cd` token, absolute paths, no build/full-verifiers/Pulse/Desk-against-real/`git push`/`bd close`, no edits outside Files, one final full suite `npm --prefix … test` with ≥600 s timeout, blocked-call handling.

## What I ran

- `git -C D:/AddictedtoAI show 443b540:openspec/changes/two-desks-work-orders-and-trains/tasks.md | Select-String -Pattern "^\s*-\s*\[\s*\]\s*2[12]\." | Select-Object -ExpandProperty Line`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "conformanceGate\|conformanceHistory\|recordConformance\|loadConformance" -- "*.mjs" | Select-Object -First 40`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "deepEqual.*conformance\|conformance.*deepEqual\|Object.keys.*LEDGER\|LEDGER_FIELDS\|passesToSupersede\|entries" -- "*.mjs" | Select-Object -First 60`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "LEDGER_FIELDS\|selectJob\|escalationTarget\|pickRunner" -- "loop/*.mjs" "loop/lib/*.mjs" "loop/tests/*.mjs" | Select-Object -First 80`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "enabled" -- "loop/lib/runners.mjs" "loop/lib/select.mjs" "loop/run.mjs" "loop/tests/runner-policy.test.mjs" | Select-Object -First 30; echo ---; git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "CHECKS\|failed\|unrecorded" -- "loop/lib/runners.mjs" "loop/tests/exit-code-refusal.test.mjs" "loop/tests/runner-policy.test.mjs" "loop/tests/selector-rules.test.mjs" | Select-Object -First 50`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "gate\.failed\|\.blocked\|conformance\.reason\|has no recorded conformance" -- "loop/tests/*.mjs" "loop/*.mjs" "loop/lib/*.mjs" | Select-Object -First 40`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "CHECKS" -- "loop/conformance.mjs" "loop/lib/runners.mjs" | Select-Object -First 20; echo ---; git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "skipTests" -- "loop/tests/portability.test.mjs" | Select-Object -First 20; echo ---; git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "LEDGER_FIELDS" -- "loop/tests/gate-transport-retry.test.mjs" | Select-Object -First 10`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "Object.keys" -- "loop/tests/selector-rules.test.mjs" "loop/tests/runner-policy.test.mjs" "loop/tests/budget.test.mjs" | Select-Object -First 20; echo ---; git -C D:/AddictedtoAI show 443b540:openspec/changes/two-desks-work-orders-and-trains/specs/loop/spec.md | Select-String -Pattern "swap has a stated|enablement|cleared for|conformance check|append" | Select-Object -First 20`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "conformance" -- "pulse/tests/publish.test.mjs" | Select-Object -First 10; echo ---; git -C D:/AddictedtoAI show 443b540:data/conformance.json | python3 -c ...` (python missing, pipe closed — retried below)
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "unknown keys\|ignores unknown\|known keys" -- "loop/lib/runners.mjs" | Select-Object -First 10`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F ls-files -- "app/*" | Select-String -Pattern "\.(tsx|ts|mjs|css|json|md|yml)$" | Group-Object { $_.Line.Substring($_.Line.LastIndexOf('.')) } | Select-Object Name, Count`
- `git -C D:/AddictedtoAI show 443b540:data/conformance.json | node -p "Object.keys(JSON.parse(require('fs').readFileSync(0,'utf8'))).length"`
- Reads (ranges, never whole files >200 lines): `loop/conformance.mjs:400-473`, `loop/lib/runners.mjs:1-35/30-149/144-187/180-187`, `loop/lib/select.mjs:115-274`, `loop/run.mjs:930-984/1105-1149`, `loop/lib/ledger.mjs:1-35/75-144`, `loop/lib/specs.mjs:1-40`, `loop/tests/conformance.test.mjs:1-180`, `loop/tests/runner-policy.test.mjs:1-140/180-289`, `loop/tests/portability.test.mjs:25-174/174-273/395-451`, `scripts/no-change-dir-refs.test.mjs` (96 lines), `loop/tests/helpers.mjs`, `loop/tests/exit-code-refusal.test.mjs:70-104`, `loop/tests/selector-rules.test.mjs:410-448`, `loop/tests/issues.test.mjs:235-254`, `loop/tests/gate-transport-retry.test.mjs:865-894`, `pulse/tests/publish.test.mjs:720-739`

## Out of scope, not pursued

- none

