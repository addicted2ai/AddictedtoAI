# BRIEF REVIEW — arch-f-brief-r1.md — VERDICT: DISPATCH

## Findings
1. (non-blocking, not a REVISE reason) Brief's "How to work" step 2 targeted command omits `loop/tests/gate-transport-retry.test.mjs`, though (vii) names it as the third red for the `LEDGER_FIELDS` mutation. The brief's sentence: "`node --test loop/tests/conformance.test.mjs loop/tests/runner-policy.test.mjs loop/tests/selector-rules.test.mjs loop/tests/budget.test.mjs loop/tests/expiring-proposal-precedence.test.mjs loop/tests/runner-health.test.mjs loop/tests/exit-code-refusal.test.mjs loop/tests/portability.test.mjs scripts/no-change-dir-refs.test.mjs loop/tests/issues.test.mjs`". Defect: a literal worker runs mutations with that list and records only two reds — exactly the failure the brief warns about. EVIDENCE: `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "authority_sha\|brief_chars\|phases" -- loop/tests/gate-transport-retry.test.mjs` printed `874: // LEDGER_FIELDS is untouched...`, `880: ['authority_sha', 'brief_chars', 'phases']`, confirming the third pin exists outside the listed command; the read-only list also names only `helpers, exit-code-refusal, selector-rules, issues`, not `gate-transport-retry`. Smallest change: add `loop/tests/gate-transport-retry.test.mjs` to the step-2 command and to the read-only list.
2. (non-blocking, not a REVISE reason) "How to work" step 1 says read `loop/tests/portability.test.mjs:25-170`, which ends mid-file before the existing per-root fixture test at `:174-201`. The brief's sentence: "`loop/tests/portability.test.mjs:25-170` and `:400-412`". Defect: worker misses the `RUNNER_FIXTURE_ROOTS` fixture context for (vi)(a) if it reads only the named range. EVIDENCE: read of `portability.test.mjs:170-224` showed `test('the runner-id scan catches a fixture under each newly covered root'...)` at `:174-201` outside the range. Smallest change: extend the range to `:25-202`.

## Checked and sound
- Check 1 VERBATIM QUOTES: passed — read both `>` blocks once; no dropped paragraph observed; linter already verified substrings (command 2: tasks.md is 2252 lines).
- Check 2 CLOSURE: passed — new/changed identifiers (`CHECKS`, `recordConformance`, `conformanceGate`/`pickRunner`, `LEDGER_FIELDS`/`authority_sha`, `selectJob`, `runnerJobTypeGate`, `BAD_REFERENCE`/`isAllowed`) pin only in files the brief permits (Files list or named read-only) or stay green by construction; `gate-transport-retry` pins `LEDGER_FIELDS`-complement but committed ledger change is comment-only so stays green (commands 5, 6, 7, 10, 11).
- Check 3 QUANTIFIERS: passed — every "every/each/all/only/the" in quoted tasks has a Resolved (i)–(vii) domain (history shape, gate walk, count carriers, three refusal sites, six-root per-root counts via `filesUnder` per root, span-vs-line, two arms for (c)); helper name for (c) is worker-chosen but self-consistent.
- Check 4 LINE NUMBERS: passed — spot-checked `runners.mjs:34-97/80-82/124-142/137-141/172-187`, `conformance.mjs:406-419/437-470`, `select.mjs:132-134/:154-167/:265-269`, `run.mjs:943-944/:970/:1125-1136`, `portability :32/:39/:91/:97-105/:133/:162/:409`, `no-change-dir :19/:69-71/:87`, `specs.mjs:23` (archive form), `conformance.test :73-103/:94/:147-166`, `runner-policy :111-131`, `issues :242-243`, `gate-transport :879-880`, `selector-rules :416-426` — all hold what the brief says (reads + commands 8, 10, 11, 12).
- Check 5 CONTRADICTIONS: passed — prose/standard/code agree on fail-open `enabled`, three refusal sites with order, `pickRunner` returns (not throws) disabled explicit id, model-scan narrowing (`:146-155`) vs runner-id widening (extensions only), temporary plant-then-remove vs "never plant a fixture that stays planted".
- Check 6 INSTRUMENTS: passed — `portability.test.mjs` enforces naming, `no-change-dir-refs.test.mjs` enforces change-dir refs, `selector-rules` loads real `runners.yml` (line 426), `budget/expiring/health` call `selectJob` (command 8), `exit-code-refusal:80` + `runner-policy:111-131` write legacy shape, escalation has no instrument as brief states.
- Check 7 MUTATIONS: passed — all named mutations realisable at named lines and go red as described, including the discriminating pairs ((g)-only for absent-as-PASS leaving (d) green; both `.ts`+`.tsx` dropped needed for `app/` since `app/sitemap.ts` alone keeps it green — app is 32 files: 27 `.tsx`, 1 `.ts`, 3 `.mjs` test-skipped, 1 `.css`, command 9).
- Check 8 AMBIGUITY: passed — no sentence with two actionable readings; `entries` count, span-vs-line, per-root `filesUnder` form, and `enabled !== true` vs ignore-field are each single-reading.
- Check 9 REPORT CONTRACT: passed — numbered `RESULT1.md`, 7 sections in order, blocked calls and unfixed findings required.
- Check 10 GROUND RULES: passed — no-`cd`, absolute `git -C`, no build/verifiers/Pulse/real-Desk/push/close, no edits outside Files, blocked-called-reported, one final full suite with ≥600s timeout, local dates.

## What I ran
- `git -C D:/AddictedtoAI show ddbfd52:openspec/changes/two-desks-work-orders-and-trains/tasks.md | Measure-Object -Line | Select-Object -ExpandProperty Lines` → `2252`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F log --oneline main..HEAD` → `(no output)`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F status --short` → `(no output)`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "LEDGER_FIELDS\|authority_sha" -- loop/tests/issues.test.mjs loop/tests/portability.test.mjs loop/lib/ledger.mjs | Select-Object -First 30` → `ledger.mjs:22 LEDGER_FIELDS`, `:83/:85/:104/:135 authority_sha`, `issues.test.mjs:242-243`, `portability.test.mjs:405/409`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "authority_sha\|brief_chars\|phases" -- loop/tests/gate-transport-retry.test.mjs | Select-Object -First 20` → `:874/:880 ['authority_sha','brief_chars','phases']` (plus exit-code grep)
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "conformanceHistory\|passesToSupersede\|entries\|enabled\|runner:disabled\|escalationTarget\|pickRunner\|conformanceGate" -- loop/tests/*.mjs loop/lib/*.mjs | Select-Object -First 60` → only `pickRunner`/`conformanceGate`/`escalationTarget` exist pre-change (as expected for author round)
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "selectJob" -- loop/tests/budget.test.mjs loop/tests/expiring-proposal-precedence.test.mjs loop/tests/runner-health.test.mjs | Select-Object -First 10` → `budget:490/556/611, expiring:124, health:321` call `selectJob`
- `Get-ChildItem -Path D:/addictedtoai-worktrees/fleet6-stage0-F/app -Recurse -File | Group-Object -Property Extension | Select-Object Name, Count` → `.css 1, .mjs 3, .ts 1, .tsx 27`; total `32`
- `git -C D:/AddictedtoAI show ddbfd52:data/conformance.json | Select-String -Pattern '"runner"' | Measure-Object -Line | Select-Object -ExpandProperty Lines` → `9`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "CHECKS" -- loop/conformance.mjs | Select-Object -First 5` → `:63 const CHECKS`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "deepEqual.*LEDGER_FIELDS\|Object.keys(line)" -- loop/tests/portability.test.mjs loop/tests/issues.test.mjs loop/tests/gate-transport-retry.test.mjs | Select-Object -First 10` → `gate-transport:879, issues:243, portability:409`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "loadRunners\|runnerJobTypeGate" -- loop/tests/selector-rules.test.mjs | Select-Object -First 10` → `:16/:18/:426 real-registry load`
- `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "DEFAULT_REPO_ROOT\|runners.yml\|REAL\|actual registry" -- loop/tests/selector-rules.test.mjs | Select-Object -First 12` → `:416-426 REAL registry`
- Plus read-only reads (ranges, not shell): `runners.mjs` whole, `conformance.mjs:400-474`, `select.mjs:118-272`, `portability.test.mjs:25-169/170-224/400-451`, `no-change-dir-refs.test.mjs` whole, `run.mjs:934-993/1108-1147`, `ledger.mjs:80-149`, `conformance.test.mjs` whole, `specs.mjs:1-40`, `runner-policy.test.mjs:1-140`, `helpers.mjs` whole.

## Out of scope, not pursued
- none

