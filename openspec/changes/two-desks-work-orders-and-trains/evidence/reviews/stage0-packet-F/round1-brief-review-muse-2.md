# BRIEF REVIEW — arch-f-brief-r1.md — VERDICT: REVISE

## Findings
1. **Brief line:** Task 22(vi)(a) mutation: "`.tsx` dropped from the list turns the `app/` per-root assertion red." **Defect:** It stays green — `app/sitemap.ts` remains scanned. `app/` is not 0-scan for the reason stated alone; the 0 comes from `skipTests`, and dropping only `.tsx` leaves `.ts`. The worker following the brief will run the mutation, see green, and file a failed mutation table (a changed line with no red arm). **EVIDENCE:** `Get-ChildItem -LiteralPath D:/addictedtoai-worktrees/fleet6-stage0-F/app -Recurse -File | Group-Object Extension | Sort-Object Count -Descending | Select-Object Name,Count | Format-Table -AutoSize` printed `.tsx 27 / .mjs 3 / .css 1 / .ts 1`; `Get-ChildItem -LiteralPath D:/addictedtoai-worktrees/fleet6-stage0-F/app -Recurse -File | Where-Object { $_.Extension -eq '.ts' } | Select-Object FullName` printed `D:\addictedtoai-worktrees\fleet6-stage0-F\app\sitemap.ts` (a non-test source); `loop/tests/portability.test.mjs:40` is `else if (skipTests && e.name.endsWith('.test.mjs')) continue;` so only `*.test.mjs` are skipped and `sitemap.ts` is scanned once `.ts` is added. **Fix:** Change the mutation to "`.ts` and `.tsx` both dropped (or the added extensions removed) turns the `app/` per-root assertion red."

2. **Brief line:** "(vi)(a) `app/` is LISTED AND CONTRIBUTES ZERO FILES: the runner-id scan's extensions are `.mjs .md .json .yml` and `app/` holds 32 files, all `.tsx`/`.ts`". **Defect:** Factually wrong count; `app/` holds 27 `.tsx` + 1 `.ts` + 3 `.mjs` + 1 `.css` = 32. The scan contributes 0 only because the 3 `.mjs` are all `*.test.mjs` and `runnerTargets()` passes `{ skipTests: true }`. **EVIDENCE:** same `Group-Object` output as (1) plus `Get-ChildItem -LiteralPath D:/addictedtoai-worktrees/fleet6-stage0-F/app -Recurse -File -Filter *.mjs | Select-Object FullName` printed `app\index-route-jsonld.test.mjs / app\sitemap.test.mjs / app\frontier\page.test.mjs`. **Fix:** Reword to "`app/` holds 32 files (27 `.tsx`, 1 `.ts`, 3 skipped `*.test.mjs`, 1 `.css`) so the current scan contributes 0."

3. **Brief line:** Task 22(vi)(a): "asserts PER ROOT that each listed root contributed at least one scanned file". **Defect:** Ambiguous domain — `runnerTargets()` lists six dirs (`loop/pulse/scripts/lib/app/tools`) plus `data/config.json`, while the existing `RUNNER_FIXTURE_ROOTS = ['lib','app','tools']` covers three. A careful worker could implement 6-root coverage or 3-root coverage; both pass today but review may expect the other. **EVIDENCE:** `loop/tests/portability.test.mjs:94-107` (`runnerTargets`) vs `:32` (`RUNNER_FIXTURE_ROOTS`) read in ranges 25-174. **Fix:** Specify "each of `lib/`,`app/`,`tools/` (`RUNNER_FIXTURE_ROOTS`)" or "each of the six dirs in `runnerTargets()`."

4. **Brief line:** Task 22 Added paragraph: "narrowing over `pulse/` and `scripts/` is DELIBERATE and recorded at `:115-123`". **Defect:** Stale citation in this tree — the `pulse/lib/derive.mjs` / `pulse/verify-zero-model.mjs` comment is at `portability.test.mjs:146-155`, not `:115-123`; `:115-130` is the model-scan name collection. Worker can still find it by search, so not load-bearing alone. **EVIDENCE:** Read `loop/tests/portability.test.mjs:25-174` shows model code at 115-130 and runner-id narrowing comment at 146-155 ("Narrower over `pulse/` and `scripts/`, and deliberately so"). **Fix:** Update to `:146-155` (or confirm the `dc54da0` line if intentionally historical).

5. **Brief line:** Files entry "`loop/conformance.mjs` — `recordConformance` appends ...; the count in its log (task 21(ii), (iv))". **Defect:** Resolved (iv) requires the count only from `run.mjs:969-977` and the selector's reason (`select.mjs:154-167`); nothing in (ii)/(iv) requires `conformance.mjs` to log a count. Worker may add an unpinned log. Harmless but contradictory. **EVIDENCE:** Quoted (iv) text vs Files line; `loop/conformance.mjs:460-467` currently logs `recorded in` without a count. **Fix:** Delete "the count in its log" or cite where the count log is required.

## Checked and sound
- Check 1 verbatim quotes present — `git -C D:/AddictedtoAI show f1d33d9:openspec/changes/two-desks-work-orders-and-trains/tasks.md | Select-String -Pattern "Resolved before|CARRIED TO PACKET|DONE at" | Select-Object -First 20` returned the Resolved/CARRIED/DONE markers; `>` blocks read once, no dropped bold paragraph seen.
- Check 2 closure otherwise sound — `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "LEDGER_FIELDS|Object.keys|deepEqual.*checks|conformanceHistory|passesToSupersede"` shows pins only in `portability.test.mjs:409`, `issues.test.mjs:242-243`, `conformance.test.mjs:37,94-96`, all in Files or read-only lists; `specs.mjs` needs no edit (already archive form at `:23-29`).
- Check 3 quantifiers otherwise resolved — `runners.mjs:34-97/124-142/172-187`, `conformance.mjs:406-419/437-470`, `select.mjs:154/265-269`, `run.mjs:943-944/970/1125-1136`, `ledger.mjs:80-140` all verified by range reads.
- Check 5 contradictions otherwise none — ground-rules scopes (`loop/` for models, `loop/pulse/scripts/lib/app/tools` for runner ids, `isAllowed` on `v.text` at `no-change-dir-refs.test.mjs:70` vs `BAD_REFERENCE` at `:19`) verified.
- Check 6 instruments exist — `git -C D:/addictedtoai-worktrees/fleet6-stage0-F ls-files loop/tests/budget.test.mjs loop/tests/expiring-proposal-precedence.test.mjs loop/tests/runner-health.test.mjs runners.yml data/conformance.json` lists all four; `grep -n "selectJob"` shows all three call `selectJob`; `no-change-dir-refs.test.mjs:73-96` and `portability.test.mjs:109-172` enforce the two scans; legacy-shape writers `exit-code-refusal.test.mjs:84-87` and `runner-policy.test.mjs:116-120` verified.
- Check 7 other mutations realisable — overwrite at `conformance.mjs:415`, threshold `3→1`, absent-as-PASS (leaves (d) green), ignore-field, default-disabled, `escalationTarget` drop, `pickRunner` skip drop, same-line plant, floor `>=0`, `skipTests` non-propagation at `:39`, `LEDGER_FIELDS` extension — all at named lines.
- Check 9 report contract — `RESULT1.md` numbered, 7 sections in order, blocked calls and findings required, final `npm --prefix ... test` with wall time required.
- Check 10 ground rules present — no-`cd`, absolute paths, no build/verify/Pulse/Desk/`git push`/`bd close`, no edits outside Files, blocked-not-routed-around, local dates.

## What I ran
1. `Read D:/addictedtoai-worktrees/fleet6-stage0-F/loop/lib/runners.mjs limit 210`
2. `Read D:/addictedtoai-worktrees/fleet6-stage0-F/loop/conformance.mjs offset 400 limit 80`
3. `Read D:/addictedtoai-worktrees/fleet6-stage0-F/loop/lib/select.mjs offset 118 limit 160`
4. `Read D:/addictedtoai-worktrees/fleet6-stage0-F/loop/run.mjs offset 934 limit 50`
5. `Read D:/addictedtoai-worktrees/fleet6-stage0-F/loop/tests/portability.test.mjs offset 25 limit 150`
6. `Read D:/addictedtoai-worktrees/fleet6-stage0-F/loop/tests/portability.test.mjs offset 174 limit 80`
7. `Read D:/addictedtoai-worktrees/fleet6-stage0-F/scripts/no-change-dir-refs.test.mjs`
8. `Read D:/addictedtoai-worktrees/fleet6-stage0-F/loop/lib/ledger.mjs offset 80 limit 70`
9. `Read D:/addictedtoai-worktrees/fleet6-stage0-F/loop/run.mjs offset 1108 limit 40`
10. `git -C D:/AddictedtoAI show f1d33d9:openspec/changes/two-desks-work-orders-and-trains/tasks.md | Select-String -Pattern "Resolved before|CARRIED TO PACKET|DONE at" | Select-Object -First 20`
11. `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "LEDGER_FIELDS\|Object.keys\|deepEqual.*checks\|conformanceHistory\|passesToSupersede" -- loop/tests/conformance.test.mjs loop/tests/runner-policy.test.mjs loop/tests/portability.test.mjs loop/lib/ledger.mjs loop/lib/runners.mjs | Select-Object -First 60`
12. `Get-ChildItem -LiteralPath D:/addictedtoai-worktrees/fleet6-stage0-F/app -Recurse -File | Group-Object Extension | Sort-Object Count -Descending | Select-Object Name,Count | Format-Table -AutoSize; Get-ChildItem -LiteralPath D:/addictedtoai-worktrees/fleet6-stage0-F/tools -Recurse -File | Select-Object -First 5 | Format-Table Name -AutoSize`
13. `Read D:/addictedtoai-worktrees/fleet6-stage0-F/loop/tests/portability.test.mjs offset 398 limit 55`
14. `Read D:/addictedtoai-worktrees/fleet6-stage0-F/loop/lib/specs.mjs offset 1 limit 40`
15. `Get-ChildItem -LiteralPath D:/addictedtoai-worktrees/fleet6-stage0-F/app -Recurse -File -Filter *.mjs | Select-Object FullName; git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "enabled" -- loop/tests/runner-policy.test.mjs loop/lib/runners.mjs loop/lib/select.mjs loop/run.mjs | Select-Object -First 30`
16. `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "deepEqual\|Object.keys\|LEDGER_FIELDS\|conformanceGate\|recordConformance\|loadConformance" -- loop/tests/conformance.test.mjs loop/tests/exit-code-refusal.test.mjs loop/tests/runner-policy.test.mjs loop/tests/selector-rules.test.mjs loop/tests/issues.test.mjs | Select-Object -First 80`
17. `Read D:/addictedtoai-worktrees/fleet6-stage0-F/loop/tests/conformance.test.mjs offset 1 limit 100`
18. `git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "conformancePath\|recordConformance\|checks.*name.*result\|mock-frontier\|mock-" -- loop/tests/runner-policy.test.mjs loop/tests/exit-code-refusal.test.mjs loop/tests/helpers.mjs | Select-Object -First 50`
19. `Get-ChildItem -LiteralPath D:/addictedtoai-worktrees/fleet6-stage0-F/app -Recurse -File | Where-Object { $_.Extension -eq '.ts' } | Select-Object FullName; Get-ChildItem -LiteralPath D:/addictedtoai-worktrees/fleet6-stage0-F/lib -Recurse -File | Select-Object -First 3 | Format-Table FullName -AutoSize`
20. `git -C D:/addictedtoai-worktrees/fleet6-stage0-F ls-files loop/tests/budget.test.mjs loop/tests/expiring-proposal-precedence.test.mjs loop/tests/runner-health.test.mjs runners.yml data/conformance.json; git -C D:/addictedtoai-worktrees/fleet6-stage0-F grep -n "selectJob" -- loop/tests/budget.test.mjs loop/tests/runner-health.test.mjs loop/tests/expiring-proposal-precedence.test.mjs | Select-Object -First 10`

## Out of scope, not pursued
- Review-file edit skipped: final user instruction imposed read-only ("you may not edit, create or delete any file; final message IS the review"), overriding the brief-review template's edit step.
- No full-suite, build, verifier, or mutated-tree test runs per hard limits.
- Exact character-diff of `>` blocks vs authority blob (linter-verified per instructions).
- `pulse/tests/publish.test.mjs:731` content check beyond closure grep.

