# BRIEF REVIEW — arch-d-brief-r1.md — VERDICT: DISPATCH

## Findings

1. **Arm (d) is the only instrument for mutation M2 ("adoption condition dropped"), and nothing in the brief or its quoted standard forces arm (d) to run `runLoop`** — quoted resolution 24(iv) mandates `runLoop` for "at least (c)" only, and "each arm observes the pure helper's return." The adoption condition lives in the `run.mjs` call site; `escalationTarget`'s return is identical under M2, so an arm (d) built purely on helper observation would stay GREEN on the mutation, and the brief's Mutations section demands a red run for every row. A worker implementing (d) per its own text ("the repair runs on the cheap entry … the log names both refusals") will naturally use `runLoop` and find the red — but the brief does not guarantee it, and a literal reading of (iv) does not require it. Evidence: M2 changes only the call-site adoption predicate; the pure helper is untouched, and in arm (d) the escalated selection is non-null (frontier selects the `repair`), so only the returned/logged `runner` distinguishes correct from mutated. Smallest fix: extend 24(iv)'s sentence "at least (c) also runs `runLoop`…" to name (d) as well. Non-blocking — the mandatory mutation row self-corrects the worker.
2. **"Never past the first step" still has no instrument, though the Properties section presents arms (b) and (d) as the first** — arms (b)/(d) instrument "no escalation on a refusal of another kind"; neither exercises the escalation entry *itself* declaring `escalates_to` (registry validation permits a target that itself declares one — only registered-id, not-self, author-role are load errors, per quoted 23(iv)), so a mutation that follows the chain a second step would stay green under all four arms. Evidence: `git grep escalates_to` at `da1ace0` returns no fixture or code occurrence, and no task-24 arm declares `escalates_to` on the frontier entry. Fix is one sentence in the Properties section noting the one-step bound is enforced only by the call site being written once. Non-blocking — the feature code is unaffected; only the mutation inventory is weaker than the prose implies.

## Checked and sound

1. **Verbatim quotes** — PASS. Both `>` blocks (task 23 with its full bold "Resolved before D's freeze (the architect's quantifier enumeration…)" paragraph; task 24 with its full bold paragraph) compare line-for-line with `git -C D:/AddictedtoAI show da1ace0:…/tasks.md` lines 983–1099, and 14 normalised-phrase `Contains` probes against the source were all True after `\s+`→space normalisation. No dropped clause, no dropped resolution paragraph, no paraphrase.
2. **File list is the closure** — PASS. Grepped every exact-pin shape: `deepEqual`/`deepStrictEqual` on `sel`/`res`/`loadRunners`/`byId`/`defaultId`, `Object.keys(` on selector results, runLoop dry-run result equality, full-log equality on runLoop output, occurrences of `escalates_to`/`topRanked`/`escalationTarget`/`runner-policy`. Only pin touching the change is `deepEqual(sel.warnings, [])` (selector-rules.test.mjs:280) — a field compare, not a key set. `run.mjs:1223` is the sole non-test `selectJob` caller. No file outside the brief's list goes red. New test file must keep generic ids (portability scans `loop/` itself, including test files) — the brief already says so.
3. **Quantifiers** — PASS. Every "every/each/all/only/the ⟨set⟩" in the quoted tasks is resolved in the quoted paragraphs with code lines (every other refusal; every phase that reads `runner` — 264/287/305/757/1439–1441; run-entry sources; arms (a)–(d); registry errors). The brief's own quantifiers ("the one call site", "every later read of runner") enumerate their own domain.
4. **Line numbers and code claims** — PASS (verified by numbered dumps at `da1ace0`): select.mjs:88–103 ordering (directives p1 :90, pre-empting p2 :94–96, queue p3 :97–99, ripe p4 :100–102, sort :103); gates :199–207 with `runnerJobTypeGate` first at :200; break-at-first-refusal :214–216; `applyUpkeepFloor` budget.mjs:553–577; `floor.candidates[0] ?? null` select.mjs:233; `loadRunners` runners.mjs:16; `job_types` validation :63–76; run.mjs:901 (`runLoop`), :910 (`pickRunner`), :1160–1221 (resume tail; branch ends :1221, selection else at :1222–1283), :1223 (sole `selectJob` call), :1303 (dry-run return incl. `runner`), :264/:287/:305/:757/:1439–1441; selector-rules.test.mjs:374–387 (own-runners.yml pattern) and :415–448 (REAL registry through `loadRunners`); helpers.mjs:69–98 (default registry, no clearance), :107 (`makeRepo`), :170 (`ledgerLine`), :199 (`writeQueue`); budget.test.mjs:477–498 (C42 ceiling pattern). `runner-policy.test.mjs` confirmed absent at `da1ace0`. `desk-chain3.sh:94-132` is orchestrator-scratchpad text, quoted verbatim, not verifiable in this repo.
5. **Contradictions** — PASS. Brief prose, quoted standard and code agree: early conformance/health/lane returns precede gathering and return `selected: null`, so "blocked escalates nothing" holds whether or not `topRanked` is set there; adoption requires `topRanked === null && selected !== null`; the escalation entry's tier, conformance and health are re-checked inside the re-run's own `selectJob` call. The commit-scope wording ("Commit ONLY the files named above" while RESULT1.md is excluded) resolves with step 5's explicit "Do not add RESULT1.md".
6. **Instruments** — PASS. `loop/tests/portability.test.mjs` enforces no model/provider/harness/runner names (scans the machinery, asserts `deepEqual(result.hits, [])`, has a self-mutation test); `scripts/no-change-dir-refs.test.mjs` enforces no unarchived change-directory references (scans lib/loop/pulse/scripts/app/tools with a 3-entry allow-list); selector-rules.test.mjs:415–448 reads the real `runners.yml`; budget (490/556/611), expiring-proposal-precedence (124) and runner-health (321) all call `selectJob`. Property 5's "no instrument exists" is true — `escalates_to` occurs nowhere in code or tests at `da1ace0`.
7. **Mutations** — PASS, traced arm by arm: key-on-`sel.selected === null` → (c) red, (a)/(b) green (the `escalationTarget` rule condition is untouched by the mutation, which is what keeps (b) green); adoption condition dropped → (d) red (Finding 1); `escalationTarget` → null → (a)/(c) red; self-named `escalates_to` accepted → registry arm (v) red; call site never re-runs → (c)'s `runLoop` dry-run arm red while pure-helper arms stay green. One that would stay green: a call site following the escalation entry's own `escalates_to` a second step (Finding 2).
8. **Ambiguity** — PASS with the note in Finding 1. No sentence offers two defensible actions with different build outcomes; the named-mutation wording "key the escalation on `sel.selected === null`" is implementable in either code shape with the same red/green arm sets.
9. **The report contract** — PASS. `RESULT1.md` (numbered, never bare), seven sections in order (Commits; What changed, per file with exact `topRanked`/`escalationTarget`/adoption/log-line/load-error shapes; Tests run with verbatim `# pass/# fail` and wall time; Mutation table; The registry half with the exact `escalates_to:` line; Blocked or refused calls; Findings not fixed), blocked calls and unfixed findings recorded, nothing deferred silently.
10. **Ground rules** — PASS. No `cd` token anywhere; absolute paths for every git invocation; no build, no `scripts/verify-*.mjs` as a whole, no Pulse, no Desk against the real repository, no push; full suite restricted to one final iteration on the committed tip with ≥600 s timeout; edits confined to the Files list; read-only files named with reasons; blocked calls reported, not routed around.

## What I ran

- `git -C D:/AddictedtoAI show da1ace0:openspec/changes/two-desks-work-orders-and-trains/tasks.md` and the same piped to `Select-String -Pattern "- \[ \] 23\.|- \[ \] 24\."` and numbered line dumps of 983–1099
- 14 normalised-phrase `Contains` probes of brief phrases against tasks.md (all True)
- `git -C D:/AddictedtoAI show da1ace0:openspec/changes/two-desks-work-orders-and-trains/specs/loop/spec.md` (including the "Runner selection is a declared policy" requirement at 1193+)
- Numbered dumps at da1ace0: select.mjs (86–107, 196–244); budget.mjs (550–579); runners.mjs (1–114); run.mjs (255–314, 895–965, 1060–1144, 1140–1314); helpers.mjs (55–230); selector-rules.test.mjs (105–205, 360–448); budget.test.mjs (470–498); portability.test.mjs (1–60, 120–200); scripts/no-change-dir-refs.test.mjs (1–90); scripts/run-tests.mjs (1–60)
- `git -C D:/AddictedtoAI ls-tree -r --name-only da1ace0 -- loop/tests scripts` (runner-policy absent; enforcement tests present)
- `git -C D:/AddictedtoAI grep -n` for: `Object.keys` in loop/tests; `deepEqual|deepStrictEqual` in loop/tests; `selectJob(` tree-wide; `dryRun: true` in loop/tests; `output()` assertions in loop/tests; `escalates_to|topRanked|escalationTarget|runner-policy|escalat` at da1ace0
- `git -C D:/AddictedtoAI show da1ace0:package.json` (`npm test` → scripts/run-tests.mjs, glob discovery of new `*.test.mjs` confirmed); `da1ace0:runners.yml` id/roles/job_types listing (codex-gpt-luna-medium and codex-gpt-luna present for the handover line)

## Out of scope, not pursued

- desk-chain3.sh:94–132 lives in the orchestrator's scratchpad outside this repo; quoted verbatim in the standard, not independently verifiable here.
- Worktree-operational claims (junction `node_modules`, branch `stage0/d-escalation`, `main..HEAD` topology) concern the worktree, which is not the review tree.
- Merit of tasks 23/24 or the escalation design itself — the quoted standard is given, not under review.
