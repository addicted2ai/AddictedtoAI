# Evidence for `two-desks-work-orders-and-trains`

Every number in `proposal.md` and `design.md` cites one of these files, a bead id,
or a script here. They were produced on 2026-09-08 (local) by research agents and
peer sessions and copied out of session-scoped temporary directories so the
evidence outlives the sessions that made it. Point-in-time counts carry the moment
they were taken; the repository moved while they were being measured.

Nothing under `lib/`, `loop/`, `pulse/`, `scripts/`, `app/` or `tools/` may import
or reference this directory: archiving moves it to
`openspec/changes/archive/<date>-<change>/evidence/`.

## Reports

| File | Produced by | What it supports |
|---|---|---|
| `architect-outline.md` | A2AI-Fable-Arch (the architect) | The decision record the artifacts were drafted from: reserved property, measured problem, D1–D7, staging, risks. |
| `desk-mech-report.md` | Opus research agent, read-only | The lifecycle of one Desk job with file:line per step; fixed vs proportional cost; brief growth 16,181 → 103,881 chars; the review-diff median 8,101 and p90 55,034 (§B); the places that assume one item per job and the places that already generalise; inflow producers; candidate inefficiencies. |
| `ledger-report.md` | Sonnet research agent, read-only | Jobs per day, per-job size (median 1 content file / 4 lines), wall-clock, dollars (sweep-priced, a floor), review rounds, proposals; method and scripts named. |
| `hist-report.md` | Opus research agent, read-only | Dated timeline since 2026-08-28, recurring failure classes, every recorded cost/time measurement, how the Desk is chained today, open machinery beads by theme, things tried and reverted, structural sources of waste (G1–G10). |
| `beads-report.md` | Sonnet research agent, read-only | Inflow sources, churn (0.3–0.6 new per closed), open backlog shape at 13:56 UTC, the `h0z0` front/back-desk comment (§F). Its per-day table is UTC-bucketed and **superseded** by `scripts/orch-beads-flow-local.mjs`; its "81 machinery / 14 content" split is **superseded** by the backlog classification (see the provenance gap below). |
| `spend-report.md` | Sonnet research agent, read-only | Workspace spend split (interactive 85.6% / Desk 14.3%, fork-corrected), lines changed by bucket (content 4.15%), caveats on what the usage files cannot see. |
| `stale-report.md` | Sonnet research agent, read-only | All 27 open beads created 2026-08-31 (local) checked against the tree: 25 still valid, 1 partial, 1 fixed-but-open; seven natural batches. |
| `stale2-report.md` | Sonnet research agent, read-only; corrected by the architect 2026-09-08 | The 21 open beads created 2026-08-29 and 2026-08-30 (local): **17 still valid** (the first totals omitted `addictedtoai-4w2`, which the table lists), 4 partial, 0 fixed; ten already carry same-day directives in `DIRECTIVES.md:116-141`, none run. With `stale-report.md`, the whole 48-bead opening cohort is checked: **42 valid, 5 partial, 1 fixed** (`en3s`, since closed) — 47 of the 48 still open at the check. |
| `runner-workflow-cost.md` | Sonnet measurement agent, read-only | Workflow-adjusted model-minutes per **merged** job per author runner × type — every role and every outcome in the cell, over the jobs that merged — beside author-only minutes. The D7 pricing table. 243 ledger lines, 2026-08-28..09-08. |
| `stage0-baseline.md` | Sonnet measurement agent, read-only (task 30) | The Stage-0 baseline on **both** definitions, n = 206 of 208 merged jobs: brief-commit → merge median 22.81 min all-time / 23.35 since 09-01, overhead 3.85 / 3.90 min; brief-commit → records-commit overhead 5.497 / 5.52 min, reproducing `desk-mech-report.md`'s 5.5. `brief_chars` median 37,183 all-time with p90 102K and the day series 16,181 → 33,491 → 70,349 → 103,881, which is why the gate's comparator is the recent window and not the all-time median. Records that **41 of the 206 have no records commit at all** (`addictedtoai-l7cx`). |
| `bd-measurements.md` | Sonnet measurement agent, 2026-09-08, against a throwaway store only (task 26) | The real `bd` CLI's behaviour the Stage-3 choke point must be built on: `show --json` wraps in an array; comment bodies need `--include-comments` and never appear in `list --json`; a second `--claim` by another actor fails (exit 1); re-closing a closed issue is a silent exit-0 no-op that discards the new reason; `reopen` clears `close_reason`; metadata round-trips as JSON objects and `--set-metadata` merges; no argv ceiling found up to 200 ids; `--json` stdout is clean. Safety finding: `bd init --db <path>` with the process cwd inside the repository auto-detected the real remote and cloned the project's Dolt history into `~/.beads/embeddeddolt/`; the stray store was deleted. The choke point must pin cwd to the store it means. |
| `codex-spend-2026-09-08.json`, `scripts/codex-spend-capture.mjs` | A2AI-Luna-Boss-2, 2026-09-08 about 10:00 local | The captured, immutable output of the codex-lane measurement (the round-2 review's spot-check 4 found the script present and its input absent): per-session rows and two aggregates — the 48 sessions started before 08:00 local: 182,873,547 total tokens / 688,798 output (the earlier 182.4M / 685K was read while three sessions were still running, so it was a running total); all 67 sessions at capture, partial day: 238,067,103 / 828,209, 96.8% of input cached, 286:1 input to output. Carries its own caveat that raw totals overstate cost because cached input bills far below fresh. Invocation: `node scripts/codex-spend-capture.mjs ~/.codex/sessions` (session-scoped input). Worktree names in the rows include conformance worktrees that name a model; evidence/ is outside the portability scan. |
| `draft-report.md` | Sonnet research agent, read-only | Digest of the tabled `addictedtoai-7z07` draft on branch `impl/spec`: what it proposes, its requirement headings, its open must-fixes, what is reusable and what conflicts. |
| `gate-timings-final.txt` | A2AI-Orch, on `main` at `78c6361`, publishing off | **Cite this one.** The combined six-gate table; `gate-timings.txt` and `gate-timings-npm.txt` are the two partial files it supersedes and are kept only as the record of how it was assembled. |
| `gate-run-2026-09-08-a2eec9f/` | A2AI-Orch's `orch-gates-only.sh`, 15:28 to 15:37 local on `a2eec9f` (the first six-gate run after packet A merged); copied by the architect | `gates-only-summary.txt`: six exit codes with durations, the test count asserted against a floor of 1,700, the sub-second refusal, and the build path verify-launch took. `gonly-3-launch.log` is the one red gate (seed prose reviewed 185/186: `content/wiki/event/chatgpt-launch.md` has no record — fleet branch 4i2, batched by a decision Orch and the architect both own). `gonly-1-test.tail80.log` and `gonly-2-build.tail60.log` are tails (the full logs were 165 KB and 98 KB); design, surfaces and analytics logs are whole. `reuse-attempt-4.log`: the captured fourth reuse attempt, which REBUILT (49 s) three minutes after a 0.365 s reuse that was reported from terminal output and is not a captured artefact; the walk between the two timestamps found seven of eight written inputs under `.beads/`, the tracker writing itself (task 3b). DISCLOSURE: the seven `.log` files in this directory were absent from the repository from `9dcaf70` (15:50, which committed only `gates-only-summary.txt`) until the commit that added the `.gitignore` negation for evidence logs — `*.log` was ignored and the copy had been checked with a directory listing, which cannot see an ignore rule. They sat in one working tree and in no commit for that interval (design.md, revision record round 14; Orch's `addictedtoai-yejx` is the same class on the producing side). |
| `gate-run-2026-09-08-d8522ac/` | A2AI-Orch's `orch-gates-only.sh`, 17:50 to 17:58 local on `d8522ac` (`fleet5/4lrp` merged alone onto the Stage 0 tip, three codex processes running alongside); copied by the architect at 18:35 | `gates-only-summary.txt`: the pin line, six exit codes with durations (test EXIT 1 in 314 s, 124 files, 1,734 tests, 1,733 passed; build, launch, design, surfaces and analytics green in 30, 32, 34, 4 and 20 s), and the launch build path (spawned — the merge had written inputs, so reuse was unavailable again). `gonly-1-test.log` is WHOLE (166,507 B): the one red is `pulse/tests/domain-seeds.test.mjs:210`, `assertNoTransportFailure` (`pulse/tests/helpers.mjs:209`) reporting `connect EADDRINUSE` — no ephemeral port to spare — which the test's own message classifies as a transport failure and tells the operator to re-run alone. `domain-seeds-alone.log`: that file re-run alone at 17:58, 10 of 10 in 5.0 s, which is what makes the red environmental rather than asserted. `gonly-2-build.tail60.log` is a tail (the full log was 97,548 B); the launch, design, surfaces and analytics logs are whole. THE `b8fa5c1` RUN'S LOGS DO NOT EXIST: the harness wrote the same six fixed names and this run overwrote them in place, so the six-green that justified the day's only push is reported from terminal (design.md, revision record round 14; `addictedtoai-yejx`). The `.log` files here entered the repository one commit after the summary (`2aea346` carried only `gates-only-summary.txt`), together with the `.gitignore` negation. |
| `reviews/stage0-packet-B1/` | codex Luna reviewers via A2AI-Luna-Boss-2's pipeline — rounds 1 to 5 sealed at max, rounds 6 and 7 delta reviews at medium (unsealed, declared so in their own text); the round 7 worker's brief; the workers' reports for rounds 4 to 7 (moved from the repository root, where `.gitignore`'s `/RESULT.md` rule did not catch the numbered names); copied by the architect at the merge, 21:46 | The implementation review record of packet B1 (tasks 5–9, the brief diet): `round1..7-REVIEW.md` (revise ×6, approve at round 7), `round4..7-RESULT.md`, `round7-agent-brief.md` (the first class-scoped brief), and `round3-RESULT.md` — RECOVERED at the worktree's teardown on 2026-09-09 00:20: the worktree's constant-name `RESULT.md` (written 18:30:42, 14,973 B, headed "round 3", authority `047e9bf`) had not been overwritten by rounds 4 to 7, which wrote numbered files, so the earlier sentence here that round 3's copy "survives only in the coordinator's scratchpad" was wrong about where and right that it was unbanked; every other leftover in the worktree matched its banked copy by SHA-256 before deletion. ABSENT, stated so nobody looks: the workers' reports for rounds 1 and 2 — `RESULT.md` was overwritten in place by round 3; the round 1 to 6 worker briefs likewise. Authorities per round: fbe1306, 8ab1dee, 047e9bf, 4d86826, d75275c, d75275c, 529a819. Merged as `aa8bb3c`. |
| `gate-run-2026-09-08-0ed3448/` | A2AI-Orch's `orch-gates-only.sh`, 18:56 to 19:04 local on `0ed3448` (the frozen tip: `d8522ac` plus eleven commits from three lanes — the architect's evidence and record commits, mem-cond's four to `FULL-MEM-LOG.md`, the 18:00 Pulse run), the first run under the per-run directory (`gate-runs/20260908-185622/`, which nothing overwrites; file names are now `1-test.log` … `6-analytics.log`); copied by the architect at 19:07 | `summary.txt`: the pin (announced and HEAD agree at `0ed3448`, 18:56:23), six exit codes, all 0 — test 353 s (124 files, 1,734 passed, floor 1,700; the count ratchet had no prior count and ESTABLISHED its baseline at 1,734, so this run was not one the ratchet checked), build 32 s, launch 32 s (spawned — the merge and the Pulse had written inputs), design 35 s, surfaces 4 s, analytics 20 s. Test and build logs are tails (`1-test.tail80.log`, `2-build.tail60.log`; the full logs were 165,331 and 97,539 B); launch, design, surfaces and analytics logs are whole. The tree carried the maintainer's twelve `.agents/skills/` deletions and an untracked `.agents/unused_skills/` throughout, so this is a green on the sha MINUS those deletions, measured immaterial (no file under `lib/`, `loop/`, `pulse/`, `scripts/`, `app/`, `tests/` or `tools/` references `.agents`, and `prebuild.mjs` does not). PUSHED by Orch as `0ed3448:main` (`b8fa5c1..0ed3448`) after three checks — HEAD still the gated sha, fast-forward, revert guard 25 paths / 24 forward edits / 1 derived recomputation / 0 reverts — with publishing left false. |
| `reviews/stage0-packet-A/` | codex Luna max sealed reviewers (six rounds) and the round 6 worker, via A2AI-Luna-Boss-2's pipeline; copied out of the gitignored worktree by the architect at merge | The implementation review record of packet A (tasks 1–4): `round1..6-REVIEW.md` (verdicts revise ×5, approve ×1), `round6-and-5-RESULT.md` (the worker's report with the first live mutation table; appended rather than overwritten, so it carries round 5 below round 6 and two authority shas, the withheld finding of round 6), and `round6-agent-brief.md` (what the last worker was told). Authorities per round: 650ad91, 40a5795, 160f1c2, b189256, 99de120, 658625d. Kept because a worktree dies like a scratchpad and these are the only record of what each reviewer actually checked. ABSENT, and stated so nobody looks for them: the worker reports of rounds 1 to 4. `RESULT.md` is a gitignored constant name that each next round overwrote in place (the same class as the gate harness in `addictedtoai-yejx`, found by Luna-Boss-2 at 18:43); their substance survives only through the REVIEW files that read them. From packet B1 round 3 onward the coordinator copies each round's report under a round-numbered name before the reviewer starts, and later briefs name `RESULTn.md`. |
| `experiments/medium-vs-max-2026-09-08.md` | A2AI-Fable-Arch; pre-registered 20:11 (amended 20:16 to two mirrored judges, before any dispatch), workers run by A2AI-Luna-Boss-2 21:44–22:20, judges run by the architect 22:52–23:52 after Luna-Boss-2's session closed | One brief (sha256 `4DCBAD94…A8197`, authority `d75275c`, base `96e15fa`) to codex Luna at medium and at max, sequential on a quiet machine: medium 7 min / 2.83M tokens / `f05cfb6`, max 28 min / 18.40M tokens / `8abe800` (4.0x clock, 6.5x tokens, 6.9x reasoning). Two blind, sealed, mirrored judges (X/Y swapped; the key sealed before dispatch) both chose MAX from opposite positions — the pre-registered quality signal — on findings that matter: medium's `> 0` scanned-files floor passes a one-file scan, its allow-list keeps join-expression alternatives that pass a named path. H1 falsified as written, H2 in direction, H3's first clause falsified and second holds, H4 holds. Outcome: max for anything touching enforcement, medium elsewhere; n = 1 task shape. Two gaps shared by both trees carried to task 22: the per-line allow-list decision and `app/` listed-not-covered (0 of 32 files scannable). Judge 1's first run is VOID (the worktrees had been moved; it read statically) and is kept as such. |
| `reviews/experiment-medium-vs-max/` | The experiment's artefacts, copied by the architect 23:21–23:56 from Luna-Boss-2's scratchpad and the two worktrees (the worktrees are gitignored for `RESULT.md`, and a scratchpad dies with its session) | `brief.md` (the one brief both received), `sealed-key.md` (the X/Y mapping, written 20:16:15), `diff-judge1-X/Y.diff` and `diff-judge2-X/Y.diff` (the mirrored pairs; byte-identical crosswise by `cmp`), `judge1-brief.md`/`judge2-brief.md`, `JUDGE1.md` and `JUDGE2.md` (verbatim verdicts: 22 and 16 hash-restored mutation arms per tree, TAP counts throughout), `JUDGE1-void-static-trees-absent.md` with `judge1-void.runner.txt` (the void run, kept and labelled), `judge1.runner.txt`/`judge2.runner.txt` (the launcher's clock lines), `RESULT-medium.md`/`RESULT-max.md` (the workers' own reports — hypotheses the judges tested), `tokens.txt` (the rollout token capture after both judges; attribution by dispatch timestamp because the judges' rollouts name both worktrees), `suite-8abe800.log` (WHOLE, 165,470 B: the architect's full suite on the max tip before the merge, 1,735 of 1,735 in 438.1 s, 23:53:09 → 00:00:34 on 2026-09-08/09) with `suite-8abe800.start-and-summary.txt` (the start stamp, the exit, the counts, and the condition: quiet until the 00:00 Pulse fired in the main checkout during the last 34 s — the worktree's own tree, which the suite read, is not what the Pulse writes). NOT copied: the codex transcripts (`f6-exp-medium.log` 775 KB, `f6-exp-max.log` 2.08 MB, the judge logs), which live only in the closed session's scratchpad. |
| `reviews/stage0-packet-B2/` | codex Luna at max throughout (the maintainer's "max for everything", 2026-09-09 00:29), briefed and dispatched by the architect as Luna coordinator through the brief linter; copied at the merge | The implementation review record of packet B2 (tasks 10–12, the cited revision brief): `round1-REVIEW.md` (sealed; revise, three tests-only findings and the brief's file-list defect), `round2-REVIEW.md` (sealed from round 1 by moving its files out of the worktree; revise, six unarmed lines), `round3-REVIEW.md` (delta; revise, four arms narrower than their property), `round6-REVIEW.md` (delta under the preamble's exit condition; approve, one green mutant carried); `round1..6-RESULT.md` (rounds 4 and 5 are STOP reports — the author stopped on an arm red against unmutated code, both times the arm's fault and the architect's brief's); `round1..6-agent-brief.md` and `round1,2,3,6-review-brief.md` (every brief as dispatched, built from the task blob and linted); the launcher runner files per round. Authority `de400f7` for every round. Merged as `0273c82`. |
| `reviews/stage0-packet-E/` | codex Luna at max throughout; briefed and dispatched by the architect through the brief linter and — from round 2, the first packet under it — the second-model brief review (OpenCode DeepSeek at max in the plan agent; Muse Spark at xhigh as the maintainer's experiment); copied at the merge, 2026-09-09 10:06–10:12 | The implementation review record of packet E (tasks 25, 26, 27 and 3b — ledger telemetry, the refusing worktree removal, the `.beads` reuse exclusion): `round1-REVIEW.md` (sealed; revise — three test gaps in the new `ledger.test.mjs` and two closure findings on the brief's file list, not charged to the author; sixteen mutations), `round2-REVIEW.md` (delta under the preamble's exit condition; approve, two green mutants of residual coverage carried); `round1-RESULT.md` (twelve mutation rows; the suite 1,780 of 1,782 at `db10eac`, the two reds the closure pins outside the file list) and `round2-RESULT.md` (1,784 of 1,784 at `ce59b3a`); `round1-agent-brief.md`, `round2-agent-brief-draft1.md` and `round2-agent-brief.md` (the revision brief as first drafted and as dispatched — the draft's read-back recipe named two dead addresses), `round1-review-brief.md`; the launcher runner files per round; and the first second-model brief reviews: `round2-brief-review-1..4.md` (DeepSeek on the revision brief: REVISE, DISPATCH, REVISE, DISPATCH — each REVISE a real defect in the read-back recipe, the second measured on the real repository), `round2-brief-review-muse-draft1.md` and `round2-brief-review-muse.md` (the Muse experiment on the same two texts: REVISE finding three of DeepSeek's four; DISPATCH in 2.5 minutes), `round2-review-brief-review-muse.md` and `round2-review-brief-review-deepseek.md` (both reviewers on the delta-review brief; DeepSeek's first session aborted server-side and is not banked — the retry is). Authority `9c1d980` for both rounds. Merged as `c3aa5c7`. |
| `reviews/stage0-packet-D/` | codex Luna at max throughout; briefed and dispatched by the architect through the brief linter and the second-model brief review (DeepSeek at max until the maintainer's 12:07 switch, Muse Spark at xhigh from then); the brief drafts and their reviews copied 2026-09-09 08:16–09:22 BEFORE the dispatch, the rounds and reviews at the merge, 13:27–13:30 | The implementation review record of packet D (tasks 23–24, escalation into the repository): `round1-REVIEW.md` (sealed; revise — two changed lines whose mutation stayed green, the three early returns' `topRanked: null` and the `escalates_to` non-empty-string guard; no production defect; the six author mutations reproduced, its own floor mutation red), `round2-REVIEW.md` (delta under the preamble's exit condition; the four named mutations and three spot-checks red; "revise" on report completeness only — six closing/declaration lines missing from the sweep — closed by the architect's dated addendum at the foot of `round2-RESULT.md`, no third round); `round1-RESULT.md` (six mutation rows; the suite 1,790 of 1,790 at `8f5e14f`) and `round2-RESULT.md` (seven arms, four named mutations red; 1,797 of 1,797 at `46528c5`; the architect's addendum appended); `round1-agent-brief-draft1.md`, `round1-agent-brief-draft2.md` (built at `da1ace0` and `2940298`) and `round1-agent-brief.md` (dispatched, authority `5414899`); `round2-agent-brief.md` (the class-scoped revision brief); `round1-review-brief.md` and `round2-review-brief.md`; the launcher runner files per round; the brief reviews: `round1-brief-review-1.md` and `-2.md` (DeepSeek at max, the two REVISEs folded into tasks 23–24 as `2940298` — the reviewer-cleared fixture entry, arms (a)/(b)/(d) may run the dry-run call, `topRanked` null on the early returns — and `9174d1f` — arm (b) spends `new_writing` past its ceiling on both tiers, the named mutation keyed at the `run.mjs` call site, Orch's handover rewrites `runners.yml:219-235`), `round1-brief-review-0-recovered.md` and `-0b-aborted.md` (the attach-drop and server-abort no-verdict shapes behind `addictedtoai-hbm4`), `round1-brief-review-muse.md` (DISPATCH), `round1-brief-review-5-aborted-400.md` (a provider HTTP 400 mid-run) and `round1-brief-review-6.md` (the retry, DISPATCH), `round1-review-brief-review-muse.md` (the sealed-review brief; DeepSeek's run on the same text died server-side and is not banked), `round2-brief-review-muse.md` and `round2-review-brief-review-muse.md` (Muse alone, per the 12:07 decision; both DISPATCH). Authority `5414899` for both rounds. Merged as `dc54da0`. |
| `gate-run-2026-09-09-f74f606/` | A2AI-Orch's `orch-gates-only.sh`, 10:19:55 to 10:27:25 local on `f74f606` (the frozen tip: packet E's merge `c3aa5c7`, the architect's record `5e71187`, Orch's four `effort:` lines `f74f606`; the per-run directory `gate-runs/20260909-101954/`); copied by the architect at 10:31 | `summary.txt`: the pin (announced and HEAD agree at `f74f606`), six exit codes, all 0 — test 317 s (127 files, 1,784 passed, floor 1,700; the count ratchet ROSE 1,773 → 1,784, a real check), build 42 s, launch 32 s (spawned — the merge had written inputs), design 34 s, surfaces 3 s, analytics 20 s. Test and build logs are tails (`1-test.tail80.log`, `2-build.tail60.log`; the full logs were 170,168 and 97,542 B); launch, design, surfaces and analytics logs are whole. The tree carried the maintainer's `.agents/skills/` deletions throughout, as every run since `0ed3448`. PUSHED by Orch as `f74f606:main` (`9c1d980..f74f606`) after three checks — HEAD still the gated sha, fast-forward, revert guard 43 paths / 43 forward / 0 reverts — with publishing left false; the deploy verified at the surface (`/status.json` commit `f74f606e950d`, built 2026-09-09T16:28:34Z; three surfaces 200). |
| `gate-run-2026-09-09-08ae8b0/` | A2AI-Orch's `orch-gates-only.sh`, 13:36:36 to 13:44 local on `08ae8b0` (the frozen tip: packet D's merge `dc54da0`, the architect's record `5ffeae5`, Orch's registry half `08ae8b0`; the per-run directory `gate-runs/20260909-133634/`); copied by the architect at 13:47 | `summary.txt`: the pin (announced and HEAD agree at `08ae8b0`), six exit codes, all 0 — test 328 s (128 files, 1,797 passed, floor 1,700; the count ratchet ROSE 1,784 → 1,797), build 40 s, launch 31 s (naming its build path), design 34 s, surfaces 4 s, analytics 20 s. Test and build logs are tails (`1-test.tail80.log`, `2-build.tail60.log`; the full logs were 171,154 and 97,541 B); launch, design, surfaces and analytics logs are whole. PUSHED by Orch as `08ae8b0:main` (`f74f606..08ae8b0`) after three checks — HEAD still the gated sha, origin/main an ancestor (fast-forward, no force), revert guard 33 paths / 33 forward / 0 reverts — with publishing re-read FROM THE FILE after the push and still false; the deploy verified at the surface (`/status.json` commit `08ae8b001b6e`, built 2026-09-09T19:45:12Z; three surfaces 200). The Vercel build landed 60–90 s after the push on all three of the day's pushes (Orch), which is an expectation rather than a guarantee: a surface still showing the old commit after ~3 minutes is wrong rather than slow. |
| `scripts/sha-sweep.mjs` | A2AI-mem-cond, 2026-09-08; placed here by the architect because a session scratchpad dies | Read-only; takes any file path; lists every commit sha the document cites and whether it is REACHABLE from a ref. `git cat-file -t` passing is NOT the check: an amended commit still resolves as a dangling object, and its orphan carries the PREVIOUS message, so a reader following the citation gets a confident, specific and false description of the commit until git collects it — strictly worse than a dead link. Found on the pre-amend object `b34f58…` (statement 21's amendment; the reachable, byte-identical object is `08b627f`; the orphan is written here in a form nothing resolves, so this sweep stays clean); the sweep of design.md's ten candidates found that one. Run it over proposal.md, design.md, tasks.md and this file before archiving. |
| `scripts/orch-gate-distribution.mjs` | A2AI-Orch, 2026-09-08 11:17; placed here by the architect | The script behind `gate-timings-surfaces-5runs.txt`: five serial runs of `verify-surfaces` against the existing `out/`, with the exact cwd and argument, no build and no lock; prints min / median / mean / max, the spread, and the 25% floor as a fraction of the observed minimum. Extend RUNS or the gate list only when the machine-wide lock is free. |
| `gate-timings-surfaces-5runs.txt` | A2AI-Orch, 2026-09-08 about 11:20 local, against the 08:12 export, no lock taken; relayed by message and recorded by the architect | The first multi-sample gate timing: `verify-surfaces` five runs, min 3.54 s / median 4.35 s / max 5.87 s, spread 1.66x. Shows the 25%-of-one-run floor rule has real margin (0.93 s is 26% of the observed minimum), that the recorded single sample was lucky (1.05x the minimum), and that the six-gate percentages are single-sample arithmetic: ranking safe, percentages approximate. |
| `gate-timings.txt`, `gate-timings-npm.txt` | A2AI-Orch, superseded by `gate-timings-final.txt` | The six-gate **push-bar** table: 442.5s total, `npm test` 314.8s (70.7%), `verify-launch` 39.6s of which its own build is 39s; 1,709/1,709 tests in 298,091 ms across 122 files. A job runs four of the six. |
| `rerun-efficiency.txt` | A2AI-Fable-Arch re-running `scripts/orch-efficiency.mjs` | Reproduction of Orch's ledger figures (242 jobs, model-minutes by role, the 1.45× gap ratio as an upper bound). |
| `rerun-beads-utc-superseded.txt` | A2AI-Fable-Arch re-running Orch's original UTC-bucketed script | Kept only to show what the UTC bucketing produced. **Superseded** by the local-day script. |

## Reviews

| File | Reviewer | What it found |
|---|---|---|
| `reviews/round1-sealed-opus-high.md` | Sealed Opus, high effort, 2026-09-08 | 1 blocker, 12 majors, 8 minors, plus §4 (the plan) and §6 (the strongest alternative per decision). Verdict: revise before implementation — and the reserved property survives every path walked. |
| `reviews/round1-sealed-luna-max.md` | Sealed codex `gpt-5.6-luna` at maximum effort, run by A2AI-Luna-Boss-2, 2026-09-08 | 2 blockers, 8 majors, 1 minor; the seven specified hunts; every path bytes take to the live site; five spot-checks; an independent requirement-to-task coverage script. Verdict: not ready. Four findings no other review raised. |
| `reviews/round2-sealed-luna-max.md` | Sealed codex `gpt-5.6-luna` at maximum effort, round 2, 2026-09-08 | 1 blocker, 8 majors; the publication contract found internally contradictory; the Pulse-to-train handoff, the train bound taken before the bytes it bounds are final, and the ordered-access seal. **Its seal broke by its own report** — it read the revision records early while checking a line count, and says so in its first paragraph — so it is not a strictly independent second reading. That is itself evidence for its own finding that an ordered-access seal is an instruction rather than a mechanism. |
| `reviews/round2-opus-closure.md` | Sealed Opus, closure check on rounds 1 and 2, 2026-09-08 | Which of F1–F21, §4 and §6 actually closed: **20 of 21**, with F21 open because its fix landed in a task and not in the requirement. Seven new text-level defects the revision itself introduced, three of them consequences of round-1 fixes. Carries the findings-per-kilobyte arithmetic and the reviewer's two cautions about reading it. |

Both reviewers ran `openspec validate --strict` and
`node scripts/check-spec-deltas.mjs --strict` themselves. `design.md`'s
**Revision record** lists every finding id and the disposition applied, including
the two not applied and why.

## Scripts

Reference copies, run from a session scratchpad. They read `data/ledger.jsonl`,
`bd list --json` output, or `~/.codex/sessions`, and **write nothing**.

| Script | Produced by | Invocation | Supports |
|---|---|---|---|
| `scripts/orch-efficiency.mjs` | A2AI-Orch | `node <script>` — reads `D:/AddictedtoAI/data/ledger.jsonl` by absolute path | Jobs/day by outcome, model-minutes by phase role, invocations per job, the gap/mm ratio. Its header states what the ledger cannot answer. |
| `scripts/orch-runner-quality.mjs` | A2AI-Orch | `node <script>` — reads the ledger by absolute path | First-pass revise rate by author runner and by type; the D7 agreement table. |
| `scripts/runner-workflow-cost.mjs` | Sonnet measurement agent | `node <script>` — reads the ledger by absolute path | Workflow-adjusted minutes per merged job per runner × type; the D7 pricing table. Reuses `orch-runner-quality.mjs`'s author-runner field so `n` agrees. |
| `scripts/orch-beads-flow-local.mjs` | A2AI-Orch | `node <script>` — **session-scoped**: spawns `bd list --all --json` through a hard-coded absolute path to `bd.js` under the user profile | Beads filed/closed per LOCAL day (313/212; last 7 days 99/69 = 1.43). Its header explains why the UTC bucketing it replaces was wrong. |
| `scripts/orch-cohort-0831.mjs` | A2AI-Orch | `node <script>` — **session-scoped**, same `bd.js` path | Re-triage status of the 08-31 cohort. |
| `scripts/orch-create-shaped.mjs` | A2AI-Orch | `node <script>` — **session-scoped**, same `bd.js` path | The triage that found no create-shaped site work left for the fleet. |
| `scripts/stage0-baseline.mjs` | A2AI-Orch | `node <script>` — reads `data/ledger.jsonl` and `git log` by absolute path | The Stage-0 baseline on both definitions, anchored per job on its ledger line's `ts` and the parent of its records commit. |
| `scripts/orch-backlog-classify.mjs` | A2AI-Orch | `node <script>` — **session-scoped**, same `bd.js` path | The backlog classification quoted in `proposal.md`: of 101 open at 2026-09-08, 52 name a code path only, 18 a content path only, 15 both, 16 neither; review-gate floor 33. Its header states what it deliberately does not do — it reports the overlap between two keyword classifications honestly rather than inventing a better classifier, so the argument rests on a number that is defined. |
| `scripts/machinery-share.mjs` | A2AI-Luna-Boss-2 | `node <script> <beads-export.json>` — takes a `bd list --json` dump as its one argument | Path-based classification of beads filed since 09-01. |
| `scripts/open-by-day.mjs` | A2AI-Luna-Boss-2 | `node <script> <beads-export.json>` | Open beads by creation day (the barbell). |
| `scripts/codex-spend.mjs` | A2AI-Luna-Boss-2 (method also on `addictedtoai-f4vy`) | `node <script> <sessions-dir>` — **session-scoped**: point it at `~/.codex/sessions` | The original codex-lane script. Its reading of 182.4M / 685K was a **running total** — three sessions were still live — and is **superseded** by `codex-spend-2026-09-08.json`, the captured fixture. |
| `scripts/fleet-worker-retired.ps1` | A2AI-luna-boss (a session that **ended** 2026-09-08 06:36), copied by A2AI-Luna-Boss-2 | **Not to be run.** Kept as the record of what D6 retires. | The hand-driven fleet's worker launcher. It names a model, a harness and an absolute path, which is why it could never enter `loop/` — see `design.md` D3, constraint 3. The fleet ran from it all day after that session had ended. |

**Session-scoped** means the script hard-codes a path under the user profile —
the `bd.js` entry point or the codex sessions directory — so it will not run
unchanged on another machine. Recorded rather than fixed: these are the scripts as
run, and editing them would break the correspondence with the outputs above.

## Provenance, and why this directory exists

A sealed reviewer's strongest procedural finding was that three load-bearing
measurements — the local-day beads flow, the gate table and the codex token
figures — were unreachable from the reports as cited. Every one of them is now
here, with the script that produced it. The backlog classification, the last
figure still resting on attribution at the end of round 1, is reproducible as of
round 2: `scripts/orch-backlog-classify.mjs` is in the table above.

Not copied: `test-raw.txt` (160 KB of raw test-reporter output; its distribution
figures are in `design.md` D2) and the UTC-bucketed `orch-beads-flow.mjs`.
