# let-the-site-see-its-own-gaps

Make the learn surface's declared coverage a measured, two-directional
invariant: the build refuses a page the map does not declare, and the queue
offers an `education` job for a page the map declares and the corpus lacks
