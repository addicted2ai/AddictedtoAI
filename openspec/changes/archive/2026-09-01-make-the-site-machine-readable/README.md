# make-the-site-machine-readable

Publish the site as structured data, announce changed URLs over IndexNow, make the crawler stance a recorded decision, and turn catalog.json into a versioned contract
