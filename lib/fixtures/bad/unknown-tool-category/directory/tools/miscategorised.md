---
# `category` is a CLOSED list (lib/schema.mjs, specs/directory). An open field
# drifts into coding / code / Coding and the grouping stops being a partition,
# so a value outside the list fails the build naming the file and the value —
# exactly as an unknown `kind` does.
title: "Miscategorised"
url: "https://example.org/miscategorised"
pricing: "free"
last_verified: "2026-08-26"
entry: tool/miscategorised
category: seo
mentions: []
---

A listing whose category is not one the directory groups by.
