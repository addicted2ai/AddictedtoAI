---
# `domains` is a CLOSED vocabulary (lib/domains.mjs, specs/blog). `text` is not
# a value it forgot: keeper ruling K38 REMOVED it, because every row in the
# catalog takes text in, out or both, so a facet value carried by every member
# of the set it divides discriminates nothing. "General" is the UNMARKED
# default — a post declares no domain at all — so there is no spelling of
# "general" here either.
title: "A flagged post tagged with a value the vocabulary removed"
date: "2026-08-20"
frontier: true
frontier_reason: F2
domains:
  - text
mentions: []
---

A post whose domain is not one the frontier section can group by.
