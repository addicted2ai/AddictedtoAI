---
# `legal` is not a domain (specs/wiki, "A domain says what a thing is for"):
# the vocabulary is closed and lives in `lib/domains.mjs`. The build must name
# the FILE, the FIELD, the offending VALUE and the values that are allowed —
# the same treatment an unknown `kind` gets, because an open field drifts into
# `coding` / `code` / `Coding` and the grouping stops being a partition.
id: model/mistagged
kind: model
display_name: "Mistagged"
status: active
maintenance: stable
aliases:
  - name: "Mistagged"
    class: manual
facts: []
timeline: []
mentions: []
domains:
  - legal
---
