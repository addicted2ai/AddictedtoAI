---
# `domains` is optional FLAGGED OR NOT: a post can say where it lands without
# claiming that it moved anything. The flag and the facet are two different
# statements and the schema keeps them independent.
title: "An ordinary post that still says where it lands"
date: "2026-08-23"
domains:
  - robotics
mentions: []
---

An unflagged post carrying a domain, which is neither a frontier record nor a
schema error.
