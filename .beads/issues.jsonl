{"_type":"issue","id":"addictedtoai-8dl","title":"Google Analytics needs a GA4 measurement ID from the owner","description":"Analytics is a hard requirement and the owner's framing is that it is the loop's only missing measurement input, not a tracking chore. But .env.local on this machine holds only VERCEL_OIDC_TOKEN and NEXT_PUBLIC_REPO_URL — there is no GA4 measurement ID anywhere locally, which is likely part of why analytics never worked on the previous site.\n\nThe ID may exist in the Vercel project environment. Retrieving it needs an authenticated 'vercel env pull', which was deliberately not attempted while the owner was away because an auth prompt would stall the run, and because it writes secrets to disk.\n\nWhat is implemented without it: the analytics component, env-var wiring, and a verification path that proves events are actually dispatched rather than merely that a script tag rendered — the precise failure mode of the previous site.\n\nWhat needs the owner: supply the GA4 measurement ID (or confirm a new property should be created), then run the end-to-end verification that events arrive in the GA property. Until then analytics is wired but unproven against a live property.","notes":"RESOLVED — the owner supplied the measurement ID G-KJPHKYL3XL, now set as NEXT_PUBLIC_GA_MEASUREMENT_ID in .env.local (gitignored, and the ID is public by design since it ships in page source).\n\nDIAGNOSIS CHANGED, and this matters more than the ID. The owner confirms the same ID was ALREADY set in the Vercel environment on the previous site, which received no events for months. So the failure was never missing configuration — it was in the code or the response headers.\n\nTwo leading hypotheses the implementation must actively rule out:\n\n1. Content-Security-Policy never allowlisted googletagmanager.com / google-analytics.com. This fits the symptom exactly: the script tag renders correctly in the HTML, so every 'is the tag present' check passes, while the browser silently refuses to execute it. Zero events, no visible error, indefinitely.\n\n2. App Router soft navigation. A page_view fires on initial load and never again as a visitor moves between routes, so real traffic looks like a trickle of single-page sessions.\n\nREQUIREMENT THIS CONFIRMS: verification must prove a hit is actually dispatched and accepted — a 2xx from the collect endpoint observed in a real page load — not that a component rendered or an env var is set. The previous site passed every presence check it had. This is the concrete instance of the project's recurring defect class: a claim written from what a mechanism was meant to do rather than a measurement of what it does.","status":"open","priority":1,"issue_type":"task","owner":"223016611+addicted2ai@users.noreply.github.com","created_at":"2026-08-28T16:28:02Z","created_by":"Andrew","updated_at":"2026-08-28T16:33:28Z","dependency_count":0,"dependent_count":0,"comment_count":0}
