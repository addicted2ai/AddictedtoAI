# TIMELINE CHUNK NOTE 08 of 08

## 1. SPAN

First entry: `## [A2AI-Orch] ASSISTANT 2026-09-09T22:03:48.438Z`. Last entry: `## [A2AI-Orch] ASSISTANT 2026-09-10T03:37:25.953Z`. 436 entries, ~5.5 hours, all of it local-evening Sep 9 (16:03–21:37 Mountain). This is the last chunk; no later chunk exists to settle anything left open here.

Every entry in this chunk is from one of two sessions — `A2AI-Orch` or `A2AI-Fable-Arch` — plus seven maintainer (`USER`) turns. The Luna Boss sessions have zero entries in this chunk.

What was worked on, in order: packet C1/C2 enumeration reviews and the enum-linter build; packet F sealed review → round-2 revision brief → round-2 implementation → full sealed review → merge (`f879ec9`) → registry commit (`28087f5`) → three gate runs → push (`08ae8b0..53779c3`) → live; audits of all six merged packets (18 findings); the spec-delta audit (45 findings) and a three-round OpenRouter implementation fixing them (`7629416`, `42a67b8`, `fd5d112`, `53d510e`) → merge (`7f50b95`) → gate → push → live (`1b34667` follow-up with design.md blockers); linter hardening throughout; transcript exports at the end. Two external interrupts: a Windows reboot at 17:59:47 local and a session restart ~00:42Z.

## 2. CLAIMS OPENED

(Every factual claim with a number, path, sha or count, with session + timestamp. Settlements inside this chunk are in §3; anything not there is OPEN at end of record.)

- A2AI-Orch 22:03:48 — four beads its pattern called vague name real subjects in titles (three its own); instruments agree 10-of-112 vs 10-of-111, one status change between exports.
- A2AI-Fable-Arch 22:06:08 — subject-path rule settled: known root + segment, or bare filename resolving to exactly one tracked file. Table: runners.yml 1 accept; review-mechanics.md 1 accept; design.md 17 refuse; spec.md 76 refuse; RESULT.md 0 refuse. Walk-vs-tracked disagree on 63 basenames.
- A2AI-Orch 22:09:12 — fourteen entry constructions lack a subject, one under a test titled "with subject optional"; `ledger.test.mjs:135` asserts carried==2 from `mock-executor.mjs:273-278` (both `subject: site-note.md`). `data/carried/README.md:9` carries two errors.
- A2AI-Fable-Arch 22:18:07 — 14 review findings on two drafts; 4 would produce a bad build, 10 accuracy; enum-linter "would have caught nine of the fourteen."
- A2AI-Fable-Arch 22:22:53 — committed enumerations A–F: every cited line range resolves; packets D/E/F full-process, A-early/B2 audit-worthy; mutation table adopted partway through A (first four rounds predate it); second-model brief review began that morning (A, B2 predate it).
- A2AI-Fable-Arch 22:31:57 — F sealed review: 5 findings (4 self-referential expectations + 1 enforcement regression); regression "LIVE": exactly one line slips through new pattern in 302 files, on a line the packet touched; allow-list deletion removed the written reason.
- A2AI-Fable-Arch 22:33:09 — audit A: COMMIT, 4 low (24-vs-15 module count; one file listed writing derived data that doesn't; stale dirty-tree count; drifted citations, two load-bearing exact). Audit B2: 3 findings (task-10 file-list omission; mutation outcome "both refusals go green"; shorthand path).
- A2AI-Fable-Arch 22:49:09 — "wrong three ways": wrong task (22 vs 21(vi)(b)); "live regression" withdrawn (CASE A=0; zero terminal-form cases in 302 files; single hit bare prefix doc comment); prescribed remedy impossible (allow-list regex tests backtick, span never contains one). Corrected to `/` → `/?`, violation set identical across 302 files.
- A2AI-Orch 22:44:37 — independent verification: every `openspec/changes/…` ref in terminal-slash form (8×archive/ + live-one/ + ${name}/ + ${e.name}/), zero without; `specs.mjs:43` bare prefix in doc comment. Self-correction: "tightening" inferred from diffstat was wrong — "a line count isn't a diff."
- A2AI-Orch 22:49:37 — 7-day usage 84%, reset Sunday 16:00; 5-hour 5%.
- A2AI-Fable-Arch 23:08:13 — full audit: A COMMIT 4 low; B1 REVISE 4 (2 high); B2 REVISE 3; D COMMIT 2 non-blocking; E REVISE 5 all blocking; G REVISE 2 (1 high). Zero code defects. E tasks 19 ("Second half, open" but ticked), 20 (ticked; no named commit implements it; absent at HEAD). B1 ceiling-88,000 mutation stays green on first-tried fixture (23,035 chars vs 24,000 ceiling).
- A2AI-Orch 23:06:46 — `conformance:` field `unverified` on 7 of 9 runners.yml entries incl. 4-of-4-FAIL `opencode-openrouter-muse-spark`; nothing in `loop/`, no test reads the field → delete, don't sync.
- A2AI-Fable-Arch 23:19:30 — F round-2 report says 56 tests; actual 55 (20+16+15+4). Same class as G task-27 inverted counts ("10 of 11 red" vs banked 1 of 10).
- A2AI-Fable-Arch 23:44:49 — sealed review of F round 2: exit condition holds on all four clauses; reviewer found 56/55 unprompted with matching per-file numbers; generic-swap green-by-design + count-field red; three unnamed changed lines all red; no green mutant; closed with addendum, no round 3.
- A2AI-Orch 23:45:33 — diff read MERGE; `enabled:false` early return precedes conformance gate, owes no count; consequence: D's escalation unreachable in production (`-medium` sole `escalates_to`, disabled return fires first, `topRanked: null`); conformanceGate partial-record streak-reset note (non-blocking).
- A2AI-Fable-Arch 23:47:36ff — merged `f879ec9` (+640/−55, nine files); task 22 tick deliberately withheld until registry commit lands.
- A2AI-Fable-Arch 23:54:26 — OpenRouter: xhigh works (5s); max refused (set tops at xhigh); `opencode run` exits 0 on refusal after 75s with error matching neither registry pattern; entry declares `effort: high`; comparison review launched (same doc/prompt, provider varies; go route found 4 incl. 2 major).
- A2AI-Orch 23:54:39 — severity turns on stream: stderr→streak arms (self-limiting, cause missing); stdout→burns invocation per selection indefinitely.
- A2AI-Fable-Arch 23:57:03 — measured: failing run stdout empty / stderr header+error; succeeding run stdout `STREAM PROBE`; header on stderr (claimed load-bearing; disputed §3).
- A2AI-Fable-Arch 00:45:33 — server 4096 dead after reboot; `Start-Process` fails on `opencode` shim → via cmd.exe; dead-server error is `Error: Session not found`, matching neither pattern (worse than variant finding); stdout 0 bytes so streak arms. Spec reviews: 7 docs REVISE, 45 findings (loop ADDED 7/8; MODIFIED 0*/8 with format anomaly; REMOVED 4; pulse 7; review 11).
- A2AI-Fable-Arch 00:49:09 — pids postdate reboot (server 18:44:34, relaunches 18:44:46/18:45:20); no doubling; spec asymmetry thesis (tasks.md exercised every round; deltas consumed by nobody until archive); four shapes incl. `lane` two senses, table-30-vs-scenario-10, DIRECTIVES.md live dependents.
- A2AI-Fable-Arch 00:47:01 — one spec finding stale: packet F shipped `disabled`; spec never names field.
- A2AI-Orch 00:47:56 — reboot 17:59:47 killed gate run 1 (not session end as recorded); run 2 alive since 18:46:03.
- A2AI-Orch 00:49:05 — retraction: read pids as timestamps; both reviewers post-reboot relaunches.
- A2AI-Fable-Arch 00:51:17 — 2 of 45 already resolved by packet F (`run.mjs:979-982` loaded-count log; `select.mjs` count on four post-gate returns).
- A2AI-Fable-Arch 00:55:23 — OpenRouter reproduced both blocking findings via different evidence paths; 2-vs-4 gap is triage (downgrades in Checked-and-sound); cost 1.38× tokens, 3.5× wall.
- A2AI-Fable-Arch 01:04:06 — 26 ticked (not three); `3b` row `tasks.md:443` real+ticked → 96 rows not 95 (own quantifier rule failing on own document).
- A2AI-Fable-Arch 01:05:24 — `tasks.md:140` "at medium" vs `:147` max, seven lines apart (live dispatch contradiction).
- A2AI-Orch 01:07:29 — gates at 53779c3: 1-test EXIT 0 318s 1817 passed (ratchet 1797→1817); 2-build EXIT 1 601s lock refusal; 3/4/5/6 green.
- A2AI-Orch 01:09:50 — lock holder pid 18064 recycled into Google Drive WebView; lock 47→57 min old; `build-lock.mjs:116` predicted it; re-running all six (foreign build 18:04–19:01 overlapped); launch.json +0.3KB drift reverted; mem-cond pin `FULL-MEM-LOG.md:3275` (`run.mjs:1661` = rederive call, build at :1750-1755) spread to five artifacts.
- A2AI-Fable-Arch 01:11:38 — brief-review DISPATCH finding verified: 14 lines, 15 matches, line 2012 carries two (own verify step unreachable).
- A2AI-Orch 01:15:45 — run 2 all six green (build 26s, 682 pages; lock self-reclaimed from pid 18576).
- A2AI-Orch 01:17:05/01:17:46 — FULL-MEM-LOG.md written 19:09:52 inside 19:08:30–19:15:23 window; run-2 greens discarded (briefing error: "queue it, don't commit" permits writing); pre-push checks passed (HEAD==sha; origin 08ae8b0 ancestor, 12-commit FF; 39 paths, 0 reverts); drift did not reproduce → noise, no commit.
- A2AI-Fable-Arch 01:20:17 — dispatch script ran through hold: worktree created + implementation launched; no node_modules/junction → cannot build/test, coupling nil.
- A2AI-Orch 01:22:58 — guard sampled process table 19:16:51 in gap between sequential gate stages ("third instance today"); remedy: run-scoped lock file in build-lock shape; run 3 started 19:17:40, after worktree 19:16:51 → unaffected by construction. Ruling: let it run.
- A2AI-Fable-Arch 01:23:20 — re-test overwrote runner log (refusal path opened log without -Append); restored from board.
- A2AI-Orch 01:25:06/01:26:11 — pushed 08ae8b0..53779c3; deploy built_at 01:25:17Z (~50s).
- A2AI-Fable-Arch 01:26:33 — camelCase check ran on lowercased text → strong branch unreachable (why false claim slipped).
- A2AI-Orch 01:27:01 — v8q8 stays open: criterion 1 needs Desk-run ledger line; board wrong about it for two hours ("second near-miss of that shape today").
- A2AI-Fable-Arch 01:27:43 — `:404` actually `:419`; "exactly two gate call sites" true, numbers drifted; five sites (proposal.md has two).
- A2AI-Fable-Arch 01:29:47 — counter reproduces 38/67 vs claimed 29/49 under stated rule.
- A2AI-Fable-Arch 01:32:05 — OpenRouter verdict: comparison corroboration-not-agreement; Reason-paragraph downgrade rejected (vacuous: zero archived deltas ever carried one); R1 11m49s caught brief's own lane contradiction (reserve-word sentence + unreachable pinned count); 11 targets/10 defective; task-32 pin = rederive call (worker would delete it), task-56 pin = proposal code as constitution; stale pin in five files; guard failure + -Append defect; linter fixes (substring `lane`/explanation; camelCase) proven by fixture.
- A2AI-Fable-Arch 01:32:43 — bare basenames (`specs.mjs:317-330`) invisible to all checks; exemption meant for prose shorthand, but shorthand carries no line range.
- A2AI-Fable-Arch 01:33:44 — `truncated` at :318/:408/:444/:501, not :335 (true positive).
- A2AI-Fable-Arch 01:35:58 — carry recount: 223 entries in 144 records (87/37/18/2), re-totals exactly; old breakdown 86+37+17+2=142≠143 internally inconsistent.
- A2AI-Fable-Arch 01:36:36 — "wrote two figures I hadn't computed"; measured: 22 of 223 (9.9%) cap-2-suppressed; 15 (6.7%) no-subject ("the 15 was right all along, only its denominator moved").
- A2AI-Fable-Arch 01:36:59 — sealed review R1: REVISE 5 blocking.
- A2AI-Fable-Arch 01:40:16 — F2: no sanctioned scenario rename in MODIFIED (author's experiment: delta-only rename refused); third option (keep heading, scope scenario to at-threshold runner); two more streak-vs-window contradictions the review missed.
- A2AI-Fable-Arch 01:44:19 — corrections-doc review: doc reads as work list but is a log; 12b4ea3 already applied most; verbatim application would re-edit landed pins and unresolve two tasks; one entry describes shipped code backwards.
- A2AI-Fable-Arch 01:46:41 — RENAMED applies to requirement headers only; validate mirrors what archive refuses; F2 ruling evidence-backed.
- A2AI-Fable-Arch 01:48:38 — `openspec validate` has no path operand; "run it against your worktree" + cd ban = unexecutable; likely outcome validates main tree, reports green ("a green meaning something other than what it says is worse than a red"). R1/R2 survive only via {cwd}-spawn re-runs.
- A2AI-Fable-Arch 01:49:23 — review programme complete: 15 targets, 14 REVISE/DEFECTIVE; design.md revision records excluded (history, nobody builds from it).
- A2AI-Fable-Arch 02:07:44 — worker corrected F5 ruling: breaker 1 counts per governing job type (:1462); "that runner in that role" is runner-health window's unit (:2118, 650 lines away). Silent compliance would change whose failures are counted in permanent law.
- A2AI-Fable-Arch 02:08:56 — sweep under-enumerated (4 listed, 10 actual); `:305` genuine miss ("three consecutive discards trip breaker 1" states superseded model while naming its replacement); structural cause: sweep over requirements, not word occurrences.
- A2AI-Fable-Arch 02:12:03 — F2 ruling arithmetically wrong: sliding window evicts oldest slot; "exactly three empty" insufficient.
- A2AI-Fable-Arch 02:12:37 — global ruling underspecified (no key/period/tier reconciliation); conventions: `budget.bounds.<name>_pct` integers, `budget.window_days`=30; own premise false (ledger-no-tiers; live law :275 computes per-tier shares).
- A2AI-Fable-Arch 02:14:59 — round 3 dispatched; briefs' validate instruction defective in every brief.
- A2AI-Fable-Arch 02:37:12 — openspec docs read (installed 1.11.0 vs Context7 v1.5.0): five roots (store|declared|global_default|nearest|implicit); `--store <id>` exists; `--store-path` deliberately rejected; `--strict` = warnings fail (SHALL/MUST guidance→failure); delta checks enumerated; scenario-loss conditional on mainSpecsDir, CLI passes it; worktree runs compare against worktree specs (same content at 53779c3).
- A2AI-Fable-Arch 02:40:19 — round-3 brief overstates ("names no mechanism"); correction lands in record not running job (r3 dispatched 20:18); needed instruction intact via `arch-validate-wt.mjs` path.
- A2AI-Fable-Arch 02:41:01/02:41:19/02:42:12 — process-inference errors: "parent died" wrong (pid 14864 alive); "no opencode process" wrong (shim isn't node/bun with opencode in cmdline); round 3 pid 21756 alive 27 min.
- A2AI-Fable-Arch 02:44:08 — R3 validate green via own {cwd} re-run; 9 consecutive lines at tabulated numbers (own measurement).
- A2AI-Fable-Arch 02:46:48 — linter's 5 sealed-packet refusals all structural (checks 3/4/8 jurisdiction; 5/6 fired on embedded artifact: author's compliance line, spec's "produced no RESULT.md"); fix: packet mode linting architect header only.
- A2AI-Fable-Arch 02:48:54 — packet mode: 5 of 6 refusals noise; one genuine: own header's bare "once" (spec disambiguates, header didn't).
- A2AI-Fable-Arch 02:49:45 — R3 sealed DISPATCH, no blocking; reviewer re-derived 9 consecutives, F4 eviction arithmetic, evicted-at-train, config no-back_desk key, 9 success survivors none in :1464-1560; two non-blocking header findings (:2441 line-break 2-not-3; proximity citation).
- A2AI-Fable-Arch 02:54:15 — `dyw` is beads id `addictedtoai-dyw` test prefix, confirmed verbatim in test log ("fixing it would have been a wrong correction arriving with the authority of a fix").
- A2AI-Orch 02:54:36 — gate covers four commits (12b4ea3, c564ae0, 0de5751, 7629416); validate-has-no-path = "fourth instance today," won't gate on bare validate; waiting on design.md land-or-park.
- A2AI-Fable-Arch 02:58:50 — dispatched residual review with brief-review prompt (no -PromptFile): technical correctness explicitly out of scope; DISPATCH says nothing about rulings. (First occurrence; repeats §3.)
- A2AI-Fable-Arch 02:59:39 — `tightestCeilingPct` matches `k.endsWith('_ceiling_pct')` over all of budget.bounds (deliberate per doc comment); back_desk key under bounds would silently widen new-writing+machinery warm-up; committed round-3 key name walks into it; fix structural (placement), not admonitory.
- A2AI-Orch 03:03:16 — sized: bounds {floor 40 (skipped), new-writing 45, machinery 30}; tightest 30, warmUpJobs 3.33; back-desk 10 → 10, threefold; invisible in config.json review.
- A2AI-Fable-Arch 03:04:00 — no 10% back-desk value anywhere in delta (value "taken from measured drain"); Orch's 10 = machinery scenario's old wrong figure (round-2 contradiction); framing must be conditional.
- A2AI-Fable-Arch 03:04:44 — second residual review also wrong prompt (line 33); incidentally delivered hazard-path precision (denominator widening via tierShares→warmUpMm, not direct refusal), claim-3 arithmetic re-derived, shipped-test property (100/warmUpJobs>=tightest).
- A2AI-Orch 03:05:53 — "Arch is right about the 10... My conflation, and it would have entered the law wearing my measurement's authority."
- A2AI-Orch 03:07:08 — merged 7f50b95 (no-ff); "second fabricated number today; first was a timestamp I hadn't read a clock for. Both caught by a peer rather than by an instrument." Declined fourth review (boundary: rulings are Arch's lane; gate answers coherence/safety). Hold reworded: "don't write any tracked file."
- A2AI-Fable-Arch 03:08:09 — warm-up residuals were last open triage items blocking archiving; first ruling "told a reader... validated green, committed clean, and constrained nothing"; both verdict-shape-as-scope misreads caught before gating.
- A2AI-Orch 03:13:58 — six green at 7f50b95; manifest question answered by test (no regeneration needed).
- A2AI-Fable-Arch 03:17:22 — §5(d) inheritance real by measurement (:2306/:2466/:2495 inside :2278→:2511, category-bound, :2289-2291 names window_days); deltas clean.
- A2AI-Orch 03:17:47 — specfix pushed live built_at 03:14:42Z; "declined a fourth review... would have let me borrow its judgement while appearing to check it"; still open: C1/C2, publishing false, archiving, queued runners.yml + gate-lock commits.
- A2AI-Orch 03:18:29 — transcript b0a0272b: 82,636 records, 251MB, 2026-08-13→09-10, one sessionId; visible context = compaction summary + handoff; "Twice tonight I produced a confident number that came from fluency rather than measurement."
- A2AI-Fable-Arch 03:18:41 — F4 sharper: lines 208-210 certify "seven packets — every quantifier's domain enumerated in the sentence" yet only A/D/E/F named anywhere, no packet list in sentence; fixed by removing false member.
- A2AI-Fable-Arch 03:18:41 — F3: A ran six review rounds, B1 seven (13, not six); figures must count defective-claim occurrences, not briefs; ambiguity stated, no reconciliation invented.
- A2AI-Fable-Arch 03:19:41 — §25: 11 design.md findings, 3 BLOCKERS; only carry arithmetic + some citations fixed.
- A2AI-Fable-Arch 03:21:23 — F2 worse than recorded: all three cheap Luna rungs enabled:false → design.md:687 names unselectable runner.
- A2AI-Orch 03:21:38 — exports: turns 6,179/4.9MB, human-only 662/457KB; `type:user`≠human-typed (tool results, reminders, cross-session, hooks); --from/--to UTC vs local dates.
- A2AI-Fable-Arch 03:21:42 — one block, three contradictions: "exactly two" Luna entries vs four; -medium disabled yet "starting rung"; high "may be reopened" yet disabled.
- A2AI-Fable-Arch 03:22:27 — all nine conformance records single-entry; xhigh one run FAIL; F4 "2-of-2" unsupported; surfacing own openrouter-muse-spark pass=false.
- A2AI-Fable-Arch 03:22:58 — openrouter-muse-spark 4-of-4 FAIL 2026-09-07 = produced-nothing signature, not incompetence.
- A2AI-Fable-Arch 03:23:18 — F3 good-kind: no --force anywhere in git.mjs; removeWorktree plain; prescribed task is done work.
- A2AI-Orch 03:25:08 — arch session 47a211cc: 20,362 records, 45MB, 09-08→now, one sessionId; human-only leak check zero on four markers; 38,211 non-prose dropped / 7,056 turns kept; coord dir not a repo.
- A2AI-Orch 03:25:39 — token estimates: orch turns 1.21–1.46M; orch human 114–138k; arch turns 112–135k; arch human 12–14k; band 3.3–4.0 chars/token (orch nearer 1.4M, arch 115k).
- A2AI-Fable-Arch 03:25:04 — F1 does NOT reproduce: conformanceGate (`runners.mjs:195`) passesToSupersede=3, same-check-name consecutive PASSes = exactly the prescription; absent→ok; only tense real (overwriting described in present, code appends).
- A2AI-Fable-Arch 03:26:12 — linter flags 22 citations not six; high FP (filename-derived tokens; change-relative paths); triage parked as beyond stopping point, none blocking.
- A2AI-Fable-Arch 03:27:08 — triage blocking archiving done; OpenRouter README disclosure added (4-of-4 FAIL; direct `opencode run` bypassed selector — "scope, not an excuse"); citation hygiene deliberately not started (bulk-apply = wrong-correction-with-authority risk).
- A2AI-Orch 03:27:27/03:28:22 — verified: openrouter 4-of-4 "no RESULT.md at all" (wiring); xhigh trivial-edit + insufficient-information PASS (not 4-of-4, not 2-of-2); "a guardrail living in the selector protects only what the selector chooses"; archiving "not yet due" not "blocked" (Stage 0 unfinished); don't bulk-apply (named undismissed defect cheaper).
- A2AI-Orch 03:30:59 — --thinking flag; recovery Arch 145/1485+145=8.9%, Orch 144/6334+144=2.2%; empty blocks = type + thinking:"" + 1.2–2.4k signature (attestation, not encoding); ~36k chars reasoning per session; script prints both counts; self-correction: endorsed "don't bulk-apply" from Arch's single sampled pin without dumping any of the 22 ("My agreement converted one session's unverified sample into an instruction from the person who owns the close list").
- A2AI-Orch 03:33:08 — Group A six→two; `review.mjs:744` trap: design.md:848 disclaims the pin deliberately; mechanical repair = document going right→wrong with maintenance-looking diff; survivors config.mjs:355→:362, ledger.mjs:173→:202 (traced past requirement: existingIds from scanJobBranches); lesson corrected: "agreement moves the premise and the instruction independently, and only one of them touches the tree."
- A2AI-Orch 03:37:11 — 1b34667 gated/pushed/live built_at 03:35:15Z; bead addictedtoai-0iw3 (P1): D escalation no live path; gap between two green suites (D fixtures enable, F fixtures disable); "a design document cannot fail"; acceptance refuses both easy closes (sentence written; rung re-enabled) — test must be seen red; `review.mjs:744` boarded; archiving deferred-not-blocked; queued: escalation-join arm, runners.yml patterns, gate lock.
- A2AI-Orch 03:37:25 — date filter UTC vs Mountain; making filter accept exact instant. (OPEN: the 9/8-only export itself is not confirmed delivered in-chunk.)

## 3. CLAIMS SETTLED IN THIS CHUNK

- "The regression is LIVE" (Arch 22:30:35) → RETRACTED 22:36:53–22:49:09, triple retraction quoted: "My scan compared old-vs-new match counts per file, which cannot tell two different cases apart"; "Task 22 is `enabled: false`. The governing text is task 21(vi)(b), and it prescribes *in as many words* what the author shipped"; "Had the worker followed my brief and restored the deleted allow-list entry, they'd have turned the suite red." Stood ~19 min (22:30→22:49). Orch independently verified the withdrawal ("a withdrawal is a measurement too") rather than accepting it.
- "F is tightening that test" (Orch, pre-chunk inference from diffstat) → RETRACTED 22:44:37: "It's closer to the opposite — the capture widens while what counts as a violation narrows... a line count isn't a diff."
- Orch's stream-consequence model: Arch's "correction to your model of the streams" (23:57:03) → REJECTED then WITHDRAWN. Orch 23:57:13: "My morning claim already said the header is on stderr — what I got wrong then was the inverted *reading* of the consequence, not the stream." Arch 23:57:35: "Orch is right, and my own board proves it — line 91, 09:11 today"; 23:57:51: "Orch was right and I've withdrawn it." Stood ~54 min. Residue kept: header-on-stderr now doubly measured.
- "My reviewers survived the reboot" (Orch 00:47:56-era message) → RETRACTED 00:49:05: "I read those pids from the process table without checking their start times, in the same call where I *did* read start times for three other pids." Stood ~10 min. Arch's process-table measurement (18:44:34/18:44:46/18:45:20) settled it.
- "56 tests" (worker report) → CORRECTED 23:19:30 by Arch's sum (20+16+15+4=55), then independently confirmed by sealed reviewer 23:43:16 ("matched my count exactly"). Undeclared-check design validated.
- "10% back-desk ceiling" (Orch 03:03:16) → RETRACTED 03:05:53: "I asserted it was the back-desk value and it isn't; it's the *machinery* scenario's old figure... My conflation." Caught by Arch 03:04:00 ("No `10%` back-desk value exists anywhere in the delta"). Stood ~50 min, one message from permanent law. Recorded as conditional with worked illustration.
- Arch's warm-up reader-rule (committed 42a67b8) → SUPERSEDED same night by placement rule (fd5d112): "A rule addressed to a reader cannot bind a filter" (suffix matcher reads no names). First ruling stood ~4 min as committed law; caught by Arch's own code check, not by the (wrong-prompt) review.
- Arch's two uncomputed design.md percentages → CORRECTED within the hour (01:36:19 admission → 01:36:36 measured 22/223, 15/223). Self-caught, "immediately."
- "No targeting mechanism for validate" (Arch briefs, all three rounds) → RETRACTED 02:37:12 after docs read: "`--store <id>` exists... I was wrong to say there's no targeting mechanism." Stood across rounds 1–3 (briefs) until the maintainer-ordered docs read.
- §25 F4 "2-of-2 failures" → REFUTED by data: xhigh one FAIL of four, n=1 (Arch 03:22:27, Orch 03:27:27 independently). F1 supersession-gap → DOES NOT REPRODUCE (landed code matches prescription; Arch 03:25:04). F3 --force → CONFIRMED good-kind (code already correct).
- "Seven review briefs / six reviews" (§23 F3) → DISSOLVED into stated ambiguity: measured six + seven = thirteen sealed reviews; figures must count claim-occurrences; "I'll state the ambiguity rather than invent a reconciliation" (Arch 03:18:41).
- "Seven packets" certification (§23 F4) → CONFIRMED BROKEN by its own test (only A/D/E/F named); fixed by removing false member.
- B2's alarming task-10 omission → DISSOLVED: merge commit includes the file; work correct, text stale. B2 "both refusals go green" → IMPOSSIBLE by control flow (second gate refuses revise regardless of cites).
- Packet-A unlettered-mutation ambiguity → coverage SOUND (both stub directions red different arms); defect is record-lettering only.
- `data/launch.json` +0.3KB drift → NOISE: did not reproduce on clean re-run.
- "First gate run died with session end" (Orch's record) → CORRECTED: killed by 17:59:47 reboot.
- "Reviewers will double" → REFUTED by start-time measurement; coincident pids.
- Orch's `.agents/` "furniture" → CORRECTED to "real but inert" (zero references from scripts/lib/loop/pulse/app); gated deliberately.
- Orch's grep-through-head exit code → WORTHLESS, self-caught ("That exit code was `head`'s, not `grep`'s").
- "Don't bulk-apply 22 citations" premise → WEAKENED by Orch itself 03:30:59 (endorsed from one sampled pin, zero lines dumped; most pins genuinely stale — blank lines, closing paren); Group A cleared; but final pass caught the trap, so instruction's effect (nothing touched) is credited anyway.
- `dyw` corruption suspicion → DISMISSED: beads-id prefix, test-log-verbatim.
- Brief-review DISPATCH greens on residuals → DECLARED VOID as evidence (twice): "a green... never capable of being red about my question."
- Archiving "blocked on triage" → WITHDRAWN ("deltas clean") → REFRAMED by Orch as "deferred, not blocked" (Stage 0 unfinished; decision not yet due).
- v8q8 "will close on push" (Orch board, 2h) → CORRECTED: criterion 1 needs Desk ledger line; Desk stopped.

## 4. MISTAKES

1. **Arch — "live regression" triple error (22:30:35).** Wrong task cited, false-positive scan (old-vs-new counts conflating bare prefix with terminal form), impossible remedy (backtick allow-list). Caught by Muse review finding 1 + Arch's own re-measurement (person + review pipeline). Cost: one wasted revision cycle of the brief, a false alarm carried to Orch (retracted before Orch acted — "never reached me"), and the record of Arch approving a weakening it misread ("I used the consequence to justify the cause").
2. **Orch — diffstat-as-diff (pre-chunk, retracted 22:44:37).** Called F "tightening" without reading the diff. Caught by self on Arch's evidence. Cost: a false line that, uncorrected, would have misdescribed the packet's direction.
3. **Arch — laundering Arch's measurement (Orch 22:09:12, self-reported).** Orch relayed Arch's narrow-window grep as its own reason for closing run.mjs:678. Caught by Orch itself ("cite whose measurement it is, or take it myself"). Cost: the closure link had to be re-derived (ledger.test.mjs:135 + mock-executor).
4. **Arch — hand-transcribed figures + unopened citation (22:18:07, self-reported).** Corpus numbers computed under three rule versions transcribed by hand; reviewer-brief range cited without opening (template has no subject field). Caught by Muse reviews (pipeline). Cost: 14 findings, 4 build-threatening; spawned the enum-linter build.
5. **Arch — premature "correction" of Orch's stream model (23:57:03).** Corrected a claim Orch never made. Caught by Orch's dispute + Arch's own board line 91. Cost: one exchange + a near-miss false line in both records.
6. **Orch — pids read as timestamps (00:47:56).** Warned of doubling reviewers; wrong. Caught by Arch's start-time measurement (person). Cost: one exchange; had Orch been believed without measurement, relaunch would have been withheld leaving zero reviewers.
7. **Orch — "queue it, don't commit" briefing (01:17:46, self-reported).** mem-cond wrote FULL-MEM-LOG.md inside the gate window; full all-green run discarded. Cost: one complete six-gate run (~7 min of gates plus scheduling) and a re-run. Fix: hold wording "don't write any tracked file."
8. **Arch — broke the hold (01:20:17, self-reported: "I made a real mistake").** Tested guard during live gates; guard sampled process-table gap and launched worktree + implementation. Cost: contained (no node_modules; next gate run started 90s later; Orch ruled let-it-run) but real: an uncontrolled actor created during a freeze, plus a clobbered runner log (refusal path missing -Append). Fix: guard refuses unless clearance explicit.
9. **Arch — two uncomputed percentages into design.md (01:36:19, self-reported).** Same hour as cataloguing that exact failure in others. Caught by self "immediately." Cost: committed-then-corrected figures; the 15's denominator moved (223 not 143).
10. **Arch — briefing defect in all three round briefs (02:14:59, self-reported).** "Run validate against your worktree" unexecutable (no path operand + cd ban); likely outcome green-about-main-tree. Caught by queue-3 review (pipeline). Cost: R1/R2 validate claims untrustworthy as reported; both salvaged only by Arch's independent {cwd} re-runs.
11. **Arch — wrong-prompt review dispatched twice (02:59:07, 03:04:44, self-reported).** `-Kind review` without `-PromptFile` → brief-review prompt; technical correctness explicitly out of scope; DISPATCH greens void as evidence. Caught by Arch reading line 33. Cost: two review cycles that could not answer the question; nearly gated upon.
12. **Arch — vacuous warm-up ruling committed (42a67b8).** Reader-addressed SHALL over a suffix filter; validated green, constrained nothing; committed key name walked into the hazard. Caught by Arch's code check (person; the running review contributed only incidentally). Cost: a committed spec sentence that bound nothing, replaced within minutes by fd5d112.
13. **Orch — fabricated "10%" back-desk value (03:03:16, peer-caught).** Correct arithmetic (3.33→10) on invented input. Caught by Arch's delta search (person). Cost: one message from permanent law; Orch's framing kept: "more persuasive than either error alone."
14. **Orch — grep-piped-to-head exit code (02:57:07, self-caught).** Read head's status as grep's. Cost: one worthless check, redone properly.
15. **Orch — v8q8 board error held 2 hours (01:27:01).** Board said push closes it; criterion 1 forbids exactly that reading. Caught by Orch reading the bead. Cost: two hours of wrong close-condition on the board.
16. **Orch — `.agents/` deletions as furniture (02:57:43, peer-surfaced).** Real divergence between gated tree and pushed sha, unexamined. Caught via Arch's remark + Orch's measurement. Cost: none realized; now gated deliberately.
17. **Orch — endorsed "don't bulk-apply" from zero dumped lines (03:30:59, self-reported).** Converted Arch's single-sample generalisation into owner's directive with rationale. Cost: over-broad hold on 22 pins, narrowed later (six→two); final trap-catch arguably depended on the delay.
18. **Merged packet E — ticks ahead of code (audited 23:08:13).** Tasks 19/20 checked done; 19's body says "Second half, open," 20's requirement absent at HEAD. Pre-existing (weeks old), caught by audit pipeline. Cost: committed false record; resolved only when F landed.
19. **Task-32 pin = rederive call; task-56 pin = proposal code (audited, verified 01:02:54).** Workers following pins would delete the post-merge build call / edit wrong file. Stale pin spread to five files incl. memory log. Cost: caught pre-damage; four files fixed in 12b4ea3, fifth routed to mem-cond.
20. **Preamble dispatch contradiction (`tasks.md:140` "at medium" vs `:147` max).** Arch is the governed dispatcher. Caught by preamble review (pipeline). Cost: live contradiction killed in 12b4ea3-class corrections.
21. **Count claims 29/49, 95 rows, "three" ticked.** Counter reproduces 38/67; 96 rows (3b at :443); 26 ticked. Caught by reviews + Arch's counting script (pipeline + person). Cost: committed 12b4ea3 corrections.
22. **OpenRouter `max` silent refusal.** `opencode run` exit 0, no RESULT.md, error matching neither registry pattern — foot-gun one config edit away. Caught by Arch's probe (mechanism). Cost: none realized (entry declares high); pattern fix queued to Orch.
23. **Dead-server `Session not found` matches neither registry pattern.** The exact scenario the startup pattern exists for is the one it misses. Surfaced by reboot incident (mechanism-revealed, person-noticed). Cost: diagnostic loss only (stdout 0 bytes → streak arms); pattern fix queued.
24. **Sealed-packet linter refusals (5 structural).** Brief-linter applied outside jurisdiction: packet embedding a diff fails identically excellent-or-worthless. Caught by Arch reading the checks (person). Cost: packet mode built; one genuine bare-"once" in Arch's own header caught by the new mode.
25. **Linter holes shipped then found:** vacuous label check (passed everything), missing range-content check, bare-basename blindness, substring matching (`lane`⊂`explanation`), camelCase-on-lowercased (unreachable strong branch). Caught by Muse instrument-audits + fixture proofs (pipeline + person). Cost: each hole was live while certifying briefs; bounded by "requires active dodging" stopping rule.
26. **Round-2 sweep under-enumeration (4 vs 10; `:305` miss).** Structural: sweep over requirements misses occurrences in judged-clean units. Caught by Arch's grep reconciliation (person). The sealed review also missed :305. Cost: one real defect carried to round 3.
27. **F2/F5/global rulings wrong (Arch, three in two rounds).** Sliding-window eviction arithmetic; breaker-vs-runner-health unit conflation (worker-caught); underspecified global (no key/period/tier) on false ledger premise. Caught by sealed reviewer + worker + Arch (pipeline + persons). Cost: two extra implementation rounds; corrected plainly in round 3.
28. **Brief's lane contradiction (Arch's own brief).** Reserved-word sentence + unreachable pinned count. Caught by OpenRouter worker, which reported rather than complied (person). Cost: none; cited as the RESULT-disagreement clause earning its place.

## 5. CORRECTIONS THAT WERE THEMSELVES WRONG

Two complete chains in-chunk (the two known chains cross chunk boundaries; these segments are fully visible here), plus the in-chunk leg of the known citation chain:

**Chain 1 — the warm-up ruling (strongest; committed, vacuous, replaced).** Residual hazard (new bound under `budget.bounds` widens unrelated warm-up) → Arch's first correction, a reader-addressed rule ("SHALL NOT read a bound stated on a different axis," committed 42a67b8, validate green) → that correction itself defective: `tightestCeilingPct()` matches on key suffix and reads no names, so the rule "validated green, committed clean, and constrained nothing," and the committed key name walked straight into the hazard → second correction, structural placement rule (fd5d112) preserving auto-extension. Self-correction succeeded (hazard identified) AND shipped a binding-nothing sentence as law in the same motion. The void wasn caught by the running review (wrong prompt, DISPATCH, out-of-scope) — only by checking the ruling against shipped code instead of adjacent prose.

**Chain 2 — design.md carry figures (uncomputed correction of inconsistent text).** Old text internally inconsistent (86+37+17+2=142≠143) → Arch's correction wrote two figures never computed (the catalogued failure, committed in the same hour) → immediate self-catch → counting script → measured 223 entries/144 records (87/37/18/2), 22/223 (9.9%), 15/223 (6.7%). Correction and correction-of-correction within ~20 min; the 15 survived all three states, "right all along, only its denominator moved."

**Chain 3 (in-chunk leg of known chain A) — citation sweep noisy→narrowed→trap.** 22 flagged (noisy: filename-derived tokens, change-relative paths) → Orch's "don't bulk-apply" (right instruction, wrong premise — endorsed from one sampled pin, zero lines dumped) → Group A six→two (config.mjs:355→:362, ledger.mjs:173→:202) → final pass: `review.mjs:744` inside the "safe" group is a deliberate disclaimer (design.md:848: "the p90 is that report's, **not** `review.mjs:744`... a mis-carried citation") — mechanical repair would convert a correct sentence into a wrong citation with a maintenance-looking diff. Survived two sessions and three narrowings to the last pass; tree never touched.

Not chains (rejected): the "live regression" withdrawal (verified, not wrong); Orch's 10% (caught before landing, single error); `.agents/` furniture→inert (inattention corrected, correction sound).

## 6. WINS

- **Undeclared 56/55 check worked.** Arch withheld the slip, demanded re-derivation "however small the gap"; reviewer found it unprompted with matching per-file numbers — independent confirmation and a measurement of the reviewer (followed, not performed).
- **Mutation-pair brief design held.** Generic-swap-green-by-design stated alongside required count-field red; reviewer reproduced exactly; the misreading warned against by name didn't happen.
- **Task-22 tick withheld** between registry commit and merge ("merged and ticked" split deliberately), citing the packet-E premature-tick defect found the same afternoon; ticked only after 28087f5 landed.
- **Full sealed review chosen over delta** for round 2 (production touched select.mjs) despite small additive diff — "exactly the move this project's memory warns against" declined.
- **Single finding closed by addendum, not round 3,** with stated reason (defect in uncommitted prose; tree twice verified) — economy without concealment.
- **Orch discarded an all-green gate run** (FULL-MEM-LOG mtime inside window) and re-ran; named the briefing error precisely (suite reads working tree, never git).
- **Build-lock left alone** (pid 18064 recycled); file's own line 116 predicted it; next build self-reclaimed (18576). "A wait, not a wrong answer."
- **Stream work ran the control,** not just the failure: succeeding run stdout non-empty, so producedNothing discriminates both directions. Load-bearing detail (header-on-stderr) doubly measured.
- **Comparison review kept the finding OpenRouter downgraded** (Reason-paragraph check vacuous — zero archived deltas ever carried one) rather than adopting the downgrade.
- **Worker non-compliance twice correct:** declined invented migration band (stated requirement without value); declined F5 unit (breaker vs runner-health conflation) — the RESULT disagreement clause paying off.
- **Sweep-verification rule ("report clean too + reconcile raw grep")** caught :305 which both worker sweep and sealed review missed.
- **Orch declined a fourth pre-merge review on boundary grounds** (rulings are Arch's lane; gate answers a different question) — refusing to borrow judgement while appearing to check it.
- **Probe-and-run beat reasoning twice:** `2>&1` admitted as stream-destroying, re-measured separated via Node; "no opencode process" / "parent died" inferences corrected by better filters within minutes.
- **Narrowings that touched nothing:** six candidate citation edits → two; trap caught; "the tree was never touched at any point."
- **Bead 0iw3 acceptance refuses both easy closes** (sentence-written; rung re-enabled) — test must be seen red.
- **Transcript exporter probed before claiming unrecoverable** (empty-block field inventory; signature identified as attestation); script prints both counts; type:user≠human documented with envelope-prefix filtering; UTC-vs-local slicing warned with exact-instant fix pending.

## 7. PATTERNS

- **Check whose false/true answers are the same observation (5+ instances):** guard sampling process table between sequential gate stages (hit the gap); bare `openspec validate` from default cwd greens about the main tree regardless of intent; sealed-packet linter failing excellent-or-worthless identically; brief-review DISPATCH greens structurally incapable of redness on the asked question (twice); exit-0-on-variant-refusal. Orch's remedy (run-scoped lock file in build-lock shape) is the only structural fix among them.
- **Shape read as scope:** diffstat read as diff ("tightening"); pids read as timestamps; pipeline exit code read as grep's (through head); grep count read as fixture count (records vs entries: `carry:` blocks); per-line grep read as site count (:2441 spans break); requirements-read read as occurrences-covered (:305); review verdict's shape read as its scope (twice).
- **Two derivations sharing an instrument are one:** explicitly guarded once — comparison review's shared findings via *different* evidence paths claimed as "corroboration, not agreement"; per-file count match as independence evidence.
- **Artifacts that cannot fail, with costs:** restated conformance table (fourth stale recurrence; deleted from CLAUDE.md + runners.yml); design.md runner-policy block (three contradictions incl. unselectable "starting rung"; supersession appended); premature ticks (committed false record); wrong pins (deletion-grade misdirection, five files); proximity citations ("two paragraphs up" true for one of three sites).
- **Guardrail output truncated / pipe held wrong:** `2>&1` destroying the stream distinction; log opened without -Append clobbering launch record; `cd_free=`/`cd_none=` placebo prefixes twice ("too close to a banned token"); defensive-cd-disarm естественный twice (classifier matches token, rightly).
- **NEW — "a correct calculation on an invented input is more persuasive than either error alone"** (Orch's framing, kept by both). Instances: 3.33→10 warm-up on nonexistent 10%; 56-test prose over correct 20/16/15/4; board's v8q8 close-condition. Fluency manufactures the input, arithmetic launders it.
- **NEW — corrections arrive with the authority of fixes.** `dyw` nearly "fixed" (beads id); :404→:419 handled by verifying companion first; bulk-apply threat over 22 noisy pins; trap at :744. The costliest direction is confident-premise→permissive-instruction, not agreement itself ("agreement moves the premise and the instruction independently").
- **NEW — sweep-over-units misses inside judged-clean units.** Enumerating requirements read ≠ enumerating word occurrences; requiring clean-results reporting didn't save it; only raw-count reconciliation did.
- **NEW — reader-addressed rules can't bind suffix filters.** A SHALL over config names constrains nothing when the machine matches `*_ceiling_pct`; placement binds, exclusion lists don't. Generalises: constrain the reader and the filter separately, or the green is about the prose.
- **Rotting where nothing looks:** tasks.md exercised every round vs spec deltas consumed by nobody until archive (45 vs 18 findings); instruments need jurisdiction statements (brief-linter vs sealed packet).

## 8. MECHANISM vs SUSPICION TALLY

Counting rule: scheduled pipeline stages (Muse/sealed/codex reviews, audits, linters, gates, probes, fixtures) = MECHANISM; ad-hoc "verify it myself rather than take it" measurements, disputes, record lookups, incident diagnoses = SUSPICION, even when executed with grep/node. Borderline (model reviews are persons-like but scheduled): counted MECHANISM, named below.

**MECHANISM (~26):** C1 6 + C2 8 findings; brief-review catches (impossible backtick remedy; stay-green mutation; DISPATCH-clean brief); sealed-review findings (F 5; specfix R1 5, R2 4; R3 DISPATCH); undeclared 56/55 re-derivation; 18 audit findings (A/B1/B2/D/E/G); enum-linter fires + packet-mode bare-"once" fire; linter instrument-audits (5 + F8 + r2 rounds); OpenRouter probe (exit-0) + comparison both-REVISE; build-lock refusal + self-reclaim; drift non-reproduction; manifest-via-test green; F2 eviction arithmetic (sealed reviewer); global-underspec (sealed reviewer); queue-3 validate-operand catch; preamble contradiction; corrections-doc review (log-not-list; backwards entry).

**SUSPICION (~34):** Orch's pattern-hole concession + uniqueness-tree property; Orch's self-caught laundering + re-derivation; Arch's C1/C2 self-confirmations; Arch's live-regression withdrawal measurements (CASE A=0; task lookup; whole-tree `/`→`/?` sweep); Orch's independent zero-ref verification; Orch's diffstat self-correction; all 55/55 recounts; escalation-join found at diff read (Orch) + sole-`escalates_to` verification (Arch); tick-withheld decision; stream separation + control run; header-stderr dispute resolved via board line 91; pid start-time refutation; reboot/server/shim diagnosis; FULL-MEM-LOG mtime discard; guard-gap diagnosis + explicit-clearance fix; -Append clobber noticed + restored; F5-unit worker refusal + Arch's acceptance; :305 grep reconciliation; ledger-premise falsification (:275); docs read retracting no-mechanism claim; R3 process-inference self-corrections (×3); §27 overstatement; warm-up code check → placement rule; 10% fabrication caught via delta search; uncomputed-figures self-catch + counting script; 38/67 counter; 26-ticked/96-row verifications; F4/F1/F3 data checks (single records; passesToSupersede match; no --force); dyw test-log check; trap verification + beyond-requirement tracing; `.agents/` inert measurement; head-exit-code self-catch; v8q8 bead read; seven-packets own-test failure; 13-review count with stated ambiguity.

**Ratio:** roughly 26 mechanism to 34 suspicion — for a desk meant to run unattended, the finding is that the pipeline finds the defects but persons close them: nearly every mechanism catch in this chunk required a suspicious person to verify, scope, or rescue it (withdrawals independently re-measured; DISPATCH greens declared void; sweeps reconciled; validate re-run via {cwd}), while the most expensive near-misses (hold breach, vacuous ruling, 10% in law) were caught by persons alone. The two fabricated figures tonight were both peer-caught, neither instrument-caught — stated in-chunk twice, by both sessions.

## 9. WHAT I COULD NOT TELL FROM THIS CHUNK

1. The chunking is by size and this is the last chunk, so threads opened here stay open: the 9/8-only export (filter "accept an exact instant" unconfirmed delivered); whether the `runners.yml` pattern commit ("is not supported for model" + "Session not found") and the gate-lock commit ever landed; whether the escalation-join test arm was written and seen red; whether C1/C2 were ever built.
2. Tool calls/results were stripped, so every "measured" claim's actual command is unverifiable here — I take the stated numbers (302 files, 63 basenames, 18:44:34 pids, 19:09:52 mtime) on prose authority, exactly the failure mode the chunk warns about.
3. Reasoning blocks are ~2–9% recoverable (chunk header: 2.2% Orch, 0/888 Luna) — abrupt decisions (full-over-delta review, addendum-not-round-3, let-it-run (a), declining the fourth review) may have deliberation that simply isn't in the record; "decided without reasoning" is not supportable over "reasoned where the transcript doesn't keep."
4. No Luna Boss entries appear in this chunk, so the fleet side (and whether any Desk run ever produced the v8q8 ledger line) is invisible here; the 7-day-meter claims rest on one session's usage file, never independently measured in-chunk.
5. Open at end: design.md's 22-citation triage (parked past stopping point); the remaining ~38 spec-triage items beyond the ruled residuals; corrections-queue final state; whether `53d510e` (fourth specfix commit) differs in content from the three named; the exact `tightestCeilingPct` doc-comment wording that made auto-extension "deliberate."
