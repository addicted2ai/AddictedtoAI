# TIMELINE CHUNK NOTE 01 of 08

## 1. SPAN
- First timestamp: `2026-09-07T23:45:08.675Z` — `[A2AI-luna-boss (1/2)] ASSISTANT` adopts coordinator role, starts `memory-condenser`.
- Last timestamp: `2026-09-08T10:03:30.442Z` — `[A2AI-luna-boss (2/2, post-compaction)] ASSISTANT` verifies batch-2 final-review ground and corrects `d1ki`.
- Local dates: UTC-6. Entries stamped `00:00Z–06:00Z` on 09-08 are local evening 09-07. Briefs pinning "Today is 2026-09-07" span local midnight.
- Work covered: round-3 review chain collision and verdicts (xdk approve/merged, nq36/5bgo/91s revise); nq36 rounds 2–4 + handover; 91s revision rounds 3–4 + handover as 5th of batch 1; batch-1 final review → `84s8`; memory audit of 58 memories → 10 rewrites + 1 retire + new `a-check-narrower-than-the-property-it-names`; quiet block (5bgo 1/20→0/20 approve, ovrk route-2 approve + `d1ki`); gates rounds 1–5 + handover + merge `8502757`; census red/halt/repair/re-review deadlock (`vqbo`) + merge; en3s approve, oq5d approve, Pulse orphans commit, batch-2 merge `67ddf62`; batch-2 final review launch.

## 2. CLAIMS OPENED
- `[luna-boss (1/2)] 23:48:33Z`: 91s revision completed as `2fda380`.
- `[luna-boss (1/2)] 23:50:29Z`: chained review stage `bcdzm2z68` armed, will launch 4 max reviewers when 5bgo exits; all four branches merge cleanly into `046bd45`.
- `[luna-boss (1/2)] 23:51:49Z`: `indexRouteDates` at `lib/sitemap-dates.mjs:147` has zero occurrences in test file; 13 tests cover only pre-existing functions.
- `[luna-boss (1/2)] 23:52:12Z then 23:52:51Z`: `gates.mjs` marker only at `:64/:68` vs at `:86`, `:126–:139`; `scripts/run-tests.mjs:105` honours `ATAI_TEST_LOCK_WAIT_MS`, nothing in `loop/` sets it.
- `[luna-boss (1/2)] 23:54:20Z`: editing `fleet3-review-run.sh` in place risks corrupting execution (bash reads by byte offset).
- `[luna-boss (1/2)] 23:56:26Z–23:56:41Z`: Bash tool has no coreutils (`codex, cat, ls` exit 127); everything must run via PowerShell.
- `[luna-boss (1/2)] 23:57:58Z`: collision pids — mine 29096 started 17:56:58, chain's four started 17:57:11.
- `[luna-boss (1/2)] 00:00:37Z 09-08`: xdk exactly `app/globals.css | 2 +-`, one `.category-note`, no competing rule; 5bgo touches only test file, no `setTimeout`; `fleet2/91s` alarming 15-file diff is older-base artifact, own work 3 files.
- `[luna-boss (1/2)] 00:07:47Z`: issues-completed-at-merge count says 46n+ae7 landed, xdk is 3rd not 1st; round-4 order gates→en3s→ovrk for quiet-machine; `f4-serial.ps1` refuses if any codex alive.
- `[luna-boss (1/2)] 00:06:06Z–00:09:07Z`: xdk reviewer ran two mutations (delete selector; rename emitted class), 16-test suite unchanged both times, restoration by blob hash; nq36 reviewer mutated `sitemap-dates.mjs:163`, both suites still passed, restored SHA-256.
- `[luna-boss (1/2)] 00:11:51Z`: briefs written from `bd show | head -N`; ACCEPTANCE at bottom truncated; same defect in gates brief (names x2jl records-retry + re-gating in prose, omits from tasks/acceptance) → filed `mq8e`.
- `[luna-boss (1/2)] 00:12:31Z`: en3s brief faithful, includes `pulse/lib/publish.mjs` `isEngineWrite`.
- `[luna-boss (1/2)] 00:20:00Z`: nq36 revision committed `4f0daeb`; Desk runs in same `D:\addictedtoai-worktrees\` as `j-20260907-25`, guard must key on `fleet` prefix.
- `[luna-boss (1/2)] 00:33:19Z–00:39:40Z`: round-2 nq36 mutations — remove `dateModified: wikiChangedOn` from page leaves 3 suites green; `/data` fixture can't distinguish 7-member fold from `newest([catalog])`; round-3 mutations — remove from `app/wiki/page.tsx` fails, from `app/data/page.tsx` fails, `newest([catalog])` at `:163` fails 6/7 (catalog passes), `newest([learn])` 8 fail; scope 2 test files only, merges into main.
- `[luna-boss (1/2)] 00:56:50Z–00:58:49Z`: round-3 reviewer ran from `fleet3-sitemap` outside worktree (cleaned up, verified gone); Finding 1 signature `TypeError` from 3-arg probe nothing calls rejected (work plan vs ACCEPTANCE); Findings 2+3 accepted (sitemap guard only catches literal `newest(site.`, local fold passed 8/8; default `2099-01-01` passed 21 JSON-LD — brief said "invert them").
- `[luna-boss (1/2)] 01:01:49Z`: stopping rule + recurrence clause (same class N vs N-1 → stop); nq36 ends at round 4; opportunity cost 4 pairs vs `bd ready` ~90.
- `[luna-boss (1/2)] 01:04:39Z`: filed `ckvd` (source guards vs behaviour, NAME<SOURCE<BEHAVIOURAL); round-4 `lib/sitemap-dates.mjs` comment-only 5 lines in JSDoc.
- `[luna-boss (1/2)] 01:15:44Z`: sealed reviewer wrote `approve` then changed to `revise` after opening round-3 review — quote: *"This changes my verdict from `approve` to `revise`."*; nq36 merges clean into `578800f`; filed `3j6w`.
- `[luna-boss (1/2)] 01:24:54Z–01:25:13Z`: 91s provenance says `measured 2026-09-06` but `out/` rebuilt today 19:24 and measurement ran today — false on both readings.
- `[luna-boss (1/2)] 01:39:01Z`: advanced-clock test changes inputs + uses `deepEqual` — proved `deepEqualPassesForReorderedKeys: true, byteEqualForReorderedKeys: false`.
- `[luna-boss (1/2)] 01:44:50Z`: round-4 dates now `2026-09-07` verified in source; byte-identity via `JSON.stringify`; unrelated `2026-08-31`, `2026-09-06` belong to different measurements.
- `[luna-boss (1/2)] 01:56:33Z`: two runs of `measure-payload.mjs` over same `out/` byte-identical; bead's N-runs experiment degenerate (gzip deterministic → spread 0 → floor 0); oscillation `ai-winter 5.4→5.3→5.1→5.4` across rebuilds (no `generateBuildId` → fresh random ID), rebuilds forbidden under shared lock; filed `tdl7`.
- `[luna-boss (1/2)] 02:10:26Z`: audit 58 memories: 7 STALE, 1 CONTRADICTED, 4 STALE-IN-DETAIL, 18 VALID, 28 UNVERIFIABLE; `build-core-invariants` says verbatim but `lib/units.mjs:145-158` scales via `displayQuantity()/formatPrice()` with passing test; `snapshot-census-daily-red` says 16 claims/9 docs red every morning vs measured 23 claims/14 docs 21 hedged 0 errors 0 debt; `AGENTS.md:220/:224` vs `CLAUDE.md` on HOLD.md clearing.
- `[luna-boss (1/2)] 02:27:16Z`: maintainer ruling — orchestrator can remove HOLD.md, not STOP; 10 rewritten verified byte-identical, 1 retired; hedge alarm past 15 at 21.
- `[luna-boss (1/2)] 02:47:44Z`: batch-1 final review — `verify-surfaces.mjs:591` enters only `if (graph.dateModified)` so deletion passes; both surfaces consume same `indexRouteDates()` so equality is self-consistency; mutating `:153` to `slice(0,1)` leaves 31/0 green; filed `84s8`; floor hides to 62B inclusive but budget reads raw bytes.
- `[luna-boss (1/2)] 03:06:11Z`: 5bgo load proof `broken 1/20, repaired 0/20`; tests sequence on reads `:293, :674, :723`; restoration proof `git diff --no-index --exit-code <path> <path>` vacuous (same path, exits 0).
- `[luna-boss (1/2)] 03:19:28Z`: ovrk brief forbids editing `scripts/run-tests.mjs`, foreclosing bead's first solution (env var honoured only under test).
- `[luna-boss (2/2)] 03:39:36Z`: ovrk `ad0aba5` route-2 — real-process wait-message replaced by direct `acquireBuildLock` contention capturing log callback; mutation `log: ()=>{}` fails; claims `run-tests.mjs`/`build-lock.mjs` untouched (to verify).
- `[luna-boss (2/2)] 04:10:52Z`: premature close was ovrk implementer `bd close addictedtoai-ovrk` at `f4-ovrk.log:5123` 22s after commit, cause bd `SESSION CLOSE PROTOCOL` hook; gates `9ac8c14` 3 files; withheld: `BUILD_LOCK_REFUSAL` matches bare `build-lock:` prefix written on succeeding paths + `runGates` `environmental` over all results (measured waited+failed → `interrupted`, real failure excused); timeout reports `environmental: child process did not start` when started+hung.
- `[luna-boss (2/2)] 04:16:27Z–04:30:47Z`: round-1 reviewer re-found both withheld with own probes +2 more (brief quoted only `0xC0000142` pair vs `0xC0000142/0xC0000005/EAGAIN/ENOMEM…before producing any output`; lock-wait test passed `lockWaitMs` to `runGates` itself, proved by deleting call site stays green); round-2 deletes second source of truth, uses x2jl *"before producing any output"* discriminator.
- `[luna-boss (2/2)] 05:01:59Z–05:05:02Z`: new lock-grace test behavioural (real lock, real `run-tests.mjs`, 4s cap, 1s margin); measured 26 runs 1 failure load-dependent (11/12 then 14/14).
- `[luna-boss (2/2)] 05:18:50Z`: `FULL-MEM-LOG.md` 119,884 bytes verbatim, index both statements; corpus back to one-index (`distilled-memory-index` 23,945 chars).
- `[luna-boss (2/2)] 05:38:53Z`: post-merge gate writes HOLD.md on build-lock refusal via real `runLoop` fixture `{"outcome":"done","calls":2,"hold":true}`; pre-review asks `gatesHitEnvironmentalFailure`, post-merge `checkBuildRed` sees only `{ok,output}`; ~180 lock files are litter (fresh `mkdtemp` → unique keys), none deleted.
- `[luna-boss (2/2)] 06:05:52Z–06:18:27Z`: `publishAfterRecords=true` at `run.mjs:1607` unconditional + `publishStep` gated on HOLD.md existence → suppressing hold removes publish gate; with `publish:true` reaches publish with no verified build; filed `ml25`; round-5 removes post-merge handling (declines to fix).
- `[Orch] 06:18:43Z–06:19:19Z`: `publish.mjs:45` gated by publish/HOLD/something-to-say, `:53` suspends phase 2, `:129` misquotes `specs/loop:578` (loop-actor ban read as maintainer-only); fix `fl/hold-publish-coupling` `1e916f3`.
- `[luna-boss (2/2)] 06:21:53Z`: round-5 early return above `authorPhase.gates={retried…}` loses retry record — quote: *"the ledger cannot tell a job that passed first time from one that needed a second run, which is the number that says whether this policy is paying for itself."* → filed `q6xp`; `lockWaitBudget(1)` invariant; test count 30→30 = -1+1 with 2 findings via extended tests.
- `[luna-boss (2/2)] 06:45:42Z`: gates handed over 5 commits 3 files `+330/-13` clean onto main; round-5 review `revise` with 2 `mq8e` items overruled, tally 3 accept narrowing /2 reject.
- `[Orch] 07:20:22Z–07:25:42Z`: window refused HOLD.md; genuine build failure (not misclassification); `z-ai-glm-5-2-free.md:45` census anchored 2026-09-07 vs 09-08 snapshot; first repair dropped ISO date → error moved to 2026-09-06 (nearest remaining date line 38); second repair build EXIT=0 23/23 hedged; page reviewed → `mismatched 1` verify-launch fails; repair+`entry: re-review` committed `4f669c2`; halt cleared with 1 gate red.
- `[luna-boss (2/2)] 07:27:34Z + [Orch] 07:28:28Z`: trend claim hedged 21→23 as arrival (new pages, mechanism worsens as Desk productive) recorded on `6nrk`.
- `[luna-boss (2/2)] 07:30:56Z + [Orch] 07:32:55Z`: correction — claim count 23 both days; prose arrived 2026-09-07 03:31 job `j-20260907-04`, page from 2026-08-28, zero new wiki pages → ageing; check cannot warn author (compliant on day of authoring).
- `[luna-boss (2/2)] 07:34:52Z`: 10 unpushed on local main; Desk `publish:true` pushes `origin main` branch-wide unconditionally → mem-cond commits ship via next Desk job (3rd firing of `zuoo`); 5/7 FULL-MEM-LOG already ancestors of `origin/main`.
- `[luna-boss (2/2)] 07:42:11Z`: filed `vqbo` — `j-20260908-03 entry failed "done with an empty diff"`, `run.mjs:390` unconditional, no `[done]` marker, no new record; directive says "may need no edit" but mismatch clears only via new record on changed bytes; `blocked:` with clean tree dodges failure but writes no record (quiet loop worse).
- `[Orch] 07:44:52Z–07:49:48Z`: parked old line (strip `- `), new directive asks real edit (doubled date *"on 2026-09-07 ... as observed on 7 September 2026"*); dry runs kept showing old text; loop's own parser 44 directives 1 selectable at line 144 new; cause `job/j-20260908-04` resumed from committed brief (`.job/source.json`, `resume.mjs` `isResumable=(last&&interrupted|capacity)||(!last&&briefPresent)`).
- `[Orch] 07:53:16Z–07:53:54Z`: breaker 1 is 3 consecutive `failed/discarded` same type (filters `l.type===type`); entry at 2 consecutive; running one job not chain.
- `[Orch] 08:14:03Z–08:22:25Z`: `j-20260908-05` done approve, declined suggestion, improved different sentence; `mismatched 0`, VL EXIT=0 15 checks, streak 0, directive MARKED DONE; gates merged `8502757` 1698 tests (1689→1698), new tests by name `lock wait below the process cap`, `lock grace period from its per-job cap`.
- `[luna-boss (2/2)] 08:23:47Z–08:42:05Z`: en3s imperative-diff adds invariant *"It NEVER submits a URL that did not change...never wrong; occasionally silent"*; en3s 1 commit 4 files no ledger, reviewer mutation `>`→`>=` kills 3; oq5d no ACCEPTANCE, `gateCommand` unused, `rmSync` at `:1298` (bead ~:1160, comment ~1090); `unlinkNodeModules` swallows fail/returns false discarded at startup vs `removeJobWorktree` hard stop — measured real Windows junction: `rmSync` does NOT follow, target survives; oq5d 1 commit 2 files `gateCommand` gone from imports, approve; tooling gap brief absent via stdin → runner now copies to `.agent-brief.md`.
- `[Orch] 09:00:36Z–09:15:26Z`: 61 modified (40 `content/wiki` `domains_seeded`) are Pulse run (`latest.json` +4,477 lines, `previous.json` rotated); snapshot advanced same date 2026-09-08 → census 23/23 untouched; `MECHANICAL_FRONT_MATTER_KEYS=['timeline','domains_seeded']` → `mismatched 0`/640; commit 60 files excludes maintainer's 12 skill deletions +13 `.agents`; closes `dn44` evidence clause.
- `[Orch] 09:24:24Z`: batch-2 merged `67ddf62` 1702 tests six gates green; `publish.mjs` auto-merged hunks `@@-126` vs `@@-205`.
- `[luna-boss (2/2)] 09:37:23Z–10:03:30Z`: batch-2 final review vs merged tree, must not prove publishing by publishing (fixture repo + bare origin in temp), explicit "Where I looked and found nothing"; question only it can ask — when a change deleted/replaced a test what did deleted prove that replacement does not (ovrk real-process→in-process, gates removed post-merge branch); one finding + re-found `d1ki` with measurement showing overstatement → correcting `d1ki`.

## 3. CLAIMS SETTLED IN THIS CHUNK
- nq36 test-file missing → retracted within 16s. `23:51:17Z` "exists on disk, is not in the commit, git isn't even reporting it" → `23:51:33Z` "already exist in main — nq36 added a function to an existing module, so nothing is missing." Caught by self re-check.
- `gates.mjs` line numbers stale → retracted within 39s. `23:52:12Z` "don't hold — marker lives only at :64/:68" → `23:52:51Z` "do hold — :86 is `gatesHitTransportFailure`, :126–:139 is `gateFailureNote`... My first grep only matched the constant name, which was too narrow." Self.
- Out-of-band xdk reviewer needed → retracted within ~2m. Launched 23:55:27Z → 23:57:42Z "reason... just evaporated... mine is now redundant" + killed pid 29096, chain's four intact, REVIEW.md unwritten. Self via process inspection.
- 5bgo revision "quick one-line fix" → retracted same hour. `00:13:49Z` "requires a load measurement... same constraint as ovrk... reordered the queue." Self after reading full bead.
- Serial guard "any codex" → retracted after ~7m. `00:15:32Z` guard blocks → `00:20:26Z` "must key on the `fleet` prefix rather than the parent path." Self after seeing Desk in same dir.
- Escalate-to-maintainer on round-5 → retracted in 4s by user veto. `00:58:49Z` "I'll stop and put the merge-with-follow-up question to you" → `01:00:01Z USER: "Dont do this... work it out with the orchestrator"` → `01:00:05Z` "that decision is mine and the orchestrator's." User mechanism.
- Census trend arrival → retracted after ~3–4m. `07:27:34Z/07:28:28Z` "Each morning's red is a new page nobody hedged... gets worse as Desk productive" → `07:30:56Z/07:32:55Z` ageing, "21+2=23... Nothing new was authored... It's ageing." + "no new wiki page was added." Caught by mem-cond arithmetic + git (`j-20260907-04` 03:31 09-07). Stood ~1h across sessions including bead write. Quote correction: Orch "I took a structural argument on trust because it was well-argued and I agreed with it."
- Hedge repair "marker with date removed" → retracted by build in ~26s. `07:21:35Z` repair → `07:22:13Z` "error changed. It now names 2026-09-06, because removing the ISO date... left... nearest anchor as the 2026-09-06 snapshot." Mechanism (build) caught person error.
- Parking worked/failed → settled via parser + resume discovery over ~5m. `07:46:14Z` "parking did not work" (grep) → `07:46:36Z` "file is actually correct" → `07:49:48Z` "Found it... `resuming job/j-20260908-04`... brief is committed." Self + loop's own parser mechanism.
- Bead replacement claim → corrected. Luna-boss said Orch "replaced erroneous section" → `07:38:47Z` "What's actually stored quotes the wrong reading, attributes it, and refutes it in place." Self via reading stored notes.
- Junction hazard → negatively settled before briefing. Suspicion discarded-return → disaster → `08:42:05Z` "Measured, and my suspicion was wrong — `rmSync` does not follow a junction; the shared install survives." Person ran real junction.
- N-runs experiment required → rejected as degenerate. Reviewer demanded N runs → `01:56:33Z` "I reject finding 1 as a blocker, and the reviewer's own evidence is the reason: it ran five measurements... then asked for exactly that experiment." + own byte-identical runs. Person + mechanism.
- Signature finding → overruled. Reviewer `TypeError` → coordinator: sketch in work plan, no caller uses 3-arg form, worse code to reshape. Person judgment, sealed-approve evidence later supports.
- Withheld date → confirmed, reviewer miss confirmed. Coordinator `measured 2026-09-06` wrong → reviewer blessed as "local measurement date" → round-4 fixed to `2026-09-07` verified in source. Person.
- Flake fixed → confirmed by two parties 40 runs. Author 20/20 (368ms spread, quiet) + coordinator 20/20 with 15,955ms outlier vs ~11.0s norm still pass. Person + mechanism.
- Post-merge halt coupling → confirmed by second session. Luna-boss `publishAfterRecords=true` + HOLD double duty → Orch independently: "`publish.mjs:45` states... Gated by `publish`, by `HOLD.md`..." + `:53`, `:1607`. Person code read.

## 4. MISTAKES
- **[luna-boss (1/2)] phantom missing file (23:51Z):** claimed test file untracked/missing; true: existed in main. Caught by self re-read. Cost: minutes + logged grading plan built on false premise.
- **[luna-boss (1/2)] narrow grep (23:52Z):** claimed dump line numbers stale; true: held, grep matched only constant. Self. Cost: false re-derivation + correction message.
- **[luna-boss (1/2)] redundant reviewer:** launched standalone xdk reviewer 13s before chain fired on same worktree → two writers one REVIEW.md. Caught by self checking processes. Cost: kill pid 29096 + verification nothing corrupted.
- **[orch/predecessor briefs, caught by luna-boss (1/2)] truncation narrowing ×4:** nq36 brief inverted acceptance (shipped refactor, dropped `dateModified`); gates brief dropped 2/3 x2jl requirements; ovrk brief banned `run-tests.mjs` foreclosing bead's solution 1; 5bgo substitution unaudited (held, later cleared). True: ACCEPTANCE at bottom, `bd show | head -N` deletes done. Caught by person audit vs full bead. Cost: revision rounds, `mq8e`, ovrk rewrite, 5-round gates cost; pattern strongest argument for mandatory audit.
- **[luna-boss (1/2)] serial guard overbroad:** refused on any codex; true: Desk shares parent dir, must key `fleet`. Self after live conflict. Cost: would have blocked loop every Desk run.
- **[luna-boss (1/2)] round-2 brief wording "invert them":** author inverted literally → default `2099-01-01` passes 21 tests. Admitted: "This one was my fault." Caught by reviewer mutation. Cost: round 4.
- **[nq36 authors r2–r3] dead tests/guards:** wiring correct but tests pass when should fail (missing `dateModified`, indistinguishable fold; then guards only catch literal string). Caught by max reviewers via mutation (person driving mechanism). Cost: 2 extra rounds.
- **[round-3 reviewer] signature TypeError:** probe calls 3-arg form nothing calls, demands reshape to pre-implementation sketch hiding `postChangedOn`. Overruled by coordinator person (ACCEPTANCE vs plan). Cost: debate + `3j6w`; sealed test later supports overrule.
- **[91s author] false provenance date:** `measured 2026-09-06` when measured/built today. Caught by coordinator person (`out/` 19:24). Reviewer missed + blessed. Cost: mandatory round 4; would have shipped same species as bead's original sin.
- **[91s author r3] wrong equality:** `deepEqual` for byte-identity (reorder passes, bytes differ). Caught by reviewer proof `deepEqualPassesForReorderedKeys:true, byteEqualForReorderedKeys:false`. Cost: round 4.
- **[round-4 91s reviewer] literalism:** demanded degenerate N-runs experiment while own 5 runs identical; missed false date. Caught by coordinator person + own runs. Cost: overrule + `tdl7` filing; lesson: judged words over measurement.
- **[5bgo author] vacuous proof:** `git diff --no-index --exit-code <path> <path>` compares file to itself, exits 0 always. Caught by coordinator person; reviewer missed but built own harness (better failure mode). Cost: grading signal + brief warning.
- **[memories, caught by audit session]** 7 stale + 4 locators: `build-core-invariants` verbatim vs scaled per-million (dangerous — could undo shipped fix); `snapshot-census-daily-red` 16/9 vs 23/14 0 errors; line numbers `reviews.mjs:216→:256`, `budget.mjs:372→:636`. Caught by max audit person reading 58 full bodies. Cost: 10 rewrites + retire; char count +5%.
- **[luna-boss (2/2) + Orch] census arrival story (07:27–07:28Z):** claimed 21→23 is new pages; true: same 23 claims ageing (page's 2 claims from `j-20260907-04`). Caught by mem-cond arithmetic + git. Cost: bead note written unchecked; Orch: "Agreement is precisely when checking stops feeling necessary." Had disproving numbers in own build output.
- **[Orch] hedge repair v1:** removed ISO date → rebound to 2026-09-06, would have been false hedge. Caught by build mechanism (different error). Cost: second edit; lesson "removing a date moves the anchor."
- **[Orch] dry-run misreads ×3:** grep wrong region, stale file (running job touched tree), uncommitted repark, missed first-two-lines resume notice. Caught by self + loop parser. Cost: ~5m + `vqbo` second half.
- **[gates author r1] classifier scope:** `BUILD_LOCK_REFUSAL` bare prefix matches succeeding paths + `environmental` over all results → waited+failed books `interrupted`, real failure excused; timeout says `did not start` when hung. Caught by coordinator person probes (withheld). Cost: rounds; reviewer independently re-found with own probes.
- **[gates author r1 finding-4] untested wiring:** lock-wait test never touched call site. Caught by reviewer mutation (delete site stays green). Cost: round 2.
- **[luna-boss brief] x2jl codes dropped:** quoted only pair, omitted `0xC0000005/EAGAIN/ENOMEM…`. Self-admitted: "Finding 2 was against my brief." Cost: round 2 carries it.
- **[gates author r3] flaky lock-grace (1/26):** 4s cap/1s margin, poll+teardown eats grace under load. Caught by coordinator person 26 runs. Cost: round 4; would have merged flake into every Desk merge gate (3 trips halt Desk — disease 3ov0 cures).
- **[gates author r4] unverified publish:** suppressed HOLD also removed publish gate. Caught by r4 reviewer person + coordinator code verify. Cost: round 5 + P1 `ml25`; lesson: guardrail gating two jobs can't be relaxed for one.
- **[gates author r5] dropped telemetry:** early return skips `authorPhase.gates`, loses retry count for policy it measures. Caught by coordinator reading diff person; sealed r5 reviewer missed despite mutating path. Cost: `q6xp` residue; lesson: verifier of new path doesn't ask what path skips.
- **[luna-boss (1/2)] seal side-channel:** put round-2 probe in `mq8e` notes readable by sealed reviewer; reviewer disclosed `bd show mq8e` before verdict. Caught by reviewer honesty. Cost: process rule — round measurements to beads only at handover; "seal over one door of room with two."
- **[luna-boss (2/2)] presence-check double-escape:** reported present strings absent; seal fine, instrument broken. Self. Cost: none, fifth instance of proof-that-cannot-fail class, mine.
- **[ovrk implementer] premature `bd close`:** closed bead 22s after commit per hook `SESSION CLOSE PROTOCOL`. Caught by coordinator log read `f4-ovrk.log:5123`. Cost: counter-instruction in 4 briefs/generators + `81fm`.
- **[Desk/loop] orphaned Pulse:** 60 files uncommitted 3h while 4 Desk jobs pushed past (exact-path staging); mem-cond 5/7 logs already on `origin/main` via publish. Caught by Orch/coord counting unpushed. Cost: separate commit + warning branches for take-backs.

## 5. CORRECTIONS THAT WERE THEMSELVES WRONG
- **Hedge repair v1 (Orch 07:21:35Z → 07:22:13Z):** correction for red census (add marker, drop ISO date) was false — rebound anchor to 2026-09-06 snapshot three lines up and demanded false hedge. Build caught it ("error changed... now names 2026-09-06"). Second repair restored anchor. Chain: correct page → wrong fix → right fix, with lesson "a repair that changes the error is not a repair."
- **Gates rounds 4 and 5 fixes introducing new defects (net-negative-risk chain):** round-4 fix for false halt opened unverified-publish path; round-5 fix removing post-merge handling opened dropped-telemetry path. Coordinator explicitly overrides recurrence rule: "rounds 4 and 5 each introduced a new defect while fixing others — two for two... further rounds net-negative-risk rather than diminishing-returns." No fifth without opportunity-cost sign-off. This is the in-chunk candidate for third chain type — self-correction overshooting in same motion.
- **Census trend correction уточнение:** Luna-boss "Orch replaced section" was itself slightly wrong — Orch quoted + refuted in place under correction heading rather than clean replace. Luna-boss corrects at 07:38:47Z: "right about effect, wrong about mechanism... bead... unable to instruct anyone with it." Minor but exact shape: correction of record needed its own correction.
- Empty on the two prompt-named cross-chunk chains (citation sweep noisy→right→six→two; fixture 32→~30→7) — neither lives in this chunk; do not force.

## 6. WINS
- **Kill redundant reviewer before write:** pid-level identification, xdk REVIEW.md verified unwritten. Hold held.
- **5bgo RESULT honesty:** explicitly refused load proof it failed to obtain; left 36s test run to reviewer rather than adding load. Left intact to grade reviewer.
- **Seal discipline that paid:** redacted prior findings + overrule rationale; sealed reviewer wrote `approve` then only flipped after reading prior round — evidence about severity + recurrence clause firing. Second instance: round-4 nq36 comment-only verified 5 insertions 0 deletions.
- **Sealer/generator refusals:** refused unsealed brief on marker mismatch (stale `.agent-brief.md`, stale round-3 target, curly vs straight apostrophe) rather than emitting. Runtime serial guard (`fleet` prefix) correctly noted Desk and proceeded.
- **Withholding to grade:** nq36 `indexRouteDates` gap, 91s false date, gates classifier holes, gates `q6xp` early-return — each guaranteed not to reach main; three re-found independently, two missed (date blessed, `q6xp` missed) giving measured reviewer-miss data.
- **Overrules with stated bar:** signature (work plan vs ACCEPTANCE + no caller), degenerate N-runs (own byte-identical runs), `mq8e` 3-vs-2 tally stated not as consensus. Each tied to bead clause or mutation.
- **Negative measurement in brief:** real Windows junction proves `rmSync` does not follow; brief explicitly bans junction handling — prevents conscientious defensive code against non-hazard.
- **Structural-over-statistical:** Orch's 5bgo generalisation carried forward — fix removed `setTimeout` entirely so race impossible by construction, 1/20/0/20 merely confirmatory; require absence assertion for mechanism-removals.
- **Orch gate verification:** 1653→1689 exactly +36 (5+4+27), files in enumeration + names in output proves executed not discovered; revert guard 3 forward/0 reverts on `loop/run.mjs` branch; three-branch batching only on disjoint hunks (`publish.mjs` 126–134 vs 205–208) + subsystem attribution.
- **Directive as question:** reparked directive gave route to disagree + anchor-trap constraint; author declined doubled-date suggestion, improved different sentence, still cleared `mismatched` — avoids handing author the writer's error.
- **Holds that held:** gates held deliberately during ovrk timing; chain stopped before merge window when lock contended; en3s held until gates merged (expired baseline `lvba`); no `bd close` after counter-instruction (gates worker ran none).
- **Reviewer independence:** reproduced load (own 1/20/0/20 harness), re-derived restoration, mutated plausible neighbour (`>`→`>=` kills 3 incl. added invariant), mutated `log:()=>{}` etc., disclosed `bd show mq8e` + timed-out `npm run build` probe.

## 7. PATTERNS
- **Guardrail whose output truncated:** 4 inherited briefs from `bd show | head -N` deleting ACCEPTANCE at bottom; x2jl fourth requirement missed with no truncation (shape of reading — numbered list vs prose imperative). Fix: diff brief vs every imperative, not enumerated list; generator refuses brief omitting an issue.
- **Check narrower than property (namesake memory, 5th instance added):** `if (graph.dateModified)` requires agreement only when present; two consumers of one fold as independent oracle; `deepEqual` for bytes; vacuous `diff <path> <path>`; swapped e2e→unit dropping integration edge (ovrk `d1ki`); source guard for call site. Diagnostic grows to: what would this still pass on? independent or one computation? require presence? could experiment return constant? can proof fail at all? (asked of command).
- **Artifacts that cannot fail:** vacuous diff proof, identical-arms MUTATION test (Orch error count), `echo` exit 0 masking script exit 3, presence-check double-escape, sealer would-have-looked-fine on stale file. Cost tonight: at least 5 instances, one mine.
- **Shape read as scope:** 15-file 91s diff as work (older base reversals); "NEW 2" string occurrences as two directives (parser says one); pending-10 as incident vs landed-5 as normal route (mem-cond better number); 21→23 hedged as arrival (claim count constant 23).
- **Two derivations sharing instrument are one:** sealed reviewer flipping only after reading prior round; bead-note side channel (`mq8e` probe) around file seal; task committed at selection outliving source (brief outlives DIRECTIVES.md; `lvba` opposite direction — claim decayed vs brief outlived source).
- **False/true same observation:** N-runs identical figures demanded as missing proof (sampling harder can't fix deterministic gzip); 0-failure quiet-machine 20/20 vs tail-event 20/20 (need competing load).
- **Guardrail gating two jobs:** HOLD.md breaker + publish gate; suppress one removes other (`ml25`); mechanical-keys exemption (`timeline`, `domains_seeded`) as guardrail not firing on own machinery.
- **New pattern — early return as invisible skip:** reviewer verifies new path does what it claims, never asks what it jumps over (`q6xp`). Control-flow change, not just behaviour change.
- **New pattern — arrival vs ageing:** hedged-count rise without claim-count rise; check cannot warn author because compliant on day of authoring (green then red overnight in someone else's run). Points at `6nrk` derived-fact vs prose hedge.
- **New pattern — log vs bead editing:** log keeps wrong turns, bead must not instruct with them (every fragment carries refutation). Replacing vs quoting+refuting both satisfy if fragment-reader can't take wrong half.

## 8. MECHANISM vs SUSPICION TALLY
- **Mechanism (build/tests/parser/counters caught):** hedge v1 rebound (build error changed 07→06); lock-wait wiring hole (mutation delete-site stays green); advanced-clock `deepEqual` gap (probe true/false split); flake-adjacent? No — suspicion first; `mismatched 1` (verify-launch); resume cause (dry-run first lines + own parser 44→1); breaker arithmetic (loop's own counter 2 consecutive, `l.type===type`); merge-tree clean/revert-guard counts; enumeration+name-in-output gate proof; 40 Pulse files exempt via `MECHANICAL_KEYS` + `mismatched 0`.
- Count: **9** mechanism-led.
- **Suspicion (person re-derived/measured/doubted):** phantom file + narrow grep self-corrections; collision pids; brief-vs-bead audits ×4 + imperative diff (invariant); serial-guard prefix; false date (`out/` 19:24); vacuous diff path; degenerate N-runs + own byte-diff; signature overrule; classifier prefix/all-results + timeout note; x2jl codes omission self-charge; `rmSync` junction experiment; 26-run flake measurement + own 20-run tail validation; publish coupling code read (both sessions); early-return skip; unpushed-10/publish sweep; arrival-vs-ageing git (page 08-28, prose 09-07 03:31, zero new pages); parking file-vs-selector reconciliation.
- Count: **19** suspicion-led.
- **Borderline (person drove mechanism):** all mutation proofs (reviewer ran, person judged); 1/20/0/20 loads; 40/40 flake fix (author 20 + coord 20 with outlier); `>`→`>=` kill-3; `slice(0,1)` 31/0 + absent-field `if` verify; independent load reproduction. Named here, not double-counted. For unattended Desk, ratio (~1:2 mechanism:suspicion) is the finding — loop does not yet catch its own narrowing, vacuous proofs, classifier scope, or publish coupling.
- Reasoning-absence caveat: Luna Boss 0/888 reasoning blocks recoverable; abrupt overrules may reflect deliberation not kept, not absence. Do not claim "decided without reasoning" where "reasoned off-transcript" fits.

## 9. WHAT I COULD NOT TELL FROM THIS CHUNK
- Batch-2 final-review verdict beyond "one finding" + `d1ki` overstatement correction — does it confirm `84s8`-class common mode across `loop/run.mjs` triple + publish path (`zuoo`/`ml25`)? Needs chunk 02.
- Exact corrected `d1ki` wording and whether ovrk's in-process swap still leaves integration edge uncovered.
- `ml25` third-state design, `vqbo` fix (empty-diff re-review + record-without-change + resumable-brief), `q6xp` 2-line fix, `mq8e` halves — all filed open, resolution outside chunk.
- Whether gates `8502757` + batch-2 `67ddf62` stay green past snapshot advance; whether `6nrk` derived-fact lands.
- Serial-scope (loop-only vs machine-wide) and memory-triage (which distilled statements load every `bd prime`) — explicitly left to maintainer, still open.
- Full cost accounting: implementer+reviewer pairs per bead (gates 5×5 most expensive) vs `bd ready` ~90 — needs later chunks for opportunity-cost judgment.

