> **PROVENANCE.** `opencode-go/muse-spark-1.3-contributor` at `--variant
> xhigh`, dispatched 23:24 local on 2026-09-09, read-only at `HEAD 95bb03e`.
> Same inputs as its sibling, no sight of it.
>
> It **discounted its own agreements before being asked**, in its own §4: *"two
> derivations sharing an instrument are one. Where I agree with the unseen
> sibling agent, discount it; the disagreements above are the product."* The
> unified report follows that instruction.

---

# WISDOM CONVERSION — Muse Spark — 2026-09-09

All dates below are this machine's LOCAL dates (UTC-6) unless marked UTC-quote from the notes. Verified read-only at HEAD `95bb03e` on 2026-09-09 local.

## 1. YOUR OWN ANSWER FIRST

Formed from the eight notes before weighing the architect's text.

The corpus did not fail for lack of sentences. It failed the way the notes keep showing: a correct sentence sat somewhere while the hand wrote something else. Four briefs built from truncated issue output deleted ACCEPTANCE and inverted it (note 01, nq36/gates/ovrk/5bgo, `mq8e`); a paraphrased task dropped one word and cost a full round on byte-identical production (note 04/05, "both" spawners, `gates.mjs` wired while `verify-launch.mjs:1026` stayed exit-status-only); a definitive scan of the issue store could not see the field the answer lived in (note 03, `comment_count` vs bodies, `h0z0` comment); a restated table went stale four times (note 07/08, seven vs nine conformance records). None of those authors lacked the rule. Each lacked a refusal at the moment of writing.

So: convert at the point of use, and delete the rest. But "point of use" has to mean a host that exists in the tree and fires without a person remembering. I checked the four hosts claimed:

- `scripts/*.test.mjs` auto-enrolled via `scripts/run-tests.mjs`: EXISTS. `SEARCH` list covers `app, lib, loop, pulse, scripts, tests`, every matching file handed to the runner. Confirmed by read.
- `loop/lib/brief.mjs` `GROUND_RULES`, mechanically copied into Desk briefs and review briefs: EXISTS. Exports plus two assembly sites and the review import confirmed by search.
- The brief linter as a tree-resident refusing script: DOES NOT EXIST at HEAD. Repo-wide search finds only evidence-banked mentions and design prose, no executable under `scripts/`, `loop/`, or `tools/`. It is process in scratchpad, not mechanism.
- The second-model brief review as a runnable gate: DOES NOT EXIST in the tree. No `loop/` entry point runs it; it is manual dispatch plus banked verdicts.

That changes the price tags. Anything routed to hosts 1 or 4 is cheap. Anything routed to hosts 2 or 3 is storage again until the host itself is committed, tested, and seen refusing. A proposal naming a host that does not exist fails its own read-before-proposing bar.

My answer, grounded only in instances with ≥3 outings:

1. Refuse new test arms that were never seen red. Instances: B1 mutation table three nils plus findings about itself every round with 1 caused / 0 saved (note 05/06, killed at `047e9bf:55-57`); `chars:used` present untouched through two completeness rounds (note 06); shortfall-any-digits green at 23/23, then `:357/:375` no-numbers, then `:381` bare assert — five consecutive relocations of one class (note 06); absent-check fixture with no absent entry, extension-drop mutation with lone scanned file left (note 07). The survivor already adopted — reviewer mutates the merge-base diff, no-arm line is a finding — is structural because a self-list cannot contain an undefended-correct line (r5 0/3 vs diff-list 3/3).
2. Require every enforcement claim to answer what it would still pass on. Instances: seven briefs naming only one test file while the enforcer was a different file (note 06, portability named, `no-change-dir-refs` enforced; six faithful sealed reviews reported an untested property clean); ledger `outcome:done` true in the broken world, fallback-only publish regex, cap hook never receiving options (note 02, ml25 triple); filename-for-alias vs alias registry with four records (note 02, jqs); debt file enumerating bindings not transclusion closure (note 02, 226f, 48 facts / 33 transclusions / 12 builds).
3. Refuse truncated enumeration at the source. Instances: four `head`-truncated briefs; UTC 1.17 vs local 1.43 understating the exact complaint (note 03); 111-title scan missing the comment field; `both:63` read past to quote single-sided 80/84 (note 03); announced sha vs logged sha after the tree moved between reads (note 05, `3f748af` vs `a2eec9f`).
4. Enforce closure, not file lists. Instances: B2 authority-shape and additive-keys pins outside the listed files, E repeating the same class as the fifth instance, F third-assertion omission plus targeted command omitting the holding file (note 07). Discipline without a pins search did not hold across three packets.
5. Treat figures as guilty until sourced. Instances: invented push label vs actual read time making the site look staler than measured (note 08); hold estimate vs actual; 182.4M read while three counters incremented (note 03, corrected to 182,873,547); 56-test prose over 20+16+15+4=55 found unprompted by the sealed reviewer (note 08); 205 vs 219 removed with 14 tilde entries missed by the repair regex (note 06); correct arithmetic on an invented 10% input one message from permanent law (note 08, self-retracted).
6. Keep judgments that must stay judgments, but give them owner and expiry instead of calling them checks. Instances: withholding `:381` / helper call-site to grade independence (note 05/06/07); split-verdict merge disposition with recorded twenty-second seal exposure and an eighth undeclared edit judged benign (note 08, C1); addendum-close instead of third round; inferred-green push refused at 5/6 plus isolated green (note 06, "an inferred green is not a green"); fourth-review decline on lane boundary (note 08). Mechanising disclosure destroys the grading value; deleting the record destroys the reasoning.

## 2. VERDICT ON EACH OF THE ARCHITECT'S SIX PROPOSITIONS

- **P1 — MODIFY.** Direction agreed: storage lost load-bearing lessons. The 7/3/1 count is not solid enough to govern everything else. Small n from one window; engagement counts come from raw transcripts outside the notes and are unverifiable here; first-half session is state 2 by construction (zero index loads over 149 entries) and must not set the price of the fix. Deeper: the preamble itself says a one-line index rule only prompts a pause and the full entry must be opened before acting — so an index-load count overstates "in context." Some counted state-3 instances may be state 2 with the body never opened. That weakens the exact ratio but strengthens the conclusion: loading more lines is not the remedy; arrival without being sought is.
- **P2 — MODIFY.** The rule as stated ("becomes something that runs and refuses, or gets deleted") would delete the highest-value records in the notes. Counter-instances from the notes, not hypotheticals: the C1 split-verdict disposition, the addendum-close of the six-line finding, the recorded refusal of the inferred-green push, the tbho four-criteria refusal to close. None can run. Each is valuable precisely as owned judgment with reasons stated. Replace with: every finding either becomes something that runs and refuses, or becomes recorded judgment with a named owner and an expiry, or gets deleted. Unowned prose is not a third option — it is deleted.
- **P3 — MODIFY.** Two hosts exist and fire (test files, ground-rules copy). Two do not exist as hosts (brief linter, second-model review) — they are valuable processes with banked evidence, currently scratchpad-resident. Until each is a committed file with its own red-then-green proof, conversions addressed to them are addressed to storage. Also incomplete even for existing hosts: ground rules reach Desk jobs but not fresh sessions or hand-spawned runners unless copied; suite checks judge after writing, they do not refuse at composition time. Name both gaps in the work order.
- **P4 — MODIFY.** Wrong ordering and one wrong host. My rank by instances × cost avoided over build cost: (1) check-narrower enforcement-claim question — dominant class, ~20 instances, each cost rounds; (2) seen-red for new arms — ≥10 instances, each cost false-green suites and defence rounds; (3) truncation-pipe refusal — few lines to build, caused backlog understatement plus two false definitives; (4) closure over file lists — recurrent across B2/E/F, each cost a full tests-only round plus re-gate; (5) figure provenance — fabricated inputs reached for permanent law twice. Drop the two-derivations check from the linter: a regex cannot tell whether two derivations share an instrument without false-positive fires on legitimate shared fixtures (the 3/112 vs 10/111 agreement-on-zero case proves the subtlety). That one belongs in the second-model prompt plus a disposition rule, not in the linter.
- **P5 — AGREE with cost tiering.** The bar stands; the demonstration price varies. Expensive to demonstrate: run-scoped lock contention (needs live overlap), stream-separation on success paths, second-model prompt checks (45–115K tokens per outing in note 07). For those, accept wild-found red preferred, else fixture-injected red with the run recorded — never a prose claim of redness. Re-demonstrate on host change. What cannot be shown refusing does not ship as a check; per P2-modified it may ship as owned judgment with expiry, never as slogan.
- **P6 — AGREE.** The delete bin should be large. The analysis already models this by demoting its own slogans. Add: second copies (restated tables, restated scan paragraphs, duplicated mutation indexes) delete by default in favour of deferral to the quoted standard — the notes show patching the copy never holds while removal does (F index structural fix, conformance passage deletion).

Answers to the five invited attacks: (1) Yes, P2 is too strong — instances above: withheld-to-grade design, split-verdict disposition, addendum-close, inferred-green refusal, criteria-gated non-close. Each is valuable, unmechanisable without destroying its function, and lost by deletion. (2) No, P1's inference is directionally sound but the ratio is soft — small n, engagement data outside the notes, index-line vs body-open ambiguity means some state-3 may be state 2; ranking individual lessons by those counts is not supported. The policy conclusion survives because both readings demand point-of-use mechanism. (3) Ranked above; drop the linter-hosted corroboration check, move it to the reader prompt. (4) P3 breaks at: author-composition time (suite judges after the defect is written), fresh sessions and hand-spawned runners (ground-rules copy is not inheritance), and the two uncommitted hosts (linter, second model) which reach nothing until committed. Finding with no host reaching its point of use: the "what would this still pass on" question for brief prose — no tree-resident check asks it today. (5) Demonstrating refusal is most expensive for contention-dependent and model-read checks named above; replacement is tiered demonstration (wild red, else recorded fixture red with run output) plus expiry, never waiver.

## 3. RANKED CONVERSION LIST

Ranked by (instances × cost avoided) ÷ (build cost). Each names the existing host, the refusal, and the negative control that must go red before trust.

1. **Seen-red for new test arms.** Host: committed brief linter once it exists; interim host `scripts/` meta-test asserting the packet's own red evidence (no new unowned gate until the linter lands). Refuses: a brief adding an arm without naming its red run. Negative control: re-introduce the B1 table arm and the `:381`-class bare assertion — both must be refused as unproven; the merge-base-diff mutation path must show red-then-green by hash with identical restorations. Bar from the notes is already written: a check never seen refusing is not yet a check; the 22:50 wild-found citation red is the strong form.
2. **Enforcement-claim interrogation.** Host: committed linter (extends the existing enforcement-claim check). Refuses: "enforced by tests" naming an instrument that does not contain the property (seven-brief class: named file vs `no-change-dir-refs`; ledger-done, fallback-only, unreceivable-cap shapes). Negative control: a brief claiming the old seven-brief wording must be refused; a brief naming the true enforcer plus the red run passes.
3. **Truncation-pipe refusal.** Host: `scripts/` (extend the existing shell-token guard family or add `no-truncated-enumeration` test scanning `scripts/`, `loop/`, `pulse/`). Refuses counting or enumerating commands piped through output shorteners. Negative control: the exact 1.17-script shape and the title-only store scan shape must go red; whole-output count-first shapes stay green. All evidence reads in this task used full output; no shortener was run.
4. **Closure over file lists.** Host: review gate plus `scripts/` pin-search (brief file list reconciled against merge-base diff; reviewer mutates the diff, not the self-list). Refuses: briefs whose listed files miss a pin the diff touches (authority-shape, additive-keys, third ledger assertion shapes). Negative control: B2/E/F round-1 brief shapes must be refused; corrected shapes with closure pass.
5. **Figure provenance.** Host: extend `scripts/mem-log-integrity.test.mjs` beyond structure (entry stating a figure carries the command or file that produced it). Refuses: unsourced counts and invented labels. Negative controls: 56-over-55 prose, invented push label, incrementing-counter total, tilde-missed removal count — each must fail until sourced; sourced re-statements pass.
6. **Run-scoped locks, writer-discriminating guards, per-run gate dirs.** Hosts: existing lock and gate scripts (finish landed work: explicit-clearance guard refusal, hold wording as no tracked-file writes, per-run evidence dirs, consulted-fixture assertions). Refuses: gate evidence from the wrong run, recompute-vs-restore conflation (Pulse 0→1 shape), gap-sampled liveness claims. Negative controls: FULL-MEM-LOG-inside-window run must be discarded and re-run; derived-recompute fixture must not refuse while same-value job restore must; gap-sampled guard proof must fail.

DELETE rather than convert (eight classes, named so the bin is countable): (i) bare slogans with no paired instrument ("be suspicious," "verify before concluding" unadorned); (ii) second-copy restatements of records (stale-prone tables, scan-paragraph copies, duplicated mutation indexes — defer to quoted standard); (iii) "no green mutant" as a bar (already withdrawn in the notes for the attainable delta rule); (iv) optional cites without an adoption mechanism; (v) unowned estimates presented as measurements; (vi) severity labels without the red run attached; (vii) file-list-as-closure assertions; (viii) corroboration claims naming no instrument per derivation. That is the delete bin; treat its size as success.

## 4. WHAT YOU CANNOT DETERMINE

- Tool results were stripped from the source, so every "measured" number (302 files, 63 basenames, pid start times, mtime windows, suite counts 1727→1817, token attributions) is taken on prose authority — exactly the failure mode the notes warn about. I verified hosts and file shapes, not the readings.
- Reasoning blocks are ~2% recoverable in one session and zero in others per the analysis; abrupt merges, approvals, addendum-closes, and let-it-run rulings may be reasoned off-transcript. "Decided without reasoning" is unsupported over "reasoned where the transcript does not keep."
- Whether suite-count rises are the same ruler or search-scope inflation; whether token totals double-count cached input across rollouts (attribution claimed unambiguous only for the single-rollout run); whether provider aborts share a root cause; whether the model-vs-model comparison generalises beyond brief-review texts (small shared sample, one real miss).
- Open at the last note with no later chunk to settle them: sealed verdict on the final author diff and whether the withheld helper call-site was found independently; whether pending packets and the deferred triage items landed; whether pattern, gate-lock, escalation-join-arm, exact-instant export, and queued registry commits landed; whether the citation trap stayed untouchable; whether publish stayed off to the last packet.
- Per-session engagement data behind the 7/3/1 ranking lives outside the notes and was not re-derived here; my P1 modification rests on the preamble's own index-vs-body distinction plus the stated method limit (first-commit arrival dates are upper bounds — lessons can be older, never newer — so pre-date relearnings may still have been available).
- One structural caveat from the notes I adopt: two derivations sharing an instrument are one. Where I agree with the unseen sibling agent, discount it; the disagreements above are the product.
