# DIRECTIVES.md — the maintainer's work, first in line

The highest-priority of the loop's three work sources (`specs/loop`). Write
one directive per list item; the Desk takes them before the derived queue and
before proposals. Nothing else writes to this file except the completion
marker described below.

**Form — one line per directive, job type first:**

```
- <job-type>: <what you want done>
```

`<job-type>` is one of the closed list in `specs/loop`: `interpret`,
`verify`, `entry`, `tutorial`, `post`, `education`, `repair`, `prune`,
`machinery`. The type is required because every selector rule — the budget
ceilings, the upkeep floor, the wall-clock cap, capacity shedding, the blog
ceiling — is keyed by it. A line without a recognised type is **skipped with
a loud warning naming the line**, never guessed at: guessing the type would
spend the wrong budget under the wrong cap.

**Completion marker.** When a directive's job completes, the loop appends
`[done <date> <job-id>]` to that line and skips it forever after. A directive
is never silently re-run. Removing finished lines is yours, at leisure.

```
- repair: the OpenRouter link on the pricing entry 404s [done 2026-09-03 j-20260903-01]
```

---

<!-- directives below this line -->

- verify: re-verify the five cited facts on `org/z-ai` against their sources. Fetch each `source_url` and confirm the page still says what the fact claims, then update `accessed` where it holds and report anything that no longer matches rather than quietly correcting it. This is upkeep, which the specs rank above new writing for a reason: a fact that has drifted is worse than a fact that is missing. [done 2026-08-29 j-20260829-02]

- entry: write a prose body for `org/z-ai`. Its record already carries five sourced facts and five dated timeline events and no prose at all, so this is writing rather than research — read what the record already establishes and find the story in it. Apply the editorial bar in `specs/editorial`: it must give an enthusiast something they did not know or assemble scattered things for the first time, be specific with dates and numbers and mechanisms, and be worth linking. Do not restate the facts table in sentences — that is on the cut list. Every new claim needs a source you actually fetched. [done 2026-08-29 front-load wave, branch fl/orgs commit 8df93fd — written by a hand-spawned agent, NOT by a loop job, so there is no job id to name; see beads addictedtoai-g1l]

- entry: re-review `content/wiki/model/moonshotai-kimi-k2-5.md`. Its approval record (`data/reviews/j-20260831-10.md`) was written against bytes that have since changed, so `verify-launch` reports the piece `REVIEWED THEN CHANGED` and the launch minimums are not met. Read the entry as it now stands and judge it on its own terms; this is a re-review, so the page may well need no edit at all. Confirm in particular that the opening no longer rests on the vanished `expiration_date` feed field, that the `deprecated`-versus-`active` disagreement between this page and the derived catalog row is still explained rather than asserted, and that no row-count census is stated without an anchor the page's own transclusions share. Context, offered as context and not as a conclusion to accept: the entry was authored and approved by job j-20260831-10, then edited under `addictedtoai-7q8`, which replaced a flat "388 rows in the OpenRouter snapshot" census with a qualitative claim needing no date. That edit was made directly on `main` by a repair sweep and so never passed the review gate, which is why this line exists. Written by the orchestrator rather than the maintainer, because a `mismatched` record cannot reach the derived queue (`addictedtoai-ccky`) and this is the only input that reaches the Desk. [done 2026-08-31 j-20260831-11]

- entry: re-review `content/wiki/model/moonshotai-kimi-k2-5.md` again. Its most recent approval (`data/reviews/j-20260901-14.md`) was written against bytes that have since been reverted, so `verify-launch` reports the piece `mismatched` and the launch minimums are not met. Read the entry as it now stands and judge it on its own terms; a re-review may legitimately conclude the page needs no edit at all. The specific thing to weigh: the page declares `status: deprecated` while the derived catalog row reads `active`, and the body explains why — Moonshot's documentation files `kimi-k2.5` under "Deprecated Models" and says calls return a 404, while OpenRouter cleared the expiry its catalog status derives from. Judge whether the page explains that divergence rather than merely asserting it, and whether a reader who wants to know "can I call this model" is answered. Context, offered as context and not as a conclusion to accept: job j-20260901-14 flipped this field to `active` and rewrote that paragraph to follow the router; the orchestrator reverted both under `addictedtoai-qupq`, because `addictedtoai-ij4h` had measured all 396 feed-bound entries days earlier and ruled that a stub's status derives from its feed while a reviewed entry's authored claim stands. You are not being asked to ratify that revert — you are being asked whether the page as it stands is sound. Written by the orchestrator because a `mismatched` record still cannot reach the derived queue. [done 2026-09-01 j-20260901-15]

- repair: three Anthropic "(Fast)" rows vanished from the OpenRouter feed together and the derived queue filed them as three separate items. Handle all three in this one job, as one cohort: `content/wiki/model/anthropic-claude-opus-4-7-fast.md`, `content/wiki/model/anthropic-claude-opus-4-8-fast.md`, and `content/wiki/model/anthropic-claude-opus-5-fast.md`. What was measured before this line was written: all three retirement events carry `date: 2026-09-02`, the same source (`openrouter-models`), the same `$retirement` kind, and each row was `active` with `expiration_date: null` immediately before vanishing — so the rows were withdrawn without an announced expiry rather than expiring. That pattern, plus the fact that every affected row is the `-fast` variant of a Claude Opus model while the non-`fast` rows of the same models are untouched, is evidence of ONE vendor or router event affecting a whole tier, not of three unrelated disappearances. Your job is to establish what actually happened and make the three pages honest about it. Find out, from Anthropic's and OpenRouter's own published sources, whether the Fast tier was retired, renamed, folded into standard service tiers, or merely delisted by this one router — these are materially different facts and the pages must not assert the wrong one. If the sources do not settle it, say so on the pages rather than guessing: `blocked` is a successful outcome here and a plausible invention is not. Do not delete the entries or their feed bindings. Bound facts already render their last-known values with an as-of date, which is the designed behaviour and needs no repair; what needs writing is the prose that tells a reader whether they can still call these models. Note for whoever reads this later: this was batched deliberately. Three separate jobs would each have independently rediscovered the same single cause at roughly three times the cost, which is the argument `data/proposals/batch-cohort-vanished-row-repairs.md` makes and which a holistic backlog review on 2026-09-01 independently reached. Written by the orchestrator, not the maintainer. [done 2026-09-02 j-20260902-01]

- interpret: the OpenRouter feed moved `z-ai/glm-4.5v` from `active` to `deprecated` on 2026-09-02, and the change is sitting in `data/changes.jsonl` uninterpreted. Say what it means for a reader. The question a reader has is the plain one — can I still call this model, and if not, what should I use instead — so answer that, from Z.ai's and OpenRouter's own published sources rather than from the feed alone. The feed records that a status field changed; it does not say whether the vendor retired the model, whether it is still callable during a deprecation window, or whether a successor exists. Those are different facts and the interpretation must not assert the wrong one. Relevant precedent, offered as context rather than as a conclusion: `addictedtoai-ij4h` measured all 396 feed-bound entries and ruled that a STUB's status derives from its feed, while a REVIEWED entry's authored claim stands — so check which of the two `content/wiki/model/z-ai-glm-4-5v.md` is before deciding whether the page's own status should follow the router. If the sources do not settle what happened, report `blocked` and say what you could not establish; that is a successful outcome and a plausible invention is not. Written by the orchestrator, not the maintainer, and routed through this file rather than left in the derived queue only because `addictedtoai-u0n5` currently blocks everything below rank 85. [done 2026-09-02 j-20260902-02]

- repair: apply BOTH carried findings that job j-20260901-23's reviewer left against `data/proposals/glm-5-3-license-revenue-gate.md`, in this one job, and delete both carry files when done. They are `data/carried/j-20260901-23-carry-1.md` and `data/carried/j-20260901-23-carry-2.md`; read them in full, they state the evidence. In summary: (1) the proposal's summary field still says GLM-5.3 is "state of the art on CyberGym and ExploitGym", but the model card's own table puts GLM-5.3 THIRD on the ExploitGym row and the card's prose claims SOTA for CyberGym only — narrow the claim, and if the ExploitGym gain is worth keeping, state it the way the card does rather than as a superlative; (2) the evidence text calls the two budgets "the 2-hour" and "the 6-hour", which reads as wall-clock, when the card's footnote says they are API inference time rescaled by per-model tokens-per-second — add that qualifier so a later post inherits it instead of rediscovering it. Verify both against the card itself rather than against the carry files' quotations of it: a reviewer's suggested wording is a hypothesis, not evidence, and this repository has had a reviser copy a figure out of a review that was right only by luck. Retirement is by deletion — the carry files must be gone from `data/carried/` in your diff, or the findings will be re-queued every run. Batched into one job because both findings are about the same file, which is the shape a holistic backlog review on 2026-09-01 recommended over one job per finding. Written by the orchestrator, not the maintainer. [done 2026-09-02 j-20260902-03]

- repair: apply BOTH carried findings that job j-20260902-02's reviewer left against its `z-ai/glm-4.5v` interpretation annotation, in this one job, and delete both carry files when done. They are `data/carried/j-20260902-02-carry-1.md` and `data/carried/j-20260902-02-carry-2.md`; read them in full, they state the evidence and cite line numbers. In summary: (1) the annotation ends "the marker has now come and gone twice in four days", but the corpus records two TRANSITIONS, not two round trips — `expiration_date` was present before 2026-08-29, absent in the 2026-09-01 snapshot, and present again on 2026-09-02 — so "has now moved twice in four days" says the same thing and is what the records actually show; (2) the annotation cites the endpoints URL inline for its callability claim but attributes a quoted schema definition only to "OpenRouter's own models API schema" — the quote is exact and the attribution is accurate, but the document is `https://openrouter.ai/openapi.json` at `components.schemas.Model.properties.expiration_date`, and naming that URL the way the sibling claim names its own would let a later reader check the quote. Note that the carry file records the rendered docs page `https://openrouter.ai/docs/api-reference/list-endpoints-for-a-model` as returning 404 — confirm that yourself before repeating it, because a claim that a URL is dead ages badly and this repository has been caught by exactly that. Verify both findings against the sources rather than against the carry files' quotations of them: a reviewer's suggested wording is a hypothesis, not evidence. Retirement is by deletion — both carry files must be gone from `data/carried/` in your diff, or the findings will be re-queued every run. Batched into one job because both are about the same annotation, and routed through this file rather than left in the derived queue only because `addictedtoai-u0n5` currently blocks everything below rank 85. Written by the orchestrator, not the maintainer. [done 2026-09-02 j-20260902-04]

- repair: apply the carried finding in `data/carried/j-20260902-01-carry-1.md` and delete that file when done. Read it in full; it states the measurement. The problem is live on published pages: `content/wiki/model/anthropic-claude-opus-4-8-fast.md` and `content/wiki/model/anthropic-claude-opus-5-fast.md` each assert in the SITE'S OWN VOICE that the old slug "still answers and still serves". That sourcing is sound as far as it goes — OpenRouter's own notice says exactly that, and the pages quote and cite it — but the router's endpoints API disagrees in shape: `GET https://openrouter.ai/api/v1/models/anthropic/claude-opus-4.8-fast/endpoints` returns HTTP 200 with the model record and an EMPTY `endpoints` array, and the same holds for `claude-opus-5-fast` and `claude-opus-4.7-fast`, while the surviving standard rows return 8, 11 and 10 endpoints respectively. Re-measure all six of those yourself before writing anything — the counts are from 2026-09-02 and a count is exactly the kind of claim that rots. What NOT to do: do not assert that the models are unroutable. Nothing published resolves whether an empty endpoints list means the slug is aliased internally or genuinely dead, and it cannot be settled without an API key, which you do not have and must not seek. The repair is a clause that lets a reader who runs the same check keep trusting the page — something of the form "the model record still answers, though the router's endpoints listing for it is now empty; OpenRouter's notice is the authority that requests still route". Check whether the 4.7 page makes the same claim and needs the same clause; the finding names only 4.8 and 5. Retirement is by deletion — the carry file must be gone from `data/carried/` in your diff. Routed through this file rather than left in the derived queue only because `addictedtoai-u0n5` currently blocks everything below rank 85. Written by the orchestrator, not the maintainer. [done 2026-09-02 j-20260902-05]

- entry: re-review `content/wiki/model/anthropic-claude-opus-4-7-fast.md`. Its approval record (`data/reviews/j-20260902-01.md`) was written against bytes that have since changed, so `verify-launch` reports the piece `REVIEWED THEN CHANGED` and the launch minimums are not met. Read the entry as it now stands and judge it on its own terms; this is a re-review, so the page may well need no edit at all. What changed and why, offered as context rather than as a conclusion to accept: the local date rolled to 2026-09-03, the page's transclusions began rendering from the 2026-09-03 snapshot, and its sentence "All three Anthropic fast rows left the catalog in that same snapshot" became a census anchored to 2026-09-02 that the build refuses (`snapshot-census`, addictedtoai-pxx1). The orchestrator added the corpus's established hedge — the exact marker `as observed on 2 September 2026` — which cleared the build. Confirm that the hedge is the right treatment here rather than a re-dated count: the claim describes a one-time event in a past snapshot, so it is a claim that should age rather than be updated, but that is the judgment to check. Confirm also that the sentence still reads in the past tense and that the paragraph's other dates (2026-05-12, 2026-05-27, 2026-07-24, and the "last-known values as of 2026-09-01" line) are untouched and still true. Written by the orchestrator rather than the maintainer, for the reason the K2.5 line above records: a `mismatched` record cannot reach the derived queue (addictedtoai-ccky), so a directive is the only input that reaches the Desk, and this one routes an unreviewed edit THROUGH the review gate rather than around it. [done 2026-09-03 j-20260903-01]

- repair: convert the two remaining PROSE price literals in the corpus from per-single-token to per-million-token, so they agree with what every entry page, the catalog and the changed feed now render. Both are on vanished-row pages, where a literal is the only option because the row is gone and a transclusion has nothing to bind to. (1) `content/wiki/model/anthropic-claude-opus-4-7-fast.md`, the sentence beginning "its last-known values as of 2026-09-01", reads `0.00003 in / 0.00015 out per token against the standard row's 0.000005 / 0.000025 — six times the base`. Per million that is `$30.00 in / $150.00 out` against `$5.00 / $25.00`, and the "six times the base" clause stays true because a ratio is scale-invariant — confirm that arithmetic yourself rather than taking it from this line. (2) `content/wiki/model/nvidia-nemotron-3-ultra-550b-a55b-batch.md` lines 52-53, "whose prompt rate fell from 0.000000625 to 0.0000006 and whose completion rate fell from 0.000003125 to 0.0000024 per token" — per million, `$0.625 to $0.60` and `$3.125 to $2.40`. Write those exactly rather than rounding to two decimals: the sentence is ABOUT a change, and `formatPrice`'s two-decimal rule would render 0.625 and 0.60 as `$0.63` and `$0.60`, which blurs the very delta the sentence exists to state. Do not touch the third file a grep for small decimals finds, `content/tutorials/model-file-header-range-requests.md` — its `0.000009999999747378752` is a `layer_norm_rms_epsilon`, not a price. Do not change any date, any source citation, or any surrounding claim; this is a units change and nothing else. Why this is a directive and not a queue item: both pages carry `reviewed:` records bound to their current bytes, so an unreviewed edit would make them `mismatched` and fail the launch minimums — routing it here puts the edit THROUGH the review gate rather than around it, on the same reasoning as the K2.5 and 4.7-fast lines above. Context, offered rather than assumed: the maintainer read a live entry page on 2026-09-03 and said "it is still displaying the price per single token ... no human wants to know that a token costs $0.000001!", and commit 4a33636 changed all 395 fact-rendered pages; these two prose passages are what that commit could not reach. Written by the orchestrator, not the maintainer. [done 2026-09-03 j-20260903-13]

- repair: the site build is RED and has been since local midnight, which blocks every Desk job's gates and stopped the 00:00 Pulse from deploying. Two snapshot-anchored censuses on `content/wiki/model/nvidia-nemotron-3-ultra-550b-a55b-batch.md` name the 2026-09-03 snapshot while the page's transclusions now render from 2026-09-04, and `lib/snapshot-census.mjs` refuses both: line 51 "three of the nine `nvidia/` rows" and line 56 "66 `:batch` rows". Fix ONLY those two claims. The two carried findings that also name this page are deliberately NOT in this job — they are already one batched queue item and the Desk will reach them once the build is green; folding them in here would hold an urgent build fix hostage to a content revision. THE JUDGMENT TO MAKE, per claim, because the two are different shapes: "three of the nine `nvidia/` rows changed" describes ONE DAY'S DIFF and is a claim that should AGE rather than be updated — re-dating it to 2026-09-04 would assert something about a different day that is very likely false. "66 `:batch` rows remain" is a census of the CURRENT catalog and could legitimately go either way: re-count it for today, or hedge it as of the day it was written. Decide each on its merits and say in `RESULT.md` which you chose and why. THE HEDGE HAS EXACT REQUIREMENTS and a near miss does not clear the check: the claim's own paragraph must carry the literal wording `as observed on 3 September 2026` — that phrasing, with the date immediately after it, in prose and not inside a code span — and the sentence must be put in the past tense to match ("carried", not "carry"). Scoping alone ("as of the 2026-09-03 snapshot", "in the snapshot of 2026-09-03") is explicitly NOT the hedge and will not clear it. The precedent to copy is four lines above this one: job j-20260903-01 hedged the same class of claim on `content/wiki/model/anthropic-claude-opus-4-7-fast.md` with exactly that marker. DO NOT add these to `data/snapshot-census-debt.json` — that ratchet is for instances already wrong, and forgiving these would switch the check off for the whole corpus. MEASURED BEFORE THIS LINE WAS WRITTEN, offered as context to check rather than as values to copy: today's `data/sources/openrouter-models/latest.json` carries `date: 2026-09-04`, 427 rows, **10** `nvidia/` rows and **67** `:batch` rows; `data/changes.jsonl` shows exactly one `nvidia/` row with a change line on 2026-09-03 (the `:batch` retirement itself) and one on 2026-09-04 (`nvidia/nemotron-3.5-content-safety`). Re-derive anything you write from the snapshot yourself — a count is exactly the kind of claim that rots, and these numbers will be a day old the moment the clock rolls again. Do not change any date, source citation, price, or surrounding claim beyond what the two censuses require. Why this is a directive and not a queue item: the page carries a `reviewed:` hash bound to its current bytes (`data/reviews/j-20260903-08.pass2.md`), so an unreviewed hand edit would make it `mismatched` and fail the launch minimums — routing it here puts the edit THROUGH the review gate rather than around it, on the same reasoning as the K2.5, 4.7-fast and price-literal lines above. Written by the orchestrator, not the maintainer. [done 2026-09-04 j-20260904-01]

- machinery: make a gate failure say what kind of failure it was, and stop counting transport failures as defects. THIS HAS NOW COST REAL WORK TWICE IN ONE CHAIN, on 2026-09-04: job `j-20260904-38` authored its repair successfully (7.96 model-minutes, author outcome `done`), then its gate run failed and the whole job was recorded `failed` and its branch left unmerged — and the closing six-gate pass failed the same way minutes later. Neither was a defect. Re-run alone with nothing else on the machine, the same suite passed 1201/1201. `loop/run.mjs:357` ends a failed gate run with the flat note `gates failed`, and `loop/lib/breakers.mjs:64` counts by outcome alone, so three such runs trip breaker 1 and halt the entire Desk on nothing. THE CAUSE IS KNOWN AND SO IS THE SIGNAL: on Windows this machine exhausts ephemeral ports under concurrent load and loopback fetches fail with `connect EADDRINUSE` (`addictedtoai-ar0`); `pulse/tests/helpers.mjs:196` already throws an error whose text contains the literal phrase `This is a TRANSPORT failure, not a logic failure`, and `gateResult.output` is already captured and already passed into `finish`, so the information needed is in hand at the exact point of the decision. THE FIX: when a gate failure's captured output contains that marker, RUN THE GATES ONCE MORE. If the retry passes, the run continues normally and nothing is recorded as a failure. If the retry fails too, record `failed` exactly as today. This deliberately needs no new outcome and no change to breaker semantics, so `specs/loop`'s breaker requirements are untouched — and a real defect still fails twice, which is the property that makes the retry safe. Key on the marker the code actually emits; do NOT pattern-match `EADDRINUSE` or any other guessed error string, because the whole point of the marker is that the emitting code knows what it saw and a downstream string match does not. Retry ONCE, never in a loop. Prove it by mutation as this repository requires: a fixture whose gate output carries the marker must be retried, one whose output does not must not be, and a marked failure that fails its retry must still end `failed`. Written by the orchestrator, not the maintainer. [done 2026-09-04 j-20260904-39]

- machinery: fix job-branch deletion, which has failed silently on every merged job branch since it was written. `loop/run.mjs:1177` calls `deleteBranch(ctx.repoRoot, branch)` while the job's own worktree still has that branch checked out — the worktree is not removed until `run.mjs:1296`, in the `finally` — and git refuses to delete a branch checked out in a linked worktree. `loop/lib/git.mjs:162` then returns a boolean the call site ignores, so the refusal is invisible. MEASURED, NOT INFERRED: a throwaway repo reproducing this exact state (job branch, checked out in a linked worktree, merged into main) gives `git branch -D` -> `error: Cannot delete branch 'job/probe' checked out at '...'` with the worktree present, and success immediately after removing it. 107 merged job branches exist locally and 107 deletions failed, which is 100% — this has never once worked. THE FIX IS TWO SMALL THINGS AND NOTHING MORE: move the `deleteBranch` call to after `removeWorktree`, and read its return value, logging a failure instead of discarding it. `deleteBranch` has exactly one caller, measured — `loop/run.mjs:1177`, import at `:36`, definition at `loop/lib/git.mjs:162`, and no others anywhere in the repository — so you may change how the call site uses it, but do NOT make the function throw: `gitTry` exists precisely so a git failure is a value to inspect rather than an exception, and every other `gitTry` caller depends on that. DO NOT write a sweep that deletes the 107 existing branches — that is separate, riskier work tracked on `addictedtoai-jl09`, and an outage fix should not carry a cleanup. Prove the fix by mutation: restore the original ordering, confirm the new test fails, restore the fix, confirm it passes. A test that passes both ways has proved nothing. The test must build a throwaway repository under the OS temp directory as every other test here does, and must never touch this repository. Written by the orchestrator, not the maintainer. [done 2026-09-04 j-20260904-40]

- machinery: the Desk publishes UNDECLARED, and pushes before its own records exist. Fix both halves together, because each one alone leaves the other's failure intact. THE MECHANISM, measured: `loop/lib/publish.mjs:72` calls the shared step as `fn(ctx.repoRoot, { dryRun, log })` with NO `owned` key, so `pulse/lib/publish.mjs:517` takes its `declared === null` branch and stages `data/`, `content/` and `public/` WHOLESALE — the code there already names this case "the undeclared caller". The Pulse, by contrast, declares exact paths and refuses on foreign dirty content. THE ORDERING, also measured: `run.mjs:1215` runs `publishStep`, and only afterwards come the directive completion marker (`:1220`), the reviewer's noted proposal (`:1253`), the carried findings (`:1279`) and `commitJobRecords` (`:1492`). So a run pushes content to the live site BEFORE the records describing that work exist, and those records then ride out on some later run's push. WHAT IT HAS ALREADY COST, so this is repair and not tidiness: this exact interaction discarded three jobs' records in one afternoon (`addictedtoai-tqpq`, jobs j-20260903-02, -05 and -06, each of which still reported `done`). The wholesale staging commits a proposal's move before `run.mjs` stages that same source path deliberately, `git add` then reports "did not match any files" and exits 128, and ONE unmatched pathspec is fatal for the WHOLE `git add` — every other path in that invocation is lost with it. THE INVARIANT TO ESTABLISH, and you should state in RESULT.md how your change satisfies it: a job's content and that job's own records reach the remote in ONE push, and the publish step never stages a file the job did not produce. TWO ROUTES EXIST AND YOU MUST CHOOSE ONE AND JUSTIFY IT: (a) declare the loop's owned paths, passing them through to the shared step exactly as the Pulse does, so the staging is exact; or (b) move `publishStep` to after the records are written and committed, so one push carries everything. They are not equivalent — (a) fixes the staging but leaves two pushes, (b) fixes the ordering but leaves the staging wholesale — so consider whether the invariant actually requires BOTH, and say so if it does. DO NOT weaken `pulse/lib/publish.mjs`'s existing declared-caller behaviour; the Pulse depends on it and it is correct. DO NOT touch `data/config.json` — it is a reserved path, a job that edits it writes `HOLD.md`, and publishing has deliberately been turned OFF by the orchestrator for the duration of this work; leave that flag exactly as you find it. TESTING, and read this before you start: you CANNOT test this end to end against the real remote, and you must not try. The house pattern is in `loop/tests/publish.test.mjs` — build a throwaway repository with a BARE origin under the OS temp directory, run the step against it, and read the commit back off the REMOTE with `git show --name-only`, because asserting on the local tree or the step's return value misses what actually got published. NEVER write a behavioural test whose failure mode is a real deploy: `addictedtoai-r8k`'s regression test is structural for exactly this reason, since "run it and assert nothing was pushed" deploys the site if the guard regresses. Prove the fix by mutation as this repository requires, and prove BOTH halves separately: a mutation that restores wholesale staging must fail a test, and a mutation that restores the original ordering must fail a DIFFERENT test. If one mutation fails no test, that half is unproven and you must say so plainly in RESULT.md rather than claiming a fix you have not measured. Written by the orchestrator, not the maintainer. [done 2026-09-04 j-20260904-50]

- machinery: the gate retry landed on 2026-09-04 (job j-20260904-39) and CANNOT SEE its signal in two measured cases. Both were found by that job's own reviewer and filed as `addictedtoai-kisa` and `addictedtoai-brsp`; fix them together, because they are one defect wearing two hats — the retry keys on a marker that the emitting side is free to word differently and the capturing side is free to throw away. CASE 1, TRUNCATION (`kisa`): `loop/lib/gates.mjs:154` builds its summary with `r.output.slice(-6000)`, so only the last 6000 characters of each gate survive. `npm test` runs 1201 tests and prints a line per test, so a transport failure raised early is pushed out of that window long before anything reads it, and the retry never fires. The detection must happen on the FULL output at the point of capture, not on the truncated summary — keep the truncation for the human-readable log, where it belongs, and record the decision as a flag computed before anything is sliced. CASE 2, A SECOND WORDING (`brsp`): `pulse/tests/helpers.mjs:197` throws "This is a TRANSPORT failure, not a logic failure" and `pulse/tests/helpers.mjs:299` throws "This is a CONNECTION failure, not a logic failure" — different word, same meaning, same class of machine failure — while `loop/lib/gates.mjs:33` exports `TRANSPORT_FAILURE_MARKER` matching only the first. So one of the two fixture sites is invisible to the retry today. THE FIX MUST BE A MECHANISM, NOT A SECOND STRING: matching both wordings with a wider regex leaves the next fixture free to invent a third, which is how this defect was born. Make the marker text exist in exactly ONE place that both emitting sites and the matcher use, so a new emitting site physically cannot word it differently. A DESIGN CONSTRAINT YOU MUST WEIGH RATHER THAN IGNORE: `pulse/` deliberately has no import edge to `lib/` or `loop/` — that separation is real and is documented as deliberate, so a naive `import { TRANSPORT_FAILURE_MARKER } from '../../loop/lib/gates.mjs'` inside `pulse/tests/helpers.mjs` would be the first such edge and may be the wrong trade for three words of text. Decide where the single definition lives, state the alternatives you rejected, and justify the one you chose in RESULT.md. If after weighing it you conclude that one shared regex covering both wordings IS the honest minimum here, that is an acceptable answer — but say so as a decision with a reason, not as a default. PROVE BOTH HALVES SEPARATELY BY MUTATION, and this matters because two fixes in one job is exactly the shape where one half rides free on the other's tests: a fixture whose transport marker appears EARLY in a very long gate output must still be retried (mutate the truncation fix back and that test must fail), and a fixture emitting the second wording must be retried (mutate the shared marker back to the single hard-coded string and a DIFFERENT test must fail). If either mutation fails no test, that half is unproven and RESULT.md must say so plainly rather than claim a fix that was never measured. Do NOT widen what counts as a transport failure beyond these two markers — a real defect must still fail twice, and that property is the only reason the retry is safe. Written by the orchestrator, not the maintainer. [done 2026-09-04 j-20260904-52]

- repair: FIVE ORG PAGES ARE PUBLISHING SELF-CONTRADICTION RIGHT NOW because a benchmark publisher rebased its index overnight. Fix the prose on `content/wiki/org/anthropic.md`, `content/wiki/org/spacexai.md`, `content/wiki/org/openai.md`, `content/wiki/org/google-deepmind.md` and `content/wiki/org/deepseek.md`, and nothing else. WHAT A READER SEES ON THE LIVE SITE, fetched 2026-09-05 and quoted exactly: "The Artificial Analysis intelligence index the catalog carries went from **not published** on the April release to **not published** in May to 54.1 in July. Two step-ups in ninety-nine days, neither of them charged for." Two of the three numbers the sentence narrates are gone and it still asserts two step-ups. THE CAUSE, measured from the committed snapshot blobs rather than inferred: between the 2026-09-04T06:00:03Z and 2026-09-05T06:00:04Z fetches of `openrouter-models`, rows carrying a number for `intelligence_index` fell from 164 to 52; of the ids scored on both days 50 went DOWN, 0 went UP, 0 were unchanged, and 114 lost their number entirely, median ratio 0.803. Artificial Analysis announced "Intelligence Index v4.2" dated 2026-09-04. That is a publisher rebase, not models getting worse. EXACTLY WHICH TRANSCLUSIONS ARE DEAD, so you do not have to hunt: `anthropic.md` — `opus-4-7` and `opus-4-8` gone, `opus-5` renders 54.1. `spacexai.md` — `grok-4-3` gone, `4-5` 45.5, `4-6` 50.6. `openai.md` — `gpt-5-4` gone, `gpt-5-6-luna` 43.4. `google-deepmind.md` — `gemini-3-1-pro-preview` and `gemini-3-5-flash` gone, `3-6-flash` 40.3, `3-8-flash` 47.1. `deepseek.md` — `gpt-5-4` and `gemini-3-5-flash` gone, `deepseek-v4-flash-0731` 40.8. DO NOT DELETE OR EDIT THE FACTS. `addictedtoai-ego8` already ruled on this shape: "A bound fact that records what a feed said is the honest artefact... the fix is a line of attribution, not the removal of measured data", and `specs/wiki` says the same about prices — the repair is to the SENTENCE, never to the value. THE JUDGMENT, and it is different per sentence rather than one substitution: a sentence that narrates a TRAJECTORY across releases cannot be salvaged by hedging when two of its three points no longer exist, so decide honestly between rewriting it to say only what the surviving values support, and dropping the numeric claim while keeping whatever the paragraph was actually arguing. A sentence whose numbers all survive may still be FALSE — "eight points of climb", "lands within a point", "higher numbers", "what rose" are typed arithmetic over values that fell NON-UNIFORMLY (p25 0.781, median 0.803, p75 0.827), and non-uniform means an ordering can invert. Re-derive every comparative claim against the values as they stand TODAY; do not assume a ratio survived. Say in RESULT.md which sentences you rewrote, which you dropped, and which you checked and left alone because they are still true. DO NOT add a "the index was rebased" aside to every page — that is a fact about the publisher, not about these companies, and five copies of it is the duplication the editorial bar exists to prevent. Why this is a directive and not a queue item: all five pages carry `reviewed:` records bound to their current bytes, so an unreviewed hand edit would report `mismatched` and fail the launch minimums — routing it here puts the edit THROUGH the review gate. Written by the orchestrator, not the maintainer. [done 2026-09-05 j-20260905-13]

- repair: SEVEN MODEL PAGES CARRY THE SAME BROKEN BENCHMARK PROSE as the org-page directive above, from the same publisher rebase, and the same rules apply — fix the sentences, never the facts. Fix `content/wiki/model/anthropic-claude-opus-4-8.md` (`opus-4-7` and `opus-4-8` both gone, `opus-5` 54.1), `content/wiki/model/cohere-command-a.md` (its own value gone, `deepseek-v4-flash-0731` 40.8), `content/wiki/model/google-gemini-3-1-pro-preview.md` (its ONLY transclusion is its own now-absent value), `content/wiki/model/moonshotai-kimi-k3.md` (`kimi-k2-5` gone, own value 50.2), `content/wiki/model/openai-gpt-5-6-luna.md` (`gpt-5-4-nano` intelligence and agentic gone; its coding_index 56.1 SURVIVED, so a sentence comparing all three indices is now half-dead and needs judgment rather than deletion), `content/wiki/model/openai-gpt-5-6-terra.md` (`gpt-5-5` intelligence gone, its coding 74.9 and agentic 37.5 survived) and `content/wiki/model/z-ai-glm-5-1.md` (BOTH its transclusions gone — `glm-5-1` and `glm-5-2`). READ THE ORG DIRECTIVE ABOVE FOR THE MEASUREMENT AND THE REASONING; it is not repeated here, but the two facts that matter are that this was a publisher rebase (164 scored rows to 52, 50 down and 0 up, median ratio 0.803, Artificial Analysis "Intelligence Index v4.2" dated 2026-09-04) and that the drop was NON-UNIFORM, so any surviving comparative claim must be re-derived rather than assumed. `google-gemini-3-1-pro-preview.md` and `z-ai-glm-5-1.md` are the two where every number is gone; those sentences cannot be repaired by rewording around a value and need the paragraph's actual argument recovered or the claim dropped. Same prohibition: do NOT edit or delete the facts themselves, and do not paste a rebase explanation onto all seven pages. Same reason for being a directive: these pages carry bound `reviewed:` hashes. Written by the orchestrator, not the maintainer. [done 2026-09-05 j-20260905-14]

- machinery: the source registry can decline a field that the corpus still binds, and nothing notices. `data/sources/registry.json` gained a `declined_fields` list on 2026-09-05 (job j-20260905-03) recording `benchmarks.artificial_analysis` as "not carried — not a column, not a fact, not an event". `pulse/lib/registry.mjs`'s `validateDeclinedFields` enforces that against `material_fields` and refuses a path that is both declined and material. But "carried" in the corpus ALSO means bound as an entry fact, and that function never reads `content/wiki/**` — so today 48 fact bindings across 29 model entries point at a path the registry formally declines, the build is green, and the contradiction is visible only to whoever happens to look. ADD THE MISSING CROSS-CHECK: a path in `declined_fields` that any content file binds as a fact SHALL fail the build, naming both the registry entry and every binding file. This is the mechanism half of a decision that currently exists only as a written intention, and this repository's own rule is that guardrails are mechanisms rather than instructions. DO NOT resolve the existing contradiction by removing the declaration or by unbinding the 48 — the first discards a measured refusal and the second is a content decision tracked separately; this job adds the DETECTOR and may legitimately leave the build red for a state that is genuinely wrong. If it would go red on merge, say so plainly in RESULT.md and add the existing 48 to an explicit, dated, shrink-only exemption list in the manner of `data/snapshot-census-debt.json` — an exemption that names what it forgives and may only get smaller, never a silent pass. Prove it by mutation: a fixture registry declining a path some fixture entry binds must fail, and one declining a path nothing binds must pass. Written by the orchestrator, not the maintainer.

- interpret: draft the blog/loop OpenSpec change addictedtoai-9c9t for DESK-ORDER-001 §1 — the post front matter `frontier` / `frontier_reason` (F1–F5) / `domains`, the build gate that fails a flag without a cited reason or a valid domain, the scout's standing F1–F5 sweep, and the blog cap clause exempting a flagged story from the three-candidates-per-day cap while the ≤45% new-writing share still binds it. The criteria, the not-qualifying list and the display contract are written out in loops/ui-loop/graph/knowledge/DESK-ORDER-001.md §1 (keeper K30, 2026-09-05) — the change transcribes them into `specs/blog` and `specs/loop` deltas with tasks, it does not re-decide them. F2 AND K24 COLLIDE AND THE COLLISION IS SETTLED; THE CHANGE MUST WRITE THE RESOLUTION DOWN OR IT WILL BE RE-LITIGATED. F2 qualifies "a lead change on a published index, or a rescoring that moved a leader", while K24 forbids rendering an index VALUE until republication rights clear — and Artificial Analysis's terms URL 404'd on 2026-09-05, so no value from it may render. The resolution (ui-loop K44, anchored in K24's own wording): an F2 record MAY describe a rescoring qualitatively, because the publisher's ACT is not the publisher's NUMBERS. Permitted in the record's copy: the publisher, the index name and version, the date, the direction of the rescoring, the coverage change as a count of rows scored before and after, and the fact that a non-uniform rescoring can invert orderings. Forbidden in the copy: any index value, ratio, rank or per-model score — those are derived from republished numbers, and they belong in the review record where the reviewer can check the work, never on the page. The anchor is the publisher's own changelog page, cited verbatim. THE WORKED EXAMPLE IS LIVE AND IT QUALIFIES TODAY: Artificial Analysis published Intelligence Index v4.2 dated 2026-09-04, and between the 06:00 fetches of 2026-09-04 and 2026-09-05 the rows carrying an intelligence score fell from 164 to 52 while 114 models lost their score entirely and none rose. That is the compression-of-cadence story the keeper asked this surface to tell, and it is precisely NOT the benchmark history he feared it would collapse into. ONE MECHANISM THE ORDER DOES NOT MENTION AND THE CHANGE MUST TASK: a post's reviewed surface is its front matter minus `MECHANICAL_FRONT_MATTER_KEYS`, which is `['timeline']` and nothing else (`lib/review-hash.mjs:71`), so adding any of these three keys to a post that already has a review record makes that record report `mismatched` and fails the launch minimums. These three are editorial judgments, not machine-maintained, so they do NOT join that list: every tagging pass goes back through the review gate, which is why the backfill below is a `verify` directive. Written by the orchestrator, not the maintainer. THE DRAFT IS NOT FINAL UNTIL THE UI-LOOP SESSION HAS READ IT, AND THAT IS AN INSTRUCTION FROM THE MAINTAINER (2026-09-05): "Run the specs by the other sessions before finalizing, since it has additional context to their origin." THE CONTEXT IS REAL AND IT IS NOWHERE IN THIS REPOSITORY. These requirements are the residue of six judged rounds, two finalist builds, a red team and an order-swapped jury; the artifacts here record WHAT was decided and the ui-loop session holds WHY — which round produced a clause, which red-team finding forced it, and what both finalist builds got wrong that the clause exists to prevent. A requirement whose reason is lost gets re-litigated or quietly dropped by whoever implements it, which is the failure this whole handoff was built to avoid. SO: draft the change in full, validate it with `openspec validate <name> --type change --strict --no-interactive`, and STOP THERE. Do NOT archive it — a change with no implemented tasks is not archivable anyway, so this does not bend the archive-promptly rule. Do NOT treat this job's merge as the change being settled. The orchestrator relays the drafted delta to that session between runs and folds the corrections back in; only after that is the implementation line below correct to run. Say in RESULT.md which requirements you were least sure about, because that is the list the review is most useful on. READ loops/ui-loop/graph/knowledge/SPEC-REVIEW-GUIDE.md BEFORE YOU DRAFT A LINE OF IT, and draft against the REASON rather than the clause. That file is a table, one row per requirement, giving what the round decided, the ruling or finding that produced it, and the implementer-ledger row or build lesson the requirement exists to prevent — the "why" that exists nowhere else in this repository and that a delta cannot carry on its own. It was written by the ui-loop session specifically so that whoever drafts these has it, and it is also the rubric your draft will be reviewed against, so there is no reason to work without it. Where the guide and this line disagree about substance, the guide is closer to the decision and wins; say so in RESULT.md rather than silently picking one. ONE CLAUSE TO KEEP EXACTLY AS WORDED, and the reason is a defect that already happened rather than a preference. The permitted list (publisher, index and version, date, direction, coverage counts) and the forbidden list (any value, ratio, rank or per-model score in the copy) must both survive into the spec intact. The board's own claim cell taught this at full price: an allow-list keyed on FIELD NAMES admitted a router's measured throughput and a third-party analysis site as "vendor claims" (implementer ledger #10, red-team FM-N3), because a field-name test is not a source test. F2 has the identical shape — a rescoring described by its numbers becomes a republished value BY ACCIDENT, without anyone deciding to republish anything. A list that says only what is permitted, and not also what is forbidden, is the same defect one layer up. [done 2026-09-05 j-20260905-15]

- interpret: draft the wiki-schema/directory OpenSpec change addictedtoai-1hjf for the closed `domain` facet per loops/ui-loop/graph/knowledge/EN-domain-facet.md §1 and DESK-ORDER-001 §3. THE THREE OPEN QUESTIONS ARE ANSWERED AND THIS LINE NO LONGER WAITS ON ANYONE: "general" is the UNMARKED default and `text` is not a tag (keeper K38), so the vocabulary is the eight tagged values `coding, agents, image, video, audio, research, science-math, robotics`; no Artificial Analysis value renders until republication rights are cleared (K24/K34, beads addictedtoai-ego8 and -c563); and domain sections order by domain id, a pure function of names, which is what `specs/directory` already requires of the tool categories. The facet is set-valued and optional on models, orgs, tools, techniques and frontier indices, it sits ALONGSIDE `kind` and tool `category` rather than replacing either, and a value outside the vocabulary fails the build. THE VOCABULARY GETS EXACTLY ONE DECLARED HOME, which both this change and addictedtoai-9c9t read, in the manner of the closed `kind` list. Two independent copies of eight strings will drift, and then §1's gate and §3's gate are two different checks on one vocabulary — which is the reads-as-present-and-does-nothing shape this repository keeps catching. SEEDED AND EDITORIAL DOMAINS ARE TWO FIELDS, NEVER ONE FIELD WITH TWO REGIMES (ui-loop K44). The reason is mechanical and it will otherwise be found as a red build: a reviewed surface covers front matter, 545 wiki files exist, and tagging entries rebinds every review record touched. Values SEEDED from the feed — the modality fields, and index presence — are machine-maintained, re-derivable on every Pulse run, and belong beside `timeline` in `MECHANICAL_FRONT_MATTER_KEYS` so a re-seed is not an edit. Values assigned EDITORIALLY — `research`, `science-math`, `robotics`, and any override of a seeded value — are judgments, do NOT go in that list, and pass through the review gate like any other front-matter edit. Give the facet two fields or a per-value source marker; one field carrying both regimes means either every re-seed demands a re-review of prose nobody touched, or an editorial judgment silently rides in as machine output. Written by the orchestrator, not the maintainer. THE DRAFT IS NOT FINAL UNTIL THE UI-LOOP SESSION HAS READ IT, AND THAT IS AN INSTRUCTION FROM THE MAINTAINER (2026-09-05): "Run the specs by the other sessions before finalizing, since it has additional context to their origin." THE CONTEXT IS REAL AND IT IS NOWHERE IN THIS REPOSITORY. These requirements are the residue of six judged rounds, two finalist builds, a red team and an order-swapped jury; the artifacts here record WHAT was decided and the ui-loop session holds WHY — which round produced a clause, which red-team finding forced it, and what both finalist builds got wrong that the clause exists to prevent. A requirement whose reason is lost gets re-litigated or quietly dropped by whoever implements it, which is the failure this whole handoff was built to avoid. SO: draft the change in full, validate it with `openspec validate <name> --type change --strict --no-interactive`, and STOP THERE. Do NOT archive it — a change with no implemented tasks is not archivable anyway, so this does not bend the archive-promptly rule. Do NOT treat this job's merge as the change being settled. The orchestrator relays the drafted delta to that session between runs and folds the corrections back in; only after that is the implementation line below correct to run. Say in RESULT.md which requirements you were least sure about, because that is the list the review is most useful on. READ loops/ui-loop/graph/knowledge/SPEC-REVIEW-GUIDE.md BEFORE YOU DRAFT A LINE OF IT, and draft against the REASON rather than the clause. That file is a table, one row per requirement, giving what the round decided, the ruling or finding that produced it, and the implementer-ledger row or build lesson the requirement exists to prevent — the "why" that exists nowhere else in this repository and that a delta cannot carry on its own. It was written by the ui-loop session specifically so that whoever drafts these has it, and it is also the rubric your draft will be reviewed against, so there is no reason to work without it. Where the guide and this line disagree about substance, the guide is closer to the decision and wins; say so in RESULT.md rather than silently picking one. PUT THE UNCERTAINTY LIST IN `proposal.md`, NOT IN `RESULT.md`, AND THIS CORRECTS AN EARLIER INSTRUCTION OF MINE THAT WOULD HAVE THROWN IT AWAY. `loop/run.mjs:1062` runs `git rm -r --ignore-unmatch .job RESULT.md` before the merge — RESULT.md is the loop's control channel, its first line is the run's status, and deleting it at merge is correct rather than a defect. What is wrong is using it as a durable record: anything said only there survives just long enough for the reviewer to read it and is then gone from git forever, which is precisely the "a thought that exists only inside something finished is already lost" failure this repository keeps paying for. So give the proposal a section — "What I was least sure about" — naming the requirements you would most want a second reader to check and why, and put any disagreement with the guide or with this line there too. The proposal is the right home on its own merits, not just for durability: the precedent is `openspec/changes/let-the-site-see-its-own-gaps`, whose proposal opens by recording what was re-measured and which prior claims had stopped holding, and the next person to read this change needs your doubts far more than the reviewer did. [done 2026-09-05 j-20260905-16]

- interpret: draft the OpenSpec change for DESK-ORDER-001 §4's vendor-claim record and the `lead-change` event kind, referencing addictedtoai-eb4l. ITS OWN CHANGE, NOT FOLDED INTO addictedtoai-9c9t (ui-loop K44): 9c9t is about the scout and posts and should land without waiting on a schema debate, while this one adds a content record type, a `lib/schema.mjs` field, and a member to `changes.jsonl`'s closed kind list — "adding a content field means editing lib/schema.mjs by design" is exactly what the change process exists to route. THE RECORD SITS BESIDE THE ENTRY, NOT ON IT, and the two reasons are mechanism rather than taste: a review record binds the entry's bytes, so a claim arriving — or a verification landing later on a claim already filed — would dirty the hash and demand a re-review of prose nobody touched; and a claim ages on its source's clock, not the entry's, so putting it in the entry's front matter makes one file answer to two freshness regimes. SHAPE: verbatim quote, source URL, accessed date, the ability or field named, and `verified` as a THREE-state value — absent, `false`, or `{by, url, date}` — because the board renders the words "not verified" only for the `false` case and must render nothing for absent. The record must also carry the source URL's HOST, so the board's vendor-sourced test (the registrable domain of the cited source equals the vendor's own; RULES.md R13 round-4/5 addenda, invariant S22(e)) can run from the record directly rather than re-parsing a URL at render time. THE MOTIVATING DEFECT, so the change knows what it is fixing: both Frontier finalist builds rendered organisation FOUNDING DATES and FOUNDERS as "claimed · unverified", independently, because the only structure the corpus offers is "any cited fact" — a founding date is not a vendor claim and must never render as one. Written by the orchestrator, not the maintainer. THE DRAFT IS NOT FINAL UNTIL THE UI-LOOP SESSION HAS READ IT, AND THAT IS AN INSTRUCTION FROM THE MAINTAINER (2026-09-05): "Run the specs by the other sessions before finalizing, since it has additional context to their origin." THE CONTEXT IS REAL AND IT IS NOWHERE IN THIS REPOSITORY. These requirements are the residue of six judged rounds, two finalist builds, a red team and an order-swapped jury; the artifacts here record WHAT was decided and the ui-loop session holds WHY — which round produced a clause, which red-team finding forced it, and what both finalist builds got wrong that the clause exists to prevent. A requirement whose reason is lost gets re-litigated or quietly dropped by whoever implements it, which is the failure this whole handoff was built to avoid. SO: draft the change in full, validate it with `openspec validate <name> --type change --strict --no-interactive`, and STOP THERE. Do NOT archive it — a change with no implemented tasks is not archivable anyway, so this does not bend the archive-promptly rule. Do NOT treat this job's merge as the change being settled. The orchestrator relays the drafted delta to that session between runs and folds the corrections back in; only after that is the implementation line below correct to run. Say in RESULT.md which requirements you were least sure about, because that is the list the review is most useful on. READ loops/ui-loop/graph/knowledge/SPEC-REVIEW-GUIDE.md BEFORE YOU DRAFT A LINE OF IT, and draft against the REASON rather than the clause. That file is a table, one row per requirement, giving what the round decided, the ruling or finding that produced it, and the implementer-ledger row or build lesson the requirement exists to prevent — the "why" that exists nowhere else in this repository and that a delta cannot carry on its own. It was written by the ui-loop session specifically so that whoever drafts these has it, and it is also the rubric your draft will be reviewed against, so there is no reason to work without it. Where the guide and this line disagree about substance, the guide is closer to the decision and wins; say so in RESULT.md rather than silently picking one. PUT THE UNCERTAINTY LIST IN `proposal.md`, NOT IN `RESULT.md`, AND THIS CORRECTS AN EARLIER INSTRUCTION OF MINE THAT WOULD HAVE THROWN IT AWAY. `loop/run.mjs:1062` runs `git rm -r --ignore-unmatch .job RESULT.md` before the merge — RESULT.md is the loop's control channel, its first line is the run's status, and deleting it at merge is correct rather than a defect. What is wrong is using it as a durable record: anything said only there survives just long enough for the reviewer to read it and is then gone from git forever, which is precisely the "a thought that exists only inside something finished is already lost" failure this repository keeps paying for. So give the proposal a section — "What I was least sure about" — naming the requirements you would most want a second reader to check and why, and put any disagreement with the guide or with this line there too. The proposal is the right home on its own merits, not just for durability: the precedent is `openspec/changes/let-the-site-see-its-own-gaps`, whose proposal opens by recording what was re-measured and which prior claims had stopped holding, and the next person to read this change needs your doubts far more than the reviewer did. [done 2026-09-05 j-20260905-18]

- interpret: apply the ui-loop session's review corrections to `openspec/changes/flag-what-moved-the-frontier` (the addictedtoai-9c9t draft, job j-20260905-15). The change was APPROVED with corrections, so this is a revision of a sound draft and not a redo — change these things and nothing else, and re-read `loops/ui-loop/graph/knowledge/DESK-ORDER-001.md` first, because §1 itself was amended on 2026-09-05 (commits c76db73 and 9936a76) and the amended text is now the source. ONE OF THE FOUR IS A CHANGE TO THE REQUIREMENT ITSELF, not a wording fix: `domains` BECOMES OPTIONAL on a frontier-flagged record, and its absence MEANS "general". The reasoning is recorded as keeper-ruling K46, reached by a blind arbiter (BLIND-002) run before the decision under K32, and it is worth carrying into the spec because it is the kind of bar that gets re-tightened by someone who forgets why it loosened: K38 makes ABSENCE a stated value of the vocabulary, so a gate that fails a record for carrying the vocabulary's own default contradicts the vocabulary; and the ≥1 bar was written while `text` was still a value that could carry a general story, which K38 then removed. The concrete test the draft failed: a court filing, a regulator's enforcement action, a licence revenue gate and a system card are F5- and F4-shaped events, four of them exist in `content/blog/` today, none maps to any vocabulary value, and under the drafted bar none could be flagged at all. FOUR PLACES TO EDIT, and the reviewing session named them: the blog delta's field definition, the gate scenario, the loop delta's candidate-flag bar, and the `domains: [legal]` example — there is no `legal` value in the vocabulary, so use a real one or drop the key from the example. The gate that remains is `frontier_reason` ∈ F1–F5 REQUIRED, and any `domains` value outside the vocabulary FAILS; absence is not a failure. Do NOT touch the display contract: a "general" lane under the same three-most-recent rule is coming, but where it sits relative to the domain-id ordering is a presentation question for BRIEF-UI-002 and is explicitly not this change's. THE OTHER THREE ARE THE TWO CARRIED FINDINGS PLUS THEIR RETIREMENT. Restore the clause elided from the §1 quotation in `proposal.md` — "with leaders-by-index secondary where a licensed index exists" — because it is the ONLY clause permitting an index-derived secondary ordering once rights clear, and trimming it makes the spec read as a flat prohibition the keeper never made. In `specs/loop/spec.md`'s preamble, drop "its cap of three," from the list of things called unchanged, since the modified bullet changes exactly that. Then DELETE `data/carried/j-20260905-15-carry-1.md` and `data/carried/j-20260905-15-carry-2.md` in the SAME diff: those files are what put the findings in the derived queue, and leaving them behind makes the same two items reappear on the next Pulse run. DO NOT ARCHIVE, DO NOT IMPLEMENT, leave every task box as you find it. The number corrections the draft already carries (113 kept the row and lost the score plus 1 that left the snapshot = 114; 14 posts plus a README = 15 files) were accepted and the order now carries them, so do not revisit them. Put anything you were unsure about in `proposal.md`, never in RESULT.md — the loop deletes RESULT.md at merge (`loop/run.mjs:1062`). Written by the orchestrator, not the maintainer. [done 2026-09-05 j-20260905-19]

- interpret: apply the ui-loop session's review corrections to `openspec/changes/tag-the-corpus-by-domain` (the addictedtoai-1hjf draft, job j-20260905-16). APPROVED, with every open judgment ratified rather than overturned — the facet on every wiki kind, both tool sets, the three invented field names, the three-plain-string-lists shape, and MODIFYING `directory`'s "No placement is ever sold" to admit `domain` all stand as drafted. Keep the superset check in the proposal (8 of the live requirement's 9 units byte-identical, 1 differing in the opening sentence); the reviewing session called it the right test and it is the evidence that the MODIFIED block clobbers nothing. Re-read `loops/ui-loop/graph/knowledge/DESK-ORDER-001.md` first — §3 was amended on 2026-09-05 (c76db73, 9936a76) with these ratifications and with two stale counts corrected. ONE REQUIREMENT TO ADD, and it is small and specific: a build gate SHALL FAIL a `domains_excluded` value that appears in neither `domains_seeded` nor `domains`. The reason to write into the delta rather than leave implied — an exclusion that removes nothing is a STALE EDIT, a value that was seeded or asserted once, then stopped being either, leaving behind a suppression nobody can see doing anything. It reads as deliberate and does nothing, which is the shape this repository keeps catching. The rendered set is unchanged and should be stated where it is not already: (`domains_seeded` ∪ `domains`) − `domains_excluded`. ONE DECISION THAT IS YOURS TO MAKE, offered as a recommendation and not as an instruction, from the session that ratified your append-only rule as K47: when a seeding signal DISAPPEARS for an entry, the Pulse could write a `field_change` record to `data/changes.jsonl` so the disappearance is visible rather than silent. The kind already exists, so this costs no new machinery. WEIGH IT PROPERLY RATHER THAN ACCEPTING IT BY DEFAULT: append-only seeding already guarantees nothing is lost, so the record buys VISIBILITY and not safety, and the volume is the thing to check — you measured `agentic_index` presence falling 166 to 99 across one rebase, so the honest question is whether that is 67 useful records or 67 lines of noise in a feed a human reads. Decide it, implement whichever way you decide, and say in `proposal.md` which you chose and why. DO NOT ARCHIVE, DO NOT IMPLEMENT, leave every task box as you find it. Do not re-litigate the ratified items; the review settled them and re-opening a settled decision is how this work loses a day. Put anything you were unsure about in `proposal.md`, never in RESULT.md — the loop deletes RESULT.md at merge (`loop/run.mjs:1062`). Written by the orchestrator, not the maintainer. [done 2026-09-05 j-20260905-21]

- interpret: apply the ui-loop session's review corrections to `openspec/changes/separate-a-claim-from-a-fact` (the DESK-ORDER-001 §4 draft, job j-20260905-18, addictedtoai-eb4l). APPROVED WITH CORRECTIONS — the hard parts are right and are not to be revisited: the record BESIDE the entry with both reasons, `verified` tri-state with absent rendering nothing, `source_host` stored and gated, no claim built from `facts`, `cause` computed and never judged, and index values gated on registered-AND-cleared with a computed empty state. The full review with file:line is `loops/ui-loop/graph/artifacts/SPECREV-001.md`; READ IT FIRST, and read its "AUTHOR'S QUESTIONS" section, which answers the draft's own uncertainty list item by item. Seven corrections follow and one of them is a shipping defect. THE ONE THAT WOULD SHIP A DEFECT, do this first (D3, `specs/wiki` around 216–221). The vendor test has no generic-token exclusion, so add the tokenisation rule: name tokens are the normalised whole names AND their individual words EXCLUDING generic corporate words (`ai`, `labs`, `lab`, `cloud`, `inc`, `corp`, `research`, `group`, `technologies`, and the rest of that family), and a token matches only the ONE ownership label of the registrable domain (the eTLD+1), never any label of it. WHY, and it is measured rather than theoretical: without the exclusion, `labs.com` or `research.example` is admitted as a vendor-owned domain for half the corpus, and matching any label rather than the ownership label re-opens red-team finding FM-N5 one label over. This is the same class as ledger row 10 — a name test standing in for a source test — arriving for the third time in this surface. THREE MORE THAT DIFFER FROM THE DECISION. (D2, same requirement) The vendor test dropped half the live rule: restore "or a registrable domain the subject's own entry records citing itself from (`facts[].source_url`, `timeline[].source_url`)". R13 (v) and `frontier.mjs`'s `orgOwnDomains` both carry both halves and invariant S22(e) re-derives both, so a silent narrowing of a gate-enforced rule is precisely what a later implementer "fixes" back. (D1, `specs/wiki` around 223–249) The draft introduced `publishes_from` instead of putting brand domains in `aliases`, and that judgment is ACCEPTED as the better mechanism — an alias is a NAME (`lib/schema.mjs:492`) and the alias registry decides linking, while §2 said "as aliases" only because no host field existed yet. Do not change the design; add ONE SENTENCE at the requirement naming the divergence, so it reads as a decision rather than as compliance. (D4, `specs/wiki` around 318) The attribution clause is one-sided: append the second half from R13 (v) — the attribution SHALL NOT consume the clamp (over 85%), because a cell showing its vendor and none of its claim satisfies "the name is visible" and fails the reader. TWO WHERE THE RULE IS RIGHT AND THE REASON IS MISSING. (RL1) At the `publishes_from` requirement, state that NO GATE CAN SEE AN ABSENT DECLARATION: a missing brand domain renders a real vendor claim as an honest-looking blank with nothing failing anywhere (FM-N6), and that undetectability is exactly why the burden sits on the org entry rather than on the renderer. (RL2) In the `review` delta, say why the build can NEVER check `quote`: verbatim-ness is a comparison against a page the build does not fetch, so that clause belongs to the reviewer alone and to nobody else. ONE FROM THE LEDGER AND TWO ONE-LINERS. (L1, `pulse` delta) Say what `data/derived/frontier.json` holds when ZERO metrics are declared — which is the exact day-one state BOTH finalist builds hit: a derived file with an empty `metrics` array and the snapshot date, never absent and never a hard-wired placeholder. The renderer must be able to LOOK UP and then collapse; ledger row 6 is Dated Ledger hard-wiring `sources.map(() => 'no index published…')` instead. At `pulse` around line 136 add the one-liner that recording a lead-change is not rendering a value, which settles the draft's own K24 tension in the text where a reader meets it. At `pulse` around 213, cross-reference the F2 rule in `flag-what-moved-the-frontier` so the two changes cite ONE rule rather than keeping two copies that drift. DO NOT ARCHIVE, DO NOT IMPLEMENT, leave every task box as you find it, and do not reopen anything the review listed as out of scope (closing the kind list, the metric registry, seeding, a seventh content type, the display contract) — those are named and justified, not oversights. Put anything you were unsure about in `proposal.md`, never in RESULT.md, which the loop deletes at merge (`loop/run.mjs:1062`). Written by the orchestrator, not the maintainer. [done 2026-09-05 j-20260905-22]

- machinery: implement DESK-ORDER-001 §1 once the blog/loop OpenSpec change addictedtoai-9c9t lands — post front matter `frontier` / `frontier_reason` (F1–F5) / `domains`, the build gate (a flag without a cited reason or a valid domain fails), the scout's F1–F5 sweep and the frontier cap exemption; spec: loops/ui-loop/graph/knowledge/DESK-ORDER-001.md §1 (keeper K30, 2026-09-05)

- machinery: implement DESK-ORDER-001 §3 once the wiki-schema/directory OpenSpec change addictedtoai-1hjf lands — the closed `domain` facet on the wiki schema and the directory, the build gate that fails any value outside the eight-value vocabulary, and the seeding pass from the feed's modality fields. Mirrors the §1 implementation line above and exists for the same reason: §3's gate had a drafting line and nothing that built it (ui-loop K44, my Q2, their omission and acknowledged as one). HOLD THE TWO-FIELD SPLIT THE CHANGE DECIDES: seeded values are machine-maintained and live beside `timeline` in `MECHANICAL_FRONT_MATTER_KEYS`, editorial values do not and go through review — implement whichever shape 1hjf actually lands with, and if it landed with one field carrying both regimes, STOP and report rather than implementing it, because that shape either forces a re-review of 545 files on every re-seed or launders an editorial judgment as machine output. The vocabulary is read from its single declared home, never re-typed. Written by the orchestrator, not the maintainer.

- machinery: register the scout's radar feeds — Hugging Face Hub, covered orgs' release RSS, arXiv, GitHub releases — as data/sources/registry.json rows with robots/terms and last-verified dates; inputs to the scout only, never displayed; DESK-ORDER-001 §5; addictedtoai-wg78

- machinery: add kind `lead-change` (arrival vs rescoring) to changes.jsonl and derive data/derived/frontier.json per frontier-plan §2.3; DESK-ORDER-001 §4; addictedtoai-eb4l

- machinery: define the vendor-claim record (verbatim quote, source URL, accessed date, ability named, verified: false | {by,url,date}) and migrate the cited facts that are claims; no founding or company fact may render as a claim; DESK-ORDER-001 §4; addictedtoai-eb4l

- verify: check Design Arena's republication terms for benchmarks.design_arena (165 feed rows); record the outcome and date in the registry row; no value renders until cleared; DESK-ORDER-001 §4; addictedtoai-c563 (sibling of addictedtoai-ego8) [done 2026-09-05 j-20260905-24]

- interpret: RULING TO RECORD, then the work it governs. Of the 34 catalog providers with no wiki org entry (measured 2026-09-05 via the board's own alias join), NOT ALL 34 ARE ORGANISATIONS, and filing them all as `wiki/org/` entries would state something false about several of them. Eight are individuals or personal handles publishing fine-tunes and merges — `sao10k`, `thedrummer`, `undi95`, `gryphe`, `anthracite-org`, `cognitivecomputations`, `mancer`, `dots-studio` — and one, `openrouter`, is the ROUTER ITSELF, whose "Auto Router" rows are a routing product rather than a model organisation's catalogue. THE RULING, made under the orchestrator's standing editorial authority and open to the maintainer overturning it: `content/wiki/org/` is for ORGANISATIONS, and an individual releasing fine-tunes under a personal handle is not one — an entry asserting otherwise would misrepresent what the thing is, which is the one thing this corpus may not do. That is NOT a judgment about their importance: `thedrummer` and `undi95` carry rows that real people use, and the ecosystem of independent fine-tuners is genuinely part of the site's subject. It is a judgment that `org` is the wrong KIND for them. OpenRouter is the opposite case and should certainly have an entry: it is an organisation, and it is the single most load-bearing entity in this site's data layer, since every catalog row in the corpus arrives through it — an entry for it is overdue on its own merits, not as backlog. YOUR TASK: write that ruling into the record where the next person filing these will meet it — the appropriate home is a short section in `content/wiki/` if one fits the corpus, or `data/README.md`, or the `2ok0` issue, and choosing correctly between them is part of the job; say which you chose and why. Then say what SHOULD carry the independent fine-tuners, if anything: an existing kind, a directory listing, a single entry about the practice itself, or nothing. Do not create a new content kind — the `kind` list is closed and changing it needs an OpenSpec change, which this is not. addictedtoai-2ok0 [done 2026-09-05 j-20260905-25]

- entry: write the wiki org entry for the catalog provider `minimax` — MiniMax (Shanghai), whose MiniMax-01 line carries the largest uncovered share of the catalog. It carries 11 catalog rows and has no entry today. DESK-ORDER-001 §2 and keeper ruling K21: a player is on the board because the site COVERS it, never because a feed carries it — feeds fill cells, they do not decide membership. The board today has 16 org entries against 58 catalog providers, so 34 providers never surface at all. WHAT THE ENTRY MUST CARRY OR THE ROW IS BLANK: its `feeds` map, which is the join the board relies on, and its product-brand registrable domains in `publishes_from` (ui-loop K48 — NOT `aliases`, because an alias is a NAME and the alias registry decides linking, `lib/schema.mjs:492`). THE BRAND DOMAIN IS NOT PAPERWORK: the board attributes a vendor claim only when the cited source's registrable domain is the vendor's own, so a missing one makes a REAL vendor claim render as an honest-looking blank — and NOTHING FAILS when the declaration is absent (red-team FM-N6), which is exactly why the burden sits on this entry rather than on the renderer. An org with no feed binding still renders an all-blank row, which is allowed and honest; an org with a wrong one is not. Apply `specs/editorial`: give an enthusiast something they did not know or assemble scattered things for the first time, be specific with dates and mechanisms, and source every new claim from a page you actually fetched. Do not restate a facts table in sentences. Written by the orchestrator, not the maintainer. addictedtoai-2ok0 [done 2026-09-05 j-20260905-26]

- entry: write the wiki org entry for the catalog provider `bytedance-seed` — ByteDance Seed, the research group behind the Seed models — note it is listed separately from the `bytedance` provider id, which carries UI-TARS, and the entry should establish whether they are one organisation or two before asserting either. It carries 6 catalog rows and has no entry today. DESK-ORDER-001 §2 and keeper ruling K21: a player is on the board because the site COVERS it, never because a feed carries it — feeds fill cells, they do not decide membership. The board today has 16 org entries against 58 catalog providers, so 34 providers never surface at all. WHAT THE ENTRY MUST CARRY OR THE ROW IS BLANK: its `feeds` map, which is the join the board relies on, and its product-brand registrable domains in `publishes_from` (ui-loop K48 — NOT `aliases`, because an alias is a NAME and the alias registry decides linking, `lib/schema.mjs:492`). THE BRAND DOMAIN IS NOT PAPERWORK: the board attributes a vendor claim only when the cited source's registrable domain is the vendor's own, so a missing one makes a REAL vendor claim render as an honest-looking blank — and NOTHING FAILS when the declaration is absent (red-team FM-N6), which is exactly why the burden sits on this entry rather than on the renderer. An org with no feed binding still renders an all-blank row, which is allowed and honest; an org with a wrong one is not. Apply `specs/editorial`: give an enthusiast something they did not know or assemble scattered things for the first time, be specific with dates and mechanisms, and source every new claim from a page you actually fetched. Do not restate a facts table in sentences. Written by the orchestrator, not the maintainer. addictedtoai-2ok0 [done 2026-09-06 j-20260906-01]

- entry: write the wiki org entry for the catalog provider `amazon` — Amazon, whose Nova line is a first-party frontier attempt from a company better known here as a host — the entry should not conflate Amazon the model builder with Bedrock the router. It carries 5 catalog rows and has no entry today. DESK-ORDER-001 §2 and keeper ruling K21: a player is on the board because the site COVERS it, never because a feed carries it — feeds fill cells, they do not decide membership. The board today has 16 org entries against 58 catalog providers, so 34 providers never surface at all. WHAT THE ENTRY MUST CARRY OR THE ROW IS BLANK: its `feeds` map, which is the join the board relies on, and its product-brand registrable domains in `publishes_from` (ui-loop K48 — NOT `aliases`, because an alias is a NAME and the alias registry decides linking, `lib/schema.mjs:492`). THE BRAND DOMAIN IS NOT PAPERWORK: the board attributes a vendor claim only when the cited source's registrable domain is the vendor's own, so a missing one makes a REAL vendor claim render as an honest-looking blank — and NOTHING FAILS when the declaration is absent (red-team FM-N6), which is exactly why the burden sits on this entry rather than on the renderer. An org with no feed binding still renders an all-blank row, which is allowed and honest; an org with a wrong one is not. Apply `specs/editorial`: give an enthusiast something they did not know or assemble scattered things for the first time, be specific with dates and mechanisms, and source every new claim from a page you actually fetched. Do not restate a facts table in sentences. Written by the orchestrator, not the maintainer. addictedtoai-2ok0 [done 2026-09-06 j-20260906-02]

- entry: write the wiki org entry for the catalog provider `nousresearch` — Nous Research, an open-weights group with no API product of its own, which is precisely the kind of player K21 was written to admit. It carries 4 catalog rows and has no entry today. DESK-ORDER-001 §2 and keeper ruling K21: a player is on the board because the site COVERS it, never because a feed carries it — feeds fill cells, they do not decide membership. The board today has 16 org entries against 58 catalog providers, so 34 providers never surface at all. WHAT THE ENTRY MUST CARRY OR THE ROW IS BLANK: its `feeds` map, which is the join the board relies on, and its product-brand registrable domains in `publishes_from` (ui-loop K48 — NOT `aliases`, because an alias is a NAME and the alias registry decides linking, `lib/schema.mjs:492`). THE BRAND DOMAIN IS NOT PAPERWORK: the board attributes a vendor claim only when the cited source's registrable domain is the vendor's own, so a missing one makes a REAL vendor claim render as an honest-looking blank — and NOTHING FAILS when the declaration is absent (red-team FM-N6), which is exactly why the burden sits on this entry rather than on the renderer. An org with no feed binding still renders an all-blank row, which is allowed and honest; an org with a wrong one is not. Apply `specs/editorial`: give an enthusiast something they did not know or assemble scattered things for the first time, be specific with dates and mechanisms, and source every new claim from a page you actually fetched. Do not restate a facts table in sentences. Written by the orchestrator, not the maintainer. addictedtoai-2ok0 [done 2026-09-06 j-20260906-03]

- entry: write the wiki org entry for the catalog provider `poolside` — Poolside, a coding-model company whose Laguna line is in the catalog but which has no entry. It carries 4 catalog rows and has no entry today. DESK-ORDER-001 §2 and keeper ruling K21: a player is on the board because the site COVERS it, never because a feed carries it — feeds fill cells, they do not decide membership. The board today has 16 org entries against 58 catalog providers, so 34 providers never surface at all. WHAT THE ENTRY MUST CARRY OR THE ROW IS BLANK: its `feeds` map, which is the join the board relies on, and its product-brand registrable domains in `publishes_from` (ui-loop K48 — NOT `aliases`, because an alias is a NAME and the alias registry decides linking, `lib/schema.mjs:492`). THE BRAND DOMAIN IS NOT PAPERWORK: the board attributes a vendor claim only when the cited source's registrable domain is the vendor's own, so a missing one makes a REAL vendor claim render as an honest-looking blank — and NOTHING FAILS when the declaration is absent (red-team FM-N6), which is exactly why the burden sits on this entry rather than on the renderer. An org with no feed binding still renders an all-blank row, which is allowed and honest; an org with a wrong one is not. Apply `specs/editorial`: give an enthusiast something they did not know or assemble scattered things for the first time, be specific with dates and mechanisms, and source every new claim from a page you actually fetched. Do not restate a facts table in sentences. Written by the orchestrator, not the maintainer. addictedtoai-2ok0 [done 2026-09-06 j-20260906-05]

- entry: write the wiki org entry for the catalog provider `inclusionai` — inclusionAI, the group publishing the Ling models. It carries 4 catalog rows and has no entry today. DESK-ORDER-001 §2 and keeper ruling K21: a player is on the board because the site COVERS it, never because a feed carries it — feeds fill cells, they do not decide membership. The board today has 16 org entries against 58 catalog providers, so 34 providers never surface at all. WHAT THE ENTRY MUST CARRY OR THE ROW IS BLANK: its `feeds` map, which is the join the board relies on, and its product-brand registrable domains in `publishes_from` (ui-loop K48 — NOT `aliases`, because an alias is a NAME and the alias registry decides linking, `lib/schema.mjs:492`). THE BRAND DOMAIN IS NOT PAPERWORK: the board attributes a vendor claim only when the cited source's registrable domain is the vendor's own, so a missing one makes a REAL vendor claim render as an honest-looking blank — and NOTHING FAILS when the declaration is absent (red-team FM-N6), which is exactly why the burden sits on this entry rather than on the renderer. An org with no feed binding still renders an all-blank row, which is allowed and honest; an org with a wrong one is not. Apply `specs/editorial`: give an enthusiast something they did not know or assemble scattered things for the first time, be specific with dates and mechanisms, and source every new claim from a page you actually fetched. Do not restate a facts table in sentences. Written by the orchestrator, not the maintainer. addictedtoai-2ok0 [done 2026-09-06 j-20260906-06]

- entry: write the wiki org entry for the catalog provider `aion-labs` — AionLabs. It carries 4 catalog rows and has no entry today. DESK-ORDER-001 §2 and keeper ruling K21: a player is on the board because the site COVERS it, never because a feed carries it — feeds fill cells, they do not decide membership. The board today has 16 org entries against 58 catalog providers, so 34 providers never surface at all. WHAT THE ENTRY MUST CARRY OR THE ROW IS BLANK: its `feeds` map, which is the join the board relies on, and its product-brand registrable domains in `publishes_from` (ui-loop K48 — NOT `aliases`, because an alias is a NAME and the alias registry decides linking, `lib/schema.mjs:492`). THE BRAND DOMAIN IS NOT PAPERWORK: the board attributes a vendor claim only when the cited source's registrable domain is the vendor's own, so a missing one makes a REAL vendor claim render as an honest-looking blank — and NOTHING FAILS when the declaration is absent (red-team FM-N6), which is exactly why the burden sits on this entry rather than on the renderer. An org with no feed binding still renders an all-blank row, which is allowed and honest; an org with a wrong one is not. Apply `specs/editorial`: give an enthusiast something they did not know or assemble scattered things for the first time, be specific with dates and mechanisms, and source every new claim from a page you actually fetched. Do not restate a facts table in sentences. Written by the orchestrator, not the maintainer. addictedtoai-2ok0

- entry: write the wiki org entry for the catalog provider `microsoft` — Microsoft, whose Phi line is in the catalog — a case where the obvious facts are widely known and the editorial bar therefore demands something an enthusiast does not already have. It carries 2 catalog rows and has no entry today. DESK-ORDER-001 §2 and keeper ruling K21: a player is on the board because the site COVERS it, never because a feed carries it — feeds fill cells, they do not decide membership. The board today has 16 org entries against 58 catalog providers, so 34 providers never surface at all. WHAT THE ENTRY MUST CARRY OR THE ROW IS BLANK: its `feeds` map, which is the join the board relies on, and its product-brand registrable domains in `publishes_from` (ui-loop K48 — NOT `aliases`, because an alias is a NAME and the alias registry decides linking, `lib/schema.mjs:492`). THE BRAND DOMAIN IS NOT PAPERWORK: the board attributes a vendor claim only when the cited source's registrable domain is the vendor's own, so a missing one makes a REAL vendor claim render as an honest-looking blank — and NOTHING FAILS when the declaration is absent (red-team FM-N6), which is exactly why the burden sits on this entry rather than on the renderer. An org with no feed binding still renders an all-blank row, which is allowed and honest; an org with a wrong one is not. Apply `specs/editorial`: give an enthusiast something they did not know or assemble scattered things for the first time, be specific with dates and mechanisms, and source every new claim from a page you actually fetched. Do not restate a facts table in sentences. Written by the orchestrator, not the maintainer. addictedtoai-2ok0

- verify: backfill `frontier` / `frontier_reason` / `domains` on every existing blog post against DESK-ORDER-001 §1's criteria F1–F5; record each decline with its reason in the review; addictedtoai-9c9t WHY THIS LINE SITS AT THE FOOT OF THE FILE RATHER THAN BESIDE THE OTHER §1 WORK: it cannot succeed until §1 is IMPLEMENTED, because `lib/schema.mjs` fails the build on an unknown front-matter key and these three keys do not exist yet — and §1's implementation is a `machinery` line, currently refused at the 10% budget ceiling. Directives are taken topmost-first and a directive that does not merge is never marked done (`loop/run.mjs:1236` appends the marker only on the merged path), so a line that is certain to block, sitting above lines that can run, is re-selected on every run and starves everything below it. Moved here by the orchestrator for that reason and no other; move it back above the ready work once §1 has landed. If you are reading this and the three keys DO exist in the schema, this line is ready and the note is stale — say so and proceed.
