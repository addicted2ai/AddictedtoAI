done

## What was wrong

The entry content/wiki/model/arcee-ai-virtuoso-large.md declared a feed row
(`openrouter-models: arcee-ai/virtuoso-large`) that is absent from the latest
snapshot. The row was present in the 2026-08-28 snapshot
(`data/sources/openrouter-models/previous.json`) and absent from the
2026-08-29 one (`latest.json`), and the diff layer had already recorded the
retirement on its own: `data/changes.jsonl` line 62, kind `retirement`,
dated 2026-08-29, source URL https://openrouter.ai/api/v1/models.

## The cheap direct check

Fetched https://openrouter.ai/api/v1/models live during this job: zero rows
match `virtuoso-large` anywhere in the payload (the only arcee row present is
`arcee-ai/trinity-large-thinking`). The delisting is real and current, not a
transient fetch blip — same verification the reviewer of the sibling olmo
repair (j-20260829-03) applied.

## The repair

Mirrors the approved olmo pattern exactly (j-20260829-03, c57de09):

- `status: active -> retired`, `maintenance: living -> dormant`
- `feeds:` binding and all four feed-bound facts removed — they cannot
  resolve against a row that no longer exists
- one dated, sourced timeline entry: "removed from the OpenRouter model
  catalog; last listed in the 2026-08-28 snapshot", date 2026-08-29 (local
  date of the machine, matching changes.jsonl and the olmo entry), source_url
  the live API
- no successor row exists; the entry records the death rather than inventing
  a replacement (brief acceptance check)

Derived files were NOT hand-edited: recomputed with the Pulse's own pipeline
(`node pulse/run.mjs --offline --no-mint --no-build --dry-run` — the publish
step in dry-run prints and executes nothing, so no commit/push occurred):

- `data/derived/feed-rows.json` — the vanished row (previously rendered as a
  last-known `$vanished: true` row) is dropped
- `data/derived/freshness.json` — `vanished_feed_rows` is now `[]`
- `data/derived/queue.json` — the `vanished-feed-row` item is gone
  (count 2 -> 1; the z-ai/glm-4.5v uninterpreted-status-change job remains)
- `data/derived/search-index.json` — regenerated by `npm run build`
  (status active -> retired)

## Gates

- `npm test` — 484 pass, 0 fail
- `npm run build` — exit 0; the retired entry still builds and is indexed
  (`out/wiki/model/arcee-ai-virtuoso-large.html` present; `retired` is an
  obituary status in lib/indexability.mjs, same mechanism the olmo review
  verified)

## Scope

Five files: the one content entry plus the four derived files the
recomputation regenerated. No machinery, no reserved paths, no changes.jsonl
or linkcheck.json changes, nothing outside the stated outcome.

## Note

Same residual gap the olmo review filed as addictedtoai-2wa applies here
(no requeue if OpenRouter ever re-lists the row) — already filed on its own
issue, not widened here.