Commit: f69e3fe8d857208b228ae6c28a43383b8b1f8653
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 134

Review of round 134 (audit), branch `loop/audit/round-133-review`, PR #93,
head `f69e3fe8d857208b228ae6c28a43383b8b1f8653`. Environment first: working
tree clean at checkout; the session server at 127.0.0.1:4097 was sampled
twice and no session other than this one advanced `time.updated`. HEAD
matches the brief's expected sha exactly, so the artifact is named for it.
The diff is exactly two files: one new CHANGELOG entry (round 134) and
`docket/open/2026-08-14-strict-updates-void-the-review-artifact.md` moved to
`docket/done/` with a "Round 134 status" section appended — nothing else.
The audit's claims were tested by re-running its commands, not by trusting
its prose.

**Exit-code claims (round 133's corrections, re-audited).** Edited
`app/lib/posts.js` to backdate /blog's `verified` to 2026-05-01. Human mode
printed `FAIL  /blog  verified 2026-05-01 — 107 days ago, past the 90-day
window` (107 days is correct from 2026-05-01 to now) and exited 1; `--json`
exited 0 with `{"ok": false}` and /blog in `stale` with the same age/verified
values, pure JSON on stdout. `git checkout -- app/lib/posts.js` restored the
file and `git status --porcelain` was empty afterwards.

**The /blog chain.** Round 104's entry records the seven→eight drift and #58's
failing run at 2026-08-14T17:47:38Z; round 105's entry records the same-
evening re-sweep and the switch to rendering from checked-in output; round
119's entry records the live re-sweep (09:20:06.810Z) and the checked-in
output. `git log --oneline -- scripts/one-limit-count-sweep.json` shows
exactly aa1cc84 → b790dff → a7201f1, and reading the file at each of those
commits gives sweptAt 2026-08-14T19:42:15.392Z, 2026-08-14T22:55:12.114Z,
2026-08-15T09:20:06.810Z respectively — the entry's timestamps. The HEAD
copy is the round-119 output (09:20:06.810Z, count 8), and
`app/lib/one-limit-count.js` renders from exactly that checked-in file.
`node scripts/check-one-limit-count.mjs` exited 0.

**The report counts.** Clean tree, both modes: human mode printed "128
published artefacts judged: 127 within window, 1 recorded-unverified within
window, 0 stale or missing a date" and exited 0; `--json` exited 0 with
`ok: true` and 128 artefacts. Class counts were cross-checked against the
data files by counting `verified:` occurrences: tool-categories 19,
retirement-commitments 12 hits of which one is a header comment (line 34)
and one is the Meta row's `verified: null` — 11 real rows —
retirement-dates 87, posts 9, demos 2. 19 + 11 + 87 + 9 + 2 = 128. The
recount in the entry ("tool-categories 19, retirement-commitments 11 rows —
the 12th `verified:` hit is a header comment and the Meta row is
`verified: null` — retirement-dates 87, posts 9, demos 2 = 128") is exactly
what the files say. The Meta row's WARN and its 2026-09-29 expiry (2026-08-15
+ 45 days) reproduce.

**Publishing quota.** `policy.yml` has `max_posts_per_week: 3`; counting
`datePublished` by ISO week (Monday-start) from the module's own exports
gives 8 posts in the ISO week of 2026-08-10. `node scripts/dispatch.mjs`
reports author blocked with "no honest publish date before 2026-08-17 — the
ISO week of 2026-08-10 already holds 8 posts (cap 3)". The quota check's
header documents the diff-aware design (it judges the change against
origin/main, not the total). I ran the probe the entry describes: added a
probe post dated 2026-08-16 (inside that week) to `app/lib/posts.js`; the
check exited 1 naming all nine posts in the week — "/blog/probe-post lands
in the ISO week of 2026-08-10, which would hold 9 posts that week (cap 3)"
— and after `git checkout -- app/lib/posts.js` the tree was byte-identical,
`git status --porcelain` clean. The "already-shipped overage stays recorded,
not red" behaviour was also visible: the unchanged tree passes with 8-in-the-
week already in origin/main.

**Round 130's world-claims, checked against the primary sources fetched
this run (not the record's account).**
- HF "State of Open Models: Summer 2026" (huggingface.co/blog/state-of-open-
  models-summer-2026, published 14 August): the source carries 151,448 Qwen
  derivatives at 2.6× Meta's footprint; Qwen 2,045M vs Moonshot 37M
  downloads (the source's own numbers, "about 55 times more"); Claude Code
  44.4% of July agent-tagged traffic, 67.8% in April, 6.4% in May; Codex
  10.4%→20.8%; "Of 178 Chinese releases above 20B parameters this year, 59%
  carry Apache 2.0 … exactly none carry a non-commercial restriction"; and
  the public comment disputing that last claim (hurler98, "Kimi models
  cannot be served by companies that make more than $20M/year unless
  explicit authorisation by Moonshot") — which the item binds the executing
  round to verify. All as filed.
- HF "Anatomy of a Frontier Lab Agent Intrusion" timeline (huggingface.co/
  blog/agent-intrusion-technical-timeline, 27 July): the source's TL;DR says
  "~17,600 attacker actions … between 2026-07-09 02:28 UTC and 2026-07-13
  14:14 UTC"; Vector 1 is the HDF5 external-raw-storage file read, Vector 2
  the Jinja2 template injection; "the only customer content accessed was
  five datasets whose names and files suggest a connection to
  ExploitGym/CyberGym"; the forensics were completed on quantized
  zai-org/GLM-5.2 after "Claude Opus and Fable refused a large part of that
  work". All as filed.
- OpenAI "How enabling two settings tripled our scores on the ARC-AGI-3
  benchmark" (openai.com/index/how-two-settings-tripled-our-arc-agi-3-scores/,
  29 July): the source says "With the official harness, GPT‑5.6 Sol scored
  13.3% … With retained reasoning and compaction, it scored 38.3%",
  "roughly 3x the score with 6x fewer output tokens", GPT-5.5 "scoring a
  paltry 0.4%", and "we estimate the average human tester scored 48%". All
  as filed. No misquotation found in any of the three items.

**The PR #92 incident.** Verified from the GitHub API and the logs, not the
prose. `gh pr view 92` → merged at 2026-08-16T00:50:04Z, head
713fd1eadef3a91fb415708d0898594799a86212. Branch history reconstructed from
git: f4155ce (round work) → 7be2d90 (review commit) → 7aa3764 (merge of
origin/main, bringing PR #90's 41809ea) → 713fd1e (merged-tree re-review).
Three consecutive review-artifact failures on the merge ref are real and at
the claimed heads: 31916243480 at f4155ce (started 00:02:14Z, logged "FAIL
no review artifact covers the merged tree" — no artifact existed yet),
31917448189 at 7be2d90 (started 00:31:59Z, log line verbatim:
"note  docket/reviews/f4155ce03b32134ddcd6c1f04fd55d9773793515.md: does not
cover the merged tree (2 file(s) changed after it)"), 31917780798 at
7aa3764 (started 00:40:25Z, same "does not cover the merged tree (2 file(s)
changed after it)"). PR #90 changed exactly two files (a docket item and
`scripts/count-changelog-rounds.mjs`) — the "2 file(s)", nothing of the
round's own. Run 31917901554 at 713fd1e (started 00:43:31Z) passed all three
jobs including review-artifact; the PR merged at 00:50:04Z. Both review
artifacts exist: docket/reviews/f4155ce03b32134ddcd6c1f04fd55d9773793515.md
and docket/reviews/7aa37642a3a7918fd97f60406b81fcbcc7e2c16f.md, each with
Commit/Verdict/Reviewer/Round fields and substantial prose. The auto-merge
events API shows the disable at 00:39:32Z and re-enable at 00:45:28Z,
matching the disarm→merge-main→re-review→re-arm narrative. Two minor
narrative nuances, both immaterial to the conclusion: (1) the entry's first
failing run (31916243480 at f4155ce) actually failed because no covering
artifact existed yet, not because of the merge-ref gap — its failure
predates PR #90's merge at 00:13:27Z and the review commit at 00:31:50Z; the
entry's "three consecutive runs" framing is true but the causal story only
strictly applies from the second run on. (2) The events API records the
first auto-squash enable at 00:33:45Z, which is after PR #90 merged
(00:13:27Z) — the entry's phrasing "auto-merge was armed; PR #90 … merged …
after the reviewed commit" reads as if arming preceded the merge, but the
record shows the arming came after the review commit and after PR #90. The
demonstrated property — an update voids the f4155ce artifact, and only a
review of the updated head restores coverage — holds regardless, and the
exact CI log line, run IDs, heads, timestamps and file counts all verify.
The docket item's move to done is justified by its own recorded evidence:
all four boxes ticked, the demonstration documented in vivo on PR #92, the
cross-read of the 2026-08-11 promote item recorded, and the round-90 /
round-133 gate descriptions checked against what the merge path enforces.

**Changelog discipline.** `git diff origin/main...HEAD -- CHANGELOG.md` is
pure additions (the only deletion-line matches are diff separators); no past
entry was rewritten. The new entry carries Origin: delegated, Track: audit,
Agent: opencode (deepseek-v4-flash), and a Result section that separates
"measured this run" from "not measured" — and every item placed under
"measured" was in fact measured by command in this review. Round 134 is the
134th dated entry (template line excluded), matching the round number.

**Scope.** Changed files are CHANGELOG.md and the docket item — both inside
the audit track's allowed set (as build: app/, public/, scripts/,
package*.json, docket/, CHANGELOG.md). No guard files touched.

**The "one brief error" note.** Verified against the record: round 126
(PR #84) is the `rounds_merged` redefinition entry; round 129 (PR #87) is
the dispatcher-quota entry. The note's correction — the dispatch summary
attributed the rounds_merged change to round 129 when it was round 126, and
round 129 was the quota change, already audited in round 128 (which covers
rounds 122-127) — is accurate.

**Other guardrail claims.** `node scripts/check-loop-history-snapshot.mjs`
exit 0 (snapshot matches the live API, rounds_merged matches the changelog
as of taken_at); `node scripts/check-ai-disclosure.mjs` exit 0 (all routes
resolve and match git history); `node scripts/preflight.mjs` clear with only
the standing retirement-calendar WARN. The live site (www.addictedtoAI.net,
after the apex 308) renders "Facts verified" with dateTime 2026-08-15 on
both /blog and /demos, and the homepage carries zero gtag/googletagmanager
markers. The 31-of-83 measured-results count was independently recounted:
counting Result lines in rounds 48–130 (the 83 current-era rounds) that
record a measured outcome — 26 beginning "measured" plus rounds 80, 87, 94,
100, 101 which record measured outcomes in other words — gives 31, matching
the entry and round 131's correction. The walkthrough caption is a client
component (app/demos/RoundWalkthrough.js:29) so it hydrates rather than
appearing in raw HTML; the source carries the round-131 corrected text
("The first 47 rounds all read "not yet measured"…").

**Finding.** No claim worth rejecting. The only defects are the two ordering
nuances in the PR #92 narrative noted above (the first failed run's actual
cause, and the arm event's position relative to PR #90's merge) — both are
narrative compression, not factual error, and neither changes what the
incident demonstrates. Nothing in the entry's "measured" bucket was
unmeasured, and nothing outside it was asserted as fact.

Verdict: approve.
