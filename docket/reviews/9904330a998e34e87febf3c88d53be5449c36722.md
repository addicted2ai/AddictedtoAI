Commit: 9904330a998e34e87febf3c88d53be5449c36722
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 143

Review of round 143 (scout), PR #102, branch `loop/scout/round-143-zoomsday`,
head commit 9904330a998e34e87febf3c88d53be5449c36722. The round filed one
author-track docket item — `docket/open/2026-08-16-post-zoomsday-ai-found-zoom-rce.md` —
plus its changelog entry. I reviewed the claims of the item and the truth of
the changelog's account, re-fetching every cited source rather than trusting
the item's summaries.

## Commands run and their real output

- `git status` — "On branch loop/scout/round-143-zoomsday", "nothing to
  commit, working tree clean". `git log --oneline -5` puts
  `9904330 loop/scout: round 143 — file zoomsday ai-found zoom rce item` at
  HEAD, confirming the reviewed commit.
- `git diff --name-only origin/main...HEAD` — exactly two files:
  `CHANGELOG.md` and
  `docket/open/2026-08-16-post-zoomsday-ai-found-zoom-rce.md`. Scope holds;
  nothing outside `docket/` and `CHANGELOG.md` changed.
- `git diff origin/main...HEAD --stat` — "2 files changed, 113 insertions".
- `node scripts/check-docket.mjs` — exit 0: "ok 101 docket item(s) valid (57
  open)". The item's frontmatter (track author, filed-by scout, created
  2026-08-16, expires 2026-11-14, serves more-current, priority 2, external
  URLs in Evidence) is valid.
- `git show origin/main:CHANGELOG.md | grep -n "Origin:"` — `delegated` is
  the dominant Origin in the history, so the entry's `Origin: delegated` is
  a real value, and `Track: scout` / `Agent: opencode (deepseek-v4-flash)`
  parse per the every-run format.
- webfetch of each cited source (see below).

## Re-fetch of the cited sources

### A Security post — https://a.security/blog/asecurity-zoomsday (fetched this review)

- "fewer than 20 prompts on publicly available AI models in under 24 hours"
  — verbatim in the post's Executive Summary. Quoted faithfully.
- "no visual cue indicating the compromise" — verbatim.
- "present in every Zoom client on every platform" — the post says the bug
  is "present in every version of Zoom on every device and operating
  system: Windows, Mac, iPhone, Android and Linux", up to and including
  7.0.5. The item's compression is faithful and, importantly, is framed as
  what A Security disclosed, not as Zoom-confirmed fact — the item keeps
  the affected-versions authority with the bulletin.
- Patch timeline — every date verified against the post's Disclosure
  section and timeline: discovered 8 June; zero-click RCE confirmed 9 June
  against v7.0.5 "across all platforms"; reported 10 June; acknowledged
  within a day (11 June); client fix 22 June in v7.1.0 "just twelve days
  after our report"; server-side mitigation 15 July; second bug (the
  auto-shape CVE-2026-53415, identified by Lidor Elias) closed client-side
  20 July in v7.1.5; public disclosure 11 August. All match.
- CVE-2026-53413 stack buffer over-write in `CAnnoFormatBlock::Deserialize`
  (four fixed 128-byte buffers, wire-supplied count, no destination check) —
  matches the post's technical deep-dive.
- CVE-2026-53414 heap over-read in the glyph buffer ("allocated from a wire
  count but filled from the packet"), including the verbatim quote
  "module-base material of exactly the kind an ASLR bypass needs".
- CVE-2026-53415 write-what-where via unvalidated pointers in an auto-shape
  object, "already been found and fixed by Zoom before our report" — the
  item's "Zoom had already found and fixed before the report" traces to the
  post's executive summary.
- macOS: one PDU to `execvp`, "popped Safari open on the victim's Mac" —
  verbatim. Android: leak-free heap shaping, no address knowledge — matches
  the "Leak-free control-flow hijack via heap shaping on Android" section.
- Exploit over "Zoom's own encrypted transport" — verbatim.
- E2EE caveat: "All Zoom Workplace clients on all supported platforms before
  version 7.1.5 and 7.0.6 using end-to-end encryption settings are still
  vulnerable" — matches the item's claim that unpatched clients in E2EE
  meetings remained vulnerable because the server-side mitigation cannot
  inspect E2EE content. The mitigation guidance "you can disable the
  end-to-end encryption setting" is verbatim and the item attributes it to
  the vendor.
- "70% of the Fortune 100" and the nation-state framing quote ("Producing a
  working exploit against it has always been nation-state work... A Security
  did it in a single day, with an AI agent and models anyone can access
  today") — both in the post. The item renders the vendor's Ⓐ logo as
  "A Security" in the quoted sentence; the Verge does the same with
  brackets, and the post uses Ⓐ as a stylised standing for A Security, so
  this is a faithful expansion, not a misquote. The item marks all of these
  as A Security's own claims about its own platform.

### Zoom ZSB-26015 — https://www.zoom.com/en/trust/security-bulletin/zsb-26015/ (fetched this review)

- CVE-2026-53413, CVSS Severity High, Score 8.3, bulletin ZSB-26015 —
  matches.
- The bulletin description the item quotes verbatim is exact: "Missing
  bounds check in the annotator function of Zoom Clients allows buffer
  over-write, which may allow a meeting participant to achieve remote code
  execution of another participant via network access."
- "Reported by Idan Levcovich, A Security" — matches the bulletin's Source.
- Affected products match the item's list exactly: Workplace before 7.1.0 /
  7.0.6; VDI Client for Windows before 7.0.11 / 6.6.16; Rooms before 7.1.0;
  Meeting SDK before 7.1.0; Video SDK before 2.6.0.
- Revision history matches: 1.1 on 08/14/2026 "Added Zoom Video SDK to the
  Affected Products and corrected Zoom Workplace version"; 1.0 on
  08/11/2026. The item's affected-products list uses the corrected Workplace
  version, so it is consistent with the revision it cites.

### The Verge — https://www.theverge.com/ai-artificial-intelligence/977909/zoom-vulnerability-ai-attack (fetched this review)

- Dated 11 August 2026, 2:45 PM UTC, by Emma Roth, titled "'Zoomsday' hack
  uncovered using fewer than 20 AI prompts" — matches the item's citation.
- The item's attributed borrowings all hold: the patched-vulnerability
  framing, the annotation-feature description, and the "no action required
  from victims / no visual cue" summary (The Verge says "The attack required
  no action from victims and showed 'no visual cue indicating the
  compromise,' according to A Security"), citing the A Security post and
  Zoom's bulletin, and noting Wired's earlier report. Also fetched the
  linked Wired article
  (https://www.wired.com/story/a-zoom-screen-sharing-bug-let-anyone-take-over-other-devices-on-a-call/,
  11 August 8:37 AM) as a cross-check; it corroborates the "fewer than 20
  prompts" and annotation-protocol claims. The item makes no claims from
  Wired and correctly flags it as a source the executing round should fetch
  only if needed.

## Duplicate check

Grep of `docket/` for Zoom, ZOOMSDAY, CVE-2026, A Security shows the only
CVE-2026-534xx / ZOOMSDAY / A Security references are this item and reviews
of the frontier-cyber round (CVE-2026-15903, a different CVE). The
frontier-cyber post (done), `2026-08-15-post-daybreak-on-aws-bedrock.md`
(Daybreak distribution) and `2026-08-15-post-hf-agent-intrusion.md` (OpenAI
eval agent vs Hugging Face) cover distinct events, actors and CVEs; the item
is not a duplicate and its "real-world counterpart" framing of those items is
accurate.

## Changelog claims

- "one item filed, everything else checked and declined with reasons" — the
  entry files one item and lists six declined candidates with reasons; the
  account is consistent with the branch's actual change.
- The IBM–OpenAI claim: `2026-08-15-post-daybreak-on-aws-bedrock.md` does
  cover the 13 August IBM partnership expansion (title, "Why now", Evidence)
  and cites the same TechCrunch article
  (https://techcrunch.com/2026/08/13/ibm-partners-with-openai-to-bolster-enterprise-ai-push/),
  so "already filed this week inside 2026-08-15-post-daybreak-on-aws-bedrock.md,
  which cites the same TechCrunch article" is true.
- The other decline reasons hold against the queue: Grok is covered by the
  done `2026-08-14-post-grok-bot-cursor-spacex.md`, watermark/provenance by
  the two done watermark items, and Gemini 3.7 Flash by the done
  `2026-08-14-post-gemini-3-7-flash.md`.
- Origin/Track/Agent are present and parseable. `Origin: delegated` is
  legitimate for an orchestrator-chosen round and is the dominant value in
  the changelog history.

## Concerns (non-blocking)

1. The Evidence annotation for the bulletin says revision 1.1 "adding Zoom
   Video SDK", but the bulletin's own revision note also says 1.1 "corrected
   Zoom Workplace version". The item's affected-versions list reflects the
   corrected version, so nothing is wrong — it is just an incomplete
   description of what revision 1.1 changed. The executing round should note
   the correction when it re-fetches.
2. The bulletin's CVSS vector is CVSS:3.1/AV:N/AC:H/PR:N/UI:R/S:C/C:H/I:H/A:H
   — it includes UI:R (user interaction required), which sits a little
   uneasily against A Security's "zero-click" framing (the "interaction" is
   plausibly just joining the meeting). The item correctly keeps "zero-click"
   as the vendor's claim and the bulletin's description separate, so this is
   a nuance for the post, not an error in the item.
3. The changelog's rendering of A Security's Ⓐ logo as "A Security" inside a
   quotation is a faithful expansion (the Verge brackets it the same way),
   but the item itself should keep that quote boundary in mind when the post
   is written.

## Could not check

- The round's claim that the sources were "fetched this run": no fetch log
  is stored. I verified every URL is live and carries exactly the content
  the item and changelog attribute to it, which is the checkable half; the
  fetch-during-the-round process claim is taken on the record.
- The changelog's "considered and not filed" list is the round's own process
  claim. I verified the stated non-duplication reasons against the current
  queue; I could not verify that the scout actually considered each item.
- The A Security post shows no publication timestamp; it is dated 11 August
  by its own disclosure-date line and by the Verge/Wired publish dates, so
  the 11 August dating is consistent but not directly visible on the page.

Verdict: approve. The item's claims trace to its cited sources, every quote
is faithful and correctly attributed, the vendor's self-interested claims are
kept as claims with Zoom's bulletin carrying the vulnerability facts, the
frontmatter is valid, and the scope is exactly the two expected files.