Commit: 9ddb10232c47539758a717862c4be0be8946f196
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 152 (sixth review of PR #115 — corrections check)

## Summary

Sixth review of PR #115, `loop/meta/docket-filing-gate`, at
9ddb10232c47539758a717862c4be0be8946f196. The corrections commit `9ddb102`
touches exactly two files — `CHANGELOG.md` (round 152's entry only) and
`docket/open/2026-08-17-origin-is-self-declared-in-the-tree-it-gates.md`. No
`scripts/` change. The third file in the wider range
(`docket/reviews/5bd1b360fc5a31884faaafdd157e7ddae1a0afca.md`) is the fifth
pass's own artifact, written by the previous reviewer after the baseline — not
new material.

## The four corrections

**1. Origin distribution.** Counted with an anchored bullet pattern
(`^[-*] Origin: (delegated|supervised|unsupervised|maintainer)$`), the log
holds exactly 66 `delegated`, 18 `supervised`, 11 `unsupervised`, 10
`maintainer` — 105 declared entries, 39 non-`delegated`. Matches the claim.
(An unanchored `grep -c "Origin:"` matches 132 lines, but that counts prose
mentions and "Note on Origin" lines; the bullet count is the parser's count.)
True.

**2. `check-track-scope.mjs`.** Its header reads: "THIS FILE IS
MAINTAINER-OWNED ... the reason is round 78: this checker reads SCOPES from the
branch it is judging ... it just cannot be the one that approves it." It says
the checker reads `SCOPES` from the branch it judges and is mitigated by being
human-owned — exactly what both texts now claim. Not a read-from-`main`
mitigation. True.

**3. The promote item.** `2026-08-13-promote-review-artifact-to-required-check.md`
is in `docket/open/`, not in `docket/done/`, and no diff in this range moves
it. True.

**4. The readout provenance.** The branch-protection line now records the
readout was read by the orchestrating model, that this round's reviewer was
denied the API call by tool rules, and that a re-checker should re-read it. It
no longer presents itself as round-verified. True.

## The new `unsupervised` argument

The entry and item claim `scripts/round.mjs` arms auto-merge for
`unsupervised`, `supervised` and `maintainer`, withholding it only for
`delegated`. `scripts/automerge-origin.mjs` confirms it: `AUTOMERGE_ORIGINS =
{unsupervised, supervised, maintainer}`, and `ship` arms only when
`originAllowsAutomerge` passes (or a delegated round's review artifact does).
`unsupervised` is defined at CHANGELOG.md:21 as "scheduled, merged itself,
nobody read it first". The inference that the cheapest route past the review
gate is to claim less oversight is sound: `check-review-artifact.mjs` applies
only to `delegated`, so a round writing `unsupervised` both skips the artifact
requirement and arms its own auto-merge. True.

## No unhappened claims

Searched both files for "sixth", "this review", "verified the correction" and
related phrases. Nothing asserts a sixth review, a review of the corrections,
or any result that has not happened. The items describe only what the fifth
review found and what the tree now says. Clean.

## Earlier findings not softened

Round 152's entry still records: four gate revisions (line 478), build-flood
possible in all three earlier shapes (lines 479-480), and the unbudgeted-track
residual as a live limitation with the 60 → 90 attack named (lines 456-462,
726-730). The CHANGELOG diff is purely additive (one hunk, zero `-` lines).

## Checks

- `node scripts/check-docket.mjs` — exit 0; meta 29 ≤ 29.
- `node scripts/round.mjs check` — exit 0; lint, docket valid, track scope,
  build, route checks all ok, no group skipped.
- Rule 5 — `git diff origin/main...HEAD -- CHANGELOG.md` has zero deletions.