Commit: a32d03d4e2bf93e09d899f6e3ec2278e7b675142
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 97

Third review of PR #48, after the first review rejected the two-PR count and
the second rejected the "47" correction. This correction fixes both: the
verification bullet now states the count the cited command actually returns,
and the #23 sentence no longer contradicts itself. This run reproduced every
remaining claim it makes. Verdict: approve.

What I ran and observed:

1. Branch state: `git rev-parse HEAD` = a32d03d4e2bf93e09d899f6e3ec2278e7b675142
   on `loop/maintain/blog-page-omits-the-admin-bypass`, clean tree. PR #48 is
   OPEN, headRefOid a32d03d4e2bf93e09d899f6e3ec2278e7b675142 (matches local
   HEAD), author and merged-by addicted2ai, reviews: [], autoMergeRequest:
   null. The branch carries seven commits beyond origin/main: the round's own
   work (67ffed8, a1c315e, 056f430), the first review artifact (16cf436), the
   first correction (0e94bdc), the second review artifact (8ca2366), and the
   correction under review (a32d03d).

2. Correction diff is minimal: `git diff 0e94bdc..HEAD --name-only` lists
   exactly CHANGELOG.md and docket/reviews/0e94bdc6605bbb0ef9cab26a27226e78316b22d7.md
   (the second review's own artifact). The CHANGELOG.md diff changes only the
   verification bullet: "returned 47 merged pull requests" becomes "returned
   45 merged pull requests — #33 and #43 are closed, not merged, and #48 is
   still open", and the #23 sentence is folded into one coherent statement:
   "Every other merged PR reports `human-owned-paths` pass except PR #23,
   whose head carries one failing run: #23 created the check and was merged
   before `human-owned-paths` was in the required list — the same finding the
   review's sweep recorded — so it is not one of the five. PRs #1–#22 predate
   the check and have no such run. The set is exactly {25, 27, 39, 40, 42}."
   The self-contradiction the second review flagged ("Every other merged PR
   reports pass" followed by a failing #23) is gone: the exception is named in
   the same sentence as its reason.

3. The count reproduces: `gh pr list --state merged --limit 100` prints 45
   rows (#47 down to #1, missing #33 and #43). The same call with
   `--json number --jq length` returns 45. The head-SHA enumeration used for
   the sweep (same call, `--json number,headRefOid`) has 45 entries. `gh pr
   view 33` and `gh pr view 43`: both state CLOSED, mergedAt null. `gh pr
   view 48`: state OPEN, mergedAt null. So "45 merged pull requests — #33 and
   #43 are closed, not merged, and #48 is still open" is exactly what the
   cited command returns, with the reason stated. No reading of any query I
   can produce yields 47.

4. The exhaustive set. For all 45 merged PRs I ran
   `gh api repos/addicted2ai/AddictedtoAI/commits/<head-sha>/check-runs` and
   read the `human-owned-paths` run on each head commit. Exactly five report
   completed/failure, and the failing run completed before the merge in every
   case:

     PR#25 failed 2026-08-11T13:09:25Z, merged 13:15:56Z
     PR#27 failed 2026-08-11T15:32:26Z, merged 15:39:31Z
     PR#39 failed 2026-08-12T05:38:08Z, merged 05:44:50Z
     PR#40 failed 2026-08-13T16:16:14Z, merged 16:29:30Z
     PR#42 failed 2026-08-13T19:49:54Z, merged 20:02:56Z

   `gh pr view` on each of the five confirms mergedBy addicted2ai, reviews:
   [], autoMergeRequest: null — matching the bullet exactly. Every other
   merged PR (#24, #26, #28–#32, #34–#38, #41, #44–#47) reports
   human-owned-paths completed/success; #1–#22 have no human-owned-paths run
   at all (the check-runs response for those heads contains no such check).

5. The #23 exclusion is sound and the bullet is now internally consistent.
   #23's head (f8780b1c) carries one human-owned-paths run:
   completed/failure@2026-08-11T12:37:57Z; #23 merged 2026-08-11T12:46:26Z by
   addicted2ai with reviews: [] and autoMergeRequest: null. #23's own PR body
   states "Until `human-owned-paths` is added to branch protection's required
   list, it will report the problem and watch the merge happen", and round
   23's changelog entry says the same ("It does not bite yet... Until
   `human-owned-paths` is added to that list it will report the problem and
   watch the merge happen"). So #23 created the check and merged before it was
   in the required list; excluding it from the five is correct, and the bullet
   states the exclusion, its reason, and the residual set in one place with no
   contradiction.

6. The page passages. `app/blog/page.js` lines 177–206 ("What is true now,
   and only this") and 219–232 ("One limit") name all five — #25, #27, #39,
   #40 and #42 — state "the account that stepped over the check five times",
   and say nothing beyond the sweep. Grep of the page for "two"/"twice"/"47":
   no "47" anywhere; the remaining "two"/"twice" hits are line 178 ("two
   required checks", accurate), line 202 ("the first two claimed a human
   check", about the two earlier human-review overstatements), and lines
   169–171 ("Correcting the same overstatement twice... it happened twice",
   about those same earlier corrections) — none is a check-count claim.
   `git diff origin/main...HEAD -- app/blog/page.js` shows only the two
   passage rewrites.

7. Branch protection and collaborators reproduce. `gh api
   repos/addicted2ai/AddictedtoAI/branches/main` returns
   protection.required_status_checks with enforcement_level "non_admins" and
   contexts ["build-and-audit","human-owned-paths"] — exactly what the
   changelog states. `gh api repos/addicted2ai/AddictedtoAI/collaborators`
   returns two accounts: addicted2ai role_name "admin", addicted2ai-loop
   role_name "write". "The only admin is the owner" is true. (The explicit
   enforce_admins.enabled field on /branches/main/protection was denied to
   me, as the entry honestly records; the enforcement_level field is the
   same fact and this run read it.)

8. The changelog entry. Round 97 (2026-08-14) is the newest entry,
   consecutive with Round 96 below it. Origin: delegated, Track: maintain,
   Agent: opencode (deepseek-v4-flash), Hypothesis before Change, Guardrails
   and Result ("not yet measured") all present. The corrected verification
   bullet states only numbers this run reproduced; the word "47" appears
   nowhere in the entry, and no "two"/"twice" appears as a claim about the
   failing-check count — the only "two"/"twice" strings in the entry are
   accurate statements ("two required checks", "two accounts") and Block 2's
   historical narrative about the first submission's false "two", which is
   the point of Block 2 ("It said two pull requests (#25 and #27)... The
   review of the first submission swept every merged pull request... and
   found five"). The five PR numbers and all five failure/merge timestamps in
   the bullet reproduce exactly (section 4). PR #48 is named in the entry
   header.

9. The done item. `docket/done/2026-08-11-blog-page-omits-the-admin-bypass.md`
   has all three boxes ticked; its "Done" section names the five with the
   same timestamps, states every other merged PR passes and the set is
   exactly {25, 27, 39, 40, 42}, and records the first submission's error
   and the correction. No false count in the "Done" section. The ticked
   "Done when" criteria still carry the 11 August "twice (#25 and #27)"
   wording — that is the original filing, dated when "twice" was true,
   superseded by the "Done" section; unchanged, not amended history.

10. Scope: `git diff origin/main...HEAD --name-only` lists CHANGELOG.md,
    app/blog/page.js, app/lib/page-origins.js,
    docket/done/2026-08-11-blog-page-omits-the-admin-bypass.md, and the two
    review artifacts in docket/reviews/. All inside maintain's allowed paths
    (app/, docket/, CHANGELOG.md). Nothing in CHARTER.md, .github/, prompts/,
    policy.yml or scripts/. The docket/reviews/ files are the two prior
    review sessions' own artifacts, part of this round's review flow.

11. `node scripts/round.mjs check` — every group passed: lint ok, docket
    valid, track scope ok, production build ok, all route checks passed.
    Nothing SKIPPED. Port 3000 was confirmed free (no listener) before the
    run. PR #48's own CI at review time: human-owned-paths pass,
    build-and-audit pending, review-artifact fail — the last is this
    artifact's job; committing it should flip that check via
    check-review-artifact.mjs.

No defect found in this head. The two defects the earlier reviews rejected
are gone, and every number the correction states reproduces. Approve.
