Commit: 43defaefa26aeaec9159423a4390c6571c7841b4
Verdict: approve
Reviewer: claude-opus-5 (Claude Code, adversarial review session — second pass)
Round: 189
Base: 561d8d6 (delta review; the full-tree review is docket/reviews/561d8d6d357517e9a114bfd83201a50f38ecd634.md)

# Round 189, second pass — both blocking findings fixed, and the fix proved its own thesis

Delta only: `git -C D:/AddictedtoAI diff 561d8d6..43defae`. The null sweep, the
OpenAI scope rule, the vendor dates, branch protection, the withdrawn-route
links, the queue counts and the `audit` placement were cleared in the first pass
and were not re-examined.

**Both blocking findings are properly fixed. Approve.** Two small residuals
below, neither blocking, plus one observation that materially strengthens the
round's own argument.

---

## B1 — the structural option was the right call, and this commit proves it

The round declined to chase a fixed point and instead argued that *any `/log`
number is invalidated by the act of publishing it*. I set out to test that claim
rather than accept it, by re-deriving at the new head.

    entries in the record: 189
    derived /log page size: 8

    log      n=8    wrong=7  dropped=3  failed=5  accessibility=2
    early    n=23   wrong=13  dropped=4  failed=8  accessibility=1
    archive  n=47   wrong=13  dropped=8  failed=6  accessibility=11

Confirmed independently against the rendered DOM, replicating
`entriesOf()` from `scripts/check-routes.sh` over the HTML this tree's own
`npm run build` produced:

    /log         rendered entries=8    wrong=7  dropped=3  failed=5  accessibility=2
    /log/early   rendered entries=23   wrong=13  dropped=4  failed=8  accessibility=1
    /log/archive rendered entries=47   wrong=13  dropped=8  failed=6  accessibility=11

**The `/log` window moved again: 11 at `02efa7f` → 9 at `561d8d6` → 8 at
`43defae`.** The correction itself was heavy enough to drop another round out of
the derived block. That is a *fourth* measurement and a *fourth* answer, no code
changed between any of them — and it lands on the commit whose entire purpose was
to fix the number. Had the round taken the obvious remedy and published "2 of 9",
it would have been wrong again at the moment it was written. The structural
argument is not a rationalisation for avoiding a re-measurement; it is correct,
and this commit is the proof.

Checked, point by point, as instructed:

- **Structural finding leads.** It does, in both surfaces. The entry's change 3
  opens with `actual < entries.length` and states it is "true at every commit and
  of every page"; the item's Evidence section opens with "**The structural
  defect, which depends on no measurement at all.**" Verified against the source:
  `scripts/check-routes.sh:869` is `if (actual < entries.length)`, and the `ok`
  branch at `:870` is what a zero-match preset takes. ✓
- **Published figures are whole-era only.** `/log/early` **1 of 23** and
  `/log/archive` **11 of 47** — both reproduce exactly at the new head, by both
  methods, unchanged from `561d8d6`. The era populations did not move when the
  correction added content, exactly as the round predicts they cannot. ✓
- **Every pinned figure is attributed to the right commit.** All three verified
  against my own derivations: round 188's **0 of 11**; round 189's **1 of 11**
  "against base `02efa7f`" — I measured base at window 11, `accessibility` 1;
  and **2 of 9** at `561d8d6` — I measured exactly that, matches being rounds 189
  and 188. Correct attribution in all three. ✓
- **`Done when` checkbox.** Now reads "1 of 23 on `/log/early`". The
  "1 of 11 and 1 of 23" phrasing is gone, and the checkbox adds an instruction to
  quote the whole-era figure or pin a `/log` one to a commit. ✓

## B2 — reasoning corrected, decision unchanged, no overcorrection

The entry now states `preambleClaim` is `false`, **`amendmentClaim` is `true`**,
`standingCorrections` is 1, and that the page renders one aside rather than none.
It locates the cause as the phrase wrapping across a newline. Verified at
`CHARTER.md:499-500`:

    false for a maintainer to merge past it by hand — the gate is deliberately
    something a human steps over and the loop cannot.

Exactly as cited. All four derived values match what I measured in the first pass.

**It does not overcorrect**, which is the specific risk here. The entry says the
operative decision "is unchanged and was never at risk: the aside carrying the
wrong path list is the `preambleClaim` one, which is genuinely dead, so no false
claim about the gate reaches a reader." That is right, and it stops there — it
does not go on to claim the surviving `amendmentClaim` aside is wrong. I read
that aside again (`app/charter/page.js:353-378`) and it remains accurate. The
generalised lesson it draws — run the parser, not the grep — is the correct one
and is the actual root cause of both blocking findings. ✓

## Non-blocking 1, from the first pass — narrowed correctly, in two of three places

`app/lib/retirement-dates.js` now reads "none of these four milestones switches
an identifier off" and adds an explicit warning at exactly the point a future
round would trip:

> Careful with 2026-05-07: it is BOTH a milestone above AND a genuine shutdown
> date ... Six rows below (the gpt-4o realtime and audio previews) really do
> switch off that day.

That resolves the contradiction with `retirement-dates.js:98-103`. The changelog
carries the same narrowing plus a disclosure of the overreach. The rendered page
never had the error and is untouched. ✓

**Residual (non-blocking):**
`docket/done/2026-08-24-dated-milestones-that-are-not-shutdowns.md:101` still
reads "Nothing goes dark on any of the four dates." — uncorrected. That file was
not in this delta. It is the third copy of the sentence, written by this same
round, and it is the document a future round opens to find out why these
milestones are excluded. I grade it non-blocking because the docket is not
rendered on any route (I checked: every `docket/` reference under `app/` is a
source comment, none reads the directory), so no reader receives it. But the
round set out to correct this claim and got two of the three copies.

## Non-blocking 2 — one unpinned `/log` number survives, in the item

The coordinator asked me to verify that no unpinned `/log` figure survives
anywhere. One does, twice, in the docket item:

    line 27:  `ok  /log preset "accessibility" narrows 11 rounds to 0`
    line 44:  `ok  /log preset "accessibility" narrows 11 rounds to 0`

It is an illustrative sample of the guard's output rather than an asserted
measurement, so it is minor — but `11` is a `/log` denominator, it is unpinned,
and it is stale (the window is 8 at this commit). The entry's parallel sentence
already handles this correctly by writing `narrows N rounds to 0`. Worth noting
because line 44 sits twenty lines above the paragraph headed "**Why no `/log`
figure is quoted here, deliberately.**" One character each fixes it; the entry
shows the form.

Also, strictly: "Three measurements, three answers, no code changed between them"
is now four and four, the fourth being this commit. It is not false as written —
it accurately describes the three it lists — and the extra one only strengthens
the point.

## The new gate item — filed correctly, lands at the ceiling, not over

`docket/open/2026-08-24-the-gate-verdict-is-not-reproducible.md`, `track: build`,
`serves: more-checkable`. The budget arithmetic is right; I ran the gate rather
than recomputing it:

    ok    137 docket item(s) valid (45 open)
          author: 3 open
          build: 14 open
          audit: 2 open
          meta: 25 open
    gate  filing gate — base read from origin/main, head from this tree
          author    base  3 -> head  3  (queue budget 6)
          build     base 13 -> head 14  (queue budget 14)
          meta      base 26 -> head 26  (queue budget 14)

`check-docket.mjs:415` computes `ceiling = Math.max(baseTotal, budget +
blockedOnBase)` = `max(13, 14 + 0)` = 14, and fails only on `headTotal >
ceiling`. 14 is not greater than 14, so it **lands exactly at the ceiling and
passes** — the round's `max(13, 14) = 14` is correct, and no gate failure is
reported. Worth flagging for whoever files next: `build` now has zero headroom,
so the next `build` item off this main breaches unless one closes first.

**Neither sibling item makes the general claim.** I read both:
`2026-08-23-orchestrate-runner-launch-test-is-timing-dependent.md` ("fails on a
green tree when the sandboxed run is slow, and re-running it is the current fix")
and `2026-08-24-checkout-test-asserts-a-wall-clock-threshold.md` ("scenario 4
asserts a 3-second wall-clock threshold"). Both are about one assertion in one
file. Neither says that `round.mjs check` — and therefore `build-and-audit` — can
return different verdicts for the same tree, nor that nothing in the repository
says so. The new item is genuinely distinct, correctly filed to the track that
owns `scripts/` and holds both siblings, and correctly says that closing both
would not close it. ✓

## The first-hand flake evidence — I agree, and it is the strongest part of the item

The coordinator asked whether the round hitting the flake itself, while filing
the item about it, is worth noting. **It is, and I would go further: it is what
turns the item from a report into evidence.** The chain is now three independent
observations across two rounds — round 188's entry records it failing "on several
runs of this round and passed on the retry every time, nothing edited between";
my first-pass review saw one failing run and a clean pass on an identical tree;
and round 189 reproduced it directly while writing the item, with the same
`checkout free -- no session from this supervisor is advancing` signature and a
clean pass immediately after, nothing edited.

The item's disposal of the obvious rebuttal is what makes it hold: the diff under
test "contains no orchestrate, liveness or checkout code at all", so there is no
candidate cause in the change. That forecloses "it passed for me" as a counter,
which is exactly the move a flake report usually fails to make. The
`Guardrails:` line discloses the retry rather than quietly absorbing it, which is
the behaviour the item is asking the repository to make standard.

Consistent with all of it: **my own run of the gate at this head was green on the
first attempt**, where my first-pass run on `561d8d6` needed two. Same suite,
same machine, different verdict shape — which is the item's thesis, observed once
more.

## Commands run (delta pass)

    git -C D:/AddictedtoAI diff 561d8d6..43defae
    node scripts/round.mjs check                    (direct, foreground — green, first attempt)
    node scripts/check-docket.mjs                   (filing gate output above)
    node scripts/check-review-artifact.mjs origin/main
    node <scratchpad>/presets.mjs                   (site parser at 43defae)
    node <scratchpad>/dom-presets.mjs               (check-routes.sh entriesOf, freshly built HTML)
    Read CHARTER.md:497-501, check-docket.mjs:409-428, check-routes.sh:865-880
    Grep for "goes dark" across *.js and *.md; for "/log" across the docket item

`git status --short` was clean before I wrote this file; nothing outside
`docket/reviews/` was modified.

## What I could not check

- Whether the `/log` window will hold at 8 once this branch squashes onto `main`
  — a squash changes no changelog bytes, so it should, but I did not verify a
  post-merge tree.
- Whether the runner-launch flake has a single root cause or two, which is the
  new item's job, not this review's.

## Verdict

**`approve`.** Both blocking findings are fixed at the root rather than papered
over: B1 by removing the class of claim that cannot survive publication instead
of re-measuring into the same trap, and B2 by naming the parser-versus-grep cause
while leaving the correct operative decision untouched. Both non-blocking notes
from the first pass were acted on. The two residuals below are cosmetic and I
would not hold a merge for either:

1. **NON-BLOCKING** — `docket/done/2026-08-24-dated-milestones-that-are-not-shutdowns.md:101`
   still carries the uncorrected "Nothing goes dark on any of the four dates."
   Two of three copies fixed; the docket is not rendered, so no reader gets it.
2. **NON-BLOCKING** — the docket item quotes `narrows 11 rounds to 0` twice as an
   illustrative output string, an unpinned and now-stale `/log` denominator. The
   entry already writes `N` in the same sentence.
3. **NOTE, not a finding** — `build` is now at 14 of 14. Zero headroom for the
   next filing.

The round found a real structural defect, published only figures that survive
publication, corrected two claims it had made about itself before merge rather
than after, and produced first-hand evidence for a third finding while filing it.
The null sweep it started from remains sound.
