Commit: 91c8a49207a01f3e0d939318730d5ec748c50d42
Verdict: approve
Reviewer: claude-sonnet-5 (adversarial reviewer, Claude Code subagent)
Round: 193

# Delta review — orchestrator commit `91c8a49` on top of round 193

One-character fix by the orchestrating session on top of the tree I already
reviewed at `2481204` (see `docket/reviews/2481204de8a1098c9985d1472d164523c46d43a1.md`,
`Verdict: approve`, still a valid record of that tree). This commit changes a
misquoted single word inside a quotation mark in `CHANGELOG.md`: `"were shut
down on"` → `"was shut down on"`. This is precisely the non-blocking nit my
prior artifact flagged (Priority 1, OpenAI citations section) — the
orchestrator correctly treated it as blocking rather than merging past it,
since the fragment sits inside quotation marks (presented as verbatim vendor
text) and `CHARTER.md` rule 5 freezes the entry at merge. I did not defer to
either my own earlier severity call or the orchestrator's fix; I re-verified
everything below from scratch.

## 1. Does the diff match exactly what was claimed?

    git diff 2481204..91c8a49 -- . ':!docket/reviews'

Output is exactly one hunk in `CHANGELOG.md`:

    -  do use past tense ("were shut down on") but that is still just a
    +  do use past tense ("was shut down on") but that is still just a

Nothing else changed. Confirmed.

## 2. Does the sentence still read correctly with the singular?

Full clause: `...with no independent confirmation language; some past
entries do use past tense ("was shut down on") but that is still just a
date-tied claim, not a separately confirmed status.`

Reads correctly. The parenthetical is a supporting example quotation — "some
past entries do use past tense, [for instance:] 'was shut down on'" — not a
grammatically fused continuation of "entries do use past tense." It does not
read as a claim that the page's grammar is plural, and it does not read as
an error: quoting the vendor's own (mildly non-standard — "these models
was") wording verbatim, including its grammar, is the correct behavior for
a verbatim quote, not something the sentence should silently correct. No
reword needed beyond the one-word swap already made.

## 3. Does origin/main still resolve to a18fdf8, and was any already-merged entry touched?

    git rev-parse origin/main  →  a18fdf81cbcc34900560f2ea8cc3c3a232cd02f1

Confirmed unchanged from the base I originally reviewed against. Checked
whether the touched line belongs to a previously-merged entry or to round
193's own new one:

    git blame -L 236,242 91c8a49 -- CHANGELOG.md

Every surrounding line, including the one just fixed, attributes to
`df3d1bfb` (round 193's own first commit, which added this entire paragraph
new) except the fixed line itself, which now attributes to `91c8a49` as
expected. This paragraph did not exist before round 193; nothing outside
round 193's own not-yet-merged entry was touched. Also grepped the whole
file for the phrase to rule out a second, unfixed occurrence:

    grep -n "shut down on" CHANGELOG.md

Two hits: the fixed line (now "was"), and one unrelated pre-existing line
about `dall-e-3`'s own shutdown date (line ~1507, not a vendor quotation, not
part of round 193, untouched). No stray "were" remains anywhere in the file.

## 4. Anything checked only via a less rigorous method than raw byte comparison?

Yes, and I closed it rather than just flagging it. In my first-pass review I
verified the OpenAI quotes (Assistants API row, both milestone quotes, the
inference-continuation sentence, both fine-tuning-narrowing dates, and the
two section headings) against a raw `curl` fetch I grepped myself — the
rigorous method. But the 10 `RETIREMENT_FLOORS` rows and 2 of the 3
Anthropic `RETIREMENT_DATES` rows were cross-checked only against `WebFetch`'s
AI-mediated markdown table extraction; I had only raw-grepped one of the
three Anthropic dated rows (`claude-opus-4-1-20250805`) directly against the
saved HTML.

I still had the raw Anthropic HTML saved in the scratchpad from the first
review, so I wrote a second script (`verify-anthropic-raw.mjs`) that strips
tags and greps directly for each of the 13 identifier/date pairs (10 floors
+ 3 retired rows) against the raw file, with no model-mediated step in
between. Result: **13/13 confirmed exactly** — every floor date and every
retired-row date/status matches `retirement-dates.js` byte-for-byte against
the raw page. This closes the one gap in my original verification; nothing
on this page that the branch's claims depend on was ultimately taken only
from a summarized extraction.

I did not find anything else quoted by this branch that I'd only checked via
the weaker method — the OpenAI side was fully raw-verified the first time,
and the Anthropic side is now fully raw-verified too.

## The O(n²) `stripInlineMarkdown` finding — my judgment, not filed

You asked whether the latent quadratic-scaling finding from my first review
deserves a docket item. My judgment: no, not right now. It's provably
non-hanging (proved in the first review, not just tested), the only caller
passes a single bounded, trusted CHANGELOG intro paragraph, and there is no
currently-reachable path to pathological input. Filing a docket item for a
currently-unreachable complexity concern would add to queues already near or
over capacity (`build` 14/14, `meta` 26/14 as of my last check) without
fixing anything live. Worth revisiting only if `stripInlineMarkdown` is ever
pointed at less-trusted or much longer input than a changelog intro — not
before. Not filing it, per your instruction.

## Verdict

**approve.** Re-verified all four things asked, independently, rather than
taking the orchestrator's `curl | grep` count on trust: reran the diff
myself, read the resulting sentence for grammatical and factual correctness
in context, confirmed the base ref and blame history to rule out touching a
merged entry, and used the opportunity to upgrade my own earlier
WebFetch-mediated Anthropic verification to a full raw byte-grep (13/13
now confirmed, versus 3/13 in the original review). Nothing else changed on
this tree; `node scripts/round.mjs check` was reported clean by the
orchestrator for this exact tree and I had no specific doubt to re-run it
against — the only change is the one word, in prose, and I verified that
prose directly.
