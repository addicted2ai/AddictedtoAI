Commit: acdfa2c12d8a0e7c360a6ea3d68064424a9bacfa
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 118

Reviewed the branch at acdfa2c (PR #74, track build) with the tree clean
(`git status --porcelain` empty before and after), base origin/main = 1fbb06c.

What I ran, and what each run showed:

1. Claim: the snapshot check gains a fourth front comparing every published
   count against the live API at check time. Confirmed by reading the diff
   and the file: scripts/check-loop-history-snapshot.mjs now compares
   runs_attempted, runs_succeeded, runs_failed, failed_run_ids and
   rounds_merged against the live API at check time (lines 268-305), after
   the unchanged as-of-taken_at front. The diff is additive; fronts 1-3 were
   not loosened.

2. Claim: a fresh-taken_at snapshot with stale numbers fails the check — the
   audit hole. Reproduced exactly: the pre-round committed snapshot
   (taken_at 2026-08-15T06:00:34.468Z, rounds_merged 64, restored from
   `git show origin/main:app/lib/loop-history.json`) was copied over
   app/lib/loop-history.json. The OLD check (`git show
   origin/main:scripts/check-loop-history-snapshot.mjs` run as a scratch
   file in the repo root) exited 0 on it — the hole was real. The NEW check
   exited 1 naming only the check-time mismatch: "rounds_merged: snapshot
   says 64, the live API has 66 merged at check time — the page's counts
   have aged past the live count". That isolates the new front: fronts 1-3
   were green on the same file. `git checkout -- app/lib/loop-history.json`
   restored the tree.

3. Claim: deliberately aging a count fails the check; restored tree passes.
   Edits made and reverted with git checkout (never committed): rounds_merged
   set to 64 on the true tree -> exit 1; runs_attempted/runs_succeeded aged
   -> exit 1 (check-time mismatch present alongside the shape mismatch);
   restored tree -> `node scripts/check-loop-history-snapshot.mjs` exits 0
   ("well-formed, within the 30-day window", matches the live API over 3
   completed runs as of taken_at), `git status --porcelain` clean.

4. Claim: the check is wired into the build and runs inside
   `node scripts/round.mjs check`. Confirmed: package.json prebuild runs
   `node scripts/check-loop-history-snapshot.mjs`; scripts/round.mjs check()
   runs `npm run build` (line 390), which triggers prebuild. Not re-run in
   full this review (the prebuild invocation was already proven by the round
   and by the live-check reproductions above).

5. Claim: the /loop-history page explains the mechanism honestly. Read
   app/loop-history/page.js lines 120-134: the "How this page is checked"
   paragraph now states the build also fails on "counts that have aged past
   the live API at build time ... a snapshot whose numbers trail the rounds
   that merged after it fails the build". No "can never go stale" or
   unverifiable claim. Note: like the pre-existing "disagrees with GitHub's
   live Actions API" sentence, this holds when the API is reachable; the
   script documents (front 1 comment) that unreachable degrades to a warning.
   Pre-existing pattern, not a regression; recorded here for the record.

6. Claim: the docket item is fully satisfied. The moved file
   docket/done/2026-08-14-loop-history-snapshot-staleness.md has all three
   "Done when" boxes ticked, and the shipped mechanism matches the allowed
   shape (a check that fails when rounds_merged trails the live count, the
   round-110 precedent). The changelog names the item as the reason the
   mechanism exists. Both failure directions were reproduced by me
   (items 2-3).

7. Changelog entry facts and fields. `git diff origin/main...HEAD --
   CHANGELOG.md` is additions-only (82 insertions, 0 deletions — the one
   leading `-` line in the raw diff is the `---` header separator). The entry
   declares Origin: delegated, Track: build, Agent: opencode
   (deepseek-v4-flash). Numbers traced to commands I re-ran this session:
   old snapshot 3/1/2/64 (git show origin/main:app/lib/loop-history.json);
   `node scripts/loop-history.mjs --json` reports 3/1/2/66; committed
   snapshot taken_at 2026-08-15T09:00:53.377Z with 3/1/2/66 (read from
   app/lib/loop-history.json); PR #72 merged_at 2026-08-15T06:54:21Z and PR
   #73 merged_at 2026-08-15T08:35:47Z (curl on api.github.com, heads
   loop/audit/rounds-111-115 and loop/build/publishing-quota-check) — both
   after the 06:00:34Z taken_at, as the entry claims. The 30-day window claim:
   policy.yml staleness_days.process_claim = 30 (parsed with js-yaml);
   policy.yml is not in the diff — untouched.

8. Scope: `node scripts/check-track-scope.mjs origin/main
   loop/build/loop-history-snapshot-staleness` reports all 5 changed files
   (CHANGELOG.md, app/lib/loop-history.json, app/loop-history/page.js,
   docket/done/2026-08-14-loop-history-snapshot-staleness.md,
   scripts/check-loop-history-snapshot.mjs) within build's scope. policy.yml,
   .github/, prompts/, CHARTER.md and scripts/check-track-scope.mjs are
   unchanged.

Verdict: approve. The check was proved able to go red in the precise
direction round 116's audit found missing (old snapshot, all old fronts
green, new front red), the failure messages name the at-check-time front
specifically, and every number in the record traces to a command re-run
here. The tree was left clean.
