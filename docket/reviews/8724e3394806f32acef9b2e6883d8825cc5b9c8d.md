Commit: 8724e3394806f32acef9b2e6883d8825cc5b9c8d
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 100

Reviewed commit 8724e3394806f32acef9b2e6883d8825cc5b9c8d (branch
`loop/author/post-fable-5-export-controls`, PR #54, two commits:
f4acb1a "publish the June export-controls episode", 8724e33 "attribute the
capability claims, quote the check output"). The head did not move while this
review ran. The artifact names the head as reviewed; nothing outside
`docket/reviews/` changed after it.

## Commands run, with real output

`git log origin/main..HEAD --oneline` — two commits as above.
`git rev-parse HEAD` — 8724e3394806f32acef9b2e6883d8825cc5b9c8d.
`git diff origin/main..HEAD --stat` — 7 files, +434/−23.

Web fetches (all three sources re-fetched this session, 2026-08-14):

- `https://www.anthropic.com/news/redeploying-fable-5` — "Redeploying
  Fable 5", Jun 30, 2026, with a Jul 1, 2026 "redeployed" update block.
  Confirms: Friday June 12 export-controls application, the foreign-national
  restriction, the all-users suspension, the June 30 lift linked to
  x.com/howardlutnick, the July 1 global return on Claude Platform /
  Claude.ai / Claude Code / Claude Cowork with AWS, Google Cloud and
  Microsoft Foundry "as quickly as possible", the June 26 government
  approval restoring Mythos 5 to a set of US organizations, the Amazon
  researchers' report, the less-capable models (Opus 4.8, GPT-5.5, Kimi
  K2.7) matching the vulnerability identification and every tested model
  matching the exploit demonstration (Haiku 4.5 upward), "did not expose
  any unique Mythos-level cyber capabilities", the improved safety
  classifier, "blocked in over 99% of cases", the Opus 4.8 fallback, CAISI
  agreement, false-positive cost, the shared-underlying-model claim, "the
  strongest safeguards we've ever applied to a model", "no such unique
  offensive capabilities", "find and exploit software vulnerabilities more
  effectively than any other model", the Amazon/Microsoft/Google/Glasswing
  severity framework with its four criteria and "work in progress", the
  HackerOne program, the four government commitments, and the "past ten
  weeks" engagement on the June 2 Executive Order. No link to the 12 June
  directive appears anywhere in this post.
- `https://www.anthropic.com/news/claude-fable-5-mythos-5` — launch post,
  Jun 9, 2026. Confirms: "Claude Fable 5 is available everywhere today",
  Mythos 5 restricted to Glasswing partners, "with the safeguards lifted in
  some areas", the 12 June update block "We are suspending access to Claude
  Fable 5 and Claude Mythos 5." (which links `/news/fable-mythos-access`).
  No directive link in this post either.
- `https://www.whitehouse.gov/presidential-actions/2026/06/promoting-advanced-artificial-intelligence-innovation-and-security/` —
  Executive Order 14409, June 2, 2026, signed DONALD J. TRUMP. Confirms:
  Sec. 3(a) classified benchmarking process and "covered frontier model"
  threshold; Sec. 3(b) voluntary framework, "for a period of up to 30 days
  before they plan to release such models to other trusted partners";
  Sec. 3(c) verbatim "Nothing in this section shall be construed to
  authorize the creation of a mandatory governmental licensing,
  preclearance, or permitting requirement for the development, publication,
  release, or distribution of new AI models, including frontier models.";
  Sec. 2(d) the AI cybersecurity clearinghouse. The full text contains no
  mention of export controls.
- `https://www.anthropic.com/news/fable-mythos-access` — a third vendor
  statement (Jun 12, 2026) linked from the launch post's update block. It
  also contains no link to the directive itself, so the claim "the 12 June
  directive itself is not linked anywhere in the vendor's posts" holds even
  under the broadest reading of "the vendor's posts".

Checks, run on this branch with the working tree clean:

- `npm run lint` — "No ESLint warnings or errors".
- `node scripts/check-docket.mjs` — "ok 56 docket item(s) valid (36 open)".
- `node scripts/check-ai-disclosure.mjs` — all 17 routes report
  "producing round N (track)" with the last commit touching each route's
  files, all `ok`; "all page disclosures resolve and match git history".
  The seven routes mapped to 100 (`/`, `/blog`, the four prior posts, the
  new post) all show this round's author-track commits as their newest.
- `node scripts/round.mjs check` — no group skipped:
  `ok npm run lint`, `ok docket valid`, `ok track scope for
  loop/author/post-fable-5-export-controls`, `ok npm run build`,
  `ok all route checks passed`. The changelog entry therefore parses with
  the fields the round claims.
- Round-100 parsing via the site's own parser
  (`node -e` + `getBuildLog()`): `{number: 100, date: "2026-08-14",
  track: "author", agent: "opencode", origin: "delegated",
  declaredOrigin: true}`, 100 entries total.
- Rendered check: `npx next start -p 3000` (PID confirmed by CommandLine
  before being killed by exact PID afterwards; port left free), then
  `curl http://localhost:3000/blog/fable-5-export-controls` → HTTP 200.
  The `<title>` equals the posts.js `metaTitle`; the description meta
  equals the posts.js `description`; JSON-LD `datePublished` is
  "2026-08-14"; the disclosure component renders the round-100 `delegated`
  meaning ("no human saw it before it landed"); the h1, post-meta and the
  full body text render. `/`, `/blog`, `/sitemap.xml` and `/feed.xml` all
  carry the new route/title (homepage teaser: 2 references; /blog: 3).
- `gh pr view 54` — `state: OPEN`, `mergeable: MERGEABLE`, head ref
  matches the reviewed branch.

Date arithmetic independently computed: 2026-06-12 is a Friday, 2026-06-09
a Tuesday, 2026-07-01 a Wednesday; 12→30 June is 18 days; 12 June→1 July is
19 days; 2→12 June is 10 days; 26 June→1 July is 5 days.

## Findings

Nothing blocking was found. Every claim in the post and in the changelog
entry that this review was dispatched to attack held under re-measurement:

1. **The post's factual claims.** All dates, day-of-week labels, numbers,
   quotes and attributions trace to the three fetched sources, verified
   line by line (list above). The "over 99% of cases" figure is quoted
   verbatim and explicitly presented as Anthropic's own measurement. The
   Fable 5 / Mythos 5 distinction is rendered as the sources state it (same
   underlying model; Fable 5 strongest safeguards; Mythos 5 "with the
   safeguards lifted in some areas", Glasswing partners only). The
   four-criteria framework is labelled a proposal throughout, and the
   vendor's capability quotes are attributed as the vendor's own.
   Partner names (Amazon, Microsoft, Google, other Glasswing partners),
   the four commitments, the 26 June Mythos partial restoration, and the
   EO's Section 3(c) quote all match the sources verbatim or near-verbatim.
2. **The changelog's two novel claims** — (a) the 12 June directive is
   not linked anywhere in the vendor's posts, and (b) EO 14409 explicitly
   disclaims mandatory licensing/preclearance — are both true. (a) was
   checked against all three Anthropic pages that exist on the record; the
   posts reference the directive only in prose. (b) is Sec. 3(c), quoted
   verbatim above.
3. **Registration consistency.** `ROUTE_FILES` and `PRODUCING_ROUNDS` agree
   route for route (checked by reading both maps and independently by
   check-ai-disclosure against git history). posts.js is a listed source
   file of `/`, `/blog`, and every post route, and this round did modify
   it, so moving all six plus the new route to 100 matches the stated
   rationale and the git record. The sitemap fallback indexing is correct:
   `posts[4]` is gpt-5-6-price-drop and `posts[5]` is
   fable-5-export-controls in array order.
4. **The checks.** All passed, no group skipped; the guardrail strings the
   changelog quotes are byte-identical to this session's run.
5. **Rendering.** The route serves the post with the disclosure component,
   correct metadata, sitemap and feed entries, homepage teaser and /blog
   listing.
6. **Docket move.** All five `Done when` boxes are satisfied by the shipped
   post: dates/who/why with claims traced (box 1), the Fable/Mythos
   distinction (box 2), the framework explained and labelled a proposal
   (box 3), no speculation beyond the sources and no security advice
   (box 4), primary-source links throughout (box 5). The completion note
   ("All three sources re-fetched that round") is consistent with the
   post's "All retrieved 2026-08-14".
7. **House style.** The post matches the voice and structure of the prior
   posts (dated timeline, attribution-heavy prose, "Every figure is the
   vendor's own" pattern), links inline to its primary sources, and does
   not present itself as security advice.

## Non-blocking concerns

- "Mythos 5 ... began returning to approved US organizations a week
  earlier" (What happened, in dates): 26 June → 1 July is five days.
  Readable as a calendar-week reference (26 June is in the preceding
  week), but loose as elapsed time, sitting one paragraph after the post
  insists the arithmetic is "worth being exact about".
- The title and the changelog's first sentence say Fable 5 was offline
  "for eighteen days", where the control period was 18 days and Fable 5
  itself was unavailable for 19 (12 June → 1 July). The body is exact
  ("eighteen days plus one more") and the changelog states both figures
  correctly in the same entry, so this is a shorthand, not an error — but
  a reader taking the headline literally would be off by one.
- "export controls were applied to the two most capable models in the
  country anyway" (The record is the story): the superlative is
  un-attributed in that sentence; the surrounding post attributes
  Anthropic's capability claims everywhere else. Similarly, the posts.js
  description/excerpt "the most capable generally available model in the
  world" is an un-attributed summary of the launch post's "capabilities
  exceed those of any model we've ever made generally available" plus
  "state-of-the-art on nearly all tested benchmarks" — close, but "in the
  world" is slightly stronger than the source's own framing.
- The suspension is described as "an update block appended to the launch
  post" — true, though that block links to a fuller third vendor statement
  (`/news/fable-mythos-access`) the post does not cite. "The record ... is
  essentially one Anthropic post" is a fair characterization of the cited
  record and hedged with "essentially"; a reader might want the third
  statement named. Not a defect, since nothing in the post depends on it.

## Could not check

- The content of the Commerce Secretary's x.com post that documents the
  lifting (x.com is not fetchable from this environment). The claim that
  the redeployment post "links a post from Commerce Secretary Howard
  Lutnick as the notice" rests on the link target
  (x.com/howardlutnick/status/...) and the link text "have been lifted",
  both seen in the fetched redeployment post.
- The round's actual fetch process ("three sources fetched this round"):
  no fetch log is committed, so that claim cannot be independently
  verified. The substance holds — every claim matches the live pages
  fetched on 2026-08-14 — but the process itself is attested only by the
  changelog.
- The round's actual invocation of `node scripts/round.mjs ship`: no log
  is committed. PR #54 exists, is open and mergeable, consistent with the
  entry's account that ship opened the PR and withheld auto-merge for this
  review.
