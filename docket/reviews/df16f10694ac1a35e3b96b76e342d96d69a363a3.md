Commit: df16f10694ac1a35e3b96b76e342d96d69a363a3
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 117

Reviewed on branch `loop/build/publishing-quota-check` at head
df16f10694ac1a35e3b96b76e342d96d69a363a3. The three prior rejections
(1749995, 003522d, aa1d0d1) were treated as context, not evidence: every
claim below was re-measured by running it this session, with my own driver
plus the fixer's. Working tree clean at start and end; every scratch edit
was reverted and verified via `git status --porcelain`.

## What I ran and what it showed

1. **The date fix is real.** `isRealCalendarDate` (scripts/check-publishing-quota.mjs:80-85)
   requires the shape `/^\d{4}-\d{2}-\d{2}$/`, a non-NaN parse, and the
   round-trip `date === new Date(date + "T00:00:00Z").toISOString().slice(0,10)`.
   Function-level probe of 25 date strings: all six review dates (2026-02-31,
   2026-02-29, 2026-04-31, 2026-06-31, 2026-09-31, 2026-11-31) → reject;
   2026-08-14 and 2026-08-17 → real. Script-level: a scratch post carrying
   each of the six impossible dates → exit 1 each, `FAIL  branch: post
   /blog/scratch-imp has no datePublished, or one that is not a real
   YYYY-MM-DD date`, reverted clean after each.

2. **The rollover exploit is dead.** Two scratch posts dated "2026-05-01"
   and "2026-04-31" → exit 1. The fail names /blog/scratch-pair-b — the post
   holding 04-31 (the pair-a post passes, pair-b fails first). Both rendered
   by the feed as Fri 01 May 2026 under the old guard; under this one the
   04-31 post is rejected outright. Reverted clean.

3. **The guard's own boundaries.** Probe results, each also run through the
   check where meaningful:
   - 2026-02-29 (2026 not a leap year) → rejected; 2028-02-29 (leap) → real;
     the check distinguishes them — a 2028-02-29 scratch exits 0 (`ok 10 posts`).
   - 2026-13-01 (month 13) → rejected (NaN guard fires; no throw), scratch exit 1.
   - Leading-space " 2026-08-17" and unpadded "2026-8-17" → rejected by the
     shape regex, scratch exit 1 each. "2026-08-9" and trailing-space
     "2026-08-09 " also reject.
   - 0000-01-01 → **accepted** as real (the regex allows four-digit year
     0000 and it round-trips). A 0000-01-01 scratch exits 0. Noted as
     observed behaviour: a real proleptic-Gregorian date, no quota threat.
   - 9999-12-31 → real, scratch exit 0. "10000-01-01" (5 digits) rejects.

4. **The three prior holes stay closed.** All re-run against the check:
   - reordered fields (`datePublished` before `path`), dated 2026-08-14 →
     exit 1 (day 5 vs cap 1, week 9 vs cap 3) — the post is visible;
   - decoy backtick-description `datePublished: "2026-08-17"` with no real
     date → exit 1 (the post named, no fabricated date); with real 08-14 →
     exit 1 naming 08-14; with real 08-17 → exit 0 (`ok 10 posts`) — the
     string is inert, the property governs;
   - duplicate path → exit 1 (`contains duplicate path "/blog"`); missing
     `posts` export → exit 1; non-array `posts` → exit 1; syntax error
     (unclosed block) → exit 1 (`could not import app/lib/posts.js`).
   Each scratch reverted, `git status --porcelain` clean after each.

5. **Diff-aware behaviour.** A clean future scratch dated 2026-08-17 → exit 0
   (`ok 10 posts`). A real breach (new post dated 2026-08-14) → exit 1
   (day 5 vs cap 1, week 9 vs cap 3). Re-dating the existing /blog post from
   2026-08-09 to 2026-08-15 → exit 1 (week 9 vs cap 3, day clean); re-dating
   it to 2026-08-14 → exit 1 (day 5, week 9). An unchanged already-breached
   08-14 in the head (identical to origin/main) with a new clean 08-17 post
   elsewhere → exit 0 — the shipped overage stays recorded, not red.

6. **The fixer's proof driver.** Read `proof-driver.mjs` at
   C:/Users/BadBitch/AppData/Local/Temp/opencode/proof-driver.mjs: it reverts
   every scratch with `git checkout -- app/lib/posts.js` and asserts
   `git status --porcelain` no longer mentions the file, and its exit-code
   expectations match mine for all 18 cases (it passed: ALL PASS). Its
   weakness — shared with the changelog's "naming the post path and the
   date" phrasing — is that the guard's fail message names the post path but
   does not echo the offending date string. Cosmetic: the exit-1 names the
   post that carries the bad date (the 04-31 post in the pair), so the
   requirement "exit 1, naming 04-31" is met by path. Not a defect, recorded
   for completeness. I did not trust its verdicts: the key cases above were
   re-run with my own driver (25 cases, all passed, all reverted).

7. **Numbers and record.** Recomputed from the imported module
   (`import { posts } from app/lib/posts.js`, 9 posts): 2026-08-11 → 3 posts,
   2026-08-14 → 4 posts (cap 1 each day), ISO week 2026-08-10 → 8 posts
   (cap 3, 2.7x); /blog on 2026-08-09 (Sunday) correctly falls in the
   previous week. The changelog entry names all three rejections (1749995,
   003522d, aa1d0d1) and the round-trip fix, carries `Origin: delegated`,
   `Track: build`, `Agent: opencode (deepseek-v4-flash)`, and
   `git diff origin/main -- CHANGELOG.md` is additions only, all inside the
   117 section (201 added lines, 0 removed).

8. **Scope.** `git diff origin/main --name-only` = CHANGELOG.md, package.json,
   scripts/check-publishing-quota.mjs, docket/done/2026-08-15-publishing-quota-unenforced.md,
   and the three review artifacts. policy.yml, .github/, prompts/, CHARTER.md,
   check-track-scope.mjs, app/ all untouched.

9. **Green on true tree.** `node scripts/check-publishing-quota.mjs` → exit 0
   (`ok 9 posts; day cap 1, week cap 3`). `node scripts/round.mjs check` →
   green: lint, docket valid, track scope, production build, and all route
   checks passed; no group SKIPPED.

## Verdict

Approve. The round-trip guard accepts exactly the real calendar dates
(leap-year and month-13 boundaries included), the rollover pair is dead, the
three prior holes stay closed, the diff-aware semantics behave as designed,
the record is accurate to the imported data, and the tree is green. The two
noted observations — the fail message not echoing the bad date string, and
0000-01-01 counting as real — are cosmetic and quota-neutral, and do not
block merge.
