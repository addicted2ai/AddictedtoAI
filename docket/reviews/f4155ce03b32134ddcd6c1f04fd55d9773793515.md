Commit: f4155ce03b32134ddcd6c1f04fd55d9773793515
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 133

Review of round 133 (maintain), branch `loop/maintain/staleness-report-sweep`,
PR #92, head `f4155ce03b32134ddcd6c1f04fd55d9773793515`. The round's two
corrections — the exit-code sentence and the /blog verified-date attribution —
were both checked by running the red states and by reading the record, not by
reading the entry.

## Environment

`git status --porcelain` empty, on `main` at start. The session endpoint at
127.0.0.1:4097 listed this session (`ses_ff8088af`, "PR #92 staleness sweep
review", updated at review time) and no other session whose `time.updated`
was advancing: the two most recent neighbours were the round's own session
(00:27:23Z) and the failed round-132 reviewer (last updated 00:14:45Z). HEAD
after checkout == f4155ce03b32134ddcd6c1f04fd55d9773793515, matching the
brief.

## What the entry claims, and how each claim was falsified

**1. The report's headline counts (128 / 127 / 1 / 0).** `node
scripts/staleness-report.mjs` (clean tree) prints exactly
`128 published artefacts judged: 127 within window, 1 recorded-unverified
within window, 0 stale or missing a date`, exit 0. Independent count:
`grep -c "verified:"` = tool-categories 19, retirement-commitments 12 (11
rows; the 12th hit is a header comment, and the Meta row is `verified: null`
with `unverifiedSince: "2026-08-15"`, per grep at lines 127-128),
retirement-dates 87, posts 9, demos 2 → 19 + 11 + 87 + 9 + 2 = 128. The one
recorded-unverified is Meta/Llama, within its 45-day window (expires
2026-09-29 = 2026-08-15 + 45 days, arithmetic checked). The standing WARN
about the missing `staleness_days.retirement_calendar` key prints, naming the
open meta item.

**2. `--json` contract.** `node scripts/staleness-report.mjs --json` exits 0,
`ok: true`, artefacts 128, stale `[]` — pure JSON on stdout (verified: stdout
parses as the object shown, WARNs on stderr).

**3. The central correction: exit codes per mode.** Backdated /blog's
`verified` to 2026-05-01 in app/lib/posts.js in the working tree: human mode
printed `FAIL  /blog  verified 2026-05-01 — 107 days ago, past the 90-day
window` and exited 1 (107, not the entry's 106, because the run happened on
2026-08-16 UTC — a day after the round; 2026-05-01 → 2026-08-15 is 106 days,
arithmetic checked, so the entry's number was measured on its own date);
`--json` exited 0 with `ok: false` and the same artefact in `stale`
(`verified: "2026-05-01"`, windowDays 90, ageDays 107). Restored with
`git checkout -- app/lib/posts.js`; `git status --porcelain` clean.

Removed a demo's `verified` line (first line of app/lib/demos.js): human mode
exited 1 with `FAIL  anatomy-of-a-round  no verified date — every artefact
must carry one`; `--json` exited 0 with `ok: false`, `"verified": null`.
Restored byte-identically, porcelain clean.

Parser-stop case: reduced demos.js to a comment — human mode exited 1 and
`--json` exited 1, both printing `FAIL  no demos matched in app/lib/demos.js
the parser regex no longer matches the file — fix it, don't ignore it` on
stderr with empty stdout. This is the only case where `--json` exits 1, and
it matches the corrected header comment. Restored, porcelain clean.

So the entry's "measured" claims are measured: the "exit 1" sentence is true
only of the human mode (what prebuild runs) and of report-cannot-be-produced
states in both modes; `--json` carries `ok: false` at exit 0. The script's
header comment now states the per-mode contracts and names the correction;
behaviour is unchanged.

**4. The /blog verified-date chain (round 119, not round 104, is the most
recent fact-check).** Read the changelog entries for rounds 104 (line 3218:
#58's `human-owned-paths` failure observed, run completed 2026-08-14T17:47:38Z,
count corrected seven → eight), 105 (line 3120: sweep re-run that evening,
eight, failing set unchanged since round 104) and 119 (line 1502: sweep
re-run live on 2026-08-15, count 8, failing set {25, 27, 39, 40, 42, 50, 52,
58}, 72 merged, "the nine newcomers all passing the check", fresh output
checked in, swept 2026-08-15T09:20:06.810Z). `git log --oneline --
scripts/one-limit-count-sweep.json` shows aa1cc84 (round 105, #61),
b790dff (audit re-sweep) and a7201f1 (round 119, #75) as the latest three
commits. The sweep file at a7201f1 (and at HEAD) carries
`sweptAt: "2026-08-15T09:20:06.810Z"`; the file at aa1cc84 carries
`2026-08-14T19:42:15.392Z` — both matching the entry's timestamps.
`node scripts/check-one-limit-count.mjs` exits 0: count 8, 8 set members,
swept 2026-08-15T09:20:06.810Z, 0 days old, within the 30-day window. The
page renders from this file: app/lib/one-limit-count.js reads
`scripts/one-limit-count-sweep.json` and builds `countSentence` =
"the sweep behind the count shown here ran on 15 August 2026 and counts
eight (...)", rendered at app/blog/page.js:217. So the most recent check of
the post's most volatile claim is round 119's, and the posts.js `verified`
date of 2026-08-15 (line 53) is measured, not assumed. The correction goes
beyond what round 132's review reached — the entry says so itself, honestly —
and the round-132 entry is not rewritten.

**5. Round 132's two non-blocking findings — both addressed.** The round-132
artifact (docket/reviews/aaa7d2d1dad5124f41968ccabf8f8e04db2931d1.md) finding
1 said the exit-code sentence and header comment were false in `--json` mode;
finding 2 said the "most recent fact-check" attribution was loose because
round 105 also re-swept. The round-133 entry addresses both — measured in
sections 3 and 4 above. One nuance: round 132's review said the 2026-08-14
date was "right either way" under 104-vs-105; round 133 moves the date to
2026-08-15 on the strength of round 119's live sweep, which the review did
not look at (it checked page.js history, not the sweep file). That is a
stronger correction than the review asked for, and it is supported by the
record as read above.

**6. page-origins moves exactly ten routes.** The diff's `+...: 133` lines
are exactly `/, /blog, /blog/frontier-cyber, /blog/claude-code-auto-mode,
/blog/cyber-eval-cascade, /blog/gpt-5-6-price-drop, /blog/fable-5-export-
controls, /blog/chatgpt-ads, /blog/gemini-3-7-flash, /blog/ultrafast-mode` —
ten, nothing else. All ten list app/lib/posts.js in route-files.js (checked
route by route). Nothing else in the map changed (all other routes keep their
rounds; confirmed by check-ai-disclosure's output below).

**7. Changelog.** `git diff origin/main...HEAD --numstat -- CHANGELOG.md` =
`113 0` — additions only; round 132's entry untouched (append-only, rule 5).
The entry carries `Origin: delegated`, `Track: maintain`, `Agent: opencode
(deepseek-v4-flash)`. Round 133 equals the count of dated `### YYYY-MM-DD`
entries in CHANGELOG.md: 133 dated headers, the trailing `### YYYY-MM-DD`
template line excluded.

**8. Scope.** `git diff origin/main...HEAD --name-only` = CHANGELOG.md,
app/lib/page-origins.js, app/lib/posts.js, scripts/staleness-report.mjs —
all four inside maintain's allowed set (app/, scripts/, CHANGELOG.md).

**9. Guards.** No diff to scripts/check-track-scope.mjs,
scripts/automerge-origin.mjs, scripts/check-review-artifact.mjs, or any
other guard; nothing was loosened.

**10. Other exit-0 claims.** `node scripts/check-loop-history-snapshot.mjs`
(snapshot well-formed, matches live API, 125 round entries), `node
scripts/check-ai-disclosure.mjs` (all routes resolve and match git history;
the ten routes report producing round 133 and everything else unchanged),
and `node scripts/preflight.mjs` (clear, with the standing retirement-
calendar WARN) all exit 0. The reader-visible render path is confirmed at
source level: app/blog/page.js and every post page render `Facts verified
<time dateTime={post.verified}>` from posts.js, and /demos renders the same
under both demo headings; the served-site render was the round's own check
(round.mjs check, not rerun here by rule).

## Findings

None blocking. Two notes, neither a defect in the round's work:

- The red-state run on 2026-08-16 UTC reports 107 days where the entry says
  106 — a one-day age-off from the date rolling over, not an error; the
  entry's arithmetic is correct for the day it was written.
- The round-132 review's finding 2 is corrected more sharply than the review
  asked (the date moved, not just the attribution); this is supported by the
  record and the sweep file's own history.

## Could not check

Nothing material. The built-and-served render of the verified dates was
checked by the round's own `round.mjs check` and is confirmed at source
level here; I did not rerun `round.mjs` per review-session rules.

## Verdict

Approve. Every claim in the entry was falsified by running the red states
(exit codes, JSON contract, parser-stop) or by reading the record (104/105/
119 chain, sweep timestamps, round count, append-only changelog), the tree
was restored byte-identically after each red state, and no guard or
human-owned path was touched.
