Commit: 103a6335f55cd9cba0bc6df011b0e815a8005c5f
Verdict: request-changes
Reviewer: opencode (deepseek-v4-flash)
Round: 124

Reviewed `loop/build/retirement-commitments-staleness` at HEAD
103a6335f55cd9cba0bc6df011b0e815a8005c5f (matches the dispatched SHA). The
tree was clean throughout; every scratch edit was restored with `git checkout`
and `git status --porcelain` was empty before each next proof.

## Blocking finding

The changelog entry's conflict report claims policy.yml's "Nothing parses
this yet" header comment "stopped being true rounds ago (three checks parse
it, including this one)". The number is falsified. Running
`grep -rln "policy.yml" scripts/` and inspecting each hit, FIVE check scripts
parse policy.yml with js-yaml: `check-tool-staleness.mjs` (this round's),
`check-retirement-staleness.mjs` (line 48), `check-one-limit-count.mjs`
(line 110), `check-loop-history-snapshot.mjs` (line 67) and
`check-publishing-quota.mjs` (line 56) — all five also run in the prebuild
(package.json line 7) — plus `dispatch.mjs` (line 38) and `preflight.mjs`
(line 36). No reading of the count — checks only, staleness-window checks
only, pre-existing checks only — yields three. This is a checkable number in
the record that does not survive its own falsifying command, which is the
defect class this project rejects rounds for. The substance of the report
(the comment exists at policy.yml line 8 and has been wrong for a while) is
true; the parenthetical count is not. Correct the number (e.g. "five checks
parse it, including this one") and the round can be approved. A falsified
claim in a changelog entry is blocking on its own, so the verdict is
`request-changes` even though every other claim measured held.

## Everything else measured, and held

1. **Counts and green line.** `node scripts/check-tool-staleness.mjs` on the
   branch prints, exactly: `ok    19 Directory tools and 11
   retirement-commitment rows judged within the 45-day window (1 unverified,
   recorded)`, exit 0 — byte-for-byte the entry's quoted green line. Independent
   counts agree: 19 `href:`/`verified:` lines in `app/lib/tool-categories.js`;
   11 `vendor:` rows in `app/lib/retirement-commitments.js`; 10 rows carry
   `verified: "2026-08-14"` and exactly one (Meta (Llama), line 127) is
   `verified: null` with `unverifiedSince: "2026-08-15"` (line 128).
2. **Prove-it-can-fail, re-run on scratch copies.** (a) OpenAI's `verified`
   backdated to 2026-05-01: `FAIL  OpenAI: verified 2026-05-01 — 106 days
   ago, past the 45-day window`, exit 1. 106 re-derived: 30 (May) + 30 (June)
   + 31 (July) + 15 (August) = 106; correct for 2026-05-01 → 2026-08-15. (b)
   Meta's `unverifiedSince` line deleted: `FAIL  Meta (Llama): verified: null
   with no unverifiedSince record — …adding unverifiedSince: "YYYY-MM-DD" and
   renewing it within the window`, exit 1. (c) `unverifiedSince` backdated to
   2026-05-01: `FAIL  Meta (Llama): verified: null — the record that it stays
   unverified (unverifiedSince 2026-05-01) is 106 days old, past the 45-day
   window; re-verify the vendor's page and set a verified date, or renew the
   record`, exit 1. (d) `vendor:` renamed to `vendor :` in the file only:
   `FAIL  no retirement-commitment rows matched in
   app/lib/retirement-commitments.js`, exit 1 — this provably exercises the
   commitments parser, since the tool parser still matched its 19 blocks and
   the failure names the commitments file. All four restores green (exit 0),
   `git status --porcelain` clean before each next proof.
3. **Null-row policy.** On the actual tree the Meta row prints `WARN  Meta
   (Llama): unverified — last re-checked 2026-08-15 (unverifiedSince); this
   check will fail once that record is past the 45-day window unless the
   vendor's page is re-verified or the record is renewed` and the check exits
   0. The expiry is enforced as a hard failure, not a warning: proof (c)
   above shows a record past the window exits 1. No forever-green path for a
   null row: the record expires on the same window, so an honest record keeps
   the build green for at most 45 days (the only longer-lived escape is a
   future-dated record, which is equally true of the Directory's future-dated
   `verified` values and is not a regression). Directory side unchanged:
   deleting one tool's `verified` line produces `FAIL  ChatGPT: no verified
   date — every entry must carry one`, exit 1 (recordedNullAllowed is false
   for tool blocks).
4. **page-origins.js.** `app/lib/retirement-commitments.js` is a listed
   source file of `/what-vendors-promise` (`app/lib/route-files.js` lines
   58-61), consumed by `scripts/check-ai-disclosure.mjs`. `git log` on both
   listed files shows the last main change before this round was round 116's
   `f6bbe69` (the audit that corrected the Foundry Meta citation — the file's
   comment chain 113 → 116 → 124 is accurate), and nothing in rounds 117-123
   touched either file; HEAD 124 modifies `retirement-commitments.js`, so
   "the newest change is this round's" is consistent with the mechanism's
   semantics. Positional round: 124 `### ` entries in CHANGELOG.md plus one
   `### YYYY-MM-DD` template heading = 125 heading lines; the new entry is
   the 124th real entry, correctly numbered.
5. **Brief-conflict report.** (i) The docket item's Evidence section still
   reads "9 carrying a `verified: "2026-08-11"` date, 2 (Google, Meta)
   carrying `verified: null`"; the file now has 10 rows at 2026-08-14 with
   Google (Gemini API) verified 2026-08-14 (line 107) and only Meta null. The
   reported conflict is real and the report is accurate. (ii) policy.yml line
   8 carries the "Nothing parses this yet" comment verbatim, and
   check-tool-staleness.mjs indeed parses policy.yml (`js-yaml` load at the
   top, window 45 from `staleness_days.directory_entry`, policy.yml lines
   61-62) — true as far as it goes, except the "three checks" count (see
   blocking finding).
6. **Meta re-fetch.** Fetched this session: `www.llama.com/docs` — HTTP 400
   (491,800-byte body) to a browser User-Agent, HTTP 301 then 200 to a plain
   curl UA, final URL `developer.meta.com/ai/docs/overview/`, and the fetched
   final HTML has no `<main>`, no model-name content and no Next.js data —
   a client-rendered shell, matching "no readable docs content". Bytes:
   entry says 297,746; my fetches returned 297,745 and 297,748 — the live
   page shifts by a couple of bytes between fetches, so the figure is a point
   measurement within noise (non-blocking; the llms.txt figure did reproduce
   exactly). `developer.meta.com/llms.txt` — HTTP 200, exactly 1,769 bytes,
   listing `Llama` at `https://ai.developer.meta.com/`; that host returns
   HTTP 302 into `auth.meta.com/oidc/` (OAuth login), and
   `ai.developer.meta.com/llms.txt` returns HTTP 404. All four status/behaviour
   claims reproduce.
7. **Scope and record.** `git diff --name-only origin/main...HEAD` lists
   exactly `CHANGELOG.md`, `app/lib/page-origins.js`,
   `app/lib/retirement-commitments.js`,
   `docket/done/2026-08-11-retirement-commitments-staleness.md` (rename from
   `docket/open/`, R064) and `scripts/check-tool-staleness.mjs` — all build
   scope, nothing guarded. The CHANGELOG diff is purely additive (the only
   `-` line is the diff's own `---` header); no past entry rewritten. The
   entry carries `Origin: delegated`, `Track: build`, `Agent: opencode
   (deepseek-v4-flash)`. All four Done-when boxes in the moved item are
   ticked, and the separate retirement-calendar window item
   (`docket/open/2026-08-14-retirement-calendar-staleness-window.md`) is
   untouched.
8. **The build still works.** `npm run lint` — no warnings or errors.
   `npm run build` — production build succeeds (all routes, including
   `/what-vendors-promise`, prerendered). `node scripts/check-docket.mjs` —
   `ok    87 docket item(s) valid (52 open)`, exit 0. (The full
   `round.mjs check` server run was not executed; nothing was left listening
   on ports 3000/8101.)

## Non-blocking concerns

- The entry says the fresh null row's warning names "its expiry". The WARN
  names the row, the record date and the window, but never states the expiry
  date itself (2026-09-29 is derivable, not printed). Minor wording
  over-claim; the louder point is the record date plus window, which is what
  the warning prints.
- The 297,746-byte figure for llama.com/docs is within a few bytes of two
  independent re-measurements (297,745 / 297,748) — a live page; not a
  falsification, but worth knowing the number is not stable to the byte.
- The entry's note that the start prompt hardcodes `supervised` but `delegated`
  is recorded per the brief is recorded faithfully; the field is present and
  has a valid value, which is what the format requires.

## Could not check

- The full `node scripts/round.mjs check` (route checks against a server on
  port 3000) was not run; the brief marked it optional. Lint, build, the
  staleness check itself and check-docket were all run directly.
- Whether any row actually goes stale before its window, and whether Meta's
  page ever becomes fetchable — the entry itself marks these "not yet
  measured"; they are inherently future facts.
