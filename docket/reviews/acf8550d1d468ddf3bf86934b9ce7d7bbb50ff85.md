Commit: acf8550d1d468ddf3bf86934b9ce7d7bbb50ff85
Verdict: request-changes
Reviewer: opencode (deepseek-v4-flash)
Round: 152 (seventh review of PR #115 — the opening paragraph)

## Summary

Seventh review of PR #115, `loop/meta/docket-filing-gate`, at
acf8550d1d468ddf3bf86934b9ce7d7bbb50ff85. The commit replaces round 152's
opening paragraph, which until now described the **first** shape of the gate —
"fails any branch that grows a track's open count while that track is over its
`queue_budget`" — and claimed "This paragraph describes the corrected gate" while
the live rule sat five hundred lines down. The replacement states the shipped
ceiling rule, points at the residual, and summarises the four findings. All of
that is done correctly. **The blocking defect is in the account of the reviews.**
It says "not one was asked to check whether the entry's own summary matched the
code it summarised" and "the lesson is about the briefs rather than the
reviewers." The fifth pass was a claim audit whose brief, as its own artifact
records, was that "every claim in the entry and the new docket item was to be
checked against the tree and against reality." The opening paragraph is a claim
in the entry. Pass five was asked to check it, did not, and should have caught
this. The entry's account lets the reviewers off at the orchestrator's expense.

## What I ran, and what it said

The commit under review touches exactly one file: `CHANGELOG.md`, round 152's
entry only. The second file in the wider range
(`docket/reviews/9ddb10232c47539758a717862c4be0be8946f196.md`) is the sixth
pass's own artifact, written by the sixth reviewer after the baseline — the same
situation the sixth pass itself noted about the fifth pass's artifact. Not stray
material. No `scripts/`, no docket item, no `policy.yml`.

**1. The new opening vs the shipped rule.** The paragraph reads:

> `scripts/check-docket.mjs` now fails any branch that leaves a budgeted track
> holding more open items than the greater of what `origin/main` already held and
> what `policy.yml` says the track should hold, and any branch that raises a
> budget and spends it in the same diff.

The shipped rule in `scripts/check-docket.mjs`:

    ceiling(track) = max( base_total(track), budget(track) + base_blocked(track) )
    FAIL if head_total(track) > ceiling(track)

The prose states the ceiling rule, and the exact formula is printed two lines
below it in the same opening block. The prose's "what `policy.yml` says the track
should hold" omits the `+ base_blocked` term; the formula immediately below
carries it, and the same opening explains the blocked-on treatment. On the
current tree the two descriptions give identical numbers for every track (author
30, build 14, meta 29). This is a simplification, not a misstatement of the
shipped rule — it is the outcome-focused ceiling rule now, not the dead
"already-over-budget" trigger. The one-line gloss understating the ceiling by the
base's blocked count is noted; it does not misstate which rule ships.

**2. The residual is pointed at honestly.** The paragraph closes the rule
statement with: "**This paragraph describes the rule as shipped; what it still
does not bound is stated under *What this gate does, and what it does not do*,
and it is not small.**" That section (CHANGELOG.md:483-497) names the residual
plainly: the gate does not bound `scout`, `maintain` or `audit`, and the fourth
review's attack — move 30 author items to `maintain`, file 30 new author items,
one diff, queue 60 → 90, exit 0 — "works today and works every round." A reader
is pointed at the limit, told it is not small, and given the exact numbers. The
60 → 90 attack would not surprise a reader of this opening.

**3. The four findings in order.** The paragraph summarises them as: items filed
with `blocked-on: maintainer` invisible to the count (pass one — correct); existing
items marked blocked to manufacture room (pass two — correct); thirty items filed
into an empty track, because a rule that only fires on an already-full track never
compares the head count to the budget (pass three, the build-flood — correct); and
a fourth pass walking the shipped rule by relabelling items into a track that
carries no budget (pass four, the unbudgeted-track relabel — correct). All four
are described accurately and in order, and each is recorded with its pasted
output in the review-and-fix sections the paragraph points to. (Pass three also
found the track-move into a *budgeted* track; that is recorded in the
review-and-fix section at CHANGELOG.md:446-450 but is not one of the four in the
summary. The task's own framing lists exactly these four findings, so the summary
matches the four it claims; the track-move is a fifth finding preserved in the
detailed sections, not a misdescription of the four.)

**4. The account of the reviews — blocking.** The paragraph says: "Six
adversarial review passes did not catch it. They were not careless — every one of
them was pointed at the rule, the exploits, the invariant or the corrections, and
every one of them found something real there. Not one was asked to check whether
the entry's own summary matched the code it summarised, so not one did. It was
found by an unprompted re-read after the sixth pass had already approved. The
lesson is about the briefs rather than the reviewers."

For passes one, two, three, four and six this is fair: each was pointed at the
mechanism and each found something real there. **For pass five it is not.**
`docket/reviews/5bd1b360fc5a31884faaafdd157e7ddae1a0afca.md` opens: "This pass
was a claim audit, not an attack round: the rule was to be read for movement
only, and every claim in the entry and the new docket item was to be checked
against the tree and against reality." The opening paragraph is a claim in the
entry. Under its own recorded brief, pass five was asked to check the entry's
claims — including the summary — against the tree, and it did not check this one.
It checked the enumerated claims (Origin, the self-exemption, the consequence,
the gate's stated limit, the boundary, the new item's facts) and missed the
paragraph that sits where the entry is read first and rendered on the site. So
the entry's "not one was asked to check whether the entry's own summary matched
the code it summarised, so not one did" is false for pass five, and its
characterisation that all six passes were "pointed at the rule, the exploits, the
invariant or the corrections" is false for pass five too — pass five was pointed
at the entry's claims, which is exactly the summary-checking territory. The
account lets the reviewer off at the orchestrator's expense: the brief did ask,
the reviewer did not do that part of it, and the entry blames the briefs for a
miss the pass-five brief covered. That is a defect in the record under the
verdict standard's "unfair in either direction", and it is the same defect class
this entry exists to record: a sentence that misdescribes what happened.

For the record, I judge my own fifth pass: it should have caught this. Its brief
covered the entry's claims; the opening paragraph is the entry's most prominent
claim; it was checkable against `check-docket.mjs` in one read. I did not read it
as a claim to verify. The entry is generous to me where it should have been
exact.

**5. No unhappened claims.** Searched the changed region for "seventh", "7th",
"this review", "unprompted", "re-read". The only match is the sentence "It was
found by an unprompted re-read after the sixth pass had already approved" — a
claim about the past, consistent with the commit message that produced this very
commit, and it asserts nothing about this review or any future result. Clean.

**6. Checks.** `node scripts/check-docket.mjs` — exit 0; meta 29 ≤ 29, author 30
≤ 30, build 0 ≤ 14; the two settings-UI items blocked on base and still counted
in the head totals; the `not bounded` line printed. `node scripts/round.mjs check`
— exit 0; lint, docket valid, track scope, production build, route checks all ok,
no group skipped. Rule 5 — `git diff origin/main...HEAD -- CHANGELOG.md` has zero
deletions (the single `-` line is the diff header). The earlier findings are
unsoftened: the four review-and-fix sections with their pasted outputs, the
build-flood red/green pair, and the residual statement all still stand.

## Why this is blocking

The replacement paragraph itself is correct — it states the shipped rule, the
formula is two lines below, the residual is pointed at and named, and the four
findings are described accurately in order. The defect is the account of the
reviews that follows: it says no reviewer was asked to check the entry's summary
against the code, when the fifth pass's own recorded brief was to check every
claim in the entry against the tree, and the opening paragraph is a claim in the
entry. Pass five should have caught it. The entry lets the reviewers off at the
orchestrator's expense and states a falsehood about the briefs in the permanent
record. The fix is small: correct the account so it does not claim that no pass
was asked to check the summary, and does not claim that every pass was pointed at
the mechanism — pass five was a claim audit pointed at the entry's claims, and it
missed the opening one.

Everything else in this revision is verified and good: the rule statement, the
formula, the residual pointer, the four findings, the checks, rule 5, and the
unsoftened record of the earlier findings.