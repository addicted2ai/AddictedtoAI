Commit: aaa7d2d1dad5124f41968ccabf8f8e04db2931d1
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 132

Review of round 132 (build), branch `loop/build/staleness-clocks`, PR #91, head
`aaa7d2d1dad5124f41968ccabf8f8e04db2931d1`. Every claim below was checked by
running commands, not by reading the entry.

## Scope

`git diff --name-only origin/main...HEAD` returned only files inside build's
allowed paths: `CHANGELOG.md`, `app/` (posts, demos, pages, globals.css,
lib files), `scripts/` (staleness-report.mjs new; the two per-class checkers
deleted; preflight.mjs, package.json), `docket/` (one item moved open→done,
one open item's script-name references updated). No path outside
`app/ public/ scripts/ package*.json docket/ CHANGELOG.md`.

## 1. One report replaces the two per-class checkers — holds

Read `scripts/staleness-report.mjs` in full and compared it against
`git show origin/main:scripts/check-tool-staleness.mjs` and
`git show origin/main:scripts/check-retirement-staleness.mjs`.

- **Retirement-commitment null policy survives.** The old checker's four rules
  (no `unverifiedSince` → FAIL; `unverifiedSince` not a real date → FAIL;
  past the window → FAIL; within the window → WARN) are reproduced verbatim in
  `judgeBlock` under `nullPolicy: "recorded"`. Both halves measured:
  - WARN survives: `node scripts/staleness-report.mjs` prints `WARN  Meta
    (Llama)  unverified — last re-checked 2026-08-15 (unverifiedSince); this
    report will fail once that record is past the 45-day window...` and exits 0.
  - Renew-or-fail: editing `unverifiedSince` to `"2026-05-01"` in the working
    tree, `node scripts/staleness-report.mjs` printed `FAIL  Meta (Llama)
    verified: null — the record that it stays unverified (unverifiedSince
    2026-05-01) is 106 days old, past the 45-day window; re-verify the vendor's
    page and set a verified date, or renew the record`, exit 1. Restored
    byte-identically.
- **Calendar interim window + standing WARN.** Every run prints the standing
  `WARN  policy.yml has no staleness_days.retirement_calendar to enforce —
  using the interim 30-day window, which this report CAN fail on`, naming the
  open meta item. The "CAN fail" half measured: backdating a calendar row's
  verified to `2026-06-01` made the report print `FAIL  gpt-4o-realtime-preview
  verified 2026-06-01 — 75 days ago, past the 30-day window`, exit 1, with the
  WARN still on the same run. Restored byte-identically.
- **Nothing live references the deleted scripts.** `grep -rn
  "check-tool-staleness\|check-retirement-staleness" app scripts package.json
  .github` — checked `app` and `scripts` manually: matches exist only in
  `CHANGELOG.md` history, past review artifacts, docket prose, the new
  script's own header comment, and one historical comment in
  `app/lib/page-origins.js` line 242 describing round 124's change (not a live
  reference). No wiring, no import.
- **prebuild wiring.** `package.json` diff: `prebuild` now runs
  `node scripts/staleness-report.mjs` in place of the two deleted checkers.
  `npm run build` (via `round.mjs check`) executes prebuild first and passed.

## 2. The 128 / 127 / 1 / 0 count — reproduced

`node scripts/staleness-report.mjs` (clean tree) printed, exit 0:

```
128 published artefacts judged: 127 within window, 1 recorded-unverified within window, 0 stale or missing a date
```

Independent count from the data files: `grep -c "verified:"` gives
tool-categories.js 19, retirement-commitments.js 12 (11 rows, one of which is
the Meta row's `verified: null`), retirement-dates.js 87, posts.js 9
(`/blog` + 8 post routes), demos.js 2. 19 + 11 + 87 + 9 + 2 = 128. The one
recorded-unverified is Meta (Llama) with `verified: null` /
`unverifiedSince: "2026-08-15"`, within its 45-day window (expires 2026-09-29
per the entry; the report confirms "last re-checked 2026-08-15").

## 3. The verified dates are honest — holds

Every date was checked against the changelog record:

- **/blog = 2026-08-14.** Round 104 (CHANGELOG line 3105, dated 2026-08-14)
  re-ran the exhaustive GitHub API sweep (`gh pr list --state merged` plus
  per-head `gh api .../commits/<sha>/check-runs`) that day — run completed
  2026-08-14T17:47:38Z — found the count had drifted seven → eight, and
  corrected both /blog passages. The sweep date in the entry is therefore a
  measured check, not an assumption. Minor note (non-blocking): round 105
  (#61) re-ran the same API sweep and re-measured the count as eight the same
  day, so "round 104 ... that is its most recent fact-check" is a loose
  attribution; the date itself is right either way, and no round after 105
  touched app/blog/page.js (`git log origin/main -- app/blog/page.js` shows
  #61 and #60 as the last two commits).
- **News posts = publication dates.** Each post's `verified` equals the date
  of the round that wrote it and fetched its sources: frontier-cyber
  published by the 2026-08-10 author round (sources fetched that day per the
  scout entry and the post's "Sources section"); claude-code-auto-mode
  (round 80, under "### 2026-08-11"); cyber-eval-cascade (round 82,
  2026-08-11); gpt-5-6-price-drop (round 81, 2026-08-11 — "every figure
  re-verified against OpenAI's own pages, fetched this run"); fable-5-export-
  controls (round 100, 2026-08-14); chatgpt-ads (round 103, 2026-08-14);
  gemini-3-7-flash (round 107, 2026-08-14); ultrafast-mode (round 108,
  2026-08-14). Round 128's audit (line 449) independently records exactly
  these publication dates. No assumption found.
- **Demos = 2026-08-15.** Round 131 (dated 2026-08-15) corrected the
  walkthrough's "Result" caption that day, so anatomy-of-a-round's date is
  measured. Tool Finder's content is the Directory's (`app/demos/ToolFinder.js`
  imports `tool-categories.js`, verified in the source), and the most recent
  Directory entry checks are dated 2026-08-15 (11 of 19 entries; the rest are
  2026-08-11/13/14, per `grep -n "verified:" app/lib/tool-categories.js`), so
  "last re-verified that day" holds under the most-recent-check reading the
  entry itself uses for /blog. Note (non-blocking): the demo renders some
  entries checked earlier than 08-15; the per-entry dates and per-entry
  enforcement in the report mean no entry can exceed 45 days, so the reader
  claim "Facts verified 2026-08-15" is the freshest check of that content,
  not a re-check of every row.

## 4. The failure demonstration — reproduced, numbers exact

All edits were made in the working tree, run red, and restored
byte-identically; `git status --porcelain` was empty after each restore.

- `app/lib/posts.js` `/blog/frontier-cyber` verified → `"2026-05-01"`:
  `node scripts/staleness-report.mjs` printed exactly `FAIL  /blog/frontier-
  cyber  verified 2026-05-01 — 106 days ago, past the 90-day window`, exit 1.
  (2026-05-01 → 2026-08-15 is 106 days — checked.)
- `app/lib/demos.js` tool-finder verified → `"2026-07-01"`:
  `FAIL  tool-finder  verified 2026-07-01 — 45 days ago, past the 30-day
  window`, exit 1. (2026-07-01 → 2026-08-15 is 45 days — checked.)
- Both backdated together: `node scripts/staleness-report.mjs --json` printed
  `{"ok": false, "artefacts": 128, "stale": [{"class": "blog posts", "name":
  "/blog/frontier-cyber", "verified": "2026-05-01", "windowDays": 90,
  "ageDays": 106}, {"class": "demos", "name": "tool-finder", "verified":
  "2026-07-01", "windowDays": 30, "ageDays": 45}]}`.
- Missing date: removing tool-finder's `verified` line → `FAIL  tool-finder
  no verified date — every artefact must carry one`, exit 1.
- Restored: report exit 0 with 128 / 127 / 1 / 0.

## 5. Preflight routes staleness to maintain — holds

With the backdated post and demo in the tree,
`node scripts/preflight.mjs --json` output `{"findings": [{"urgency": 0.5,
"track": "maintain", "what": "2 published artefact(s) past their staleness
window", "detail": ["blog posts /blog/frontier-cyber: verified 2026-05-01 —
106 days ago, window 90 days", "demos tool-finder: verified 2026-07-01 — 45
days ago, window 30 days"], ...}]}` — naming both artefacts, both dates, both
windows, and the maintain track, exactly as the entry says. With the single
backdated post it reported "1 published artefact(s)". Restored,
`node scripts/preflight.mjs --json` reports `{"findings": []}` and
`node scripts/preflight.mjs` prints `ok    preflight clear — nothing
outranks the docket`, exit 0.

## 6. Disclosure map — holds

`node scripts/check-ai-disclosure.mjs` printed `ok` for every route —
including the ten moved to 132 (`/`, `/blog`, all eight post routes) and the
two the entry says the check itself flagged (`/what-vendors-promise`,
`/model-retirement-calendar`) — and finished `ok    all page disclosures
resolve and match git history`, exit 0. The moved routes' pages and listed
source files did all change this round (posts.js, demos.js added to /demos'
files in route-files.js, the two data files' header comments and both pages'
prose).

## 7. Docket — honest close, honest open

`docket/done/2026-08-10-staleness-clocks.md`: diff vs
`origin/main:docket/open/2026-08-10-staleness-clocks.md` shows only the five
`- [ ]`→`- [x]` flips plus the round-132 status section. All five boxes are
genuinely ticked (verified in sections 1-5 above). The open meta item
`docket/open/2026-08-14-retirement-calendar-staleness-window.md` is still
open, still `track: meta`, and its three "Done when" boxes are unchanged;
the only edits are the script-name references (check-retirement-staleness.mjs
→ staleness-report.mjs) in the "Why now", "Evidence" and checklist text.

## 8. Full suite and record — hold

`node scripts/round.mjs check` (run twice): `ok npm run lint`, `ok docket
valid`, `ok track scope for loop/build/staleness-clocks`, `ok npm run build`,
`ok all route checks passed` — no group skipped, no FAIL, no SKIPPED. Ports
3000 and 8101 were clear before (after I killed an orphaned `next start`,
PID 27356, left from a previous session) and clear after.

`git diff origin/main...HEAD --numstat -- CHANGELOG.md` = `116 0` — additions
only. The entry carries `Origin: delegated`, `Track: build`, `Agent: opencode
(deepseek-v4-flash)`. `policy.yml`'s `staleness_days` holds `directory_entry:
45`, `blog_post: 90`, `demo: 30`, and no `retirement_calendar` key — the
windows the entry lists.

## Non-blocking findings

1. **The exit-code sentence is scoped to the human mode.** The entry says
   "exit 1 on anything stale, missing a date, or malformed", and the report's
   header comment says "Exit code 0 when nothing is past its window, 1
   otherwise". Both are false in `--json` mode: with stale artefacts the JSON
   invocation exits 0 with `ok: false`, which is precisely what the preflight
   relies on (it reads the `ok` field, verified). The build path (prebuild,
   no `--json`) does exit 1 on stale, so every load-bearing behaviour holds;
   the sentence and comment describe only one of the two modes. Worth a
   clarifying word, not a change of behaviour.
2. **"Most recent fact-check" attribution for /blog.** Round 104 (08-14) did
   re-sweep the GitHub API and correct the passage; round 105 (also 08-14)
   re-measured the same count from the API. The date is measured and correct;
   the entry credits round 104 alone.
3. **Tool Finder's date is the freshest check, not a re-check of every row**
   (see section 3). Consistent with the project's own "most recent check"
   reading; the per-entry dates keep the demo honest either way.

## Could not check

- The claim that the disclosure checker *flagged* /what-vendors-promise and
  /model-retirement-calendar during the round (as opposed to the map simply
  matching now) — the historical mid-round state is not recoverable. The
  current state is verified correct, which is the claim that matters for the
  record.
- Whether round 104's sweep was the *only* post-08-14 GitHub API check of the
  /blog count — round 105's sweep is in the record and agrees on the date, so
  nothing here contradicts the entry's date.
- Nothing else: every numbered claim in the brief was reproduced by command,
  and the tree was byte-identical after each red-state test.
