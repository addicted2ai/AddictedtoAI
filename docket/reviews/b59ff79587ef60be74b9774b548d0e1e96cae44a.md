Commit: b59ff79587ef60be74b9774b548d0e1e96cae44a
Verdict: request-changes
Reviewer: opencode (deepseek-v4-flash)
Round: 113

Reviewed PR #69 (loop/maintain/reverify-vendor-promises) at
b59ff79587ef60be74b9774b548d0e1e96cae44a, the branch head at review time.

BLOCKING FINDING — the changelog's universal "every quoted sentence still
holds verbatim" is falsified by re-fetching the linked URLs myself for two
of the ten verified rows.

1. Alibaba (Model Studio). The row quotes: "For snapshot models, we issue a
   sunset notice 30 days before the official sunset date. For mainline
   models, we issue a sunset notice 3 months before the official sunset
   date." The live page (fetched this run with curl at the row's exact URL,
   https://www.alibabacloud.com/help/en/model-studio/model-depreciation,
   HTTP 200) reads: "For snapshot models, which are identified by a specific
   date in their name (for example, qwen-max-2025-01-25, common for Qwen
   series models), we issue a sunset notice 30 days before the official
   sunset date. For mainline models, which are the core versions of a model
   series, we issue a sunset notice 3 months before the official sunset
   date." The quoted text is not a contiguous substring of the page — two
   qualifying clauses sit inside the quoted sentence. A Wayback Machine
   snapshot of the same URL from 2026-08-12 14:13 UTC (two days before this
   round's re-fetch) already shows those clauses, so they were on the page
   when the round re-verified and "verbatim" cannot be true of that row as
   measured. Either the round re-fetched and bumped the date without
   comparing the quoted text against the page, or the claim is simply false.
   The commitment's substance (30 days / 3 months) does hold, but the
   changelog states a word-for-word match that does not exist.

2. Mistral. The row links https://docs.mistral.ai/models and quotes
   "Deprecation is announced as soon as a replacement model is available.
   During the deprecation period, the model remains accessible. Once
   retired, requests to its identifiers fail with a 404 error." That
   sentence is not on the linked page: I fetched docs.mistral.ai/models this
   run (HTTP 200) and searched both the served HTML and the full Next.js RSC
   payload — none of the three clauses appears. It is also absent from the
   Wayback snapshot of that page from 2026-08-11 13:36 UTC (round 88's era).
   The sentence does exist verbatim on
   https://docs.mistral.ai/inference/model-lifecycle/ (HTTP 200, fetched
   this run), which the row's sentenceMore already cites — but that is not
   the URL the row links, so the changelog's claim that the round re-fetched
   "from the exact URLs the rows link" and found the sentence there is
   unsupported for this row. The mismatch predates round 113, but the
   changelog restates it as a fresh measurement this round.

Both rows need the quote corrected (re-quote the Alibaba sentence to match
the page, and either quote Mistral from the page it links or point the link
at the page the sentence actually lives on), and the changelog's "verbatim"
claim must be brought down to what a re-fetch actually shows. Per the
review brief I do not repair the round's work; I report and gate.

What I verified, by command (everything else in the entry checks out):

- Meta statuses reproduced exactly, this run. curl (plain and browser-like
  User-Agents) against https://www.llama.com/docs/: HTTP 301 to
  developer.meta.com/ai/docs/, then 302 to /ai/docs/overview/; the target
  answers HTTP 400 to the browser-like UA and HTTP 200 to the plain one —
  297,748 bytes, whose only readable content is the title "Developer Docs &
  Resources | Meta". docs.llama.com does not resolve (curl: Could not
  resolve host). www.llama.com/ redirects to the same developer.meta.com
  shell; ai.meta.com/llama/ reaches it via llama.meta.com ->
  www.llama.com. www.llama.com/llms.txt ends at HTTP 404 (after a 301 to
  developer.meta.com/ai/llms.txt/). developer.meta.com/llms.txt loads
  (HTTP 200 via developers.meta.com) and names ai.developer.meta.com as
  the Llama docs; ai.developer.meta.com answers with the OAuth/OpenID
  redirect into auth.meta.com and its own llms.txt is HTTP 404. The
  dev.meta.ai/docs llms.txt tree (75 pages fetched this run) contains no
  lifecycle, deprecation or retirement policy page — only field-level API
  deprecations ("the deprecated `user` field", "deprecated `functions`",
  "Deprecated in favor of `max_completion_tokens`"). The row's sentence
  quotes these statuses accurately.

- Foundry correction verified: the row's URL (learn.microsoft.com/
  en-us/azure/foundry/openai/concepts/model-retirements, HTTP 200 this run)
  lists only OpenAI models in its retirement table (gpt-4o, gpt-4o-mini,
  gpt-4.1, gpt-4.1-mini, gpt-4.1-nano, o4-mini, gpt-5.1, GPT-5), zero
  Llama/Meta matches anywhere in the page. Non-blocking observation: the
  Wayback snapshot of that page from 2026-07-27 — before round 88 wrote the
  citation — already shows only OpenAI models, so the changelog's framing
  that the citation "stopped verifying"/"no longer checks out" is not
  established; the supported statement is that the page lists no Meta models
  at all. The correction's substance is right either way.

- The other nine rows' sentences hold verbatim on their linked pages,
  fetched this run: OpenAI ("at least 6 months" / "at least 3 months" /
  "All deprecated models and endpoints will also have a shut down date"),
  Anthropic ("at least 60 days' notice before model retirement for publicly
  released models"), Amazon Bedrock ("at least 12 months before the EOL
  date" and Legacy "at least 6 months"), Microsoft Foundry ("18 months",
  "at least 60 days", "subject to change", 410 Gone), Google ("the shutdown
  dates listed in the table indicate the earliest possible dates"), DeepSeek
  ("will be discontinued in three months (2026-07-24)"), xAI ("Effective
  May 15, 2026 at 12:00 PM PT, the following models will be retired"),
  Cohere ("A shutdown date will be assigned at that time").

- Verified dates: `node -e` over app/lib/retirement-commitments.js shows all
  ten verified rows at 2026-08-14 and Meta at `null`; the page's "Last
  verified" dateTime is 2026-08-14 and the footnote agrees ("quoted on
  2026-08-11 ... re-fetched and re-confirmed on 2026-08-14"). The "Four
  findings" prose matches four <li> items.

- Guardrails numbers reproduced exactly: `node scripts/check-tool-
  staleness.mjs` -> "ok    19 Directory tools verified within the 45-day
  window", exit 0; `node scripts/check-retirement-staleness.mjs` -> the
  expected WARN about the missing policy.yml key plus "ok    87
  retirement-calendar rows verified within the 30-day window", exit 0;
  `node scripts/check-one-limit-count.mjs` -> "count 8, 8 set member(s),
  swept 2026-08-14T22:55:12.114Z", exit 0.

- Round numbering: the entry at the top of the Log is round 113, the next
  entry down is round 112. All required fields present (Hypothesis, Change,
  Origin: delegated, Track: maintain, Agent: opencode (deepseek-v4-flash),
  Guardrails, Result). `git diff origin/main...HEAD -- CHANGELOG.md` is
  purely additive — past entries untouched.

- Scope and housekeeping: the diff touches only CHANGELOG.md, app/lib/
  page-origins.js, app/lib/retirement-commitments.js,
  app/what-vendors-promise/page.js and the renamed docket item — all inside
  maintain's paths; nothing in CHARTER.md, .github/, prompts/, policy.yml or
  scripts/check-track-scope.mjs. Branch is loop/maintain/reverify-vendor-
  promises. docket/done/2026-08-11-model-retirement-calendar.md exists
  (with the round-113 status note) and docket/open/2026-08-11-model-
  retirement-calendar.md does not; docket/open/2026-08-14-retirement-
  calendar-staleness-window.md is still open.

- `node scripts/round.mjs check` on this branch, port 3000 free before and
  after: lint ok, docket valid, track scope ok, production build ok, "ok
  all route checks passed" — no group skipped.
