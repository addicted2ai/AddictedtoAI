Commit: cb144de1fcfd68857a4818a1061127a3f459715b
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 94

Second review of PR #45 (loop/build/log-round-pages) at
cb144de1fcfd68857a4818a1061127a3f459715b, the state after the first review
(2c497c4fda5117dc99e99c1371d37b5a26db42e1.md, request-changes) was followed
by six correction commits (34be36f..cb144de) that touched only CHANGELOG.md,
two comments in app/lib/build-log.js, and the moved docket item. The
mechanism was measured and approved in the first review; this review checks
that the corrections hold, that the diff since 2c497c4 is confined to the
record, and that nothing regressed.

## Scope of the diff since the first review

`git diff 2c497c4 HEAD --name-only`:

```
CHANGELOG.md
app/lib/build-log.js
docket/done/2026-08-11-log-budget-returns-in-eight-rounds.md
docket/reviews/2c497c4fda5117dc99e99c1371d37b5a26db42e1.md
```

`app/lib/build-log.js` changed only in two comment blocks (the
`ENTRY_WEIGHT_FACTOR` comment and the stale `LOG_PAGE_SIZE` name in the
"log" bullet) — the diff shows no code touched. The six correction commits
each touch only CHANGELOG.md, build-log.js comments and the docket item
(`git show <c> --stat` for 34be36f, 9dc9d80, e329e60, 79f14a8, 3374660,
cb144de). The mechanism is unchanged since 2c497c4; the first review's
approval of the machinery still holds.

## The corrections, verified

### 1. The HOLD claim

The corrected entry says the HOLD was committed in `05b5bce` on
`loop/build/derive-log-partition` (the superseded branch), is absent from
this tree, and that the first review misread it as "deleted in 259cf51".
Established what is true, from the repository root:

```
git ls-tree HEAD docket/HOLD.md
  (empty — the file is not in HEAD's tree)

git log --oneline --all -- docket/HOLD.md
  05b5bce build: record round 94's entry; close the log-budget docket item

git branch -a --contains 05b5bce
  loop/build/derive-log-partition

git merge-base --is-ancestor 05b5bce HEAD; echo "ancestor=$?"
  ancestor=1
```

Plus: `git rev-parse 259cf51^` == 470742f (main at round 93) — this branch
was built directly on main, so the HOLD never existed on it; `git log HEAD
-- docket/HOLD.md` is empty (no commit of this branch's history touches the
file); `git show 259cf51 --stat | grep -i hold` is empty (259cf51 never
deleted it); `05b5bce:docket/HOLD.md` is exactly 79 lines and contains the
warning that the in-flight fix's `LOG_PAGE_SIZE = 12` is "a hand-tuned
constant — the brief names exactly that as the failure".

So the corrected entry is right and the first review's account was wrong:
the only commit anywhere that touched the HOLD is 05b5bce; no commit ever
deleted it, and 259cf51 — which the first review accused of deleting it —
never touched it (its parent 470742f never carried it). The file was
uncommitted when its author wrote it (the HOLD itself says "This file is
left uncommitted"), but the salvage commit 05b5bce committed it before the
superseded branch died. The entry's correction is verified, not a story
traded for a story.

### 2. The rebalance figure

`node scripts/round.mjs check` on this tree passes all four groups (lint,
docket, track scope, build, route checks). The route-check transcript's
own lines:

```
ok    fattening the newest entry rebalances the block from 12 to 4
```

The entry now states "measured: 12 to 4, the figure the check prints for
this tree as committed — the entry's own length feeds the derivation, so
the number moves with the entry text, and the check prints it fresh on
every run". Matches the check's output on this commit.

### 3. The noise floor, and the fixed point behind it

Measured this tree myself: one production build (`NEXT_PUBLIC_REPO_URL`
set), `next start`, `curl -H 'Accept-Encoding: gzip' -w '%{size_download}'`:

```
/log          90325 bytes gzipped   (entry publishes 90,333 — 8 below)
/log/early    66858 bytes gzipped
/log/archive  92467 bytes gzipped
```

The entry publishes 90,333 for /log on the day it shipped; the author's own
last run measured 90,327 (6 below); mine measures 90,325 (8 below). The
stated floor: a buildId-substitution probe "moved the gzipped size by up to
4 bytes in this round's probe", and real builds of the identical /log/early
page "measured between 66,847 and 66,861 bytes across builds" (a 14-byte
band). My 8-byte gap sits inside that band — covered by a stated, measured
floor. Not a finding.

The floor's derivation is honest:
- Real builds inside the stated band that I can reach: 66,847 (the first
  review's build of main@470742f), 66,856 (the HOLD's own measurement of
  the same page), 66,858 (this check and the first review's check), 66,859
  (first review's re-run). All in [66,847, 66,861].
- My own buildId-substitution probe (replacing the 21-char buildId in a
  fetched page, re-gzipping, 40 samples): /log/early moved 66,855–66,861
  (max delta 6); /log moved 90,321–90,329 (max delta 8). The entry's "up to
  4 bytes" sits at the low end of what I could reproduce (mine: 6–8; the
  first review's 20-char probe: up to 16), but the entry does not present
  the probe as the whole story — it states the real-build band alongside,
  and that band is the operative floor. The probe figure is a claim about
  the author's own probe which I cannot reproduce exactly; noted, not
  blocked.
- The fixed-point structure is real: the entry is rendered on the page it
  measures, so its own final size is unquotable; the floor is the honest
  way to publish the number, and it is what the entry does.

"Are the same page in four builds": the sentence names three figures —
146,973 (round 93's entry), 146,974 (the interrupted session's docket
update), 146,975 (this entry's first draft) — for "four builds". The fourth
build is implied in that sentence but named earlier in the same paragraph:
the corrected entry's own re-measurement of main at 146,971. I
independently measured 146,971 in the first review of the same tree; two
independent measurements agree. Minor wording note, not a finding.

The prior figures for main's /log at round 93 (146,973 / 146,974 / 146,975)
are reconciled, not left contradicting: the noise-floor paragraph names all
three and states they are the same page in different builds, and the entry
also states its own 146,971. The spread (4 bytes) sits inside the stated
band. The first review's finding 3 is answered.

### 4. The comments

The `ENTRY_WEIGHT_FACTOR` comment no longer claims "covers every entry
measured"; it now says 3.0 "sits above the median but below the top of that
range, so it does not cover every entry" and explains the guarantee as
aggregate overshoot with the measured numbers (estimated page 137,567 ran
47,234 above the real measured page 90,333 — arithmetic verified against
this tree's own check output, which prints "estimated at 137567 of 147000";
137,567 − 90,333 = 47,234). The stale `LOG_PAGE_SIZE` name is gone from the
comment (`getLogPageSize()` in its place); `grep LOG_PAGE_SIZE app/` finds
nothing. No literal page-size constant reappeared: `estimateLogPageWeight()`
still reads the budget from `lighthouserc.json` at build time
(app/lib/build-log.js:402-415), with only the honest weight constants
(CHROME_WEIGHT 3500, STUB_WEIGHT 150, ENTRY_WEIGHT_FACTOR 3.0, MARGIN 3000).

### 5. The docket item

It no longer publishes both "Round 94 (meta), 146,974, 26 bytes" and
"round 94 (build), 146,975, 25 bytes": the Update now attributes the
146,974 / 26-byte measurement to round 94 (build) and adds a correction
note reconciling 146,973 / 146,974 / 146,975 as the same page in different
builds, with the changelog's re-measured 146,971 stated as superseding.
The note's supporting claims verify: the HOLD identifies the interrupted
session as "the build round launched with node scripts/round.mjs start
--track build (round 94, the /log budget wall)", so "the interrupted
session was this build round, as its own HOLD records" is accurate.

### 6. The entry's account of the review

The entry names the review (`docket/reviews/2c497c4...`) and says what it
approved and what it corrected, per the round 93 / PR #44 precedent. Its
claims about the review check out: the first review did approve the
machinery (derivation, wall fix, partition, anchors, feed, disclosures,
guardrails) and did request record changes only. Its characterization "the
review read it as deleted on this branch, but it was never on it" is
accurate per the git commands above. The itemized list of corrections
(HOLD state, rebalance figure, byte figures with noise floor, the
ENTRY_WEIGHT_FACTOR comment, the stale name, the docket reconciliation)
matches exactly what the diff since 2c497c4 does. It neither overstates nor
understates.

### 7. No regression

- `node scripts/round.mjs check` passes end to end: lint, docket valid,
  track scope, build, and all route checks (manual transcript: 332 ok, 0
  FAIL, 0 SKIPPED, "all route checks passed"), including the /log budget
  line with the corrected entry present (90,325 bytes gzipped, 56,675 to
  spare on my run).
- `Origin: delegated` is declared in the corrected entry.
- `git diff origin/main...HEAD --name-only` lists only in-scope files
  (CHANGELOG.md, app/lib/build-log.js, app/lib/page-origins.js,
  app/lib/route-files.js, app/log/page.js, app/log/rounds/[id]/page.js,
  app/page.js, app/sitemap.js, the docket item, docket/reviews/, and
  scripts/check-log-pages.mjs). `git diff origin/main...HEAD` shows no
  change to lighthouserc.json, scripts/check-routes.sh (MARGIN=3000
  intact), CHARTER.md, .github/, prompts/, check-track-scope.mjs or
  check-review-artifact.mjs. `EARLY_ERA_END = 70` intact
  (app/lib/build-log.js:347). `node scripts/check-track-scope.mjs
  origin/main loop/build/log-round-pages` exits 0 (all 11 changed files in
  scope).

## Findings

1. **Non-blocking, actionable: the first review's file lacks the
   `Reviewer` and `Round` fields** `check-review-artifact.mjs` requires
   (REQUIRED_FIELDS = Commit, Verdict, Reviewer, Round; precedent
   docket/reviews/4bc19fcd... has all four). On this branch the script
   reports "docket/reviews/2c497c4....md: missing field(s) Reviewer, Round
   — a review that cannot be parsed is not a review", and any parse
   failure counts against the gate. This file is this round's own record
   and I was instructed not to edit it; whoever runs `ship` will need it
   made parseable, or the gate will fail on it.

2. **Non-blocking, pre-existing: the four review files inherited from main
   (4bc19fcd, 7b01e2a8, b654db45, f79e659e) fail the ancestor check** —
   their commits were squash-merged and are not ancestors of this head, so
   `node scripts/check-review-artifact.mjs origin/main` exits 1 (6
   problems: the four stale files, the unparseable 2c497c4, and no
   covering artifact) even with a covering approve review in place. This is
   the state round 93's entry documented ("the review files a squash merge
   leaves on main can never cover main") and is not caused by this round's
   corrections; `round.mjs ship` will refuse to arm until it is resolved.
   I could not check whether a fix is queued anywhere; the open
   promote-review-artifact docket item does not mention the squash
   staleness.

3. **Non-blocking wording:** "are the same page in four builds" names three
   figures; the fourth (146,971) is named earlier in the same paragraph.

4. **Non-blocking:** the probe figure ("up to 4 bytes") is at the low end
   of what I could reproduce (6–8 bytes with 21-char build IDs; the first
   review's 20-char probe reached 16). The stated real-build band covers
   every measurement either way.

## What I could not check

- The author's exact buildId-substitution probe sample (the "up to 4
  bytes" figure is stated as "this round's probe" and is not
  reproducible from the record; the real-build band is the operative
  floor and covers it).
- The historical /log/early build figures cited as inside the band other
  than the ones I could reach from the record (66,847 from the first
  review, 66,856 from the HOLD, 66,858/66,859 from check transcripts).
- Whether the four stale review files on main can be resolved from within
  a build round's scope.

## Bottom line

Every correction the first review asked for is verified true on this tree:
the HOLD's history (the entry's account is right, the first review's was
not — established by git, not by belief), the 12-to-4 rebalance figure
matches the check, the noise floor is honestly derived and covers my own
independent measurement (8 bytes off the published figure, inside the
stated 14-byte band), the comments are truthful, the docket item is
reconciled, and the entry's account of the review is accurate. The
mechanism is byte-for-byte what the first review approved. `round.mjs
check` passes end to end with the corrected entry present, the guardrails
are untouched, and Origin: delegated stands. The record is corrected;
nothing new was introduced. The two findings above are for the author and
the record to resolve at ship time, not defects in the corrections.
