Commit: e3e6a2cc0df70ffea77d38ed8e7587b8fca1c528
Verdict: approve
Reviewer: claude-opus-5 (Claude Code subagent)
Round: 177

# Delta review — round 177, the claim behind the string

Fourth and final pass. Scoped to the delta from `3ea6c97`: three files,
10 insertions / 9 deletions, terminology only — `FRAME.md`,
`scripts/check-governance-claims.mjs`, `CHANGELOG.md`. (The diffstat also shows
`docket/reviews/3ea6c97….md`, which is my own request-changes artifact going
upstream; it is in the record and stays byte-identical.)

I verified everything else at `3ea6c97` and said that with B1 fixed this was an
approve with no other reservation. That work is not redone here.

**B1 is fixed, and the claim is gone rather than reworded.** I swept for the
idea rather than the string, because that was the whole finding and the round
had twice removed a string and left the claim.

---

## 1. The two carriers — genuinely rewritten

**`FRAME.md:443–446`**, in the Round 177 finding paragraph:

> The heading is now **compared against both derivations — checked, not
> promoted to a derivation of its own** — so the command this file and
> `CLAUDE.md` both tell a reader to run is no longer the one with the hole in
> it.

**`scripts/check-governance-claims.mjs:466`**, the canary's `why`:

> Round 177 made that command compare the typed heading against both of its
> derivations -- **the heading is the value under test, not a derivation of its
> own**; this is the second lock, from the other side.

Read against **`FRAME.md:429–435`**, the Claim paragraph in the same fact:

> … compared against a third value that is ***not* a derivation**: the number
> typed in this fact's own heading. All three must agree, and the two
> derivations must be non-zero … Calling the heading a third "independent way"
> would flatter it: it is the typed value under test, which is the whole reason
> it is checked rather than trusted.

All three now say the same thing, in the same direction: two derivations, one
typed value under test. The contradiction is gone. Both replacements carry the
negation explicitly ("not promoted to a derivation of its own", "not a
derivation of its own") rather than merely dropping the offending noun, which
is what makes this a fix to the claim and not another pass at the string.

**"Compared against both derivations" is accurate**, and I checked it rather
than assumed it. The block tests `heading_count = file_count` directly and
`file_count = parsed_count` alongside it, so the heading must equal both by
transitivity — which is exactly what the Claim paragraph states as "All three
must agree." No overstatement.

## 2. The sweep — the idea, not the phrase

This is the part I attacked hard, since the claim had already survived two
string-level removals. I did not repeat the round's grep list; I built a
broader one and then read every relevant sentence by hand.

**Mechanical half.** Eight pattern families over **151 files** (whole
repository, excluding `node_modules`, `.git`, `.next`, and `docket/`, with
whitespace flattened first so line-wrapped phrases could not hide):

```
three (independent )?(ways|derivations|methods|sources|checks|derivation)
third (derivation|way|independent|method|source|check|value)
3 (ways|derivations|independent)
derived? (it )?three
triple|thrice
heading[^.]{0,80}\bderiv        \bderiv[^.]{0,80}heading
heading[^.]{0,60}\b(third|three)\b
```

**47 hits.** Every one classified:

| Location | Hits | Verdict |
|---|---|---|
| `AGENTS.md` | 4 | all one sentence — a **correct negation** ("not a third derivation") |
| `prompts/shared/every-run.md` | 4 | all one sentence — the same correct negation |
| `FRAME.md` | 6 | correct negations, "the two derivations", and accurate descriptions of the old gap |
| `scripts/check-governance-claims.mjs` | 4 | the fixed `why` plus accurate descriptions of the old gap |
| `CHANGELOG.md` | 23 | 2 inside round 177's entry (below); 21 in frozen past entries, unrelated ("three sources", "triple-backtick", other rounds' "three ways") |
| `app/blog/fable-5-export-controls/page.js` | 1 | "Three sources carry it" — export-controls post, unrelated |
| `archive/prs.json` | 1 | heading outlines "all three nest correctly", unrelated |
| `scripts/check-briefs.mjs` | 1 | "three ways past `check-briefs.mjs`", unrelated |
| `scripts/check-frame.mjs` | 1 | "a heading indented by one to three spaces", unrelated |
| `scripts/check-routes.sh` | 1 | "the record now spans three pages", unrelated |
| `scripts/test-orchestrate-hold.mjs` | 1 | "defeated it three ways", unrelated |

**Nothing anywhere asserts that the heading derives anything.**

**Human half.** A mechanical sweep can only find phrasings I thought of, so I
also dumped and read **every sentence mentioning "heading"** in the two files
that describe fact 14's mechanism — 10 in `FRAME.md`, 8 in
`check-governance-claims.mjs`. All 18 are either the corrected statements, the
check block itself, accurate descriptions of the old gap ("compared its two
derivations to each other, never to the heading above them"), or about an
unrelated referent (CommonMark heading shapes, `CHARTER.md`'s rule headings,
`CHANGELOG.md`'s `## Log` heading). None promotes the heading to a derivation.

**Round 177's own changelog entry, isolated** from the 21 frozen hits elsewhere
in the file (lines 93–162): exactly **2 hits**, both correct — `"three
independent ways"` appears only as the quoted error inside the new review
credit, and the other describes the old gap. Neither asserts anything about the
heading deriving.

**Adjacent carriers checked too**, since the failure mode is a description
living somewhere nobody swept: `CLAUDE.md:11–12` says the count is "checked two
independent ways rather than retyped here" — true of the two derivations,
claims no third, and needs no change. `README.md` does not mention fact 14.

## 3. Nothing else moved

Confirmed rather than accepted: zero changed lines in the delta touch
`file_count`, `parsed_count`, `heading_count`, `grep -m1`, `sed -n`,
`statesLiveRuleCount`, any `needle:`, any `source:`, `CLAIMS_DECLARED`, or
either anchor sentence. `AGENTS.md` and `prompts/shared/every-run.md` are not
in the delta at all. The fenced check block is byte-identical.

## 4. Re-verification

| Check | Result |
|---|---|
| `check-frame.mjs` | **18 candidate headings, 18 well-formed, 0 malformed**; completeness 18 vs 18; **sequence [1..18] vs {1..18}**; fact 14 **`verified`**; **all 16 checkable facts passed** |
| "999 rules" mutation, detached worktree at `e3e6a2c` | **exit 1** — `file-regex-count=22 parser-count=22 heading-count=999` |
| "21 rules" / number removed / heading indented | exit 1, exit 1, exit 1 |
| registry | **24 registered, 18 allowances, 11 tripwires, 61 files, 26 hits, 0 unregistered**, all green |
| the canary specifically | `ok FRAME.md fact 14 (canary) -- fact 14's heading states the live CHARTER.md rule count (22)` — the reworded `why` did not disturb it |
| `round.mjs check` | **exit 0**, zero SKIPPED — watched to completion, not asserted early |
| `.github/` across `82df8a4..e3e6a2c` | untouched |
| `CHARTER.md` across `82df8a4..e3e6a2c` | unamended |
| working tree | clean; scratch worktree removed |

**`CHANGELOG.md`** is **exactly 70 lines** (lines 93–162, header inclusive),
measured directly rather than inferred — unchanged in length, two hunks at
lines 100 and 129, both inside the round's own unmerged entry. The first is a
pure reflow (three lines to two, identical words); the second compresses the
fixture caveat and adds the review credit (three lines to four). Net zero. No
past entry touched, no new paragraph or section. Rule 5 holds. The fixture
caveat survives the compression with both halves intact: "it reads `FRAME.md`
by name, so a fixture re-verification false-greens while firing correctly the
documented way."

## 5. The review credit is accurate

The entry now carries three credit sentences. The third is new:

> Review caught this fact's prose twice: "three independent ways" overstated
> it, then the fix left the claim behind.

Both are accurate and both were mine — the first raised at `e151300` as N2, the
second at `3ea6c97` as the blocking B1. This also resolves the non-blocking note
I raised last pass, that the entry disclosed two of three review-driven
corrections; it now discloses all three.

---

# Findings

None blocking. None new.

**Carried forward, unchanged and still correctly out of scope:** `README.md:66–68`
carries three false claims ("21 rules", "the loop cannot amend", "Human-owned,
enforced by `CODEOWNERS`"). It is in no track's `SCOPES` entry, so no round can
write it; it needs the maintainer or a `SCOPES` widening merged by hand.

**Also carried forward, for a later round and already recorded in the entry:**
`round.mjs:542` and `automerge-origin.mjs:43` both call `review-artifact` a
non-required check. The live API returns `build-and-audit`,
`human-owned-paths`, `review-artifact`. Both files are under `scripts/`, in
`meta`'s scope.

The cosmetic rewrapping I noted at `3ea6c97` (`AGENTS.md:37–38`,
`every-run.md:12–13`) is unchanged and remains a non-defect — it renders
identically and breaks no needle.

---

# Verdict

**approve.**

The claim is gone, not reworded. Both carriers now state the opposite of what
they said, explicitly, and agree with the Claim paragraph nine lines above them.
I swept 151 files with eight pattern families and then read all 18 sentences
mentioning the heading in the two files that describe the mechanism, plus
`CLAUDE.md` and `README.md` as adjacent carriers: nothing anywhere asserts the
heading derives anything, and the only surviving occurrences of the old phrasing
are three correct negations and one quoted error inside a rule-5 correction.

Everything the fix could have disturbed is intact: the check block is
byte-identical, the anchors and needles are untouched, all 24 claims resolve
with the canary green, `check-frame.mjs` still extracts 18 facts in sequence
with 16 passing and fact 14 `verified`, the "999 rules" guard still exits 1,
the changelog is still 70 additive lines under rule 5, nothing under `.github/`
changed and `CHARTER.md` is unamended, and `round.mjs check` is exit 0 with zero
SKIPPED.

I have no remaining reservation on this round.
