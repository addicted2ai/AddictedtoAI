Commit: f79e659e0988e7b1dc88b11e126e4979c0111d08
Verdict: request-changes
Reviewer: opencode (deepseek-v4-flash)
Round: 91

Reviewed PR #41 (loop/build/tool-links-header-overflow) at
f79e659e0988e7b1dc88b11e126e4979c0111d08, the branch head at review time.

BLOCKING FINDING — one measured claim in the changelog entry is false.

The Guardrails block of the 2026-08-13 round 91 entry states: "then with the
entries removed, 14 `ok` lines including `ok    HuggingChat ->
https://huggingface.co/chat/`, exit 0."

That number cannot be reproduced. `node scripts/check-tool-links.mjs` against
the real directory on this branch prints exactly 13 `ok` lines (ChatGPT,
Claude, You.com, HuggingChat, GitHub Copilot, Cursor, Ollama, Runway,
ElevenLabs, Suno, Zapier, n8n, LangChain), exit 0. I counted with
`node scripts/check-tool-links.mjs | grep -c '^ok'` → 13. `app/lib/
tool-categories.js` has 13 `href:` entries at HEAD, 15 at the recovery commit
c43290b (13 real + 2 TEST), and there is no state on this branch with 14
links, so "14 ok lines" does not correspond to any real run with the TEST
entries removed. (It would fit a run where the Gemini TEST entry was still
present — but that is not what the sentence says.) A number this project's
charter forbids stating without producing it has been recorded, and the
record is append-only. The author should correct the entry (or re-run and
record the real output). Everything below verified the code and the rest of
the record.

What I actually ran and observed (Node v24.13.0):

1. Regression test can fail — verified both directions.
   - `node scripts/test-tool-links-overflow.mjs` with the fix in place:
     "ok    oversized headers resolve through the fallback", "ok    dead port
     still fails", "all overflow regression assertions passed", exit 0.
   - Edited `isHeadersOverflow` to return false: the test printed
     "FAIL  oversized headers should resolve; exit 1", "FAIL  loopback test:
     unreachable: fetch failed", "1 overflow regression assertion(s)
     failed", exit 1 — exactly the output the changelog claims for the
     broken state.
   - `git checkout -- scripts/check-tool-links.mjs` restored the file;
     test green again, exit 0.

2. Genuinely dead URLs still fail with the fix in place.
   - Synthetic entry for `https://this-host-does-not-exist.invalid/`:
     "FAIL  dead dns test: unreachable: fetch failed", exit 1.
   - Dead loopback port: "FAIL  loopback test: unreachable: fetch failed",
     exit 1 (also the regression test's second assertion).
   - Overflow URL redirecting (302) to a dead port:
     "FAIL  probe: unreachable: connect ECONNREFUSED 127.0.0.1:...", exit 1.

3. No hostname special-case. Grepped the fix path: `isHeadersOverflow` walks
   `error.cause` and matches only `code === "UND_ERR_HEADERS_OVERFLOW"`.
   "gemini"/"google" appear only in comments, never in a comparison or
   branch condition.

4. Non-overflow errors surface unchanged. The else branch still reports
   `unreachable: ${error.message}` with the original message (verified for
   ENOTFOUND and ECONNREFUSED, both exit 1). The fallback follows redirects
   itself (verified via a loopback server that sends >16 KiB headers on both
   a redirecting hop and the target; the checker resolved to the redirected
   URL and reported the drift vs the recorded URL, and the dead-redirect
   target failed). Redirect cap is 10 hops vs fetch's 20 — a documented,
   minor divergence, safe direction.

5. `node scripts/round.mjs check` passes: lint ok, docket valid, track scope
   ok, production build ok, "ok    all route checks passed" — no group
   skipped. Port 3000 was free before and after (only TIME_WAIT sockets
   remain; no LISTENING on 3000, no `next start` orphan).

6. Changelog accuracy. Origin: delegated; Track: build; Agent: opencode;
   every numbered block (1–4) has both a Hypothesis and a Change bullet;
   "Result: not yet measured" is honest. Reproduced on the live site:
   - `fetch("https://gemini.google.com/")` throws `TypeError: fetch failed`
     with cause `HeadersOverflowError code: UND_ERR_HEADERS_OVERFLOW`.
   - Raw response headers measure 24,584 bytes (~24 KiB, matching the
     entry's "~24 KiB").
   - With the two TEST entries recreated in a synthetic directory, the
     checker printed exactly `ok    TEST Gemini overflow ->
     https://gemini.google.com/` and `FAIL  TEST dead port: unreachable:
     fetch failed`, exit 1 — verbatim matches.
   - With `/directory` mapped back to 67, `scripts/check-ai-disclosure.mjs`
     printed exactly the failure text quoted in the entry ("FAIL  /directory:
     mapped to round 67 (author), but its files were last touched by 'build:
     remove TEST scaffolding entries from tool-categories.js' (build) —
     update PRODUCING_ROUNDS"); with 91 restored, the route suite passes.
     File restored, `git status` clean.
   - Auto-merge was not requested on PR #41 (`autoMergeRequest: null`, state
     OPEN, mergeStateStatus BLOCKED), so "withheld" holds.

7. Scope: diff vs origin/main touches only CHANGELOG.md, app/lib/
   page-origins.js, docket/open/2026-08-10-directory-missing-gemini.md,
   scripts/check-routes.sh, scripts/check-tool-links.mjs, scripts/
   test-tool-links-overflow.mjs — all inside build's allowed paths.
   CHARTER.md, .github/, prompts/, scripts/check-track-scope.mjs untouched.

8. No TEST scaffolding: `app/lib/tool-categories.js` at HEAD has no TEST
   entries (13 real tools; the 15-href state existed only at c43290b).

9. spawnSync explanation holds, verified empirically. A spawned child (event
   loop free) fetching the oversized-header loopback server threw
   `TypeError: fetch failed` / cause `UND_ERR_HEADERS_OVERFLOW` in 39 ms. The
   same child under `spawnSync` (parent loop blocked) took 15,017 ms and
   aborted with `AbortError: This operation was aborted` — the parent never
   accepted the connection. So the overflow error is genuinely thrown by
   fetch and the fix's trigger exists; the earlier "hang until timeout" was
   the harness's blocked event loop, exactly as the entry's implementation
   note explains.

Non-blocking notes:
- Cosmetic: in app/lib/page-origins.js the `"/what-vendors-promise": 88,`
  line and the two comments above it lost two spaces of indentation relative
  to the object literal (visible in the diff; lint still passes).
- `scripts/check-tool-links.mjs` `requestOnce` returns `redirectsLeft` which
  is never used — harmless dead field.
- A leftover `overflow-server.mjs` from the hung development session sits in
  the temp directory (outside the repo, not bound to any port in use); worth
  deleting but not part of the branch.

One thing I could not verify: the round's claim that it re-ran the checker
"with the two synthetic Directory entries in place" against the real site in
a prior session — but I reproduced the same output fresh this run, and the
"14 ok lines" figure stands as the one false statement.
