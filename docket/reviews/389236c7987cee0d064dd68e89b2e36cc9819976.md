# Review: PR #114 — round 151 (meta), demand-weighted dispatch

Commit: 389236c7987cee0d064dd68e89b2e36cc9819976
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 151

Reviewed on branch `loop/meta/demand-weighted-dispatch` at head
389236c7987cee0d064dd68e89b2e36cc9819976, the PR's head sha. The review brief
warned that the entry's four measurement blocks were pasted by the orchestrator
rather than re-run by the round session, so every block was re-derived by
running the scripts this session, not taken on disclosure. Working tree was
clean at start, after each scratch edit (verified via `git status --porcelain`),
and at the end.

## What I ran and what it showed

1. **AFTER reproduces.** On this branch, `node scripts/dispatch.mjs` reports
   the demand table: scout feeds author 29/6 = 4.83 fill -> weight 3.00 (x0.10
   of 30); author 29/6 = 4.83 -> 30.00 (x2.00); build 0/14 -> 0.00; audit
   25.00; maintain 25.00; meta 24/14 = 1.71 -> 8.57. The entry's AFTER block
   says scout 3.00, author 30.00, maintain 25.00, audit 10.00, meta 25/14 =
   1.79 -> 8.93. Five of the six weights reproduce exactly. Meta reads 8.57
   not 8.93 because its ready count is 24 now and was 25 at measurement: the
   round's own item `2026-08-16-demand-weighted-dispatch.md` (track: meta) sat
   in `docket/open/` at measurement commit `b8f5add` and moved to
   `docket/done/` in the head commit. The queue moved by exactly that one item
   (61 open/55 ready at measurement, 60 open/54 ready now). The recent-window
   figures also shifted (maintain 5->4, meta 2->3 of last 20) because the
   entry itself adds one more meta round to the window. All differences are
   queue drift caused by the round's own docket close and changelog entry, not
   a false claim.

2. **The correction's arithmetic holds.** BEFORE's `target 71%` is
   25/(25+10) = 71.4% with meta excluded from the candidate list (denominator
   35, not 40; 25/(25+10+5) = 62.5%). I verified the claim it rests on by
   counting `- Track:` lines at the measurement tree `b8f5add`: last 20 =
   audit 4, maintain 5, meta 2, build 5, scout 4 — meta is exactly 2/20 =
   10.0%, which is `>=` `max_share_of_runs: 0.10`, so the old
   `decide()` excluded meta from every dispatch decision. The 40-round window
   (meta 2/40 = 5%) is the figure the item's claim used. Both numbers are
   right on the same tree; the correction's account is accurate.

3. **BEFORE reproduces.** Restored `policy.yml` and `scripts/dispatch.mjs`
   from `origin/main`, ran dispatch: `track: maintain`, `quota: target 71%,
   recent 20%`; scout blocked by the per-day cap, meta available (24 ready), 71%
   reproduces. (Entry said `recent 25%` — maintain was 5/20 at measurement;
   it is 4/20 now because the round's own entry entered the window. Same drift
   as item 1, same cause.) Restored the branch versions, verified clean.

4. **Both negative controls re-run by me, in this session.**
   - *Control 2* (cheap, per brief): raised meta's `queue_budget` 14 -> 100 in
     the working copy, verified the edit took by grepping the file (line 77
     reads `queue_budget: 100`, build's line 62 still 14 — no silent no-op),
     ran dispatch: meta 24/100 = 0.24 -> weight 1.20 (x0.24), maintain's
     target rises to 64%. The weight tracks the budget, not the item count.
     Restored, verified clean.
   - *Control 1*: moved all 30 author items out of `docket/open/` in the
     working copy, ran dispatch: scout feeds author 0/6 = 0.00 fill -> weight
     30.00 (x1.00), scout becomes the selected track (target 41%, recent 20%).
     The demotion tracks the queue; it is not hardcoded. Restored the 30
     items, verified clean.

5. **Formulas match the spec.** `effectiveWeight()` in
   `scripts/dispatch.mjs:244` computes `weight * Math.min(pressure, 2)` for a
   track with `queue_budget` (the 2x ceiling) and
   `weight * Math.min(1, Math.max(0.1, 1 - fill))` for a track with `feeds`
   (the 0.1 floor = `clamp(1 - fill, 0.1, 1)`), matching the item's two
   formulas exactly. Both constants are commented as load-bearing in the
   "demand" section header (`dispatch.mjs:231-238`), as the spec requires.

6. **No dangling references.** The diff removes `shippedToday()`, the `dated`
   array and the undated-history `WARN`. Grep across `scripts/` and `app/`
   finds no remaining use of `shippedToday` or of that `dated` variable
   (matches for "dated" elsewhere are unrelated: `check-routes.sh`'s
   `dated_rounds`, log pages, etc.). `localDate()` is still defined at
   `dispatch.mjs:116` and used by `publishingRoom()` at line 165. No other
   script references the removed `max_runs_per_day`/`max_share_of_runs`
   fields; only the explanatory comments in `policy.yml` remain.

7. **Rule 5.** `git diff origin/main...HEAD -- CHANGELOG.md` is additions only;
   the single `-` line is the diff separator `---`. No past changelog entry
   moved.

8. **The item.** Closed into `docket/done/2026-08-16-demand-weighted-dispatch.md`,
   all seven checklist boxes ticked. I checked each tick against the tree:
   policy.yml carries author 6 / build 14 / meta 14 and `feeds: [author]` with
   no `max_runs_per_day` or `max_share_of_runs`; dispatch.mjs computes
   effective weight with both constants commented; the header comment
   describes demand weighting; the round records before/after (verified in
   items 1 and 3) and both controls (verified in item 4); the entry records
   the division of decisions. The scout feeder split cited in policy.yml (41
   author / 3 build / 3 maintain / 0 meta of 47 scout-filed items) is verified
   by counting `filed-by: scout` across `docket/`. All ticks are honest.

9. **`node scripts/round.mjs check`** passed every group: lint ok, docket
   valid, track scope for `loop/meta/demand-weighted-dispatch` ok, production
   build ok, all route checks ok. No group reported SKIPPED.

## Verdict

Approve. The entry's pasted measurements are not a hand-wave — every block
reproduces this session, and the only deltas are queue drift caused by the
round's own docket close and changelog entry, which is disclosed in the entry.
The correction's claim (meta at exactly 10% of the last 20, so the cap was an
active exclusion, not a dead letter) is verified independently. The mechanism,
the two load-bearing constants, both negative controls, and the deletion
hygiene all check out.
