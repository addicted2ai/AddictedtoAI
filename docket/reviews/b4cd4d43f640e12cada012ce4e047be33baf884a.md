Commit: b4cd4d43f640e12cada012ce4e047be33baf884a
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 135

## What I ran

Fetched the branch, read `scripts/check-prebuild-single-branch.sh`, the
`check-routes.sh` diff, the workflow, the changelog entry and the docket
item at the reviewed commit via `git show`. Ran the script itself from the
branch tree (the working tree is checked out at b4cd4d4 and clean):

    bash scripts/check-prebuild-single-branch.sh 2>&1; echo "EXIT=$?"
    EXIT=0
      WARN  origin/main is not in this checkout — counting the changelog from HEAD instead
      WARN  origin/main is not in this checkout — the diff-aware comparison is skipped this run
      WARN  policy.yml has no staleness_days.retirement_calendar to enforce ...
    ok    prebuild passes in a single-branch checkout with no remote refs (head b4cd4d4)
    ok    deleting the origin/main fallback makes the same prebuild fail — the guard is real, not assumed

Both directions executed and passed from the branch's own tree.

## What I falsified-by-construction (can-it-go-red tests)

All in throwaway clones under the approved temp dir; the repo tree was
never modified (`git status` clean throughout, no leftovers).

1. **Red direction is real, not green-only.** Built a shaped checkout of
   the reviewed commit (`git init`, `git fetch <repo> HEAD`,
   `git checkout FETCH_HEAD` — no origin refs, verified by
   `rev-parse origin/main` failing), ran `npm ci`, then applied the script's
   own `sed 's/return "HEAD";/return "origin\/main";/'` to
   `count-changelog-rounds.mjs` and ran `npm --prefix <dest> run prebuild`:
   exit 1 with exactly `fatal: bad revision 'origin/main'` then
   `FAIL could not count the changelog as of 2026-08-15T18:46:41.179Z` — the
   historical failure the entry quotes. The sed mutates a copy in the shaped
   checkout, not the real file. Confirmed.

2. **Shape assertion can fire.** In another clone, reformatted the fallback
   to `return "HEAD" ;` (still a working guard, different shape) and
   committed it: the script exits 1 with
   `FAIL scripts/count-changelog-rounds.mjs no longer has the fallback shape
   this check must prove`. In a clone where the fallback was renamed to
   `HEAD_SUBSTITUTE` (functional shape break), the green direction itself
   failed. Both directions of the guard-shape test go red as claimed.

3. **Quota-guard asymmetry is honestly stated and not a hole.** The script
   only re-deletes the changelog guard's fallback; the quota guard's red
   direction is not re-run by the script. I removed the quota guard's
   fallback entirely (unguarded `git show origin/main:app/lib/posts.js`) in
   a clone, committed it, and ran the script: it exits 1 with
   `FAIL prebuild fails in a single-branch checkout with no remote refs`.
   The green direction catches a quota guard that silently loses its
   fallback on every PR, exactly the reasoning the entry states. I also
   reproduced the entry's own quota-red demo (`process.exit(0)` → 1 on the
   missing-base path): prebuild exits 1. Both stated directions hold.

4. **Green run is the real chain in the real shape.** The script's green
   output and my independent shaped `npm ci && npm run prebuild` both print
   the two guards' degradation WARNs and both agree on the anchored count:
   `ok rounds_merged matches the changelog: 125 round entries as of
   2026-08-15T18:46:41.179Z` — matching `countRoundsAsOf(...)` = 125
   directly and `app/lib/loop-history.json` (`rounds_merged: 125`,
   `taken_at: 2026-08-15T18:46:41.179Z`).

5. **API degradation.** `check-loop-history-snapshot.mjs` with the Actions
   API unreachable (broken proxy): prints
   `WARN could not reach the Actions API — live agreement not checked this
   run`, prebuild still exits 0. "Degrades to WARN" is true.

## CI-path verification

- `.github/workflows/pr-checks.yml` runs `bash scripts/check-routes.sh` in
  `build-and-audit` (`if: always()`), and `check-routes.sh:123` runs
  `bash scripts/check-prebuild-single-branch.sh || failures=$((failures + $?))`
  with `exit 1` on accumulated failures — so a red prebuild-shape run fails
  CI on every pull request. No skip path exists: every branch of the script
  either prints `ok` after doing the work or prints `FAIL` and exits 1; no
  `ok` is ever printed for a run that did not happen. CI has network and a
  full clone (the source repo), so `git fetch <path> HEAD` and `npm ci` in
  the shaped checkout both work there as they did locally.
- `round.mjs` `check` runs `check-routes.sh` (line 413), so the local claim
  holds too. In CI `HEAD` is the merge commit, matching "the tree the deploy
  would run".

## Audit claims (docket box 3), grepped at the reviewed commit

- `staleness-report.mjs`, `check-one-limit-count.mjs`: zero matches for
  git/execFile/spawn/curl — "shell out to no git" is true.
- `check-loop-history-snapshot.mjs`: imports `countRoundsAsOf` from
  `count-changelog-rounds.mjs`, uses curl/gh with WARN fallback — true.
- `count-changelog-rounds.mjs`: `git log origin/main|HEAD --before=` behind
  a `baseRef()` guard — true.
- `check-publishing-quota.mjs`: `git show origin/main:` behind the #83
  guard (WARN + exit 0 when the ref is missing) — true.
- Outside the deploy path: `check-ai-disclosure.mjs` shells out to git and
  is run only from `check-routes.sh` (line 139) — true; `loop-history.mjs`
  guards its own `git fetch origin main` — true; `next.config.js` is an
  empty config — true.

## Numbers

- Round number: `origin/main` CHANGELOG.md holds 134 dated entries; the
  branch holds 135, so this is round 135. Correct.
- "125 round entries as of 2026-08-15T18:46:41.179Z": reproduced
  (`countRoundsAsOf` = 125; snapshot file agrees). Correct.
- Both "exit 1" figures reproduced with the exact quoted output lines.
  Correct.

## Non-blocking observations

- The brief said the round claims "npm run build passes in the shaped
  checkout". It does not: the script runs `npm ci` and `npm run prebuild`
  only, and that is all the entry claims. No discrepancy.
- The green direction prints the degradation WARN lines but does not assert
  their presence (the `grep ... || true` is informational). The changelog
  guard's shape is asserted separately, and I demonstrated a quota guard
  that stops degrading fails the green run — so nothing load-bearing
  depends on the WARNs being present.
- `check-routes.sh` runs after the build in the workflow, so CI installs
  dependencies twice (once for the build, once inside the shaped checkout).
  Cost, not correctness; the script's own `npm ci` failure mode is loud.
- The historical outage narrative in the docket (deploy timestamps, `fatal:
  invalid object name 'origin/main'` in the Vercel log) is background
  context, not a claim of this round; I verified the class reproduces in the
  shaped checkout, which is the round's actual claim.

## What I could not check

- A real GitHub Actions run of the new step (no PR from this branch has
  exercised it in CI yet; my verification is the local execution of the
  identical command plus reading the workflow).
- `node scripts/round.mjs check` end-to-end (requires the build and a server
  on port 3000; the invocation chain it depends on was verified by reading
  and by running the script directly).
- The exact 756a58a-shaped-checkout measurement cited in the Guardrails
  section: 756a58a is this branch's parent and predates the script. I
  re-measured on the reviewed head b4cd4d4 instead, with identical results
  (exit 0, both WARNs, 125 anchored count, red direction exit 1), so the
  published numbers hold at the commit being merged.
