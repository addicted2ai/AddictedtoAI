Commit: cc5000aa5e176e06e57e4b2ea83d773d036e1159
Verdict: approve
Reviewer: claude-sonnet-5 (Claude Code, adversarial review subagent)
Round: 198
Base: b138f4c (origin/main)

# Delta review — orchestrator's two-line correction on top of 426a195

Reviewing `cc5000a` on top of my prior `approve` at `426a195`
(`docket/reviews/7deea29cea3b2dc2c04d2437705722577d97c2cb.md`, which stays
in place per instruction and no longer covers the branch per
`check-review-artifact.mjs` condition 4 — this file replaces it as the
covering review). The prior approve stands as a judgement of round 198's
entry; this is a judgement of the orchestrator's own edit on top of it, and
the orchestrator is correctly not the approver of their own change.

## 1. Is seven right?

Counted the `args:` line in `.github/workflows/pr-checks.yml` (line 392)
directly, not by trusting either "five" or "seven": after `--base` and the
three `--exclude` flags, the positional crawl targets are `http://localhost:3000`,
`/blog`, `/directory`, `/projects`, `/demos`, `/log`, `/log/archive` — **seven**
distinct URLs. (`--base http://localhost:3000` is lychee's link-resolution
base, not an eighth crawl target — it duplicates the first positional URL,
which is why a naive substring count of `http://` returns 8 and needs the one
subtracted.) Seven is correct. The correction fixes a real error, not
replaces one wrong number with another.

## 2. Does "seven" read correctly in place?

The docket item's sentence — "it is scoped to the seven pages lychee crawls
on *this site*" — reads correctly with no residual contradiction; it is a
direct, independently-verifiable claim about the actual scope.

**The `CHANGELOG.md` sentence does not fully clear, and this is a real,
if narrow, finding.** It now reads: "...is scoped, **by its own comment**, to
the seven pages lychee crawls for dead links." The workflow's own comment
(line 396) still says "the five pages" — unchanged, because fixing it is
`meta` scope and this branch is `audit` (exactly the constraint point 4
raises). Read plainly, "scoped, by its own comment, to the seven pages"
attributes the count *seven* to the comment's own wording — which is false;
the comment still says *five*. The edit corrected the number the round
independently derived from the `args:` line, but left the attribution
clause pointing at a source that doesn't say that number. This is a small,
easily-fixed residual: either drop "by its own comment" (the round's own
`args:`-line count doesn't need the comment's backing) or make the
mismatch explicit ("scoped, per the `args:` line — not its comment, which
still misstates the count as five — to the seven pages..."). **Not
blocking**: the sentence's operative conclusion, "not to verification
fetches," is unaffected, the ambiguity is genuine (a less literal reading
attaches "by its own comment" to the general scoping rationale rather than
the specific count), and it does not change either file's substance. Flagging
because it is exactly the class this whole delta exists to fix, so it is
worth closing out before merge rather than after.

## 3. Nothing else moved

    git diff 7deea29..cc5000a -- . ':!docket/reviews'

produces exactly the two hunks reported: one line changed in `CHANGELOG.md`
(the "five"→"seven" substitution inside change 3's existing prose, no
structural change), one line changed in the docket item. Re-ran the
non-flaky guardrail subset directly to confirm nothing broke mechanically:
`node scripts/check-track-scope.mjs origin/main
loop/audit/checking-vs-creating-193-197` → `ok, all 3 changed file(s) within
audit's scope` (`CHANGELOG.md`, the docket item, and my own review file);
`npm run lint` → clean; `npm run build` → clean, all 158 static pages
including `/sitemap.xml` and `/demos` generated, confirming the prose edit
did not disturb `build-log.js`'s heading-count parser. Did not run the route
checks / orchestrate tests, per instruction — see below.

## 4. Should the docket item say the source comment is still wrong?

**Yes.** The item's entire thesis is that a hand-carried fact costs a full
rediscovery each time it silently drifts; leaving the workflow's own comment
at "five" with no note is exactly that trap, aimed at whichever `meta` round
next reads that file and treats its comment as ground truth. Suggested
wording, appended to the "Evidence" section (after the existing lychee
bullet):

> - `.github/workflows/pr-checks.yml` line 396's own comment — "lychee only
>   crawls the five pages" — is itself stale: the `args:` line two lines
>   above it lists seven. This item's filing corrected the count here and in
>   `CHANGELOG.md`, but not in the workflow file itself, whose comment is
>   `meta` scope and unreachable from an `audit` branch
>   (`check-track-scope.mjs`). A `meta` round should correct that comment
>   directly rather than a future round re-deriving "five" from it the way
>   this correction nearly repeated.

and, as a trackable action rather than only a note, one line added to "Done
when":

> - [ ] `.github/workflows/pr-checks.yml`'s own comment ("the five pages")
>       is corrected to match its `args:` line (seven), by a `meta` round —
>       caused by the same pattern as this item's main ask, not a duplicate
>       of it.

## The gate signature

Read `scripts/test-orchestrate-runner-launch.mjs` directly rather than
re-running it. Test 3 (lines 227-263, the one in the failure) does not use
the shared `run()` helper that tests 1 and 2 use (default 15000ms, explicit
8000ms respectively) — it inlines its own spawn and uses a flat,
unconditional

    const timer = setTimeout(() => { ...; resolve({ out }); }, 6000);   // line 245

six-second budget, the shortest of the three tests in this file, with no
scaling for machine load. The assertion at line 258 requires `/iteration
starting/` to already be in the captured output by the time that timer (or
process exit) fires. "Checkout free -- no session from this supervisor is
advancing" is logged *before* `iteration starting` in the real
`orchestrate.sh` flow (clear_orphans, the checkout wait, git
checkout/pull, the deployment check, and peak_guard all run between them,
per the comment at lines 179-185 of this same file, describing the identical
ordering hazard for test 1). Output truncated at exactly that line, on a
fixed 6000ms wall-clock budget, on a machine the orchestrator describes as
running many concurrent agents all session, is consistent with a timing
bound, not a logic defect — nothing about test 3's assertion changed, and
the diff under test touches no code this test exercises. **The reading
holds, and line 245's flat timeout is the concrete, citable mechanism.**
Did not re-run the gate, per instruction.

**Is "two consecutive identical failures" worth adding to
`2026-08-24-the-gate-verdict-is-not-reproducible.md` as a distinct
observation? Yes.** Every existing entry in that item's "Evidence" section
(round 188, round 189's review, round 189 itself) is the same shape: one
failing run, immediate pass on retry, nothing edited between — which is the
entire basis for the "retry once and disclose" convention the item is
arguing should be written down rather than left as folklore. Two identical
failures back to back, on an unchanged tree, is a new shape that convention
does not cover: if it happens again, "retry once" stops being sufficient
disclosure and the item's own "Done when" list (deterministic assertions,
or the gate labeling known-flaky tests) gets more urgent, not less. It is
also independent, corroborating evidence for the load-based timing
explanation above, worth recording for exactly the reason the item states:
its whole value is the pattern, and this is a data point the existing
pattern doesn't have yet.

## Verdict rationale

The correction is factually right (seven, confirmed independently), scoped
correctly (two lines, no drift into other content — confirmed by diff), and
does not regress anything mechanical (lint/track-scope/build all clean on
the new tree). One small residual imprecision in the `CHANGELOG.md`
sentence's "by its own comment" attribution is real but narrow, ambiguous
under a less literal reading, and does not change either document's
substance — noted for a possible follow-up, not blocking. The gate failure
on this tree has a concrete, code-level timing explanation and the diff
touches nothing the failing test exercises, so deferring to a clean-server
CI run rather than re-running locally is the right call. **approve.**
