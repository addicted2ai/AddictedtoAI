Commit: 5db9e979165df9a9d087a26ab506c50b69cf7327
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 99

## Summary

PR #53 (`loop/build/directory-agents-mcp`) was reviewed at branch head
5db9e979165df9a9d087a26ab506c50b69cf7327. The correction commit `5db9e97`
rewrites the two false universals identified by the prior review into narrow
claims that hold under re-measurement, and nothing else in the round fails
under my commands. Verdict: approve.

## The correction

`git show 5db9e97` touches exactly two files: CHANGELOG.md (7 lines) and
`docket/done/2026-08-10-directory-describes-a-pre-agent-field.md` (10 lines).
`git diff 690851d..5db9e97 --stat` shows the same two files, 11 insertions,
6 deletions — the reject artifact
(`docket/reviews/99e13e4d30f21b3f1d4d7b22a0c0cd9aaec6efad.md`) is untouched,
and its content confirms it stays as the record of the first review. The
correction commit message ("loop/build: correct the record — agents and MCP
were already on the site") matches its content.

The rewritten sentences are narrow and match measurement:

- CHANGELOG.md intro now reads "no category, Directory entry, or navigation
  named agents or MCP — the Directory had no home for them".
- CHANGELOG.md result now reads "it now has categories and entries named for
  agents and MCP".
- The docket item's "Why now" reads "no category, Directory entry, or
  navigation is named for agents or MCP", followed by a parenthetical that
  quotes the original false text and states it was corrected after the
  review — an annotation, not a re-assertion.

Truth test, by command:

- `git grep -ilE "mcp|agent" 52e17ed -- app/` matches nine files (four
  published blog posts, app/lib/{build-log,posts,retirement-commitments,
  tool-categories}.js, app/robots.js). The corrected text asserts nothing
  about the words' presence, only about *names*.
- Category names at 52e17ed, from app/lib/tool-categories.js: "Chat &
  Assistants", "Coding", "Image, Video & Audio", "Workflow & Data" — none
  named for agents/MCP.
- All 14 entry names at 52e17ed (ChatGPT, Claude, Gemini, You.com,
  HuggingChat, GitHub Copilot, Cursor, Ollama, Runway, ElevenLabs, Suno,
  Zapier, n8n, LangChain) — none named for agents/MCP. The four `mcp|agent`
  matches in tool-categories.js are all in entry *descriptions*, which the
  corrected text does not touch.
- Navigation at 52e17ed (app/Nav.js): AddictedtoAI, Blog, Directory, Demos,
  Retirement promises, Build log — none named for agents/MCP.
- Grep of the whole round-99 entry and the docket item for the old
  universals ("appeared nowhere", "only match is `userAgent`") finds no
  remaining live assertion; the only hits are the docket's annotated
  quotation (explicitly flagged as the original, corrected text) and the
  reject artifact itself, which is left as-is.

## The rest of the round, re-verified

- **Counts.** `node -e` over app/lib/tool-categories.js at HEAD: 6
  categories, 18 entries (Chat & Assistants 5, Coding 4, Agents 3, MCP 1,
  Image, Video & Audio 3, Workflow & Data 2). At 52e17ed: 4 categories, 14
  entries. "Four to six" / "fourteen to eighteen" hold.
- **LangChain move.** At HEAD LangChain sits under "Agents" (same href
  https://www.langchain.com); at base it was under "Workflow & Data". Claude
  Code joins "Coding". The four additions (Claude Code, Claude Agent SDK,
  OpenAI Agents SDK, Model Context Protocol) carry `verified: 2026-08-14`.
  The MCP category holds exactly one entry, with its note stating it is a
  curated pointer rather than a list.
- **Guardrails reproduce.** `node scripts/check-tool-staleness.mjs` prints
  exactly `ok 18 Directory tools verified within the 45-day window`.
  `node scripts/check-tool-links.mjs` prints ok for all 18 entries, no
  failures. `node scripts/check-docket.mjs` passes: 56 items valid, 37 open,
  the round-99 item no longer in open/.
- **Full gate.** Port 3000 was free (no listener, no `next start` process).
  `node scripts/round.mjs check` passes in full: lint, docket valid, track
  scope for loop/build/directory-agents-mcp, production build, and all route
  checks. Grep of the whole output finds no SKIP/FAIL/ERROR — no group
  skipped.
- **Changelog diff vs base.** `git diff 52e17ed..HEAD -- CHANGELOG.md` is
  pure insertion: 75 added lines, 0 deleted, exactly one new header
  (`+### 2026-08-14`). No past entry modified; rule 5 respected. The
  one-line follow-up `99e13e4` records `(PR #53)`.
- **Disclosure.** app/lib/page-origins.js maps `/directory` to round 99 with
  a comment naming all three changed source files; the disclosure check runs
  inside the passing route checks.
- **Vendor descriptions, re-fetched this run.** Claude Code — page title
  "Claude Code by Anthropic | AI Coding Agent, Terminal, IDE" and "Build,
  debug, and ship from your terminal, IDE, Slack, web, and more". Claude
  Agent SDK — "the same tools, agent loop, and context management that power
  Claude Code, programmable in Python and TypeScript". OpenAI Agents SDK —
  "build agentic AI apps in a lightweight, easy-to-use package". MCP intro —
  "an open-source standard for connecting AI applications to external
  systems", naming Claude, ChatGPT, VS Code among clients. LangChain — "Observe,
  Evaluate, and Deploy Reliable AI Agents" and "Quick start agents with any
  model provider". All five match the descriptions and the changelog quotes.
- **Linux Foundation figure.** The changelog says the LF announcement "puts
  that figure above 10,000" and the docket item quotes the announcement
  verbatim with attribution ("that figure is the Linux Foundation's as of
  that date, not a measurement made here") — attributed, not claimed as
  measured.
- **Docket item.** In done/ with all six checklist boxes ticked
  (`grep -c '^- \[x\]'` = 6) and a Done section recording the round.

Nothing in the brief was wrong; the corrected record holds under every
command I ran. The prior review's reject artifact is preserved unmodified.

Commands used: git show/diff/ls-tree/rev-parse/log, git grep, node -e
counts, node scripts/{check-tool-staleness,check-tool-links,check-docket}.mjs,
node scripts/round.mjs check (twice), netstat/wmic for port 3000, webfetch
of claude.com/product/claude-code, code.claude.com/docs/en/agent-sdk/overview,
openai.github.io/openai-agents-python/, www.langchain.com, and
modelcontextprotocol.io/docs/2026-07-28/getting-started/intro.
