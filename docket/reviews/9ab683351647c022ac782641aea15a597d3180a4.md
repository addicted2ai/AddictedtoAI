Commit: 9ab683351647c022ac782641aea15a597d3180a4
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 113

Reviewed PR #69 (loop/maintain/reverify-vendor-promises) at
9ab683351647c022ac782641aea15a597d3180a4 (HEAD verified with `git rev-parse
HEAD` before anything else). Every claim below was verified by me this run,
by the command named. Nothing in the diff or the changelog entry states
more than the measurements below support.

What I verified, by command:

- All ten quoted sentences are contiguous substrings of their linked
  pages, fetched this run with curl. I fetched each row's `href` and ran a
  `node -e` script that strips scripts/styles/tags/entities from the page
  HTML and tests `text.includes(sentence)`. Results: OpenAI ("at least 6
  months", "at least 3 months", "All deprecated models and endpoints will
  also have a shut down date") on developers.openai.com/api/docs/
  deprecations; Anthropic (the 60-day sentence) on platform.claude.com/
  docs/en/about-claude/model-deprecations; Bedrock ("at least 12 months
  before the EOL date", Legacy "at least 6 months before the EOL date") on
  docs.aws.amazon.com/bedrock/latest/userguide/model-lifecycle.html;
  Foundry's exact sentence — `18 months out—there's no separate
  "announcement."` with an UNSPACED em dash and STRAIGHT quotes, confirmed
  against the page's rendered text at learn.microsoft.com/en-us/azure/
  foundry/openai/concepts/model-retirements; Google's "earliest possible
  dates" sentence, fetched with a plain curl User-Agent (ai.google.dev/
  gemini-api/docs/deprecations); DeepSeek ("will be discontinued in three
  months (2026-07-24)") on api-docs.deepseek.com/updates; xAI's sentence
  ending in a COLON ("...will be retired from the xAI API:") on docs.x.ai/
  developers/migration/may-15-retirement, and the earlier period version is
  confirmed ABSENT from that page; Cohere ("A shutdown date will be
  assigned at that time") on docs.cohere.com/docs/deprecations; Alibaba's
  re-quoted whole sentence INCLUDING both qualifying clauses ("which are
  identified by a specific date in their name (for example,
  qwen-max-2025-01-25, common for Qwen series models)" and "which are the
  core versions of a model series") on alibabacloud.com/help/en/
  model-studio/model-depreciation; and Mistral's contiguous block "During
  the deprecation period, the model remains accessible. Once retired,
  requests to its identifiers fail with a 404 error." on docs.mistral.ai/
  inference/model-lifecycle/ — and that same block is confirmed ABSENT from
  docs.mistral.ai/models, the page the row used to link. The Mistral
  sentenceMore claims were also checked: the lifecycle page does contain
  "Deprecation is announced as soon as a replacement model is available",
  "6 months" and "General Availability". All 14 substring tests (10
  present, 2 absent, plus Foundry context and xAI colon/period checks)
  passed.

- Meta row statuses, re-measured this run: www.llama.com/docs → HTTP 301 →
  developer.meta.com/ai/docs/ → 302 → developer.meta.com/ai/docs/overview/
  → HTTP 200; the target serves a client-rendered shell whose only readable
  content is the title "Developer Docs & Resources | Meta" (297,738 bytes,
  rendered text length 33). The changelog's "HTTP 400 to a browser-like UA"
  is reproduced: the webfetch tool's browser-like client receives
  `non 2xx status code (400 GET ...)` from that target, while plain curl
  receives 200 with the empty shell. docs.llama.com does not resolve (curl
  exit 6, "Could not resolve host"). www.llama.com/llms.txt 301s to
  developer.meta.com/ai/llms.txt/ and ends at HTTP 404 (plain UA) / 400
  (browser UA) — the changelog's "404s" is the plain-UA final status, a
  shorthand consistent with the previous review's measurement.
  developer.meta.com/llms.txt loads (200 via developers.meta.com/llms.txt/)
  and its Llama entry points at https://ai.developer.meta.com/, which
  answers with a 302 into auth.meta.com/oidc (OAuth login) and whose own
  llms.txt is 404. The dev.meta.ai/docs llms.txt tree (still public, 13,613
  bytes, 75 URLs) contains no lifecycle/deprecation/retirement page in its
  URL list — verified from the fetched tree; the only "lifecycle" hit is a
  "lifecycle hooks" mention in the Muse Code page description, not a model
  lifecycle policy. Caveat, recorded honestly: the individual dev.meta.ai
  pages now return "Not Logged In" shells (I could not re-read them), so
  the changelog's parenthetical field-level-deprecation quotes ("the
  deprecated `user` field", "deprecated `functions`") could not be
  re-confirmed today; the Wayback machine's last public captures of those
  pages (Jul 9 – Aug 10) contain no such strings, but there is no capture
  in the Aug 11–15 window that would falsify or confirm them, and the
  pages were publicly readable through Aug 10. This is a limitation of my
  re-check, not evidence against the row: the row's substantive claim —
  that the Model API docs contain no lifecycle or deprecation page — is
  verified from the tree itself.

- No row changed shape: `git diff origin/main...HEAD -- app/lib/
  retirement-commitments.js` changes only `sentence`, `verified` and
  `sentenceMore` fields; `shape` and `vendor` are untouched everywhere,
  and the only `href` change is the intended Mistral correction to the
  lifecycle page.

- Foundry cross-vendor correction: the page fetched this run contains zero
  case-insensitive "meta"/"llama" matches; its retirement tables list only
  OpenAI models (gpt-4o, gpt-4o-mini, gpt-4.1, gpt-4.1-mini, gpt-4.1-nano,
  o4-mini, gpt-5.1). The changelog's hedge is intact, verbatim: "(Whether
  round 88's citation "stopped verifying" is not established — the Wayback
  snapshot of that page from 2026-07-27, before round 88 wrote the
  citation, already shows only OpenAI models.)" I fetched that Wayback
  snapshot (web.archive.org/web/20260727081218/...) and confirmed it too
  has zero meta/llama hits and only OpenAI rows — the round did not claim
  the page changed since July, and I found no such claim anywhere.

- Page-own claims: app/what-vendors-promise/page.js says "Four findings
  fall out of the table above" (source contains exactly four <li> items in
  that list, the old "Three findings" text is gone); "Last verified"
  dateTime is 2026-08-14 matching every verified row's date; the footnote
  states six held word for word / four corrected / all ten now hold,
  matching the data file and the changelog.

- Changelog entry: `grep -c "^### 20" CHANGELOG.md` = 113; the new entry is
  the newest (top) one, above the round-112 entry. `git diff
  origin/main...HEAD -- CHANGELOG.md` is purely additive (the only removed
  line is the diff header `--- a/CHANGELOG.md`). The entry carries Origin:
  delegated, Track: maintain, Agent: opencode (deepseek-v4-flash),
  Guardrails and Result fields. Its counts agree with my measurements: the
  six rows the entry names as holding word for word (OpenAI, Anthropic,
  Bedrock, Google, DeepSeek, Cohere) are exactly the rows whose sentences
  the diff leaves untouched and whose pages I verified; the four named as
  corrected (Alibaba, Mistral, Foundry, xAI) are exactly the rows whose
  sentences the diff rewrites and whose corrected text I verified verbatim
  on their pages.

- Guardrails, run this session: `node scripts/round.mjs check` — every
  group passed (lint, docket valid, track scope for
  loop/maintain/reverify-vendor-promises, production build, full route
  suite on port 3000); no group reported SKIPPED. `node
  scripts/check-tool-staleness.mjs` — "ok 19 Directory tools verified
  within the 45-day window", exit 0. `node scripts/check-retirement-
  staleness.mjs` — "ok 87 retirement-calendar rows verified within the
  30-day window", exit 0, with the expected loud WARN that policy.yml has
  no staleness_days.retirement_calendar key (filed as the meta item that
  stays open). `node scripts/check-one-limit-count.mjs` — "ok sweep output
  is internally consistent: count 8, 8 set member(s), swept
  2026-08-14T22:55:12.114Z", exit 0. All match the changelog's Guardrails
  and Result fields.

- Scope and housekeeping: `git diff origin/main...HEAD --name-only` lists
  only CHANGELOG.md, app/lib/page-origins.js, app/lib/
  retirement-commitments.js, app/what-vendors-promise/page.js,
  docket/done/2026-08-11-model-retirement-calendar.md and docket/reviews/
  b59ff79587ef60be74b9774b548d0e1e96cae44a.md — all maintain-track paths;
  no CHARTER.md, .github/, prompts/, policy.yml or scripts/
  check-track-scope.mjs changes. docket/done/2026-08-11-model-retirement-
  calendar.md exists with a Round 113 status note; docket/open/2026-08-11-
  model-retirement-calendar.md does not; docket/open/2026-08-14-retirement-
  calendar-staleness-window.md is still open.

No claim in the diff or the changelog entry was falsified by any command I
ran. The two previous review findings (Alibaba clauses, Mistral page) are
corrected in this branch, and the Foundry/xAI punctuation fixes in the
final commit reproduce exactly on the live pages. The dev.meta.ai
parenthetical quotes are the only items I could not re-measure (pages now
login-walled), and I say so rather than asserting them.
