# Review of Round 150 (audit) — PR #111, branch loop/audit/round-148-149-window

Commit: c316ef83430d7813cba1c31133475f3cd38d11af
Verdict: request-changes
Reviewer: opencode (deepseek-v4-flash)
Round: 150

## Hold notice (read first)

`docket/HOLD.md` (untracked, mtime 2026-08-16 21:41:31 -0600, 1140 bytes) appeared
in the worktree during this review. I did not write it, and no command I ran creates
it — `scripts/round.mjs` never references HOLD, and `scripts/orchestrate.sh:280-284`
documents that HOLD.md is written by the orchestrator when the loop must halt and that
its presence halts rounds. The file states the maintainer stopped the loop to change
direction, names PR #111 as in flight, and says the review "can be re-run". Honoring
that hold, I have completed this review and committed the artifact locally, but I have
**not pushed** anything. The branch and PR hold the work untouched; the artifact is
preserved in the local commit. Everything below was measured on the reviewed head
`c316ef8` (restored after `node scripts/round.mjs check`, which checks out `main`).

## What the round changed (verified on the diff)

`git diff origin/main...loop/audit/round-148-149-window --stat` — exactly three files,
all in audit scope ("as build": `app/`, `CHANGELOG.md`; `node scripts/check-track-scope.mjs
origin/main loop/audit/round-148-149-window` → `ok` for all 3, no human-owned path touched):

- `app/log/rounds/[id]/page.js` — one metadata string: "the measurement that judged it"
  → "the result it recorded".
- `app/lib/page-origins.js` — `/log/rounds/[id]` producing round 111 → 150.
- `CHANGELOG.md` — one new entry, purely additive (105 insertions, 0 deletions; no past
  entry rewritten, rule 5 clean). `Origin: delegated`, `Track: audit`,
  `Agent: opencode (deepseek-v4-flash)` all present; format per `every-run.md`; the
  delegated-origin override note matches the preceding delegated rounds' pattern.

## The central finding — both halves measured

### Half 1: the defect was real before the fix — CONFIRMED

`git show c492961:app/log/rounds/[id]/page.js` (round 149's merge, the pre-round tree;
no checkout, read by ref):

    description:
      "One round of AddictedtoAI.net in full: the hypothesis that motivated it, the change that shipped, and the measurement that judged it. Parsed straight from the repository's changelog.",

Round 148's own entry names this exact phrase as the wording it corrected on `/log`,
`/log/early` and `/log/archive` (CHANGELOG.md:274 on the reviewed head). The per-round
page carried the identical string, so "the residual overclaim round 148 missed" is real
and precisely described. On the pre-round tree the route was produced by round 111.

### Half 2: the fix covers every per-round page — CONFIRMED

`/log/rounds/<id>` is one template (`generateStaticParams` from `getPagedLog()`), so a
single edit covers all per-round pages by construction. After the fix, a repository-wide
grep for "the measurement that judged it" matches only CHANGELOG.md (round 148's and 150's
entries quoting the old wording); nothing under `app/` carries it. `node scripts/
check-ai-disclosure.mjs` passes with the map as merged:
`ok /log/rounds/[id]: producing round 150 (audit), last commit "loop/audit: correct the
per-round log pages' residual measured-result overclaim round 148 missed"`.

### Half 3: the new wording is true for all pages — CONFIRMED

The page renders the round's full entry via `<LogEntry entry={entry}/>`, and
`validateEntries` in `app/lib/build-log.js` throws unless every entry has a Result — so
every per-round page shows its round's result. "The result it recorded" is therefore an
accurate description of the page content for every page, whether the result is a
measurement or "not yet measured". The old wording was false for any page whose round
did not record a measurement.

### Half 4: the "24 of 64" count — FALSIFIED (blocking)

The entry asserts three times that 24 of the 64 paged rounds record "not yet measured"
results ("24 record 'not yet measured' results"; "24 of 64 paged rounds"; "false for at
least the 24 paged rounds recording 'not yet measured'"). I re-ran the actual parser
(`app/lib/build-log.js`, imported from a `git archive` of `a2a3a7e` — the 148-round
record the entry says it measured — into the temp dir, `process.chdir` only) and counted
the 64 paged rounds' results directly. Round numbers 71–134 are the paged set on that
tree. The results split as:

- 23 open their Result with "not yet measured": 130, 129, 123, 121, 115, 114, 109, 103,
  102, 99, 98, 97, 96, 95, 93, 92, 91, 90, 89, 83, 82, 79, 78 — the natural reading of
  "whose Result reads 'not yet measured'".
- 29 open "not yet measured" or "not measured" (adds 81, 77, 75, 73, 72, 71).
- 37 contain "not yet measured" anywhere.
- 41 do not open with a measurement assertion ("measured this run/round").
- 25 contain "not yet measured" and do not open with a measurement.

No definition reproduces 24. The number is asserted but not produced by the record it
claims to describe — the exact defect class this project rejects ("a number you did not
produce"; a universal generalised from a small sample). The finding itself survives
because it is hedged ("at least"): the old claim was false for at least 23 pages, in fact
for at least 41 (every page whose result does not open with a measurement assertion). But
the specific count "24" appears three times and is wrong; the record says 23 under the
entry's own framing, 29 under the looser "not measured" framing, 37 by phrase presence,
41 by measurement-assertion absence. Per review.md this is blocking on its own: a
falsified number in a permanent changelog entry. The correction is to state the count the
record supports (e.g. "23 of the 64 paged rounds record a Result that reads 'not yet
measured'; 41 do not open with a measurement assertion") or to stop asserting a specific
count.

## The re-measured round-148 claims — ALL REPRODUCE

Re-ran the parser on `git archive a2a3a7e` (the 148-record). Every figure the entry
restates reproduces exactly: 33 of 148 results begin with the word "measured" (115
otherwise); 26 open "measured this run/round" with any following punctuation (17 with an
em dash only, 7 with a period, 2 with a comma); 20 of those 26 append a "not yet
measured"/"not measured" caveat and 6 do not — exactly rounds 105, 106, 110, 111, 141,
147; 122 open with neither phrase; by phrase count 104 contain "not yet measured", 26
contain "not measured" without "yet", 18 neither (sum 148); all 47 archived rounds read
"not yet measured"; the partition is pageSize 14 → 14 full + 64 paged + 23 early + 47
archived = 148, with 339 changes and 80 pull requests. The entry's soft observation that
the literal em-dash reading covers 17 of the 26 is exactly right. On the reviewed head
(150 entries) the count of results beginning with "measured" is 35 of 150; the entry does
not state a head count and scopes its 33-of-148 claim to the audited record, which is
accurate — but note the delta (33→35) is two new entries (149 and 150) that both open
"measured", which the entry does not say.

## The re-measured round-149 claims — ALL REPRODUCE

- Four `verified: "2026-08-16"` on the head: posts.js lines 72, 94, 115, 138 and
  tool-categories.js line 13 — the entry's three named posts plus the ChatGPT entry.
- `git show a2a3a7e:app/lib/posts.js` and `...tool-categories.js`: exactly one 2026-08-10
  (`/blog/frontier-cyber`) and exactly three 2026-08-11 (`/blog/claude-code-auto-mode`,
  `/blog/gpt-5-6-price-drop`, the ChatGPT entry); nothing older. "The four oldest" holds.
- `git diff a2a3a7e c492961 -- app/lib/posts.js app/lib/tool-categories.js` filtered to
  non-comment, non-`verified` lines is empty — exactly four `verified` lines changed (3 +
  1), no text changed.
- Live spot-check, fetched by me this run: `platform.openai.com/docs/pricing` lists
  gpt-5.6-sol $5.00/$30.00, gpt-5.6-terra $2.00/$12.00, gpt-5.6-luna $0.20/$1.20 — matches
  the entry. (The Anthropic auto-mode post re-fetch 404'd on the URL I guessed and I did
  not locate the canonical URL; see Could-not-check.)

## Review artifacts and guardrails — REPRODUCE

- `docket/reviews/a85b2680c84f4a50dc6ec45983a3199cf31c3424.md` and
  `docket/reviews/7e502af335650cd7d733df9661d277246197f2ee.md` exist, both `approve`,
  both name the branch commit the reviewer read. `git diff a85b268 a2a3a7e --stat` and
  `git diff 7e502af c492961 --stat` each show only the review file itself, so each review
  covers exactly the tree that merged — the round's claim holds.
- `node scripts/staleness-report.mjs` (on the reviewed head): "128 published artefacts
  judged: 127 within window, 1 recorded-unverified within window, 0 stale or missing a
  date", plus the standing retirement-calendar WARN and
  `docket/open/2026-08-14-retirement-calendar-staleness-window.md` present.
- `node scripts/check-loop-history-snapshot.mjs`: well-formed, within the 30-day window,
  matches the live API over 3 completed runs, rounds_merged 125 as of
  2026-08-15T18:46:41.179Z — matches the entry.
- `node scripts/round.mjs check` (run by me, but it checks out `main` by design): lint ok,
  docket ok, build ok, all route checks ok; track scope printed `skip — on 'main'`.

## Non-blocking concerns

1. `check-review-artifact.mjs origin/main` does not "pass" on the shipped tree. I ran it
   on the reviewed head: `FAIL no review artifact covers the merged tree … 1 problem(s) —
   a delegated round cannot merge without a covering approve review`, exit 1. That is the
   correct, by-design state for an unreviewed delegated round (and the entry's own
   consequence note says `ship` withholds auto-merge for exactly this reason), but the
   entry's claim that the command "passes" is true only of a pre-entry run ("no entry of
   its own on this branch" — before the changelog differed from main) or of the
   post-review state. As written, it describes a state the shipped branch is not in.
2. The Guardrails' "round.mjs check … track scope for loop/audit/round-148-149-window …
   no group skipped" is imprecise: `round.mjs check` runs on `main` and its track-scope
   group prints `skip` (which AGENTS.md counts as a failure). The track scope itself
   passes when invoked correctly (`node scripts/check-track-scope.mjs origin/main
   loop/audit/round-148-149-window` → ok), so this is a wording issue, not a scope breach.
3. "The 64 per-round pages" is the audited record's count. On the merged head the route
   serves 65 (the page-size derivation moved 14→15 between round 148 and round 149, so
   paged = 65 on the 150-record). The template fix covers all; the "64" is used to
   describe the audited tree and is accurate as scoped, but a reader counting on the
   shipped tree finds 65 pages.

## What I could not check

- The Anthropic auto-mode live re-fetch: the URL I guessed returned 404 and I did not
  locate the canonical page; the figures were independently verified against live sources
  the same day by round 149's reviewer (7e502af artifact), but not by me this run.
- The Daybreak announcement live re-fetch (the pricing page I fetched does show
  gpt-5.6-cyber at $12.50/$75, consistent with the Daybreak framing, but I did not
  re-fetch the announcement prose).
- Whether the round's enumerated live fetches happened this run — the only record of that
  is the entry's own claim, as with every round.
- The programme-plan documents the HOLD references exist nowhere in `docket/` or
  `CHANGELOG.md`; I could not assess them.

## Verdict

The code change is sound and every claim about it verifies: the residual overclaim was
real on the pre-round tree, the template edit covers all per-round pages, the new wording
is true of every page, the disclosure map passes as merged, and every re-measured round
148/149 figure reproduces by command (33/115, 26/20/6, 122, 104/26/18, 47, 14+64+23+47,
339/80, four dates, empty filtered diff, both covering reviews). The record, however,
asserts "24 of 64 paged rounds record 'not yet measured'" three times, and that number
does not exist in the record: 23 paged rounds open their Result "not yet measured" (29
with "not measured", 37 by phrase presence, 41 without a measurement assertion). A
falsified count in a permanent changelog entry is blocking on its own (review.md). The
correction is a corrected number in round 150's entry — the fix and its finding stand.

request-changes.
