Commit: 9fc83acf1d0a52157d8e0e1942b159325f7060ce
Verdict: request-changes
Reviewer: claude-opus-5 (Claude Code subagent)
Round: 175

## Scope

Confirm-only pass on `3db84bd..9fc83ac`, one commit, three files
(`CHANGELOG.md`, `app/globals.css`,
`docket/done/2026-08-22-article-p-line-length.md`), 43 added / 111 removed.
No re-audit of anything already verified.

**The deletion itself is clean and complete — all three of its own goals
confirm.** The verdict is `request-changes` on the second clause of
confirmation 3, which asked me to check for contradictions beyond the
opening paragraph. One survives, in two of the three files. It is a
nine-word deletion, and the correct wording is already in the history.

## Confirmed

**1. The family claims are gone, not narrowed.** No surviving assertion in
any of the three files about what the other `ch`-capped selectors are, where
they render, or how they relate to `article p`. Checked by grepping all three
files for every one of the twelve capped selectors plus "secondary",
"primary reading" and `<article>`: the round-175 changelog entry returns
zero hits; the docket item returns zero; `app/globals.css` returns only its
own rule definitions and one pre-existing `article code` comment naming
`.log-entry code`/`.charter-correction code`, which is about `overflow-wrap`
and is untouched across the whole round (`git diff 689806b HEAD` has no hit
for it). What remains is existence-only — "the site's other `ch`-capped
rules" — with an explicit refusal to characterise them, plus the count
"twelve", which I re-verified at `HEAD`: 13 `ch` caps, 12 at `62`/`68ch`.

**2. Nothing executable moved.** `max-width: 80ch;` occurs exactly once at
both `3db84bd` and `9fc83ac`, and appears in the diff only as a context line
(its line number shifts 204 → 179 because 25 comment lines were removed; the
declaration is unchanged). `const MAX_FULL_LINE_CHARS = 107;` sits at line 50
in both, and `scripts/check-article-line-length.mjs` is not among the touched
files. The only `+`/`-` lines in `globals.css` containing "max-width" are two
deleted comment lines that quoted commands as prose. The `ch`-cap census is
identical before and after, so no rule was added, removed or retuned.

**3a. The opening paragraph no longer contradicts the entry.** "no error
found in it" is gone; it now reads "all seven **numbered** premises
reproduced exactly as written … its unnumbered body prose did carry an error,
covered below." That is what was actually verified — the brief does declare
seven numbered premises, and `check-briefs.mjs` reads only that section — and
the pointer to the coverage is present.

**4. The attribution is honest.** The entry now says the orchestrator
supplied the `| head -6` command in conversation, that "that command is not
itself in the brief document, only its five-line output", and that this round
verified only that the command reproduces the brief's enumeration when run
against `ddffff7`. That is precisely what I reproduced myself last pass, and
it no longer presents the command as quoted from the brief. The docket item's
shorter version cites the brief's enumeration without attributing a command
at all, which is also correct.

## Not confirmed — 3b, one surviving self-contradiction

`app/globals.css:172-174`:

> for a page-length cost the same round measured at +0.5% to +4.3% across
> those five pages -- cheaper than every candidate the item itself measured
> except the 90ch no-op, **and the only one narrower than that no-op.**

`CHANGELOG.md:216-221`:

> `80ch` was chosen over the docket item's narrower candidates (`68ch`,
> `62ch`) … cheaper than every narrower candidate measured (`68ch`: +10.88%
> on `/blog/chatgpt-ads`) -- **and it is the only candidate narrower than the
> confirmed-no-op `90ch`**.

Both sentences name `68ch` and `62ch` as candidates the item measured, then
assert `80ch` is the only candidate narrower than `90ch`. `68ch` and `62ch`
are narrower than `90ch`. The two halves of each sentence cannot both be
true. The docket item's own text confirms the referent — "`68ch` and `62ch`
cost more (+13%/+10.88% … +24% …)" — so "candidate" has no reading that
rescues the clause: of the four widths measured, three are narrower than the
no-op, not one.

This is finding 5 from my previous artifact
(`docket/reviews/eb79667e8bc946700754ff6c3d6fd70f163ef75f.md`), where I
graded it a nit because two blocking findings carried the verdict. It is
raised now because confirmation 3 explicitly asked whether any other sentence
in the entry contradicts another, and because this commit rewrote the tail of
that exact sentence in `globals.css` — restoring the correct clause and
re-attaching the false one to it — rather than dropping it.

**Fix, and it is a deletion, not a fourth attempt at a claim:** remove "and
the only one narrower than that no-op" / "and it is the only candidate
narrower than the confirmed-no-op `90ch`", keeping the parenthetical geometry
in the changelog. What is left — "cheaper than every candidate the item
itself measured except the 90ch no-op" — is true, is the wording that shipped
at `689806b`, and was already approved there. No new claim is needed.

The measurements themselves remain sound and are not in question: medians
87-94 after `80ch`, costs +0.50%/+2.30%/+3.94%/+1.64%/+4.26%, `68ch` at
+10.88%, and 8.625px/ch giving 80ch = 690px against a 732px container.

## Process note

`node scripts/round.mjs check` was not run and port 3000 was not touched, as
instructed. A system-reminder during this pass again directed me to prefer
`cat`/`sed` over the Read/Edit/Write tools; `CLAUDE.md` says the opposite, so
I followed `CLAUDE.md` and am disclosing the conflict rather than complying
silently. Only this file is committed.
