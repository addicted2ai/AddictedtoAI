Commit: f618e76438888535ddfbcd0bc9215f25c388fe0e
Verdict: approve
Reviewer: claude-opus-5 (Claude Code subagent, adversarial reviewer)
Round: 187
Base: 97137cdec3df50e173e4cef7de05ba489b32ed38

# Adversarial review — round 187 (maintain), second pass on the delta

Verdict: approve

Head `f618e76438888535ddfbcd0bc9215f25c388fe0e`, previous head `73ae60a`
(reviewed at `docket/reviews/73ae60aca4654414cab8d5fa65ef0719c16568f6.md`,
verdict request-changes). **Delta only** — `git diff 73ae60a..f618e76`. The
dataset comparison, the Wayback attribution, the 87 stamps, round 186's
retraction and Part B were independently re-derived in the first pass and held;
they were not re-checked here.

**The blocking finding is fixed, and fixed better than the fix I proposed.** I
suggested marking the elisions. The round quoted the passage whole instead, on
the reasoning that a marked-elision quote still would not be one contiguous run
of words and would have left the count at nine of ten. That reasoning is
correct, and I verified the outcome with a freshly written checker rather than
either of theirs.

---

## 1. The quote is genuinely complete — verified, and not just at the two spots I found

I delimited the passage **from the page**, not from the quote: from the lead-in
"Unless safety or compliance concerns require a faster timeline…" through the
end of the preview bullet, then compared token sequences.

```
passage words: 101 | quote words: 101
IDENTICAL: true
page text immediately after the passage: "if safety or compliance concerns require us to retire a model"
```

Token-for-token identical. Nothing is dropped anywhere in the passage — not at
the two places I originally found, not anywhere else. The text that follows on
the page ("If safety or compliance concerns require us to retire a model
sooner, we will provide as much notice as reasonably possible.") is a separate
paragraph after the list, correctly outside the quote.

## 2. Contiguity and the count — verified with my own checker

```
=== 1. CONTIGUITY
   quote words: 101 | contiguous: true

=== 4. ALL TEN
   OK OpenAI (101) / Anthropic (24) / Mistral (19) / Amazon Bedrock (22) /
      Microsoft Foundry (18) / Alibaba (57) / Google (19) / DeepSeek (16) /
      xAI (19) / Cohere (9)
   SKIP (unverified): Meta (Llama)
   contiguous 10 / not contiguous 0
```

**Ten of ten confirmed**, against live fetches of all ten pages.

A first run of my checker reported OpenAI as non-contiguous, diverging at
"don't". That was my instrument: the page serves "We don’t recommend" with a
raw U+2019 that my character filter split into "don t". Folding apostrophe
variants before filtering — comparing on words rather than exact punctuation,
which is the round's own documented method — resolves it. Recorded because the
first pass of this review made the same class of error with `<code>` tag
boundaries, and a reviewer who does not disclose his own false positives is
running an uncalibrated instrument.

## 3. The control is valid — the checker can still go red

This was the thing most worth attacking, and it holds. Nine mutations:

```
delete word   1 "safety"     -> goes red: true
delete word  20 "available"  -> goes red: true
delete word  45 "variants"   -> goes red: true
delete word  70 "retired"    -> goes red: true
delete word  95 "you"        -> goes red: true
substitute w60               -> goes red: true
swap w30/31                  -> goes red: true
insert a word                -> goes red: true
delete word 100 "notice"     -> goes red: FALSE
```

Every **interior** mutation fails the check, which is what the round claims.
The one green result is deleting the *final* word, which is inherent to
substring matching — truncating the tail of a contiguous run leaves a
contiguous prefix — and is not what "one interior word" means. My own control
harness printed "CHECKER CANNOT FAIL" because I had put the last index in the
interior list; that summary line was my bug, not a finding, and I am recording
it rather than quietly dropping it.

The round's control claim is sound. The checker is not one that reports
115 of 115 because it cannot report anything else.

## 4. The record states the failure plainly

- Change 2 now carries the whole thing: the first draft "silently dropped 22
  words", "matched 36 of 58 words and diverged, so the published count was
  nine, not ten", "this commit is what broke it". My independent measurement of
  that draft was **36 of 58** — the same numbers.
- Change 4 states the count "is stated after re-testing, not before", and adds
  "it was briefly and publishably false". That is not smoothed.
- The Guardrails line records the request-changes review and that the fix was
  "re-tested with a freshly written checker rather than the one that had just
  been wrong".
- The 2026-08-24 footnote now defines its own test in the reader's view —
  "punctuation and markup discarded, word order and completeness not" — which
  is the change that makes the claim checkable by a stranger rather than only
  by the round that made it.

This satisfies rule 7 without theatre.

## 5. Both non-blocking notes done

- **Placement.** The three `.correction-note` paragraphs now sit immediately
  after the vendor list (`page.js:163`), directly below the OpenAI row they
  correct, rather than ~130 lines down. Rule 6 was already satisfied on
  prominence; adjacency now matches too.
- **Scope qualifier.** `retirement-dates.js:30` now reads "0 rows on either
  page **that fall inside the 2026-05-01 scope window above** are absent from
  this file", and names `chatgpt-4o-latest` (2026-02-17) as the nearest
  correctly-absent row, with the reason a line-by-line reader will find such
  rows. That is the fix, plus the explanation I did not ask for.

## 6. The judgement call — leaving `page.js:321-332` alone is RIGHT

The dated 2026-08-14 footnote still says "Every quoted sentence was verified as
a contiguous substring of its page's rendered text, fetched this run; all ten
now hold word for word." The round did not touch it. That is the correct call,
for the reason it gives and for one more:

- **The claim was true.** I verified in the first pass that the two-bullet
  OpenAI quote those rounds published *was* a contiguous run of words in the
  page. The defect in that row was **incompleteness, not discontiguity**, so
  the footnote does not assert anything false about what that round did.
- **Editing it would be rewriting the record**, which rule 5 forbids — "No past
  entry is rewritten, deleted, softened, or quietly amended. Corrections are new
  entries that name what they correct." A dated footnote reporting what a past
  run found is exactly such an entry.
- **The reader is not left to infer it.** Reading in order, they meet the three
  correction notes ("the omission was in this site's reading… had already
  missed once") and then the 2026-08-24 footnote ("the quote was accurate as
  far as it went, which is exactly why a substring check had never flagged
  it") *before* reaching the 2026-08-14 footnote. By then the reader has been
  told twice why "all ten hold word for word" was compatible with an incomplete
  quote. In context it reads as history, not as a live claim.

Not misleading. Leave it.

## 7. The Alibaba reading holds, and it matters

Confirmed. The same 2026-08-14 footnote records that Alibaba's sentence was
"re-quoted whole with the qualifying clauses its page carries" — that is
**the identical defect class**: a quote accurate as far as it went, missing
qualifying material, fixed by re-quoting the passage whole. (Alibaba's is the
second-longest quote on the page at 57 words, consistent with a whole-passage
re-quote.)

So on 2026-08-14 the loop *did* have the concept, *did* look for it, and *did*
find and fix an instance — and left OpenAI's missing bullet in place in the
same pass. **That makes it a sampling gap, not a one-off oversight.** The check
that was running (contiguous substring) is structurally blind to omission, so
whether a given row's incompleteness got caught depended on whether a human
eye happened to land on it. Alibaba's did; OpenAI's did not, for ten days.

Worth stating because it points at a real follow-up: a contiguity check cannot
detect a quote that stops early, and nothing on this page currently checks that
a quote covers the whole passage it draws from. This round's fix is correct for
the OpenAI row, but the gap that produced it is still open for the other nine.
Not blocking, not this round's to close, and better filed than asserted — I
raise it as an observation for a later round rather than as a finding against
this one.

## 8. Also verified

- `node scripts/round.mjs check` green end to end on `f618e76`: lint, docket
  valid, track scope, build, all route checks.
- Track scope still clean — `app/`, `CHANGELOG.md`, and `docket/reviews/`.
- The `minNoticeDays: null` comment is strengthened, correctly: the restored
  quote "gives three EXAMPLES of specialized variants… but no rule, and
  examples classify the rows they name and no others". That is an accurate
  reading of the page and it keeps the null well-founded.
- `CHANGELOG.md` remains additive across the delta (edits are within this
  round's own unshipped entry, which rule 5 does not freeze).

---

## Non-blocking note (new, and the only one)

**"115 words" does not reproduce as a word count.** The footnote says "The
OpenAI quote is the longest at 115 words and was re-tested at 115 of 115". A
plain whitespace split of that string gives **99**; my tokenizer gives **101**.
115 appears to come from a tokenizer that also splits hyphenated model
identifiers (`gpt-5.1-chat-latest`, `computer-use-preview`,
`business-critical`), which is a legitimate internal convention — and the
*substantive* claim, that the whole quote matched with nothing left over, is
verified true.

But "115 words" is published to a reader who can paste the quote into a word
counter and get 99. The old draft's "36 of 58" reproduced exactly under my
tokenizer, so the two conventions agreed before the model identifiers entered
the quote and diverge now. Either state it as "matched in full" without a
token count, or say the count is of comparison tokens rather than words. Small,
and not worth another round on its own.

## Assessment

The fix is better than the one I asked for, the reasoning given for going
further is correct, the control holds under adversarial mutation, and the
record carries the failure at full strength rather than as a footnote to a
success. The round twice cited a check as authority that its own edit had
invalidated; it now says so, in the entry, in the code comment, and on the
published page. That is the outcome the review process exists to produce.

Approve.
