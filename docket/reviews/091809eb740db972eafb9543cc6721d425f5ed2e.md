Commit: 091809eb740db972eafb9543cc6721d425f5ed2e
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 153

## Summary

Review of PR #117, `loop/maintain/triage-author-news-queue`, round 153
(maintain), at 091809eb740db972eafb9543cc6721d425f5ed2e. This is the first
use of `docket/dropped/` in the project's history: 25 of 30 author items
moved there (6 for test 1, 14 for test 2, 5 by consolidation into
`2026-08-17-post-the-ai-security-week.md`), leaving author at 6 against the
queue budget of 6. I sampled every `## Dropped` section (all 25), read the
full body of the most aggressive drops, diffed the consolidated item's
sources against its five predecessors, resolved all 14 of its URLs, verified
the numbers against the tree with commands, confirmed the Origin and the
withheld auto-merge, and ran `node scripts/round.mjs check`. It stands: the
drops are judgeable, the consolidation is complete and its sources hold, the
numbers are true, and the entry describes what happened. Aggressive but
defensible; approve.

## The drops — sampled, not skimmed

The entry's split (5 consolidated, 6 test 1, 14 test 2 = 25) reproduces
exactly by script across the 25 files in `docket/dropped/`. Every `## Dropped`
section names the test it failed or the consolidated item that absorbed it,
summarises the item's substance in its own terms, and gives a concrete refile
condition; none rests on date alone and none says "stale" without a stated
judgment. The two oldest items (10 August) were kept, and several 16 August
items were dropped — recency was not the test.

Most aggressive, all defensible:
- `claude-text-watermarking` (priority-1, filed 14 Aug): dropped for test 2.
  The reasoning is honest and cites the item's own restate-the-page
  Done-when as the test-2 signature; refile condition (the promised detection
  API ships, or the EU Code of Practice changes) is concrete. Aggressive, not
  indefensible.
- `google-assistant-retirement`: its own reasoning calls it "a legitimate
  'tools shut down' story for this site" but drops it for test 2 — the site
  cannot verify the email and would restate a dated announcement; refile is
  explicitly the measured 4 September aftermath.
- `twitch-trains-amazon-ai` (priority-1, the CPO's "nobody would opt in"
  quote): dropped for test 1 — disclosure-week peg; the durable policy fact
  lives on Twitch's own support page.
- `qwen3-8-open-weights` (first Max-class open release): dropped for test 2 —
  the site has no open-models beat, and the item restates model cards, the
  licence file and NVIDIA's blog.

I did not find an item that clearly passes both charter tests and was dropped
because it was easier to drop than to write. The survivors are the six where
the site adds analysis (the two 10-Aug measurement/EU-AI-Act pieces),
verification (the price-war item, checked against both vendors' pricing
pages), actionability (Manus's dated user-data deadline) or the consolidated
security arc; the dropped items are the ones whose Done-when is "state what
the vendor's page says".

## The consolidated item — sources hold

`2026-08-17-post-the-ai-security-week.md` carries exactly 14 citations in its
Evidence section, and diffing the URL lists (not eyeballing) shows every one
of its five predecessors' 14 formal Evidence sources appears in it:
CloudSEK/Hudson Rock/Ars (LiteLLM), A Security/Zoom ZSB-26015/The Verge
(ZOOMSDAY), OpenAI-AWS/OpenAI-expand/TechCrunch-IBM (Daybreak),
HF-timeline/OpenAI-HF/HF-July-disclosure (intrusion), and
Anthropic/TechCrunch (multiagent). All five predecessors name the
consolidated item in their `## Dropped` sections (5/5). All 14 URLs resolve —
11 returned HTTP 200, and the three OpenAI pages (which block curl) were
fetched live and return the dated content the item describes; none 404s, so
the volunteered "each re-fetched and verified this run" claim holds. One
non-blocking nuance: the Daybreak predecessor also carried an IBM newsroom
link as a parenthetical inside the TechCrunch bullet, which the consolidated
item does not carry; it was an inline pointer, not a formal Evidence source
of any claim, so the "all fourteen of their sources" count is true as stated.

## Numbers, record, scope

- Numbers match the tree: 30 post- items on origin/main -> 6 on this branch;
  25 dropped; 30 - 25 + 1 (the new consolidated item) = 6, which the entry
  states rather than leaving the reader to notice the discrepancy.
- `node scripts/check-docket.mjs`: "109 docket item(s) valid (35 open)",
  "author base 30 -> head 6 (queue budget 6)" — matching the entry's
  guardrails line.
- Opening paragraph: every number is reproducible by a command; the
  first-use-of-dropped claim holds (`docket/dropped/` on origin/main held only
  `.gitkeep`).
- Origin: delegated (read via the site's own build-log parser), and `ship`
  withheld auto-merge — PR #117 is OPEN with auto-merge not requested and
  merge status BLOCKED, consistent with a delegated round awaiting this
  covering review artifact.
- The consequence is stated: the Result section says the steady-state effect —
  scout can only refile as fast as author publishes against a queue at budget
  — is the outcome the dispatcher will measure.
- Scope clean: the PR touches only `CHANGELOG.md` and `docket/`
  (done/dropped/open); `docket/HOLD.md` untouched; rule 5 clean.
- `node scripts/round.mjs check`: pass (lint, docket valid, track scope,
  production build, route checks).

## Commands run

`git diff --stat/--name-status origin/main...HEAD`, `git ls-tree`
counts of docket/open and docket/dropped post-* items, a script tallying the
test-1/test-2/consolidation split, URL extraction and diff of the consolidated
item vs its five predecessors, curl (and direct fetch for the three
bot-blocked OpenAI pages) on all 14 URLs, `node scripts/check-docket.mjs`,
`node scripts/round.mjs check`, the build-log Origin parser, and `gh pr view
117` for the merge state.
