Commit: 8afaea1ac5c15c1a06d0cb4976c050544472b8c5
Verdict: approve
Reviewer: claude-sonnet-5 (Claude Code subagent, adversarial reviewer)
Round: 192

# Adversarial review — round 192 (audit)

Base `0d72ab5`, head `8afaea1`, two commits
(`016e6eb` finding + `8afaea1` disclosure-map fix). Diff touches exactly
`CHANGELOG.md`, `app/lib/page-origins.js`, `app/lib/sections.js`,
`docket/open/2026-08-24-the-homepage-sells-the-loop-not-the-site.md`, and a
new `docket/open/2026-08-25-priority-binds-nothing-and-machinery-crowded-out-visitor-work.md`.

## 1. The central claim: `priority` has no code path — VERIFIED, exhaustively

Grepped `priority` (case-insensitive) across the entire repository, then
narrowed by hand:

- `scripts/dispatch.mjs`: zero matches. Confirmed `ready`/`canPick`
  (lines 98, 281-332) operate on **counts per track**
  (`readyCount(track) = ready.filter(item => item.track === track).length`);
  nothing orders or selects an individual item.
- `scripts/round.mjs`: zero matches.
- `scripts/check-docket.mjs` lines 172-174: the *only* code anywhere that
  reads `priority`, and it only validates membership in `["1","2","3"]`.
- `app/`: the only `priority` hits are in `app/sitemap.js` — the unrelated
  XML sitemap-priority field (SEO weighting per route), not the docket
  field. Confirmed by reading the file; it has nothing to do with docket
  items.
- `.github/`, `prompts/`, `policy.yml` (both the root file and `.github/`
  were checked — there is only one, at repo root): zero matches.
- `CHARTER.md`: one mention, in History prose ("Filed at priority 1 for a
  `build` round") — not code.
- `docket/README.md`: documents the frontmatter template
  (`priority: 1 | 2 | 3`) — documentation, not enforcement.
- All other hits are `priority:` frontmatter lines inside individual
  docket items — data, not code that reads it.

The claim holds without qualification: nothing in this repository ranks,
sorts, or selects docket work by `priority`. This is the same shape as the
dispatch-bypass round 185 fixed — a governance signal present in the data,
enforced nowhere. The entry states this as a fact ("priority is validated,
never read") and separately, correctly, states the *causal* link to the
homepage item's neglect as a hypothesis it checked rather than asserted
("either something dispatched around it for a defensible reason every
time, or nothing was ever going to protect it. Checked which."). It does
not overclaim that the missing mechanism *caused* the allocation — it
shows the mechanism doesn't exist and separately, plainly, tallies what
shipped. Test 2 (true, checkable) is satisfied.

## 2. The 1-of-12 blog count — VERIFIED exactly

Read `app/lib/posts.js` in full: 12 posts. Post 1 (`/blog`, "How an AI
builds this site") is about the project. The other 11
(`frontier-cyber`, `claude-code-auto-mode`, `cyber-eval-cascade`,
`gpt-5-6-price-drop`, `fable-5-export-controls`, `chatgpt-ads`,
`gemini-3-7-flash`, `ultrafast-mode`, `ai-security-week`,
`manus-meta-split`, `copilot-consolidation`) are dated, sourced reporting
on vendor events — confirmed by reading each post's title/description.
`app/blog/` has exactly 11 subdirectories plus the root `/blog/page.js`,
matching 12. The count in the entry, the docket item, and the new
`sections.js` comment are all consistent with the source of truth. The
replacement description ("Dated, sourced reporting on what AI vendors
actually did — plus one account of how this site itself is built.") is
accurate for what `/blog` actually contains.

`app/lib/route-files.js` line 15 lists `app/lib/sections.js` as a source
file of `/`, confirming the `PRODUCING_ROUNDS["/"]` move to 192 was
required, not optional. Ran `node scripts/check-ai-disclosure.mjs`
directly against the committed tree: green, `/` now resolves to round 192
correctly, matching every other route. The mechanism the entry describes
(UNVERIFIED on uncommitted diffs, FAIL when a listed source file moved
under a stale map entry, fixed by updating the map with a comment, never
by suppressing the check) matches the script's actual logic
(`check-ai-disclosure.mjs` lines 30-321).

## 3. Rule 12 boundary — held, and correctly reasoned

Rule 12: "No run judges its own output... The run that publishes something
is never the run that audits or retracts it." Round 186 and round 192 are
different runs, so a later audit assessing an earlier audit's work is the
system working as designed, not a violation — that reading would make
sequential audits impossible. The real question is narrower: did round
192 re-judge *whether round 186 was right* to withdraw
`/promise-vs-practice` and `/model-migration-chains`, or the merits of the
homepage finding itself?

It did not. The entry explicitly declines ("not this round's to re-judge
under rule 12... which this round does not re-score") and only
characterizes the withdrawal descriptively (removed vs. added) for its own
separate allocation tally — a factual sort, not a verdict on round 186's
judgement. The one action taken on round 186's open item — ticking the
`/blog`-description checkbox — is remediation of a narrow, independently
falsifiable claim the item itself flagged, not a re-audit of round 186's
reasoning. `docket/` is in `audit`'s own writable scope regardless of an
item's `track:` label (confirmed via `check-track-scope.mjs`); a docket
item's `track:` field routes ownership of the *remaining* work, it isn't
an access lock. The entry is explicit that it left the other four
checklist items (hero reorder, grid addition, `check-routes.sh`
assertion) untouched precisely because those are `build`-sized editorial
calls this round should not make unilaterally. That is a clean,
well-reasoned line, not a rules-lawyered dodge.

## 4. The 184-191 classification — re-derived independently, holds

Pulled `getBuildLog()` from `app/lib/build-log.js` directly:

```
192 audit   191 build   190 scout   189 maintain
188 maintain  187 maintain  186 audit  185 meta  184 build
```

Matches the entry's `- Track:` attributions exactly. Diffed the actual
merge commits for 184, 185, 188, 191 (`6a7efe2`, `d73284b`, `02efa7f`,
`0beb776`): all four touch only `CHANGELOG.md`, `docket/`, `scripts/`, and
(185 only) one file in `prompts/` — nothing in `app/` or `public/`. None
of the four shipped anything a visitor's browser would ever load. Round
187's changes confirmed the OpenAI notice-period quote correction on
`/what-vendors-promise`; round 189's confirmed the retirement-calendar
scope-rule note; round 190's confirmed a docket-item-only diff (nothing
published). Round 186's diff confirmed the two route withdrawals plus the
homepage item filing. The arithmetic (2 visitor-facing + 1 withdrawal +
1 deferred + 4 internal = 8) is correct and reproducible from
`CHANGELOG.md`/`build-log.js` independent of the entry's own prose.

**On dismissing round 185 (dispatcher reconnection):** I looked for
grounds to push back here, since fixing the mechanism that selects all
future work is arguably categorically different from an ordinary internal
fix. I don't think there's a strong dissent available: the entry already
anticipates this exact objection in its "too harsh" paragraph, explicitly
invoking CHARTER.md rule 22 (an unenforced guardrail is a real defect, not
a nicety) and stating plainly that fixing three silent defects in five
rounds "is not obviously excessive on its own." It never says round 185
was unimportant — it says round 185 is not something a first-time visitor
would ever see, which is a factual, charter-grounded axis (test 1), not a
value judgment about the work's importance. The finding is about
allocation across all six machinery rounds happening *while* a
maintainer-flagged priority-1 item sat untouched, not a claim that round
185 shouldn't have been built. I find this treatment fair as written.

## 5. Withdrew nothing — reasonable, though one framing nit found

Checked the survivors (187's correction, 189's scope note) against test 2:
both are corrections/clarifications, not new unverified claims — nothing
in either warrants retraction. The application of rule 20 ("producing
nothing is a valid outcome") to "withdrawing nothing" is an analogical
extension of rule 20's letter (which is literally about an empty
*production* queue) rather than a direct citation — but it sits squarely
inside Section V ("Restraint"), which rules 20-22 as a group are about,
and the entry doesn't misstate what rule 20 says. Non-blocking.

**One real, non-blocking imprecision:** the entry's opening line —
"Eight rounds shipped since the last audit" — is not literally accurate.
Round 186 *is* "the last audit"; only rounds 187-191 (5 rounds) shipped
strictly after it, which matches the entry's own `Dispatch:` field later
in the same entry ("audit due: 5 shipped round(s) since the last audit").
Rounds 184 and 185 shipped *before* round 186, not since it. The 8-round
window (184-191) is internally consistent and correctly enumerated by
number immediately afterward, and the arithmetic and conclusions are
unaffected — but the framing sentence conflates "the last audit round" as
a boundary with "the last eight rounds" as a review window. Worth fixing
in a future pass; does not change any of the round's substantive findings
and does not rise to a false claim about the codebase.

## 6. Filing to `audit` — correct placement, not track-shopping

`node scripts/check-docket.mjs`, run against the working tree: `build`
14/14 (base and head both 14 — genuinely at its `queue_budget` ceiling),
`meta` 26 total against a budget of 14 (25 unblocked + 1
`blocked-on: maintainer`, matching the script's own reconciliation).
Independently confirmed the *base* (`origin/main` = `0d72ab5`) already had
exactly 2 open `track: audit` items before this round
(`2026-08-24-a-log-preset-that-matches-almost-nothing-passes-the-guard.md`,
`2026-08-24-a-route-needs-a-reader-not-a-dataset.md`), and 3 after — the
entry's "audit 2 -> 3" is exactly right, not asserted from the brief.

Round 152's own text (read in full from `build-log.js`) says almost
verbatim what this entry attributes to it: audit/scout/maintain are
deliberately left unbounded, and a round that "deliberately relabels
items" to dodge a full track is a known, named attack the check does not
stop — "This is a decision, not an oversight." The new item's own content
is a re-derivation question for a later audit ("did the balance and the
mechanism correct themselves"), not an implementation task mislabeled to
get around `build`'s ceiling — and `build` and `meta` were verifiably
full, not merely claimed full. This reads as legitimate placement, not
track-shopping.

## 7. Scope — clean

`node scripts/check-track-scope.mjs origin/main
loop/audit/machinery-crowds-out-visitor-value`: all 5 changed files pass,
explicitly listed ok. Confirmed `SCOPES.audit` in
`scripts/check-track-scope.mjs` (lines 50-58) is exactly `app/`,
`public/`, `scripts/`, `package.json`, `package-lock.json`, `docket/`,
`CHANGELOG.md` — nothing in `.github/`, `CHARTER.md`, `policy.yml`,
`lighthouserc*.json`, or `docket/reviews/` is touched by this diff (diff
stat confirms only the 5 files listed above changed).

## 8. Provenance and gates — all green, independently re-run

- `node scripts/check-changelog-provenance.mjs`: ok, all 192 rounds agree
  with `build-log.js`; the new round declares a well-formed `Dispatch:`
  and a registered `Agent:`.
- `node scripts/check-docket.mjs`: ok, gate math as above.
- `node scripts/check-ai-disclosure.mjs`: ok, all routes resolve,
  including `/` at round 192.
- `node scripts/check-frame.mjs`: all 16 checkable `FRAME.md` facts still
  verified, 2 attested as before — this round's diff doesn't touch
  anything load-bearing there.
- `node scripts/round.mjs check` (full pipeline: lint, docket, track
  scope, production build, all route checks): **ran it myself, foreground,
  one pass** — clean throughout, matching the entry's own three-run
  account (one UNVERIFIED, one real FAIL fixed at source, one clean).
- `CHANGELOG.md` diff is purely additive — the new `### 2026-08-25`
  section is inserted before the existing `### 2026-08-24` entries with
  zero lines removed from prior content (rule 5).
- `Dispatch: dispatcher — audit due: 5 shipped round(s) since the last
  audit (max 5)` and `Agent: claude-sonnet-5` are both present in the new
  entry, exactly as `check-changelog-provenance.mjs` requires.

## Verdict

Approve. The round's central claim (`priority` has no mechanical effect
anywhere in this repository) is verified exhaustively, not just in the
three files it names. The 1-of-12 blog count is exact. The
`PRODUCING_ROUNDS["/"]` fix is a real, source-verified correction to a
real gate failure, not a workaround. The rule 12 boundary against round
186 held — the entry judges allocation, not round 186's own withdrawal
decisions, and the one edit made to round 186's item is ordinary
remediation within audit's own scope. The 184-191 classification
reproduces exactly from `CHANGELOG.md`/`build-log.js`, and the treatment
of round 185 as important-but-not-visitor-facing is fair and
self-aware rather than dismissive. Filing to `audit` is correct
placement given verified capacity at both bounded tracks, not
track-shopping. Scope, provenance, and all gates are clean, independently
re-run.

One non-blocking finding: the entry's opening "Eight rounds shipped since
the last audit" is imprecise — literally only 5 rounds (187-191) shipped
after round 186, which the entry's own `Dispatch:` line elsewhere gets
right. The 8-round enumeration itself, and everything built on it, is
correct; only the framing sentence conflates the audit-window boundary
with the review window. Worth a one-line fix in a future round; not
disqualifying here.
