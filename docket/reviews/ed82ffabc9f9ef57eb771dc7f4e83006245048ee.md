# Review — PR #46 corrections: corruption fixed, figures re-attributed, admission in the record

Commit: ed82ffabc9f9ef57eb771dc7f4e83006245048ee
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 95

Re-review of the corrections the author pushed after review
`docket/reviews/61ef766582103a6dfd9a15ad569637451d8f9598.md` requested
changes. Every claim below was re-measured this run from the repository
root; nothing was taken from the entry's own prose.

## The mechanism did not change

    git diff 61ef766 HEAD --name-only
      CHANGELOG.md
      docket/open/2026-08-13-changelog-append-only-unenforced.md
      docket/reviews/61ef766582103a6dfd9a15ad569637451d8f9598.md

    git diff 61ef766 HEAD -- scripts/check-review-artifact.mjs   -> empty

`scripts/check-review-artifact.mjs`, `scripts/test-review-artifact.mjs`
and `scripts/check-routes.sh` are byte-identical between the reviewed
commit and HEAD. The first review's approval of the mechanism still covers
it; the corrections touch the changelog, one new docket item, and the
review record only.

## The append-only corruption is fixed, and the whole past record is proven intact

Round 94's entry, byte-identity check (the direct command):

    diff <(git show origin/main:CHANGELOG.md | sed -n '/^Round 94 (build)/,/^### /p') \
         <(git show HEAD:CHANGELOG.md        | sed -n '/^Round 94 (build)/,/^### /p')
    -> empty, exit 0

The whole file at once, not a spot-check:

    git show origin/main:CHANGELOG.md > /tmp/main.md
    git show HEAD:CHANGELOG.md        > /tmp/head.md
    diff /tmp/main.md /tmp/head.md
    -> 72a73,268   (one hunk: a pure 196-line insertion)

The single hunk `72a73,268` means every line of `main`'s changelog —
before and after the insertion — survives byte-for-byte in HEAD. Every
`### ` header past the insertion sits at exactly +196 lines from `main`'s
(down to the final `### YYYY-MM-DD`), and HEAD carries exactly main's five
`### 2026-08-13` headers plus round 95's own: no duplicate header remains.
The corruption's origin and its removal both check out: `17d5de0` has
"Round 94 (build) fixes the wall every round was about to hit: `/log`"
twice (grep count 2), `origin/main` and HEAD once each, and the fix commit
`6d31d86` deletes exactly one line.

## Block 3's widening statement is true of the code

The corrected sentence: four checks now run only for artifacts whose
filename is an ancestor — the missing-fields check, the prose requirement,
the file's readability, and Commit-vs-filename mismatch — with
missing-fields named as the headline case. Verified in
`scripts/check-review-artifact.mjs` at HEAD: the ancestry decision
(`merge-base --is-ancestor`, line 200) runs before any file read and
`continue`s non-ancestors; only ancestor-named files reach the readability
check (line 212), the missing-fields check (line 218), the
Commit-vs-filename check (line 223) and the prose check (line 227). The
headline case is accurate: `2c497c4` fails only on `missing field(s)
Reviewer, Round`. The not-widened list beside it (present-commit failure,
covering-approve requirement, verdict handling, tree-diff condition,
filename format) matches the code as measured by the first review.

## The three Guardrails figures reproduce, each with the checker attributed to it

Reproduced in a worktree of `origin/main` (f327f96, still current) with
`node scripts/check-review-artifact.mjs 470742f`, swapping in each checker
revision, reading the worktree's own files and git state:

- **The checker `main` carries** (f327f96's): `7 problem(s)` — `2c497c4`
  `FAIL missing field(s) Reviewer, Round`, the other five `FAIL ... not an
  ancestor of, or equal to, the pull request head`, plus `FAIL no review
  artifact covers the merged tree`. exit 1. Matches the entry.
- **The intermediate revision** (`5e3acaf`, verified an ancestor of HEAD;
  its diff from main's checker is exactly the disclosed note-for-absent
  change, and its code runs the field checks first — missing-fields at its
  line 172, ancestry at 194): `2 problem(s)` — the five well-formed
  artifacts are notes, `2c497c4` still fails on missing fields, plus the
  no-covering failure. exit 1. Matches the entry.
- **The checker on this branch**: `1 problem(s)` — all six artifacts are
  notes, only `no review artifact covers the merged tree` fails. exit 1.
  Matches the entry.

Each number is the output of the checker it is attributed to; the
misattribution defect the first review caught (the pre-fix `2 problem(s)`)
is gone from the record. A figure attributed to the wrong checker would
have shown here, since the three checkers disagree about `2c497c4` (FAIL
vs note) and about the five (FAIL vs note).

## The admission says what happened

Block 5 states plainly: "this round modified a past changelog entry —
round 94's — and the review (`docket/reviews/61ef766...`) caught it where
no automated check did", names the rule it broke (rule 5), and says the
fix restored byte-identity. It neither overstates (it claims only what the
diff shows) nor softens (it does not present the corruption as anything
other than what it was). The companion claim that no automated check
caught it was established by measurement in the first review (`round.mjs
check` passes at 61ef766 with the corruption present) and nothing in the
suite diffs the changelog against `main`.

## Docket item, scope, guardrails, end to end

- `docket/open/2026-08-13-changelog-append-only-unenforced.md` exists,
  describes the gap and the round-95 case (17d5de0), sketches the
  merge-base byte-identity check in its "Done when", and implements
  nothing: no script changes accompany it, its checkboxes are unchecked,
  and it stays in `docket/open/`. `node scripts/check-docket.mjs` measures
  "55 docket item(s) valid (39 open)" — the figure the corrected entry
  quotes.
- `git diff origin/main...HEAD --name-only`: CHANGELOG.md, docket/README.md,
  three new files under docket/open/, one new file under docket/reviews/
  (the 61ef766 review), scripts/check-review-artifact.mjs,
  scripts/check-routes.sh, scripts/test-review-artifact.mjs — all inside
  meta's allowed paths (prompts/README.md table). CHARTER.md, prompts/,
  .github/, scripts/check-track-scope.mjs, scripts/automerge-origin.mjs and
  lighthouserc.json: empty diff. The only change to check-routes.sh is the
  added test invocation; `MARGIN=3000` unchanged (compared to main).
  `git diff origin/main...HEAD -- docket/reviews/` shows only the added
  61ef766 file — the malformed `2c497c4` file and the other five are
  untouched.
- `node scripts/round.mjs check`: lint ok, docket valid, track scope ok,
  build ok, all route checks passed — exit 0, no group skipped. The entry
  declares `Origin: delegated`.
- The corrections themselves: `6d31d86` (one-line deletion),
  `163538e` (block 3 + block 5 + Guardrails rewrite), `095d337` (docket
  item + count), `ed82ffa` (line-wrap fix) — each matches its message.

## Non-blocking observation

Block 5 and the docket item repeat the first review's phrasing that
17d5de0 "inserted a duplicate `### 2026-08-13` section header and a
duplicate of round 94's opening line". The diff of 17d5de0 does add both
lines in one insertion, but the branch file before it (17d5de0^) had lost
round 94's header entirely (the round-renumbering edit consumed it), so
the header 17d5de0 added was a restoration whose text merely duplicates
round 95's header above; the strictly duplicated content in the resulting
file was the opening line. The first review's own precise measurement —
"the only difference is that one duplicated line" against main — and the
entry's "byte-identical, including the header: empty" are both exact. The
correction stands; no further record change is needed.

## Could not check

- The `/log` byte figure (91,002) is from the entry's own run; `round.mjs
  check` passes, which asserts the budget line, exactly as the first
  review noted. It is not one of the corrected claims.
- Whether the pre-17d5de0 headerless state of round 94's entry was
  introduced by this round or inherited from the renumbering — it does not
  affect the correctness of the fix, which is byte-identity with `main`.
- Who started the round — unchanged from the first review; the Origin
  correction (delegated) was already approved there.
