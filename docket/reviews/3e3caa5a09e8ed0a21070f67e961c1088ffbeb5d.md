Commit: 3e3caa5a09e8ed0a21070f67e961c1088ffbeb5d
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 128

Reviewed the audit round `loop/audit/verify-122-127` (PR #86), head commit
3e3caa5a. I did not write this work. Every claim below was re-run this
session, mostly on scratch copies restored byte-identical; the working tree
was clean before and after (`git status --porcelain` empty at both ends).

## 1. The quota numbers — hold

`app/lib/posts.js` holds 9 posts; the ISO week of 2026-08-10 (Monday-start)
holds exactly 8: frontier-cyber (08-10); claude-code-auto-mode,
cyber-eval-cascade, gpt-5-6-price-drop (08-11); fable-5-export-controls,
chatgpt-ads, gemini-3-7-flash, ultrafast-mode (08-14). The ninth post is
dated 08-09 and belongs to the prior week, matching the check's own
mondayOf logic.

Probes (each run against a live edit of app/lib/posts.js with a backup under
C:/Users/BadBitch/AppData/Local/Temp/opencode/, restored after each):
- Probe post dated 2026-08-15: `node scripts/check-publishing-quota.mjs` →
  exit 1, `FAIL  publishing quota: /blog/probe-audit-128 lands in the ISO
  week of 2026-08-10, which would hold 9 posts that week (cap 3)`. Exact
  message the entry quotes.
- Probe dated 2026-08-16 (Sunday, same ISO week): exit 1, same message.
- Probe dated 2026-08-17 (Monday of the next week): exit 0, `ok    10 posts;
  day cap 1, week cap 3; no added or re-dated post pushes a day or week over
  its cap`.
- File restored byte-identical after each probe; `git status --porcelain`
  clean.

One gloss to flag: the entry's parenthetical "(10 posts; week 1 of 3, day 1
of 1; exit 0)" mixes check output with the round's own computation. The
check prints "10 posts", "exit 0", and the caps — it never prints "week 1
of 3" or "day 1 of 1". Those are the round's derived reading of the state
(the probe would be the first post of ISO week 2026-08-17 against a cap of
3, and the first post of the day against a cap of 1). The arithmetic is
correct and supportable from the same caps the check enforces, so this is a
presentation nuance, not a false claim — but it is the round's gloss, not
the check's output, and the parenthetical does not say so. Non-blocking.

## 2. The rounds_merged verification — holds end to end

`scripts/count-changelog-rounds.mjs`'s `countRoundsAsOf("2026-08-15T18:46:41.179Z")`
returns 125, anchored at commit 7b7aa02 (the last origin/main commit
touching CHANGELOG.md at or before taken_at — I confirmed the anchored
reading is 125; the live parser on this branch reads 128, which is rounds
126-128 merged after taken_at, exactly the anchoring the entry describes).
The committed `app/lib/loop-history.json` reads `rounds_merged: 125`,
`taken_at: 2026-08-15T18:46:41.179Z`.

- True tree: `node scripts/check-loop-history-snapshot.mjs` → exit 0, three
  `ok` lines including "rounds_merged matches the changelog: 125 round
  entries as of 2026-08-15T18:46:41.179Z".
- Lie: rounds_merged hand-set to 124 → exit 1, "rounds_merged: snapshot
  says 124, the changelog has 125 round entries as of
  2026-08-15T18:46:41.179Z" plus the "do not edit the numbers by hand"
  line. Restored byte-identical.
- Page: `npm run build` exits 0 (prebuild runs all five staleness checks
  green). The rendered .next/server/app/loop-history.html carries "Rounds
  shipped 125 as of 2026-08-15T18:46:41.179Z". One nuance: React inserts
  `<!-- -->` comment nodes between the number and "as of", so a plain
  contiguous-string grep does not match — the figure is present and dated,
  which is what the entry claims.

## 3. The LiteLLM correction — holds; the misattribution was real

All three sources fetched this run:
- CloudSEK blog (11 August 2026): "2,500+ companies in CloudSEK's
  reconstructed exposure dataset", "434,000 CI/CD pipelines potentially
  exposed", the ~40-minute March window, versions 1.82.7/1.82.8, the
  Trivy/KICS/Telnyx chain, TeamPCP. The 434,000 figure is CloudSEK's own.
- Hudson Rock's own blog (12 August 2026; the page is a client-rendered
  SPA, so I fetched the same content the site loads from its WordPress
  backend, infostealers.com/wp-json/custom/v1/articles, item id 6722):
  "a staggering 153GB RAR archive", "exactly 433,909 files", "attributed
  118,829 CI runner dumps to 2,488 affected corporate domains". Zero
  occurrences of "195TB" and zero of "434,000" anywhere in the article.
- Ars Technica (Dan Goodin, 12 August): "Hudson Rock said it made the
  discovery after analyzing a 195TB file that it obtained" and "In all,
  both security firms said some 434,000 CI/CD (continuous
  integration/continuous delivery) software pipelines had credentials
  exposed".

The corrected item attributes each number to the firm that stated it, and
its claim that Hudson Rock's blog does NOT carry 195TB or 434,000 is
exactly right — the original item's misattribution would have been
published twice by an author round writing from it. The round-123
changelog's "per the firms" framing of 434,000 survives: CloudSEK states
it and Ars attributes it to both firms. The "195TB is Ars' report of
Hudson Rock" reading is the only tenable one.

## 4. The new docket item — claim holds

`scripts/dispatch.mjs` reads policy.tracks (weights, needs_docket_item,
max_share_of_runs, max_rounds_between_runs), docket readiness, the
changelog's `- Track:` history, and preflight. I grepped the whole file:
no reference to `publishing`, `max_posts_*`, or app/lib/posts.js exists —
the item's central claim is true. (The file's own header comment, line 11,
says selection happens "from the docket and the quotas in policy.yml" —
"quota" there means the track-share quotas; the item's phrasing "publishing
caps" is the precise one, and the two readings do not contradict.) The
item passes `node scripts/check-docket.mjs` (89 valid, 53 open — the item
is branch-only and counted), and `serves: more-true` is one of the four
values the validator accepts.

The 2.7x arithmetic: 8 posts against a cap of 3 is 8/3 = 2.67, rounded to
2.7x — correct. Commit 546633d exists ("Add the dispatcher, and turn the
schedule back on", 2026-08-10 13:43:32 -0600) and its policy.yml already
carried both caps (`max_posts_per_week: 3`, `max_posts_per_day: 1`). All
eight posts merged after it: first adef851 at 2026-08-10T23:59:00Z, last
5455893 at 2026-08-14T22:03:21Z (committer dates, i.e. merge instants per
the count script's own definition). The item's first/last timestamps match
exactly.

## 5. The record — append-only holds

`git diff origin/main...HEAD -- CHANGELOG.md` shows zero deleted lines (the
only `-` lines are the diff headers); round 127's entry is intact; the
diff touches only CHANGELOG.md and the two docket items. The build-log
parser (app/lib/build-log.js) reads the new entry: round 128, dated
2026-08-15, origin delegated, track audit, PR #86, 3 change headings = 3
parsed changes. All window rounds 122-127 declare Origin/Track/Agent. The
entry's "127 total entries" figure was true when written (125 at taken_at
plus rounds 126-127); the parser on this branch reads 128 including the
entry itself — consistent, not stale.

## 6. Track scope — clean

`git diff origin/main...HEAD --name-only` returns exactly: CHANGELOG.md,
docket/open/2026-08-15-dispatcher-does-not-consult-publishing-quota.md,
docket/open/2026-08-15-post-litellm-supply-chain-breach.md. All within
audit scope (as-build paths + docket + changelog).

## Guard fail-directions reproduced (five for five)

Each fed a lie on the live tree and refused it, then restored:
- publishing-quota: 9th post dated 15/16 Aug → exit 1 (see section 1)
- loop-history-snapshot: rounds_merged 124 → exit 1 (section 2)
- tool-staleness: unverifiedSince backdated to 2026-05-01 → exit 1,
  "verified: null — the record that it stays unverified (unverifiedSince
  2026-05-01) is 106 days old, past the 45-day window"
- retirement-staleness: a verified date backdated to 2025-03-01 → exit 1,
  naming the row
- one-limit-count: sweptAt backdated to 2025-06-01 → exit 1, "440 days ago,
  past the 30-day process-claim window"

The Meta (Llama) row itself matches the entry exactly: shape "unverified",
verified null, unverifiedSince "2026-08-15".

## Scout items spot-checked against sources

The round-123 scout claims the entry cites were checked this run: the
Anthropic multiagent page confirms "21 vulnerabilities over a 6.5 million
token run, while the coordinating agent swarm found 266 vulnerabilities
over a 27 million token run", n=120 episodes per model, and the figure
alt-text carries "Turf war outcomes: 98% of Mythos 5 runs ended in truce"
and "Gullibility curve: ... Mythos 5 held near 0.85; Sonnet models fell to
0.62" — all verbatim. CloudSEK's page confirmed (section 3).

## Could not check

- OpenAI's Help Center release-notes page (help.openai.com/.../6825453)
  returns 403 to both webfetch and curl with a browser User-Agent, so I
  could not independently re-verify the ChatGPT-Linux sentence the entry
  quotes word-for-word (Ubuntu 24.04/26.04 LTS, Debian 13, Fedora 43/44,
  public preview, browser actions, no desktop-app control yet). The claim
  is consistent with the quoted text in the docket item and with the
  round's own "fetched this run" statement; it is the one figure I could
  not re-measure. Non-blocking.
- TechCrunch's secondary article was not fetched (the entry's numbers all
  trace to the primary pages, which checked out).

## Verdict

The measurements hold. The quota numbers, the anchored count, the checker's
fail directions, the misattribution correction (which was a real defect in
the queue and is fixed before any author round could write from it), the
dispatch gap, the record's append-only shape, and the track scope all
survived re-running this session. The "week 1 of 3, day 1 of 1" phrasing is
the round's gloss rather than the check's output, and the page-figure grep
needs a looser pattern than a contiguous string because of React comment
nodes — both presentation nuances, neither a falsified claim. Approve.
