# Review: PR #64 re-review — the corrected Ultrafast post

Commit: 403fc7e7e88de5127145a85f9a17d1ed3b709f87
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 108

Re-review of the correction to the round-108 Ultrafast post, following the
request-changes review of `15aefe6ceb3c08114e76ef3dc8b8f5a2c83055e9`. The
blocking defect was a sentence in the post's own voice claiming OpenAI
"already sells a slower 'Fast mode' for Sol at a premium over Standard" —
not on the page fetched this round, contradicting the post's sourcing line,
and repeating the price-drop post's content. The correction commit claims to
have dropped that sentence and only that, and asserts the changelog entry's
claims now hold without editing the entry.

The correction is exactly as claimed. The sentence is gone, nothing else
changed, the post's sourcing line now holds of every figure it states, both
changelog claims hold of the shipped text, the checks pass on this head, and
the round numbering holds. Approve.

## 1. The fix is the only change since the reviewed commit

`git fetch origin && git rev-parse origin/loop/author/post-ultrafast-mode`
-> `403fc7e7e88de5127145a85f9a17d1ed3b709f87` — matches the brief's expected
head. `git log 15aefe6..403fc7e`: exactly two commits — the review commit
`08905dc` and the correction `403fc7e`.

- `git diff --stat 15aefe6 08905dc` -> only
  `docket/reviews/15aefe6ceb3c08114e76ef3dc8b8f5a2c83055e9.md` (the
  request-changes artifact, 176 insertions). Nothing else.
- `git show 403fc7e --stat` -> only `app/blog/ultrafast-mode/page.js`,
  2 insertions / 3 deletions.
- `git show 403fc7e -- app/blog/ultrafast-mode/page.js` -> the diff removes
  exactly "the company already sells a slower &ldquo;Fast mode&rdquo; for Sol
  at a premium over Standard," and the sentence's grammatical connector
  "and a reader who has seen the price-drop post knows this family's price
  moves fast" is reflowed into the preceding sentence. Nothing else in the
  file changed — no other post text, no route files, no docket changes, no
  changelog changes.

## 2. The claim is gone

Grep of the corrected file (`grep -inEo 'fast mode|premium|per token|2\.5|twice|[0-9]+\.[0-9]+|cost'`):
- "Fast mode" appears nowhere except inside "Ultrafast mode" (2 hits: the
  quoted "GPT-5.6 Sol on Ultrafast mode" line and the source title). A
  rendered-page grep for "Fast mode" not followed by "m" finds nothing.
- "premium": 0 hits. "per token": 0. "2.5": 0. "twice": 0.
- "$": 1 hit in the file — the JS template literal `` `${getSiteUrl()}${post.path}` `` in the JSON-LD block, not price content.
- "cost" appears twice, both words rather than figures: "what Ultrafast
  costs" (the post's statement that the page says nothing about price) and
  "what the family costs" (describing the site's own price-drop post as the
  price axis). Neither asserts a price.
- The paragraph reads sensibly and its claim still holds:
  "One thing the page does not say: what Ultrafast costs. ... There is no
  price, no per-token rate, and no billing detail anywhere in the
  announcement". Verified against the fetched page below.

## 3. The sourcing line now holds

The post's line "Every figure in this post is OpenAI's own, read off that
page, which was fetched for this post" was checked against a fresh fetch.

Fetch notes: the first node https request with a Chrome user-agent returned
Cloudflare's 403 challenge (as the round's Guardrails paragraph records as
one of its outcomes). A node https request with a Safari user-agent returned
HTTP 200 and 380,027 bytes of the real page; `<title>` =
"Previewing Ultrafast mode: GPT-5.6 Sol at up to 14X the speed | OpenAI";
article text extracted and read in full. (This differs from the prior
review, where Chrome-UA succeeded; Cloudflare challenges are
request-specific.)

Every remaining figure and quoted claim verifies against that page:
- "13 August 2026" — the page is dated "August 13, 2026".
- "up to 14× faster than Standard processing" — verbatim on the page.
- "up to 750 output tokens per second" — verbatim on the page.
- "Powered by Cerebras" — verbatim on the page.
- "available in a limited preview today to a select group of customers"
  and "expand access as capacity grows" — verbatim on the page.
- "Ultrafast marks the next step in our partnership with Cerebras" —
  verbatim in the "Powered by Cerebras" section.
- "Until now, getting real-time speed typically meant choosing a smaller or
  more specialized model." and "Ultrafast points to progress in a new
  direction: more useful work per second." — both verbatim.
- The four customer quotes and their attributions (John Crepezzi — AI
  Assistants, Jane Street; Courtland Lykins — Product Lead—Voice AI,
  Podium; Mitch Troyanovsky — Co-Founder, Basis; Alex Wang — Applied AI,
  Rogo) — all verbatim.
- The five use-case scenarios and the internal-use paragraph — faithful to
  the page.

Price check on the fetched page: the article body contains no price, rate,
billing, or "$" amount for Ultrafast. The word "price" appears on the page
only as the anchor "more affordable" linking to OpenAI's own GPT-5.6
announcement, and the nav link "Pricing". The post's "the announcement
states no price" claim holds, and the corrected post publishes no price.

The one number not on the fetched page is "30 July" inside the anchor text
of the link to the site's own price-drop post ("the 30 July price cuts that
made Luna the free default"). This is the site's own coverage, explicitly
attributed ("That post is the price axis — what the family costs"), scoped
by the post's footnote ("its claims about the world are sourced to the
announcements it fetched on 2026-08-11, and are not relied on for any figure
here"), and was present in the originally reviewed text. See non-blocking
concerns.

## 4. The changelog entry claims hold of the shipped text

- "links the site's GPT-5.6 price-drop post as the price axis of the same
  story without repeating it" — holds. The corrected post links the
  price-drop post and no longer repeats its content; the Fast-mode premium
  sentence, which was the repetition, is gone.
- "The post contains no number that was not read off the fetched page this
  round" — holds of every figure in the post. The numbers are: 13 August
  2026, 14×, 750 (all on the fetched page) and "30 July" in the link anchor
  (the site's own coverage, per above). The entry's Result paragraph scopes
  the claim to the post's figures, and every figure traces to the page.

## 5. Checks pass on this head

`node scripts/round.mjs check` -> exit 0: lint ok, docket valid, track scope
for `loop/author/post-ultrafast-mode` ok, build ok, "all route checks
passed". Grep of the output for skip/fail/error is empty — no group was
SKIPPED. Against a server started on port 3000 (killed afterward by PID;
ports 3000 and 8101 verified free before and after): `/blog/ultrafast-mode`
-> 200, contains the post title, "Up to 14", "750", and no residual Fast-mode
premium content; `/blog` lists the post (3 "ultrafast-mode" matches);
`/sitemap.xml` contains it (1 match); `/feed.xml` contains it (3 matches).

## 6. Round numbering holds

`git show 403fc7e:CHANGELOG.md | grep -c '^### '` -> 109 (main: 108),
matching the brief's expectation of 108 on main plus this round's entry.
The round-108 entry carries `Origin: delegated`, `Track: author`,
`Agent: codex`, and "(PR #64)".

## Non-blocking concerns

- The "30 July price cuts that made Luna the free default" anchor text is a
  number/date in the post not read off the fetched page this round; read as
  a universal, the entry's "no number that was not read off the fetched page
  this round" is overbroad, since the price-drop post is the source of that
  date. The post attributes it to its own price-drop post and the footnote
  disclaims reliance on that post "for any figure here", and the prior
  review accepted this framing; I do not treat it as blocking. An author who
  wants the universal to survive a strict reading could qualify either the
  anchor or the entry's claim.
- The post's phrase "sign up to be notified when access expands" paraphrases
  the page's "sign up to get notified when access expands"; the paraphrase
  is outside quotation marks and is accurate.
- The word "cost" remains twice in the post's own voice; both are statements
  about the announcement's silence on price and about the price-drop post's
  role, not price figures.

## What I could not check

- `Agent: codex` — no independent way to verify which model wrote the work
  (same as the prior review).
- The prior review's report that a node https request with a Chrome
  user-agent got HTTP 200; this session's Chrome-UA request got Cloudflare's
  403. Cloudflare challenges are request-specific, so both observations can
  be true; the round's Guardrails record (curl 403, node https 403, page
  fetch tool 200) is consistent with what I reproduced for curl/Chrome-UA
  and with the round's own tool outcome.

## Evidence (commands and fetches, 2026-08-14)

- `git fetch origin`; `git rev-parse origin/loop/author/post-ultrafast-mode`
- `git log 15aefe6..403fc7e`; `git diff --stat 15aefe6 08905dc`;
  `git show 403fc7e --stat`; `git show 403fc7e -- app/blog/ultrafast-mode/page.js`
- `git show 403fc7e:docket/reviews/15aefe6ceb3c08114e76ef3dc8b8f5a2c83055e9.md`
  (the request-changes artifact, read in full)
- node https GET of https://openai.com/index/previewing-ultrafast/ with
  Chrome UA -> HTTP 403, 10,038 bytes (Cloudflare challenge); with Safari UA
  -> HTTP 200, 380,027 bytes, saved; `<title>` and article text extracted
  and read; grep of raw HTML for "Fast mode", "premium", "billing",
  "per-token", "pricing", "price", "$" (contexts checked: the "premium"
  hits are the footer's unrelated "Premium seats are coming to ChatGPT
  Business" card; "$" hits are React JSON markers like `$undefined`, `$L95`)
- `grep -inEo 'fast mode|premium|per token|2\.5|twice|[0-9]+\.[0-9]+|cost'`
  on the corrected post; `grep -n '\$'` on the corrected post
- `node scripts/round.mjs check` (exit 0, no group skipped) — run twice
- `npm run start` on port 3000 (backgrounded, output redirected); curl of
  `/blog/ultrafast-mode` (200, content present), `/blog` (listed),
  `/sitemap.xml` (1 match), `/feed.xml` (3 matches); server killed by PID,
  ports 3000 and 8101 verified free
- `git show 403fc7e:CHANGELOG.md` and `origin/main:CHANGELOG.md`:
  `^### ` counts 109 / 108; round-108 entry fields and "(PR #64)"
