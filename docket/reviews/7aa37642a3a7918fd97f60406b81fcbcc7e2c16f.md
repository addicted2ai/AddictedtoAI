Commit: 7aa37642a3a7918fd97f60406b81fcbcc7e2c16f
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 133

Merged-tree re-review of round 133 (maintain), branch
`loop/maintain/staleness-report-sweep`, PR #92, head
`7aa37642a3a7918fd97f60406b81fcbcc7e2c16f` — the merge of origin/main at
`41809ea29b4c1d69bcd6a5ff5eaccaed4b9de534` (PR #90) into the round's branch.
The round's own work (f4155ce) was already reviewed and approved; this review
checks that its claims hold on the tree CI will actually compute, and covers
that tree with an artifact naming its head.

## Environment and identity

`git status` clean before and after; no other live session at
127.0.0.1:4097 (100 sessions listed, `time.updated` stable across two
snapshots 4s apart; the only session touching now is this one). After
`git fetch origin`, `git rev-parse HEAD` ==
`7aa37642a3a7918fd97f60406b81fcbcc7e2c16f`, matching the brief, and
`git rev-parse origin/main` == `41809ea29b4c1d69bcd6a5ff5eaccaed4b9de534`.
`git merge-base --is-ancestor origin/main HEAD` exits 0 — the branch
contains origin/main, so the merged tree is exactly HEAD.

`git diff f4155ce03b32134ddcd6c1f04fd55d9773793515..HEAD --stat` shows only
PR #90's two files (scripts/count-changelog-rounds.mjs, the new docket item)
and the round-133 review artifact; `git log --oneline f4155ce..HEAD` is the
merge, the review commit, and PR #90's commit. The round's four files
(CHANGELOG.md, app/lib/page-origins.js, app/lib/posts.js,
scripts/staleness-report.mjs) are byte-identical since f4155ce (empty diff),
so every exit-code measurement the prior review took by backdating and
restoring still applies; this review relied on that identity and states so.

## Claims, re-run on the merged tree

- `node scripts/staleness-report.mjs` exits 0 and prints exactly
  "128 published artefacts judged: 127 within window, 1 recorded-unverified
  within window, 0 stale or missing a date"; the one recorded-unverified is
  Meta/Llama with `unverifiedSince 2026-08-15`, inside its 45-day window,
  and the standing WARN about the missing
  `staleness_days.retirement_calendar` key prints.
- `node scripts/staleness-report.mjs --json` exits 0 with pure JSON on
  stdout — `ok: true`, `artefacts: 128`, `stale: []` — and the WARNs on
  stderr only.
- `node scripts/check-one-limit-count.mjs` exits 0: count 8, 8 set members,
  swept 2026-08-15T09:20:06.810Z, 0 days old, within the 30-day window.
- `node scripts/check-loop-history-snapshot.mjs` exits 0: snapshot
  well-formed and within window, matches the live API, and
  "rounds_merged matches the changelog: 125 round entries" — this now runs
  through PR #90's guarded count-changelog-rounds.mjs and passes on the
  merged tree.
- The changelog record: 133 dated `###` round headers at HEAD, counted
  from the file with the same split the build-log parser uses — the
  entry's "round 133" and the prior review's count-133 claim still hold.
- page-origins: `git diff origin/main..HEAD -- app/lib/page-origins.js`
  contains exactly ten `: 133` lines — /, /blog, /blog/frontier-cyber,
  /blog/claude-code-auto-mode, /blog/cyber-eval-cascade, /blog/gpt-5-6-price-
  drop, /blog/fable-5-export-controls, /blog/chatgpt-ads, /blog/gemini-3-7-
  flash, /blog/ultrafast-mode — nothing else changed.

## PR #90 does not disturb round 133

Read, not assumed: scripts/count-changelog-rounds.mjs defines "a round" for
the loop-history count and anchors it at origin/main with a HEAD fallback
for single-branch checkouts; it touches no file round 133 claims about, and
the count it produces (125, via check-loop-history-snapshot) is unchanged.
The docket item (track: build) records the prebuild git-history class and
its repro; it changes no code and no claim round 133 depends on. Neither
file overlaps the staleness report, the one-limit sweep, page origins, or
the changelog record.

## The artifact chain

The prior artifact names f4155ce and does not cover the merged tree.
`node scripts/check-review-artifact.mjs origin/main` on the pre-artifact
head printed, for docket/reviews/f4155ce03b32134ddcd6c1f04fd55d9773793515.md,
"does not cover the merged tree (2 file(s) changed after it)" as a note, and
failed: "FAIL  no review artifact covers the merged tree", exit 1. After
writing this artifact (naming 7aa37642a3a7918fd97f60406b81fcbcc7e2c16f) the
same command must pass; that is the check CI runs.

## Verdict

Approve. The merged tree (branch + origin/main at 41809ea) satisfies every
round-133 claim, re-measured from commands this run; the round's code is
unchanged since the approved f4155ce; PR #90's files do not intersect the
claims; and this artifact covers the tree that will merge.
