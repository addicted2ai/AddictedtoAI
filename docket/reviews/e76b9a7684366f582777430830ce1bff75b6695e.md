Commit: e76b9a7684366f582777430830ce1bff75b6695e
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 139

Re-review of round 139 (scout), branch `loop/scout/round-139-outward-survey`,
at the correction commit e76b9a7. The first review (artifact
docket/reviews/49b580a132c45cdd9e55017ac061423b1c4b647d.md) returned
request-changes on one blocking finding: the NVIDIA item misattributed the
sentence "The more than $500 billion figure represents aggregate third-party
capital ... not NVIDIA revenue, a single fund or a commitment to a single
customer" to the NVIDIA Newsroom press release when it is in Jensen Huang's
blog, and the release's Evidence bullet listed the "not NVIDIA revenue / not a
single fund / not a commitment" clarifications among the release's contents.
This session verified that fix and only that fix.

## What I ran and what it showed

1. **Correction scope.** `git diff aa7371b..e76b9a7 --stat` → exactly one
   file: `docket/open/2026-08-16-post-nvidia-compute-financing.md` (12
   insertions, 10 deletions). The diff touches two regions: the "What the
   press release says it is not" bullet in "Why now", and the Evidence
   bullets for the Newsroom release and Huang's blog. Nothing else in the
   repository changed.

2. **Source fetches (this run).** `curl`ed both cited URLs to
   /tmp/opencode/. The Newsroom release (125 KB) and Huang's blog (114 KB)
   both fetched complete. Searches over the fetched release text:
   "aggregate" → 0 hits; "not nvidia revenue" → 0 hits; "single fund" → 0
   hits. Searches over the fetched blog text: the disputed sentence appears
   verbatim, including the em-dash and "the capital is not NVIDIA revenue, a
   single fund or a commitment to a single customer"; "aggregate", "not
   NVIDIA revenue" and "single fund" each appear once (the sentence itself).
   The sentence is in the blog and is NOT in the release, confirming the
   first review's finding.

3. **The fix matches the finding.** In "Why now", the sentence now sits in a
   new bullet headed "What the $500 billion figure is not" and is introduced
   as "Huang's post states it plainly". The "What the press release says it
   is not" bullet now contains only the subject-to-final-agreements sentence,
   which the release does contain (verified: 1 hit this run). In Evidence,
   the Newsroom bullet no longer lists the "not NVIDIA revenue / not a
   single fund / not a commitment" clarifications; they moved to Huang's
   blog bullet. What remains attributed to the release is still true of it:
   the six institutions (Apollo, BlackRock, Blackstone, Brookfield, Goldman
   Sachs, KKR all present), "mobilize over $500 billion" (8 hits), the
   "investable asset" framing (2 hits), and "subject to execution of the
   final agreements" (1 hit). What remains attributed to the blog is also in
   it: the up-to-25% residual-value support sentence (1 hit), "We are
   bringing independent, long-term institutional capital into the AI
   infrastructure market" (1 hit), the A100 six-year claim (1 hit), the H100
   rental-price sentence (1 hit), the on-demand median sentence (1 hit), and
   the B200 rate range (1 hit).

4. **Untouched fields.** The item's track (author), priority (2), title,
   created/expires dates, and the "Done when" checklist are identical to the
   pre-correction state — the diff shows no edits to them, and the full
   file read confirms the six checklist items stand as before.

5. **Changelog untouched.** `git diff aa7371b e76b9a7 -- CHANGELOG.md` is
   empty (0 bytes).

6. **Branch checks.** `node scripts/round.mjs check` → `ok npm run lint`;
   `ok docket valid`; `ok track scope for
   loop/scout/round-139-outward-survey`; `ok npm run build`; `ok all route
   checks passed`. No group skipped; exit 0. Ports 3000 and 8101 were free
   before and after.

## Concerns

Non-blocking only:

- The correction introduced a cosmetic double space after the blog URL in
  the Evidence section ("https://blogs.nvidia.com/blog/nvidia-ai-factory-compute/   —"
  — three spaces before the em-dash). Harmless; the URL is intact.
- The first review's non-blocking items (aging-GPU framing labelled as
  NVIDIA's position, the round-82 CNN judgment note, the unreported rejected
  candidates) are unchanged by the correction and remain as recorded there;
  none of them blocks.

## Could not check

- Whether the round itself fetched the sources on 2026-08-16 — my fetches
  confirm the URLs are live and contain the quoted text, but not when the
  round fetched them.
- The behavior of the first review's artifact in the checker output — I did
  not inspect its status beyond the orchestrator's note that request-changes
  artifacts may report as FAIL.

Working tree was clean before this review and the only change this session
makes is this review file.
