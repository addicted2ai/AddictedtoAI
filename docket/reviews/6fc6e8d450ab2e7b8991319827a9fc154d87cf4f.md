Commit: 6fc6e8d450ab2e7b8991319827a9fc154d87cf4f
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 161

Review of PR #126, head `6fc6e8d450ab2e7b8991319827a9fc154d87cf4f`, branch
`loop/maintain/docket-drop-direct-arm-item-and-verify-meta-queue`. I verified the
branch name, PR number and head SHA match the brief (`gh pr view 126` → OPEN,
headRefName and headRefOid match). `git status` is clean at the reviewed commit
and stayed clean; I wrote nothing but the review file. The PR is a delegated
round, so my artifact must cover the merged tree — after this commit, the only
change to the tree is `docket/reviews/6fc6e8d4...md`, which satisfies the
review-artifact condition (`git diff --name-only <sha>..HEAD` lists only this
file).

## Claim 1: the drop

The round moved `docket/open/2026-08-11-every-run-and-loop-yml-still-instruct-direct-arm.md`
to `docket/dropped/`, arguing the direct-arm instruction is gone from every path
a round reads. I read the actual files rather than the item's own note:

- `scripts/build-prompt.mjs` lines 71–76: "When you are done, run 'node
  scripts/round.mjs ship'. It pushes, opens the pull request, and decides
  whether to arm auto-merge from the round's own Origin. Do not run 'gh pr merge
  --auto --squash' yourself". No direct-arm instruction.
- `prompts/shared/every-run.md` lines 102–110: the shipping section says "How
  the run ends is the last instruction of the prompt that launched you. That
  instruction lives in `scripts/build-prompt.mjs` ... it is deliberately not
  restated here". No merge instruction.
- `.github/workflows/loop.yml` line 25: `on.schedule` is commented out
  (`# schedule:` / `#   - cron: "0 */3 * * *"`); only `workflow_dispatch` is
  active. The workflow builds its prompt from `build-prompt.mjs` (line 133) and
  invokes no merge command anywhere.
- `scripts/orchestrate.sh` is the loop that ships rounds: it runs `opencode run`
  with `prompts/orchestrator.md`, which at line 131 directs the run to use
  `node scripts/round.mjs start/check/ship`.

The direct-arm command is gone from every place a round would read. `grep` for
`gh pr merge` across `prompts/`, `AGENTS.md` and `.github/` returns only
`gh pr merge --disable-auto <n>` in `prompts/orchestrator.md` line 179 — the
disarm instruction, the opposite of the direct-arm this item was filed to
remove. The drop is justified by the current tree, not merely by the item's own
2026-08-17 note.

The `## Dropped` section names the reason (overtaken: direct-arm gone from the
active path, the one open box on a path that does not run), states what would
make it refilable (`on.schedule` uncommented without the workflow invoking
`ship`), and preserves the priority-1 warning — both in the `## Dropped` text
("Refilable — and priority 1 again the same day") and in the untouched item body
at line 92 ("If the schedule is ever uncommented, this is priority 1 again the
same day"). The item's frontmatter (`priority: 3`, `blocked-by` now in
`docket/done/`) is intact.

## Claim 2: the two re-verified items stay open

Scope-check item (`2026-08-11-ship-and-scope-check-disagree-on-maintainer-branches.md`):
- I ran `node scripts/check-track-scope.mjs origin/main maintainer/queue-repairs`
  against a non-loop branch name. Output: `skip  branch "maintainer/queue-repairs"
  is not loop/<track>/<slug> — no track scope applied` and `(maintainer branches
  are not track-scoped; loop branches must be)`. Exit 0. This confirms the scope
  check exits 0 on a maintainer branch.
- `scripts/round.mjs` line 465: `if (!/^loop\/[a-z]+\/.+/.test(current))` →
  prints `FAIL  branch '<name>' is not loop/<track>/<slug>` and exits 1. `ship`
  rejects the same branch the scope check passes.
- `round.mjs check` reports the scope check as a skip on a non-loop branch:
  lines 376–379 print `skip  track scope — on '...', not a loop/<track>/<slug>
  branch`.
- The disagreement the item describes is real and current. Item correctly stays
  open. (I did not run `ship` itself — it would push and open a PR; I read the
  branch guard at round.mjs:465 and the changelog gate at round.mjs:509-517.)

Changelog-entry item (`2026-08-11-check-cannot-see-a-missing-changelog-entry.md`):
- The ship half is closed: `round.mjs` lines 509–517 run
  `git diff --name-only origin/main...HEAD -- CHANGELOG.md` and fail closed with
  "this branch changes no changelog entry — auto-merge withheld", exit 1, when it
  is empty. Confirmed by reading.
- The check half is open: `round.mjs check` runs lint, the docket validator,
  track scope, the build and the route suite (lines 361–435). None of them
  reads the diff to detect a missing changelog entry. The item stays open
  correctly.

Minor precision concern, non-blocking: the changelog entry says `round.mjs check`
still runs "nothing that reads the diff". Literally, the track-scope step does
run `git diff --name-only` (check-track-scope.mjs line 126), so the sentence is
imprecise — but the scope check reads the diff to enforce file paths, not to
verify a changelog entry was added. The substance of the claim — that `check`
cannot detect a missing changelog entry — is true and is what keeps the item
open. The item's own 2026-08-17 note uses the same loose phrasing ("none of them
reads the diff"), so the imprecision is inherited, not introduced.

## Claim 3: the record

- The entry is the newest: `### 2026-08-18` at line 72, above the previous
  round's entry at line 129.
- It has `Origin: delegated`, `Track: maintain`, `Agent: opencode
  (deepseek-v4-flash)`, a Hypothesis and Change per block, and a Guardrails line
  naming the checks run. `Origin` is present, so the build's Origin requirement
  is met.
- No past entry was rewritten: the diff against `origin/main` only adds the new
  entry and moves the docket file.
- I re-ran every guardrail check and each produced the stated output:
  - `node scripts/preflight.mjs --json` → `{"findings":[]}`, exit 0.
  - `node scripts/staleness-report.mjs` → "129 published artefacts judged: 128
    within window, 1 recorded-unverified within window, 0 stale or missing a
    date", exit 0.
  - `node scripts/check-one-limit-count.mjs` → "ok ... count 10, 10 set
    member(s), swept 2026-08-17T23:42:47.265Z ... within the 30-day
    process-claim window", exit 0. The sweep file itself was last touched by
    PR #124 and the loop-history snapshot by PR #125 — the round ran the checks
    but did not re-sweep or refresh either, as the entry claims.
  - `node scripts/check-loop-history-snapshot.mjs` → three `ok` lines, exit 0.
- `node scripts/round.mjs check` (the command the guardrails claim ran before
  ship) exits 0: `ok npm run lint`, `ok docket valid`, `ok track scope for
  loop/maintain/docket-drop-direct-arm-item-and-verify-meta-queue`, `ok npm run
  build`, `ok all route checks passed`. No group reported SKIPPED.
- `npm run build` alone also exits 0.

## Scope and what the round must not have done

- `node scripts/check-track-scope.mjs origin/main loop/maintain/docket-drop-direct-arm-item-and-verify-meta-queue`
  exits 0: "ok CHANGELOG.md / ok docket/dropped/2026-08-11-...-direct-arm.md /
  ok all 2 changed file(s) within maintain's scope".
- `git diff --name-only origin/main...HEAD` lists exactly two files: CHANGELOG.md
  and the docket rename. No `CHARTER.md`, `.github/`, `prompts/`, or
  `scripts/check-track-scope.mjs` change — none of the human-owned paths was
  touched.
- No one-limit re-sweep and no loop-history refresh happened (the relevant
  files were last modified by PRs #124/#125; the diff touches neither).
- No past changelog entry was rewritten.

## Round number

The loop-history snapshot records 159 rounds merged at PR #125; PR #121 audited
rounds 151–155, making this PR (#126) round 161. Consistent with the brief.

## What I could not check

- I did not run `node scripts/round.mjs ship`, so I could not observe the
  branch-name rejection or the changelog-entry fail-closed live; I verified both
  by reading the exact guard code at round.mjs:465 and round.mjs:509-517.
- The claim that the round "re-read" the files this round is self-reported; I
  independently verified the current tree matches everything the entry claims
  about it, so the record's description of the drop is accurate regardless.