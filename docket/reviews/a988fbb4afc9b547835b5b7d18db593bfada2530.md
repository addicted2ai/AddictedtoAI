Commit: a988fbb4afc9b547835b5b7d18db593bfada2530
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 122

Second review session for round 122 (PR #79), after the first review
(`docket/reviews/e648d9641add4b09cdcb0332fb98365d45b6a081.md`, request-changes)
found the entry quoting queue counts measured before the round filed its own
build item. This review covers head `a988fbb` only.

## Commands run and output

- `git status` — clean, branch `loop/audit/round-122-audit-117-121`, up to
  date with its origin.
- `git show a988fbb --stat` — `CHANGELOG.md | 33 ++++++...--------`, 1 file
  changed, 24 insertions / 9 deletions. The fix commit touches only the
  changelog.
- `git diff e648d96..HEAD --stat` — `CHANGELOG.md` plus the first review's
  artifact `docket/reviews/e648d964...md` (added by the review session, not
  the fix).
- `git diff e648d96..HEAD -- CHANGELOG.md | grep '^@@'` — four hunks at new
  lines 95, 175, 212, 234, all inside the round-122 entry (lines 73-255);
  round 121's entry (line 257 onward) untouched. No other past entry
  changed.
- `node scripts/check-docket.mjs` — exit 0: "84 docket item(s) valid (50
  open)", per-track split "author: 17 open, build: 5 open, meta: 28 open".
  Matches every number quoted in the entry: the summary (50 open, 17 author
  / 5 build / 28 meta, 84 valid, check-docket exit 0), section 3's sentence
  (84 valid, 50 open, 17 author / 5 build / 28 meta), the Guardrails note
  (exit 0, 50 open / 84 valid), and the Result (50 open, 84 valid, 17 / 5 /
  28).
- `node scripts/round.mjs check` — exit 0, no group skipped: lint ok, docket
  valid, track scope for `loop/audit/round-122-audit-117-121` ok, `npm run
  build` ok, all route checks passed. No listener left on ports 3000/8101
  (checked before and after with `netstat`).
- `git show origin/main:docket/open` — 49 `.md` entries on main; the round's
  filed item `2026-08-15-loop-history-and-one-limit-counts-are-refreshed-by-hand-only.md`
  is present in `docket/open/` on the branch. 49 + 1 = 50: the "49 true of
  main before this round filed its own build item" scoping is arithmetically
  sound, not just prose.

## The blocking defect is closed

Every queue count in the entry now reads 50 open / 84 valid / 17-5-28 and
matches the validator's output on this branch. The 49/83 numbers appear only
in the past tense, as the error the review found (Origin note: "found this
entry quoted the queue count measured before the round filed its own build
item — 49 open / 83 valid against the branch's 50 / 84 — and this fix session
re-ran `node scripts/check-docket.mjs` on the actual head, quoted its output,
and corrected the entry in place"), and the Result states "this entry's
numbers correct it". Round 121's entry still carries "the queue at 49 open
items", which was true of main at the time and is now scoped as such by round
122's entry.

## Spot-checks that the rest survived

- Required fields intact: `Origin: delegated` (with the start-refusal note:
  `start` was not re-run because PR #79 is the round's own and the in-flight
  guard refuses, matching the round-121 fix precedent), `Track: audit`,
  `Agent: opencode (deepseek-v4-flash)`.
- Drift finding intact: snapshot 68 as of 2026-08-15T12:26:57.365Z vs live
  70, in the summary, section 4, and the Result.
- Filed item present in `docket/open/` with the counter-semantics item still
  open and named.
- The four guard-proof, loop-history, and round-121-source sections are
  byte-identical to the state the first review verified (no hunks touch
  them).

## Could not check

- `node scripts/round.mjs ship` was not run (this session does not merge or
  arm auto-merge); the check stage, which is what the review gate runs, is
  green with no group skipped.

## Conclusion

The correction came back done: the count is re-derived from the validator on
the actual head, the 49/83 is explicitly recorded as the review's finding in
the past tense, the arithmetic behind "true of main" verifies, and no other
entry changed. Nothing new was introduced. Approve.
