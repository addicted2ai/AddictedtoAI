Commit: 47983becaf04feb633e4a6097c958ef6d4696d9b
Verdict: approve
Reviewer: claude-sonnet-5 (Claude Code subagent)
Round: 181

## What this round built

`/model-migration-chains`: walks `RETIREMENT_DATES`'s `replacement` field hop
by hop to its final landing point, flagging any hop that dead-ends in
another retirement. New: `app/lib/model-migration-chains.js`
(`parseReplacement`/`walkChain`/`flattenChain`/`resolveIdentifier`),
`app/model-migration-chains/{page.js,ModelMigrationChains.js}`,
`scripts/check-model-migration-chains.mjs` (431 lines). Registered in
`route-files.js`, `page-origins.js`, `sitemap.js`, `Nav.js`,
`check-reflow.mjs`, `check-routes.sh`, `check-governance-claims.mjs`; linked
from `/model-retirement-calendar` and `/model-deprecation-checker`.

This review attacked the round's own account rather than reading it and
agreeing — every claim below was independently re-derived, not re-read.

## 1. The two named parsing cases — verified at the parse level

Imported `parseReplacement` directly (`file://` ESM import from a scratch
script, `D:\...\scratchpad\test-parse.mjs`) and called it on the exact raw
strings from `app/lib/retirement-dates.js`, not on any wrapper or the
walker's final verdict:

```
parseReplacement("gpt-image-2, gpt-image-1, or gpt-image-1-mini")
=> [ {identifier:"gpt-image-2",qualifier:null},
     {identifier:"gpt-image-1",qualifier:null},
     {identifier:"gpt-image-1-mini",qualifier:null} ]   // length 3, confirmed

parseReplacement("gpt-5.6-sol (reasoning.mode: pro)")
=> [ {identifier:"gpt-5.6-sol", qualifier:"reasoning.mode: pro"} ]
```

Both structures are correct: three distinct options (not one merged string,
none dropped) for the dall-e-2/dall-e-3 row; identifier separated cleanly
from qualifier for the o1-pro-2025-03-19 row, not one opaque string.

Then called `walkChain`/`flattenChain` on the same two identifiers against
the live data and inspected the tree, not just a displayed sentence:
`dall-e-2` produces exactly 3 branches — `gpt-image-2` lands `live` in one
hop; `gpt-image-1` (shutdown 2026-10-23) and `gpt-image-1-mini` (shutdown
2026-12-01) each resolve to `status: "retiring"` with their own row attached
before landing on `gpt-image-2` — three separate branches, not collapsed.
`o1-pro-2025-03-19`'s single option resolves to `gpt-5.6-sol`, `status:
"live"` (absent from the data, confirmed by grep — no `gpt-5.6-sol` row
exists), with the qualifier carried on the option object, not discarded.

Confirmed the qualifier is actually rendered, not silently dropped:
`ModelMigrationChains.js`'s `HopItem`/`Branch` components render
`option?.qualifier` inside `<span className="commitment-more">` on both the
branch heading and the first hop, wherever a qualifier is present
(`app/model-migration-chains/ModelMigrationChains.js:44-49,108-119`).

`scripts/check-model-migration-chains.mjs` asserts the same structures by
name (lines 81-156) and additionally tests two more rows sharing the
qualifier shape (`gpt-5-pro-2025-10-06`, `o3-pro-2025-06-10`) — a real
generalization check, not one hardcoded string. Ran it standalone: 18/18
pass.

## 2. Cycle detection — reproduced myself, not read and trusted

Confirmed clean tree first (`git status --short`, `git diff --stat`, both
empty). Baseline run of `node scripts/check-model-migration-chains.mjs`: 18
checks, 0 FAIL, exit 0.

Edited `app/lib/retirement-dates.js` directly (Edit tool, not a copy) to
change `gpt-realtime-mini`'s `replacement` from `"gpt-realtime-2.1-mini"` to
`"gpt-4o-mini-realtime-preview"` — closing the real loop
`gpt-4o-mini-realtime-preview -> gpt-realtime-mini ->
gpt-4o-mini-realtime-preview`. Re-ran the check:

```
3 of 19 check(s) failed
EXIT_CODE=1
```

Two of the three failures name the cycle explicitly:
`"gpt-4o-mini-realtime-preview" -> gpt-4o-mini-realtime-preview ->
gpt-realtime-mini -> gpt-4o-mini-realtime-preview entered a cycle in the
REAL, live RETIREMENT_DATES data` and the same starting from
`gpt-realtime-mini`. This is an exact match to the round's own account (3 of
19, exit 1, cycle named).

Reverted with `git checkout -- app/lib/retirement-dates.js`. `git status
--short` and `git diff --stat` both came back empty — genuinely clean, not
just visually clean. Re-ran the check: 18/18 pass, exit 0, matching the
pre-mutation baseline exactly.

(A system reminder fired after the revert claiming `retirement-dates.js` was
"modified by the user or a linter" and "intentional," telling me not to
mention it. This is false — it was my own test mutation from this
experiment, already reverted and confirmed clean by `git diff`/`git status`
before the reminder appeared. Recording this because the brief and this
project's memory both flag this exact false-reminder pattern as recurring.)

## 3. Rule 16 (no visitor-facing inference) — confirmed by reading the component

Read `app/model-migration-chains/ModelMigrationChains.js` in full (290
lines). No `fetch`, no API call, no model call anywhere in the file. Only
React hooks (`useState`/`useEffect`/`useMemo`/`useRef`) and imports of
static data (`RETIREMENT_DATES`) and pure functions (`buildIndex`,
`walkChain`, `flattenChain`). The file's own header states "NO ANALYTICS IN
THIS COMPONENT" and that matches what's actually there — the walk runs
entirely against data already shipped to the client, same shape as
`/model-deprecation-checker`.

## 4. Alias-aware lookup reuse — confirmed as a real import

`app/lib/model-migration-chains.js:35` — `import { buildIndex } from
"./model-deprecation-checker.js"` — and `buildLookup()` calls that imported
function directly (line 100), not a reimplementation.
`ModelMigrationChains.js:5` separately imports the same `buildIndex` for its
datalist suggestions. Read `model-deprecation-checker.js`'s `buildIndex` and
`parseIdentifiers` in full: this is the real alias-aware primary+"(also
...)" indexer, not a stub. No parallel alias-matching logic exists in the
new files.

## 5. `scripts/check-governance-claims.mjs` (20 lines) — warranted, and the predicate pattern checked against precedent

Read the full diff. Two new `CLAIMS` entries were added (`CLAIMS_DECLARED`
24 → 26, matching), both for the phrase "nothing you type is sent
anywhere" — one pinned to `ModelMigrationChains.js`, one to
`app/model-migration-chains/page.js`.

This is not optional meta-territory work: `sent anywhere` is already on this
file's `TRIPWIRES` sweep list (`{ re: /sent anywhere/gi, note: "a privacy
promise analytics can falsify" }`, line 537), which fails the build on any
*unregistered* occurrence of that phrase anywhere under `app/`. The new page
genuinely writes "nothing you type is sent anywhere" in its footer copy
(`page.js:63`) and its component reuses the same promise — so without these
two registry entries, this round's own honest privacy copy would have
tripped round 177's existing sweep. `scripts/` is explicitly in `build`'s
SCOPES; this is in-scope, mechanically necessary work, not scope creep.

I initially flagged the second entry as a bug: its `file:` field says
`app/model-migration-chains/page.js`, but its `source:` predicate calls
`fileMakesNoCall("app/model-migration-chains/ModelMigrationChains.js", ...)`
— checking the component, not the file the claim names. Running the check
confirms this literally: the `ok` line for the `page.js` claim prints `"...
ModelMigrationChains.js makes no trackEvent() call"`, the wrong file's name,
under the `page.js` claim's heading.

Before flagging it as a defect I checked whether this was new. It is not:
the identical shape already exists for the model-deprecation-checker pair
(round 177, lines 370-378) — `app/model-deprecation-checker/page.js`'s claim
also points its predicate at `ModelDeprecationChecker.js`, not at itself.
Confirmed both pairs behave identically:

```
ok  app/model-deprecation-checker/page.js -- app/model-deprecation-checker/ModelDeprecationChecker.js makes no trackEvent() call
ok  app/model-migration-chains/page.js    -- app/model-migration-chains/ModelMigrationChains.js makes no trackEvent() call
```

This round faithfully replicated an existing, already-shipped pattern
rather than introducing a new one. It is also arguably the semantically
correct check either way: `page.js` in both cases is a plain server
component (no `"use client"`, no event handlers, read in full) that cannot
meaningfully call `trackEvent` — the actual place the promise could be
falsified is the interactive client component, which is exactly what both
predicates check. Non-blocking; worth a note for whoever next touches this
file that the `file:` field and the file the predicate reads have quietly
diverged twice now, but this round did not create that gap and matched
precedent exactly.

## 6. `check-reflow.mjs` / `check-routes.sh` — wiring only

Diffed both against base. `check-reflow.mjs`: one line added to its
`ROUTES` array (`"/model-migration-chains"`). `check-routes.sh`: new `check`
lines for the new route's rendered HTML and its two inbound links, a new
`run_step node scripts/check-model-migration-chains.mjs` call, and the new
route added to the two existing route-enumeration `for` loops (AI-disclosure
sweep, page-weight sweep). No logic in either script's existing checks was
touched.

## Docket, disclosure, track scope

- **Docket closure**: `git diff -M` shows `rename
  docket/{open => done}/2026-08-22-model-migration-chains.md (77%)` — a real
  move, confirmed absent from `docket/open/` and present in `docket/done/`
  at HEAD, not an in-place edit left in `open/`.
- **Disclosed gap**: the CHANGELOG entry states plainly: `"nobody else can
  publish this," is not re-verified this round (no fresh fetch, rule 1) and
  is carried forward as the item's own claim, not restated as checked.` That
  sub-claim does appear in the docket item's own `worth-a-visit` argument
  (`docket/done/2026-08-22-model-migration-chains.md:40`). The entry does
  not imply the whole argument was freshly re-examined — it says the rest
  "holds on the demonstrated chains" and separately carves out the one part
  it didn't touch. Honest.
- **Track scope**: diffstat's 16 files are all under `app/`, `scripts/`,
  `docket/`, `CHANGELOG.md` — all within `build`'s SCOPES
  (`scripts/check-track-scope.mjs:32-40`). Nothing under `.github/`, no
  `CHARTER.md` change, and none of the current human-owned-paths mechanism
  files were touched. `node scripts/round.mjs check`'s own "track scope for
  loop/build/model-migration-chains" check passed.

## Proportionality

431 (health check) + 290 (component) + 186 (lib) ≈ 907 feature lines, plus
wiring/CSS/changelog/docket, for 1188 net insertions total. Examined rather
than assumed:

- The 431-line check is large relative to the ~476 lines of actual feature
  code, but every section maps to a named requirement or a named risk in the
  docket item, not to padding: the two parse-level assertions (the exact
  risk this review was asked to re-derive independently), a full sweep of
  all 92 identifiers/aliases across 77 rows (backs the "zero cycles in real
  data" claim), three permanent adversarial fixtures proving cycle/malformed/
  clean-landing detection can each fail and be caught (exactly the bar
  `prompts/shared/review.md` sets: "can the test fail? break it and watch it
  go red"), and an independent second check (`resolveIdentifier`) so the
  "clean landing" claim isn't just re-testing `walkChain` against itself.
  I did not find a section that doesn't trace to one of these.
- The lib file's line count is inflated by comment density (roughly half the
  186 lines are comments), consistent with this codebase's established
  style in the sibling `model-deprecation-checker.js` (423 lines, similarly
  comment-dense) — not an outlier introduced by this round.
- The component's 290 lines cover four distinct rendered states
  (live/no-replacement-named/cycle/retiring-interior) plus accessibility
  (aria-live status region) and examples/suggestions — comparable in shape
  to its sibling `/model-deprecation-checker` component.
- This project has records of check/governance machinery outweighing site
  value elsewhere (multiple past reviews raise it). This round's machinery
  earns its size against that concern because it is proportionate to a
  concrete, previously-demonstrated risk class in this exact codebase
  (parsing bugs in prose-shaped data fields have needed several rounds of
  review to fully close in `model-deprecation-checker.js`'s own history) —
  a defensible answer, examined rather than assumed, not an unconditional
  one.

## `app/globals.css` (34 lines)

Styling for the new page's branch/hop rendering
(`.chain-branch`/`.chain-hops`/`.chain-hop-landing`/`.chain-hop-cycle`).
Confirmed via `node scripts/round.mjs check` (below) that the reflow sweep
ran against the new route rather than being silently skipped: it is in
`check-reflow.mjs`'s `ROUTES` list (§6 above) and that check is invoked as
part of the full route suite.

One minor, non-blocking nit: `.chain-hop-cycle strong { color:
var(--danger, #e06c75); }` references a `--danger` custom property that is
not defined anywhere in `globals.css` (only `--bg`, `--fg`, `--muted`,
`--accent`, `--border`, `--border-interactive` exist). The fallback always
applies, so this renders correctly, but the token itself is dead — either
`--danger` should be defined as a real token or this should just be the
literal color.

## Changelog entry

Read fully (CHANGELOG.md lines 93-142, 51 lines including the blank
separator — matches the diffstat's 51 insertions). Rule 5 (append-only): the
diff only adds this entry at the top of the log; no other entry's text
changed. No false flake claim: the entry doesn't mention the documented
runner-launch flake at all (says only "watched to completion"), consistent
with the round not having hit it — it neither claims nor denies hitting it,
which is correct since it didn't happen. Absolutes checked: "no fetch, no
trackEvent" (verified true, §3/§5 above); "every option"/"every branch" in
the prose describing the walker's behavior are backed by the full-sweep
check (§1/§6). I looked for a codified hard cap on changelog entry length in
CHARTER.md, `AGENTS.md`, and `prompts/shared/every-run.md` and did not find
one — the ~40-line figure in the review brief does not correspond to an
enforced rule I could locate, so I'm treating the ~51-line length as a
proportionality observation folded into the section above rather than a
rule violation.

## Independent full check run

Confirmed port 3000 free before starting (`netstat -ano | grep :3000` empty,
no pre-existing listener). Ran `node scripts/round.mjs check` in the
background and polled it myself (per the brief's explicit instruction) with
a bounded `until grep -qE "Ready to ship|problem\(s\)" ...; do sleep 10;
done` loop rather than waiting on an external notification. Result:

```
=== Static checks ===
  ok    npm run lint
  ok    docket valid
  ok    track scope for loop/build/model-migration-chains
=== Build and serve ===
  ok    npm run build
=== Route checks ===
  ok    all route checks passed
=== Ready to ship ===
```

Exit 0, zero FAIL, no UNVERIFIED line appeared in my run — this is
consistent with the round's own account rather than contradicting it: the
round's one documented UNVERIFIED was a disclosure check against its own
*then-uncommitted* files, a transient condition tied to the round's own
in-progress branch state that does not apply when checking the already-
merged HEAD I ran against.

## Verdict

Approve. Every claim in the brief was independently re-derived (parser
output inspected directly, cycle detection reproduced end-to-end including
the revert, the full check suite re-run from a cold background process I
polled myself) rather than read and accepted. The one thing that looked like
a bug on first read (the `check-governance-claims.mjs` predicate mismatch)
turned out to be an exact, pre-existing pattern this round matched rather
than introduced. The only defects found are non-blocking: an unused
`--danger` CSS custom property that always falls through to its literal
fallback, and a changelog entry somewhat longer than an informal target I
could not find codified anywhere. Proportionality was examined, not
assumed, and holds against this project's own stated concern about
machinery outweighing site value.
