Commit: 7e7891af0228fda1cec49427a05c837ce488e0db
Verdict: approve
Reviewer: claude-sonnet-5 (Claude Code subagent)
Round: 173

Delta re-review of the fix commits on top of 763e380
(`docket/reviews/763e38042088dd96bb853b5ac371c351b2565256.md`, my prior
`request-changes`), diffed as `3ea3a36..7e7891a` (my review-artifact commit
through the fix round's final commit; `63657f2` is the fix, `7e7891a` records
it). Working tree clean before and after — every temporary edit made during
this pass (a scratch runner entry, a reverted `excluded_model_patterns`
pattern used twice to force both new checks red, an `ORCHESTRATE_SERVER`
override) was reverted or was environment-only; `git status --short` is
empty. Nothing outside `docket/reviews/` is staged. `763e380`'s review file
is untouched by the fix round, confirmed (`git diff 3ea3a36..7e7891a --
docket/reviews/` is empty) — correct: a review artifact is the reviewer's
record, not the round's to edit.

My finding stands and was fixed correctly. I could not reopen it, could not
find a false positive, and could not manufacture a third defect despite
trying.

## Re-verifying my own finding, independently

Re-ran my exact live reproduction from the prior review
(`node scripts/runner-preflight.mjs` against a temporary `openrouter` /
`openai/gpt-oss-20b:free` entry) — now `FAIL model not excluded`, correctly.
Re-fetched `GET http://127.0.0.1:4097/provider` fresh (not reusing my prior
pass's saved copy) and scanned it myself, independent of
`scripts/check-free-model-exclusion.mjs`:

```
connected: [ 'openrouter', 'ollama-cloud', 'opencode-go', 'opencode' ]
total reachable (connected providers): 395
contains "free" as ANY substring: 23
of those, missed by NEW word-boundary pattern: 0
```

23 matches the round's figure exactly, and my own tally of the raw catalogue
(17 on `openrouter` — 16 colon-suffixed, 1 the bare `openrouter/free` I did
not separately call out in my first review — plus 6 hyphen-suffixed on
`opencode`/Zen) sums to 23 and reconciles with the round's "`-free$` caught 6
of 23" claim. The round's correction that I undercounted by missing
`openrouter/free` is accurate; noted for the record, not disputed.

## The four things asked

**1. Tried to reach a free model again.** Two angles, both empty-handed:

- *Missed exclusions.* Scanned not just the 4 connected providers but all
  193 providers in the live catalogue (74 models total contain "free" as a
  word across all of them) for any id where "free" is glued to adjacent
  alphanumerics without a separator (`freeTier`, `free3`, digit- or
  camelCase-joined) — the one shape that would defeat a word-boundary regex.
  Zero found, in the connected set or outside it. Listed and eyeballed all
  74 matches by hand: every one ends in a recognizable free-tier marker
  (`-free`, `:free`, `/free`); none is a plausible false positive
  (`freedom`, `freeway`, a proper name like "FreeWilly" — none of these
  exist in the current catalogue).
- *Wrong exclusions.* Same 193-provider scan found zero cases where the new
  pattern matches an id containing no "free" substring at all (a sanity
  check that would only fire on a regex bug), and the fixed-table guard
  (`test-free-model-pattern.mjs`'s `MUST_NOT_EXCLUDE`) already covers the
  camelCase/glued-word shape directly (`freeform-assistant`,
  `wildfreedom-7b` — correctly not excluded).

I agree with the round's own honest framing: nothing here proves a
free-tier model named without the word "free" ("trial", "oss", a bare id)
would be caught, and nothing claims otherwise anymore.

**2. The two new checks, proved able to fail, and the UNVERIFIED
distinction.** Forced both red myself, independently of the round's own
proof, by editing `scripts/runners.yml`'s pattern back to `-free$` (reverted
after; `git status --short` empty):

```
$ node scripts/check-free-model-exclusion.mjs
FAIL 17 "free"-named reachable model(s) are NOT excluded by scripts/runners.yml's current pattern(s):
  openrouter/nvidia/nemotron-3-super-120b-a12b:free
  ... (17 total, openai/gpt-oss-20b:free among them)

$ node scripts/test-free-model-pattern.mjs
FAIL  'nvidia/nemotron-3-super-120b-a12b:free' is NOT excluded -- ...
FAIL  'openai/gpt-oss-20b:free' is NOT excluded -- ...
FAIL  'openrouter/free' is NOT excluded -- ...
FAIL  'free' is NOT excluded -- ...
FAIL  'FREE-tier-model' is NOT excluded -- ...
5 check(s) failed
```

Restored the pattern; both back to green. Then tested the UNVERIFIED path
specifically, since that is the exact distinction round 171 got wrong:

```
$ ORCHESTRATE_SERVER="http://127.0.0.1:1" node scripts/check-free-model-exclusion.mjs
UNVERIFIED http://127.0.0.1:1/provider unreachable -- cannot check the live catalogue
$ echo $?
0
```

`UNVERIFIED`, printed plainly, exit 0 — not a silent `PASS` and not a build
failure for something structurally unknowable without a local OpenCode
server (the CI case). `scripts/check-routes.sh` adds this exit code straight
into its `failures` accumulator, so an honest 0 here correctly does not fail
the suite while a genuine mismatch (exit 1, demonstrated above) does. This
is the right shape.

**3. The residue and the word "absolute".** `scripts/runners.yml`'s header
states it plainly, in the file every future runner entry is added to and the
file this round's own header says is read "before a single runner is
declared": *"a provider naming a free tier without the word 'free' at all —
'trial', 'oss', a bare model name with no marker — would not [match], and
nothing here claims otherwise."* That is where a reader hits it, not in a
commit message or a buried test comment. "Absolute" survives in the text,
but only to describe the maintainer's standing instruction (attested,
FRAME.md fact 18) — the paragraph directly below it, headed "WHAT 'ABSOLUTE'
ACTUALLY MEANS HERE, STATED PLAINLY," draws the line between the instruction
and what a name-pattern check can prove. I read this as accurate, not as the
word doing more work than the mechanism backs up.

**4. The rejected cost-based check.** Verified directly against the live
catalogue's `cost.input`/`cost.output` fields, not taken on the round's
word:

```
provider ollama-cloud: 20 models -- zeroCost=20 nonZeroCost=0 noCostField=0
provider opencode-go:  18 models -- zeroCost=0  nonZeroCost=18
provider openrouter:   350 models -- zeroCost=23 nonZeroCost=327
```

Every one of `ollama-cloud`'s 20 models reports cost 0 — a pure cost-0 rule
would have excluded a legitimate, fully connected, non-free-tier provider
wholesale. The claim is true and is a sound reason to keep the name-based
check as primary rather than switch mechanisms.

## The premise-gap shape, verified

Read `FRAME.md` fact 18's own embedded check directly (not taken from the
round's paraphrase):

```
/free/i.test(String(m.id||''))||/free/i.test(String(m.variant||''))||/free/i.test(String(m.providerID||''))
```

Unanchored, case-insensitive substring match — broader even than the new
word-boundary pattern, and it has read this way since the commit that
introduced `FRAME.md` (`7b25c44`, "round 8 of the current Claude-Code-subagent
era" per `CHANGELOG.md` line 799 — the commit immediately preceding this
round's own base). The original round cited `frame:18` (premise 7) as this
mechanism's justification while shipping something narrower than what
`frame:18`'s own check already demonstrated. `scripts/check-briefs.mjs`
verifies a `frame:N` citation only by confirming the fact still exists as a
heading — not that the citing round's implementation matches or exceeds what
the fact's own check does — so this gap is invisible to that tool by
construction, structurally distinct from round 8/9's "citation resolves but
doesn't support the claim" gap: here the citation *does* support the claim,
and the implementation still fell short of its own source. Worth recording
as a separate premise-gap shape, as flagged; I found nothing to add to it.

## Minor, non-blocking

`CHANGELOG.md`'s guardrails line reports `test-free-model-pattern.mjs` as
"9/9 `ok`"; running it prints 10 `ok` lines (1 for "declares N
`excluded_model_patterns` rule(s)", 6 for `MUST_EXCLUDE`, 3 for
`MUST_NOT_EXCLUDE`) — the tally undercounts by one. Doesn't change the
verdict: the check's pass/fail behavior is exactly as claimed and verified
above, this is a miscount in a prose summary, not a defect in the guard.

## Verdict

Approve. Both real findings — the exclusion gap and the premise-narrower-
than-its-own-citation gap — are fixed, and I verified the fix independently
rather than re-reading the round's proof: live catalogue queries I ran
myself, both new checks forced red and green by hand, the UNVERIFIED path
exercised directly, and the rejected-alternative claim checked against raw
`cost` fields rather than trusted. I attacked the mechanism the same way I
did the first time and it held.
