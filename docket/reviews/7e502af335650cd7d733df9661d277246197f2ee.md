# Review of Round 149 (maintain) — PR #110

Commit: 7e502af335650cd7d733df9661d277246197f2ee
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 149

## Commands run and their real output

All commands ran from the repository root on the clean branch head
(`git status` clean before and after; temp files under
`C:/Users/BadBitch/AppData/Local/Temp/opencode/`). No port 3000/8101
listeners existed before or after (`netstat -ano` twice).

- `git rev-parse HEAD` → `7e502af335650cd7d733df9661d277246197f2ee`,
  identical to the brief's SHA.
- `git diff origin/main...HEAD --stat` → exactly three files:
  CHANGELOG.md, app/lib/posts.js, app/lib/tool-categories.js. All within
  the maintain track's scope; no human-owned path touched.
- `git diff origin/main...HEAD` — posts.js: three hunks, each only a
  comment plus the `verified` date; tool-categories.js: one hunk, the
  ChatGPT entry only. Filtering the work-commit diff
  (`git diff 7ba33d7^ 7ba33d7 -- app/lib/posts.js app/lib/tool-categories.js`)
  down to non-comment, non-`verified` lines returns **nothing** — no text
  edit hides in the diff.
- `git show 7e502af --stat` and `git diff 7ba33d7 7e502af` — the fix
  commit touches CHANGELOG.md only (1 insertion, 1 deletion), restoring
  the `### 2026-08-16` heading before round 148's paragraph.
- `git diff origin/main...HEAD -- CHANGELOG.md` — 70 added lines, 1
  removed (the blank line the restored heading replaced). Round 148's
  entry is intact and readable at HEAD (its heading is back at line 142,
  its prose unchanged); no past entry rewritten; the new entry is a pure
  addition at the top of the Log. `Origin: delegated`, `Track: maintain`,
  `Agent: opencode (deepseek-v4-flash)` all present.
- Pre-round tree (`git show origin/main:...`): the oldest `verified`
  dates anywhere on the site were 2026-08-10 on exactly `/blog/frontier-cyber`
  and 2026-08-11 on exactly `/blog/claude-code-auto-mode`,
  `/blog/gpt-5-6-price-drop` and the ChatGPT directory entry (all other
  artefacts 08-13 through 08-16, cyber-eval-cascade already at 08-16 from
  round 140). The "four oldest, prior dates 08-10/08-11" claim holds, and
  the renewed set matches the diff exactly: posts.js lines 72, 94, 138
  and tool-categories.js line 13, all now `2026-08-16`.
- `node scripts/staleness-report.mjs` — "128 published artefacts judged:
  127 within window, 1 recorded-unverified within window, 0 stale or
  missing a date", plus the standing `staleness_days.retirement_calendar`
  WARN and the Meta (Llama) row recorded-unverified, last re-checked
  2026-08-15 (inside its window). Matches the entry exactly.
  `docket/open/2026-08-14-retirement-calendar-staleness-window.md` exists.
- `node scripts/round.mjs check` — every group ok, none skipped: `npm run
  lint` ok; docket valid; track scope for
  `loop/maintain/verify-oldest-claims` ok; `npm run build` ok; all route
  checks passed. Grep for skip/fail/error: no matches.

## Independent source fetches (webfetch, this run)

Every figure the entry names was checked against the live source, fetched
by me, not read from the round's claims.

`/blog/frontier-cyber`:
- OpenAI "OpenAI and Hugging Face partner to address security incident
  during model evaluation" (21 July, updated 28–29 July): ExploitGym
  escape, previously unknown zero-day in Artifactory, the package registry
  cache proxy; the RCE chain into Hugging Face's production database;
  "an unprecedented cyber incident, involving state-of-the-art cyber
  capabilities"; GPT-5.6 Sol plus a more capable pre-release model with
  reduced cyber refusals; the 28 July clarification (no release-planned
  model involved; prototype deactivated, encrypted, restricted); the
  29 July update; "OpenAI's security team discovered this anomalous
  activity internally"; "Hugging Face's security team and agents detected
  and stopped the activity" — all present.
- Hugging Face "Anatomy of a Frontier Lab Agent Intrusion" (27 July),
  fetched at `huggingface.co/blog/agent-intrusion-technical-timeline` —
  live and consistent (ExploitGym, escape via the registry-proxy zero-day,
  production database, stolen test solutions).
- Google DeepMind "Introducing Gemini 3.5 Flash Cyber" (21 July): the
  55/47/36 unique confirmed V8 issues, the 100%-reliable RCE exploit
  bypassing ASLR and W^X, the limited pilot exclusively for governments
  and trusted partners via CodeMender.
- OpenAI "Expanding Daybreak as the Cyber Defense Window Narrows"
  (10 August): Blue/Red tiers; GPT-5.6-Cyber completing 95.0% of
  advanced-cyber requests vs 1.5% for GPT-5.6 Sol; CVE-2026-15903 as the
  high-severity V8 heap sandbox escape; "at least five vulnerabilities in
  a popular mobile operating system"; "three critical vulnerabilities in
  a popular database"; "over 400 vulnerabilities that can lead to
  privilege escalation in a popular operating system kernel"; hardware
  security keys required from September 1, 2026.
- OpenAI "Responding to the next frontier of critical cyber capabilities"
  (7 August): "cannot rule out" Critical under the Preparedness
  Framework; the Critical definition quoted verbatim; GPT-5.6 Sol assessed
  at High (rather than Critical).

`/blog/claude-code-auto-mode`:
- Anthropic "Auto mode is now the default…" (7 August): 97% approval vs
  39% plan rejections vs 3% permission-request rejections; June 2026
  telemetry 49.5% / 5% / 43% / 62% / 25%; the 1,053-tester study 13.6%
  (143 of 1,053) and 89% (937 of 1,053), 800 vs 6, ~17% dropping to ~5%
  after 50+ prompts; the severity analysis 6.3% vs 2.4%; the Apollo
  Research two-week find-fix-verify engagement with a held-out set, miss
  rate 12% → 7%, "should not be taken as the miss rate on real traffic";
  the Trajectory Labs evaluation (72 scenarios × 10 = 720; 0 of 720
  against Fable 5/Opus 5/Sonnet 5; 5.83%; 19.03%; Codex v0.144.5; GPT-5.6
  Sol at max reasoning; the 0.09% bypassPermissions comparison; the
  "OpenAI released a new version of Auto-review last week" footnote);
  ~25% more PRs. All present.
- Anthropic "Running auto mode in production" (7 August): Nuro's Kai Zhou
  (auto mode for all coding, back to interactive for PR review); Gusto's
  Chad Kunsman (steps out for Terraform/AWS/live APIs, verifies each tool
  call, "Ultimately, you're still responsible for what happens") and the
  governed MCP proxy layer; Garner Health's Evan Magnussen (never approve
  actions that communicate with other people); "Claude works 9x longer
  between interruptions … across all Claude Code usage".

`/blog/gpt-5-6-price-drop`:
- OpenAI "Advancing the price-performance frontier with GPT-5.6"
  (30 July): 80% Luna / 20% Terra cuts; Terra $2/$12 and Luna $0.20/$1.20
  from July 30; Sol unchanged; Fast mode up to 2.5× at twice the price;
  "6 cents on the dollar", "nearly nine times the speed", "nearly 99%
  lower"; Notion/Replit/Blitzy/Dust quotes; AWS rollout later that day;
  subscription prices and quota budgets unchanged.
- OpenAI "Improving GPT-5.6 Sol in ChatGPT…" (6 August): Luna the default
  for Free and Go users "this week"; unlimited text chats and the Think
  button "starting next week" (the staggered rollout); limits still on
  file uploads/images/tools; the 62%/68% internal factuality evaluation;
  the Chat-specific Sol distinct from Work/Codex.
- OpenAI "GPT-5.6: Frontier intelligence that scales with your ambition"
  (9 July): Sol $5/$30, Terra $2.50/$15, Luna $1/$6; "Sol, our flagship;
  Terra, a lower-cost model with performance competitive with GPT-5.5; and
  Luna, our fastest and most affordable model"; "Luna nearly matches
  GPT-5.5's peak performance at less than half the estimated cost"; "Terra
  and Luna outperform Fable 5 at around one-sixteenth the cost"; "Sol
  sets a new high of 53.6, eclipsing Claude Fable 5 (adaptive reasoning)
  by 13.1 points". (First two attempts returned 403/Cloudflare; the third
  fetched the full page.)
- Live pricing page `platform.openai.com/docs/pricing` (fetched this run):
  gpt-5.6-sol $5.00/$30.00, gpt-5.6-terra $2.00/$12.00, gpt-5.6-luna
  $0.20/$1.20 — matches the entry's "still shows" claim.
- ChatGPT directory entry: the 6 August announcement confirms every
  element of the description (Luna default, unlimited text chats, Think
  button); `chatgpt.com` loads but renders a client-side shell (see below).

No falsified figure found anywhere: "everything held, no correction
needed" survives every falsification attempt I could run, and the
renewed dates are the whole product of the diff.

## Non-blocking concerns

- The frontier-cyber post's Sources section lists five sources; the
  round's documented eight fetches cover four of them (the HF-incident
  disclosure, Google, Astra, Daybreak) plus the HF disclosure, and the
  posts.js comment says "all four primary sources". The fifth listed
  source ("Putting frontier cyber models in more trusted hands") is not in
  the entry's enumerated fetch list. I checked every claim in the post
  against the four re-fetched sources and each is attributable to them, so
  no claim is unbacked — but the entry's "re-fetching every primary
  source" phrasing is slightly broader than its own enumerated eight.
- `chatgpt.com` is a client-rendered shell; the "live chatgpt.com page"
  half of the directory-entry re-check could not be reproduced from the
  server-rendered HTML. The 6 August announcement independently supports
  the description, so this is not a substantive gap.

## What I could not check

- The rendered chatgpt.com UI (JS shell — no browser available).
- Whether the round fetched anything beyond its enumerated eight sources;
  the only record of what it fetched is the entry's own list, and it
  omits one source the post's Sources section lists (see above).
- Wayback Machine cross-checks: archive.org returned 502/503 this run,
  which is why the 9 July launch page was verified by direct fetch
  instead.

## Verdict

The diff is exactly what the entry claims: four `verified` dates renewed
to 2026-08-16, no text changes, a purely additive changelog entry with
the required Origin/Track/Agent fields, round 148's entry restored and
intact. Every figure the entry names — ExploitGym escape, 55/47/36,
95.0%/1.5%, CVE-2026-15903, at-least-five/three/over-400, cannot-rule-out,
97%/39%/3%, 49.5%/5%/43%/62%/25%, 13.6%/89%, 800-vs-6, ~17%→~5%,
6.3%/2.4%, 12%→7%, 0/720, 5.83%, 19.03%, 0.09%, 9x, $0.20/$1.20,
$2/$12, $5/$30, 80%/20%, 53.6/13.1, one-sixteenth — was re-verified by
me against its live source this run and held. The guardrail claims
(staleness report, round check) were reproduced by running them.
approve.
