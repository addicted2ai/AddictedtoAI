# Review: PR #60 — blog "one limit" count drift, seven becomes eight

Verdict: approve

The count is true, the sweep is exhaustive, the correction is honest and in
scope, and the machinery holds. Nothing in this branch should block merge.

## Verified (command + observed output)

1. **Head and scope.** `gh pr view 60 --json headRefOid` →
   `e9f5b3d5f93dd59ae713747238e2f18b6efbfa42` (matches
   `git ls-remote origin refs/heads/loop/maintain/blog-one-limit-count-drifts`).
   `git diff origin/main <head> --stat` → 5 files changed: `CHANGELOG.md`,
   `app/blog/page.js`, `app/lib/page-origins.js`,
   `docket/done/2026-08-14-blog-one-limit-count-drifts.md` (rename from
   open), `docket/open/2026-08-14-render-one-limit-count-from-sweep-output.md`
   (new). All inside maintain scope (`app/`, `docket/`, `CHANGELOG.md`;
   prompts/README.md table: maintain = build = app, public, scripts,
   package*.json, docket, CHANGELOG.md).

2. **The exhaustive sweep, re-run by me this session.** `gh pr list --state
   merged --limit 100` → 57 merged PRs (#33, #43 closed-not-merged). For
   every merged PR, `gh api repos/addicted2ai/AddictedtoAI/commits/<headRefOid>/check-runs`
   read from the PR head (not the merge commit). Observed totals:
   failure=9, success=26, no-run=22, total=57. The failing heads are exactly
   {23, 25, 27, 39, 40, 42, 50, 52, 58}. With #23 as the documented
   exception, the merged-over-failing-check set is exactly
   {25, 27, 39, 40, 42, 50, 52, 58} — eight. The "other 48 pass or predate
   the check" universal holds over the full list: 26 pass + 22 predate
   (#1-#22 have no run) = 48, plus the #23 exception + 8 counted = 57.
   No API failures in my sweep data (no API-FAIL markers).

3. **#58 specifically.** headRefOid `d52854db1f2cbce54df4941b61f65f2e73a979a3`;
   its check-runs: `human-owned-paths` failure, completed_at
   2026-08-14T17:47:38Z; `gh pr view 58 --json mergedAt,mergeCommit` →
   mergedAt 2026-08-14T17:54:20Z (after the failing run completed), merge
   commit `a1108a7d…`, mergedBy `addicted2ai`. `gh pr diff 58 --name-only`
   → `prompts/orchestrator.md` only (human-owned path). Reviews: 0
   non-comment; timeline: 0 auto-squash/auto-merge/force-push events.

4. **The same shape for the other seven.** For each of #25, #27, #39, #40,
   #42, #50, #52: failing run completed before mergedAt (e.g. #50
   13:08:50Z → 13:11:59Z, #52 13:46:31Z → 13:53:35Z, matching the entry's
   timestamps exactly), 0 reviews, 0 auto-merge/force-push timeline events,
   merged by `addicted2ai`.

5. **#23 exception.** Round 97's entry documents it (CHANGELOG.md,
   2026-08-14): "#23 created the check and was merged before
   `human-owned-paths` was in the required list". #23's own PR body
   confirms it created the check. Exception claim is sourced, not assumed.

6. **Blog passage.** `app/blog/page.js` on the branch: both passages say
   "eight" and name {25, 27, 39, 40, 42, 50, 52, 58}; the "snapshot that
   keeps moving" framing is kept with the new history (two → five → seven →
   eight, #58 at 17:54 UTC) and the 14 August re-verification date. No
   claim of stability or finality. The only remaining "seven" is the
   historical "seven by that evening", which is correct.

7. **Changelog.** 104 dated entries after `## Log` = round 104.
   `git diff origin/main <head> -- CHANGELOG.md` is 75 insertions, 0
   deletions — no past entry rewritten (rule 5 holds). Origin: delegated,
   Track: maintain, Agent: opencode (deepseek-v4-flash) present; the
   delegated note matches the five preceding delegated rounds; **1.** block
   has Hypothesis/Change. Round 101's "seven" entry is untouched and this
   entry names what it corrects.

8. **Docket.** `docket/done/2026-08-14-blog-one-limit-count-drifts.md`:
   all three boxes ticked, "Shipped" note consistent with the post. New
   `docket/open/2026-08-14-render-one-limit-count-from-sweep-output.md`:
   frontmatter has track/filed-by/title/created/expires/serves/priority;
   values pass check-docket.mjs rules (build is an advancing track, serves
   `more-true` is a valid non-floor value, expires > created); 6-item
   unchecked "Done when" checklist. Track choice is right: the item is a
   mechanism change needing `scripts/` + `app/` + a guardrail, which is
   build's scope and charge ("make the site do something it could not
   before"), and it genuinely machine-derives the count (renders from a
   checked-in sweep output with a guardrail), not another manual sweep.

9. **Machinery.** `node scripts/round.mjs check` on the branch: npm run
   lint ok, docket valid ok, track scope ok, npm run build ok, all route
   checks ok — nothing SKIPPED. `app/lib/page-origins.js` moves `/blog` to
   104 with a consistent comment (posts.js untouched, / stays 103).

## Problems

None found.

Minor notes:
- The brief's head SHA `e9f5b3d5d7e10d0ddcb89e4f9dd53c1d0d1a98ca` is wrong
  — it does not exist in this repo (`git cat-file -t` fails). The actual
  head is `e9f5b3d5f93dd59ae713747238e2f18b6efbfa42`; this review is of
  the actual head, and the verdict file is named for it. A wrong brief,
  not a wrong PR.

## Evidence (API calls / fetches, 2026-08-14)

- `gh pr view 60 --json headRefOid,headRefName,state,title,mergedAt`
- `gh pr diff 60`
- `git ls-remote origin refs/heads/loop/maintain/blog-one-limit-count-drifts`
- `gh pr list --state merged --limit 100 --json number,headRefOid,mergedAt`
  (57 results)
- `gh pr list --state closed --limit 100` (#33, #43 closed-not-merged)
- `gh api repos/addicted2ai/AddictedtoAI/commits/<head>/check-runs` for all
  57 heads (per-PR files under the opencode temp dir)
- `gh pr view 58 --json number,title,headRefOid,mergedAt,mergeCommit,state,mergedBy`
- `gh pr diff 58 --name-only`
- `gh pr view 23 --json title,mergedAt,body`
- `gh pr view {25,27,39,40,42,50,52,58} --json reviews` + `gh api
  .../issues/<n>/timeline` (reviews/auto-merge/force-push counts)
- `git diff origin/main <head> --stat` and `-- CHANGELOG.md` (0 deletions)
- `git show <head>:app/blog/page.js`, `:CHANGELOG.md`,
  `:docket/done/…`, `:docket/open/…`
- `node scripts/round.mjs check` (all groups ok)
- `scripts/check-docket.mjs` source read for frontmatter validation rules
- `prompts/README.md` scope table, `CHARTER.md` track charges

Not checked: whether the count drifts again before the machine-derived
rendering lands (the entry itself says it is not yet measured); the
`enforce_admins` setting on GitHub (read from the record, not re-fetched —
out of scope for this branch).
