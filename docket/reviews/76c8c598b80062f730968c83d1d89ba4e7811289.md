# Review: PR #96 — round 137 corrected record (build)

Commit: 76c8c598b80062f730968c83d1d89ba4e7811289
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 137

Review of the corrected head of `loop/build/unfreeze-shallow-clone-count`
after the fix round amended the round-137 entry (the first draft's failure
enumeration and depth claim were false). Reviewed by running commands, not by
re-reading the entry. Every claim in the brief was verified by measurement.

## Head and scope

- `git fetch origin loop/build/unfreeze-shallow-clone-count`; HEAD ==
  `76c8c598b80062f730968c83d1d89ba4e7811289`, working tree clean.
- `git diff --name-only 4031c58..HEAD` → only `CHANGELOG.md` and
  `docket/done/2026-08-15-site-frozen-third-freeze-rounds-134-135.md` — the
  amendment touched nothing else.
- `git diff --name-only origin/main...HEAD` → exactly the 7 in-scope paths
  (CHANGELOG.md, the docket item in open and done, and the four scripts).
- `git diff origin/main...HEAD -- CHANGELOG.md | grep -c '^-'` = 1, and that
  single match is the diff header `--- a/CHANGELOG.md` — zero lines deleted
  from any entry. The amendment's 16 `-` lines sit entirely inside the
  unmerged round-137 draft entry (old-file lines 114 and 159), never
  published, so rule 5 holds.
- Entry still carries `Origin: delegated`, `Track: build`, `Agent: opencode
  (deepseek-v4-flash)`; one round-137 entry, no renumbering.

## Claims verified by running

1. Vercel log readable, no credentials: `npx --yes vercel inspect
   dpl_Hnipa5aKBn53KVhAMx1p1bFZS487 --logs` printed the full build log of
   `993f006` (clone line: "Branch: main, Commit: 993f006", 758 ms) with no
   login or credential step. Same for `dpl_Cdb3gCcCqJ6WvUjovR4ZfG5WZnDn`
   (commit 756a58a).
2. The log ends exactly as quoted:
   `FAIL  rounds_merged: snapshot says 125, the changelog has 0 round entries as of 2026-08-15T18:46:41.179Z`
   and `FAIL  re-run node scripts/loop-history.mjs --snapshot to take a fresh one — do not edit the numbers by hand`,
   then `Error: Command "npm run build" exited with 1`. Identical two lines
   in 756a58a's log.
3. Root-cause mechanism reproduced: in pre-fix shallow clones the anchor
   `git log HEAD --before=2026-08-15T18:46:41.179Z -1 -- CHANGELOG.md` comes
   back empty at the depth boundary and `countRoundsAsOf` returns 0.
4. Fix side (depth-11 clone of `4031c58`, `fix11`): exit 0 with `ok
   rounds_merged matches the changelog: 125 round entries`; with
   `HTTPS_PROXY=http://127.0.0.1:9` exit 0 with the loud degrade WARNs
   ("neither this checkout's history nor the GitHub API could produce it");
   snapshot edited to `rounds_merged: 999` exits 1 ("snapshot says 999, the
   changelog has 125"), restored to 125 afterwards.
5. Guard both directions: `bash scripts/check-prebuild-single-branch.sh`
   exits 0, green (prebuild passes in the `--shallow-since` shape, head
   76c8c59) and red (deleting `return "HEAD";` makes the chain fail with
   `bad revision 'origin/main'`).
6. Full chain: `npm run prebuild` exit 0, `npm run build` exit 0, `npm run
   lint` exit 0 (0 warnings/errors).
7. Enumeration re-measured from the API this session (100 deployments +
   newest production status each): the window `d709a7b`→`993f006` holds
   exactly ten failures and two successes, every sha, status timestamp and
   state matching the entry: failures `d709a7b` 19:14:03Z, `362c0b9`
   19:51:36Z, `d8b2c23` 20:33:04Z, `07e5a5c` 21:14:59Z, `bdf5a71`
   21:38:04Z, `5104a16` 23:04:58Z, `14dc95d` 23:46:37Z, `756a58a` 01:46:39Z,
   `19cb78d` 03:14:36Z, `993f006` 04:18:40Z; successes `41809ea` 00:14:02Z,
   `1468e81` 00:50:36Z. `7b7aa02`'s failure (18:16:59Z) precedes the window;
   the last fourteen production deployments (`6ec241d` through `993f006`)
   hold eleven failures. The three shas the first draft omitted are all
   present. I did not falsify any sha, timestamp or state.
8. Depth experiments re-run with pre-fix code (tree at `993f006`):
   - `git rev-list --count 7b7aa02..X` = 9, 10, 11, 12, 13 for `41809ea`,
     `1468e81`, `756a58a`, `19cb78d`, `993f006` — the record sits 9–13
     commits below, i.e. 10th–14th counting both endpoints, as claimed.
   - `756a58a` depth 10: anchor empty, `node scripts/check-loop-history-
     snapshot.mjs` prints the two FAIL lines, exit 1 (matches Vercel).
   - `756a58a` depth 11: anchor `6ec241d` (18:33:00Z, verified no
     CHANGELOG.md change — `git show --name-only` touches only the docket
     item and `check-publishing-quota.mjs`), counts 125, exit 0 — the
     shallow-walk pathspec quirk, exactly as written.
   - `41809ea` depth 10: anchor `7b7aa02`, counts 125, exit 0.
   - `1468e81` depth 10: anchor `6ec241d` (its boundary), counts 125,
     exit 0.
   Depth 10 fits every observation; depth 11 does not.
9. Result honestly stated: the entry's Result line is "not yet measured —
   the acceptance test is the next production deployment", and the docket
   item's first box is left unticked for the same reason.
10. Supporting claims also held: `14dc95d`'s log (`dpl_BWkQdrukRLubrzj77sEutptuyvA1`)
    shows `fatal: bad revision 'origin/main'` — the round-#90 class, not the
    count-0 class, as the entry says. Live `/log` serves round 133, and
    `node scripts/check-deployments.mjs` exits 1 naming `993f006`.

## Defect-class hunt

The correction was hunted the same way the original was: every new number
was re-produced from the API or from fresh command runs, including the
depth-10/11 boundary fit that contradicts the first draft. No claim written
from intent instead of measurement was found. The only discrepancy with the
brief worth recording: `vercel inspect ... --logs` exits 1 when the
deployment failed (the CLI returns the deployment's own error state) — the
log content is fully readable and that exit code is the deployment's, not a
credentials problem; it does not affect the claims.

## Caveat

The acceptance criterion for the fix is the next production deployment,
which had not happened at review time; the entry says so itself.
