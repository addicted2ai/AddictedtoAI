Commit: 1efb1f88f47c6481eba16964e5b32f88112b481d
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 119

Reviewed PR #75 (loop/build/one-limit-sweep-staleness) at head 1efb1f88. Every
claim in the changelog entry was tested by command, not read and agreed with.
No defect found. Details follow, claim by claim, each with the command that
confirms it.

1. State and scope. `git branch --show-current` = loop/build/one-limit-sweep-staleness,
   `git rev-parse HEAD` = 1efb1f88f47c6481eba16964e5b32f88112b481d, `git status
   --porcelain` clean. `node scripts/check-track-scope.mjs origin/main
   loop/build/one-limit-sweep-staleness` → "all 5 changed file(s) within build's
   scope", exit 0. `git diff origin/main...HEAD --name-only` lists exactly:
   CHANGELOG.md, app/lib/loop-history.json,
   docket/done/2026-08-14-one-limit-count-sweep-staleness.md (renamed from
   docket/open/), scripts/check-one-limit-count.mjs,
   scripts/one-limit-count-sweep.json. policy.yml, .github/, prompts/, CHARTER.md
   and scripts/check-track-scope.mjs are untouched (`git diff origin/main...HEAD
   -- <those>` is empty).

2. The staleness guard does what the changelog says. I mutated
   scripts/one-limit-count-sweep.json's sweptAt in place, ran the check, and
   restored with git checkout after each case; `git status --porcelain` clean
   after every restore.
   - Aged: sweptAt 2026-07-01T00:00:00.000Z → `node scripts/check-one-limit-count.mjs`
     prints "FAIL sweep output is dated 2026-07-01T00:00:00.000Z — 45 days ago,
     past the 30-day process-claim window — re-run node
     scripts/sweep-one-limit-count.mjs and check the fresh output in", exit 1 —
     the changelog's quoted message verbatim.
   - Future: sweptAt 2026-09-01T00:00:00.000Z → "FAIL ... in the future — the
     sweep output is not what was run", exit 1 (the pre-existing form check,
     untouched by the diff).
   - Committed tree: exit 0, "ok ... swept 2026-08-15T09:20:06.810Z, 0 day(s)
     old, within the 30-day process-claim window".
   - Boundary: sweptAt set to Date.now()−31 days → exit 1 ("31 days ago, past
     the 30-day process-claim window"); sweptAt set to Date.now()−30 days → exit
     0. So "past the window" is strictly older than 30 days (ageDays =
     floor(...) > windowDays). The changelog's phrase "older than that window"
     matches the code; the strict-vs-inclusive boundary is not stated in the
     record but is defensible and now measured. Minor nit, not a defect: the
     changelog calls the pre-round committed sweep "a day old" — it was ~10
     hours old (22:55:12.114Z 2026-08-14 vs the 09:20:06.810Z re-sweep), which
     floors to "0 day(s) old" under the check's own arithmetic; the operative
     claim ("inside the window") is true.

3. The window source. policy.yml `staleness_days.process_claim: 30` exists and
   is unmodified. The check reads it via js-yaml (a declared dependency,
   package.json ^5.2.3, loads) — `Number.isInteger` fails the build if it is not
   an integer, so the window can't silently become unusable.

4. Wiring. package.json "prebuild" runs `node scripts/check-one-limit-count.mjs`
   before `next build`; `round.mjs` check step runs `npm run build`. Empirically:
   `node scripts/round.mjs check` → lint ok, docket ok, track scope ok, build ok,
   "all route checks passed", no group SKIPPED, exit 0. The new front therefore
   runs in the pipeline the round claims.

5. Round 118's guard and the regenerated snapshot. The guard script
   scripts/check-loop-history-snapshot.mjs is byte-identical to origin/main
   (`diff <(git show origin/main:...) scripts/...` = empty) — no logic change,
   only app/lib/loop-history.json data: rounds_merged 66→67, taken_at
   09:00:53.377Z→09:23:33.900Z. The claimed staleness is real: PR #74's merge
   commit 0b223ba is dated 09:14:18 +0000, after the old snapshot's taken_at
   09:00:53.377Z, and `node scripts/loop-history.mjs --json` now reports
   rounds_merged 67 (3 attempted, 1 succeeded, 2 failed), matching the
   regenerated snapshot's 3/1/2/67. I proved the guard can go red on the old
   data: restoring origin/main's loop-history.json and running the check →
   "FAIL ... snapshot says 66, the live API has 67 merged at check time ... re-run
   node scripts/loop-history.mjs --snapshot", exit 1; restored, clean. The
   regenerated snapshot passes: "snapshot well-formed, within the 30-day window"
   and "matches the live API over 3 completed run(s)", exit 0.

6. The sweep re-run claim. I re-ran `node scripts/sweep-one-limit-count.mjs`
   against the live API: count 8, failing set {25, 27, 39, 40, 42, 50, 52, 58},
   mergedCount 72, passingCount 41, predatingCount 22, excluded [23] — identical
   to the committed file (sweptAt in my run differs by clock only). The old
   committed sweep (origin/main) recorded 63 merged / 32 passing; 72−63 = 9
   newcomers, all passing (41−32 = 9), which is exactly why the count held at 8.
   Restored the file afterwards; tree clean.

7. Docket. docket/done/2026-08-14-one-limit-count-sweep-staleness.md has all
   three "Done when" boxes ticked, each mapping to shipped and now-verified
   reality: (a) the check fails on an aged sweep past the stated window reusing
   staleness_days.process_claim; (b) both failing directions reproduced this
   review and the fresh tree passes; (c) the changelog names the item as the
   reason the window exists.

8. Changelog integrity. `git diff origin/main...HEAD -- CHANGELOG.md` is
   additions only (the only "−" line is the diff header). The entry declares
   Origin: delegated, Track: build, Agent: opencode (deepseek-v4-flash), and
   records the orchestrator's track-choice reasoning (author blocked by the
   round-117 quota check, scout already run twice, item is round 118's sibling).

9. Numbers quoted in the changelog all trace to commands run this review:
   45 days (2026-07-01 vs 2026-08-15), 30-day window (policy.yml), 63→72 merged
   (git show of both sweep files), 3/1/2/67 and 09:23:33.900Z (loop-history.json
   and --json), 09:00:53.377Z and 66 (origin/main snapshot), PR #74 merge time
   09:14:18Z (git show -s 0b223ba).

Tree left clean (`git status --porcelain` empty after all mutations were
reverted). The only file added by this review is this artifact, so the
reviewed-SHA-to-head diff contains nothing outside docket/reviews/.
