Commit: 0897c429238981f39a3fd73442447cbb5dd7451c
Verdict: approve
Reviewer: claude-sonnet-5 (Claude Code subagent)
Round: 183

## Scope

Follow-up review of round 183 (`loop/meta/loud-origin-exemption`) after a
one-word fix landed on top of the commit I reviewed as
`de9fb9db8735d4293d90d4c8cd70df62d983edda`
(`docket/reviews/de9fb9db8735d4293d90d4c8cd70df62d983edda.md`, verdict
approve). That fix corrects a citation defect my prior review flagged:
"Round 16/179" — an unsourced session-relative round number that had leaked
into the permanent, project-numbered `CHANGELOG.md` — corrected to plain
"Round 179", matching how every other entry in the file cites rounds.

This review is written to stand on its own (see "Note on the prior review
artifact" below for why), so it restates the substance rather than pointing
at a file that is not currently reachable from this branch.

## 1. The one-word delta — confirmed, and confirmed to be the only delta

    git diff de9fb9d 0897c42 --stat
      CHANGELOG.md | 2 +-
      1 file changed, 1 insertion(+), 1 deletion(-)

    git diff de9fb9d 0897c42 -- CHANGELOG.md
      -**Round 16/179's covering-approve fix, re-verified, not regressed:**
      +**Round 179's covering-approve fix, re-verified, not regressed:**

Checked the full-repository diff (`--stat`, not just the `CHANGELOG.md`
diff), not only the file the coordinator named, to confirm nothing else
moved: exactly one file, one line changed, one word removed ("16/"). No
script, docket item, or any other file touched.

## 2. `node scripts/round.mjs check` — re-run on the new head, still fully green

Ran directly in the foreground, no backgrounding, on `0897c42`:

    === Static checks ===
      ok    npm run lint
      ok    docket valid
      ok    track scope for loop/meta/loud-origin-exemption
    === Build and serve ===
      ok    npm run build
    === Route checks ===
      ok    all route checks passed
    === Ready to ship ===
      node scripts/round.mjs ship

A text-only edit inside a fenced/quoted `CHANGELOG.md` heading does not
touch anything `check` validates (lint targets `.js`/`.jsx` under `app/`,
docket validation reads `docket/`, track scope reads the branch name, the
build and route checks do not parse `CHANGELOG.md` prose) — green here is
exactly what should happen and confirms the edit disturbed nothing.

## 3. The substantive findings from the prior review — re-affirmed, not re-derived from zero

Nothing under review changed except the one word above, so the findings from
the prior review stand without new derivation needed for most of them; I
re-confirm each holds against `0897c42`, since that's what this artifact
needs to cover:

- **Central finding**: `scripts/check-review-artifact.mjs` at base `7fb2832`
  has no code path that reads Origin and skips the covering-approve
  requirement — verified directly against the file's text, confirmed round
  179 (`b82a2bb`, PR #144) is a real ancestor of the base and is what removed
  the exemption. Unaffected by this fix (no diff to this file between
  `de9fb9d` and `0897c42`).
- **Anchor-drift bypass**: traced in full; can only ever subtract trust
  (force `shouldArm` false), never grant it — `originAllowsAutomerge` plus
  the unconditional review-artifact requirement form a ceiling the anchor
  mechanism cannot exceed regardless of whether `start()` was called.
  Unaffected (no diff to `scripts/round.mjs`).
- **Demonstration**: reproduced independently in a from-scratch git
  construction — Case A (Origin: supervised + covering request-changes)
  fails for the right reason; Case B (genuine covering approve) passes for
  the right reason. Unaffected (no diff to the checker).
- **Counts**: re-derived via `app/lib/build-log.js` against a worktree at
  base `7fb2832` — 135 declared-Origin entries, 43 non-delegated (18
  supervised / 14 maintainer / 11 unsupervised), exact match. Unaffected (no
  diff to any changelog entry other than the one word above, which is deep
  inside prose, not a parsed field).
- **`test-review-artifact.mjs`**: 11/11 pass, confirmed by direct re-run
  against the current tree. Unaffected (no diff to the checker or the test
  file).

## 4. The two minor findings from the prior review — still accurately non-blocking

Both were about content the coordinator's fix did not touch, so both stand
exactly as previously characterized:

- **Docket checkbox convention inconsistency** (the first docket item's
  "Done when" boxes stay unchecked despite its note claiming most are
  satisfied): the fix touched only `CHANGELOG.md`; both docket items are
  byte-for-byte unchanged between `de9fb9d` and `0897c42`
  (`git diff de9fb9d 0897c42 -- docket/` produces no output). Still a minor,
  non-blocking documentation-hygiene nit.
- **"3 of 5 vs. 4 total" brief-vs-actual mismatch**: this was about the
  guardrails section of the `CHANGELOG.md` entry, a different paragraph from
  the one word that changed. Confirmed unaffected — the only hunk in the
  diff is the "Round 16/179" line quoted in §1. Still correctly attributed to
  the review brief's own wording, not a fault of the round.

## Note on the prior review artifact — a real finding, not a formality

The coordinator's message described the prior artifact
(`docket/reviews/de9fb9db8735d4293d90d4c8cd70df62d983edda.md`) as staying
"as-is, byte-identical." Checked this directly rather than assuming it:

    git cat-file -e 0897c42:docket/reviews/de9fb9db8735d4293d90d4c8cd70df62d983edda.md
      fatal: path does not exist in '0897c42'

It is not present in the tree at the new head, not present in the current
working directory, and not on `origin` (I was correctly instructed not to
push it after committing it locally as `e60c904`). The local reflog in this
shared working directory shows a `reset: moving to origin/loop/meta/loud-
origin-exemption` landing at `de9fb9d` — before the coordinator's own
`0897c42` commit — which is consistent with a fetch+reset to the
then-current `origin` state (which never had my unpushed `e60c904`)
happening in this same checkout before the one-word fix was made. The
`e60c904` commit object itself still exists locally (`git cat-file -e
e60c904` succeeds) but is unreferenced by any branch and will not survive
indefinitely.

Practical consequence, checked directly:

    node scripts/check-review-artifact.mjs origin/main
      Origin: delegated — no Origin exempts a branch from this check; requiring a review artifact that covers the merged tree
      exit code: 1 (no covering review artifact — all existing docket/reviews/ files on this
      branch belong to already-merged/squashed trees and are informational only)

As of right now, before this file is committed, this branch carries **zero**
covering review artifacts and would fail its own required `review-artifact`
gate. This is not a defect in the round's code — it is a gap in how a
reviewer's commit survives a shared-working-directory reset when the
reviewer is (correctly) told not to push. This file, once committed, is
itself the covering artifact this branch needs; whoever pushes next should
push this commit along with it, or the branch will still fail
`review-artifact` in CI despite the substance below being sound. Surfacing
this plainly rather than letting "byte-identical" stand uncorrected.

## Verdict

The one-word fix is exactly what it was described as: a text-only
correction to an already-unmerged, not-yet-permanent entry, touching nothing
else. `round.mjs check` is green on the new head. Every substantive finding
from the prior review continues to hold against `0897c42` with no new logic
to re-derive. Approving `0897c429238981f39a3fd73442447cbb5dd7451c` on that
basis — with the one caveat above (the prior review artifact's absence from
the branch) flagged for whoever pushes next, since it affects whether this
branch can currently pass its own required gate, independent of anything
this review found wrong with the work itself.
