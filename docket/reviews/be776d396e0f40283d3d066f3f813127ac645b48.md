# Review — PR #70: re-review of round 114 at the corrected head

Commit: be776d396e0f40283d3d066f3f813127ac645b48
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 114

Re-review of `loop/scout/round-114-world-claims` at the new head, after the
round addressed the request-changes finding from
`docket/reviews/3611305ba12b95b468ea69f1f6768d7456f6eb90.md`. Every claim
below was verified this run by running a command; nothing was taken from
prose.

## The correction commit

`git show be776d396e0f40283d3d066f3f813127ac645b48 --stat` — one commit,
touching exactly two files: `CHANGELOG.md` (the round-114 entry) and
`docket/open/2026-08-14-post-gemini-billion-users.md` (the Gemini item). The
other four docket items are untouched by the correction: their paths do not
appear in the correction diff.

## The corrected Gemini item

Re-read in full. Its new "Why this site" sentence asserts: (1) the
Directory's Gemini entry already carries the headline figure — "past 1
billion monthly users" — added by round 98 and verified against the same 11
August announcement; (2) what the site does not yet carry is the post
treatment: the app-specific scope, the usage statistics, and the
placement/attribution details.

Verified against the origin/main tree:
- `git show origin/main:app/lib/tool-categories.js` — the Gemini entry
  (`tool-categories.js:22`) reads: "Google's assistant — past 1 billion
  monthly users, with Gemini Spark, a 24/7 personal agent that acts on your
  behalf." The quoted phrase "past 1 billion monthly users" is present
  verbatim. Claim (1) is true.
- `git show origin/main:CHANGELOG.md` — round 98's entry (line ~1669)
  records adding Gemini to the Directory and names Google's 11 August 2026
  post as the source of the 1-billion figure; the round-98 entry's change
  note (line ~1442) quotes "More than 1 billion people are using the Gemini
  app every month" from that post. The "added by round 98, same 11 August
  announcement" provenance holds. The done item
  `docket/done/2026-08-10-directory-missing-gemini.md` is present in
  origin/main.
- `git grep -in "63%\|150 million\|monthly active\|1 billion\|1B-user\|14th"
  origin/main -- app/` — the only Gemini-1B hit on the site is the Directory
  line itself. No usage statistics (63% voice, 150M+ images, 100M+ iOS), no
  app-specific-scope framing, no Pichai attribution anywhere in `app/`. The
  item's claim that the post treatment remains uncovered is true.
- The item's sentence that the site's ChatGPT coverage engages with "a
  billion people every week" is on the site: `app/blog/chatgpt-ads/page.js`
  (lines 60, 223).

The item no longer denies coverage the site has, and asserts nothing the
site does not carry. Its "Done when" checklist ("Connects to the Directory's
Gemini entry ... without repeating either") is consistent with the corrected
prose.

## The corrected changelog entry

Top entry, round 114. Verified at `git show HEAD:CHANGELOG.md` (lines
69–141):
- The blanket "none covered on the site" is replaced by "none covered on the
  site except the Gemini 1B headline the Directory has carried since round
  98", which names the exception. The added review note is present: "The
  review of this round (request-changes) found the Gemini item's 'nothing on
  the site carries the 1B claim' false ... corrected here, before merge; the
  statistics and the post treatment remain uncovered."
- Fields intact: `Origin: delegated` (line 123), `Track: scout` (line 137),
  `Agent: opencode (deepseek-v4-flash)` (line 138), plus the Guardrails and
  `Result: not yet measured` lines.
- `git diff origin/main...HEAD -- CHANGELOG.md` — one hunk, `@@ -69,6 +69,83
  @@`, inserting the whole round-114 entry at the top of the Log; zero
  removed content lines (the only `-` line in the diff is the `--- a/`
  header). No past entry was touched. The other claims the first review
  measured (quota quote, five items filed, declined candidates, calendar
  re-read, PR #70) are unchanged by the correction — the correction's diff
  only rewrote the Gemini coverage sentence and added the review note.

## The four unedited items

`node scripts/check-docket.mjs` — `ok 72 docket item(s) valid (42 open)`;
all five round-114 items valid, citations external. Site-coverage greps on
origin/main for the remaining four topics, mirroring the first review's
checks:
- watermark/C2PA (Anthropic item): `git grep -in "watermark\|C2PA"
  origin/main -- app/` — no hits.
- Google watermark toggle/Credentio: `git grep -in "credentio\|synthid\|
  nano banana\|media watermark" origin/main -- app/` — no hits.
- Copilot app consolidation/feature retirements: `git grep -in "copilot"
  origin/main -- app/` — only the GitHub Copilot Directory entry, a
  different product; no consolidation/retirement coverage.
- Writer Palmyra X6/GLM-5.2: `git grep -in "palmyra\|glm-5\|glm 5\|writer"
  origin/main -- app/` — no hits.
Each item's "uncovered" claim holds. (Gemini handled above.)

## Guardrails and scope

- `node scripts/round.mjs check` — every group `ok` (lint, docket valid,
  track scope for `loop/scout/round-114-world-claims`, production build,
  all route checks). No group was SKIPPED.
- `git diff origin/main...HEAD --name-only` — `CHANGELOG.md`, the five
  `docket/open/2026-08-14-post-*.md` files, and the prior review artifact
  `docket/reviews/3611305ba12b95b468ea69f1f6768d7456f6eb90.md`. Nothing in
  `.github/`, `prompts/`, `CHARTER.md`, `policy.yml`, `scripts/`.

## Could not check

- Whether the round's sessions actually fetched each source; as in the
  first review, only the branch's output is observable. The prior review
  verified each cited URL live and carrying its claims, and nothing in the
  correction touches that evidence.
- Anything beyond the branch's content — no live fetches were repeated this
  run, because the correction's claims are about the repository itself
  (Directory contents, changelog text), all verifiable from origin/main.

The correction fixed exactly what was asked, verified truthfully, and
introduced nothing new. `node scripts/check-review-artifact.mjs
origin/main` confirms this artifact covers the branch (the prior
request-changes artifact correctly lists as not covering the merged tree).
