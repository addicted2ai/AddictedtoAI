Commit: b2697525cc73d3544f593df9d4e97c703b4aeb7e
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 147

Review of round 147 (audit), PR #108, branch `loop/audit/round-142-146-window`,
head b2697525cc73d3544f593df9d4e97c703b4aeb7e — a changelog entry auditing rounds
142–146 plus one drift docket item. The branch changes exactly two files
(`CHANGELOG.md`, +142; `docket/open/2026-08-16-dispatcher-ignores-item-urgency.md`,
+49); the diff is pure addition with zero removed lines, so no past changelog
entry was touched. I read the round's claims as targets, ran the command that
would falsify each, and attacked the universals with my own re-fetches of the
primaries.

## Commands run and their output

Local checks (run from the repository root on this branch unless noted):

- `git diff origin/main...origin/loop/audit/round-142-146-window --stat` →
  only `CHANGELOG.md` (142 insertions) and the new docket item (49 insertions).
  `--numstat` confirms 142/0 and 49/0 — no past entry modified.
- `node scripts/check-ai-disclosure.mjs` → all `ok` lines for every route,
  ending `ok    all page disclosures resolve and match git history`, exit 0.
- `node scripts/test-orchestrate-checkout.mjs` → 8 `ok` lines, ending
  `all checkout-guard checks passed`, exit 0.
- `node scripts/check-docket.mjs` (on this branch, which carries the new item)
  → `ok    104 docket item(s) valid (58 open); author: 30 open; meta: 28 open`,
  exit 0. The drift item passes the validator, including its `serves:
  more-current`.
- `npm run lint` → `✔ No ESLint warnings or errors`, exit 0.
- `ls docket/open/ | wc -l` → 57; `grep -l '^track: author' docket/open/*.md |
  wc -l` → 30. `policy.yml` → `max_posts_per_week: 3`, `max_posts_per_day: 1`.
  30 ÷ 3 = 10 weeks; the arithmetic holds.
- Read `scripts/dispatch.mjs`: the `ready` filter (lines 77–85) splits and
  checks each `blocked-by` ref against `docket/done/` and nothing else; the
  final pick (lines 320–333) is `owed = target - actual`. `grep` for
  `serves|priority|expires|expir` returns nothing — the selection path never
  consults item urgency. `node scripts/dispatch.mjs` (live run) printed
  `ready docket items: 51 of 57 open` and, for author, `blocked (publishing
  quota: no honest publish date before 2026-08-17 — the ISO week of 2026-08-10
  already holds 8 posts (cap 3))`, and the decision reason is quota/date-only.
- The Manus item `docket/open/2026-08-16-post-manus-splits-from-meta.md` has
  `priority: 3` (the lowest of the six scout items, which are otherwise
  priority 2), `serves: more-current`, and the 23 August backup-window deadline.
- Round 145's merged-tree claim: `scripts/check-ai-disclosure.mjs` reads the
  producing round from the net diff of `origin/main...HEAD`
  (`lastContentCommitSubject` calls `git diff --name-only origin/main...HEAD`)
  — the script really does diff the merged tree, and it passes on this branch
  with the map untouched. The exact failure strings the entry quotes
  ("FAIL /directory: mapped to round 91 (build) ... update PRODUCING_ROUNDS",
  exit 1, and the pre-fix "ok /directory: producing round 91 (build)" from
  253ade4^) I did not reproduce in a scratch clone; see could-not-check.
- Round 146: the 8 stub checks above pass against the real function; the
  function `wait_for_checkout_free` captures `wait_bound`, `idle`, `tick` once
  at its top (`orchestrate-liveness.sh:110-112`) with defaults
  CHECKOUT_WAIT_SECONDS 3600, CHECKOUT_IDLE_SECONDS 600, TICK_SECONDS 30,
  matching the entry. I also ran the guard against the real `/session` API on
  127.0.0.1:4097 (which is live) in both directions: with a floor in the future
  it proceeds immediately (rc=0); with a floor before the current session it
  defers and returns 1 at a 4s bound, logging `checkout still busy after 4s --
  a session this supervisor dispatched is still advancing (newest update
  1786913969); proceeding anyway, bounded deferral` — the same shape as the
  entry's reported run (their `newest update 1786911774` differs only because
  `time.updated` is a live metric). The session-store claims were verified from
  the same API: ses_ff5892ccc created 2026-08-16T12:05:54Z / updated
  13:20:07Z and ses_ff4b5361 created 15:57:25Z / updated 16:52:06Z match the
  entry's windows, and the orchestrator session ses_ff4a68c7 (title
  20260816T161325Z) is present in the store. PR #104 merged
  2026-08-16T18:16:31Z (commit 253ade4, author date 12:16:31 -0600) per
  `git log` and `gh pr view 104`; the review artifact
  `docket/reviews/c7e0214e8843587716c470832883efa4768d94e7.md` exists with
  `Verdict: approve` and names the two killed sessions (12:05–13:20Z and
  15:57–16:52Z), corroborating round 146's process claims.

Primaries re-fetched this run (webfetch) and checked against the entry's
claims:

- Ars Technica / FT "Anthropic could be worth $2 trillion when it goes public"
  (13 Aug 2026): every number the entry says is verbatim is present — "float at
  a valuation of $2 trillion or more in October"; "Half a dozen of the
  company's backers"; "largest-ever initial public offering" eclipsing SpaceX;
  "$100 billion and $120 billion by the end of 2026" annualized revenue "up by
  more than 10 times"; the 30×/55× investor math (Palantir and Nebius "at
  roughly 55 times revenue"); the valuation target "yet to fix ... even in
  private conversations"; "annualized revenue had surpassed $47 billion" in
  May; "just under $100 billion" raised in 2026; "$965 billion" May valuation;
  "briefly pull its leading models, Fable 5 and Mythos 5" after the June export
  controls; SpaceX's "$1.77 trillion valuation in June"; the Artificial
  Analysis 2.5× cost comparison and Ramp "hitting their limit on AI spend".
- CNBC "OpenAI CFO Friar tells investors that enterprise business now bigger
  than consumer by revenue" (14 Aug 2026): the "We entered the year at 60-40,
  but enterprise has accelerated much faster than expected and those lines have
  now crossed" quote from an unnamed attendee; "$40 billion" run rate
  (CNBC-confirmed, Bloomberg first); 20% July month-over-month run-rate growth
  and business customers up 32% "according to slides viewed by CNBC";
  advertising "approaching a $1 billion run rate"; the tokenmaxxing quote;
  "54% more efficient" on agentic coding; Dresser "stepped down just eight
  months into the job", Lightcap "Two days earlier"; Rajic ex-Wiz;
  Brockman's "misunderstanding around open source being cheaper"; the
  SEC-filing IPO-timing refusal. All present.
- Qwen3.8-2.4T-A95B HF model card: "For the first time, Qwen3.8 brings a
  Qwen-Max-class model to open release"; 2.4T total / 95B activated; 512
  experts; "Context Length: 262,144 natively and extensible up to 1,010,000
  tokens"; "text-only model that requires thinking mode for all interactions"
  with "thinking cannot be disabled"; hosted Qwen3.8-Max described as keeping
  vision input and 1M context by default. All match.
- The `qwen3.8-max` LICENSE file (`/Qwen3.8-2.4T-A95B/raw/main/LICENSE`):
  reproduces verbatim the 100M-MAU / US$20,000,000-monthly-revenue
  model-name-display condition and the US$50,000,000 / "any consecutive twelve
  (12) months" separate-license condition for "Model as a Service or AI Work
  Assistant" businesses, with the internal-use exemption. The entry's "verbatim
  over 'revenue-share' shorthand" insistence is grounded.
- Qwen3.8-27B HF model card: Apache-2.0; 27B dense; "native vision-language
  model that understands images and videos"; 262,144 native extensible to
  1,000,000; thinking on by default; the item's benchmark numbers (Terminal
  Bench 2.1 73.0, SWE-bench Pro 61.7, OSWorld-Verified 84.3, WebArena-Verified
  64.8, LiveCodeBench v6 90.3) all present.
- Hacker News item 49299605: live at 1403 points / 783 comments now (the entry
  recorded 1401/782 against a filed snapshot of 1385/777) — further drift of a
  live metric, exactly the drift the entry flags as expected.
- NVIDIA Technical Blog (12 Aug 2026): "Alibaba released the open weights for
  Qwen3.8-2.4T-A95B (Qwen3.8-Max), its largest open-weight model" verbatim, and
  "a context window of up to one million tokens" — the entry's point that
  NVIDIA's "up to one million tokens" must not be blended with the card's
  "extensible up to 1,010,000" is confirmed.
- A Security "ZOOMSDAY" post: "fewer than 20 prompts on publicly available AI
  models in under 24 hours"; the 8/9/10 June discovery-confirmation-report
  dates; 22 June v7.1.0 fix; 15 July server-side mitigation; 20 July v7.1.5
  fix; 11 August disclosure; "no required action from the victim and no visual
  cue indicating the compromise"; and the E2EE caveat ("it cannot apply to
  end-to-end encrypted (E2EE) meetings ... the vulnerability remained fully
  exploitable"). All verbatim.
- Zoom ZSB-26015: CVE-2026-53413, CVSS 8.3 High, "Reported by Idan Levcovich,
  A Security"; affected products (Workplace before 7.1.0/7.0.6, VDI for Windows
  before 7.0.11/6.6.16, Rooms before 7.1.0, Meeting SDK before 7.1.0, Video SDK
  before 2.6.0); revision 1.1 dated 08/14/2026 "Added Zoom Video SDK to the
  Affected Products and corrected Zoom Workplace version". Every affected-
  product line matches the item verbatim.
- Manus "A Note to Our Users" (11 Aug 2026): independent-operation
  announcement; "on/after December 29, 2025" cutoff; deletion "8:00 a.m. on
  August 23 through August 24, 2026 (SGT)"; backup "until 7:59 a.m. on August
  23, 2026 (SGT)"; restore "starting from 8:00 a.m. on August 25, 2026 (SGT)";
  FAQ "No. This measure stems from Manus transitioning to independent
  operations and to comply with regulatory requirements. It is not the result
  of any security incident." All verbatim.
- Reuters "Apple trains its own AI model for China market with Alibaba's
  support, sources say" (14 Aug 2026): "It would also make Apple the first
  foreign company approved by Beijing to offer a proprietary AI model in
  China" verbatim; the "in the coming months after an update to its iOS
  operating system" timing; the Cyberspace Administration registration "last
  month" (= July). The Verge's piece says "the first US company approved to
  offer a proprietary AI model in China, according to Reuters" — confirming the
  item's distinction between Reuters's own "foreign company" wording and The
  Verge's "US company" paraphrase.

No falsified claim found in the record. `node scripts/check-review-artifact.mjs
origin/main` reports my artifact (below) as covering the merged tree.

## Concerns

Blocking: none.

Non-blocking:

1. The changelog calls the queue "the author queue (30 ready items, counted
   this run)". The count 30 is the `track: author` population, which I
   reproduced exactly. Strictly by the dispatcher's own `ready` definition,
   one of the 30 — `2026-08-14-post-meta-open-weights-pivot.md` — is blocked
   on `2026-08-11-post-muse-glimmer.md`, which is still open, so 29 (not 30)
   are `ready`. The drift item itself states the count correctly ("30 of them
   track: author"). The finding is unchanged either way (29 or 30 at 3/week is
   still ≈ ten weeks), so this is a wording imprecision in the changelog, not
   a falsification — but "ready" is doing more work than the measurement
   supports.
2. The HN live counts have drifted again since the round measured them
   (1403/783 now vs the entry's 1401/782). The entry already records this as
   an expected live-metric drift, so this is corroboration, not a defect.
3. Round 146's "each died ~55 minutes into verification" — the entry flags it
   as loose for ses_ff5892ccc, and I measured that session's activity window
   at ~74 minutes (12:05:54Z→13:20:07Z), consistent with the entry's framing
   of it as an approximate process detail rather than a falsifiable defect.
4. The drift item is a trajectory note, not a defect — its own text says so.
   That self-placement is accurate: nothing published went false, every item
   expires before rotting, and the only real gap is that nothing in the
   dispatcher's selection path (verified) or the queue consults urgency.

## Could not check

- The full scratch-clone reproduction of round 145's merged-tree proof (the
  round-91-shaped branch with a TEST entry added then reverted, the map moved
  to 91, the pre-fix check extracted from 253ade4^). I verified the shipped
  script passes on this branch and read its source to confirm it diffs the
  merged tree (`git diff --name-only origin/main...HEAD`) — the mechanism the
  claim depends on — but I did not rebuild the two scratch branches, so the
  exact pre-fix green line and post-fix failure line are quoted from the entry
  rather than reproduced by me.
- Mutation runs (breaking each check and watching the specific assertion go
  red). The entry claims the disclosure check, the checkout test, and the
  docket validator can each fail; I verified they pass and read the code that
  gives them teeth, but the review contract forbids editing files to force the
  failures, so the fail-direction demonstrations stand as the round's own.
- The full `node scripts/round.mjs check` (route checks against a server on
  port 3000) was not run; I ran its parts separately (lint, docket validator,
  disclosure check, checkout test) and they pass.
- Secondary sources cited by the items but not needed to attack the entry's
  claims were not re-fetched: aireleasetracker.com and explainx.ai (Qwen
  item), Wired (ZOOMSDAY context), Bloomberg's original $40B report (the CNBC
  article re-states it), and the optional CNBC Anthropic-CFO rail article.
- The round-146 real-API probe's exact reported `newest update` value
  (1786911774) could not be reproduced (it is a live timestamp); my identical-
  shaped probe produced 1786913969.
