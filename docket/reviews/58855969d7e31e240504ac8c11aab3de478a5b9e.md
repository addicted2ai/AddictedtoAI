Commit: 58855969d7e31e240504ac8c11aab3de478a5b9e
Verdict: approve
Reviewer: claude-sonnet-5 (Claude Code subagent)
Round: 168

Scoped confirmation pass on `c9ccb48..5885596`, per the coordinator's
instruction: confirm the four named items and stop, not a sixth
adversarial review.

## 1. My own 248/5808 repro now returns zero wrong rows

Re-ran the exact two scripts from my last review, unmodified, against the
fixed module:

```
invisible-character insertion sweep: 5808 probes across 86 identifiers x 4 invisible chars
  wrong-row matches: 0
  still resolved to own row (lucky): 5808
  safe miss: 0

em-dash/en-dash/NBSP/apostrophe mid-identifier sweep: 5808 probes, wrong-row: 0
```

Every probe that previously produced a wrong row now recovers the correct
one. Confirmed.

## 2. No legitimate match was lost

`node scripts/check-model-deprecation-parser.mjs` → `all 401 checks passed`
(the pre-existing 400 plus one new aggregate assertion), reproduced
directly, not taken from the entry. Since this is the direction a fix like
this most plausibly breaks — the visible-separator path glues a "loose
run" that, by construction, can span far wider than one ambiguous
character (the pattern's alternation includes a literal space, so an
entire punctuation-light sentence can become one loose run before the
glued attempt is tried) — I went beyond re-running the shipped suite and
wrote my own natural-language probes to stress exactly that mechanism:

```
"We're still on gpt-4-0613 in prod and it's about to retire. We'll also
 need to migrate off gpt-3.5-turbo before it's gone too."
   -> gpt-4-0613, gpt-3.5-turbo (both correct)
"Our team—three engineers—still relies on gpt-4-0613 daily."
   -> gpt-4-0613 (correct; the em dash elsewhere in the loose run doesn't confuse it)
"gpt-4-0613—gpt-3.5-turbo were the two models in question."
   -> gpt-4-0613, gpt-3.5-turbo (correct, both recovered)
"The "legacy" model gpt-4-0613 is retiring soon."
   -> gpt-4-0613 (correct)
"We  still  reference  gpt-4-0613  in  prod  today." (NBSP as every space)
   -> gpt-4-0613 (correct)
```

No lost or wrong match in any of these. Confirmed independently, not just
by trusting the claim.

## 3. The `\p{Cf}` strip is not over-broad

Checked the data directly rather than reasoning about it: scanned every
field (`what`, `replacement`, `note`, `href`, `vendor`) of all 77
`RETIREMENT_DATES` rows for any `\p{Cf}` character, and separately
confirmed stripping `\p{Cf}` from every parsed primary/alias identifier
leaves it byte-for-byte unchanged:

```
No \p{Cf} character found anywhere in RETIREMENT_DATES's text fields --
stripping Cf cannot remove part of a real identifier.
identifiers whose OWN text is altered by the Cf strip: 0
```

Confirmed the strip cannot remove any part of a real identifier, because
no real identifier contains one of these characters in the first place.

## 4. The red-before-green arithmetic

Reproduced in a disposable `git worktree` (pre-finding-6 matcher from
`5933d28`, shipped health check pinned to read it):

```
868 of 1268 check(s) failed
```

exact match. Categorized every `FAIL` line: all 868 came from the new
Unicode sweep specifically, none from anywhere else, and broke down to
exactly 62 per character across all 14 characters tested (14 × 62 = 868,
no remainder). `62/1452 = 248/5808 = 0.0427` — the entry's claimed 4.27%
reproduces exactly, confirmed by direct division, not eyeballed. Exit code
1 pre-fix, confirmed. Worktree removed after.

Also spot-checked confinement since it's cheap: `check-track-scope.mjs` →
20 files, all build-scoped; `CHANGELOG.md` diff against `origin/main` →
907 insertions, 0 deletions, all hunks confined to round 168's own entry;
`npm run lint` → clean.

## Verdict

`approve`. All four items hold, independently reproduced rather than
trusted. The finding from my last pass is closed: the base-tokenizer split
that survived the dot-truncation fix (present since the tool's first
commit) is fixed at the root, correctly distinguishes invisible format
characters (stripped unconditionally, verified safe) from visible
ambiguous separators (glued-then-fallback through the same pipeline
findings 3–5 already built, verified against both the isolated
single-character sweep and my own multi-word natural-language probes), and
nothing regressed.
