Commit: 632ddfce7847e0daca653359787c413be72a67a9
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 159

Review of PR #124, `loop/maintain/correct-blog-required-checks-and-one-limit-count`,
head commit 632ddfc (correction commit), round 159 (maintain, Origin: delegated).
This is the correction round responding to the earlier request-changes review at
`docket/reviews/9ea5ebe691cad5d0f47d5272d45092d18841d9c9.md`. Both blocking
findings of that review are fixed at this head, and the two headline claims still
hold under measurement. Approve.

## Blocking finding 1 (stale PRODUCING_ROUNDS["/blog"]) — fixed

- Round arithmetic: `git show origin/main:CHANGELOG.md | grep -c '^### 20'` → 158;
  `grep -c '^### 20' CHANGELOG.md` on the branch → 159. This branch adds one entry
  over origin/main, so this is round 159. (The earlier review's "160" was wrong.)
- `git show origin/main:app/lib/page-origins.js` maps `/blog` to 154 (stale, the
  author round whose posts.js feeds the blog list). The branch's
  `app/lib/page-origins.js` maps `/blog` to 159 with a comment naming round 159
  and the maintain round's change to app/blog/page.js. Matches what the earlier
  review asked for.
- `node scripts/check-ai-disclosure.mjs` → exits 0; `/blog: producing round 159
  (maintain)`, and all other routes resolve. The route that previously failed now
  passes.
- Full gate `node scripts/round.mjs check` → no FAIL, no group skipped:
  - `ok npm run lint`
  - `ok docket valid`
  - `ok track scope for loop/maintain/correct-blog-required-checks-and-one-limit-count`
  - `ok npm run build`
  - `ok all route checks passed`
  - "Ready to ship"

## Blocking finding 2 (changelog parenthetical misdescribed its diff) — fixed

The corrected parenthetical in the 2026-08-17 entry says four things, all checked
against the diff and the API:

1. "the corrected passage's own 'and `enforce_admins` is false' sentence was
   removed and replaced with this phrasing." Origin/main's corrected passage
   (page line ~220) read "...the required checks are `build-and-audit` and
   `human-owned-paths`, and `enforce_admins` is false." HEAD's same passage reads
   "...the contexts carry `enforcement_level: non_admins`..." — the literal
   "and `enforce_admins` is false" is gone, replaced by the non_admins phrasing.
   TRUE.
2. "a separate, older sentence elsewhere on the page — the 11 August read in the
   'did not go far enough' paragraph — still says '`enforce_admins` is false'."
   `grep -n "enforce_admins" app/blog/page.js` → line 168, inside the paragraph
   beginning "That correction did not go far enough" (line ~152): "Read from the
   GitHub API on 11 August 2026: `required_approving_review_count` is 0 and
   `enforce_admins` is false." That sentence is unchanged in the diff. TRUE.
3. "the branch endpoint carries no `enforce_admins` field to verify." `gh api
   repos/addicted2ai/AddictedtoAI/branches/main` → `'enforce_admins' in
   b.protection` evaluates to `false` (field absent from the payload). TRUE.
4. "the `/protection` endpoint is denied on this harness, so `enforce_admins`
   itself could not be re-read." Not attempted (instructed not to). Consistent
   with the earlier review's identical note. Consistent with what the entry says.

The parenthetical now describes the diff as it is, not as the edit was meant to
be. The earlier review's finding 2 is resolved.

## Original round's claims at the corrected head — hold

- Required contexts from the branch endpoint:
  `gh api repos/addicted2ai/AddictedtoAI/branches/main` →
  `["build-and-audit","human-owned-paths","review-artifact"]` (three contexts),
  `enforcement_level: "non_admins"`, and no `enforce_admins` field
  (`'enforce_admins' in b.protection` → `false`).
- One-limit sweep: re-ran `node scripts/sweep-one-limit-count.mjs` → count 10,
  failing set [25, 27, 39, 40, 42, 50, 52, 58, 116, 123], mergedCount 121,
  passing 88, predating 22, excluded [23]. Identical to the committed file except
  `sweptAt` (my run 2026-08-18T00:32:24Z vs committed 2026-08-17T23:42:47Z).
  Arithmetic: 10 + 88 + 22 + 1 = 121. Restored the file with
  `git checkout -- scripts/one-limit-count-sweep.json`; `git status` clean.
- Rendered page: real `npm run build` + `npm run start` on port 3000 (server
  killed afterwards via taskkill on its PID; ports 3000 and 8101 left free; tree
  left clean). The rendered /blog HTML contains: "three required checks";
  "review-artifact joined the required list on 2026-08-17"; "the contexts carry
  enforcement_level: non_admins"; "merge past a red required check"; and the
  sweep sentence "counts ten (#25, #27, #39, #40, #42, #50, #52, #58, #116 and
  #123)". "two required checks" is absent. `node
  scripts/check-one-limit-count.mjs --rendered http://localhost:3000/blog` → ok,
  "rendered page carries the sweep sentence".
- Historical "was two on 11 August … eight by nightfall" sentences are untouched:
  the full PR diff for app/blog/page.js changes only the two passages the entry
  describes; `git diff origin/main...HEAD -- app/blog/page.js` shows no +/- lines
  touching those sentences, and they are present unchanged at lines ~211-214.
- Build-time sweep check on the committed file: "ok sweep output is internally
  consistent: count 10, 10 set member(s), swept 2026-08-17T23:42:47.265Z, 0
  day(s) old, within the 30-day process-claim window".

## Scope and hygiene — clean

- `git diff origin/main...HEAD --stat` → only CHANGELOG.md (60 +), app/blog/page.js
  (21 +, 8 -), app/lib/page-origins.js (5 +, 2 -),
  scripts/one-limit-count-sweep.json, and docket/reviews/9ea5ebe…md (the earlier
  review file, added this branch). Nothing under .github/, prompts/, CHARTER.md,
  scripts/check-track-scope.mjs, docket/open, docket/done.
- `node scripts/check-track-scope.mjs origin/main
  loop/maintain/correct-blog-required-checks-and-one-limit-count` → "all 5
  changed file(s) within maintain's scope".
- CHANGELOG.md diff is purely additive at the top: `--numstat` shows 60 added, 0
  removed. The correction edited only the not-yet-merged entry's parenthetical
  (that entry's own lines), leaving every older entry untouched. Append-only
  respected.
- `git status` clean after sweep re-run and server runs (verified both times).
- Entry has the required fields: Origin: delegated, Track: maintain, Agent:
  opencode (deepseek-v4-flash), plus Guardrails and Result lines per the shared
  format.

## Non-blocking notes

- The page still carries "`enforce_admins` is false" at line 168 as a dated
  11-August API read, and the branch endpoint carries no field that could verify
  or falsify it. The entry now says exactly this, so the residual is honestly
  recorded rather than a defect in the record. Same note as the earlier review
  carried.
- Minor: the page's line 168 and the changelog's own sentence about the branch
  endpoint are consistent: the branch endpoint omits `enforce_admins`; the page
  does not claim to have re-read it, only that the older 11-August read still
  stands unverified. Fine.
- The "Result: not yet measured" line is honest about the residual: no check
  compares branch protection to the site's claims, and the one-limit count will
  drift again by the time a next round merges. Stands as written.

## Could not check

- The `/protection` endpoint (denied on this harness; instructed not to attempt),
  so `enforce_admins` and `required_approving_review_count` themselves were not
  re-read. The entry claims only that the branch endpoint carries no
  `enforce_admins` field, which I verified, and that the /protection endpoint is
  denied on this harness, which I did not attempt.
- The review-artifact gate's verdicts on the merged tree (this review being the
  artifact under test; the earlier 9ea5ebe review correctly reports as not
  covering the merged tree since two files changed after it).