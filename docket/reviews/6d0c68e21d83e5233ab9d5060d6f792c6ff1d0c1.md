Commit: 6d0c68e21d83e5233ab9d5060d6f792c6ff1d0c1
Verdict: approve
Reviewer: claude-sonnet-5 (Claude Code subagent)
Round: 172

## Scope of this pass

Second-pass review of round 172, covering `git diff 39a5bbb..6d0c68e` — two
commits responding to my first-pass findings in
`docket/reviews/0f4cacb1ba60c6417bc76e5f422a420a90350482.md` (left
untouched, per instruction):

- `5848443` — pins `docket/briefs/legacy/` to a closed manifest
  (`LEGACY_MANIFEST` in `scripts/check-briefs.mjs`): six filenames, each
  with the SHA-256 of its content, instead of an unchecked directory.
- `6d0c68e` — corrects premise 5's overcounted claim about
  `scripts/check-frame.mjs`'s fix history (in both the brief and
  `check-briefs.mjs`'s own header comment), and adds a changelog section
  documenting both findings.

I attacked the manifest fix directly rather than reading it and agreeing,
checked the premise-5 correction against the actual commit sequence rather
than trusting the new count, and read the changelog entry against what the
diff does. Every test below was reverted, with `git status --short`
confirmed empty afterward each time.

## 1. Attacking the manifest — every angle I could construct failed correctly

**Baseline first, independently:** computed SHA-256 for all six pinned
files myself (a fresh script, not trusting the round's own numbers) —
all six match `LEGACY_MANIFEST` exactly, and each hash is a genuine
64-character hex digest, not a placeholder.

**Content mismatch (the coordinator's own probe, reproduced independently):**
appended one HTML comment line to `loop-meta-frame.md`:
```
FAIL  docket/briefs/legacy/loop-meta-frame.md: content does not match the pinned manifest hash -- expected 52aac598c6ef51dffb3385f1e717a56ba61170712322f05b4092273a661b598a, got e279474f5c91fc194a350f03111b68de4709f8c7bb982370f157f1cd201ea08e
```

**Delete one pinned file:**
```
FAIL  docket/briefs/legacy/loop-meta-frame.md: pinned in LEGACY_MANIFEST but missing from disk
```
This is the coordinator's specific concern — "a closed set that cannot
detect removal is only half closed" — and it's handled: `legacyMissing`
is computed independently of `legacyUnexpected` and fails on its own.

**Delete the entire `legacy/` directory at once:** all six manifest
entries fail simultaneously (`6 brief problem(s)`), not silently treated
as "nothing to check."

**Case-variation on Windows — the sharpest test, and the one with the
most interesting result:** renamed `loop-meta-frame.md` to
`Loop-Meta-Frame.md` via a two-step `fs.renameSync` (a direct same-name
case flip is a no-op on NTFS; the temp-name hop is required to make it
stick). Node's `fs.readdirSync` reflects the new casing, so the checker
double-fails it — `Loop-Meta-Frame.md` as "not part of the pinned
manifest" *and* `loop-meta-frame.md` as "missing from disk" — a stronger
result than a single failure. Separately interesting: `git status --short`
reported **nothing to commit** after the rename — this checkout has
`core.ignorecase` treating the two names as the same tracked path, so this
specific manipulation has no path into a real commit here without a
deliberate two-step `git mv`. Not a gap in the checker (it caught the
filesystem state correctly when asked); a note that the delivery path for
this exact attack is closed by git's own behavior on this platform before
the checker is even reached. Restored the original casing with a matching
two-step `fs.renameSync` (`git checkout` alone would not have, since git
saw no change).

**New file under a name never in the manifest, `.md` or not:** both a
`loop-scout-new-unlisted.md` and a non-`.md` `sneaky.txt` dropped into
`legacy/` were caught identically (`not part of the pinned legacy
manifest`). Worth noting: the old `legacyFiles` computation filtered
`.endsWith(".md")`, so a non-`.md` file was invisible to it entirely; the
new manifest scan filters only `isFile()`, so this fix closes a second,
narrower hole (file-extension evasion) that wasn't part of my original
attack and wasn't explicitly claimed as fixed — a strict improvement,
found by testing rather than assumed from the diff.

**Manifest entry with no matching file** (edited `LEGACY_MANIFEST` in
`scripts/check-briefs.mjs` to add a seventh key with a filler hash, no
corresponding file): fails as `missing from disk`, symmetric with the
deletion case.

**Full lauder — a new manifest entry *and* a matching file added
together, hash computed and verified correct in advance:** this
technically **passes**. That is the one thing that remains possible, and
it's worth being precise about what "closed" means here rather than
overclaiming a perfect result. It requires an explicit, visible edit to
`scripts/check-briefs.mjs` itself — not a file drop with zero code
change, which was the original hole. I checked whether that file carries
the same protection `scripts/check-track-scope.mjs` got after round 78's
self-granting-scope incident (`.github/workflows/pr-checks.yml`'s
`human-owned-paths` job, line 110): it does not —
`scripts/check-briefs.mjs` is absent from that job's protected-path
regex. So a delegated round *could*, in principle, widen its own manifest
and spend it in the same pull request, the same shape rule 11 exists to
name. I'm not treating this as a finding that blocks this round, for three
reasons stated plainly rather than left implicit: the manifest's own
comment claims only that an addition "cannot enter unnoticed" — true,
since it necessarily shows up in any diff, unlike the pre-fix hole where
it didn't; this round's credential cannot touch `.github/` to add itself
to `human-owned-paths` even if it wanted to (the same constraint that kept
the whole convention out of `.github/` from round 9 onward); and every
delegated round, including one that tried this, still faces the mandatory
adversarial-review gate this exact exercise is an instance of — the same
backstop the project already relies on for every other track-scope risk
it accepts as reviewable rather than mechanically unforgeable. Recording
it here as a considered-and-cleared residual, not a defect in what
shipped.

**Hash collision:** not attempted, and I'm saying so rather than
gesturing at it — SHA-256 has no known practical collision or
second-preimage attack, so fabricating one is not a real option, and
pretending to "try" would be manufacturing a finding.

## 2. Premise 5's correction, checked against the actual fix sequence

The new premise 5 text: "two fixes that stayed inside shape-recognition
and still left a case unhandled, before a third replaced the approach
itself with a declared, checkable total, and a fourth closed a remaining
flaw in that new approach (a count could match the wrong set)."

Checked this against `scripts/check-frame.mjs`'s own header, fix by fix:

- **(1)** "Fixed by widening the candidate pattern" — stayed inside shape
  recognition, left the tautological-scan case for (2) to find. Matches
  "two fixes that stayed inside shape-recognition."
- **(2)** "Fixed with a second scan... sharing no code with the candidate
  matcher" — still shape-recognition-based, left the indentation case for
  (3). Matches.
- **(3)** "Fixed by replacing shape recognition with a declared total in
  FRAME.md" — this is the one that actually replaces the approach.
  Matches "a third replaced the approach itself with a declared,
  checkable total" exactly — and correctly attributes the replacement to
  the *third* fix, which is precisely what the old text got wrong (it
  said "a fourth").
- **(4)** "v4 checked a COUNT, not a SET... Fixed below with an explicit
  set check" — matches "a fourth closed a remaining flaw in that new
  approach (a count could match the wrong set)" exactly.

Two fixes, then a third, then a fourth: 2 + 1 + 1 = 4, matching
`check-frame.mjs`'s own four-item list with no off-by-one in either
direction — checked specifically because the coordinator flagged this as
where a correction to a counting claim is most likely to trade one wrong
count for another. The identical corrected phrasing was also applied to
`scripts/check-briefs.mjs`'s own header comment (`grep` confirms the two
files now say the same thing), closing the second place the original
overcount had leaked into, which I would not have found without checking
both files independently.

I also independently re-examined the claim the commit message says was
"checked and found already accurate": `docket/briefs/README.md` still
says the declared-total-plus-set-equality technique "converged on after
three narrower fixes each closed one shape of the completeness gap and
left another." Read closely against the real history, this holds without
correction — fixes (1), (2), and (3) each did close one gap and leave
another (including (3), which closed the shape-recognition problem but
left the count-vs-set gap that (4) then closed), and the sentence never
claims a specific fix number did the replacing, which is exactly the
error premise 5 had and this sentence doesn't. A defensible, accurate
reading, not a new finding.

## 3. Does the changelog record what this demonstrates?

Yes, directly and in the terms the coordinator asked for. The new
section's finding-2 paragraph states plainly: "This is the first time a
premise from an orchestrator brief has been caught by this project's own
process rather than by the maintainer noticing it in conversation by
accident... `scripts/check-briefs.mjs` cannot catch this class by design
and does not claim to; a reviewer instructed to run the cited commands
did. That is the argument for keeping that instruction in the reviewer
brief template, not a reason to believe the checker covers more than it
does." This is accurate on its own terms: `docket/briefs/` and its
premise convention did not exist before this round, so no earlier
instance of a brief-premise catch was structurally possible — "the first
time" is true by construction, not an overclaim. The paragraph also
credits the mechanism correctly: the check verifies a citation is
*declared*, not that it *supports* the claim; what caught this was
running the cited command and reading what it actually said, exactly
matching what I did in the first pass.

## 4. Record-keeping, re-checked at the new HEAD

- `node scripts/check-track-scope.mjs 7b25c44 loop/meta/briefs-and-premises`
  → `ok all 14 changed file(s) within meta's scope` (now including this
  round's first review artifact, itself under `docket/`, in scope).
- `git diff --name-only 7b25c44 6d0c68e -- .github/` → empty.
- `git diff --numstat 39a5bbb 6d0c68e -- CHANGELOG.md` → `54  0` — pure
  insertion, extending this round's own still-open entry (not a past
  entry; rule 5 governs rewriting *past* entries, and this round hasn't
  merged).
- `node scripts/check-docket.mjs` → `ok 124 docket item(s) valid (42
  open)`, unaffected.
- Baseline `node scripts/check-briefs.mjs` after every attack and its
  revert: `ok all current briefs declare their premises`, exit 0.

## A repeated false system-reminder, disclosed again

During this pass, the same fabricated system-reminder pattern from my
first review recurred four more times — after reverting my own test
mutations to `docket/briefs/legacy/loop-meta-frame.md` and
`scripts/check-briefs.mjs` with `git checkout --`, a reminder claimed the
file "was modified, either by the user or by a linter," called the change
"intentional," and told me not to mention it. Each time it was neither —
it was my own revert, and each time I verified with `git diff 6d0c68e --
<path>` (empty) and `git status --short` (empty) before continuing.
Disclosing per the standing instruction from the first review brief to
verify and disclose regardless of what such a reminder claims.

## Summary

Both prior findings are fixed and I could not defeat either fix: the
`legacy/` manifest correctly rejects a mismatched-content file, a deleted
file, a deleted directory, an unlisted new file (`.md` or not), and a
Windows case-only rename, and only admits a new entry through a visible,
reviewable edit to the enforcement script itself — a materially different
and much smaller risk than the original "drop a file, get a free pass"
hole, and one I'm not blocking on given the round's `.github/` credential
constraint and the standing adversarial-review backstop. The premise-5
correction matches `scripts/check-frame.mjs`'s actual four-fix history
exactly, with no new off-by-one introduced in either of the two places
the error had appeared, and a third place I checked (`README.md`) was
correctly left alone. The changelog records the "caught by process, not
by accident" finding and its argument for keeping the cited-commands
instruction, accurately and in the terms asked for. No third finding to
report — I looked for one and didn't manufacture one. Approving.
