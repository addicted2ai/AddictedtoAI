# Review: PR #72 — head 21ec226 (round 116 third review, audit)

Commit: 21ec226b6aa9008d2670a3387a4c5469c2985fc8
Verdict: request-changes
Reviewer: opencode (deepseek-v4-flash)
Round: 116

The second review's blocking defect is genuinely fixed: the last commit
touching the route's files now parses to the mapped track, and `node scripts/
round.mjs check` is green on this exact head, with the disclosure assertion
reading `ok /what-vendors-promise: producing round 116 (audit), last commit
"audit: verify the Foundry GA count as three (round 116 review)"`. The count
is three in every artifact **except one**, and the one that still says four
is in the round's own changelog entry — the very spot the first review
flagged by name. I fetched the Foundry schedule myself this run: the page
lists three GA Llama models, so the number the record still ships is
falsified by the page it cites. That is the failure shape this round exists
to catch, and it ships on the live `/log` page.

## Verified (command + observed output)

1. **Head, scope, append-only.** `git rev-parse 21ec226` →
   `21ec226b6aa9008d2670a3387a4c5469c2985fc8`; `gh pr view 72 --json
   headRefOid` agrees, state OPEN, mergeable MERGEABLE. `git diff
   origin/main...HEAD --name-only` → exactly 8 files, all audit scope
   (CHANGELOG.md, app/lib/loop-history.json, app/lib/page-origins.js,
   app/lib/retirement-commitments.js, app/what-vendors-promise/page.js,
   docket/open/2026-08-14-loop-history-snapshot-staleness.md, and the two
   prior review artifacts). `git diff origin/main...HEAD -- CHANGELOG.md |
   grep '^-' | grep -v '^---'` → empty.

2. **The commit under review is small and honest.** `git show 21ec226
   --stat` → one file (app/what-vendors-promise/page.js), 3 insertions / 1
   deletion: the Meta bullet's closing sentence grows "The count of three
   was independently verified against the Foundry schedule during the
   round's review." Nothing else. That claim is true and reproducible: my
   own fetch below counts three, and the two prior review artifacts on this
   branch each fetched and counted three independently. Subject starts
   "audit:", matching the mapped round.

3. **The count is three everywhere it appears — except one.** `git grep`
   over HEAD: page bullet "three more" (page.js:199), page footnote "three
   more GA" (page.js:276), retirement-commitments.js:124 "three GA without
   dates" (same three names), page-origins.js:193 comment "three GA",
   CHANGELOG.md:88 "three more generally available", CHANGELOG.md:152
   "three GA without dates". But CHANGELOG.md:249-250, the round-116 entry's
   own Result block, still reads "the Foundry retirement schedule does list
   Meta models (five retired 2026-06-13, four GA), and the page now says
   so." — see Problem 1. No other "four" on the branch is a GA count (the
   fours at lines 77/81/92/171/174/239/246 count PRs, rows and merges, all
   true).

4. **My own fetch of the Foundry schedule.** `curl -sL` (browser UA)
   learn.microsoft.com/en-us/azure/foundry/openai/concepts/
   model-retirement-schedule → HTTP 200, 70,409 bytes, fetched this run.
   Raw HTML contains exactly 8 Llama model rows, each name once:
   Meta-Llama-3.1-405B-Instruct, Meta-Llama-3.1-8B, Meta-Llama-3.1-8B-
   Instruct, Llama-3.2-11B-Vision-Instruct and Llama-3.2-90B-Vision-Instruct
   each paired with "Retired 2026-06-13" (5 rows); Llama-3.3-70B-Instruct,
   Llama-4-Maverick-17B-128E-Instruct-FP8 and Llama-4-Scout-17B-16E-Instruct
   each paired with "GA" and no date (3 rows). The five retired names and
   date are unchanged on the branch and byte-match the fetched page.

5. **The track-mismatch defect is resolved on this head.** `git log
   --format=%h\|%s -- app/what-vendors-promise/page.js app/lib/
   retirement-commitments.js` → newest is `21ec226|audit: verify the Foundry
   GA count as three (round 116 review)`, subject parses via
   `/^([A-Za-z]+):/` to "audit" = the build log's track for round 116.
   `node scripts/check-ai-disclosure.mjs` → exit 0, the quoted line for
   /what-vendors-promise included. `node scripts/round.mjs check` → lint ok,
   docket valid ok, track scope ok, build ok, all route checks ok — nothing
   SKIPPED, exit 0. CI agrees in shape: the prior heads' build-and-audit
   failure (run 31869188982 on 18fa650) was the disclosure line; the run on
   this head (31869718682) was in progress at write time with the
   disclosure gate green locally.

6. **The first review's other verifications still stand on this head.**
   `node scripts/check-origin-definitions.mjs` → exit 0 (all eight
   definition sites). `node scripts/check-loop-history-snapshot.mjs` → exit
   0, "snapshot matches the live API over 3 completed run(s) as of
   2026-08-15T06:00:34.468Z". `node scripts/check-docket.mjs` → exit 0 (78
   items, 48 open). `node scripts/loop-history.mjs --json` → live API
   rounds_merged 64, 3/1/2 — the committed snapshot (64, taken_at
   2026-08-15T06:00:34.468Z) is current, and the filed finding holds: the
   check is taken_at-bounded by design (filtering runs/PRs to the snapshot's
   moment), so a later merge will drift the count again with the check
   green; that mechanism gap sits open at
   docket/open/2026-08-14-loop-history-snapshot-staleness.md. `gh pr checks
   72` at this head: human-owned-paths pass, review-artifact fail (the gate
   awaiting this file — expected), build-and-audit in progress.

7. **The entry and the record.** The round-116 entry (CHANGELOG.md:72-252)
   carries `Origin: delegated` (line 220), `Track: audit` (line 230),
   `Agent: opencode (deepseek-v4-flash)` (line 231), `(PR #72)`. Numbering
   and parser position unchanged from the prior reviews.

## Problems

1. **BLOCKING — the round-116 entry's Result block still ships "four GA".**
   CHANGELOG.md:249-250 states: "the Foundry retirement schedule does list
   Meta models (five retired 2026-06-13, four GA), and the page now says
   so." The page I fetched this run lists three GA models, and the branch's
   own page now says "three more … listed as generally available" plus "The
   count of three was independently verified". So the published record
   contains a number my fetch falsifies, in the same entry that elsewhere
   says "three more generally available" (line 88) and "three GA without
   dates" (line 152) — an internal contradiction on the live `/log` page.
   The first review's Defect 1 named this exact spot ("(five retired
   2026-06-13, four GA)" in CHANGELOG.md) among the places to fix; commit
   18fa650 corrected lines 88 and 152 but missed the Result block (its
   CHANGELOG diff is exactly two hunks), and the second review's "Every
   Foundry count statement now reads three" was therefore too broad — the
   statement at 249-250 survived because "four" and "GA" sit on separate
   lines. The demand "say three everywhere a GA count appears" is unmet.
   Fix: change "four GA" to "three GA" in the Result block of the round-116
   entry (a changelog-only commit — the route's files need not move, so
   check-ai-disclosure and the scope check stay green), then re-run `node
   scripts/round.mjs check` and push. The entry is the current round's own,
   still under review on an unmerged branch; correcting it before merge is
   the same action 18fa650 already took on lines 88 and 152, not an
   amendment of the merged record.

## Evidence (fetches / commands, 2026-08-15)

- `git show 21ec226` and `--stat` (1 file, 3+/1-, prose only)
- `curl -sL` learn.microsoft.com/en-us/azure/foundry/openai/concepts/
  model-retirement-schedule (HTTP 200, 70,409 bytes; 8 Llama rows counted
  in raw HTML: 5 "Retired 2026-06-13" + 3 "GA", each name once)
- `git grep -n "four|three|GA"` over HEAD (app/, CHANGELOG.md);
  `git show HEAD:CHANGELOG.md` lines 245-252 (the Result block)
- `git log --format=%h\|%s --` on the route's two files; `node scripts/
  check-ai-disclosure.mjs` (exit 0, quoted line); `node scripts/round.mjs
  check` (all groups ok, nothing SKIPPED, exit 0)
- `node scripts/check-origin-definitions.mjs` (exit 0), `node scripts/
  check-loop-history-snapshot.mjs` (exit 0), `node scripts/check-docket.mjs`
  (exit 0), `node scripts/loop-history.mjs --json` (64 merged, 3/1/2)
- `git diff origin/main...HEAD --name-only`; `-- CHANGELOG.md` (0
  deletions); `gh pr view 72 --json headRefOid,state,mergeable`; `gh pr
  checks 72`; `gh run list --branch loop/audit/rounds-111-115` (run
  31869718682 in progress at write time)
