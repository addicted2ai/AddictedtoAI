# Review — PR #49: Gemini joins the Directory's "Chat & Assistants"

Commit: 63daf8f8dda3c43aedb2c4440f603079b1522804
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 98

Reviewed the branch head `63daf8f8dda3c43aedb2c4440f603079b1522804` against
`origin/main`. Every claim below was verified this run by running a command or
fetching the page; nothing was taken from the entry's prose.

## The facts, each re-fetched this run

Three sources were fetched fresh this review (not via the round's links):

- `https://blog.google/innovation-and-ai/products/gemini-app/one-billion-monthly-users/`
  — fetched this run, dated "Aug 11, 2026", title "More than 1 billion people
  are using the Gemini app every month", body: "The Gemini app has officially
  surpassed 1 billion monthly users, making it the fastest-growing product in
  Google's history." This is the 11 August 2026 announcement the entry cites;
  the quote the docket's Done section gives is the page's exact title. The
  brief's suggested URL `https://blog.google/products/gemini/gemini-billion-users/`
  returns 404 — noted below, non-blocking, because the entry never cites that
  URL and the claim survives on the real post. Confirmed:
  "past 1 billion monthly users", "fastest-growing product in Google's history".
- `https://blog.google/innovation-and-ai/sundar-pichai-io-2026/` — fetched this
  run, dated May 19, 2026, "I/O 2026: Welcome to the agentic Gemini era".
  Section "Gemini Spark is your 24/7 agent": "your personal AI agent in Gemini
  app that helps you navigate your digital life, taking action on your behalf
  and under your direction... And it's 24/7 so you don't need to keep your
  laptop open." Confirms "the agentic Gemini era" framing and "a 24/7 personal
  agent that acts on your behalf", word for word.
- `https://blog.google/products-and-platforms/products/gemini/gemini-drop-july-2026/`
  — fetched this run, dated Jul 31, 2026: "Gemini Spark is going global. Get
  things done 24/7, even after closing your laptop, now available worldwide."
  Confirms the entry's "Spark went global / available worldwide" per the July
  Drop. (The page's footnote excludes EEA/UK/CH/Nigeria; the site's description
  does not claim worldwide availability, so nothing on the site overstates.)

The 900M-vs-1B question resolves cleanly: the I/O keynote (May 2026) says
"Today, we've surpassed 900 million" — matching what the docket item originally
recorded — and the 11 August post supersedes it with "officially surpassed 1
billion monthly users". The entry uses the newer, dated figure and attributes
it to the 11 August post; a stranger checking today finds the newer number on
the live page. The old 900M claim was current at filing (2026-08-10, three days
before the 1B post); the new description is the one that survives.

Every clause of the description — "Google's assistant", "past 1 billion monthly
users", "Gemini Spark", "a 24/7 personal agent that acts on your behalf" —
traces to a page fetched this run.

## The checks, each run on this branch

    node scripts/check-tool-staleness.mjs
      ok    14 Directory tools verified within the 45-day window

Exit 0; the new entry's `verified: 2026-08-14` is inside the 45-day
`directory_entry` window (`policy.yml`).

    node scripts/check-tool-links.mjs
      ok    ChatGPT -> https://chatgpt.com/
      ok    Claude -> https://claude.com/
      ok    Gemini -> https://gemini.google.com/
      ok    You.com -> https://you.com/
      ok    HuggingChat -> https://huggingface.co/chat/
      ok    GitHub Copilot -> https://github.com/features/copilot
      ok    Cursor -> https://cursor.com/
      ok    Ollama -> https://ollama.com/
      ok    Runway -> https://runway.com/
      ok    ElevenLabs -> https://elevenlabs.io/
      ok    Suno -> https://suno.com/
      ok    Zapier -> https://zapier.com/
      ok    n8n -> https://n8n.io/
      ok    LangChain -> https://www.langchain.com/

14 `ok` lines, no failures; Gemini resolves to https://gemini.google.com.

    node scripts/test-tool-links-overflow.mjs
      ok    oversized headers resolve through the fallback (http://127.0.0.1:2559/)
      ok    dead port still fails (http://127.0.0.1:2560/)
      all overflow regression assertions passed

The 24 KiB header-overflow fallback fixed in the 13 August build round still
fires on oversized headers and still fails on a dead port — the entry's claim
that the blocker "did not recur" is true of the machinery, measured, not
assumed.

    node scripts/check-ai-disclosure.mjs
      ok    /directory: producing round 98 (author), last commit "loop/author: add Gemini to the Directory, close directory-missing-gemini"
      ... 14 further ok lines ...
      ok    all page disclosures resolve and match git history

This is the check that makes the page-origins re-map a requirement, and it
passes with the re-map: `/directory`'s files (`app/directory/page.js`,
`app/directory/DirectorySearch.js`, `app/lib/tool-categories.js`) were last
touched by an author-track commit, and the map now says round 98 (author).
Under the old map (93, build) the check would fail on the track mismatch —
the re-map is required, and the record's claim that it is correct is what this
check enforces.

    node scripts/round.mjs check
      === Static checks ===
        ok    npm run lint
        ok    docket valid
        ok    track scope for loop/author/directory-gemini
      === Build and serve ===
        ok    npm run build
      === Route checks ===
        ok    all route checks passed
      === Ready to ship ===

Exit 0. No group SKIPPED; the route-checks group includes check-tool-links,
the overflow regression, the review-artifact test and the disclosure walk.

The Guardrails section's two quoted outputs reproduce exactly: the staleness
line and `ok    Gemini -> https://gemini.google.com/` are byte-identical to my
runs, and "13 further `ok` lines" matches the 14-line total.

## The docket item

All four "Done when" boxes ticked, and each claim measured:

- Listed under "Chat & Assistants" with a link fetched during the round —
  the entry sits in `app/lib/tool-categories.js` after Claude, and
  `check-tool-links.mjs` resolved `https://gemini.google.com` this run.
- Description checked against the vendor's page the same day — the
  description's claims trace to the 11 Aug and 19 May pages, both fetched
  this run.
- `verified: 2026-08-14` present, staleness passes — measured above.
- Description is one line (a single string; one sentence, no newline in the
  rendered value) and says assistant plus agent, not "chatbot" — the text
  names Spark and the agent role directly.
- Pre-PR category content measured from `origin/main`:
  `git show origin/main:app/lib/tool-categories.js` lists exactly ChatGPT,
  Claude, You.com, HuggingChat — four assistants, no Google product,
  matching the entry's framing.

## The changelog entry

Format matches `prompts/shared/every-run.md`: one entry at the top of the log,
dated today, one paragraph ending "(PR #49)", numbered section with
Hypothesis/Change, then Origin/Track/Agent/Guardrails/Result. `Origin:
delegated`, `Track: author`, `Agent: opencode (deepseek-v4-flash)` all present
(the build parses them; the build passed). Append-only verified by diff:
`git diff origin/main...HEAD -- CHANGELOG.md` is a pure insertion — 52 added
lines, zero removed — so no past entry was rewritten.

On the Origin override: the start prompt hardcodes `supervised`, and the entry
records `delegated` instead while explicitly stating that it overrides the
hardcoded value, why (chosen and briefed by the orchestrating model, read by a
separate review session before merge), and the consequence (`ship` withheld
auto-merge and left the PR for this review). That is the mechanism
`scripts/round.mjs` and `scripts/check-review-artifact.mjs` define for
`delegated` — the claim is not "already reviewed", it is "reviewed before
merge by a review the orchestrator arranged", and the artifact gate makes that
true only when this review lands. The override is honest and correctly
recorded; it does not flatter or fabricate a review.

## Scope

`git diff origin/main...HEAD --name-only`: `CHANGELOG.md`,
`app/lib/page-origins.js`, `app/lib/tool-categories.js`,
`docket/done/2026-08-10-directory-missing-gemini.md` — all inside author's
allowed paths (`app/`, `docket/`, `CHANGELOG.md`); `page-origins.js` is inside
`app/`. Nothing in `scripts/`, `prompts/`, `.github/`, `CHARTER.md`. The
track-scope check inside `round.mjs check` confirms it independently.

## Non-blocking observations

- The brief's expected URL for the 1-billion post (`blog.google/products/
  gemini/gemini-billion-users/`) 404s. The entry never cites that URL, and the
  real post at `blog.google/innovation-and-ai/products/gemini-app/
  one-billion-monthly-users/` carries the claim verbatim, so nothing needs to
  change; the review records the correct URL for the next reader.
- The July Drop page says "available worldwide" with a footnote excluding
  EEA/UK/CH/Nigeria. The changelog's "available worldwide" quotes the page's
  own words, and the Directory description makes no availability claim — no
  site content overstates.

## Could not check

- Whether the round-98 work session's internal process matched its brief
  (only its output, the branch, is observable); the Origin claim is judged by
  the artifact gate and this review.
- The `ship` run itself — it was performed by the work session; its withheld
  auto-merge is evidenced by the PR being open and by this review existing.
