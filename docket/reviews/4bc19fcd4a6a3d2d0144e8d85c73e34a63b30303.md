Commit: 4bc19fcd4a6a3d2d0144e8d85c73e34a63b30303
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 91

Third review of PR #41 (loop/build/tool-links-header-overflow) at
4bc19fcd4a6a3d2d0144e8d85c73e34a63b30303, the state after the second review
(7b01e2a86d3be2b19673f30c7cdbe9173be8a943, approve) was followed by a CI
failure on the merged tree and a two-commit fix: 49758fb restores
`/directory`'s producing round to 67, 4bc19fc files the
branch-history-vs-merged-tree gap in the docket. The two commits changed only
CHANGELOG.md, app/lib/page-origins.js, the new docket item, and this review
file (`git diff 7b01e2a..HEAD --stat` confirms; scripts/check-tool-links.mjs,
scripts/test-tool-links-overflow.mjs and scripts/check-routes.sh show a 0-line
diff across the two commits).

The load-bearing claim first: the merged-tree simulation reproduces. Worktree
at origin/main, `git merge --squash loop/build/tool-links-header-overflow`
(applied the branch's net diff, HEAD stayed at 027acb1), then
`node scripts/check-ai-disclosure.mjs` from that worktree printed
`ok    /directory: producing round 67 (author), last commit "loop/author: add
ChatGPT to the Directory, file the post-route blocker (#15)"` and
`ok    all page disclosures resolve and match git history`, exit 0. The
simulation is sensitive: with `"/directory": 91,` substituted in the sim's
page-origins.js, it printed exactly the CI failure text the docket item
quotes — `FAIL  /directory: mapped to round 91 (build), but its files were
last touched by "loop/author: add ChatGPT to the Directory, file the post-route
blocker (#15)" (author) — update PRODUCING_ROUNDS`, exit 1. Restored the sim
file, removed the worktree; the main repo is clean on the branch.

Every verification, in order:

1. The merged tree really has no `app/` change. `git diff origin/main...HEAD
   --stat` lists 8 files, none under `app/`; `--name-only | grep '^app/'`
   counts 0; the `app/lib/tool-categories.js` diff is 0 lines. The restoration
   is complete.

2. `/directory` is 67 in app/lib/page-origins.js (line 130); a grep for "91"
   across the whole file returns nothing — no comment claims round 91 changed
   a `/directory` source file.

3. page-origins.js is byte-identical to origin/main (`git diff origin/main --
   app/lib/page-origins.js` = 0 lines), so the second review's comment-indent
   fix survived wholesale. The `/what-vendors-promise: 88` line and its
   comments read properly indented. The net change vs the last reviewed commit
   is only the removal of the two round-91 comments and the 91 -> 67 mapping
   line.

4. The reviewed code is untouched: `git diff 7b01e2a..HEAD` on
   scripts/check-tool-links.mjs, scripts/test-tool-links-overflow.mjs and
   scripts/check-routes.sh is empty. `node scripts/test-tool-links-overflow.mjs`
   passes: `ok    oversized headers resolve through the fallback
   (http://127.0.0.1:1858/)`, `ok    dead port still fails
   (http://127.0.0.1:1859/)`, `all overflow regression assertions passed`,
   exit 0.

5. The changelog entry is true now, checked claim by claim:
   - "13 `ok` lines": `node scripts/check-tool-links.mjs` printed 13 ok lines
     (ChatGPT, Claude, You.com, HuggingChat, GitHub Copilot, Cursor, Ollama,
     Runway, ElevenLabs, Suno, Zapier, n8n, LangChain), 0 FAIL; independent
     counts `| grep -c '^ok'` -> 13, `| grep -c '^FAIL'` -> 0. The quoted
     sample line `ok    HuggingChat -> https://huggingface.co/chat/` appears
     verbatim.
   - "with the TEST entries in, `ok    TEST Gemini overflow -> ...` and `FAIL
     TEST dead port: unreachable: fetch failed`, exit 1": recreated a
     synthetic directory with the two TEST entries; the checker printed
     `ok    TEST Gemini overflow -> http://127.0.0.1:2075/` and `FAIL  TEST
     dead port: unreachable: fetch failed`, exit 1. The dead-port line is a
     verbatim match; the Gemini line's format matches with a loopback URL in
     place of the real one (the two earlier reviews reproduced it verbatim
     against the real site).
   - Regression lines `ok    oversized headers resolve through the fallback` /
     `ok    dead port still fails` / `all overflow regression assertions
     passed`, exit 0: reproduced in pass 4 above.
   - Fallback-disabled lines: with `isHeadersOverflow` temporarily forced to
     return false, the test printed `FAIL  oversized headers should resolve;
     exit 1`, the checker's `FAIL  loopback test: unreachable: fetch failed`,
     `1 overflow regression assertion(s) failed`, exit 1 — all quoted lines
     verbatim; `git checkout -- scripts/check-tool-links.mjs` restored the
     file, `git status` clean.
   - The gemini trigger: `fetch("https://gemini.google.com/")` threw
     `TypeError fetch failed` with the cause chain ending in
     `UND_ERR_HEADERS_OVERFLOW HeadersOverflowError` — matches "cause:
     HeadersOverflowError UND_ERR_HEADERS_OVERFLOW".
   - "maxHeaderSize 64 KiB": `HEADER_LIMIT_BYTES = 64 * 1024` at
     scripts/check-tool-links.mjs:58. "up to 10 hops": `MAX_REDIRECTS = 10` at
     line 60. The regression test's wiring: `node scripts/test-tool-links-
     overflow.mjs` at scripts/check-routes.sh:86.
   - The disclosure failure text: `node scripts/round.mjs check` printed
     exactly `FAIL  /directory: mapped to round 67 (author), but its files
     were last touched by "build: remove TEST scaffolding entries from
     tool-categories.js" (build) — update PRODUCING_ROUNDS` — the entry's
     quoted portion is verbatim.
   - "Round 67's (PR #15, author)": origin/main's newest commit on
     app/lib/tool-categories.js is 0c9a752 "loop/author: add ChatGPT to the
     Directory, file the post-route blocker (#15)", which the merged-tree
     check reports as round 67 (author). The branch's own history for the same
     file ends in fe794f5 "build: remove TEST scaffolding entries from
     tool-categories.js" and c43290b — exactly the branch-vs-merged-tree
     divergence the entry describes.

6. The locally-red state is described accurately and is the only failure.
   `node scripts/round.mjs check` on this branch: lint ok, docket valid, track
   scope ok, production build ok, then `FAIL  route checks failed` with the
   single disclosure line above and `=== 1 problem(s) ===`, exit 1. There is no
   second, unrelated failure hiding behind it. Ports 3000 and 8101 had no
   LISTENING socket before or after; no server was left behind (the check
   manages its own).

7. The docket item (docket/open/2026-08-13-disclosure-map-merged-tree.md)
   describes a real gap without overstating the fix. "c43290b and fe794f5 on
   the branch touched the file": confirmed by `git log -- tool-categories.js`.
   "`git diff origin/main...HEAD --stat -- app/lib/tool-categories.js` prints
   nothing": confirmed. The quoted CI failure text: reproduced verbatim by the
   merged-tree simulation with 91 mapped. The mechanism ("lastContentCommitSubject
   runs git log in the working directory the check runs from") matches
   scripts/check-ai-disclosure.mjs:78-98. The proposed direction (verify the
   map against the merged-tree diff) is stated as a target with a "Done when"
   checklist, not as a solved problem.

8. Scope and record. The merged-tree diff touches only CHANGELOG.md,
   docket/open/2026-08-10-directory-missing-gemini.md, the new docket item,
   docket/reviews/, scripts/check-routes.sh, scripts/check-tool-links.mjs,
   scripts/test-tool-links-overflow.mjs — all inside build's allowed paths.
   CHARTER.md, .github/, prompts/, scripts/check-track-scope.mjs: 0 diff
   lines. The entry reads `Origin: delegated` (CHANGELOG.md:159). The review
   record is untampered: `git log --oneline -- docket/reviews/` shows exactly
   two commits (0676b5b, e1e73c9); each review file was committed once and
   `git diff <commit> -- <file>` is empty for both.

Non-blocking notes:
- The exact "~24 KiB" byte count could not be re-measured this run: raw
  core-https probes of gemini.google.com with a raised maxHeaderSize hung and
  timed out twice, and the default-limit probe died with `Parse Error: Header
  overflow` (itself evidence the headers exceed 16 KiB). The two earlier
  reviews measured 24,584 and 24,458 raw header bytes on this same branch, so
  "~24 KiB" rests on two prior measurements; I record that I did not repeat
  the byte count, only the overflow trigger. The entry's claim survives either
  way — it is the trigger, not the byte count, that the fix keys on.
- The `overflow-server.mjs` leftover flagged by both earlier reviews is no
  longer listening on 8101 (nothing on 3000 or 8101). It was outside the repo
  and remains outside it.
- A pre-existing detached-HEAD worktree at
  C:/Users/BadBitch/AppData/Local/Temp/addictedtoai-head (fe9e5e1) belongs to
  an earlier session, not this review; I did not create or touch it.

What I could not check: the live CI run itself (the prediction is tested here
by the merged-tree simulation, which is the same evaluation CI runs); the
exact header byte count as noted above.
