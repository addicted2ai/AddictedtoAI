Commit: cb6f4159d566a32cae983b11ea2a9d4b107605da
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 144

Reviewed branch `loop/scout/round-144-qwen38-open-weights`, head commit
cb6f4159d566a32cae983b11ea2a9d4b107605da, as a separate session from the round
that did the work. The review decided whether `Origin: delegated` can merge.

## What I ran

- `git fetch origin loop/scout/round-144-qwen38-open-weights` then checked out;
  `git rev-parse HEAD` = cb6f4159d566a32cae983b11ea2a9d4b107605da. Matches the
  commit named in the brief.
- `git diff origin/main...HEAD --name-only` — exactly two files:
  `CHANGELOG.md` and `docket/open/2026-08-16-post-qwen3-8-open-weights.md`.
  Nothing else. A scout round that touched `app/`, `scripts/`, `prompts/`,
  `.github/`, or anything else would have failed its charge; none did.
- `node scripts/check-track-scope.mjs origin/main loop/scout/round-144-qwen38-open-weights`
  — `ok    all 2 changed file(s) within scout's scope`.
- `npm run lint` — no warnings or errors.
- `node scripts/check-docket.mjs` — `ok    102 docket item(s) valid (58 open)`;
  author 30 open, build 1, meta 27.
- `node scripts/check-publishing-quota.mjs` — ok; 9 posts, day cap 1, week cap
  3, no post pushes a day or week over. This grounds the changelog's "author
  track blocked until 17 August" claim.
- `git diff origin/main...HEAD -- CHANGELOG.md` — pure insertion of one new
  entry at the top of the Log (the single `-` line is the diff header
  `--- a/CHANGELOG.md`). No past entry rewritten; rule 5 holds.

I did NOT run `node scripts/round.mjs check` (it starts a server on port 3000,
which the brief permits me to skip). I ran lint, the docket validator, the
track-scope check, and the publishing-quota check directly.

## Item and changelog format

Frontmatter: track author, filed-by scout, title one imperative line, created
2026-08-16, expires 2026-11-14 (a real future date), serves more-current (one
of the four values), priority 2 (1–3). Evidence section carries external links
with retrieval date 2026-08-16; check-docket accepts it. Format matches
`docket/README.md`.

Changelog: exactly one new entry at the top (`### 2026-08-16`, round 144), in
the `prompts/shared/every-run.md` shape (paragraph, `**1. Title**`, Hypothesis,
Change, Origin, Track, Agent, Guardrails, Result). `Origin: delegated` with the
one-line note that the start prompt hardcodes `supervised`; `Track: scout`;
`Agent: opencode (deepseek-v4-flash)`. The entry's claim that round 13's
changelog predicted this release was checked against the record: round 13
(PR #13, `### 2026-08-11`) reads "Alibaba's promised Qwen3.8 open weights,
which would be that lab's first Max-class open-weights release and was
committed to the week of 10 August … a later scout round will see it if it
does." The quotes in the new entry match. The framing that rounds 139, 142 and
143 are all dated 2026-08-16 also checks out.

## Sources re-fetched this session, and whether they match the item

1. HF model card Qwen/Qwen3.8-2.4T-A95B — matches. "For the first time, Qwen3.8
   brings a Qwen-Max-class model to open release"; 2.4T total / 95B activated;
   512 experts (10 routed + 1 shared); context "262,144 natively and extensible
   up to 1,010,000 tokens"; "text-only model that requires thinking mode for
   all interactions … thinking cannot be disabled"; license badge
   `qwen3.8-max`; hosted Qwen3.8-Max keeps "vision input & non-thinking
   support, 1M context length by default". The item's text-only/thinking-only/
   262K-native description and its "open checkpoint ≠ hosted Max" split are
   exactly what the card says. The card page itself carries no release date;
   the 12 August date is supported by NVIDIA (below) and both explainers.
2. HF model card Qwen/Qwen3.8-27B — matches. License apache-2.0; 27B
   parameters; "a native vision-language model that understands images and
   videos"; context "262,144 natively and extensible up to 1,000,000 tokens";
   "thinking mode is on by default and can be disabled per request" with
   `reasoning_effort`. Benchmark figures match exactly: Terminal Bench 2.1 73.0,
   SWE-bench Pro 61.7, OSWorld-Verified 84.3, WebArena-Verified 64.8,
   LiveCodeBench v6 90.3.
3. LICENSE at Qwen/Qwen3.8-2.4T-A95B/raw/main/LICENSE — matches, titled
   "Qwen3.8-Max License". Condition 1: products/services with more than
   100,000,000 MAU or US$20,000,000 monthly revenue must prominently display
   the model name. Condition 2: a licensee or affiliate running a "Model as a
   Service or AI Work Assistant" business whose aggregate revenue exceeds
   US$50,000,000 in any consecutive twelve months must obtain a separate
   license, with internal use that does not make the model available to third
   parties exempt. Both conditions quoted in the item are verbatim from the
   file. It is a custom license, not Apache-2.0.
4. NVIDIA Technical Blog, 12 August 2026 — matches. Contains exactly "Alibaba
   released the open weights for Qwen3.8-2.4T-A95B (Qwen3.8-Max), its largest
   open-weight model"; 2.4T total / 95B activated; fine-grained MoE with a
   hybrid of full and linear attention; "a context window of up to one million
   tokens" (the item's "up to one million tokens" quote); "over 4K tokens per
   second per GPU and over 350 tokens per second per user on NVIDIA GB300 NVL72
   in FP8 precision on Day 0" without tuning; NeMo AutoModel post-training;
   hosted providers DeepInfra, DigitalOcean, Fireworks AI, Modal, OpenRouter
   (the item lists four of the five — a subset, not a falsehood). Post is dated
   Aug 12, 2026.
5. Hacker News item 49299605 via the Algolia API (the item's own stated method)
   — matches. `{"id":49299605,"title":"Qwen 3.8 27B","created_at":"2026-08-14T15:00:00.000Z", ...}`.
   The posted timestamp 2026-08-14T15:00:00Z matches exactly and is the only
   dated source for the 27B release, which is how the item uses it. Points now
   read 1386 via Algolia / 1387 on the HN page against the item's 1385, and 777
   comments match. Point counts are dynamic vote totals; the round's 1385 was a
   snapshot at its fetch, and the item uses the story only to date the 27B
   release and record the level of interest. Not a defect.
6. aireleasetracker.com/model/qwen/qwen3.8-max — matches. "by a wide margin the
   largest weights any lab had published to that point"; "on August 12, 2026 it
   opened the text-only base model … under a custom licence"; "the hosted Max
   kept vision input, non-thinking mode and the full 1M context for itself";
   "the smaller Apache-licensed Qwen3.8-27B followed two days later".
7. explainx.ai Qwen3.8-Max open-weights post — matches. BF16 ~4.89 TB and GGUF
   ~397 GB–1.31 TB figures; the "revenue-share" shorthand (the item warns the
   executing round to read the license rather than repeat it, and the item
   quotes the actual conditions, verified above); the HF discussion reaction
   called a "huge disappointment"; and the post carries its own "Update —
   August 15, 2026" noting the 27B then shipped, so the item's "superseded by
   its own 15 August update" note is accurate.

## Not a duplicate

I read the titles and evidence of the author-track items in `docket/open/`.
`grep -il qwen` finds only three open items: the round's own new item, the
state-of-open-models item (covers Hugging Face's Summer 2026 report and Qwen's
Hub ecosystem position — 151,448 derivatives, downloads — not this release),
and a Writer Palmyra X6 item that mentions "Qwen 3.6" once in a benchmark
comparison. Nothing else in the queue covers the Qwen3.8 open-weights release.
The new item is not a duplicate of anything filed.

## Queue claims in the changelog's "Considered and not filed"

Verified against the record, not the round's word: the state-of-open-models
item does cite DeepSeek-V4-Flash; the price-war item does cite V4;
`docket/dropped/` is empty; the author queue holds 29 items before this
round's filing (30 after, per check-docket); and the "blocked until 17 August"
claim is grounded in the publishing quota (week cap 3 already met, confirmed by
`check-publishing-quota.mjs`).

## Could not check

- That the round ran `node scripts/round.mjs check` with no skipped groups, or
  that its sources were fetched *during* the round rather than reconstructed:
  neither is independently verifiable from the record. I re-fetched all seven
  sources this session and verified the artifact claims against what they say
  now.
- I did not run the full `round.mjs check`; it starts a server on port 3000.

## Findings

No defect that would fail the round. The specific failure mode this project
hunts — a claim written from intent rather than measurement, or a universal
from a small sample — does not appear: every factual claim in the item and the
entry was traced to a source I re-fetched this session, and the sources say
what the item says they say, including the exact dates, the custom
`qwen3.8-max` license (not Apache-2.0), the parameter counts, the text-only /
thinking-required qualifiers, and "native vision-language" as the 27B card
actually words it. Two minor observations, neither disqualifying: the HN point
count has drifted 1385 → 1386/1387 (a dynamic vote total; the timestamp
matches exactly), and the "Unsloth GGUF quantizations followed within a day"
claim is attributed in prose to the HF trending page without a URL in Evidence
— but the claim is true on its face (Unsloth GGUFs are in use in the same-day
HN thread) and is not load-bearing for the item's checklist.