Commit: 7b01e2a86d3be2b19673f30c7cdbe9173be8a943
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 91

Second review of PR #41 (loop/build/tool-links-header-overflow) at
7b01e2a86d3be2b19673f30c7cdbe9173be8a943, the corrected state after the first
review (f79e659e0988e7b1dc88b11e126e4979c0111d08, request-changes) blocked on
the changelog's "14 `ok` lines" figure. I re-verified the fix from the current
bytes rather than trusting the earlier approval, because the correction commit
touched code, not just prose.

The correction commit (7b01e2a, "fix review findings: measured ok-line count,
page-origins indent, drop dead redirectsLeft") changed exactly three things:
CHANGELOG.md (14 -> 13), app/lib/page-origins.js (comment indentation only),
scripts/check-tool-links.mjs (removed the `redirectsLeft` parameter, which was
passed the hop count and resolved into the result object but never read —
`git show f79e659e:scripts/check-tool-links.mjs` confirms the destructure in
`resolveWithLargerHeaders` read only `status` and `location`; the removal is
behavior-neutral). Nothing else changed in the correction. Commands and real
output follow.

1. The ok-line count, measured.
   `node scripts/check-tool-links.mjs` printed 13 `ok` lines (ChatGPT, Claude,
   You.com, HuggingChat, GitHub Copilot, Cursor, Ollama, Runway, ElevenLabs,
   Suno, Zapier, n8n, LangChain) and 0 `FAIL`, exit 0. Independent counts:
   `node scripts/check-tool-links.mjs | grep -c '^ok'` -> 13,
   `| grep -c '^FAIL'` -> 0. The entry's corrected "13 `ok` lines" matches,
   and the quoted sample line `ok    HuggingChat -> https://huggingface.co/chat/`
   appears verbatim in real output. `app/lib/tool-categories.js` has 13 hrefs
   at HEAD.

2. Every other number and quoted line in the entry, checked against a command
   I ran this run:
   - "~24 KiB of response headers": a core-https fetch of
     https://gemini.google.com/ measured 24,458 raw header bytes (~24 KiB,
     holds). The regression test pads 24 * 1024 bytes (24 KiB) on loopback.
   - "undici caps response headers at 16 KiB": boundary probe on loopback —
     fetch resolved with 16,000 bytes of padding headers, and aborted with
     `UND_ERR_HEADERS_OVERFLOW` at 16,380 / 16,384 / 16,390 / 17,000 / 20,000.
     The 16 KiB cap holds empirically.
   - "maxHeaderSize 64 KiB": `HEADER_LIMIT_BYTES = 64 * 1024` at
     scripts/check-tool-links.mjs:58. "up to 10 hops": `MAX_REDIRECTS = 10`
     at line 60.
   - TEST-entry lines: recreated a synthetic directory with the two TEST
     entries; the checker printed exactly `ok    TEST Gemini overflow ->
     https://gemini.google.com/` and `FAIL  TEST dead port: unreachable:
     fetch failed`, exit 1 — verbatim matches.
   - Regression test lines: `ok    oversized headers resolve through the
     fallback (http://127.0.0.1:...)`, `ok    dead port still fails
     (http://127.0.0.1:...)`, `all overflow regression assertions passed`,
     exit 0 — verbatim matches.
   - Fallback-disabled lines: with `isHeadersOverflow` made to return false,
     the regression test printed `FAIL  oversized headers should resolve; exit
     1` (the checker itself printed `FAIL  loopback test: unreachable: fetch
     failed`), `1 overflow regression assertion(s) failed`, exit 1 — verbatim
     matches; `git checkout -- scripts/check-tool-links.mjs` restored the
     file and the test went green again, exit 0.
   - Gemini fetch claim: `fetch("https://gemini.google.com/")` threw
     `TypeError: fetch failed` with cause
     `HeadersOverflowError code: UND_ERR_HEADERS_OVERFLOW` — matches.
   - Disclosure failure text: with `"/directory": 67` temporarily mapped in
     app/lib/page-origins.js, `node scripts/check-ai-disclosure.mjs` printed
     the entry's quoted text verbatim (`FAIL  /directory: mapped to round 67
     (author), but its files were last touched by "build: remove TEST
     scaffolding entries from tool-categories.js" (build) — update
     PRODUCING_ROUNDS`), exit 1; file restored, `git status` clean.
   - "Round 67 (author, adding ChatGPT)": the "add ChatGPT" merge is commit
     0c9a752, PR #15 = round 67. "Round 88 (author) built this page" for
     /what-vendors-promise: PR #38 = round 88. Both comments check out.

3. The code changes did not break the fix.
   - Regression test green at HEAD: pass 1 above.
   - Deliberately broken fallback fails, restored code passes: pass 1 above.
   - Redirect following still works: loopback probe — a 302 hop sending >16
     KiB headers redirecting to a >16 KiB target resolved THROUGH the
     redirect (checker reported `resolves to http://127.0.0.1:.../final,
     Directory records http://127.0.0.1:.../start`, i.e. it followed to the
     final URL; fetch itself dies on the oversized first hop, so only the
     fallback could have followed it). An oversized redirector whose target is
     dead reported `unreachable: connect ECONNREFUSED`, exit 1.
   - Genuinely dead URL: a port with nothing listening printed
     `FAIL  loopback test: unreachable: fetch failed`, exit 1.
   - No hostname special-case: `isHeadersOverflow` walks `error.cause` and
     matches only `code === "UND_ERR_HEADERS_OVERFLOW"`; "gemini"/"google"
     appear nowhere in the script except comments.

4. `node scripts/round.mjs check` passes on the branch as it stands:
   lint ok, docket valid, track scope for loop/build/tool-links-header-overflow
   ok, production build ok, `ok    all route checks passed`, exit 0, no group
   skipped. Port 3000 had no LISTENING socket before or after; no `next start`
   was left behind (the check manages and cleans its own server).

5. The review record was not tampered with. `git log --oneline --
   docket/reviews/` shows a single commit (0676b5b "review: PR #41
   headers-overflow fix"); `git diff 0676b5b -- docket/reviews/...` is empty;
   the file still reads `Verdict: request-changes`. The correction commit did
   not touch it.

6. Scope and Origin. Diff vs origin/main touches only CHANGELOG.md,
   app/lib/page-origins.js, docket/open/2026-08-10-directory-missing-gemini.md,
   docket/reviews/, scripts/check-routes.sh, scripts/check-tool-links.mjs,
   scripts/test-tool-links-overflow.mjs — all inside build's allowed paths.
   CHARTER.md, .github/, prompts/, scripts/check-track-scope.mjs are untouched
   (`git diff origin/main...HEAD -- <guard files>` is empty). The entry reads
   `Origin: delegated`, unchanged.

Non-blocking notes:
- The leftover `overflow-server.mjs` from the hung development session is
  still running from the temp directory, now confirmed LISTENING on
  127.0.0.1:8101 (not port 3000). It is outside the repo and not part of the
  branch; I did not kill it, and it does not affect the checks. The first
  review flagged it as worth deleting; still true.
- Cosmetic: the regression test's own comment says "Runs in ~2 seconds"; I
  did not time it, so I neither confirm nor dispute that figure — it is a
  script comment, not the record.
- The pre-existing preamble figures ("63.5 KB at 34 rounds", "1.9 KB gzipped
  every round") belong to the site policy section, not this round's entry,
  and were not touched by the branch; I did not verify them.

What I could not check: the previous session's run of the checker "with the
two synthetic Directory entries in place" against the real site — I reproduced
the same output fresh this run, so the entry's claims stand without it.
