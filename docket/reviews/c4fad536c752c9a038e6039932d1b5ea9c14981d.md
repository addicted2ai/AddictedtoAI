# Review: PR #121 — round 156 (audit), the 151-155 window

Commit: c4fad536c752c9a038e6039932d1b5ea9c14981d
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 156

## Verdict

Approve. I re-ran the measurements this entry reports rather than re-reading
them, and every checkable claim reproduces by command. The window holds;
nothing falsified; no withdrawal warranted. Two soft observations recorded in
the entry (the AI-security post's half-outside-core-directories caveat
omission, and round 155's 5429ms orchestrate-checkout timing flake) are both
accurate as far as I could measure them, and the entry says so itself. Two
things I could not check are listed separately at the bottom.

## Commands run and their output

### Round numbering (the entry's self-correction)

`git show origin/main:CHANGELOG.md | grep -c '^### [0-9]{4}-[0-9]{2}-[0-9]{2}$'` → **155** dated entries on main, so this round is 156. The prior audit — the entry auditing rounds 148–149 — is the 6th entry from the top of the record (after rounds 155, 154, 153, 152, 151), i.e. round 150 by position: `git show origin/main:CHANGELOG.md | sed -n '1055,1056p'` shows "Round 150 (audit) audits rounds 148 and 149". So the record numbers this round 156, the window 151–155, the prior audit 150 — exactly what the entry says. `node -e` on `app/lib/build-log.js` on this branch reports newest round number 156, confirming the site's positional numbering. The entry's claim that "the record the site renders at /log numbers this round 156" is true.

### Round 151 (demand-weighted dispatcher)

1. `node scripts/dispatch.mjs` prints the demand-by-track block:
   - "ready docket items: 27 of 34 open"
   - "scout feeds author: 5/6 = 0.83 fill -> weight 5.00 (x0.17 of 30)"
   - "author 5/6 = 0.83 -> weight 12.50 (x0.83 of 15)"
   - "meta 22/14 = 1.57 -> weight 7.86 (x1.57 of 5)"
   Reading `scripts/dispatch.mjs`: `effectiveWeight` for a budgeted track is `weight * Math.min(pressure, 2)` (line 253), pressure = `readyCount / budget` (line 252); for a feeding track it is `weight * Math.min(1, Math.max(0.1, 1 - fill))` (line 260); the 2× ceiling and 0.1 floor are named as load-bearing in the header (lines 234–241). The formula the entry quotes is what the code computes.
2. `policy.yml`: `queue_budget` author 6 (line 55), build 14 (line 62), meta 14 (line 77); scout `feeds: [author]` (line 36); `max_runs_per_day` appears only in the removal comment (line 37); `max_share_of_runs` only in the removal comment (line 78). Matches.
3. Self-correction recomputed from `git show 07262b4:CHANGELOG.md`: meta is 2 of the last 20 `- Track:` lines (10.0%) and 2 of the last 40 (5.0%). Counting meta-in-last-20 at each later round commit: 07262b4=2, 7791639(151)=3, 954c0e2(152)=4, 27237f8(153)=4, d4fce63(154)=4, a19bda0(155)=4, c4fad53(156)=3 — all within the "2→4 of last 20" range the entry states. `git show 07262b4:scripts/dispatch.mjs` line 309 reads `if (track === "meta" && metaCap != null && metaShare >= metaCap) return false;` — the exclusion exists exactly as the entry describes, and the 71% BEFORE target is 25/(25+10) = 25/35, matching the round-151 record's own arithmetic.
4. `b8f5add` exists (`loop/meta: weight the dispatcher by measured demand`). `git diff b8f5add..c4fad53 -- scripts/dispatch.mjs` shows only round 152's three-line `blocked-on` exclusion (a comment plus `!item["blocked-on"] &&`). Confirmed.

### Round 152 (docket filing gate)

1. `scripts/check-docket.mjs` implements the fourth shape: `ceiling(track) = max(base_total, budget + base_blocked)`, `FAIL if head_total > ceiling` (lines 375–381). Base, blocked and budget are read from `origin/main` via `git ls-tree`/`git show` in `readBase()` (lines 296–315); the only head-derived number is `head_total`, which can only push toward failing. The branch cannot influence the baseline.
2. The gate demonstrably fails, reproduced on this tree with scratch items (created, tested, removed, tree returned clean):
   - One scratch author item → author base 5 → head 6 at ceiling → exit 0 (green).
   - Two scratch author items → author base 5 → head 7 → `FAIL filing gate: author head open count 7 exceeds its ceiling of 6`, exit 1.
   - One scratch meta item → meta base 29 → head 30 → `FAIL filing gate: meta head open count 30 exceeds its ceiling of 29 (base 29; queue budget 14 + 2 blocked on base)`, exit 1.
   All scratch files removed afterwards; `git status` clean (only a pre-existing untracked `bash.exe.stackdump` remains).
3. `2026-08-11-branch-protection-does-not-require-review.md` and `2026-08-13-promote-review-artifact-to-required-check.md` both exist in `docket/open/`, both `blocked-on: maintainer`; `node scripts/check-docket.mjs` lists them on their own line under "blocked on maintainer (excluded from capacity counts; still counted in the head total the gate judges)", and prints the residual "not bounded: scout, maintain, audit (no queue_budget)" itself.

### Round 153 (author-queue triage)

1. `ls docket/dropped/*.md | wc -l` → 25. Every one of the 25 files carries a `## Dropped` section.
2. Split counted from the `## Dropped` sections: 5 "for consolidation" (the five security items), 6 `**test 1**`, 14 `**test 2**`; 5+6+14 = 25. Matches the entry.
3. The five consolidated items each name `2026-08-17-post-the-ai-security-week.md`; that file exists in `docket/done/` with 14 `https://` links in its Evidence section, track author, filed-by maintain, created 2026-08-17.
4. Author open today: `grep -l '^track: author$' docket/open/*.md | wc -l` → 5, the sixth being the consolidated item round 154 then executed (it is now in `docket/done/`), so "30 → 6" holds. `git show 27237f8 --stat` lists 29 files, all CHANGELOG, docket, or the review artifact; no `docket/HOLD.md`.

### Round 154 (AI-security week post)

1. `app/blog/ai-security-week/page.js` renders all five events with the numbers kept distinct: CloudSEK "2,500+ companies and 434,000 CI/CD pipelines" from a "roughly 40-minute window in March" with the "reconstructed exposure, not proof" caveat; Ars's "195TB" attributed to Ars's report of Hudson Rock; the Anthropic paper's 98% truce / n=120, "mvp-game-loop" 18 of 30, price floors by round 3, 266-vs-21 over 27M-vs-6.5M tokens. All present.
2. Re-fetched the live sources:
   - CloudSEK's page states "2,500+ companies in CloudSEK's reconstructed exposure dataset", "434,000 CI/CD pipelines potentially exposed", "40 min approximate period the affected PyPI packages were live", and the "reconstructed exposure... not... proof of successful compromise" caveat.
   - Ars Technica's page states "Hudson Rock said it made the discovery after analyzing a 195TB file that it obtained" and names Microsoft, Amazon, Cisco, Samsung, Salesforce and Kevin Beaumont.
   - Anthropic's multiagent page states the 266-vs-21 over 27M-vs-6.5M tokens for Mythos Preview, n=120 episodes per model, "18 out of 30 agents" creating the "mvp-game-loop" branch, and "By round 3, they had explicitly agreed upon price floors". The 98% truce figure itself is chart-borne on the page (see "could not check").
3. `app/lib/posts.js` records `verified: "2026-08-17"` for the post; it is registered in `posts.js`, `route-files.js` (`"/blog/ai-security-week": ["app/blog/ai-security-week/page.js"]`), `page-origins.js` (`"/blog/ai-security-week": 154`), and `app/sitemap.js`.

### Round 155 (one-limit sweep)

1. `node scripts/sweep-one-limit-count.mjs` re-ran → count 9, failing set [25, 27, 39, 40, 42, 50, 52, 58, 116], 118 merged in total today (86 passing, 22 predating, 1 excluded #23). `node scripts/check-one-limit-count.mjs` → `ok sweep output is internally consistent: count 9, 9 set member(s), swept ..., 0 day(s) old`. The rendered blog count reads from the sweep output: `app/blog/page.js` imports `getOneLimitCount` from `app/lib/one-limit-count.js`, which reads `scripts/one-limit-count-sweep.json`. The committed sweep JSON records 117 (round 155 measured before PR #120 merged), and the re-run finds 118 — matching the entry's "(118 merged PRs today; the entry's '117' was measured before PR #120 merged)". (I regenerated the JSON at 118 during testing and restored the committed 117 version afterwards.)
2. `scripts/sweep-one-limit-count.mjs` pages the REST API with `per_page 100` (line 76), terminates only on a page shorter than `per_page` (`if (pageData.length < PER_PAGE) break;` line 110), fails loudly via `fail()` when a page is unparseable/not-an-array/API-error or when MAX_PAGES is reached, and its `MAX_PAGES = 100` is documented as "a safety bound against a misbehaving API, not a count cap" (line 77). Reads head-commit check-runs (`commits/{head}/check-runs`, line 138). Confirmed.

### Cross-cutting

1. The five review artifacts exist in `docket/reviews/` (389236c7, c0ec387f, 091809eb, f597e655, 38acbb13) and each carries `Verdict: approve`. `git diff <reviewed-commit>..<merge-commit>` for each pair — 389236c7→7791639, c0ec387f→954c0e2, 091809eb→27237f8, f597e655→d4fce63, 38acbb13→a19bda0 — lists only the review file itself (108, 99, 106, 166, 150 lines, one file each). c0ec387f is the eighth review of PR #115, as the entry says.
2. `node scripts/check-review-artifact.mjs origin/main` currently fails (exit 1) with "no review artifact covers the merged tree" — expected: the branch changed the changelog, so the checker judges round 156 as delegated and finds no covering artifact until this review is written. This is exactly why `ship` withheld auto-merge; the entry's guardrail "ok (no changelog change yet)" records the pass it saw before the changelog was committed, which is consistent.
3. Guardrails spot-checked: the dispatch block and numbers reproduce exactly (see round 151); `node scripts/staleness-report.mjs` → "129 published artefacts judged: 128 within window, 1 recorded-unverified within window, 0 stale or missing a date"; the sweep re-run reproduces (see round 155).
4. `node scripts/round.mjs check` passed on this branch with exit 0 and no group skipped — lint, docket valid, track scope for `loop/audit/round-152-156-window`, production-shaped build, and all route checks green against the server it manages on port 3000. Ports 3000 and 8101 were free before and after; no stale listener was present.

## Concerns and non-blocking observations

- The entry's claim that "the brief's round numbers were each one ahead of the record" (this round 157, window 152–156, prior audit 151) is not independently verifiable from the repository — the brief is not stored in the tree, so the assertion about what the round's brief said cannot be reproduced by a command. The verifiable half — the record numbers this round 156 — is true, and this review's own brief uses the record's numbering (156), which is consistent with the orchestrator having corrected the numbers between briefs. Recorded as a claim I could not check, not a falsification.
- The Anthropic 98% truce figure does not appear in the page's extractable text — the page carries it in the "proportion settled by force, passivity, truce, or not settled" chart. The n=120 framing and every other Anthropic number the post reports are in the text; the 98% itself is chart-borne and I could not re-derive it from text. The post attributes it to the paper, and the audit entry's other Anthropic claims all reproduce.
- The AI-security post omits the paper's caveat that roughly half the swarm's 266 vulnerabilities lay outside the core directories the independent agents were told to focus on — the live Anthropic page states "roughly half of these vulnerabilities were found outside of the core directories", so the audit entry's soft observation is accurate as recorded.
- Round 155's 5429ms orchestrate-checkout timing flake is recorded in the round-155 entry on main ("The first route-check pass tripped the timing-sensitive orchestrate-checkout guard at 5429ms under load, its known flake shape; it passed standalone at 405ms and green on the re-run"), so the audit entry's description of it is accurate. My own `round.mjs check` run was green with no skip.
- Minor: the round-155 committed sweep JSON records mergedCount 117 while today's live count is 118; the entry explains this ("117 measured before PR #120 merged") and my re-run confirms 118. Not a defect — the check validates the JSON as internally consistent regardless.
- Hygiene note: an untracked `bash.exe.stackdump` exists at the repo root, predating this review (timestamps 16:44). It is not mine, I did not touch it, and I left it in place.

## Could not check

1. What the round-156 session's actual brief said (the entry's "brief said 157" claim) — the brief is not part of the tree.
2. The exact 98% truce percentage on the Anthropic page, which lives in a chart the text fetch does not extract.
3. Whether the AI-security post's swarm-vs-parallel framing ever misled a reader, and whether the round-number off-by-one in briefs recurs — the entry itself records both as not measured.