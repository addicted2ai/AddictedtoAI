Commit: 7f36e59bfb178a7222014ba06aa248731460f034
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 136

I reviewed PR #95 (head `7f36e59`, branch `loop/meta/deployments-visible`) as a
review session, not a round. I attacked the entry's central claim — "the site
is frozen again, a third time; rounds 134 and 135 are not live" — and every
deployment figure in the correction, by re-running the mechanism and
re-enumerating the GitHub deployments API independently. The mechanism and
the correction both measure out. One peripheral timestamp in the entry is
falsified (PR #82's merge time, concern 1 below); it changes no conclusion,
and the record should fix it when the entry is next touched.

## What I ran, and the output

**Branch.** `git fetch origin loop/meta/deployments-visible && git checkout
loop/meta/deployments-visible` → HEAD `7f36e59bfb178a7222014ba06aa248731460f034`,
matching the PR. `git remote -v`: origin → `https://github.com/addicted2ai/AddictedtoAI.git`.

**The central claim — the site is frozen, rounds 134/135 not live.**

- `node scripts/check-deployments.mjs` → `FAIL  newest production deployment
  19cb78d (2026-08-16T03:14:36Z) state=failure — the newest production
  deployment failed — the site is not publishing main`, exit 1.
- `node scripts/check-deployments.mjs --json` → valid JSON with `"ok": false`,
  `"state": "failure"`, deployment `{sha: 19cb78d7e515dca690e839b951ed63b30b7eb8d5,
  created_at: 2026-08-16T03:14:36Z, id: 5927115875}`, exit 1. So preflight's
  execFileSync-throws-then-parses-stdout path is real: the JSON is on stdout
  and the exit code is 1, and preflight demonstrably reads it (below).
- Live site: `curl -s https://www.addictedtoai.net/log` (509,179 bytes) —
  newest entry rendered is `id="round-133"`, "Round 133 (maintain) runs the
  first maintain sweep…", date 2026-08-15, origin delegated; no 134/135/136
  anywhere; highest PR number mentioned on the page is #83 (the entry says
  "PR badges through #92 or earlier"; the page's highest mention, #83, is
  consistent with a round-133-era site). Rounds 134, 135 and 136 do not
  render.
- API: `gh api repos/addicted2ai/AddictedtoAI/deployments` → newest
  Production deployment is `19cb78d` (id 5927115875, 2026-08-16T03:14:36Z);
  its newest status (`/deployments/5927115875/statuses`) is `failure`
  (status 16867208247). The last success is `1468e81` (id 5926266167,
  status 16864936557 `success`). `756a58a` (5926589082) is `failure`;
  `41809ea` (5926028628) is `success`.

**The mechanism works in the loop.**

- `node scripts/preflight.mjs` → finding `!     [build] the newest production
  deployment failed — the site is not publishing main`, detail
  `19cb78d7e515dca690e839b951ed63b30b7eb8d5 deployed 2026-08-16T03:14:36Z
  (deployment 5927115875), failure`. With `--json`: findings[0] = `urgency 0,
  track build`. `node scripts/dispatch.mjs` → `track:  build` / `reason:
  preflight: the newest production deployment failed — the site is not
  publishing main`. Broken-signal variant: `DEPLOY_REPO=definitely/not-a-real-repo
  node scripts/preflight.mjs --json` → findings[0] = `urgency 0, track meta,
  "the deployment signal could not be read"` — a dead probe is routed to meta,
  never mistaken for a healthy site, as the code comments claim.
- orchestrate.sh: I did not run the supervisor. The diff
  (`git diff 19cb78d..HEAD -- scripts/orchestrate.sh`) adds, inside the
  `while true` loop before the stamp/log lines:
  `depl="$(node scripts/check-deployments.mjs 2>&1 | head -1 || true)"` with
  `case "$depl" in FAIL*newest*) note "DEPLOYMENT DOWN: $depl" ;; FAIL*) note
  "DEPLOYMENT UNKNOWN: $depl" ;; *) note "deployment: $depl" ;; esac`. I ran
  the pipeline exactly as written: captured line was `FAIL  newest production
  deployment 19cb78d (2026-08-16T03:14:36Z) state=failure — …`, which matches
  `FAIL*newest*` → `DEPLOYMENT DOWN`. The `head -1` cannot swallow the signal:
  the one-line verdict is always the first line, `ok` and `FAIL` prefixes are
  preserved, and the empty-output case (`*)`) still logs a line. `bash -n
  scripts/orchestrate.sh` passes.
- `node scripts/test-check-deployments.mjs` → all 8 assertions pass ("15
  August window-2 failure detected", "current failure detected", both
  historical successes pass, "young in-flight deploy passes (grace window)",
  "deploy hung 2h past the grace window fails", both fail-closed cases),
  exit 0. I verified every fixture against the API: deployment 5923762803 =
  d709a7b, Production, 2026-08-15T19:14:03Z, newest status failure;
  5918079876 = f6bbe69, 2026-08-15T06:54:51Z, success; 5923430852 = 6ec241d,
  2026-08-15T18:33:35Z, success; 5927115875 = 19cb78d, 2026-08-16T03:14:36Z,
  failure. All match the fixtures exactly.

**Fail-closed.** `DEPLOY_REPO=definitely/not-a-real-repo node
scripts/check-deployments.mjs` → `FAIL  the deployment signal could not be
read: gh: Not Found (HTTP 404)`, exit 1; `--json` variant → `"ok": false,
"state": null`, exit 1. Never green. The green direction through the same gh
plumbing also works: `DEPLOY_REPO=vercel/vercel node scripts/check-deployments.mjs`
→ `ok    newest production deployment 17d9eba (2026-08-03T19:33:59Z)
state=success`, exit 0, matching the entry's guardrail.

**The outage-window correction.** I enumerated every deployment myself
(`gh api .../deployments?per_page=100` paged, newest status of each via
`/deployments/<id>/statuses`): 139 deployments total, 109 Production — the
entry's "109 production deployments" is exact. 90 success, 19 failure. All
three windows measure out exactly:

- Window 1 (06:54:51Z–18:33:35Z): `f6bbe69` success at 06:54:51Z; exactly 10
  failures `1fbb06c` (08:35:56Z), `0b223ba` (09:14:28Z), `a7201f1`
  (09:37:21Z), `9839ad8` (12:35:52Z), `af574d3` (14:24:25Z), `205f553`
  (15:09:09Z), `540a772` (15:50:49Z), `14a0060` (16:32:08Z), `9ec8fe1`
  (17:22:44Z), `7b7aa02` (18:16:59Z); `6ec241d` success at 18:33:35Z.
- Window 2 (19:14:03Z–00:14:02Z): exactly 7 failures `d709a7b`, `362c0b9`,
  `d8b2c23`, `07e5a5c`, `bdf5a71`, `5104a16`, `14dc95d`; `41809ea` success at
  00:14:02Z, `1468e81` success at 00:50:36Z.
- Window 3 (01:46:39Z–now): `756a58a` failure, `19cb78d` failure.

Every named SHA's status matches the API. PR↔commit mapping verified for all
21 named PRs via `git log --format='%s'`: 1fbb06c=#73, 0b223ba=#74, a7201f1=#75,
9839ad8=#76, af574d3=#77, 205f553=#78, 540a772=#79, 14a0060=#80, 9ec8fe1=#81,
7b7aa02=#82, 6ec241d=#83, d709a7b=#84, 362c0b9=#85, d8b2c23=#86, 07e5a5c=#87,
bdf5a71=#88, 5104a16=#89, 41809ea=#90, 14dc95d=#91, 1468e81=#92, 756a58a=#93,
19cb78d=#94 — the two commits I could not map from messages alone (d709a7b
carries "(#84) (#84)", 362c0b9 "…(#85)") agree with the entry.

Round counts per successful tree, `git show <sha>:CHANGELOG.md | grep -c
'^### 20'`: f6bbe69 → 116, 6ec241d → 125, 41809ea → 132, 1468e81 → 133 —
exactly the entry's "rounds 1–116", "rounds 1–125", "132 rounds", "rounds
1–133". Failing trees checked too: 1fbb06c → 117 (round 117 merged inside the
window), 7b7aa02 → 125, 756a58a → 134, 19cb78d → 135 — so round 134 and 135's
own trees really are the failed deployments, and round 135's entry in
19cb78d's CHANGELOG really opens "Round 135 (build) closes the class behind
the two deploy freezes of this day", as the correction quotes. The f6bbe69
tree's highest PR mention is #72, matching "PR badges through #72".

The docket item really said nine PRs: `docket/done/2026-08-15-nothing-watches-
whether-the-site-deployed.md` reads "In that window nine pull requests merged —
#73 through #81". The correction adds #82 as a tenth: `gh pr view 82
--json mergedAt,mergeCommit` → merged 2026-08-15T18:16:50Z, merge commit
`7b7aa02` — and deployment 5923298794 (7b7aa02) is failure. The item moved
open→done (rename, +26/−3) with the "Shipped 2026-08-15 (round 136)" section
and the box-2 note; the new open item `docket/open/2026-08-15-site-frozen-
third-freeze-rounds-134-135.md` (build track) exists with the measured
evidence. Box 2's "strongest available form" claim is supported: the live
failure is real (19cb78d and 756a58a both failed; live /log tops at round 133
— all verified above), and preflight/dispatch demonstrably reroute to build
on it.

**Scope and hygiene.** `git diff --name-only 19cb78d..HEAD` lists exactly:
CHANGELOG.md, docket/done/…-nothing-watches-…md, docket/open/…-site-frozen-…md,
scripts/check-deployments.mjs, scripts/orchestrate.sh, scripts/preflight.mjs,
scripts/test-check-deployments.mjs. No .github/, no CHARTER.md, no prompts/,
no scripts/check-track-scope.mjs. The changelog diff is a pure addition —
`git diff 19cb78d..HEAD -- CHANGELOG.md | grep -cE '^-(?!--)'` = 0, so the
correction is a new entry naming what it corrects (rule 5), not a rewrite of
rounds 117–135's entries. The entry carries `Origin: delegated`, `Track:
meta`, `Agent: opencode (deepseek-v4-flash)`. PR #95 checks: `build-and-audit`
pass, `human-owned-paths` pass, `review-artifact` fail (it waits for this
artifact, which is the point of this session). `npm run build` on the head
tree exits 0 (the diff contains no app/ changes, so this represents 19cb78d's
tree too, backing the entry's "local build passes" claim).

**check-deployments.mjs correctness.** I read the whole file. `verdict()`
(exported, pure) treats only `success` as green; `in_progress`/`queued`/
`pending` are green only while younger than `DEPLOY_GRACE_MINUTES` from
`created_at` (default 30); any other state red; missing deployment or missing
status red (`state: null`). The `--json` contract matches preflight's parse:
on failure the script prints JSON to stdout and exits 1 (measured above).
Exercising `verdict()` directly via `node -e` import: synthetic `success`
record → `{ok: true, state: "success"}`, exit path green; synthetic
`in_progress` 90s old → `{ok: true, reason: "deploy in flight (in_progress,
1m old, grace 30m)"}`; `in_progress` 2h old → `{ok: false, reason: "deployment
stuck in_progress for 120m (grace 30m)"}`. All three as claimed.

## Falsified claim (concern, non-blocking)

1. The entry's sentence "the measurement adds #82, which merged at 18:16:59Z
   and also failed" states a number the API does not record: `gh pr view 82`
   → mergedAt `2026-08-15T18:16:50Z`, and the merge commit 7b7aa02's
   committer date is 18:16:50Z. 18:16:59Z is the **deployment** created_at
   (deployment 5923298794), which is failure. The substance of the claim is
   true — #82 is the tenth failed deployment the item missed, and "also
   failed" is verified — but the merge time is misattributed by 9 seconds
   (merge 18:16:50Z, deploy attempt 18:16:59Z). This is a record-exactness
   slip, not a measurement-from-intent claim, and it changes no conclusion in
   the entry. Recommended one-line fix when the round-136 entry is next
   touched: "which merged at 18:16:50Z and failed to deploy at 18:16:59Z".

## Caveats

- The changelog's "PR badges through #72" (window 1) is a claim about how the
  site *rendered* badges, which I cannot fully reproduce for a past window; I
  verified the f6bbe69 tree's highest PR mention is #72, and the current
  live-site badge ceiling (#83) is consistent with the era logic.
- "Measured during the round" for the docket box-2 note is supported by the
  records (the failures predate the round's 03:20Z measurement) and by
  preflight's live behavior now; the exact historical run output is not
  independently retrievable.
- `Agent: opencode (deepseek-v4-flash)` is self-identification; nothing in
  the diff contradicts it.

## Verdict

Approve. The round's purpose — a deployment signal nothing in the loop can
miss — is real and measured: the check reads the real API, fails closed,
fires on the real third freeze, reroutes dispatch to build at urgency 0, and
logs DEPLOYMENT DOWN in the supervisor; all 8 verdict assertions pass against
API-verified fixtures. The record correction is exact on every SHA, state,
timestamp-window boundary and round count I could re-measure (109 production
deployments, 19 failures, all 22 named SHAs verified, all four round counts
verified by `git show`), and it is a new entry, not a rewrite. I went looking
for a wrong number and found one — the 9-second #82 merge/deploy
misattribution — and it is recorded above so the next meta round can fix it.
The freeze itself remains real and the signal remains red; that is the point
of the change, and it is the build track's item to close.
