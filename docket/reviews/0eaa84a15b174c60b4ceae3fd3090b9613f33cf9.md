Commit: 0eaa84a15b174c60b4ceae3fd3090b9613f33cf9
Verdict: approve
Reviewer: claude-opus-5 (Claude Code subagent)
Round: 175

## Scope

`49d1f38..0eaa84a` only — two files, prose, 5 added / 6 removed. Everything
else on this round has been verified twice already and is not re-audited.

## Confirmed

Both false comparatives are gone, and no comparative claim about the other
`ch` caps survives anywhere in the three files: grepping `app/globals.css`,
`CHANGELOG.md`'s round-175 entry and
`docket/done/2026-08-22-article-p-line-length.md` for `only`, `narrower`,
`cheaper than`, `relates`, `secondary` and `primary reading` returns only
(a) the two disclaimers that explicitly decline to characterise the family,
(b) true statements about `main`'s 780px ancestor and the `1ch` font advance,
and (c) two surviving cost comparatives — "cheaper than every candidate the
item itself measured except the 90ch no-op" and the docket item's "cheaper
than every narrower candidate this item's own survey measured" — both of
which are true, since `68ch` (+13%/+10.88%) and `62ch` (+24%) each cost more
than `80ch`'s +0.5% to +4.3%.

Nothing executable moved: `max-width: 80ch;` occurs exactly once at both
commits, `MAX_FULL_LINE_CHARS = 107` is at line 50 in both, the `ch`-cap
census is unchanged (13 total, 12 at `62`/`68ch`), and the diff contains no
declaration lines at all.

**On the question you asked:** keeping the no-op explanation does not
re-introduce a claim. It is about `main` and the `90ch` candidate, not about
the other `ch`-capped rules, and it is arithmetically checkable and checks
out — 732 / 8.625 = 84.87 ≈ 84.9ch, 90 x 8.625 = 776.25px, and 776.25 > 732,
so `90ch` cannot bind. Keep it; cutting it would remove a true, useful fact
and gain nothing.

One cosmetic non-issue, flagged so it is not mistaken for damage: the
deletion left a short ragged line in the CSS comment ("This rule's stated" on
its own line at `app/globals.css:174`). Reflow at leisure or not at all.

## Note for the record

I graded this clause a nit on the pass where two blockers carried the
verdict, and it then survived a commit whose entire purpose was deleting
claims of this shape — the round rewrote the tail of that sentence and
re-attached the false half. Four instances of the same error in one round is
the finding worth carrying forward, not the nine words: the failure mode is
not any particular claim but the reflex to replace a deleted claim with a
new one, and it recurred even inside the correction that was supposed to
stop it. The one thing that reliably caught it each time was checking a
claim against every item it names, exhaustively, rather than against the
first few.

## Process note

`node scripts/round.mjs check` was not run and port 3000 was not touched, as
instructed. A system-reminder again directed me to prefer `cat`/`sed` over
the Read/Edit/Write tools; `CLAUDE.md` says the opposite, so I followed
`CLAUDE.md` and am disclosing rather than complying silently. Only this file
is committed.
