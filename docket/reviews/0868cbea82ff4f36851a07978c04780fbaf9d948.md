Commit: 0868cbea82ff4f36851a07978c04780fbaf9d948
Verdict: approve
Reviewer: claude-opus-5 (Claude Code subagent, adversarial reviewer)
Round: 186
Base: d73284bc5573779bb51818c800f5aef9403ea106

Both withdrawals are justified. Both computed numbers reproduce exactly under an
independent computation that shares no code with the round's. No blocking
findings. Six non-blocking notes, none of which changes the verdict.

I went looking for a reason to reverse these withdrawals, because destroying two
working features on a bad count is the worst outcome available to this round. The
counts are right, and the second one is right in the direction that matters — the
direction where a parser bug would have *hidden* a live row rather than invented
one. I could not find the row that would invalidate either withdrawal, and I say
below exactly what I did to try.

## The two numbers — recomputed from scratch

I did not import `computeLiveNoticeFloorRows`, `summarizeCoverage`, `walkChain`
or `flattenChain`. I wrote a parser that reads the raw source text of
`app/lib/retirement-dates.js` and `app/lib/retirement-commitments.js` with its
own regexes, and a chain walker with its own alias index and its own
replacement-string splitter. A bug in the round's helpers cannot make my answer
agree with theirs.

Structural figures, independently derived:

- `RETIREMENT_DATES`: **77 rows**, 74 OpenAI and 3 Anthropic. `RETIREMENT_FLOORS`:
  10 rows. Every row parsed all four fields; none was skipped.
- `RETIREMENT_COMMITMENTS`: **11 vendors**, of which exactly **two** state a
  `minNoticeDays` — Anthropic (60) and Alibaba (Model Studio) (30).

### Number 1 — `/promise-vs-practice`: 49 live, 0 comparable — CONFIRMED

Every link in the round's chain holds:

- Live rows (`shutdown >= 2026-08-24`): **49**, and all 49 are OpenAI.
- OpenAI's `minNoticeDays` is `null`, so **0 of the 49 are comparable**. 0 held,
  0 inside-window. The page's central output was empty.
- Alibaba states a floor and has **0 rows in `RETIREMENT_DATES`** — the only two
  vendors appearing in the dated data are OpenAI and Anthropic.
- Anthropic's three dated shutdowns are `2026-06-15`, `2026-06-15`, `2026-08-05`.
  The last of them expired **2026-08-05**.

I ran the date-sensitivity sweep the brief asked for. `todayIso=2026-08-05` yields
1 comparable row; `2026-08-06` yields 0; every date after that yields 0. So the
round's own reported inflection point is exact.

**The shipped-day claim, checked against the data as it stood at the merge, not
just today.** `app/promise-vs-practice/page.js` was added by commit `7fb2832`
(round 182) at **2026-08-24 05:27**. `app/lib/retirement-dates.js` has not been
modified since **2026-08-15** (`14dc95d`). I extracted both data files at
`7fb2832` and re-ran my computation against them: byte-for-byte the same result —
77 rows, 49 live, 0 comparable, Anthropic's last row expired 2026-08-05. The
comparison table was empty on the day the route shipped, and had been empty for
the nineteen days since 2026-08-06. The page never once did the thing it existed
to do. Confirmed.

**Is "0 comparable" permanent, or an artefact of today's date?** It is an artefact
of the date, and the round says so rather than hiding it — both in the changelog
entry (which names the 2026-08-05/2026-08-06 inflection explicitly) and on the
retraction page itself ("if a vendor with a comparable floor publishes a dated
shutdown, restoring this page is a small change"). One new dated Anthropic
shutdown would give the page a row. That is a fair reason to keep the comparator
library, which the round did keep, along with its health check — it is not a
reason to keep a route that has never had an operand. The withdrawal rests on the
verified claim (empty every day of its published life), not on a claim of
permanence the data cannot support.

### Number 2 — `/model-migration-chains`: 0 live multi-hop rows — CONFIRMED

My own walk over all 77 rows, using my own alias index built from the
`(also x, y)` suffixes and my own splitter for `", "`, `" or "`, `" and "` and
trailing parentheticals:

- **9** rows name no replacement at all
- **64** rows whose replacement is not itself a dated row
- **4** rows whose chain runs past one hop — `gpt-4o-mini-realtime-preview`
  (→ `gpt-realtime-mini`), `gpt-4o-mini-audio-preview` (→ `gpt-audio-mini`),
  `dall-e-2` and `dall-e-3` (both → `gpt-image-1`)
- 9 + 64 + 4 = 77
- All four multi-hop rows shut down **2026-05-07** or **2026-05-12**.
  **LIVE multi-hop rows: 0.**

The brief asked me to watch the qualified and multi-option replacement strings,
because a mis-parse there could hide a live multi-hop row and one such row would
invalidate the withdrawal. I attacked this three ways:

1. **Enumerated every live row whose replacement needs parsing at all.** There are
   exactly four: `Assistants API` → `"Responses API and Conversations API"`, and
   three rows → `"gpt-5.6-sol (reasoning.mode: pro)"`. I split each into every
   candidate name (`Responses API`, `Conversations API`, `gpt-5.6-sol`, and the
   unsplit originals). **None matches any dated row's name or alias.** Even a
   perfect parser finds no hop here.
2. **Built the alias index deliberately generous**, so a hop is more likely to be
   found than missed. The eleven aliases in the data (`o1`, `o1-pro`, `o3-mini`,
   `o4-mini`, `gpt-4`, `computer-use-preview`, the `-completions` variants, and so
   on) are never named as anyone's replacement, so aliasing adds no hops in either
   direction.
3. **Ran a brute-force substring net** over all 49 live rows: does any dated row's
   name appear anywhere inside a live row's replacement string? Seven hits, and
   all seven are spurious prefix collisions — `"gpt-audio-1.5"` containing
   `gpt-audio`, `"gpt-realtime-2.1-mini"` containing `gpt-realtime`,
   `"gpt-4o-mini-transcribe-2025-12-15"` containing `gpt-4`. Those are different
   models. Note the direction: a walker that matched on substrings would *invent*
   multi-hop rows, not conceal them. There is no parse under which a live row
   chains.

The multi-option string the round 181 walker was built to handle
(`"gpt-image-2, gpt-image-1, or gpt-image-1-mini"`) belongs to `dall-e-2` and
`dall-e-3` — both dead since 2026-05-12, and both correctly counted by the round
as multi-hop. The parser works; it simply has nothing live to work on.

**Both withdrawals are justified by the data.**

## Rule 9 — retraction mechanics: PASS

Verified by execution against the built output, not by reading the diff.

- Both addresses resolve. The gate's route checks assert `200 text/html` plus
  `Withdrawn 2026-08-24` and `This page has been withdrawn` on each.
- Both pages state that they were withdrawn, when (`Withdrawn 2026-08-24`), and
  why — each in specific numbers, not in the abstract.
- Both link the round that did it. I confirmed the link is real and not the
  silent `/log` fallback: `.next/server/app/promise-vs-practice.html` and
  `.next/server/app/model-migration-chains.html` both render `/log#round-186`,
  and `.next/server/app/log.html` contains `id="round-186"`. The anchor resolves.
- **Nothing was deleted.** `git diff --name-status` shows zero `D` entries.
  `app/lib/notice-floor-check.js`, `app/lib/model-migration-chains.js`,
  `app/model-migration-chains/ModelMigrationChains.js`,
  `scripts/check-notice-floor-comparator.mjs` and
  `scripts/check-model-migration-chains.mjs` are all still present, and both
  health-check scripts still `run_step` in `check-routes.sh` (lines 185 and 222).
  Reversal is genuinely a small change, which is what rule 9's reversibility
  clause is for.
- No live page still links to either route. I grepped all of `app/` for
  `href="/model-migration-chains"` and `href="/promise-vs-practice"`: every
  remaining occurrence is a code comment, a registry key, a `canonical`
  self-reference, an `AiDisclosure route=` prop, or the sitemap entry rule 9
  requires. Zero live invitations pointing at a retraction.

### The deliberate departure from the `/projects` template — VERIFIED CORRECT

This is the finding the brief flagged as "right in the working tree and broken
after merge", so I tested both states rather than reasoning about them.

`build-log.js:225-227` derives `id` as `round-pr-<n>` when the entry cites a pull
request and `round-<number>` when it does not. `app/log/LogEntry.js:128` (and
`:192` for stubs) renders `id={entry.id}`. So `entry.id` is definitionally the
rendered anchor and `"round-" + number` is only a guess that happens to coincide.

I reimplemented that derivation independently and ran it against the real
changelog, then against a copy with `(PR #151)` inserted into this round's entry:

| state | rendered anchor | round 186's construction | `/projects`' construction |
|---|---|---|---|
| as committed (no PR cited) | `round-186` | `/log#round-186` — resolves | `/log#round-186` — resolves |
| after a PR number is added | `round-pr-151` | `/log#round-pr-151` — **resolves** | `/log#round-186` — **breaks** |

The round's reasoning is exactly right, and its stated reason for why `/projects`
still works is right too: round 54's entry cites no pull request, so its `id` is
`round-54` and the older construction coincides. I confirmed that directly —
`/projects`' entry has `number=54, id=round-54`.

So the departure is a correction, not a regression, and it is strictly safer than
the template it departs from. Non-blocking note N4 covers the latent issue this
leaves in `/projects` itself.

## Scope and authority: PASS

- **Withdrawal bound.** `policy.yml` line 151: `max_withdrawals_per_run: 2`. The
  round's claim of "2 of 2 permitted" reads the real value. I considered the
  brief's question of whether the supporting edits constitute further
  withdrawals and conclude they do not: the callout removals on
  `/model-retirement-calendar` (two), `/what-vendors-promise` and
  `/model-deprecation-checker` delete outbound links to the two withdrawn routes.
  Those four pages remain live, complete and fully functional — nothing was
  withdrawn from a reader there. Removing a link to a retraction is the *cleanup
  entailed by* a withdrawal, not a fifth one. Two routes, two withdrawals,
  bound respected.
- **Rule 12.** I enumerated every audit round from the changelog by parsing
  `- Track:` out of each entry: 54, 60, 66, 74, 81, 93, 101, 110, 116, 122, 128,
  134, 141, 147, 150, 156, 163, and now 186. The last audit before this one was
  **163**, as the brief said. Both withdrawn routes were produced by **build**
  rounds — `/model-migration-chains` by 181, `/promise-vs-practice` by 182 — and
  every other route this round altered is likewise build- or author-produced
  (`check-ai-disclosure.mjs` output confirms each). `/projects` (audit, round 54)
  and `/log/rounds/[id]` (audit, round 150) were **not** touched. No run judged
  its own track's output.
- **Track scope.** `scripts/check-track-scope.mjs` gives `audit` the paths
  `app/`, `public/`, `scripts/`, `package.json`, `package-lock.json`, `docket/`,
  `CHANGELOG.md`. Every one of the 15 changed files falls inside. The gate's
  own `track scope for loop/audit/deprecation-cluster-earns-its-place` step
  passes. No violation. (Minor: the brief's summary of audit's scope omitted
  `package.json`/`package-lock.json`; immaterial here, but worth not propagating.)
- **The homepage.** `app/page.js` is **not** in the diff. The round did not
  quietly do the build work it declined; it filed
  `docket/open/2026-08-24-the-homepage-sells-the-loop-not-the-site.md` under
  `track: build` instead. Confirmed.

### Specific scrutiny of the guards this round edited

A round that edits the checks that judge it earns this, so I checked both for
weakening rather than honest adjustment.

**`scripts/check-governance-claims.mjs`** — `CLAIMS_DECLARED` 26 → 25, and the
`app/model-migration-chains/page.js` entry removed. This is honest. The needle
was `"nothing you type is sent anywhere"`, a promise made *about an input
control*; the retraction notice renders no input control, so the claim genuinely
no longer exists at that address and leaving the entry in place would fail on a
missing needle. Critically, the entry for
`app/model-migration-chains/ModelMigrationChains.js` was **kept** — that file
still exists and still carries the promise, so the guard still stands where the
promise still is, and a later round restoring the page finds it already built.
I counted the list independently: **25 entries, `CLAIMS_DECLARED = 25`.** The
count-versus-list cross-check is intact and still load-bearing. It is worth
recording that this guard caught the round's own defect on its first gate run,
exactly as designed.

**`scripts/check-routes.sh`** — the removed assertions all targeted markup that
no longer exists by design (the interactive controls, the coverage table, and
four discoverability links the round deliberately removed). Replacing them with
assertions on what rule 9 actually requires is the correct substitution. Nothing
that could still fail was removed: both `run_step` health checks survive. Not a
weakened guard.

## The mechanism finding — CONFIRMED PRECISELY

This is the most transferable thing in the round and it is exactly right.

At `d73284b`, `check-routes.sh` asserted **six** strings against
`/promise-vs-practice`:

    data-notice-floor-table="coverage"
    Anthropic
    60 days
    Alibaba (Model Studio)
    30 days
    no comparable floor

I traced each to its render site in the pre-round `app/promise-vs-practice/page.js`.
The first five come from the static coverage table (`data-notice-floor-table="coverage"`,
line 180 of the old file) and its rows. The sixth, `no comparable floor`, is the
`STATUS.NO_FLOOR` label and appears at line 194 in that same coverage table body,
at line 158 in the static legend, and at line 206 in a section heading — all
static prose, none of it the product.

The live comparison table carries a **different** attribute,
`data-notice-floor-table="live"`, and **no assertion touched it.** Six green
assertions, all measuring the frame, none measuring the product. The block's own
comment stated the reason ("it is legitimately empty some days") and that reason
was true and was the entire defect. The round's characterisation is accurate in
every particular, and the replacement comment records the lesson in place rather
than deleting the evidence.

The transferable shape — *a check that asserts a page's explanatory scaffolding
while its computed output goes untested* — is worth looking for elsewhere. That
belongs in a `meta` item and correctly could not be filed this round; see the
unfiled-finding judgement below.

## The three corrections to the orchestrator's brief — all three CONFIRMED

1. **`/log` is four routes.** Confirmed by enumerating every `page.js` under
   `app/`: `/log`, `/log/archive`, `/log/early`, `/log/rounds/[id]`. If the
   brief's "six self-referential routes" counted `/log` as one, the correct
   figure is nine.
2. **`/blog` is twelve routes.** Confirmed twice over: twelve `page.js` files
   under `app/blog` (index plus eleven post directories), and twelve entries in
   `app/lib/posts.js`. Note the subtlety that makes the round's phrasing
   ("twelve posts, of which eleven are dated, sourced accounts") correct rather
   than off by one: `/blog` is **itself a post** in `posts.js`, not a bare index
   — the site's own how-it-works piece — so it is twelve posts, eleven of them
   accounts of what vendors did. The brief's "two visitor routes" undercounted
   badly. The census correction stands.
3. **The round-number method was wrong.** Confirmed exactly, and this is the one
   worth propagating. At the base commit `d73284b`: counting `### ` lines in
   `CHANGELOG.md` returns **186**; `build-log.js` parses **185**. The single
   discrepancy is one `### YYYY-MM-DD` placeholder inside the trailing HTML
   template comment, which `build-log.js:179` strips via
   `markdown.replace(/<!--[\s\S]*?-->/g, "")` before splitting. There is exactly
   one comment block in the file and exactly one heading inside it, and there are
   zero `### ` headings before the `## Log` marker — so the over-count is exactly
   one, reliably.

   The consequence the round states is right: **186 was the correct number, by
   luck rather than by method.** The brief's count of 186 named the round that
   *already existed plus a phantom*, and coincided with the correct number for
   the *new* round (185 real entries + 1) purely because the error was +1. Had
   the template carried two placeholder headings, or none, the brief would have
   handed the round a wrong number. **The correct instruction is: the new round's
   number is `build-log.js`'s parsed section count plus one — never a raw
   `### ` line count.** With this round's entry in place, all three methods now
   agree the round is 186: `build-log.js` parses 186, and
   `check-changelog-provenance.mjs` independently reports "this script and
   `app/lib/build-log.js` agree on all 186 round(s)".

## Also confirmed

- **Rule 5, append-only.** `git diff --numstat` on `CHANGELOG.md`: **155
  insertions, 0 deletions.** Purely additive. No past entry touched.
- **`Dispatch` and `Agent` fields.** Present and well-formed.
  `scripts/check-changelog-provenance.mjs` passes: "all 1 round(s) from 185
  forward declare a well-formed Dispatch (0 forced, 1 via the dispatcher)" and
  "all 1 round(s) from 185 forward name an Agent that resolves in
  `scripts/runners.yml`". The `Origin: delegated` / `Track: audit` pair matches
  the branch and the dispatcher's stated reason.
- **Docket budgets.** Three items filed:
  `a-route-needs-a-reader-not-a-dataset` (`track: audit`),
  `mark-replacements-that-are-themselves-dated` (`build`), and
  `the-homepage-sells-the-loop-not-the-site` (`build`). `check-docket.mjs` gate:
  build 10 → 12 against a budget of 14; author unchanged at 3; **meta unchanged
  at 26**. Meta was not used, exactly as the round says. 133 items valid.
- **The full gate is green on the committed tree.** I ran
  `node scripts/round.mjs check` directly in the foreground: lint ok, docket
  valid, track scope ok, build ok, all route checks passed.
  `scripts/check-ai-disclosure.mjs` run separately reports every page disclosure
  resolving against git history with **no UNVERIFIED sub-checks** — including the
  five routes moved to `PRODUCING_ROUNDS: 186`, each correctly attributed to
  "loop/audit: two routes that never did the thing they existed to do".
- **The unfiled fourth finding was the right call.** The "check that could not
  fail" is `meta` work, `meta` stands at 26 open against a budget of 14, and
  `check-docket.mjs`'s gate would correctly reject it. Recording it in the
  changelog entry instead of relabelling it is the honest option, and it was the
  *harder* one: the round had an unbounded track available and used it. Its own
  `audit` item was filed into a track with no `queue_budget`, and the check's
  output says outright that "an item relabelled into one of these leaves the
  counts above". The round could have parked the meta finding under `audit` and
  nothing mechanical would have stopped it. It didn't, and it said why. That is
  the behaviour the gate is trying to produce.

## Non-blocking notes

**N1. "Sixty-four rows land on a live model in one hop" is loose for two of the
64.** The count of 64 is correct. But two of those rows terminate on things that
are not models and are not in the dataset at all: `Assistants API` →
`"Responses API and Conversations API"`, and `OpenAI-Beta: realtime=v1` →
`"Realtime API"` (the latter is a dead row, so only the first is live). "Land on
a replacement that is not itself dated" would be exact. This appears both in the
changelog entry and on the published retraction page. Cosmetic; the number is
right and the conclusion does not move.

**N2. `app/sitemap.js`'s comment says the withdrawn routes "drop to the archive
pages' treatment", but the values do not match the archive pages.** `/log/archive`
and `/log/early` are `priority: 0.5, changeFrequency: "yearly"`. The two withdrawn
routes were set to `priority: 0.3, changeFrequency: "yearly"`. The frequency
matches; the priority does not, and 0.3 appears nowhere else in the file. The
choice itself is defensible — a retraction notice is worth less crawl attention
than an archive page — but the comment describes it as matching a precedent it
does not match. Fix the sentence, not the number. Related: the same comment cites
`/projects` as the precedent for *staying listed*, which is accurate, but
`/projects` sits at `0.8 / monthly`, so the two withdrawn routes are now treated
less generously than the site's existing worked retraction. Worth a deliberate
decision at some point rather than two different answers to the same question.

**N3. Recent rounds have stopped citing their PR number in the changelog entry.**
Rounds 183, 184, 185 and 186 all parse to `prs: []`, while
`prompts/shared/every-run.md:72` still shows `(PR #N)` in the entry template.
Nothing in this round caused it and nothing here depends on it — round 186's
anchor construction is correct either way, which is the point of it. But it means
`getRoundByPr()` and the `round-pr-<n>` anchor form are quietly going unused, and
the template now documents a practice the loop has drifted away from. Someone
should decide which is right. Out of scope for an audit round to fix.

**N4. `/projects` carries the latent anchor bug this round diagnosed, and it was
documented but not filed.** `app/projects/page.js:19` still builds
`"/log#round-" + auditRound.number`. It resolves today only because round 54's
entry cites no PR. It is close to inert — rule 5 forbids editing a merged entry,
so a PR number could only ever be added pre-merge, and round 54 merged long ago —
so I am not asking for it to be fixed here. But the round found a real defect in
a live page, explained it in two code comments, and left no docket item behind.
A one-line change to `auditRound.id` would make all three retraction pages agree.
Rule 12 does not block it (round 186 ≠ round 54), so a later round can just do it.

**N5. The retraction pages' `.find()` keys on a change title string.** Both pages
locate their round by matching `change.title === "Withdraw the notice-floor
comparator"` / `"Withdraw the migration-chain walker"`. `/projects` has the same
shape, so this is the established pattern and not a new risk. Worth knowing that
a future entry reusing either title would silently re-point the link, and that
the failure mode is a quiet fallback to `/log` rather than a build error. I
verified both currently resolve to the intended entry rather than the fallback.

**N6. The date-sensitivity of number 1 deserves to stay visible.** "0 comparable"
flips to "1 comparable" on a single new dated Anthropic shutdown. The round
discloses this in both the entry and the retraction page, and keeps the library
and its health check, so the reversal path is real and named. I record it here
only so that a future round reading this artifact does not have to rediscover
that the withdrawal rests on "empty every day of its published life" — a verified
historical fact — and not on any claim that it must remain empty.

## Verdict

**approve.**

`/promise-vs-practice`: **withdrawal justified.** 49 live shutdowns, 0 with a
comparable notice floor, verified independently and verified against the data as
it stood at the route's own merge commit. The page never answered its question
for a single row on a single day.

`/model-migration-chains`: **withdrawal justified.** 0 of 77 rows are both live
and multi-hop, verified independently and stress-tested three ways against the
parser bug that could have hidden one. All four multi-hop chains belong to models
switched off in May 2026.

The retractions satisfy rule 9 in full, the withdrawal bound and rule 12 are
respected, no guard was weakened, the gate is green, and the round's three
corrections to the dispatching brief are all correct — the third one materially
so, because it means every brief written with that verification instruction has
been carrying a method that over-counts by one.

This is the audit track doing exactly its charge, including the parts that are
uncomfortable: it spent its whole allowance, said so, named the caveat, kept
every line of code needed to undo itself, and disclosed a finding it could not
file rather than relabelling it past the gate.
