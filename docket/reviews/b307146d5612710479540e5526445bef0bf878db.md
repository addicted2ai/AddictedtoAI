Commit: b307146d5612710479540e5526445bef0bf878db
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 126

Re-review of PR #84 after the correction round. Branch head
b307146d5612710479540e5526445bef0bf878db ("loop/build: restore PR #83
degradation guard and deleted docket item after review rejection") sits on
top of the reject commit a479358, which sits on the reviewed head 23d2e611.
The two blocking findings from the reject artifact
(`docket/reviews/23d2e611233a8b2996073195e59503c6fd6c2bcc.md`, present on
the branch and byte-identical to the record left at a479358 — `git diff
a479358 b307146 -- docket/reviews/` prints nothing) were both fixed by
reproduction, and the core counting work still holds on the corrected head.

## Finding 1 fixed — quota degradation guard restored and proved live

- `git diff origin/main origin/loop/build/rounds-merged-counts-rounds --
  scripts/check-publishing-quota.mjs` prints NOTHING (exit 0). The script is
  byte-identical: `git ls-tree` shows blob e6553d1b881bacd2c536724a28a824a7761f10f6,
  mode 100755, on both refs — the mode flip from the first ship is gone too.
- The guard actually degrades. I captured `git rev-parse origin/main` →
  6ec241d8489d0313a12fb8ff195081dfdc8befb1, deleted the ref
  (`git update-ref -d refs/remotes/origin/main`), and ran
  `node scripts/check-publishing-quota.mjs`:

      WARN  origin/main is not in this checkout — the diff-aware comparison is skipped this run
      ok    9 posts; no origin/main in this checkout, so no change was compared
      EXIT:0

  No FAIL, exit 0 — exactly the PR #83 behavior, not the
  "fatal: invalid object name 'origin/main'" / FAIL / exit 1 the reject
  session measured on the reverted head. Restored the ref from the captured
  sha; `git rev-parse origin/main` → 6ec241d8… again, `git status
  --porcelain` clean, and the check is green with the ref present (`ok 9
  posts; day cap 1, week cap 3…`, exit 0).

## Finding 2 fixed — docket item restored byte-identical

- `git ls-tree -r --name-only origin/loop/build/rounds-merged-counts-rounds
  -- docket/open/` contains `docket/open/2026-08-15-nothing-watches-whether-the-site-deployed.md`,
  blob e9827bf2a4cc8c4a2275c1f63d0e6599c17f1dc4 — the same blob as origin/main's
  copy; `git diff origin/main origin/loop/build/rounds-merged-counts-rounds --
  docket/open/2026-08-15-nothing-watches-whether-the-site-deployed.md` prints
  nothing.
- The one other difference in `docket/open/` vs origin/main is the round's own
  work item, moved (not deleted): `2026-08-15-loop-history-count-counts-hand-built-pull-requests.md`
  now lives in `docket/done/` as a rename with the shipped note and checked
  boxes, verified by the first review and unchanged here.

## The record now tells the truth

- `git diff origin/main origin/loop/build/rounds-merged-counts-rounds --
  CHANGELOG.md` is purely additive: 0 removed lines, 127 added, so no earlier
  entry was rewritten. The round-126 entry now states plainly that the first
  ship carried the quota-script reversion and the docket-item deletion, that
  the review session rejected both, and that this correction restores them
  byte-identical ("…restores the script byte-identical to origin/main,
  restores the docket item to docket/open/, and changes nothing else — see
  item 2"). `Origin: delegated`, `Track: build`, `Agent: opencode
  (deepseek-v4-flash)` all present. The correction's own red-green (ref
  deleted → WARN/ok, ref restored → green) is recorded honestly in the entry,
  and my reproduction above matches it line for line.
- The reject artifact `docket/reviews/23d2e611233a8b2996073195e59503c6fd6c2bcc.md`
  exists on the branch (141 lines, header intact, content matches what I read
  at a479358) and was not modified by the correction commit.

## Core work holds on the corrected head

- `node scripts/check-loop-history-snapshot.mjs` passes on the corrected head:
  "ok loop-history snapshot well-formed, within the 30-day window", "ok
  snapshot matches the live API over 3 completed run(s) as of
  2026-08-15T18:46:41.179Z", "ok rounds_merged matches the changelog: 125
  round entries as of 2026-08-15T18:46:41.179Z", exit 0.
- One red-direction I ran myself on the corrected head: backed up
  `app/lib/loop-history.json`, hand-set `rounds_merged` to 124, ran the
  checker → "FAIL rounds_merged: snapshot says 124, the changelog has 125
  round entries as of 2026-08-15T18:46:41.179Z", exit 1; restored the file
  (backup at C:/Users/BadBitch/AppData/Local/Temp/opencode/loop-history-review-backup2.json),
  `git status --porcelain` clean, check green again.
- The counts reproduce on the corrected head: `countRoundsAsOf('2026-08-15T18:46:41.179Z')`
  → 125, `countRoundsAsOf('2026-08-15T12:26:57.365Z')` → 119.
- `node scripts/round.mjs check` ran in full on the corrected head: lint ok,
  docket valid, track scope for loop/build/rounds-merged-counts-rounds ok,
  production build ok, all route checks ok, no group skipped, exit 0. (I ran
  it rather than trusting the earlier verification, because the correction
  touched the changelog entry and the docket — the two things the entry's
  own gates judge.)
- PR #84: OPEN, MERGEABLE, headRefOid b307146d…. Checks at review time:
  human-owned-paths pass, review-artifact fail (expected — it waits for this
  artifact), build-and-audit pending.

## Non-blocking

- The entry's item-2 "Hypothesis" sentence is a fragment ("the round's own
  unmerged entry, so adding to it is a correction rather than a rewrite of a
  past record") — a prose awkwardness in an otherwise exact correction
  account.
- `Origin: delegated` against a start prompt that hardcodes `supervised`:
  established pattern, not adjudicated here (same note as the previous
  review).
- The docket/open/ list diff vs origin/main moves the round's work item to
  done/ — that is the legitimate transition for the item a round works from;
  noting it so a reader of the diff is not surprised.
- `build-and-audit` was still pending when I checked; the branch's own
  `round.mjs check` covers the same ground locally and passed.

## What I could not check

- Vercel's actual production checkout (no access) — the guard's degradation
  is proved working in-repo; that the guard is what runs there rests on the
  prebuild wiring, which is unchanged from PR #83.
- Nothing else: the correction touched only the quota script, the docket
  item, and the changelog entry, and I verified each by reproduction.

## Verdict

`approve`. Both reject findings are fixed by reproduction — the quota script
is byte-identical to origin/main (mode 100755 included) and degrades to
WARN/ok with the ref absent, the docket item is restored byte-identical, the
entry records the whole arc, and nothing earlier was rewritten. The core
counting work holds on the corrected head (snapshot check green, my own
red-direction red, counts 125/119 reproduce, full `round.mjs check` green,
no group skipped). The correction fixed what was asked and introduced
nothing new.
