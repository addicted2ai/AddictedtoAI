Commit: c373d68d028368319e6ff4b82123729cec1f6091
Verdict: approve
Reviewer: opencode (deepseek-v4-flash)
Round: 125

Review of round 125 (maintain), branch loop/maintain/meta-llama-recheck-sweep, PR #82, head c373d68d028368319e6ff4b82123729cec1f6091. All claims were checked by running commands, not by reading prose.

## 1. Meta (Llama) row stays honestly unverified — verified

`app/lib/retirement-commitments.js` lines 120-129: `shape: "unverified"`, `verified: null`, `unverifiedSince: "2026-08-15"`. The diff changes only the sentence; `unverifiedSince` was already "2026-08-15" from round 124 (also run 2026-08-15), so the record is dated today, not backdated. Verified by `git show 9ec8fe1:app/lib/retirement-commitments.js`.

The sentence's specific claims were reproduced this session:

- Redirect block: `curl -L -A "<Chrome UA>" -H "Accept: ..." -H "Accept-Language: ..." -H "Accept-Encoding: ..." https://www.llama.com/docs/` → HTTP 400 at redirect end developer.meta.com/ai/docs/overview/ (I measured 71,107 bytes vs the entry's 71,442; same qualitative result, exact byte count depends on the round's exact header set). Same URL with a plain curl UA → HTTP 200, 298,267 bytes vs the entry's 298,266 (one byte off — effectively exact); body is a React bootstrap shell, title "Developer Docs & Resources | Meta", no docs text.
- archive.org CDX `url=developer.meta.com/ai/docs&matchType=prefix&fl=original&filter=statuscode:200` → **39 unique URLs — exact match with the entry**. None is a lifecycle/deprecation/retirement/EOL page; the closest is https://developer.meta.com/ai/docs/deployment/versioning/, whose archived snapshot (20260710235427) title is exactly "Versioning, updates and migration | Deployment guides".
- archive.org CDX over dev.meta.ai/docs* → I enumerated 196-313 unique URLs depending on filters (the entry says 135; my set is a superset taken hours later, consistent with ongoing crawling), and **none is a lifecycle or deprecation page** in any variant. The substantive claim holds on a larger sample.
- `curl -s -L https://dev.meta.ai/docs` → body contains "Not Logged In"; the claim is phrased as this run's observation ("now answer HTTP 200 ... they moved behind a login since"), not as a universal.
- llms.txt roots, browser UA: llama.com/llms.txt and www.llama.com/llms.txt → HTTP 400 into the developer.meta.com/ai/llms.txt/ shell; ai.meta.com/llms.txt and ai.meta.com/llama/llms.txt → 400; ai.developer.meta.com/llms.txt → 404; dev.meta.ai/llms.txt → 404; dev.meta.ai/docs/llms.txt → 500; www.meta.com/llms.txt → 400; developer.meta.com/horizon/llmstxt/documentation/ → 200, 0 bytes. All match the entry. (With a plain curl UA the 400s come back 404 and dev.meta.ai/docs/llms.txt comes back 200 — the entry's numbers reproduce exactly under the browser UA its own phrasing implies.)
- developer.meta.com/llms.txt (plain curl UA, following redirects) → HTTP 200, exactly 1,769 bytes, contains "ai.developer.meta.com" — exact match with the entry.

## 2. The 10 Directory entries were re-fetched, not just re-dated — verified

- Exactly 10 entries were `verified: "2026-08-10"` on origin/main (`git grep`), all 10 changed to "2026-08-15" on HEAD; none remains 2026-08-10, and the diff changes no other date — no date bumped without being named, none named but missed.
- The diff to `app/lib/tool-categories.js` contains **only** `verified:` date changes — no description rewrites hiding a fact change (`git diff | grep` for non-verified +/- lines returned nothing).
- The 10 hrefs are claude.com, huggingface.co/chat, github.com/features/copilot, www.cursor.com, ollama.com, runway.com, elevenlabs.io, suno.com, zapier.com, n8n.io. Spot-fetched four (claude.com, runway.com, n8n.io, huggingface.co/chat) → all HTTP 200. No date renewed on a dead page in the sampled set.

## 3. Producing-round bump — verified

origin/main had `/what-vendors-promise: 124` and `/directory: 106`; HEAD has 125 for both (`git show origin/main:...page-origins.js` vs the working file). Round 125 genuinely rewrote the Meta row's sentence in retirement-commitments.js (a listed source file of /what-vendors-promise) and renewed 10 verified dates in tool-categories.js (a listed source file of /directory), so "the newest change is this round's" follows the rule the file already documented. The rationale comments match the diff.

## 4. Track scope — verified

`node scripts/check-track-scope.mjs origin/main loop/maintain/meta-llama-recheck-sweep` → "ok all 4 changed file(s) within maintain's scope" (CHANGELOG.md, app/lib/page-origins.js, app/lib/retirement-commitments.js, app/lib/tool-categories.js). No human-owned path touched (no CHARTER.md, .github/, prompts/, scripts/check-track-scope.mjs, policy.yml).

## 5. The record — verified

Changelog entry (2026-08-15) has Origin: delegated, Track: maintain, Agent: opencode (deepseek-v4-flash), Guardrails and Result fields, matching the format in prompts/shared/every-run.md. It is a new entry, not a rewrite of round 124's.

## 6. Checks — verified, outputs match the Guardrails claims exactly

- `node scripts/check-tool-staleness.mjs` → "ok 19 Directory tools and 11 retirement-commitment rows judged within the 45-day window (1 unverified, recorded)" plus the expected WARN on Meta's unverified record, exit 0. The unverified record is not past its window (2026-08-15 + 45 days = 2026-09-29, as the entry says).
- `node scripts/check-retirement-staleness.mjs` → "ok 87 retirement-calendar rows verified within the 30-day window" with the standing policy.yml WARN, exit 0.
- `node scripts/preflight.mjs` → "ok preflight clear", exit 0.
- `node scripts/check-one-limit-count.mjs` → "ok sweep output is internally consistent: count 8, 8 set member(s), swept 2026-08-15T09:20:06.810Z, 0 day(s) old", exit 0.
- `npm run lint` → no errors. `npm run build` → exit 0.
- `node scripts/round.mjs check` → lint, docket valid, track scope, build, and route checks all ok, no group skipped, exit 0.

## Discrepancies found (none falsifying)

- The entry's 71,442-byte figure for the HTTP 400 body measured 71,107 bytes in my reproduction (header-set dependent; same status, same redirect end).
- The dev.meta.ai/docs* archive.org count (135) does not re-derive: I enumerate 196-313 unique URLs depending on filters. The direction is a superset taken later, consistent with the archive growing; the claim that matters — no lifecycle/deprecation page exists in the archived docs tree — holds on my larger sample.
- With a plain curl User-Agent several Meta llms.txt roots answer 404/200 rather than the entry's 400/500; the entry's numbers reproduce exactly with a browser User-Agent.

None of these makes any published claim false or uncheckable; the substantive findings all reproduce. Approve.
