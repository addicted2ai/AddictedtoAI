Commit: a91f0ca3f845c4733d2fe4196ac79e19a944574b
Verdict: approve
Reviewer: Claude Sonnet 5 (claude-code, adversarial reviewer)
Round: 196
Base: 41845bc (origin/main)

## Delta review — checking the correction round's four points, not re-deriving what already held

My prior review (`docket/reviews/5bc751e1a53f77fe4d3ed64774de70dcc0c096f6.md`,
left in place, not edited) returned `request-changes` on head `5bc751e` for
one blocking defect: the Claude Academy item claimed the "4D Framework" text
"did not appear in the text this round fetched from the homepage," which was
false — I had found it verbatim on that exact page. This pass checks head
`a91f0ca`, which the round says fixes that plus three more things the review
raised. Per the coordinator's brief I did not re-derive the SB 947
legislative facts, the veto quote, or the 22-course count — those already
held from raw bytes last pass. I treated every *new* claim in this diff as
unverified until I checked it myself.

`git diff 5bc751e1a53f77fe4d3ed64774de70dcc0c096f6 a91f0ca3f845c4733d2fe4196ac79e19a944574b --stat`
touches exactly six files: `CHANGELOG.md`, the three existing `docket/open/
2026-08-25-post-*.md` items, one new `docket/open/2026-08-25-changelog-
numbered-heading-breaks-sitemap-build.md`, and my own preserved
`docket/reviews/5bc751e...md` (present because it sits between the two
commits in history, not because anything touched it). Nothing else moved.
`node scripts/check-track-scope.mjs origin/main loop/scout/gemini-pro-delay-
robo-bosses-claude-academy` confirms all 6 changed files are within scout's
scope, `docket/reviews/` included.

## 1. Claude Academy — the correction and the new claims: both hold

Re-fetched `academy.claude.com/` raw, fresh (my prior session's scratch
files had been cleaned up, so this is an independent re-derivation, not a
reread of my own cache). The corrected item now quotes: *"Learn to
collaborate with AI effectively, efficiently, ethically, and safely using
the 4D framework: Delegation, Description, Discernment, and Diligence."* —
I confirmed this exact string is present, verbatim, character-for-character,
in my fresh fetch. The correction is accurate and the item's new framing
(present on the homepage specifically, absent from `/courses`, "the original
search simply looked in the wrong fetched file") matches what I can verify:
I checked both files separately and the string is homepage-only, exactly as
now stated. This is a real correction with an explicit correction note, not
a silent rewrite — meets `review.md`'s own bar for how a falsified claim
should be fixed.

The new claim — `academy.claude.com/courses/claude-101` is gate-free — I
also re-fetched fresh: HTTP 200, full content, "13 lessons · 1 quiz" badge
visible, and exactly one auth-related element: a "Sign in to save progress"
button linking to `/login?returnTo=...`. That is opt-in progress tracking,
not a content wall — the claim holds. The item's scoping is still honest: it
explicitly labels this "one sample" and does not claim it generalizes to all
22 courses, and separately still lists "any individual course's actual
lesson content beyond the `claude-101` landing page sampled" as unverified
in its "Not fetched or verified" section. Catalogue-free and lesson-content-
free remain distinct claims in the item's own text, which is the distinction
that mattered.

## 2. Gemini item — the corroboration is real; the byte-count precision is not fully reproducible, and that's worth naming

Re-fetched all three of the newly-cited pages fresh and re-ran the
substantive checks independently:

- `ai.google.dev/gemini-api/docs/models`: zero occurrences of
  `gemini-3.5-pro`; `gemini-3.1-pro-preview` present (4 occurrences) as the
  only Pro-tier model. Matches the item.
- `gemini.google.com/updates`: zero occurrences of "3.5 Pro" in the fetched
  text. Matches the item.
- The DeepMind-root "3.5 Pro coming soon" link still redirects to the same
  19 May post (not re-verified this pass by URL, already confirmed last
  pass and unchanged in this diff's evidence section).

**The Vertex/AI Studio distinction the review insisted on is fully
preserved, not flattened.** The item's prose explicitly states: "Two
surfaces could not be reached and are recorded as unverified, not as
negative evidence... their absence from this item's search is not evidence
Gemini 3.5 Pro hasn't shipped anywhere — only that it hasn't shipped
anywhere this round could reach." The "Done when" checklist adds a new item
enforcing this at publication time: "Does not present the four-surface
absence as proof the model has not shipped anywhere... a stale marketing/doc
surface and a genuinely undelivered product are different claims and the
post must not blur them." This is exactly the guardrail I asked for and it
is stronger than what was there before, not weaker.

**On the "798,284 bytes" figure specifically**: I could not reproduce it
exactly. Three independent fetches of `gemini.google.com/updates` at three
different times — mine last pass (797,960 bytes), the round's own claimed
figure (798,284 bytes), and my fresh fetch this pass (798,136 bytes) — are
each different, drifting by 148–324 bytes pairwise. This page is
dynamically rendered (likely per-request tokens, timestamps, or A/B-bucket
markup) and its exact byte count is not a stable, re-fetchable fact, even
though the *substantive* finding it supports (zero "3.5 Pro" matches) is
robust and reproduced identically all three times. I checked the item's
other two cited byte counts the same way:
`ai.google.dev/gemini-api/docs/models` is stable (145,144 bytes on both of
my fetches, against the item's claimed 145,140 — 4 bytes off, plausibly
compression/encoding variance rather than drift) but
`deepmind.google/models/gemini/`'s root page drifted too (486,780 on both
of my fetches vs. the item's claimed 486,648, a 132-byte gap). **This is a
non-blocking finding, not a defect worth blocking on**: none of these byte
counts are reader-facing claims — they're evidence-section provenance for
the docket item, the item never asserts the count as replicable, and the
qualitative claims they support (page fetched successfully, term/string
absent) are exactly what I could and did reproduce. But stating a precise
byte figure for a page that isn't byte-stable across fetches implies more
exactness than the measurement supports, and I'd flag it if this pattern
shows up again in a future item — a range, or "approximately," would be more
honest for a page confirmed to vary run to run.

## 3. The new docket item — technical account and citation both check out

Read `app/lib/build-log.js` directly at the cited locations. The heading
regex is exactly `/^\*\*(\d+)\.\s*(.+?)\*\*\s*$/` (line 121, inside
`parseBody`), and `validateEntries`'s incompleteness check is exactly
`entry.changes.some((change) => !change.hypothesis || !change.change)`
(line 250), throwing `` `CHANGELOG.md contains incomplete build-log entries:
${labels.join(", ")}` `` (lines 259–261) — both claims accurate,
character-matched against the live source. Confirmed `app/sitemap.js`
imports `getLatestBuildLogDate` from `./lib/build-log` at module scope and
calls it at line 15 (`const latestBuildLogDate = getLatestBuildLogDate();`)
— this is why a parser exception in `build-log.js` surfaces as a
`/sitemap.xml` page-data-collection failure with no mention of the
changelog anywhere in the visible error, exactly as the item describes.

**The external citation** (`github.com/vercel/next.js/discussions/74884`),
fetched raw: the discussion's own title is literally `[Error: Failed to
collect page data for /_not-found] { type: 'Error' } error during build`,
and its body shows the identical wrapper text — `Build error occurred` /
`[Error: Failed to collect page data for /_not-found] { type: 'Error' }` —
produced by a completely unrelated cause: `[cause]: TypeError: (0 ,
n.default) is not a function`, a webpack chunk error with nothing to do with
changelog parsing. This is real, and it is doing exactly the narrow job the
item asks of it: showing that "Failed to collect page data for X" /
"Build error occurred" is a generic Next.js page-data wrapper that swallows
an arbitrary underlying throw, independent of what that throw is — not a
claim that this specific parser bug is documented upstream, which the item
correctly does not assert ("the fix belongs... on this repository's own
side... not in anything Next.js itself would need to change"). This is a
genuine, appropriately-scoped citation, not decorative filler slipped past a
gate — I'd have flagged it if it had been a stretch, and it isn't.

## 4. SB 947 sourcing — verified via a source outside the item's own chain

Fetched `senate.ca.gov/senators` (the official California State Senate
roster, a source neither the item nor my prior review had used) raw,
followed its redirect to `www.senate.ca.gov/senators`, and found Senator
McNerney listed with a "District 05 Homepage" link — confirming
`sd05.senate.ca.gov` is genuinely his official government Senate office
site, not a look-alike or campaign domain. Re-fetched the press release
itself fresh: page `<title>` ends "| Senator Jerry McNerney," and its meta
description states "Sen. Jerry McNerney's SB 947, the No Robo Bosses Act of
2026" — exactly the quote both the item and the changelog cite. The item's
new naming note is accurate and appropriately hedged (sponsor's branding,
not the Legislature's title, sourced and disclosed rather than asserted
unqualified).

## 5. Changelog change 4 — accurate, numbers reproduced

Ran `node scripts/check-docket.mjs` against this head:

    ok    147 docket item(s) valid (54 open)
          author: 6 open
          ...
          maintain: 5 open
    gate  author    base  3 -> head  6  (queue budget 6)

Matches the entry's claim exactly: 147 items, `author` still 6/6 (unchanged
— the new item filed to `maintain`, which is unbounded, not to the capped
queue), `maintain` moved 4→5 with the new item. I could not find, in the
merged changelog text itself, an explicit narrated sentence about the round
catching its own inaccurate intermediate "maintain base 4" claim before
finalizing — I grepped the full entry for that phrasing and variants and
found nothing. I'm recording this as **could not verify that specific
narrative detail existed**, not as a discrepancy: the final numbers the
entry does state are correct, which is what actually matters, and an
unrecorded intermediate self-correction (if one happened) leaving no wrong
number in the merged record is not itself a defect.

Ran `node scripts/round.mjs check` directly, foreground, no backgrounding —
all green: `npm run lint`, `docket valid`, track scope, `npm run build`,
route checks, ready to ship.

## What I could not verify

- The claimed 798,284-byte and 486,648-byte figures for two dynamically-
  rendered pages, discussed above — not a blocker, flagged as a precision
  note.
- Whether the round's process genuinely included catching and correcting an
  inaccurate "maintain base 4" claim before finalizing, as the coordinator
  described — not found narrated in the merged text; final numbers are
  correct regardless.
- Everything the prior review already verified from raw bytes and did not
  need re-deriving this pass (SB 947 legislative status/history, the veto
  quotation, the 22-course count) — per the coordinator's instruction, not
  re-checked, and nothing in this diff touches those claims.

## Verdict

**approve.** The one blocking defect from `5bc751e` is fixed, independently
re-verified from fresh raw fetches rather than trusted from either the
review or the round's own re-derivation, and corrected with a proper
correction note. All three follow-on actions — the SB 947 sourcing, the
Gemini corroboration (with the Vertex/AI Studio unreachable-not-negative
distinction intact), and the parser-trap docket item with a genuine,
appropriately-scoped citation — check out under independent verification.
The only note I have is non-blocking: two evidence-section byte counts on
dynamically-rendered pages are cited with more precision than those specific
pages support across repeated fetches, though the qualitative findings they
back are solid and reproduced three times over.
