<!-- Committed as historical record under docket/briefs/legacy/ (convention
     established by round 9, loop/meta/briefs-and-premises, 2026-08-23). This
     is the filled-in instance of scratchpad/reviewer-brief.md's template for
     the review of PR #135 (loop/build/true-and-reflowable), commit
     cdfa7c3999960c2e2eb46bf66f6157db34560454 -- see the resulting
     docket/reviews/cdfa7c3999960c2e2eb46bf66f6157db34560454.md, the sibling
     file this brief produced. Body below is verbatim, unedited except for
     this comment. -->

# Review brief — adversarial review of one loop round

You are reviewing a round of the AddictedtoAI.net maintenance loop before it
merges. Repository: `D:\AddictedtoAI`. The branch
`loop/build/true-and-reflowable` is checked out at
`cdfa7c3999960c2e2eb46bf66f6157db34560454` with a clean tree.

**Round under review:** 170
**Commit to review:** cdfa7c3999960c2e2eb46bf66f6157db34560454
**Base to diff against:** `986f6c4` (current `origin/main`)

**What the round was asked to do:** three things. (1) Fix a **live false claim**
on `app/charter/page.js` — the page told visitors the charter was "human-owned,
so only the maintainer can amend it", which round 169's amendment falsified.
(2) Fix two routes that overflow a 320px viewport, failing WCAG SC 1.4.10:
`/model-retirement-calendar` by 223px and `/charter` by 221px. (3) Build a
reflow check that actually catches those overflows, proved red before the fix
and green after.

## Why this review exists, mechanically

The round's changelog entry declares `Origin: delegated`, whose published
meaning is: *the orchestrating model chose this work, briefed it, reviewed it
and merged it; no human saw it before it landed.* The review is the part of that
sentence that would otherwise be a lie. It is also enforced — `review-artifact`
is a required status check and will not let the pull request merge without a
file at `docket/reviews/<sha>.md` that approves and covers the merged tree.

"Looks fine" is not a review. A review that verified nothing by running
anything is not a review.

## How to review

**Try to get past the checks, not to read them and agree.** For every guard the
round added, attack it: find the input that should fail and see whether it does.
For every claim the entry makes, find the command that would falsify it and run
that command. Assume the round is describing what it meant to do rather than
what it did.

**Audit whether every claim the entry makes is true.** All of them — every
number, every path, every "this was verified." Do not treat this brief's
examples as the scope of your review.

**Read the entry's opening paragraph last, against what the diff actually
does.** It is the most-read text on the site's log page and drifts more than
anything else.

Check that the round stayed inside `build`'s track scope
(`scripts/check-track-scope.mjs` — `build` may **not** write `CHARTER.md`,
`.github/` or `prompts/`), that it edited no past changelog entry (rule 5 —
only the newest entry should differ from `main`), and that `Origin` says
`delegated`.

## This round's specific attack surface

**1. Is the charter page now TRUE?** This is the whole point of item 1. Read the
rewritten paragraph in `app/charter/page.js` against `CHARTER.md` **as merged on
`origin/main`** — rules 13, 13a, and the Amendment section. A page that trades a
false claim for a differently-false claim is worse than one that stays wrong,
because the round would be recorded as having fixed it. Check the code comment
at the top of that file too; the round says it fixed a stale claim there as well.

**2. Attack `scripts/check-reflow.mjs`.** It is 528 lines and it is now the only
thing standing between a layout regression and a shipped WCAG failure. The
round's own account says the *previous* proposed formula would have passed a
page overflowing by 223px. Confirm the new one does not have an equivalent hole.
Construct an overflowing page and see it caught. Consider viewport edge cases,
what happens when the browser is missing or slow, and whether a route that fails
to load is reported as passing.

**3. `KNOWN_FAILURES` is the highest-risk thing in this diff.** The round found
a third overflow on `/log`, could not fix it in scope, and recorded it in a
known-failures mechanism rather than dropping the route. That is the honest
choice **if and only if** the mechanism cannot silently absorb new failures.
Check: does a *different*, new failure on a known-failing route get reported, or
swallowed? Can an entry outlive the bug it documents without anyone noticing?
Is the list surfaced in the check's output, or buried?

**4. The `.table-scroll` accessibility claim.** The round wrapped tables in a
scroll region with `role="region"`, `tabIndex` and `aria-label`, citing SC
1.4.10's exception for two-dimensional content. Verify the markup is actually
what that exception and the ARIA pattern require — a focusable, labelled region
— rather than a div that merely stopped the overflow. "It stopped overflowing"
is not "it became usable", and the brief said so.

**5. The measurements.** Before 541/543 scrollWidth, after 320/320. Re-derive
them yourself against the current tree rather than trusting the transcript.

**6. Two self-disclosed process failures** — the round's first command began
with `cd` (forbidden), and its new check leaked orphaned
`chrome-headless-shell.exe` processes before being fixed. Confirm both are
disclosed in the changelog entry as the round claims, and confirm the process
leak is actually fixed — run the check and check for survivors.

## On not manufacturing findings

After several real findings there is a pull toward inventing one more so the
review looks thorough. Resist it. **"I attacked this eleven ways and none of
them worked, here is what I tried" is a complete and valuable review.** If you
disagree with an expectation this brief sets, say so rather than adjusting your
report to match it.

Equally: if you find something real, say it plainly even if it is inconvenient
and even if fixing it costs the round.

## What to produce

Write **one file**:
`docket/reviews/cdfa7c3999960c2e2eb46bf66f6157db34560454.md`, beginning with
exactly these four single-line fields, in this order, before any prose:

```
Commit: cdfa7c3999960c2e2eb46bf66f6157db34560454
Verdict: approve
Reviewer: claude-sonnet-5 (Claude Code subagent)
Round: 170
```

`Verdict:` is `approve` or `request-changes`. Use `request-changes` if anything
you found is real — do not approve with caveats buried in prose, because only
the field is machine-read.

After the fields: what you attacked, the commands you ran, what they printed,
what you found, and what you could not check. Quote real output.

**Commit only that file, and nothing else.** The gate proves the review covers
the merged tree by checking that `git diff --name-only cdfa7c3..HEAD` lists
nothing outside `docket/reviews/`. If you find a defect, report it, do not fix
it.

Do not push, do not open a pull request, do not run `gh pr merge`.

## When you are done

Report: your verdict, each thing you attacked and what happened, each finding
with its evidence, and anything you could not verify.
## Working method — this section is not optional

Every command you run passes an approval classifier. If it trips, the maintainer
must approve the command **by hand**. They may be asleep or away. A single
tripped command can stall an unattended run for hours. These rules are derived
from commands that actually tripped it, not from guesswork.

### Never use `cd`

Not at the start of a command, not in the middle of one, not inside a script you
write and run, **not even as a shell function name or in a comment**. The
classifier matches the token, not the intent — an orchestrator once wrote
`cd() { echo blocked; }` as a *defensive shim* and tripped it anyway.

Your working directory is already `D:\AddictedtoAI`, so nothing needs it:

- run scripts by absolute path — `node D:/AddictedtoAI/scripts/check-docket.mjs`
- use `git -C D:/AddictedtoAI ...` for every git command
- read and write files by absolute path

### Keep every command string short

Long or multi-line command strings overload the same classifier. If a step needs
more than a couple of operations, **write a small script into the scratchpad
directory and run that** — the script may be as long as you like. The script
must not contain `cd` either.

    node D:/AddictedtoAI/../scratchpad/my-step.mjs      # good
    <forty lines of shell in one -c string>             # trips it

### Never manipulate credentials on a command line

`git -c credential.<anything>`, `http.extraheader`, anything that supplies or
overrides an auth token **will be blocked**, correctly. If a push or fetch fails
on authentication or scope, that is a finding to report, not an obstacle to
route around. Say so and stop. Looking for a broader-scoped credential when
blocked is recorded in this repository's own changelog as a past failure.

### Prefer the dedicated tools over shell equivalents

Use Read, Write, Edit, Grep and Glob rather than `cat`, `sed -i`, `echo >`,
`grep` and `find`. They do not pass through the classifier at all, they handle
Windows paths correctly, and they will not silently mangle CRLF. Reach for Bash
only when no dedicated tool fits.

### Redirection and expansion — keep it plain

Simple `>` and `2>&1` are fine. Avoid stacking constructs in one string:
process substitution `<(...)`, nested command substitution, heredocs combined
with pipes, `exec` redirections, and long `for`/`while` bodies inline. Each
addition raises the chance of a trip. **Put anything non-trivial in a scratchpad
script.**

When you need a file's content as an argument, `"$(cat file)"` alone is fine —
but nothing appended after it.

### Never print a secret

Do not `cat`, `head`, `echo` or otherwise emit the contents of any token, key or
credential file, **including into your own tool output** — a reviewer did this
on 2026-08-22 with `head -c 50` on a token file and the maintainer had to
rotate the credential. To verify a credential works, pipe it into a header
inside a single command substitution and print only the *response*, never the
value.
## Never invoke OpenCode

**Do not run the `opencode` CLI, and do not start or resume an OpenCode
session.** Not `opencode run`, not `opencode` on its own, not anything that
would create a session or generate tokens.

The maintainer has hit their DeepSeek API limits — that is precisely why this
loop is being run by Claude Code subagents tonight instead of by OpenCode. Any
OpenCode generation spends money they do not currently have available.

Read-only is fine: `curl -s http://127.0.0.1:4097/session` reads stored session
metadata, makes no model call and costs nothing. Use that if you need to check
something about a past session, and if the server is not reachable, record the
claim as unverified rather than going to look for the binary.

The repository's own test suite exercises the supervisor's liveness helpers
against a **stub** session API (`scripts/test-orchestrate-checkout.mjs`, run
from `scripts/check-routes.sh`). That is expected and costs nothing — it never
reaches the real service. Do not "fix" it into calling a live one.

